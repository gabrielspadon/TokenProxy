export const UNAVAILABLE_CONTROLS = Object.freeze({
  memoryHandoffEnabled: 'No gateway project identity or handoff producer is connected. Client-reported handoffs remain separate evidence.',
  headroomLossless: 'This saved compatibility flag has no execution reader. Headroom uses its separate content-change permission.',
});
