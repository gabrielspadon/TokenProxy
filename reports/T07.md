# T07 reliability packet report

## 2026-09-12 checkpoint

- Recovered branch `audit/local-dev/tokenproxy-t07-semantics` at `14e9da4e`.
  The CommandCode Provider API and Ollama NDJSON protocol work is already in
  `3177b842`; no provider calls or production writes were made.
- `run-capability-matrix.mjs` now requires a started gateway URL and a separate
  provider-stub control URL. It sends all 36 primary fixtures to the gateway,
  checks every successful case dispatched exactly once, and checks all six
  malformed cases returned gateway 4xx without increasing the upstream count.
- Authorization and model overrides are ordinary client request values. Fixture
  headers no longer select stub outcomes. Stream assertions require endpoint
  event order, terminal cardinality, and response schema rather than a fixed
  complete event array.
- Targeted Node 24.15.0 checks passed: capability runner 1 test; protocol,
  Ollama, and CommandCode contracts 16 tests across 5 files.

## Integration gate

T09/root must call the runner against the started TokenProxy gateway and its
separately started stub, for example:

```bash
node tests/contracts/run-capability-matrix.mjs \
  --gateway-base-url=http://127.0.0.1:<gateway-port> \
  --provider-control-url=http://127.0.0.1:20210/__tokenproxy_fixture/control \
  --authorization='Bearer <fixture-client-key>' \
  --model=<fixture-routed-model>
```

That started-product check remains unrun here because root owns the gateway
configuration and process setup.
