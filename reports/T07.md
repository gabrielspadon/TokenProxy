# T07 reliability packet report

## 2026-09-12 Git-bound started-Next matrix receipt

The former receipts are tainted and excluded. The current immutable receipt is
[T07-matrix-receipt.json](./T07-matrix-receipt.json), SHA-256
`9d3d5d033173ae9ef23d4e629475b756eb982b603ae6b160c06145d96af7990f`.
It was generated from Git `ed83f35b01ebbe0add5f0673454b89ad3ce0bc62` with
the committed command below.

```bash
/home/spadon/.local/share/mise/installs/node/24.15.0/bin/node \
  tests/contracts/run-started-capability-matrix.mjs
```

The runner records its exact child CLI command, dynamically chosen loopback
gateway port, separate provider-control origin, Node version and Git SHA. It
uses an environment-variable authorization source and never writes a bearer
credential to argv.

The new real Next run passed all 36 primary cells. It observed 39 Next rewrite
responses, 30 exact-once provider dispatches, six local 400 responses with
unchanged provider ingress and dispatch, and 33 total upstream ingress and
dispatches including three controlled outcome cases. Provider errors were
exactly `529/server_error/internal_server_error`; abrupt transport was exactly
`502/server_error/bad_gateway`.

Every dispatched fixture label has a provider-ingress redacted semantic shape
and SHA-256 digest in the receipt. The runner validates every system-text hash,
message role, text atom, declared tool, tool-call ID and arguments, tool-result
ID and content, reasoning atom, and image payload hash. It retains no prompt,
tool-result or media content. Responses trailing reasoning now crosses the
OpenAI boundary on an empty assistant continuity message rather than being
dropped by the OpenAI formatter.

The started fixture passes a relative `NEXT_DIST_DIR` whose resolved directory
is an owned `.tokenproxy-capability-next-*` path. Its cleanup receipt proves
both fixture data and build output are absent. It signals only the validated
child process group, and validates PID, start time, PGID, cgroup and TCP
listener before each signal. The unrelated `next-server` cleanup regression
passes. Fifteen legacy absolute-dist fixture orphans, including the 112 MB
known orphan, were layout-validated and removed without signaling a process.

Node `v24.15.0` focused validation passed six tests across the provider-stub
gateway matrix, semantic receipts, cleanup isolation, stream protocol and
trailing Responses reasoning. The fresh committed-run matrix is recorded in
the receipt above.
