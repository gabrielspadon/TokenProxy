# Capacity and Economics completion receipt

This receipt covers uncommitted source in `/Users/gabrielspadon/Documents/ChatGPT/Token Router/tokenproxy-current`, based on `4c5b584b`. The September 7 completion brief, `WORKSPACE-CONTRACT.md`, `IMPLEMENTATION-20260907.md` and `CONTRIBUTING.md` govern this lane. The existing light Plex composition and installed Mantine 9.6, ECharts and TanStack Table 9 packages are retained. Context7 quota was unavailable; current official Mantine documentation and installed TanStack skill/API documentation supplied the implementation reference. No dependency was added.

Current integration outcomes are recorded in the final section below; earlier test and rerun statements retain their historical scope.

## Requirement and evidence map

- Exact account capacity and unknown evidence are owned by `src/app/dashboard/page.js`, `AccountPolicyEvidence.js`, `capacityControlsModel.js` and `QuotaEvidence.js`. `/api/admin/health`, `/api/admin/drain?all=true`, account details, settings, qualification, eligibility and retained-pin reads keep stored health, enabled state, drain, concurrent streams, provider ceilings, model support, observed validation and current local admission separate. Missing quota and healthy status do not establish entitlement or eligibility. Unobserved usage remains unknown rather than zero. Each quota window exposes its age and source separately. Pins are routing continuity records rather than concurrent streams. These views are passive; exact connection management links retain the account ID.
- Model support uses an exact JSON provider/model pair as selector identity. Duplicate model names cannot select the first matching provider. Recent activity projects both requested model and recorded model from `requestStats`; it does not infer a requested model from the served model. Economics labels a served model only for stored success/ok status. Failed, pending, aborted, cancelled and unknown status retain the recorded attempt identity and explicitly do not establish successful service. `usageRepo.getActiveSessions()` now retains stored `connectionId`, or null when absent, supporting exact account scope in the Sessions lane.
- Account comparison and reviewed changes are owned by `CapacityControls.js` and `capacityControlsModel.js`. Comparison retains up to four explicit account IDs. A review names every account and its active-stream count, distinguishes enabled state, discloses separate saves, and explains reversal and effects on future selection, retained pins and active streams. POST `/api/admin/drain/{id}` and DELETE with `ifMatch` change only the reviewed account's durable drain state. Each response is followed by a fresh full drain read; identity, state and version must all agree before confirmation. Stale/refused/uncertain outcomes remain per account, prior successes remain visible, and uncertain mutations are not retried. Actual UI persistence is exercised by the lead-owned disposable browser journey described below.
- Quota history identity is owned by `QuotaHistoryWorkbench.js`, `quotaWorkbenchModel.js` and the existing `quotaWorkbenchQueries.mjs`/`quotaTrend.mjs`. `/api/admin/quota/workbench` retains account, scope, duration, resource, unit, measurement and observation-kind identity through a 64-hex series ID. Same-scope requests, USD and percentage histories remain separate. Shared selection now stores the exact `windowId` through the lead-owned workspace/investigation state. A missing retained series shows unavailable evidence and never substitutes another history or forecast.
- Reset checks are filtered by the chosen account and reported scope. Their retained schema does not distinguish resource or unit, and the UI explicitly discloses that scope-only correspondence. A reset timestamp is a deadline, not proof that replenishment occurred. History shows observation points, measured coverage and explicit increase boundaries. Chart zoom filters contributing rows; point selection preserves exact observation identity. No samples or interpolated monitoring history are fabricated.
- Forecast/scenario presentation uses the existing source-shared `analyzeQuotaSeries` and `projectQuotaScenario`. It exposes median observed consumption, the current segment and sample span, balance time, age at range end, cadence-derived staleness and recorded reset. Missing/unknown/stale/sparse/conflicting/flat evidence refuses an exhaustion claim. Scenarios accept a local 0.1–10 workload multiplier and account exclusion, disclose fixed request mix/cache/charging assumptions and no simulated redistribution, and perform no mutation. Sensitivity bounds are observed-rate ranges, never confidence intervals. A projected deadline before the range end requires a fresh observation; projection beyond a reset never assumes replenishment. Units remain native balance units/hour or percentage points/hour.
- Exact quota export is owned by `evidenceQueries.mjs`. Account selections with `windowId` export the chosen historical series under `quotaHistory`, including the requested shared time range and source coverage. Current account/quota snapshots remain separately described as current configuration with unknown absolute units. A missing selected series yields an empty historical array with an explicit selection manifest and unavailable reason. A model filter is not misrepresented as attribution of account-level quota history. Truncated history is refused instead of silently exporting a partial forecast.
- Economics attribution is owned by `usageRepo.js`, `economicsLinks.mjs`, `activityQueries.mjs` and `evidenceQueries.mjs`. `/api/analytics?view=economics` and selected evidence export use exact recorded IDs. A server-generated UUID identifies each final completion and is retained as `completionId` in both the usage and counterfactual ledgers. Schema 21 adds nullable unique indexes on those bindings. Client-controlled `rid` prefixes and legacy `meta.costLedgerId` are neither binding evidence nor public identity fields. A failed ledger write cannot attach an older row with the same `rid`; historical rows remain unbound without backfill. Request enrichment now requires agreement on provider, model and account as well as logical request, explicit context session and attempt where these identities were retained. Conflicts leave request-side links unavailable. Missing historical identities are never reconstructed from time or labels. Requested and recorded provider/model evidence remain separate.
- Cost provenance and drilldown are owned by `EconomicsLens.js` and `EconomicsEvidence.js`. The ledger labels the chosen recorded USD, application estimate and upstream report separately, preferring an upstream USD report when present without summing both. Explicit upstream zero is distinguishable from ambiguous historical zero. Captured rates use USD per million tokens; component totals and signed discrepancy remain visible. Missing quantities are not silently priced as zero. Token categories, modeled dollars, recorded dollars, milliseconds and body bytes are separate. Task-success cost, subscription payments and confirmed invoice charges are not inferred from request success. Exact request/session/project-reference drilldowns and same-row cost/latency denominators remain available.
- Counterfactual evidence is owned by `counterfactualEvidence.mjs` and `EconomicsEvidence.js`. It requires one uniquely mapped server completion UUID, compatible identity, provider-reported usage and matching safe nonnegative integer input/output/cache components with cache read plus write no greater than cache-inclusive input, plus reconciling finite arithmetic. Indexed lookups run in batches of at most 400 selected UUIDs; grouped cardinality checks also refuse duplicates in imported databases whose unique indexes failed. Legacy rid-only links, ambiguous bindings, conflicts, unretained records and inconsistent usage refuse attribution. Exports contain the UUID rather than the client rid or private session identifier. The UI retains signed baseline-minus-usage-model USD, including negatives. Historical zero/zero attribution splits remain unavailable. It names the pre-processing serialized UTF-16-code-unit/4 baseline estimator, same-output assumption, separate calculator and missing baseline token/rate snapshot. It never adds this modeled difference to completion cost or labels it verified savings. Export whitelists the evidence without exposing the legacy session identifier.
- Inspection preserves the surrounding Economics cohort population. The inspector provides a distinct `Filter contributing ledger to this cohort` action; unassigned accounts use a precise missing-connection predicate when supported. Shared restored record IDs highlight the exact ledger row. Capacity, inspected Economics records and compared Economics cohorts preserve their order during live quantity updates, and explicit scope/sort changes release the prior order. Clearing comparison releases comparison-only order retention. Server ledger sorting applies to the whole contributing population, not only the displayed page.

## Deterministic fixture and actual persistence journeys

`tests/e2e/capacity-economics-seed.mjs` exports `CAPACITY_ECONOMICS_FIXTURE` and `seedCapacityEconomics(db, { fixtureKind })`. It takes an already-open caller-verified synthetic adapter, opens no database and makes no network call. It requires explicit synthetic-fixture designation and credentialless `capacity-fixture-a` and `capacity-fixture-b`, retaining those exact account IDs. Fixture version is `capacity-economics-v1`, captured at `2026-09-07T12:00:00.000Z`. The lead applied it to the isolated private runtime and holds the exact `completion-fixture-receipt.json`.

The seed retains 49 observations, four current quota windows and five reset-check events. It includes same-scope requests/USD series, percentage series, observed balance increase after a reset, stale evidence and unknown balance. Eight completion records include immutable synthetic rates, explicit upstream zero, unavailable rates, exact additional attempts, unbound historical counterfactual identity, negative modeled difference and conflicting request identity. Synthetic values are fixture evidence only and never described as observed production usage.

`tests/e2e/capacity-economics-completion-bindings.mjs` provides `bindCapacityEconomicsFixtureCompletions(db, { fixtureKind, usageIds })` for a caller-owned disposable database after schema migration. It requires the original eight numeric usage IDs from the fixture receipt and validates exact request/cost IDs, timestamps, provider, model, account, attempt, token quantities and legacy mappings before any update. Only fixture cases 1–5 and 8 receive their declared UUIDs; cases 6 and 7 stay unbound. Its transaction binds six usage rows and three existing cost rows, is idempotent, and refuses identity or timestamp drift. At the lane checkpoint this helper had only been exercised on isolated test databases. The lead subsequently applied it to the private synthetic runtime; `completion-binding-receipt.json` records six usage bindings, three cost bindings and the two intentionally unbound legacy cases.

`tests/e2e/capacity-economics-persistence.spec.mjs` contains two non-intercepted browser journeys. It requires an explicit loopback `E2E_BASE`, `SMOKE_PASSWORD`, and `E2E_DATA_DIR` pointing to the disposable database directory containing `data.sqlite`. It checks the synthetic-preview response header before login and every API read. `capacity-economics-fixture-guard.mjs` permits temporary directories or an explicit canonical `E2E_FIXTURE_ROOT` with exact `runtime/db` layout and a non-symlinked `fixture-manifest.json`. The private-root manifest must be `operator-workspace-v2`, synthetic-local-policy, inference disabled, and list exactly the four known capacity/connection fixture IDs with their expected providers and Synthetic name prefixes. The read-only database must satisfy the same identity/name checks. Subsequent encrypted runs use only runner-supplied `E2E_FIXTURE_DB_KEY`, the backend's SHA-256 key derivation and AES-256-GCM `enc1` format. Decryption failure refuses the fixture; recursive nonempty credential fields also refuse it. Neither key material nor decoded data enters diagnostic messages, and the test never reads the private authentication file. The first journey reviews and applies a drain for `capacity-fixture-a`, checks exact API and SQLite `kv/admin.drain` persistence, reloads, reviews stopping the drain, and verifies restoration in both API and SQLite. On assertion failure it attempts only version-checked restoration of that exact fixture account. The second restores a saved USD history series across reload and opens the exact negative counterfactual record. Both attach JSON receipts and make no provider call. The lead owns running these journeys and inspection at 1440×1000, 1920×1080 and narrow widths.

## Historical focused verification and integration attempts

Deterministic tests cover the real fixture's repository projections, exact joins, rates, signed differences, unknown/refused attribution, history/export identity, quota arithmetic, per-account mutation/readback outcomes, actual rendered review/refusal controls, restored ledger highlights, live row stability and server sorting. `capacity-controls-ui.test.js` intercepts only local fetch in jsdom to verify partial success and stale refusal; it is not claimed as durable API persistence.

The final focused recipe is `CI=true RUN_REAL=0 RUN_E2E=0 npx vitest run unit/economics-lens.test.js unit/capacity-controls-ui.test.js unit/capacity-controls.test.js unit/capacity-economics-fixture.test.js unit/economics-evidence.test.js unit/counterfactual-evidence.test.js unit/quota-evidence.test.js unit/quota-workbench-ui.test.js unit/usage-repo-live-state.test.js unit/quota-workbench.test.js unit/quota-trend.test.js unit/quota-history.test.js unit/admin-capacity-api.test.js --maxWorkers=1 --testTimeout=30000` from `tests`. The final result after the two last presentation corrections at 11:47:57 local tool clock was 13 files passed, 147 tests passed, zero failures or collection errors. Production-file ESLint, test-file ESLint and `git diff --check` passed. The two actual browser journeys were discovered successfully; discovery does not establish that they passed.

At that lane checkpoint, actual browser persistence, integrated build and rendered acceptance remained with the lead. The current outcomes below supersede that pending status. Source wiring, unit tests and test discovery are not substituted for those results. This lane performed no benchmark, profiling, paid/model/sidecar call, production-state change, service restart, commit, push or deployment.

The follow-up fixture guard suite passed 13 tests covering plaintext compatibility, encrypted repeated readback, missing/wrong keys, decrypted nested credentials, manifest and stored identity/name drift, wrong canonical roots and symlink refusal. Scoped ESLint passed and both browser journeys were discovered again. The lead's actual first integrated screenshot exposed overlap after the Capacity table gained its local-gates column. The assigned CSS correction gives all eight columns explicit widths totaling 1248 px, keeps a 1248 px table minimum within the existing native scroll container, wraps gate text and headers within their cells, and clamps attempt meters. Text remains at least 13 px. Prettier and whitespace checks passed. CSS SHA-256 is `a0887db4b1844d2aab9f40542b14c3a8bfb3e31bcd87538cd770a7fe0e3302ed`; rendered acceptance of this source correction awaits the next lead-owned build and capture.

The exact-completion correction adds schema 21 UUID bindings, fail-closed cache accounting and a guarded synthetic-fixture binding helper. The final focused run at 12:09:06 local tool clock passed 8 files and 70 tests in 1.32 s, with zero failures. It exercises the real final usage writer and chat handler, colliding client rid prefixes, failed ledger writes retaining an older row, upgrade backup without identity backfill, indexed bounded lookups, malformed imported duplicates, privacy projection, invalid cache accounting and fixture-repair refusal. Scoped ESLint and `git diff --check` passed. An independent read-only reviewer returned `LGTM` after 4 files and 32 tests plus duplicate-binding probes with each unique index removed; ambiguous rows stayed unavailable and no retained rows were deleted. The lead still owns build, restart, fixture-helper invocation and browser revalidation.

## Current integrated acceptance

The consolidated [resumed acceptance ledger](IMPLEMENTATION-20260907.md#current-resumed-acceptance) owns the current source and build boundary. Earlier pending statements and CSS hashes above are historical lane checkpoints.

Both actual Capacity/Economics journeys now pass across the archived runs. `browser-completion-fourth.log` records reviewed drain persistence/restoration; `browser-completion-fifth.log` records exact saved USD history across reload and negative modeled-cost drilldown. The fifth run passed both remaining integrated failures, two tests total, not a fresh full-suite run. No ledger cost is promoted from modeled evidence to verified savings.

The fixture guard now checks the authoritative database name column and any duplicate extra-data name. Its corrected suite passed 17 tests, including encrypted/read-only safety and column-only legacy rows; `triage-fixture-actual.json` verifies all four private runtime accounts. The earlier 13-test checkpoint did not cover column-only names.

The first integrated 58-state audit recorded Capacity contrast defects and no horizontal overflow. Contrast has a source correction; final rebuilt acceptance is pending. Economics now uses the shared bounded desktop lens structure after a 1,038 px document was observed at a 1,000 px viewport. It still requires rebuilt height/heading/inspector evidence. `quota-chart-interaction.spec.mjs` adds actual point/zoom/scenario verification; its first marker selector counted the legend, and the corrected run is pending. These are the remaining domain acceptance checks, not missing repository arithmetic or cost-binding implementation.
