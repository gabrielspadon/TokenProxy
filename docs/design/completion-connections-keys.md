# Connections and access completion receipt

This domain starts from `4c5b584b` in the shared `tokenproxy-current` checkout. The September 7 operator brief and `WORKSPACE-CONTRACT.md` govern the work. Changes are local and uncommitted. Other domain owners share the checkout.

Current integration outcomes are recorded in the final section below; earlier test and rerun statements retain their historical scope.

## Composition

Connections and Network use compact status strips instead of large number cards. A stable account list retains actual provider marks and individual account identities. Connections selection opens the existing resizable dock, then links to detailed controls. Network account selection shows the stored account-to-pool-to-provider policy. Keys retain compact lifecycle disclosures and add an access-profile inventory with a selected policy inspector. Primary control and table text stays at least 13 px. White surfaces, cool gray canvas and indigo selection follow the existing light workspace.

The installed frontend-design skill was read. Context7 returned a quota-exhaustion response; official Mantine 9.6 documentation for Tabs and SegmentedControl was consulted alongside installed source. No package or second chart/query framework was added. Existing `SelectionDock`, native disclosures, confirmation dialogs and provider marks remain the shared primitives.

## Capability contracts

| Capability | Source and observable contract | Units, permission and persistence | Check |
| --- | --- | --- | --- |
| Account inventory | `src/app/dashboard/connections/page.js`; `/api/providers`, qualification and drain reads | One row per account ID. Stored configuration and observed qualification are separate. Absent qualification remains unknown. Dashboard/operator read; no probe. | Multi-account presentation fixture and selected dock assertions |
| Account control | `src/app/dashboard/connections/[id]/page.js`; provider PUT, qualification recheck, drain APIs | Priority, concurrency counts and per-window percentages retain existing API units. Existing scoped confirmations remain. Disable, drain and credential replacement remain different actions. Credentials clear on dismissal. | Isolated account fixture controls; no OAuth or upstream validation exercised |
| Model access | `AccountModelAccess.js`; `/api/models/disabled`; `disabledModelsRepo.js` | Exact model IDs, account ID and provider ID. Dashboard operator mutations. First edit copies the inherited exclusion list; subsequent edits remain account scoped. Empty account list does not restore provider inheritance. Readback verifies each local change. | Exclusion and re-enable UI-to-API journey |
| Provider support | Account inspector and service list | Registered service types describe the adapter. Neither health nor an enabled account proves model entitlement. Upstream entitlement stays unknown where no supporting observation exists. | Unknown-health presentation assertion |
| Profile CRUD | `AccessProfiles.js`; access-profile routes and `accessProfilesRepo.js` | Token ceilings, USD recorded cost ceiling, whole expiry days. Local operator permission. Settings edits mint versions; rename alone retains policy version. Optional expected version and name reject stale writes/deletes atomically. | Repository and API refusal tests; real UI competing-edit 409 journey |
| Profile adoption | Existing `KeyLifecycle.js`, profile endpoint and `keyLifecycleRepo.js` | Copies current policy onto one key. Editing/deleting profiles never silently changes copied limits. Required expiry replaces key expiry with days from adoption; no required expiry leaves the date unchanged. | UI adoption, behind-version, manual drift and deletion-release journey |
| Key listing/reveal | `/api/keys`, per-key route, reveal endpoint, Keys UI | List and detail stay redacted. Explicit reveal uses existing show-once dialog, 60-second timeout, visibility dismissal and clipboard disclosure. Only operator credentials may reveal. | Redacted API reload and no-secret-in-list checks |
| Key rotation | Existing rotation endpoint and `KeyLifecycle.js` | Explicit overlap hours, bounded by existing expiry. Successor secret is returned deliberately. Original overlap remains historical when a later expiry edit changes enforcement. | One-hour isolated rotation; changed-expiry rendering assertion |
| Key expiry | `src/app/api/keys/[id]/route.js` and Keys expiry editor | Exact ISO UTC timestamp or null. Invalid dates and normalized-overflow dates refuse with 400; invalid input cannot silently clear an expiry. Local operator required. Fresh per-key readback verifies persistence. | Set/clear, invalid calendar/input, inference/remote refusal unit tests; UI date save |
| Key limits | Existing Keys limits editor and key/budget repositories | Lifetime prompt tokens, completion tokens and recorded USD estimates; model allowlist and budget policy. Strict bounds and best-effort reservations remain distinct. | Profile adoption and manual cost-limit drift; existing budget coverage retained |
| Client setup | Existing `ClientSetup.js` and connectivity route | Configuration tier contacts nothing. Authentication tier contacts this gateway only. No inference test is offered. Endpoint copy never automatically reveals a key. | Local configuration UI check; no gateway-authentication or provider-call claim |
| Account network path | `AccountPaths.js`, `accountPath.js`, existing connection proxy resolver | Stored pool IDs, explicit direct, legacy account override and unselected/global/environment state remain distinct. Missing/disabled selected pool refuses; strictness uses the account snapshot. No URL secret or reachability inference. | Direct, unavailable, unknown-snapshot and strict/fallback unit fixtures |
| Proxy pools | Existing pool APIs and Network control surface | Strictness, active state, no-proxy hosts and URLs. Existing in-use deletion refuses. Local inventory readback verifies create/edit/delete. Network tests retain explicit side-effect disclosure. | Pool create, account bind, explicit direct, delete and restore UI journey |
| Virtual-account pool rotation | Network strategy selector; `providerStrategyPatch`; `src/sse/services/auth.js` and `connectionProxy.js` | Fixed, round-robin or random over active pools with addresses. Registry no-auth providers only in this editor. Selected pools supply their strictness. Scope review and fresh settings readback precede a success receipt. Unselected does not mean forced direct. | Isolated `edge-tts` fixed/round-robin/random/clear journey; initially absent strategy restored |
| Custom nodes | Network editor and existing provider-node APIs | Separate OpenAI and Anthropic endpoint URLs for multi-compatible nodes, optional Responses transport. Create/edit retain all transports; local inventory readback verifies values. | Three-transport create/edit/reload/delete UI journey |
| Sign-in settings | Access page, auth/settings APIs | Existing permission and lockout rules retained. Nonsecret settings are verified with fresh settings readback. Password and OIDC secret values cannot be verified by plaintext readback. No federated sign-in test runs on page load. | Existing Access refusal/secret tests; local setting checks use isolated runtime |
| Partial/interrupted outcomes | Profile, key, model policy, Network controls | Accepted but unverified state is visible. Interrupted profile/key/model/network mutations are not automatically resent. | Failed-profile-readback presentation fixture; actual stale profile refusal |

## Fixture and historical verification evidence

`tests/e2e/connections-keys-fixture.mjs` is `connections-keys-v1`, fixed at `2026-09-07T12:00:00.000Z`. The seed specifies two disabled OpenAI accounts with no API key, access token, refresh token or cookie. The integrated preview owner assigned `connection-fixture-alpha` and `connection-fixture-beta`, alongside the Capacity domain's two synthetic account IDs. Preview startup guards independently reject unknown accounts or provider credentials. All transport, inference, OAuth, provider validation and process operations remain blocked.

`tests/e2e/connections-keys-persistence.spec.mjs` exercises actual UI submissions against isolated APIs and SQLite-backed repositories. It requires an explicit loopback URL, supplied isolated password and `x-tokenproxy-preview-kind: synthetic-fixture` before sending credentials or writing. Created keys/profiles/pools/nodes are tracked by returned IDs and cleaned up explicitly. Account policy tests use the known synthetic account. Model exclusion removal intentionally leaves an explicit empty account policy, matching the API contract; it does not claim to restore absent provider inheritance. The virtual-account strategy starts absent and is removed again in cleanup. Browser interception is absent from these journeys.

`tests/e2e/connections-keys-contract.spec.mjs` is presentation/request-handling evidence only. It covers accepted-but-unverified profile state, known zero USD, long labels at 390 px, two accounts under one provider, and unknown health.

The focused command `CI=true RUN_REAL=0 RUN_E2E=0 npx vitest run unit/access-profiles.test.js unit/account-path-projection.test.js unit/keys-lifecycle-render.test.js unit/network-probe-consequence.test.js --maxWorkers=1 --testTimeout=30000 --hookTimeout=30000` passed 4 files and 39 tests. A subsequent affected-caller run also included `unit/multi-compatible-provider-nodes.test.js`, `unit/api-keys-bulk-delete-2120.test.js` and `unit/provider-proxy-config.test.js`, passing 7 files and 61 tests. The latter run emitted one nonfatal `TimeoutNaNWarning`; it had no failed or skipped tests. Scoped ESLint and `git diff --check` passed after implementation. These are functional checks, not performance measurements.

The atomic strategy patch now forwards explicit pool clears as repository deletion values, removing both the existing pool and its server-managed strictness snapshot. Bulk settings normalization remains unchanged. Four regression cases first reproduced the retained-binding defect, then passed for null, empty string, the no-selection sentinel and whitespace. The full `unit/settings-connect-timeout.test.js` run passed 63 tests, including bulk clear and fixed/round-robin/random readback through PATCH, GET and SQLite export. The final settings route, Network controls and associated test files passed scoped ESLint; `git diff --check` passed.

The integration lead owns the isolated build, browser execution, SQLite final readback and rendered acceptance at 1440×1000, 1920×1080 and 390×844. Acceptance requires passing persistence journeys and verification of rendered follow-up corrections. Existing unit success is not substituted for browser or database evidence. No production restart, real provider sign-in, remote process control, paid inference, benchmark, profiling, publication or deployment occurred.

The first integrated run passed all four domain presentation tests. Its three persistence journeys timed out. Retained snapshots show the key flow reached profile adoption and the custom-node flow reached type selection; both tests used exact implicit-label matching, which included option text. The tests now locate selects by accessible combobox name. Account actions now wait for confirmation closure and explicitly open model-access and Tuning disclosures. A 10-second action limit preserves time for cleanup and the original failure. These corrections await the lead's rerun. The actual 390-pixel review also found touching account standing labels, now separated by wrapping 6-by-10-pixel gaps, and an obsolete key-expiry limitation, now removed. Scoped ESLint and whitespace checks pass for these follow-up changes.

The next integrated run passed the complete key/profile persistence journey (2.4 seconds) and the complete multi-compatible node journey (782 milliseconds). The account journey persisted exclusions, pool binding, direct routing, all three rotation modes and strategy clear before its final delete selector matched two same-named pools. Its newly created pool was removed by cleanup. Read-only SQLite identified the older residue from the first timed-out run as `87d22bdd-33f8-439e-8482-07d6afcd1a87`, reported to the integration lead for exact-ID cleanup. The test now uses a unique pool name, matches the exact inventory name and asserts one row before deletion. The final account journey still requires rerunning; reported durations are test-run observations only.

The integration lead subsequently removed that older pool by exact ID. A later account rerun exposed a real detail-page discrepancy when a selected pool coexisted with a retained direct marker. The authoritative resolver prioritizes the selected pool; clearing only the binding restores the retained account policy. The connection detail page now reuses `accountPath` to match the other account inspectors and the resolver. A rendered regression failed before the fix, and a local API/SQLite/resolver regression confirms the direct-to-pool-to-clear transition without contacting a provider. The scoped three-file suite passed 19 tests after the fix. Integrated rendered acceptance requires the lead's rebuilt-preview rerun.

## Scope reconciliation

Network's provider-strategy description now names virtual providers with no credentials, matching the scope read by `src/sse/services/auth.js`. Stored credentialed accounts do not inherit that strategy. The earlier claim that proxy rotation was unsupported was disproven by that runtime and replaced with the existing fixed/round-robin/random selector. Connections labels absent qualification timestamps “Not recorded.” Client setup describes an upstream generation request as potentially billed, instead of claiming every successful upstream request is billed.

External provider entitlement and real remote reachability remain unknown by design when the corresponding source does not report them. Production execution is a separate authorization and evidence boundary.

## Current integrated acceptance

The consolidated [resumed acceptance ledger](IMPLEMENTATION-20260907.md#current-resumed-acceptance) supersedes the earlier pending rerun statements. `browser-completion-fourth.log` records all three actual domain journeys passing through the rebuilt isolated application. This closes key/profile lifecycle, account exclusion/pool/direct/rotation/clear restoration, and multi-compatible node create/edit/reload/delete. The earlier direct-marker/pool discrepancy has a source fix and passing actual persistence evidence. The four intercepted contract journeys also passed and remain presentation/request-handling evidence.

The 58-state all-route audit found no horizontal overflow, browser errors or unexpected API failures for this domain. It does not finish visual acceptance. Rendered review still requires the Connections closed-inspector blank-space correction, followed by final captures.

Full deterministic and focused test counts, the final source freeze, and the pending rebuilt audit are recorded in the central ledger. Local passing journeys do not establish OAuth, provider entitlement, real remote connectivity or production readiness.

## Current supporting redesign verification

Connections now shares the exact account selection with Network and the analytical workspace. Account links retain interval, provider, model and comparisons. Inventory filtering applies provider and account scope; historical interval and model remain available for analysis. Qualification unknown, unqualified, disabled and observed unhealthy are separate. Failed inventory, qualification and drain reads remain visible beside retained observations. Wide rows align account identity, qualification, observation and actions; Network uses a responsive account/path/participation comparison and the shared selection dock. Tools, Keys and System use compact summaries. Global proxy-off copy no longer implies that every account bypasses its pool. Keys points to its existing local model-permission check.

The final focused command was `CI=true RUN_REAL=0 RUN_E2E=0 npm test --prefix tests -- --run unit/connections-investigation.test.js unit/account-path-projection.test.js unit/connections-parity-ui.test.js unit/supporting-page-read-errors.test.js unit/operations-parity-contracts.test.js unit/notifications-parity-controls.test.js unit/compatibility-ui.test.js unit/system-import-ui.test.js unit/network-probe-consequence.test.js unit/client-setup-connectivity.test.js --maxWorkers=1`. It passed 10 files and 67 tests. Explicit-path ESLint and `git diff --check` passed. The existing probe test now mounts the same Mantine provider as the application and supplies the missing jsdom observers; all five disclosure/history/credential assertions remain intact.

Disposable development fixture `S5cNBg` passed all three actual key/profile, account/pool and node persistence journeys, plus the four intercepted presentation contracts. The browser uses explicit accessible labels for the expanded pool/direct/inherited selector. The fixture was stopped through its owned endpoint after verification. Earlier runtime failures were isolated-preview defects, including Next worker compilation and a SQLite guard conflict, not successful acceptance. Final emitted-artifact screenshots and interaction receipts remain separately attributed by the integration lead.

`redesign-capabilities-connections.json.currentReconciliation` preserves the original inventory and maps current entry points. Its effective counts are 109 supported/exposed, one partial, eight unavailable and three internal. The partial combined refresh capability exposes lead policy and latest refresh/expiry evidence; a complete retained refresh-event timeline is still absent from its API. Source exposure does not establish a successful external authentication or provider operation.
