# Routing, Models, Sessions and Context completion receipt

Source checkout is `/Users/gabrielspadon/Documents/ChatGPT/Token Router/tokenproxy-current`, based on `4c5b584b`. This receipt describes uncommitted local implementation. The September 7 completion brief, `WORKSPACE-CONTRACT.md`, `IMPLEMENTATION-20260907.md` and `CONTRIBUTING.md` govern the work. The referenced `open-sse/AGENTS.md` is absent in this checkout. No production service or database was changed by this lane.

Current redesign outcomes are recorded in the final section below. Earlier appearance, test and rerun statements retain their historical scope.

## Revalidation and implemented behavior

- The versioned configuration workbench already exists in `src/shared/models-policy/ModelsPolicy.js`, `PolicyEditor.js` and `PolicyHistory.js` at `/dashboard/models`. It preserves ordered members, direct aliases, member account restrictions, fallback/round-robin/fusion overrides, stored draft revisions, validation, covered diffs, activation and restoration. System should link to this workbench instead of implementing another configuration lifecycle.
- Routing simulation already uses the real `accountAdmissionReason`, `temporaryPinWait`, `planAccountSelection`, disabled-model, quota and capacity helpers. Its captured account decision is local and excludes combo execution, fusion, virtual auto routing and capability-adapter expansion. Capabilities/context-fit evidence is not misrepresented as an account gate or upstream entitlement. Draft declarations are validated but explicitly do not modify captured account ranking. No provider request is made.
- Cascade was implemented in `src/lib/stepRouter.js` and `src/sse/handlers/chat.js` without a dedicated control surface. The new Solo chat cascade tab supplies exact pair mappings, source-shared thresholds, applicability, escalation status classes, separate process-local pin lifetime, reviewed persistence, stale-revision refusal, explicit receipt and reload verification. It does not alter runtime routing semantics or the existing pin store.
- New `/api/routing-cascade` GET/PUT is guarded by `requireAdmin` before reading or parsing. PUT requires operator authentication and loopback according to the existing admin policy. The repository validates at most 64 unique non-self pairs, retains colon/slash normalization and bare direct aliases, compares the expected current hash inside the write transaction, updates only `settings.cascadePairs`, and appends an immutable receipt in `kv` scope `cascadePolicyReceipts`. Memory-backed sql.js writes are refused because this control requires durable storage. No schema migration is needed.
- Cascade scope is `global-solo-chat-cascade`. It is excluded from `routing-plans-v1` activation/restoration. Threshold is strictly below 65,536 estimated prompt tokens using four UTF-16 code units per token. Strong-model continuity lasts 1,800,000 ms from escalation in the current process when a session ID exists. Pair edits do not clear escalation pins or account affinity. The UI distinguishes those two pin stores.
- The catalog API reversed its alias storage contract in both projection and its legacy PUT. GET now projects alias-to-target mappings, including object targets and multiple aliases; PUT writes alias then target. Catalog disabled filtering now uses the real shared global disabled-model policy. The catalog confirmation callbacks now receive the current form at submission instead of the stale form captured when opening the dialog. Aliases remain directly editable through the versioned workbench.
- A submitted configuration publication closes its Apply dialog and blocks further publication until its exact active hash has been read back. Partial or interrupted publication retains its evidence and provides a read-only current-policy/receipt reconciliation action. The receipt tab is opened for inspection. No uncertain activation or restoration is automatically replayed.
- Clearing affinity previously hid its successful receipt because the selected pin disappeared. Pin receipts now remain above the list independently of pin retention. An uncertain preview/application keeps the exact attempted action ID, disables another change and offers a read-only receipt lookup. Reading ordinary history no longer reopens the timeline or discards its cursor. Selection cannot change during an outstanding mutation. Existing server-side revision, preview expiry, queued reassignment and clear-cancellation behavior remains intact.
- Recent and paginated pin history now link exact physical request IDs and the exact joined context session to Context. No request or session is joined by timestamp, label or nearest neighbor. Unknown session joins do not expose an evidence link.
- Context resolves selected attempts by their exact session/request identity even outside the currently displayed page or population. The cohort and shared time/provider/account/model filters remain intact. Missing exact evidence is disclosed without substituting another attempt. Comparison baselines use the attempt's own session ID.
- The Context recorded-control projection now includes structure measurement, epoch micro/auto, Diet, Lingua and adaptive cache TTL flags. The ordered stage labels include the corresponding byte stages. Comparison unions baseline and selected stage identities so an absent stage stays unavailable instead of disappearing. The ledger names its retained source and discloses that detailed execution/skip reasons are not retained; no missing reason or whole-pipeline total is invented.
- Context separates selected model from served model. A failed or pending attempt does not confirm service. Logical request identity, physical attempt number, requested model, three measured structure boundaries and explicit client reports remain separately inspectable. Prefix discontinuity is labeled as an observation and never asserted as compaction. No agent hierarchy is inferred.
- Sessions activity uses exact provider/model/account IDs and started-time bounds. New rows append in started-time order with stable request-ID keys. Active counts exclude completed linger rows. Summary/paused/historical/snapshot modes disclose their source limitations. The shared observation policy and `getActiveSessions` exact account projection are owned by the integration and Economics lanes.
- The old `RouteMap.js` has no import consumer in current `src`. Its dark diagram was not revived. The earlier Plex/indigo composition and 13 px floor were subsequently superseded by the September 7 Manrope/petrol redesign described below. No chart, table, query or animation framework was added.

## Units, joins and permission boundaries

Request structure is serialized UTF-8 JSON bytes. Stage change is signed after-minus-before bytes and preserves growth, unchanged zero and missing observations. Provider tokens, context estimates, client-reported tokens, cache observations, milliseconds and USD remain separate. A client report is authenticated client evidence, not provider verification. Installation-keyed fingerprints are compared only within their installation key.

Pin mutations use `/api/admin/session-pins/preview` and `/apply`, followed by the exact `/actions/{id}` receipt and refreshed list. Queued reassignment changes a later eligible selection, never an in-flight request. Clearing cancels queued reassignment and may allow the same account to be selected again. Moving account may rebuild a provider prompt cache. Account disabling, draining and credential rotation are separate controls.

Configuration APIs remain `/api/admin/configuration`, `/drafts`, `/drafts/{id}`, `/validate`, `/activate`, `/versions/{id}/rollback` and retained receipts. Only their existing covered families are restored. Cascade has its own reviewed pair-list restoration and receipts, with no claim of whole-platform rollback.

## Fixtures and isolated journeys

`tests/e2e/routing-completion-seed.mjs` exports `ROUTING_FIXTURE`, `seedRoutingCompletion`, `routingFixtureStructure` and the bounded `refreshRoutingFixtureStructures` helper. Fixture version is `routing-completion-v1`, captured at `2026-09-07T12:00:00.000Z`. The seed takes an already-open, caller-verified synthetic adapter, checks two known OpenAI fixture accounts for absent credentials, and inserts one explicit routing identity, two physical attempts for one logical request, growth/reduction/skipped stages, six structure boundaries, an explicit client compaction report and one affinity row. It opens no database and makes no outbound call.

Fixture account IDs are `connection-fixture-alpha` and `connection-fixture-beta`; model is `gpt-4o`; physical IDs are `routing-fixture-attempt-1` and `routing-fixture-attempt-2`. The integration seed returned context session 1. Structure body bytes reconcile at 16,000 before and 15,700 after the second request's stages. The targeted structure repair verifies exact fixture ownership and updates only six known structure rows.

`tests/e2e/routing-completion-persistence.spec.mjs` contains three non-intercepted journeys. They exercise reviewed draft/alias activation and version restoration, cascade save/reload/restoration, and pin exact evidence/expiry/queued reassignment/clear/cancellation/retained receipt. Every journey checks loopback and the synthetic preview response header before authentication. Reads verify persisted projections after mutation and reload. Each writes a JSON evidence attachment. They require the lead-owned disposable preview, seeded fixture and explicit `E2E_BASE`/`SMOKE_PASSWORD` environment. They do not start or restart a server.

## Historical focused verification

Focused recipe from `tests` was `CI=true RUN_REAL=0 RUN_E2E=0 npx vitest run unit/cascade-policy.test.js unit/cascade-policy-api.test.js unit/models-catalog-aliases.test.js unit/models-policy.test.js unit/session-pins-ui.test.js unit/session-pin-timeline-ui.test.js unit/context-workspace.test.js unit/context-evidence-ui.test.js unit/routing-simulator.test.js unit/routing-simulator-api.test.js unit/routing-simulator-parity.test.js unit/session-pin-controls.test.js unit/session-pin-timeline.test.js unit/configuration/versioned-routing.test.js unit/configuration/versioned-routing-api.test.js unit/configuration/versioned-routing-persistence.test.js unit/step-router.test.js --maxWorkers=2 --testTimeout=30000 --hookTimeout=30000`.

Result at `2026-09-07 11:35:50` local tool clock was 17 files passed, 222 tests passed, no failures or collection errors, 5.53 seconds. These are deterministic functional checks, not a performance benchmark. The initially changed UI expectations were updated to assert exact missing-identity refusal, selected/served distinction and closed-publication reconciliation; receipt-history refresh was corrected to preserve its open timeline. New tests verify independent durable cascade readback, stale revisions, malformed/oversized/duplicate/self pairs, normalization across 0–64 pairs, sql.js refusal, route-local auth short-circuit, and alias storage direction.

Scoped ESLint and whitespace checking passed. The integration owner runs the production build, browser specs and actual screenshot inspection at 1440×1000, 1920×1080 and 390×844, including RTL and reduced motion. Browser persistence and visual acceptance remain pending here until those actual results are recorded. This lane has not claimed them from unit tests, source wiring, screenshot filenames or a server status.

No benchmark, profiler, load campaign, model training, paid inference, sidecar operation, production mutation, service restart, commit, push or deployment occurred in this lane. No throughput/latency result or provider acceptance is claimed.

## Historical integrated browser follow-up

The first integrated browser run passed the routing draft activation/restoration journey and the independent cascade persistence journey. Exact pin-to-attempt navigation reached the ordered context stage table. The pin-control journey then stopped before any mutation because its test searched for a region while the shared inspector is an aside with the complementary role. The selector now matches the rendered role. No pin fixture cleanup was needed. Cascade receipt verification now checks the exact two new receipt IDs in newest-first order, preserving prior immutable history when the journey is rerun. These are test-only corrections; the application routing source stayed frozen.

## Historical integrated acceptance

The consolidated [resumed acceptance ledger](IMPLEMENTATION-20260907.md#current-resumed-acceptance) supersedes the earlier pending pin rerun. `browser-completion-fourth.log` records versioned routing activation/restoration and independent cascade persistence passing. `browser-completion-fifth.log` records the exact pin attempt, expiry, queued reassignment, clear and retained receipt journey passing. The final two-test run closed the remaining failures; it is not a new full browser sweep.

Shared investigation filters, account selection, comparison, reload and exact evidence export also passed through the actual isolated application in the fourth run. The original model-context persistence journey passes; the subsequent dangling `aria-controls` correction was reproduced in mounted model and saved-key inventories, then passed with the editor contracts, 13 tests total.

Current rendered acceptance still requires the Context desktop-height correction after the 1,127 px capture, plus the rebuilt inspector-reference audit. The audit now records document height, scroll and heading position, and inspector bounds, with a desktop fit gate for the three primary lenses. These changes must be observed after the final source freeze.

The original automatic-compaction trigger remains an independent unresolved harness problem. Exact model-window persistence, a 1M configured window, and a synthetic reported boundary do not establish a production trigger percentage. Retained stage reasons, when absent from the schema, remain explicitly unavailable rather than inferred.

## September 7 redesign verification

The accepted composition now uses the shared Manrope, navy navigation and petrol selection/action system. Sessions presents its pin working surface before recent activity, preserves exact account/model/request selection, and exposes its native narrow table scroller to the keyboard. Shaping uses the shared adaptive SelectionDock, grouped controls and a single-column inspector. Its 27 catalogue controls, 42 advanced profile fields, baseline gate and 15 per-plan overrides remain inspectable with actual scope, prerequisites and effective state. Inactive mechanisms are visibly unavailable, with stored compatibility values preserved.

The additional reviewed workflows are automatic routing for the three runtime classifier classes, safe legacy plan import into a versioned draft, exact-key bulk context-window edits, global shaping controls, per-plan shaping controls and service runtime settings. They use their existing execution readers, operator authentication, narrow validated field sets, stale-read checks and retained receipts where supported. An interrupted or unconfirmed write blocks replay until reconciliation. Import preserves absent plans, aliases and account restrictions; legacy capacity-adapter content requires explicit exclusion and is never imported through this surface. Routing activation/restoration excludes plan shaping, automatic rules, cascade and service runtime settings. Saving service URLs does not probe or start a sidecar. PXPIPE Stop is correctly described as unloading the current process, which may reload on a later eligible request.

`POST /api/admin/shaping/controls`, `/plans` and `/runtime` require their reviewed expected hashes and explicit applicable consent. Plan updates also check the global profile hash. Their corresponding receipt reads expose immutable local changes. `POST /api/admin/auto-routing` changes only its three classifier rules. `POST /api/model-context` compares the exact touched keys inside the transaction and preserves concurrently changed siblings; durable flush acknowledgement is required before the UI reports persistence. These local configuration writes neither authorize provider access nor prove inference quality.

The parent-built production artifact has build ID `DakYc7wCDHrFelJfIwBfI` and source hash `fcf00570ee1de8610901db84b23c364624f05bf19ba600dd437b2aa8d8113bf6`. The private `tokenproxy-redesign-vglskk` preview used isolated credentialless data and the launcher's outbound/process guard. Five actual UI journeys passed in `output/playwright/routing-redesign/persist.mjs`, with exact local receipt IDs retained in `persistence.json`. They cover plan import/activation, plan control reload, automatic-rule save/reload/restoration, bulk context-window save/reload/removal, and covered routing restoration. The existing routing persistence suite then passed all three draft, cascade and exact-pin journeys, including expiry, queued reassignment, clear cancellation and retained receipt readback. Its final `.last-run.json` reports `passed` and no failed tests. These eight journeys used actual local handlers and persistence, with no intercepted successful mutation responses.

The final focused recipe passed 31 tests across `session-pin-timeline-ui`, `session-pins-ui`, `shaping-control-inventory-ui`, `routing-controls-ui` and `plan-transfer`. Scoped ESLint passed. The timeline regression now asserts the exact account/model in the shared inspector's accessible heading while retaining selected-row and pagination checks. New mounted coverage exercises uncertain-write replay refusal, exact successful readback, preserved concurrent override keys and unavailable-control behavior. Earlier bounded API, SQLite reopen, shaping profile and projection checks retain their individual evidence; overlapping test slices are not summed here.

Read-only browser verification recorded 27 checks with zero page errors or outbound requests in `output/playwright/routing-redesign/inspection.json`. Captures were inspected for Sessions, Models, Shaping and Context windows at the recorded 390, 768 and 1440 px widths. Sessions keyboard input moved the narrow native scroller from 0 to 40 px at both 390 and 768 px; there was no page-level horizontal overflow. The selected Shaping surface at 1440×1000 fit a 1000 px document. These route checks used the isolated development preview; the parent owns comprehensive rendered production acceptance.

The final review also identified a retained automatic-rule draft that could remain stuck against an old hash after a 409. Refresh now loads the latest baseline while preserving the intended rules, closes the stale review and requires renewed before/after review. Missing refresh data does not unlock submission. Interrupted submissions remain blocked. The mounted conflict journey failed before this repair and passes afterward, including the exact new expected hash and immutable receipt readback.

The Context operator status route now explicitly requests a strict store snapshot. A missing file remains a successful empty observation. Malformed JSON, invalid entry envelopes and I/O failures throw typed errors and produce HTTP 503 without false empty entries. Existing request-path readers and writers keep their best-effort behavior. Five new tests cover actual store-to-route behavior; the final bounded sweep passed 52 tests across context status read errors, store, REST, MCP and automatic-routing controls. Scoped ESLint passed. These two final repairs postdate the production artifact above and await the parent's final immutable rebuild and conflict browser journey.

The capability matrix records 154 rows with 145 supported/exposed, two partial, four unavailable and three internal-only. Partial simulation excludes virtual auto routing, fusion and capability-adapter execution; local profile comparison is a bounded fixture/byte-integrity check. Missing runtime handoff provenance, the unused lossless flag, unretained stage reasons and unverified hierarchy remain explicit unavailable capabilities. Host-managed Lingua configuration, engine calibration state and the unmounted RouteMap remain internal-only. Exact evidence and source locations are in `redesign-capabilities-routing-context.json`.

No production service, production data, provider operation, paid inference, sidecar operation, benchmark, commit or deployment is represented by these checks. Disposable fixture data and audit receipts are retained as local test evidence; preview process ownership and shutdown are recorded separately by the launcher.
