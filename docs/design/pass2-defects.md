# Pass-1 screenshot defects, ranked (steers pass-2 per-screen slices)

Read against DIRECTION-2.md; "change N" refers to its numbered changes.

1. all slices 1440 — content column caps ~800px, right half dead — control panels plus tile grid fill width (change 2, 4)
2. all slices 1440 — no strap, no telemetry above fold beyond prose kv rows — mount strap under rail header (change 1)
3. all slices both — zero icons, nav bare text — sprite public/icons.svg per nav item and verb (change 3)
4. sessions 1440 — doc-heavy prose before any live data — tiles first, prose folds below (change 2, 6)
5. sessions 390 — prose wall, one narrow column, no chrome — tiles 2-up, details collapse (change 2)
6. usage 1440 — Totals/Response time flat kv text, day buckets invisible 1px bars — `--t-30` mono tiles plus real sparkline bars (change 2)
7. usage fa 1440 — mixed EN/FA headers mid-column — locale backfill plus literals gate
8. models 1440 — ~100-row unbroken catalog table, tiny text — tiles atop, sticky filter, grouped panels (change 2, 4)
9. shaping 1440 — 17 identical toggle rows ungrouped — panel grouping with verb rows (change 4)
10. system 1440 — Shut down/Import/Update buttons float detached from prose — bordered control panels with verb row (change 4)
11. system fa 390 — mixed EN/FA strings — locale backfill plus literals gate
12. remote 1440 — Start relay/mesh buttons detached, kv prose blocks — control panels with verb row (change 4)
13. access 1440 — Turn sign-in off floats unpanelled below essay — panel with precondition/blast-radius verb row (change 4)
14. connections 1440 — detail is kv prose, flat 9-button action row — verb row with primary plus quick options (change 4)
15. connections 390 — Configured/Enabled stack as label-number prose — tiles wrap 2-up (change 2)
16. connections fa — mirrors correctly, some EN residuals — locale backfill
17. network 1440 — Save strategy form naked full-width inputs — panel with constrained fields (change 4)
18. keys 1440 — mostly permission-model essay, one Decisions block of live data — tiles plus essay folds into details (change 6)
19. tools 1440 — no live data, three prose blocks — keep voice, panel the two unreported-gap notices, sprite icons (change 3, 6)
20. notifications 1440 — rule form unpanelled full-width inputs, event list unstyled — panel plus event chips (change 4)
21. translation 1440 — snapshot rows and console lack panel chrome, editor floats unlabelled — panel treatment only (change 4)
22. shell _callback both — bare centered card, acceptable — sprite/strap inheritance only
