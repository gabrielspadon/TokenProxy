{
  "schemaVersion": 1,
  "files": {
    "tests/auth/saml.test.js": [
      "Claims Extraction pickSamlEmail & pickSamlDisplayName",
      "formatX509Certificate normalizes Base64 strings into PEM blocks",
      "generateSamlMetadata produces valid SP XML",
      "isSamlConfigured checks required fields"
    ],
    "tests/e2e/redesign-fixtures/fixtures.test.js": [
      "redesign fixture isolation and retained arithmetic anchors dev rows to one UTC minute while preserving stable identities and relative observation ages",
      "redesign fixture isolation and retained arithmetic attributes browser fixtures to immutable runtime receipts and registers explicit faults separately",
      "redesign fixture isolation and retained arithmetic blocks fetch, sockets, DNS and subprocesses while fixing the synthetic clock",
      "redesign fixture isolation and retained arithmetic models lost activation response after a local handler and streaming interruption without dispatch",
      "redesign fixture isolation and retained arithmetic raises the navigation ceiling and leaves the clock real for a dev receipt, and rejects an unparseable one",
      "redesign fixture isolation and retained arithmetic refuses a login through context.request whose process receipt names a non-loopback target",
      "redesign fixture isolation and retained arithmetic seeds edge identities, exact budget links, expiry and partial-deletion preconditions without inventing cost evidence",
      "redesign fixture isolation and retained arithmetic seeds empty with its exact population",
      "redesign fixture isolation and retained arithmetic seeds repeatable canonical rows with credentialless accounts and separated costs",
      "redesign fixture isolation and retained arithmetic seeds single with its exact population",
      "redesign fixture isolation and retained arithmetic seeds twelve representative accounts with local eligibility and exact retained histories"
    ],
    "tests/fixtures/offline-runner/config-canary.test.js": [
      "keeps direct-config test state outside the fake production home"
    ],
    "tests/fixtures/offline-runner/environment-probe.test.js": [
      "allows only runner-owned filesystem Unix sockets",
      "blocks raw sockets and child-process egress",
      "does not inherit provider or proxy credentials",
      "uses only the runner-owned home, tmp and data roots"
    ],
    "tests/qa/context-capture-benchmark.test.js": [
      "measures the incremental default capture cost on actual mocked requests"
    ],
    "tests/qa/context-evidence-preview.test.js": [
      "creates a clearly synthetic preview through actual mocked capture and event ingestion"
    ],
    "tests/qa/gateway-performance/harness.test.js": [
      "performance receipt validity keeps intentional cancellation distinct from an upstream or protocol failure",
      "performance receipt validity rejects fast empty, truncated, reordered, duplicated and cross-request output",
      "performance receipt validity removes provider residence time without silently dropping negative paired differences",
      "runtime guard permits numeric loopback bind while denying outbound sockets, DNS and subprocesses"
    ],
    "tests/qa/platform-audit/combo-replay-boundary.test.js": [
      "independent combo replay boundary does not assume replay is safe when failure evidence is absent",
      "independent combo replay boundary does not change models after accepted empty SSE",
      "independent combo replay boundary does not change models after an uncertain transport exception",
      "independent combo replay boundary does not change models after an unsafe 429 response",
      "independent combo replay boundary does not change models after an unsafe 503 response",
      "independent combo replay boundary preserves a certified client wait without rotating to another model"
    ],
    "tests/qa/platform-audit/compactor-transactions.test.js": [
      "independent compactor transaction preservation does not mutate when preserving an early error leaves no useful prefix to compact",
      "independent compactor transaction preservation finds deep error evidence when serialization succeeds, including cycles hidden by toJSON",
      "independent compactor transaction preservation leaves cyclic or deeply nested inputs unchanged without a traversal failure",
      "independent compactor transaction preservation retains old error error evidence together with its tool call",
      "independent compactor transaction preservation retains old is_error error evidence together with its tool call",
      "independent compactor transaction preservation retains old isError error evidence together with its tool call",
      "independent compactor transaction preservation retains old status error evidence together with its tool call",
      "independent compactor transaction preservation retains the complete claude transaction across a recent-window cut",
      "independent compactor transaction preservation retains the complete gemini transaction across a recent-window cut",
      "independent compactor transaction preservation retains the complete openai transaction across a recent-window cut",
      "independent compactor transaction preservation retains the complete responses transaction across a recent-window cut",
      "independent compactor transaction preservation retains unresolved historical calls rather than summarizing away their transaction identity"
    ],
    "tests/qa/platform-audit/cross-format-preservation.test.js": [
      "independent cross-format and adversarial semantic contracts detects deliberate corruption instead of accepting parse-equivalent or anchor-losing rewrites",
      "independent cross-format and adversarial semantic contracts preserves claude current instructions, errors and complete tool transactions across both RTK/pressure subsets",
      "independent cross-format and adversarial semantic contracts preserves gemini current instructions, errors and complete tool transactions across both RTK/pressure subsets",
      "independent cross-format and adversarial semantic contracts preserves numeric lexemes, duplicate keys, escapes and Unicode over 256 seeded whitespace layouts",
      "independent cross-format and adversarial semantic contracts preserves openai current instructions, errors and complete tool transactions across both RTK/pressure subsets",
      "independent cross-format and adversarial semantic contracts preserves responses current instructions, errors and complete tool transactions across both RTK/pressure subsets",
      "independent cross-format and adversarial semantic contracts rejects malformed JSON without a partial rewrite"
    ],
    "tests/qa/platform-audit/executor-replay-boundary.test.js": [
      "independent executor POST replay boundary does not dispatch after the caller aborts during a rejected attempt",
      "independent executor POST replay boundary does not retry accepted 200 even if retry configuration mistakenly includes it",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After -5 while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After 0 while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After 1e308 while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After Infinity while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After not-a-date while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary ignores invalid or nonfuture Retry-After Wed, 01 Jan 2020 00:00:00 GMT while retaining bounded explicit-rejection retries",
      "independent executor POST replay boundary never retries an uncertain transport failure ECONNRESET after request write",
      "independent executor POST replay boundary never retries an uncertain transport failure fetch failed",
      "independent executor POST replay boundary never retries an uncertain transport failure upstream socket closed",
      "independent executor POST replay boundary retains a bounded retry for an explicit rejected HTTP response",
      "independent executor POST replay boundary returns a 429 provider deadline intact without retry or URL fallback",
      "independent executor POST replay boundary returns a 503 provider deadline intact without retry or URL fallback"
    ],
    "tests/qa/platform-audit/modality-coordinator-replay.test.js": [
      "independent embedding account replay boundary can rotate only after a certified HTTP rejection",
      "independent embedding account replay boundary does not infer replay permission from an unclassified failure status",
      "independent embedding account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent embedding account replay boundary stops after an uncertain or accepted failure",
      "independent fetch account replay boundary can rotate only after a certified HTTP rejection",
      "independent fetch account replay boundary does not infer replay permission from an unclassified failure status",
      "independent fetch account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent fetch account replay boundary stops after an uncertain or accepted failure",
      "independent image account replay boundary can rotate only after a certified HTTP rejection",
      "independent image account replay boundary does not infer replay permission from an unclassified failure status",
      "independent image account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent image account replay boundary stops after an uncertain or accepted failure",
      "independent moderation account replay boundary can rotate only after a certified HTTP rejection",
      "independent moderation account replay boundary does not infer replay permission from an unclassified failure status",
      "independent moderation account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent moderation account replay boundary stops after an uncertain or accepted failure",
      "independent ocr account replay boundary can rotate only after a certified HTTP rejection",
      "independent ocr account replay boundary does not infer replay permission from an unclassified failure status",
      "independent ocr account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent ocr account replay boundary stops after an uncertain or accepted failure",
      "independent rerank account replay boundary can rotate only after a certified HTTP rejection",
      "independent rerank account replay boundary does not infer replay permission from an unclassified failure status",
      "independent rerank account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent rerank account replay boundary stops after an uncertain or accepted failure",
      "independent search account replay boundary can rotate only after a certified HTTP rejection",
      "independent search account replay boundary does not infer replay permission from an unclassified failure status",
      "independent search account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent search account replay boundary stops after an uncertain or accepted failure",
      "independent transcription account replay boundary can rotate only after a certified HTTP rejection",
      "independent transcription account replay boundary does not infer replay permission from an unclassified failure status",
      "independent transcription account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent transcription account replay boundary stops after an uncertain or accepted failure",
      "independent tts account replay boundary can rotate only after a certified HTTP rejection",
      "independent tts account replay boundary does not infer replay permission from an unclassified failure status",
      "independent tts account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent tts account replay boundary stops after an uncertain or accepted failure",
      "independent video account replay boundary can rotate only after a certified HTTP rejection",
      "independent video account replay boundary does not infer replay permission from an unclassified failure status",
      "independent video account replay boundary preserves the pin and a usable retry delay during a transient wait",
      "independent video account replay boundary stops after an uncertain or accepted failure"
    ],
    "tests/qa/platform-audit/modality-core-replay.test.js": [
      "embedding credential refresh boundary does not refresh or resend when x-should-retry denies replay",
      "embedding credential refresh boundary does not refresh or resend when x-tokenproxy-replay-safe denies replay",
      "embedding credential refresh boundary does not return the old replayable 401 after the refreshed POST fails uncertainly",
      "embedding credential refresh boundary starts cancellation before the second dispatch without awaiting a stuck cancellation",
      "image credential refresh boundary does not refresh or resend when x-should-retry denies replay",
      "image credential refresh boundary does not refresh or resend when x-tokenproxy-replay-safe denies replay",
      "image credential refresh boundary does not return the old replayable 401 after the refreshed POST fails uncertainly",
      "image credential refresh boundary starts cancellation before the second dispatch without awaiting a stuck cancellation",
      "independent embedding core replay evidence does not certify ambiguous HTTP408",
      "independent embedding core replay evidence does not certify ambiguous HTTP409",
      "independent embedding core replay evidence does not certify ambiguous HTTP500",
      "independent embedding core replay evidence does not certify ambiguous HTTP502",
      "independent embedding core replay evidence does not certify ambiguous HTTP503",
      "independent embedding core replay evidence does not certify ambiguous HTTP504",
      "independent embedding core replay evidence does not certify an uncertain transport exception as replayable",
      "independent embedding core replay evidence marks HTTP 401 rejection as replayable",
      "independent embedding core replay evidence marks HTTP 429 rejection as replayable",
      "independent embedding core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent embedding core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent embedding core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent embedding core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "independent image core replay evidence does not certify ambiguous HTTP408",
      "independent image core replay evidence does not certify ambiguous HTTP409",
      "independent image core replay evidence does not certify ambiguous HTTP500",
      "independent image core replay evidence does not certify ambiguous HTTP502",
      "independent image core replay evidence does not certify ambiguous HTTP503",
      "independent image core replay evidence does not certify ambiguous HTTP504",
      "independent image core replay evidence does not certify an uncertain transport exception as replayable",
      "independent image core replay evidence marks HTTP 401 rejection as replayable",
      "independent image core replay evidence marks HTTP 429 rejection as replayable",
      "independent image core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent image core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent image core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent image core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "independent ocr core replay evidence does not certify ambiguous HTTP408",
      "independent ocr core replay evidence does not certify ambiguous HTTP409",
      "independent ocr core replay evidence does not certify ambiguous HTTP500",
      "independent ocr core replay evidence does not certify ambiguous HTTP502",
      "independent ocr core replay evidence does not certify ambiguous HTTP503",
      "independent ocr core replay evidence does not certify ambiguous HTTP504",
      "independent ocr core replay evidence does not certify an uncertain transport exception as replayable",
      "independent ocr core replay evidence marks HTTP 401 rejection as replayable",
      "independent ocr core replay evidence marks HTTP 429 rejection as replayable",
      "independent ocr core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent ocr core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent ocr core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent ocr core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "independent rerank core replay evidence does not certify ambiguous HTTP408",
      "independent rerank core replay evidence does not certify ambiguous HTTP409",
      "independent rerank core replay evidence does not certify ambiguous HTTP500",
      "independent rerank core replay evidence does not certify ambiguous HTTP502",
      "independent rerank core replay evidence does not certify ambiguous HTTP503",
      "independent rerank core replay evidence does not certify ambiguous HTTP504",
      "independent rerank core replay evidence does not certify an uncertain transport exception as replayable",
      "independent rerank core replay evidence marks HTTP 401 rejection as replayable",
      "independent rerank core replay evidence marks HTTP 429 rejection as replayable",
      "independent rerank core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent rerank core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent rerank core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent rerank core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "independent transcription core replay evidence does not certify ambiguous HTTP408",
      "independent transcription core replay evidence does not certify ambiguous HTTP409",
      "independent transcription core replay evidence does not certify ambiguous HTTP500",
      "independent transcription core replay evidence does not certify ambiguous HTTP502",
      "independent transcription core replay evidence does not certify ambiguous HTTP503",
      "independent transcription core replay evidence does not certify ambiguous HTTP504",
      "independent transcription core replay evidence does not certify an uncertain transport exception as replayable",
      "independent transcription core replay evidence marks HTTP 401 rejection as replayable",
      "independent transcription core replay evidence marks HTTP 429 rejection as replayable",
      "independent transcription core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent transcription core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent transcription core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent transcription core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "independent video core replay evidence does not certify ambiguous HTTP408",
      "independent video core replay evidence does not certify ambiguous HTTP409",
      "independent video core replay evidence does not certify ambiguous HTTP500",
      "independent video core replay evidence does not certify ambiguous HTTP502",
      "independent video core replay evidence does not certify ambiguous HTTP503",
      "independent video core replay evidence does not certify ambiguous HTTP504",
      "independent video core replay evidence does not certify an uncertain transport exception as replayable",
      "independent video core replay evidence marks HTTP 401 rejection as replayable",
      "independent video core replay evidence marks HTTP 429 rejection as replayable",
      "independent video core replay evidence preserves HTTP401 permission {\"x-tokenproxy-replay-safe\":\"false\"}",
      "independent video core replay evidence preserves HTTP429 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent video core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"}",
      "independent video core replay evidence preserves HTTP503 permission {\"x-tokenproxy-replay-safe\":\"true\"}",
      "rerank credential refresh boundary does not refresh or resend when x-should-retry denies replay",
      "rerank credential refresh boundary does not refresh or resend when x-tokenproxy-replay-safe denies replay",
      "rerank credential refresh boundary does not return the old replayable 401 after the refreshed POST fails uncertainly",
      "rerank credential refresh boundary starts cancellation before the second dispatch without awaiting a stuck cancellation"
    ],
    "tests/qa/platform-audit/optional-service-current-request.test.js": [
      "optional compression cannot erase the current request detects pressure-oracle violations for current, schema, transaction and signed reasoning anchors",
      "optional compression cannot erase the current request rejects Headroom current-user changes with lossy consent false",
      "optional compression cannot erase the current request rejects Headroom current-user changes with lossy consent true",
      "optional compression cannot erase the current request rejects PXPIPE loss of current-user despite explicit visual consent",
      "optional compression cannot erase the current request rejects PXPIPE loss of live-thinking despite explicit visual consent"
    ],
    "tests/qa/platform-audit/proxy-replay-boundary.test.js": [
      "independent proxy transport replay boundary honors method and URL carried by a Request object",
      "independent proxy transport replay boundary keeps existing read-only GET fallback semantics",
      "independent proxy transport replay boundary never falls through to direct transport after an uncertain POST at https://audit.proxy.individual.githubcopilot.com/chat/completions",
      "independent proxy transport replay boundary never falls through to direct transport after an uncertain POST at https://provider.invalid/generate"
    ],
    "tests/qa/platform-audit/replay-boundary.test.js": [
      "independent money boundary at the real chat coordinator does not bench or rotate an account for local admission402",
      "independent money boundary at the real chat coordinator does not bench or rotate an account for local admission503",
      "independent money boundary at the real chat coordinator does not dispatch again when the caller aborts after the first attempt",
      "independent money boundary at the real chat coordinator does not replay an accepted empty SSE response",
      "independent money boundary at the real chat coordinator does not replay an uncertain or partial 429 failure",
      "independent money boundary at the real chat coordinator does not replay an uncertain or partial 503 failure",
      "independent money boundary at the real chat coordinator does not replay an uncertain or partial 507 failure",
      "independent money boundary at the real chat coordinator fails closed when replay safety evidence is absent",
      "independent money boundary at the real chat coordinator hands a visible partial stream to the caller without account rotation",
      "independent money boundary at the real chat coordinator requires literal consent for historical user compression (1)",
      "independent money boundary at the real chat coordinator requires literal consent for historical user compression (false)",
      "independent money boundary at the real chat coordinator requires literal consent for historical user compression (false)",
      "independent money boundary at the real chat coordinator requires literal consent for historical user compression (null)",
      "independent money boundary at the real chat coordinator requires literal consent for historical user compression (true)",
      "independent money boundary at the real chat coordinator rotates after an explicit safe 429 account rejection without retrying the rejected account or changing model",
      "independent money boundary at the real chat coordinator rotates after an explicit safe 503 account rejection without retrying the rejected account or changing model",
      "independent money boundary at the real chat coordinator starts each safe account attempt with the pristine caller body",
      "independent money boundary at the real chat coordinator waits for a transient 429 cooldown without rotating a healthy pin",
      "independent money boundary at the real chat coordinator waits for a transient 503 cooldown without rotating a healthy pin"
    ],
    "tests/qa/platform-audit/saver-subsets.test.js": [
      "independent production saver subset coverage exercises active pressure combinations in both safe and explicit lossy profiles",
      "independent production saver subset coverage keeps a transformed prefix stable when the same caller appends a turn",
      "independent production saver subset coverage preserves 32 seeded full-pipeline variants of error aliases, JSON layouts and current instructions",
      "independent production saver subset coverage preserves semantics and fixed production order for every subset of fourteen stage families",
      "independent production saver subset coverage preserves typed custom tool schemas and caller input across a compatible-provider translation"
    ],
    "tests/qa/platform-audit/scheduler-boundary.test.js": [
      "independent scheduler entitlement and cache boundary excludes depleted general quota even when a different window is unreadable",
      "independent scheduler entitlement and cache boundary holds a healthy agent pin through load changes and refuses its full capacity without spending a new cache",
      "independent scheduler entitlement and cache boundary honors account enabledModels restrictions while retaining other account inventory",
      "independent scheduler entitlement and cache boundary honors monthly then weekly then session reset lexicographically, ahead of idle load and configured priority",
      "independent scheduler entitlement and cache boundary isolates exhausted Opus entitlement from healthy Sonnet entitlement on the same account",
      "independent scheduler entitlement and cache boundary spreads distinct agents only after equal entitlement deadlines, then retains both pins"
    ],
    "tests/qa/quota-workbench-preview.test.js": [
      "retains synthetic quota observations and scheduler outcomes for visual qualification"
    ],
    "tests/translator/assistant-prefill-policy.test.js": [
      "assistant prefill policy — native Claude passthrough completes trailing tool_use with an error tool_result",
      "assistant prefill policy — native Claude passthrough drops an empty trailing assistant",
      "assistant prefill policy — native Claude passthrough drops signed thinking whose content contains only whitespace before Claude preparation",
      "assistant prefill policy — native Claude passthrough drops signed thinking whose content is absent before Claude preparation",
      "assistant prefill policy — native Claude passthrough drops signed thinking whose content is empty before Claude preparation",
      "assistant prefill policy — native Claude passthrough keeps final user unchanged",
      "assistant prefill policy — native Claude passthrough preserves an empty trailing assistant when explicitly requested",
      "assistant prefill policy — native Claude passthrough preserves assistant prefill when explicitly requested",
      "assistant prefill policy — native Claude passthrough preserves non-empty signed thinking",
      "assistant prefill policy — native Claude passthrough preserves valid redacted thinking",
      "assistant prefill policy — native Claude passthrough turns trailing assistant text into a continuation turn",
      "assistant prefill policy — translated Claude target completes trailing tool_use with an error tool_result",
      "assistant prefill policy — translated Claude target drops an empty trailing assistant",
      "assistant prefill policy — translated Claude target drops signed thinking whose content contains only whitespace before Claude preparation",
      "assistant prefill policy — translated Claude target drops signed thinking whose content is absent before Claude preparation",
      "assistant prefill policy — translated Claude target drops signed thinking whose content is empty before Claude preparation",
      "assistant prefill policy — translated Claude target keeps final user unchanged",
      "assistant prefill policy — translated Claude target preserves an empty trailing assistant when explicitly requested",
      "assistant prefill policy — translated Claude target preserves assistant prefill when explicitly requested",
      "assistant prefill policy — translated Claude target preserves non-empty signed thinking",
      "assistant prefill policy — translated Claude target preserves valid redacted thinking",
      "assistant prefill policy — translated Claude target turns trailing assistant text into a continuation turn",
      "native Claude blank thinking cleanup drops a nonfinal assistant left empty by blank thinking removal and merges adjacent users"
    ],
    "tests/translator/bugs-antigravity.test.js": [
      "Antigravity → Claude tool call input_json_delta includes Anthropic index",
      "Antigravity → OpenAI functionCall without id keeps a stable matchable id",
      "Antigravity → OpenAI functionResponse + functionCall in same content keeps both",
      "Antigravity → OpenAI signature-only part does not produce empty text",
      "Antigravity executor does not inject the legacy Antigravity default system prompt for Claude-backed models",
      "Antigravity executor does not inject the legacy Antigravity default system prompt for Gemini-backed models",
      "Antigravity executor strips optional from nested tool schemas",
      "Antigravity executor wraps array tool outputs in an object for the Gemini functionResponse",
      "Antigravity executor wraps array tool outputs in the Claude tool_result path too"
    ],
    "tests/translator/bugs-claude-thinking-tags.test.js": [
      "Claude → OpenAI: thinking block boundaries delivers thinking text via reasoning_content and answer via content",
      "Claude → OpenAI: thinking block boundaries never emits literal <think>/</think> in delta.content",
      "Claude → Responses full pivot (the #454 end-to-end scenario) reasoning section closes before text, no literal tags anywhere",
      "OpenAI → Responses: reasoning closes on reasoning→content transition (#454) emits reasoning done events before the first output_text.delta",
      "OpenAI → Responses: reasoning closes on reasoning→content transition (#454) keeps inline <think> tag parsing working for providers that emit tags in content"
    ],
    "tests/translator/bugs-claudeCode-context.test.js": [
      "Claude Code CLI context → OpenAI assistant thinking block survives Claude→Claude passthrough",
      "Claude Code CLI context → OpenAI rejects image tool results rather than changing their role or dropping them",
      "Claude Code CLI context → OpenAI rejects opaque redacted thinking without leaking its payload",
      "Claude Code CLI context → OpenAI system array keeps all text parts"
    ],
    "tests/translator/bugs-codexCli-responses.test.js": [
      "Codex CLI Responses → OpenAI function_call arguments end up as a string",
      "Codex CLI Responses → OpenAI rejects nameless tool calls instead of dropping the transaction",
      "Codex CLI Responses → OpenAI rejects uploaded image references the Chat bridge cannot resolve",
      "OpenAI → Codex Responses (reverse) call_id longer than 64 chars is clamped",
      "OpenAI → Codex Responses (reverse) maps developer messages to Responses API instructions"
    ],
    "tests/translator/bugs-gemini-cursor-commandcode.test.js": [
      "OpenAI → CommandCode image content is preserved",
      "OpenAI → CommandCode rejects malformed tool arguments without substituting an empty object",
      "OpenAI → Cursor rejects images unsupported by the implemented Cursor transport",
      "OpenAI → Cursor respects client max_tokens",
      "OpenAI → Gemini multiple system messages are all kept in order",
      "OpenAI → Gemini retains system and developer text bytes including identity and Unicode"
    ],
    "tests/translator/bugs-kiro.test.js": [
      "Kiro upstream model IDs does not rewrite an already valid or non-Claude dotted ID",
      "Kiro upstream model IDs does not rewrite an already valid or non-Claude dotted ID",
      "Kiro upstream model IDs normalizes Claude letter-dot-digit IDs from Claude input",
      "Kiro upstream model IDs normalizes Claude letter-dot-digit IDs from OpenAI input",
      "Kiro upstream model IDs strips synthetic suffixes before normalizing a dotted Claude ID",
      "OpenAI → Kiro malformed tool arguments do not throw the whole request",
      "OpenAI → Kiro maps max_completion_tokens before Kiro dispatch",
      "OpenAI → Kiro maps max_output_tokens before Kiro dispatch",
      "OpenAI → Kiro maps max_tokens before Kiro dispatch",
      "OpenAI → Kiro preserves Responses reasoning.effort high through the full Kiro route",
      "OpenAI → Kiro preserves Responses reasoning.effort low through the full Kiro route",
      "OpenAI → Kiro preserves Responses reasoning.effort medium through the full Kiro route",
      "OpenAI → Kiro preserves the Responses output limit through its Chat bridge",
      "OpenAI → Kiro rejects remote images instead of replacing them with URL text",
      "OpenAI → Kiro respects client max_tokens"
    ],
    "tests/translator/bugs-null-content-parts.test.js": [
      "null content parts drops null content parts before OpenAI strip handling",
      "null content parts drops null parts without media stripping before the Claude translator",
      "null content parts drops null parts without media stripping before the Kiro translator",
      "null content parts drops null parts without media stripping before the OpenAI translator"
    ],
    "tests/translator/bugs-openai-bridge.test.js": [
      "bug: Claude → OpenAI bridge data loss image with source.type=url is preserved exactly",
      "bug: Claude → OpenAI bridge data loss rejects tool error flags that the target cannot represent",
      "bug: Claude → OpenAI bridge data loss rejects tool images when the target tool role accepts only text",
      "bug: Claude → OpenAI bridge data loss system array non-text parts are not silently dropped",
      "bug: Claude → OpenAI bridge data loss thinking block survives round-trip Claude→OpenAI→Claude",
      "bug: empty content message handling assistant message with only tool_calls is not dropped",
      "bug: tool_call id stability across bridge sanitized tool id stays matched between call and result"
    ],
    "tests/translator/bugs-toClaude-context.test.js": [
      "OpenAI → Claude context mapping assistant reasoning_content becomes a thinking block",
      "OpenAI → Claude context mapping DeepSeek Claude transport adds a thinking placeholder before tool_use in thinking mode",
      "OpenAI → Claude context mapping does not inject Claude Code system prompt for compatible providers",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation adaptive thinking (no budget_tokens) is left untouched",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation high budget under max_tokens → both unchanged",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation large client max_tokens on a 128k model is allowed up to maxOutput",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation max effort budget on a 128k model → max_tokens up to 128000, budget preserved just under",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation max effort budget on a 64k model → budget < max_tokens ≤ 64000",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation over-large client max_tokens on a 64k model is still clamped to 64000",
      "OpenAI → Claude context mapping max_tokens vs thinking.budget_tokens reconciliation xhigh budget with a low client max_tokens → raise max_tokens, preserve budget",
      "OpenAI → Claude context mapping preserves caller identity and developer instructions without adding a new identity",
      "OpenAI → Claude context mapping rejects audio that the Messages transport cannot represent",
      "OpenAI → Claude context mapping remote http image_url is preserved",
      "OpenAI → Claude context mapping tool_choice=none remains an explicit prohibition",
      "OpenAI → Claude context mapping user message with only an image is not dropped as empty"
    ],
    "tests/translator/claude-kiro-direct.test.js": [
      "Claude → Kiro (direct route) carries the Claude system prompt on the user message and never as a top-level field",
      "Claude → Kiro (direct route) does not send additionalModelRequestFields for Kiro models without effort support",
      "Claude → Kiro (direct route) guard 1: with no tools, a dangling tool_result is flattened to text (no structured ref)",
      "Claude → Kiro (direct route) guard 2: with tools, an orphaned tool_result is folded into user text",
      "Claude → Kiro (direct route) injects thinking_mode tag when model implies thinking",
      "Claude → Kiro (direct route) keeps conversationId stable from client session headers and replays frozen msg0",
      "Claude → Kiro (direct route) keeps explicit Claude effort ahead of an injected OpenAI effort",
      "Claude → Kiro (direct route) keeps GPT-5.6 reasoning intentionally disabled for effort disabled",
      "Claude → Kiro (direct route) keeps GPT-5.6 reasoning intentionally disabled for effort none",
      "Claude → Kiro (direct route) keeps GPT-5.6 reasoning intentionally disabled for effort off",
      "Claude → Kiro (direct route) keeps the frozen session start stable across turns and keeps the clock off it",
      "Claude → Kiro (direct route) keeps the legacy thinking fallback for unsupported GPT-5.6 effort auto",
      "Claude → Kiro (direct route) keeps the legacy thinking fallback for unsupported GPT-5.6 effort minimal",
      "Claude → Kiro (direct route) keeps the legacy thinking fallback for unsupported GPT-5.6 effort ultra",
      "Claude → Kiro (direct route) maps Claude-format effort to GPT-5.6 reasoning fields without legacy prompt tags",
      "Claude → Kiro (direct route) maps output_config.effort high to Kiro CLI-style additionalModelRequestFields for effort models",
      "Claude → Kiro (direct route) normalizes an unsupported Kiro intensity suffix while preserving agentic behavior",
      "Claude → Kiro (direct route) produces a Kiro conversationState payload",
      "Kiro → Claude (direct route, OpenAI-shaped chunks from executor) finish chunk emits message_delta + message_stop with stop_reason",
      "Kiro → Claude (direct route, OpenAI-shaped chunks from executor) first text chunk emits message_start + content_block_start + text_delta",
      "Kiro → Claude (direct route, OpenAI-shaped chunks from executor) reasoning_content maps to a thinking block",
      "Kiro → Claude (direct route, OpenAI-shaped chunks from executor) tool_calls map to a tool_use block with buffered input_json_delta"
    ],
    "tests/translator/coverage-all-models.test.js": [
      "coverage: every model translates supported input 'af': all models OpenAI→target",
      "coverage: every model translates supported input 'ag': all models OpenAI→target",
      "coverage: every model translates supported input 'alicode-intl': all models OpenAI→target",
      "coverage: every model translates supported input 'alicode': all models OpenAI→target",
      "coverage: every model translates supported input 'alims-intl': all models OpenAI→target",
      "coverage: every model translates supported input 'alitp-intl': all models OpenAI→target",
      "coverage: every model translates supported input 'anthropic': all models OpenAI→target",
      "coverage: every model translates supported input 'assemblyai': all models OpenAI→target",
      "coverage: every model translates supported input 'black-forest-labs': all models OpenAI→target",
      "coverage: every model translates supported input 'blackbox': all models OpenAI→target",
      "coverage: every model translates supported input 'bm': all models OpenAI→target",
      "coverage: every model translates supported input 'byteplus': all models OpenAI→target",
      "coverage: every model translates supported input 'bzl': all models OpenAI→target",
      "coverage: every model translates supported input 'cbai': all models OpenAI→target",
      "coverage: every model translates supported input 'cbcn': all models OpenAI→target",
      "coverage: every model translates supported input 'cc': all models OpenAI→target",
      "coverage: every model translates supported input 'cerebras': all models OpenAI→target",
      "coverage: every model translates supported input 'charm-hyper': all models OpenAI→target",
      "coverage: every model translates supported input 'chenzk': all models OpenAI→target",
      "coverage: every model translates supported input 'cloudflare-ai': all models OpenAI→target",
      "coverage: every model translates supported input 'cohere': all models OpenAI→target",
      "coverage: every model translates supported input 'comfyui': all models OpenAI→target",
      "coverage: every model translates supported input 'commandcode': all models OpenAI→target",
      "coverage: every model translates supported input 'cu': all models OpenAI→target",
      "coverage: every model translates supported input 'cx': all models OpenAI→target",
      "coverage: every model translates supported input 'deepgram': all models OpenAI→target",
      "coverage: every model translates supported input 'dv': all models OpenAI→target",
      "coverage: every model translates supported input 'edge-tts': all models OpenAI→target",
      "coverage: every model translates supported input 'elevenlabs-tts-models': all models OpenAI→target",
      "coverage: every model translates supported input 'fal-ai': all models OpenAI→target",
      "coverage: every model translates supported input 'featherless': all models OpenAI→target",
      "coverage: every model translates supported input 'fireworks': all models OpenAI→target",
      "coverage: every model translates supported input 'gc': all models OpenAI→target",
      "coverage: every model translates supported input 'gcli': all models OpenAI→target",
      "coverage: every model translates supported input 'gemini-tts-models': all models OpenAI→target",
      "coverage: every model translates supported input 'gemini-tts-voices': all models OpenAI→target",
      "coverage: every model translates supported input 'gemini': all models OpenAI→target",
      "coverage: every model translates supported input 'gh': all models OpenAI→target",
      "coverage: every model translates supported input 'glm-cn': all models OpenAI→target",
      "coverage: every model translates supported input 'glm': all models OpenAI→target",
      "coverage: every model translates supported input 'google-tts': all models OpenAI→target",
      "coverage: every model translates supported input 'grok-web': all models OpenAI→target",
      "coverage: every model translates supported input 'groq': all models OpenAI→target",
      "coverage: every model translates supported input 'huggingface': all models OpenAI→target",
      "coverage: every model translates supported input 'hunyuan': all models OpenAI→target",
      "coverage: every model translates supported input 'hyperbolic': all models OpenAI→target",
      "coverage: every model translates supported input 'if': all models OpenAI→target",
      "coverage: every model translates supported input 'kc': all models OpenAI→target",
      "coverage: every model translates supported input 'kgw': all models OpenAI→target",
      "coverage: every model translates supported input 'kimchi': all models OpenAI→target",
      "coverage: every model translates supported input 'kimi-cn': all models OpenAI→target",
      "coverage: every model translates supported input 'kimi': all models OpenAI→target",
      "coverage: every model translates supported input 'kr': all models OpenAI→target",
      "coverage: every model translates supported input 'llm7': all models OpenAI→target",
      "coverage: every model translates supported input 'local-device': all models OpenAI→target",
      "coverage: every model translates supported input 'meta': all models OpenAI→target",
      "coverage: every model translates supported input 'minimax-cn': all models OpenAI→target",
      "coverage: every model translates supported input 'minimax': all models OpenAI→target",
      "coverage: every model translates supported input 'mistral': all models OpenAI→target",
      "coverage: every model translates supported input 'morph': all models OpenAI→target",
      "coverage: every model translates supported input 'ms': all models OpenAI→target",
      "coverage: every model translates supported input 'nanobanana': all models OpenAI→target",
      "coverage: every model translates supported input 'nebius': all models OpenAI→target",
      "coverage: every model translates supported input 'nous': all models OpenAI→target",
      "coverage: every model translates supported input 'novita': all models OpenAI→target",
      "coverage: every model translates supported input 'nube': all models OpenAI→target",
      "coverage: every model translates supported input 'nvidia': all models OpenAI→target",
      "coverage: every model translates supported input 'ogw': all models OpenAI→target",
      "coverage: every model translates supported input 'ollama-local': all models OpenAI→target",
      "coverage: every model translates supported input 'ollama': all models OpenAI→target",
      "coverage: every model translates supported input 'openai-tts-models': all models OpenAI→target",
      "coverage: every model translates supported input 'openai-tts-voices': all models OpenAI→target",
      "coverage: every model translates supported input 'openai': all models OpenAI→target",
      "coverage: every model translates supported input 'opencode-go': all models OpenAI→target",
      "coverage: every model translates supported input 'opencode-zen': all models OpenAI→target",
      "coverage: every model translates supported input 'openrouter-tts-models': all models OpenAI→target",
      "coverage: every model translates supported input 'openrouter-tts-voices': all models OpenAI→target",
      "coverage: every model translates supported input 'openrouter': all models OpenAI→target",
      "coverage: every model translates supported input 'perplexity-agent': all models OpenAI→target",
      "coverage: every model translates supported input 'perplexity-web': all models OpenAI→target",
      "coverage: every model translates supported input 'perplexity': all models OpenAI→target",
      "coverage: every model translates supported input 'poolside': all models OpenAI→target",
      "coverage: every model translates supported input 'qd': all models OpenAI→target",
      "coverage: every model translates supported input 'qianfan': all models OpenAI→target",
      "coverage: every model translates supported input 'recraft': all models OpenAI→target",
      "coverage: every model translates supported input 'runwayml': all models OpenAI→target",
      "coverage: every model translates supported input 'samba': all models OpenAI→target",
      "coverage: every model translates supported input 'sdwebui': all models OpenAI→target",
      "coverage: every model translates supported input 'selfhosted-embedding': all models OpenAI→target",
      "coverage: every model translates supported input 'selfhosted-stt': all models OpenAI→target",
      "coverage: every model translates supported input 'selfhosted-tts': all models OpenAI→target",
      "coverage: every model translates supported input 'sensenova': all models OpenAI→target",
      "coverage: every model translates supported input 'siliconflow': all models OpenAI→target",
      "coverage: every model translates supported input 'sp': all models OpenAI→target",
      "coverage: every model translates supported input 'stability-ai': all models OpenAI→target",
      "coverage: every model translates supported input 'together': all models OpenAI→target",
      "coverage: every model translates supported input 'tokenrouter': all models OpenAI→target",
      "coverage: every model translates supported input 'venice': all models OpenAI→target",
      "coverage: every model translates supported input 'vertex-partner': all models OpenAI→target",
      "coverage: every model translates supported input 'vertex': all models OpenAI→target",
      "coverage: every model translates supported input 'volcengine-ark': all models OpenAI→target",
      "coverage: every model translates supported input 'voyage-ai': all models OpenAI→target",
      "coverage: every model translates supported input 'x5l': all models OpenAI→target",
      "coverage: every model translates supported input 'xai': all models OpenAI→target",
      "coverage: every model translates supported input 'xiaomi-mimo-tts-models': all models OpenAI→target",
      "coverage: every model translates supported input 'xiaomi-mimo': all models OpenAI→target",
      "coverage: every model translates supported input 'xiaomi-tokenplan': all models OpenAI→target",
      "coverage: every model translates supported input 'xunfei': all models OpenAI→target",
      "coverage: every model translates supported input 'zmf': all models OpenAI→target",
      "coverage: image-strip models drop image content 'kr'/'deepseek-3.2' strips image when strip=[image]",
      "coverage: image-strip models drop image content 'kr'/'qwen3-coder-next' strips image when strip=[image]"
    ],
    "tests/translator/format-roundtrip.test.js": [
      "roundtrip: Claude source preserves core fields → OpenAI system → system role",
      "roundtrip: Claude source preserves core fields → OpenAI tool arguments are valid JSON string",
      "roundtrip: Claude source preserves core fields → OpenAI tool_result → tool message with matching id",
      "roundtrip: Claude source preserves core fields → OpenAI tool_use → assistant.tool_calls with matching id",
      "roundtrip: OpenAI tools → Claude → keeps tool name tool name survives openai→claude",
      "roundtrip: parallel tool calls keep distinct ids each tool_call has a matching tool result",
      "roundtrip: parallel tool calls keep distinct ids two tool_calls, two distinct ids"
    ],
    "tests/translator/golden-request.test.js": [
      "GOLDEN request: OpenAI → Claude full body (system/image/tool/tool_result)",
      "GOLDEN request: OpenAI → Claude reasoning_effort → adaptive output_config (claude 4.6+)",
      "GOLDEN request: OpenAI → Gemini full body (system/image/tool/tool_result)",
      "GOLDEN request: OpenAI → Gemini Gemini CLI tool requests include validated toolConfig and enough output for high thinking",
      "GOLDEN request: OpenAI → Kiro full body (image base64 + tool_result)",
      "GOLDEN request: reproducibility cleaned Kiro body carries no raw uuid",
      "GOLDEN request: reproducibility OpenAI → Claude → identical output on two runs",
      "GOLDEN request: reproducibility OpenAI → Gemini → identical output on two runs",
      "GOLDEN request: reproducibility OpenAI → Kiro → identical output on two runs"
    ],
    "tests/translator/golden-response-stream.test.js": [
      "GOLDEN response stream: Claude → OpenAI text + thinking + tool_use + usage + finish",
      "GOLDEN response stream: Gemini → OpenAI image output (inlineData → delta.images)",
      "GOLDEN response stream: Gemini → OpenAI text + thought(no-sig) + functionCall + usage + finish",
      "GOLDEN response stream: Kiro → OpenAI text + reasoning + toolUse + usage + stop",
      "GOLDEN response stream: Ollama → OpenAI content + thinking + tool_calls + done usage",
      "GOLDEN response stream: OpenAI-Responses (codex) → OpenAI error event → error chunk (fallback id/created)",
      "GOLDEN response stream: OpenAI-Responses (codex) → OpenAI text + reasoning + tool_call + completed usage"
    ],
    "tests/translator/golden-snapshot-portability.test.js": [
      "golden-url-header snapshot portability does not embed the app version, which every release bumps",
      "golden-url-header snapshot portability does not embed the recording machine's hostname",
      "golden-url-header snapshot portability does not embed the recording machine's platform or Node version",
      "golden-url-header snapshot portability keeps the app-version headers on the placeholder, not on a release number"
    ],
    "tests/translator/golden-translator-concerns.test.js": [
      "GOLDEN passthrough: same format = no translation request openai→openai keeps messages (filterToOpenAIFormat normalize)",
      "GOLDEN passthrough: same format = no translation response openai→openai returns chunk unchanged",
      "GOLDEN response stream: CommandCode → OpenAI text + reasoning + tool + finish-step usage",
      "GOLDEN response stream: Kiro → OpenAI (finish after tool) toolUse then stop — lock current finish_reason behavior",
      "GOLDEN response stream: Ollama → OpenAI (finish after tool) tool_calls then done_reason=stop — lock current finish_reason",
      "GOLDEN usage math: Claude prompt = input + cache (lock) prompt_tokens sums input + cache_read + cache_creation"
    ],
    "tests/translator/golden-url-header.test.js": [
      "GOLDEN buildHeaders (default executor providers) alicode → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) alicode-intl → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) alims-intl → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) alitp-intl → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) anthropic → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) api-airforce → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) assemblyai → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) baidu → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) bazaarlink → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) blackbox → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) bluesminds → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) byteplus → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) cerebras → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) charm-hyper → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) chenzk → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) chutes → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) claude → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) cloudflare-ai → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) codebuddy-cn → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) codebuddy-intl → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) cohere → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) crof-ai → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) deepgram → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) devin → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) featherless → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) fireworks → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) gemini → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) gitlab → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) gitlawb-opengateway → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) glm → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) glm-cn → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) grok-cli → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) groq → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) hyperbolic → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kenari → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kilo-gateway → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kilocode → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kimchi → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kimi → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) kimi-cn → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) llm7 → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) meta → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) minimax → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) minimax-cn → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) mistral → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) mmf → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) modelscope → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) morph → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) nanobanana → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) nebius → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) nous → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) novita → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) nube → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) nvidia → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) ollama → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) openai → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) opencode-zen → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) openrouter → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) perplexity → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) perplexity-agent → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) poolside → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) sambanova → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) sensenova → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) siliconflow → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) sumopod → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) tencent → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) together → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) tokenrouter → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) venice → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) vercel-ai-gateway → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) volcengine-ark → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) x5lab → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) xai → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) xiaomi-mimo → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) xunfei → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) zed → headers (apiKey / oauth)",
      "GOLDEN buildHeaders (default executor providers) zenmux-free → headers (apiKey / oauth)",
      "GOLDEN buildUrl (default executor providers) alicode → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) alicode-intl → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) alims-intl → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) alitp-intl → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) anthropic → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) api-airforce → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) assemblyai → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) baidu → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) bazaarlink → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) blackbox → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) bluesminds → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) byteplus → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) cerebras → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) charm-hyper → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) chenzk → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) chutes → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) claude → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) cloudflare-ai → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) codebuddy-cn → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) codebuddy-intl → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) cohere → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) crof-ai → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) deepgram → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) devin → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) featherless → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) fireworks → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) gemini → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) gitlab → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) gitlawb-opengateway → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) glm → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) glm-cn → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) grok-cli → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) groq → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) hyperbolic → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kenari → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kilo-gateway → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kilocode → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kimchi → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kimi → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) kimi-cn → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) llm7 → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) meta → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) minimax → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) minimax-cn → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) mistral → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) mmf → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) modelscope → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) morph → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) nanobanana → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) nebius → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) nous → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) novita → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) nube → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) nvidia → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) ollama → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) openai → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) opencode-zen → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) openrouter → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) perplexity → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) perplexity-agent → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) poolside → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) sambanova → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) sensenova → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) siliconflow → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) sumopod → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) tencent → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) together → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) tokenrouter → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) venice → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) vercel-ai-gateway → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) volcengine-ark → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) x5lab → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) xai → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) xiaomi-mimo → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) xunfei → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) zed → url (stream + non-stream)",
      "GOLDEN buildUrl (default executor providers) zenmux-free → url (stream + non-stream)"
    ],
    "tests/translator/real/all-formats.real.test.js": [
      "REAL all-formats matrix has active providers in DB"
    ],
    "tests/translator/real/antigravity-models.real.test.js": [
      "antigravity models — real ag/claude-opus-4-6-thinking: simple prompt",
      "antigravity models — real ag/claude-opus-4-6-thinking: tool call with optional field in schema",
      "antigravity models — real ag/claude-sonnet-4-6: simple prompt",
      "antigravity models — real ag/claude-sonnet-4-6: tool call with optional field in schema",
      "antigravity models — real ag/gemini-3-flash-agent: simple prompt",
      "antigravity models — real ag/gemini-3-flash-agent: tool call with optional field in schema",
      "antigravity models — real ag/gemini-3-flash: simple prompt",
      "antigravity models — real ag/gemini-3-flash: tool call with optional field in schema",
      "antigravity models — real ag/gemini-3.1-pro-low: simple prompt",
      "antigravity models — real ag/gemini-3.1-pro-low: tool call with optional field in schema",
      "antigravity models — real ag/gemini-3.5-flash-extra-low: simple prompt",
      "antigravity models — real ag/gemini-3.5-flash-extra-low: tool call with optional field in schema",
      "antigravity models — real ag/gemini-3.5-flash-low: simple prompt",
      "antigravity models — real ag/gemini-3.5-flash-low: tool call with optional field in schema",
      "antigravity models — real ag/gemini-pro-agent: simple prompt",
      "antigravity models — real ag/gemini-pro-agent: tool call with optional field in schema",
      "antigravity models — real ag/gpt-oss-120b-medium: simple prompt",
      "antigravity models — real ag/gpt-oss-120b-medium: tool call with optional field in schema"
    ],
    "tests/translator/real/file-base64-survey.real.test.js": [
      "REAL file base64 survey has active providers in DB"
    ],
    "tests/translator/real/nvidia-thinking.e2e.test.js": [
      "nvidia thinking e2e nvidia/deepseek-ai/deepseek-v4-flash-0731 with reasoning_effort -> no 'thinking' 400",
      "nvidia thinking e2e nvidia/minimaxai/minimax-m3 with reasoning_effort -> no 'thinking' 400",
      "nvidia thinking e2e nvidia/moonshotai/kimi-k2.6 with reasoning_effort -> no 'thinking' 400",
      "nvidia thinking e2e nvidia/nvidia/nemotron-3-ultra-550b-a55b with reasoning_effort -> no 'thinking' 400",
      "nvidia thinking e2e nvidia/z-ai/glm-5.2 with reasoning_effort -> no 'thinking' 400"
    ],
    "tests/translator/real/provider-cases.real.test.js": [
      "REAL provider behavior cases antigravity: responds OK",
      "REAL provider behavior cases codex: session stickiness (cached_tokens on 2nd turn)",
      "REAL provider behavior cases gemini: finish_reason stop",
      "REAL provider behavior cases kiro: tool turn -> tool_calls",
      "REAL provider behavior cases ollama: max_tokens -> length"
    ],
    "tests/translator/real/smoke-providers.real.test.js": [
      "REAL provider smoke has active providers in DB"
    ],
    "tests/translator/real/thinking.real.test.js": [
      "REAL thinking normalization has active providers in DB"
    ],
    "tests/translator/real/vision-capability-survey.real.test.js": [
      "REAL vision capability survey has active providers in DB"
    ],
    "tests/translator/structured-output.test.js": [
      "OpenAI Chat → Responses leaves text unset when no response_format is requested",
      "OpenAI Chat → Responses maps response_format json_object to text.format",
      "OpenAI Chat → Responses maps response_format json_schema to text.format",
      "OpenAI Responses → Chat maps text.format back to response_format and drops the Responses-only field"
    ],
    "tests/translator/thinking-unified.test.js": [
      "applyThinking per provider format aggregator (siliconflow) GLM model → forced openai reasoning_effort",
      "applyThinking per provider format applies a supported Codex Ultra suffix",
      "applyThinking per provider format claude 4.6+ → adaptive thinking + output_config (no budget_tokens)",
      "applyThinking per provider format claude 4.6+ leaves auto effort to the upstream",
      "applyThinking per provider format claude haiku → enabled+budget",
      "applyThinking per provider format DeepSeek → enabled + reasoning_effort high (low→high)",
      "applyThinking per provider format gemini-2.5 → thinkingBudget",
      "applyThinking per provider format gemini-2.5 budget thinking keeps enough room for answer tokens",
      "applyThinking per provider format gemini-3 → thinkingLevel",
      "applyThinking per provider format gemini-3 clamps unsupported max/xhigh thinking levels to high",
      "applyThinking per provider format gemini-3 high thinking raises too-small maxOutputTokens",
      "applyThinking per provider format gemini-3 maps auto thinking level to high instead of sending unsupported auto",
      "applyThinking per provider format GLM off → enable_thinking:false (not thinking.disabled)",
      "applyThinking per provider format keeps Chat Completions reasoning effort flat",
      "applyThinking per provider format keeps Codex-only GPT-5.6 levels out of Kiro translation",
      "applyThinking per provider format Kimi auto → supported reasoning_effort",
      "applyThinking per provider format Kimi on → reasoning_effort",
      "applyThinking per provider format Kimi unsupported OpenAI levels → supported reasoning_effort",
      "applyThinking per provider format MiniMax M3 → adaptive",
      "applyThinking per provider format non-reasoning model → strips thinking",
      "applyThinking per provider format Ollama auto → think true",
      "applyThinking per provider format Ollama budget → think level",
      "applyThinking per provider format Ollama extracts top-level think shape",
      "applyThinking per provider format Ollama gpt-oss clamps max → high",
      "applyThinking per provider format Ollama level → think string",
      "applyThinking per provider format Ollama minimal → think low",
      "applyThinking per provider format Ollama none → think false",
      "applyThinking per provider format Ollama xhigh → think max",
      "applyThinking per provider format openai keeps xhigh for reasoning models",
      "applyThinking per provider format Qwen on → enable_thinking + thinking_budget",
      "applyThinking per provider format QwQ cannot disable → clamp minimal",
      "applyThinking per provider format serializes Codex gpt-5.6-luna effort max as Responses reasoning",
      "applyThinking per provider format serializes Codex gpt-5.6-luna effort ultra as Responses reasoning",
      "applyThinking per provider format serializes Codex gpt-5.6-sol effort max as Responses reasoning",
      "applyThinking per provider format serializes Codex gpt-5.6-sol effort ultra as Responses reasoning",
      "applyThinking per provider format serializes Codex gpt-5.6-terra effort max as Responses reasoning",
      "applyThinking per provider format serializes Codex gpt-5.6-terra effort ultra as Responses reasoning",
      "applyThinking per provider format suffix overrides body",
      "extractReasoningText (response shapes) no reasoning → empty",
      "extractReasoningText (response shapes) reasoning fallback",
      "extractReasoningText (response shapes) reasoning_content (GLM/Qwen/DeepSeek)",
      "extractReasoningText (response shapes) reasoning_details[] (MiniMax split)",
      "extractThinking claude disabled",
      "extractThinking claude enabled+budget",
      "extractThinking gemini thinkingBudget 0 → none",
      "extractThinking no intent → null",
      "extractThinking openai reasoning_effort",
      "extractThinking qwen enable_thinking false",
      "extractThinking responses reasoning.effort none",
      "parseSuffix no suffix → passthrough",
      "parseSuffix parses auto / none",
      "parseSuffix parses level suffix",
      "parseSuffix parses numeric budget suffix",
      "parseSuffix parses ultra suffix"
    ],
    "tests/translator/zai-reasoning-effort-2721.test.js": [
      "extractThinking effort precedence an explicit Claude budget still wins over reasoning_effort",
      "extractThinking effort precedence reasoning_effort wins over thinking:{type:enabled}",
      "extractThinking effort precedence reasoning.effort wins over thinking:{type:enabled}",
      "extractThinking effort precedence thinking:{type:disabled} still wins over reasoning_effort",
      "extractThinking effort precedence thinking:{type:disabled} with no effort still means none",
      "zai reasoning_effort is gated on thinkingEffortSupported disabling thinking still wins over the effort field",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-4.7 does not declare the flag and is unchanged — z.ai ignores the field",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.2 declares the flag and emits the effort",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 high → reasoning_effort=high",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 low → reasoning_effort=low",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 max → reasoning_effort=max",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 medium → reasoning_effort=high",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 minimal → reasoning_effort=low",
      "zai reasoning_effort is gated on thinkingEffortSupported GLM-5.3 xhigh → reasoning_effort=max"
    ],
    "tests/unit/aborted-stream-usage-3488.test.js": [
      "a client that hangs up mid-stream keeps the content generated before the hang-up",
      "a client that hangs up mid-stream reports it as aborted, not as a normal completion",
      "a client that hangs up mid-stream reports usage so the partial generation is accounted for",
      "a client that hangs up mid-stream still reports the stream as finished",
      "a stream cancelled before anything arrived reports once, as aborted, without inventing content",
      "a stream that ends normally does not report a second time if the reader is cancelled afterwards",
      "a stream that ends normally still reports exactly once, and not as aborted",
      "passthrough streams also record the partial turn when the client hangs up"
    ],
    "tests/unit/abuse-gate-fallback-3602.test.js": [
      "free-tier abuse gates trigger account rotation (#3602) backs off rather than locking the account outright",
      "free-tier abuse gates trigger account rotation (#3602) carries no duplicate rules, which are unreachable by construction",
      "free-tier abuse gates trigger account rotation (#3602) no rule before it matches this message first",
      "free-tier abuse gates trigger account rotation (#3602) rotates on the reported message whatever status carries it"
    ],
    "tests/unit/access-controls-ui.test.js": [
      "edits SAML fields directly, preserves sibling values, reviews a frozen request and separately reads back",
      "invalidates a secret-bearing review when the document becomes hidden",
      "keeps a failed sign-in status visible and retryable while editing single sign-on",
      "keeps accepted configuration with missing readback uncertain and prevents an immediate repeat",
      "requires matching passwords and clears submitted secrets after a refusal without automatic retry",
      "retains direct edits during polling and clears secrets when a review is cancelled",
      "uses verified saved and cleared configuration as the direct form baseline without reviving old values"
    ],
    "tests/unit/access-profiles.test.js": [
      "carries compliance on the keys list without the secret",
      "copies the profile's settings onto the key at adoption",
      "counts the keys following each profile without disclosing key material",
      "does not mint a version for a rename alone",
      "keeps every adopted setting when a key stops following a profile",
      "leaves the key's own expiry alone when the profile requires none",
      "re-adopting the current version clears both signals",
      "refuses a duplicate profile name and an unparseable ceiling",
      "refuses a stale rename even when policy version did not change",
      "refuses expiry writes from inference callers and remote operators",
      "refuses fractional expiry days rather than issuing an immediately expired key",
      "refuses invalid expiry \"\" without silently removing it",
      "refuses invalid expiry \"2030-01-02\" without silently removing it",
      "refuses invalid expiry \"2030-02-30T00:00:00Z\" without silently removing it",
      "refuses invalid expiry \"garbage\" without silently removing it",
      "refuses invalid expiry {} without silently removing it",
      "refuses invalid expiry 0 without silently removing it",
      "refuses stale edits and deletes without changing a profile or adopting keys",
      "releases keys without changing their settings when a profile is deleted",
      "reports a hand-edited key as drifted, naming the fields",
      "reports a key as behind without calling it drifted when the profile advances",
      "resolves the profile's expiry policy to a date on the key",
      "sets and clears existing key expiry through the redacted operator API"
    ],
    "tests/unit/account-admission-credential.test.js": [
      "credential presence is a live-selection gate, not an admission reason the simulator would echo",
      "credential presence leaves cookie, public, credential-free and untyped accounts to their own gates",
      "credential presence reads a refreshable or presentable OAuth account and a keyed API-key account as held",
      "credential presence reads an OAuth account with neither an access token nor a refresh token as empty"
    ],
    "tests/unit/account-board-fields.test.js": [
      "commits a changed number on Enter and on blur, once each, and never an unchanged one",
      "follows a new saved value from outside",
      "renames on Enter, keeps the name on Escape, and ignores blank or unchanged names",
      "reverts to the saved value on Escape and on an emptied field, and sends one save per value"
    ],
    "tests/unit/account-board-model.test.js": [
      "account buckets counts a manual pause apart from an automatic quota pause",
      "account buckets flags low quota only from fresh known windows",
      "account buckets gives the card chip and the health strip one answer, not two",
      "account buckets keeps the specific gate word for attention states",
      "account buckets reads an enabled healthy account with headroom as ready",
      "account buckets refuses to read an absence of evidence as capacity",
      "account buckets separates an account with nothing left from one that is merely low",
      "account buckets sums every bucket over the given population",
      "account buckets treats a threshold above 10 as the low line for that window",
      "board filters and labels composes the serving-state axis with the health axis rather than replacing it",
      "board filters and labels derives credential modes from a registry entry",
      "board filters and labels filters by bucket and by a case-insensitive needle over name, email, provider and id",
      "board filters and labels flattens product groups into labelled lines",
      "board filters and labels lists providers once, sorted",
      "board filters and labels shortens reset labels without inventing a replenishment",
      "seat labels splits a login name into its login and its seat",
      "window visibility does not let a stale depleted weekly window hide anything",
      "window visibility grades a window green, yellow, red or depleted",
      "window visibility hides the session window while the weekly window is depleted and shows it again after",
      "window visibility keeps a manual hide until the person shows the window again, whatever the weekly does",
      "window visibility scopes the depletion rule to one product"
    ],
    "tests/unit/account-control-mutations.test.js": [
      "account policy mutations compares threshold maps independently of insertion order and returns persisted priority after renumbering",
      "account policy mutations does not resurrect a connection deleted between the route read and transaction",
      "account policy mutations preserves a refresh between route read and write for guarded and legacy narrow updates",
      "account policy mutations refuses invalid JSON and non-object bodies without writing account data",
      "account policy mutations rejects a concurrent change to each policy field atomically, even after the route read",
      "account policy mutations rejects malformed snapshots, invalid controls, and unrelated writes without changing the row"
    ],
    "tests/unit/account-control-panel-model.test.js": [
      "account panel local ordering can order an observed reset with unknown percentage without treating it as known headroom",
      "account panel local ordering orders known fresh headroom and reset values before missing, stale or unlimited observations",
      "account panel local ordering uses stable name ties and excludes passed resets without inventing replenishment",
      "account panel persistence accepts only a validated persisted identity and complete policy snapshot",
      "account panel persistence accepts server priority renumbering and removed zero thresholds only after matching readback",
      "account panel persistence does not repeat a conflict or network failure and never claims an inconsistent readback",
      "account panel persistence rebases explicitly edited fields without overwriting new untouched policy",
      "account panel persistence saves only changed policy fields and treats new zero thresholds as unchanged",
      "account panel persistence sends the exact control snapshot and verifies a separate readback",
      "account panel quota evidence distinguishes configured enablement from missing qualification and observed problems",
      "account panel quota evidence keeps configured accounts missing from health and a stable identity order",
      "account panel quota evidence keeps configured pause, recorded health and simultaneous local gates distinct",
      "account panel quota evidence marks unknown, future, old and reset-passed observations as stale",
      "account panel quota evidence never combines older unlimited state or an undated snapshot with newer dated percentages",
      "account panel quota evidence requires valid priority and thresholds while preserving explicit zero",
      "account panel quota evidence retains zero after a stored reset passes and treats unlimited as unconstrained",
      "account panel quota evidence unions database scopes, snapshot-only windows and threshold-only keys without invented quota",
      "account panel quota evidence uses the newest comparable observation, and never derives entitlement from a denominator"
    ],
    "tests/unit/account-fallback-classification-properties.test.js": [
      "429 is decided by status before any body text (#2556) a body matching a pass rule cannot demote a 429 to a client error",
      "429 is decided by status before any body text (#2556) always falls back and escalates backoff for every adversarial body",
      "4xx partition: timing retries vs deterministic client errors every 4xx lands in the partition its config membership dictates",
      "4xx partition: timing retries vs deterministic client errors RETRYABLE_CLIENT_ERROR_STATUSES contains only 4xx statuses",
      "apply/reset lifecycle round-trip a locking status excludes the account while its timestamp is future",
      "apply/reset lifecycle round-trip a non-locking classification leaves rateLimitedUntil null and the pool open",
      "apply/reset lifecycle round-trip applyErrorState carries the escalated backoff level forward",
      "apply/reset lifecycle round-trip resetAccountState after applyErrorState restores a pool-admissible account",
      "backoff schedule derives from BACKOFF_CONFIG doubles exactly per level until the cap binds",
      "backoff schedule derives from BACKOFF_CONFIG escalation through checkFallbackError saturates at maxLevel",
      "backoff schedule derives from BACKOFF_CONFIG is non-decreasing and never exceeds the cap",
      "backoff schedule derives from BACKOFF_CONFIG starts at the configured base",
      "checkFallbackError output sanity for the whole status space never returns negative or NaN cooldownMs for any status x body",
      "cooldown timestamps: one clock, two consumers excludeId removes the failed account regardless of its cooldown state",
      "cooldown timestamps: one clock, two consumers getUnavailableUntil(ms) parses to now + ms exactly under fake timers",
      "cooldown timestamps: one clock, two consumers isAccountUnavailable and filterAvailableAccounts never disagree on a timestamp",
      "unknown 5xx and out-of-range statuses fall back transiently any 5xx unclaimed by ERROR_RULES rotates with a positive finite cooldown"
    ],
    "tests/unit/account-fallback-request-replay.test.js": [
      "request replay fallback classification does not lock an account for Envoy request replay buffer overflow"
    ],
    "tests/unit/account-fallback-rules.test.js": [
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account for a generic HTTP 422 request error",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account on status 400 (Bad Request)",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'context_length_exceeded'",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'improperly formed request'",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'invalid_request_error'",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'maximum context length'",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'prompt is too long'",
      "accountFallback: checkFallbackError rules pass: true client-side errors (context length exceeded) does not fallback or lock account when message contains 'unsupported parameter'",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules handles 401 unauthorized with fixed long cooldown",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules handles rate limit (429) with exponential backoff",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules keeps a 503 Service Unavailable on the same account with a short cooldown, not a quota lock",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules keeps a 529 Overloaded on the same account with a short cooldown, not a quota lock",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules keeps text-signaled 422 capacity errors eligible for fallback",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules keeps text-signaled 422 overloaded errors eligible for fallback",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules keeps text-signaled 422 rate limit errors eligible for fallback",
      "accountFallback: checkFallbackError rules standard rate-limit and auth error rules returns transient cooldown for unknown errors"
    ],
    "tests/unit/account-import-options.test.js": [
      "preserves an import wrapper and reports partial results without including credentials",
      "preserves omitted write-only options and maps provider options without leaking them into unrelated fields"
    ],
    "tests/unit/account-path-projection.test.js": [
      "does not expose proxy secrets or claim reachability",
      "keeps default egress unknown and explicit direct distinct",
      "selected missing and disabled pools refuse rather than silently becoming direct",
      "uses the account strictness snapshot and preserves unknown legacy snapshots"
    ],
    "tests/unit/account-section-model.test.js": [
      "an account with no quota evidence is listed only when something proves it works",
      "an account with no quota evidence is not listed when nothing does",
      "depletion benches an account on ANY exhausted general window, not only on all of them",
      "depletion does NOT call an account depleted once its reset has elapsed",
      "depletion lets a sub-quota run dry without benching the whole connection",
      "depletion still calls a stale exhausted reading depleted while its period is running",
      "evidence bands demotes to stale when one general window could not be read at all",
      "evidence bands ignores a sub-quota when deciding how much is known about the account",
      "evidence bands names a current reading fresh, an old one stale, and no reading unknown",
      "gates and return times files only the self-clearing holds under cooling down",
      "gates and return times returns when the LAST exhausted window rolls, not the first",
      "one window, read the way the router reads it reads an unlimited window as full and an unreadable one as nothing",
      "one window, read the way the router reads it treats a window whose reset has passed as replenished, not as a zero",
      "sections orders cooling down by soonest return, with no known return last",
      "sections orders equal names numerically, so +2 precedes +10",
      "sections orders serving by headroom, most first, with a proof-only account last",
      "sections places every account in exactly one section"
    ],
    "tests/unit/account-state-taxonomy.test.js": [
      "hasCredential reaches the board without the credential doing so derives the flag from the raw row and still removes the token",
      "hasCredential reaches the board without the credential doing so is the gateway own predicate, not a second rule",
      "one word per state, and only one of them is \"Paused\" a 401 that disabled the account asks for a sign-in and never reads as Paused",
      "one word per state, and only one of them is \"Paused\" a 403 that disabled the account is an error, not a sign-in and not a pause",
      "one word per state, and only one of them is \"Paused\" a crossed threshold is a quota pause, named with its window and its line",
      "one word per state, and only one of them is \"Paused\" a row holding no credential says so, active or not",
      "one word per state, and only one of them is \"Paused\" an operator hold is the only state that says Paused, and it says nothing failed",
      "one word per state, and only one of them is \"Paused\" leaves a serving account exactly where it was",
      "one word per state, and only one of them is \"Paused\" the codex reauth path is the same state, read from testStatus alone",
      "the four accounts that were all reading \"Paused\" on 2026-09-11 gives each card a reason drawn from what actually switched it off",
      "the four accounts that were all reading \"Paused\" on 2026-09-11 reads none of them as Paused and files none of them under Cooling down"
    ],
    "tests/unit/account-unavailability-reset-precedence-3075.test.js": [
      "account reset metadata precedence (PR #3075) does not persist or replay a malformed request with future reset metadata",
      "account reset metadata precedence (PR #3075) retains a precise future reset for a genuine 429 rate limit"
    ],
    "tests/unit/activity-analytics-api.test.js": [
      "operator analytics boundary and actual worker leaves source rows unchanged after filtered aggregate reads",
      "operator analytics boundary and actual worker reads both named views through the shared read-only worker",
      "operator analytics boundary and actual worker rejects anonymous and inference-only clients before exposing the ledger",
      "operator analytics boundary and actual worker validates unknown, duplicate and reserved parameters before querying"
    ],
    "tests/unit/activity-analytics.test.js": [
      "analytical workspace read contract does not invent continuous utilization for gaps",
      "analytical workspace read contract does not turn invalid cache quantities into uncached usage {\"cached_tokens\":-2,\"cache_creation_input_tokens\":0}",
      "analytical workspace read contract does not turn invalid cache quantities into uncached usage {\"cached_tokens\":\"invalid\",\"cache_creation_input_tokens\":0}",
      "analytical workspace read contract does not turn invalid cache quantities into uncached usage {\"cached_tokens\":1e999,\"cache_creation_input_tokens\":0}",
      "analytical workspace read contract does not turn invalid cache quantities into uncached usage null",
      "analytical workspace read contract exposes inconsistent cache totals without negative uncached input or invented savings",
      "analytical workspace read contract groups model names within provider and account IDs without label joins",
      "analytical workspace read contract keeps attempts, completion ledger and estimated costs separate",
      "analytical workspace read contract keeps chart, groups, summary and paged rows in the same filtered population",
      "analytical workspace read contract marks limited dimension results instead of presenting top100 as the whole population",
      "analytical workspace read contract parameterizes hostile filter text without widening results",
      "analytical workspace read contract preserves missing cache fields as unknown rather than zero",
      "analytical workspace read contract reads a chosen chart scale, never finer than the point cap allows",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"groupBy\":\"apiKey\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"model\":\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"operation\":\"anything\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"page\":\"2oops\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"page\":0}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"pageSize\":101}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"provider\":[\"claude\"]}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"sortBy\":\"apiKey\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"sortDirection\":\"DESC;SELECT\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"sql\":\"SELECT * FROM settings\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"start\":\"2026-02-30T00:00:00Z\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"start\":\"2026-09-06T12:00:00\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"start\":\"2026-09-07T00:00:00Z\",\"end\":\"2026-09-06T00:00:00Z\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"start\":\"tomorrow\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"status\":\"any\"}",
      "analytical workspace read contract rejects unsupported queries before executing SQL {\"view\":\"sql\"}",
      "analytical workspace read contract reports malformed timestamps and keeps other observations chartable",
      "analytical workspace read contract retains extreme measured latency and distinguishes missing samples",
      "analytical workspace read contract returns explicit empty measurement state",
      "analytical workspace read contract sorts the complete filtered population before pagination and filters outcomes",
      "analytical workspace read contract uses exclusive end boundaries and exact account/provider/model conjunctions",
      "analytical workspace read contract works before context instrumentation exists and never backfills"
    ],
    "tests/unit/activity-interval-selection.test.js": [
      "filters a clicked chart bucket to its contributing population without widening the original range",
      "offers the same interval selection from the keyboard-reachable data table"
    ],
    "tests/unit/adapter-unreachable-provider-2555.test.js": [
      "an unreachable adapter model never enters the chain (#2555) drops a pool entry whose provider has no connection",
      "an unreachable adapter model never enters the chain (#2555) keeps one that is reachable, still ahead of the requested model",
      "an unreachable adapter model never enters the chain (#2555) keeps the reachable half of a mixed pool",
      "an unreachable adapter model never enters the chain (#2555) leaves the list alone when the adapter added nothing",
      "an unreachable adapter model never enters the chain (#2555) never drops a model the user actually asked for",
      "the handler and the credential layer are wired for it a repo failure does not take routing down",
      "the handler and the credential layer are wired for it all three augmentation sites go through the filter",
      "the handler and the credential layer are wired for it auth exposes the reachable-provider set",
      "the handler and the credential layer are wired for it no site still derives adapterAdded by re-filtering the augmented list"
    ],
    "tests/unit/adaptive-stripper.test.js": [
      "adaptiveStripper addRejectedFields / getRejectedFields adds and retrieves rejected fields for provider+model",
      "adaptiveStripper addRejectedFields / getRejectedFields normalizes fields to lowercase",
      "adaptiveStripper extractRejectedFieldNamesFromError extracts field from Groq-style bracketed error",
      "adaptiveStripper extractRejectedFieldNamesFromError parses Groq-style error message",
      "adaptiveStripper extractRejectedFieldNamesFromError parses Mistral-style 422 extra_forbidden error (issue #1649)",
      "adaptiveStripper extractRejectedFieldNamesFromError parses NVIDIA-style Unsupported parameter error with backticks",
      "adaptiveStripper extractRejectedFieldNamesFromError parses unsupported field error",
      "adaptiveStripper stripRejectedFields removes blocked fields from messages",
      "adaptiveStripper stripRejectedFields returns body unchanged when no fields rejected",
      "adaptiveStripper stripRejectedFields returns null when blocklist has no matching fields"
    ],
    "tests/unit/add-account-row-oauth-option-fields.test.js": [
      "offers sign-in for codex, which declares optional workspace fields",
      "still offers sign-in for an OAuth provider that declares no option fields",
      "still sends an API key provider that needs endpoint fields to Connections"
    ],
    "tests/unit/add-account-row-paste-back.test.js": [
      "finishes the grant through manual-code with the state the sign-in issued",
      "finishes the sign-in on Enter in the paste box instead of starting a second one",
      "offers no metadata field before the sign-in, which used to be all a Codex row showed",
      "offers the paste box while the sign-in is still running, not only once it has timed out",
      "states the gateway refusal verbatim when the pasted URL belongs to another sign-in"
    ],
    "tests/unit/add-account-row-provider-change.test.js": [
      "releases the row when the provider changes during a pending grant"
    ],
    "tests/unit/admin-authz-decision-log.test.js": [
      "/api/admin route gate (guard.js) emits through the collector loopback GET /api/admin/health with no key emits AUTHZ.admit by=loopback",
      "/api/admin route gate (guard.js) emits through the collector no credential at all refuses with the frozen 401 wire and logs refused presented=none",
      "/api/admin route gate (guard.js) emits through the collector operator POST from a non-loopback peer refuses with the frozen wire and logs mutation-refused",
      "adminDecision contract admits inference class by each of the three conditions, named",
      "adminDecision contract carries presented/required on the inference-class 401 and keeps the wire",
      "adminDecision contract carries presented=inference/required=operator on the 403 forbidden_class",
      "adminDecision contract marks the non-loopback mutation refusal so the collector names the peer",
      "AUTHZ collector lines (logAdminAuthz) admits by operator and by inference name their discriminator",
      "AUTHZ collector lines (logAdminAuthz) classifies an unproven peer as unstamped rather than trusting its header",
      "AUTHZ collector lines (logAdminAuthz) loopback admit with no key shows by=loopback and all three booleans, peer stamped",
      "AUTHZ collector lines (logAdminAuthz) refused carries presented and required; mutation-refused carries the peer class",
      "AUTHZ collector lines (logAdminAuthz) treats a legacy null decision as an operator admit"
    ],
    "tests/unit/admin-capacity-api.test.js": [
      "passive capacity and eligibility API distinguishes an unobserved account from an absent account",
      "passive capacity and eligibility API redacts storage errors instead of returning credential-bearing exception text",
      "passive capacity and eligibility API redacts storage errors instead of returning credential-bearing exception text",
      "passive capacity and eligibility API rejects malformed query \"\" before reading state",
      "passive capacity and eligibility API rejects malformed query \"provider=claude\" before reading state",
      "passive capacity and eligibility API rejects malformed query \"provider=claude&model=%00\" before reading state",
      "passive capacity and eligibility API rejects malformed query \"provider=claude&model=\" before reading state",
      "passive capacity and eligibility API requires operator authorization and returns the guard refusal before state reads",
      "passive capacity and eligibility API returns 24 eligibility cells with only 3 configured for the requested model",
      "passive capacity and eligibility API returns every account and all 39 stored scopes in one batch without mutation or contact",
      "passive capacity and eligibility API the actual endpoint resolves configured fixture-proxy without provider discovery",
      "passive capacity and eligibility API the actual endpoint resolves configured openai-compatible-fixture-node without provider discovery"
    ],
    "tests/unit/admin-decisions-endpoint.test.js": [
      "GET /api/admin/decisions ANDs cls, verdict, rid and conn filters",
      "GET /api/admin/decisions applies since and rejects a malformed one",
      "GET /api/admin/decisions caps at limit from the tail, newest-last",
      "GET /api/admin/decisions refuses when requireAdmin refuses",
      "GET /api/admin/decisions returns empty records when the sink does not exist yet",
      "GET /api/admin/decisions serves what decide() wrote, through the same sink path",
      "GET /api/admin/decisions skips a corrupt line instead of failing the request"
    ],
    "tests/unit/admin-eligibility-evidence.test.js": [
      "passive account and model evidence blocks account-specific disables even without a provider-wide list",
      "passive account and model evidence distinguishes enforced model disables from a provider flag not enforced on authenticated accounts",
      "passive account and model evidence distinguishes explicit local permission from unknown provider entitlement",
      "passive account and model evidence does not mutate inputs or publish arbitrary stored fields",
      "passive account and model evidence honors explicit empty account overrides while siblings inherit provider disables",
      "passive account and model evidence never advances an expired reported deadline in displayed evidence",
      "passive account and model evidence never promotes a credential check or default active status to model verification",
      "passive account and model evidence preserves all accounts and explains simultaneous blockers without leaking errors",
      "passive account and model evidence reports pause thresholds and recorded model exhaustion as stored gates",
      "passive account and model evidence uses provider defaults for no-auth routing instead of a fabricated account override",
      "passive account and model evidence uses the real scoped quota rule and keeps unknown windows fail-open",
      "passive account and model evidence uses the shared alias-normalized account model-disable gate"
    ],
    "tests/unit/admin-eligibility-node-policy.test.js": [
      "passive eligibility follows configured provider-node routing does not invent a node alias from model names or unrecognized node types",
      "passive eligibility follows configured provider-node routing gives configured prefixes precedence over built-in aliases without admitting built-in accounts",
      "passive eligibility follows configured provider-node routing honors account empty overrides across node-id and prefix spellings",
      "passive eligibility follows configured provider-node routing keeps a node's global disable separate from the built-in provider whose alias it shadows",
      "passive eligibility follows configured provider-node routing resolves additional static aliases through the actual engine registry",
      "passive eligibility follows configured provider-node routing uses the selector's node-type precedence when multiple nodes share a prefix",
      "passive eligibility follows configured provider-node routing uses the verified node prefix's disable policy for fixture-proxy",
      "passive eligibility follows configured provider-node routing uses the verified node prefix's disable policy for openai-compatible-fixture-node"
    ],
    "tests/unit/admin-quota-evidence.test.js": [
      "passive quota evidence projection derives only explicitly named durations and keeps passed deadlines unchanged",
      "passive quota evidence projection does not identify an unknown synthetic denominator as actual entitlement",
      "passive quota evidence projection does not label a future observation as fresh evidence",
      "passive quota evidence projection does not turn \"   \" into measured zero",
      "passive quota evidence projection does not turn \"\" into measured zero",
      "passive quota evidence projection does not turn \"broken\" into measured zero",
      "passive quota evidence projection does not turn {} into measured zero",
      "passive quota evidence projection does not turn false into measured zero",
      "passive quota evidence projection does not turn null into measured zero",
      "passive quota evidence projection does not turn null into measured zero",
      "passive quota evidence projection does not turn undefined into measured zero",
      "passive quota evidence projection keeps separately sourced percentages and excludes credential-bearing extras",
      "passive quota evidence projection preserves real zeros and does not reclassify an old measurement as an estimate"
    ],
    "tests/unit/admission-api.test.js": [
      "persisted admission operator controls persists the validated policy and reports effective override and process ownership",
      "persisted admission operator controls rejects invalid operator bounds without mutating policy"
    ],
    "tests/unit/admission-identity.test.js": [
      "does not share a result between independent validators",
      "uses the recognized key after invalid first credentials and reuses only admission identity"
    ],
    "tests/unit/admission-key-gate-decisions.test.js": [
      "chat.js API-key gate an authenticated caller takes a concurrency slot, not a window hit",
      "chat.js API-key gate D-10: a presented-but-invalid key gets REQ.refused why=key-invalid",
      "chat.js API-key gate D-10: the key-gate refusal also emits REQ.refused with the same rid as the ADM line",
      "chat.js API-key gate key-invalid: a presented key that fails validation -> presented=true, key never appears",
      "chat.js API-key gate key-required: env-only tightening -> source=env",
      "chat.js API-key gate key-required: no key presented, setting fired -> source=setting presented=false, no AUTH warn"
    ],
    "tests/unit/admission-media-cancellation.test.js": [
      "real media core transport cancellation with mock upstream embeddings fetch inherits caller signal and cannot retry after abort",
      "real media core transport cancellation with mock upstream rerank fetch inherits caller signal and cannot retry after abort",
      "real media core transport cancellation with mock upstream stt fetch inherits caller signal and cannot retry after abort",
      "real media core transport cancellation with mock upstream tts fetch inherits caller signal and cannot retry after abort"
    ],
    "tests/unit/agent-role-model-groups-1092.test.js": [
      "detection goes through the shared clientTool detector (#1092) a client the detector maps to a different tool gets null, same as unknown",
      "detection goes through the shared clientTool detector (#1092) still recognises Claude Code by its detector-mapped identity, not a private string",
      "narrowing a combo to the role's group (#1092) falls back to the whole combo rather than routing nowhere",
      "narrowing a combo to the role's group (#1092) gives a sub-agent its group",
      "narrowing a combo to the role's group (#1092) gives the parent its group",
      "narrowing a combo to the role's group (#1092) is inert when the role is unknown",
      "narrowing a combo to the role's group (#1092) is inert with no groups configured",
      "narrowing a combo to the role's group (#1092) keeps the combo's own order, not the group's",
      "narrowing a combo to the role's group (#1092) leaves an empty or non-list input alone",
      "telling a sub-agent from the parent (#1092) calls the main loop the parent",
      "telling a sub-agent from the parent (#1092) is null for a client whose preamble is not known",
      "telling a sub-agent from the parent (#1092) only matches the preamble at the start, not a mention further in",
      "telling a sub-agent from the parent (#1092) reads it from an OpenAI-shaped system message",
      "telling a sub-agent from the parent (#1092) reads it from Anthropic system blocks",
      "telling a sub-agent from the parent (#1092) reads the sub-agent preamble from a top-level system string",
      "telling a sub-agent from the parent (#1092) treats an unrecognised preamble as the parent, which is the old behaviour"
    ],
    "tests/unit/ai-sdk-image-part-1330.test.js": [
      "AI SDK image parts survive OpenAI-input translation (#1330) Claude: becomes a proper image content block",
      "AI SDK image parts survive OpenAI-input translation (#1330) Gemini (formats/gemini.js convertOpenAIContentToParts): becomes inlineData",
      "AI SDK image parts survive OpenAI-input translation (#1330) Kiro: lands in userInputMessage.images, not text",
      "AI SDK image parts survive OpenAI-input translation (#1330) Ollama: raw base64 lands in message.images[]",
      "AI SDK image parts survive OpenAI-input translation (#1330) OpenAI Responses: becomes input_image",
      "extractAiSdkImageUrl (#1330) does not shadow the Claude-shaped passthrough block",
      "extractAiSdkImageUrl (#1330) reads the AI SDK image string",
      "extractAiSdkImageUrl (#1330) returns null for anything else"
    ],
    "tests/unit/alibaba-token-plan-provider.test.js": [
      "Alibaba Token Plan provider does not collide with the other three Alibaba key types",
      "Alibaba Token Plan provider exposes the models the plan actually serves",
      "Alibaba Token Plan provider is registered as an OpenAI-compatible apikey provider",
      "Alibaba Token Plan provider keeps every registry id unique after adding the provider",
      "Alibaba Token Plan provider targets the Singapore Token Plan host in compatible mode"
    ],
    "tests/unit/alicode-cache-control-2069.test.js": [
      "filterToOpenAIFormat cache_control handling (#2069) always strips signature regardless of preserveCacheControl",
      "filterToOpenAIFormat cache_control handling (#2069) does not add cache_control when block had none (preserveCacheControl: true)",
      "filterToOpenAIFormat cache_control handling (#2069) preserves cache_control when preserveCacheControl option is true (alicode/DashScope)",
      "filterToOpenAIFormat cache_control handling (#2069) strips cache_control by default (all standard OpenAI providers)"
    ],
    "tests/unit/alicode-intl-endpoint-2591.test.js": [
      "alicode-intl endpoint (Coding Plan keys) keeps the chat/completions path and preserveCacheControl quirk",
      "alicode-intl endpoint (Coding Plan keys) routes to the coding-intl host for Coding Plan keys",
      "alims-intl endpoint (standard DashScope keys) does not use the coding-intl host that rejects standard keys",
      "alims-intl endpoint (standard DashScope keys) routes to the compatible-mode DashScope endpoint for standard keys"
    ],
    "tests/unit/already-shipped-capabilities-2.test.js": [
      "#1035 the container entrypoint is the wrapped server, not a bare next server",
      "#1136 fetches live model lists from providers that offer one",
      "#1192 no importer can silently drop rows, because none reads outside state",
      "#1525 can disable a model per provider",
      "#1526 transcribes through NVIDIA",
      "#2357 sends anthropic-version lowercase, so it cannot duplicate",
      "#2435 resolves target format and transport together",
      "#2472 writes no request log, so nothing accumulates on disk",
      "#2487 pins a Cursor client version rather than an outdated default",
      "#2616 no longer lists the NVIDIA ids that were withdrawn",
      "#2712 keys the byApiKey aggregate by a masked key",
      "#2759 replays a JetBrains h2c upgrade over HTTP/1.1"
    ],
    "tests/unit/already-shipped-capabilities-3.test.js": [
      "#1050 a non-SSE upstream body is short-circuited before the stall timer",
      "#1087 #1126 a malformed request does not lock the account",
      "#1188 antigravity is a normal oauth provider, not hidden",
      "#1276 thinking blocks carrying a foreign signature are dropped",
      "#1632 the CLI build handles the nested standalone layout",
      "#1877 fusion degrades on a thin panel instead of failing outright",
      "#1905 #1982 the heap cap is overridable, not hardcoded",
      "#2021 mimo-v2.5-free is multimodal"
    ],
    "tests/unit/already-shipped-capabilities-4.test.js": [
      "#2083 OpenRouter is asked to fall back off a broken upstream",
      "#2376 cloudflare-ai messages are flattened to text",
      "#2458 a missing stream key means non-streaming",
      "#2876 a fusion panel body is closed with a user turn",
      "#2936 passthrough streams normalize reasoning to reasoning_content",
      "#3003 qoder accepts a PAT as well as OAuth",
      "#3308 a null or non-string tool arguments value is coerced"
    ],
    "tests/unit/already-shipped-capabilities.test.js": [
      "a non-SSE upstream body is not piped as a stream (#2039) checks the content type before piping, which is what crashed",
      "Codebuddy quota expiration ordering (#2981) separates a cycle reset from a resource expiry",
      "CommandCode never sends stream=false upstream (#1840) no longer needs to force streaming, because the endpoint changed",
      "CommandCode never sends stream=false upstream (#1840) still honours forceStream for a provider that does declare it",
      "DeepSeek reasoning_content is echoed back (#1543) carries assistant reasoning across the pivot in every request translator",
      "Headroom compression integration (#1871) ships both the compression hook and the install detection",
      "installation troubleshooting guide (#3093) ships the document the issue asks for, with real sections",
      "New Models Discovery covers every provider (#3603) tracks seen models in a repo wired to a route and a dashboard control",
      "proxy pool rotation (#3354) picks a pool member by strategy from the live account-selection path",
      "quota safety-margin auto-pause (#3583) exposes the pause predicate and consults it on the routing path",
      "thinking level bound to the model id (#2973) derives the effort from the id's suffix before dispatch"
    ],
    "tests/unit/always-protected-message-933.test.js": [
      "the always-protected gate explains itself (#933) deliberately does not honour requireLogin=false there",
      "the always-protected gate explains itself (#933) says why, instead of a bare Unauthorized",
      "the always-protected gate explains itself (#933) still gates the dangerous routes on a real identity",
      "the always-protected gate explains itself (#933) the export route keeps its second factor"
    ],
    "tests/unit/analytics-diagnostics.test.js": [
      "bounded analytics diagnostics distinguishes EACCES without paths",
      "bounded analytics diagnostics distinguishes ENOENT without paths",
      "bounded analytics diagnostics distinguishes SQLITE_BUSY without paths",
      "bounded analytics diagnostics distinguishes SQLITE_CORRUPT without paths",
      "bounded analytics diagnostics does not accept arbitrary values as diagnostic fields",
      "bounded analytics diagnostics retains safe operation, failure phase and engine code"
    ],
    "tests/unit/anthropic-advisor-beta-2582.test.js": [
      "Anthropic Advisor beta flag (#2582) includes it unconditionally, not gated to opus/sonnet like the heavy-agent flags",
      "Anthropic Advisor beta flag (#2582) includes the advisor-tool beta flag for a Claude Code main model"
    ],
    "tests/unit/antigravity-autoping-2564.test.js": [
      "an account-level refusal does not walk the other families (#2564) classifies auth and rate-limit statuses as being about the account",
      "an account-level refusal does not walk the other families (#2564) does not continue to another family after an uncertain transport outcome",
      "an account-level refusal does not walk the other families (#2564) retains uncertainty when the first family throws without sending another family",
      "an account-level refusal does not walk the other families (#2564) stops after a 401 instead of poking the next family",
      "an account-level refusal does not walk the other families (#2564) stops after a 403 instead of poking the next family",
      "an account-level refusal does not walk the other families (#2564) stops after a 429 instead of poking the next family",
      "Antigravity auto-ping is opt-in (#2564) makes no request at all when the setting is absent",
      "Antigravity auto-ping is opt-in (#2564) makes no request when the connection is listed but switched off",
      "Antigravity auto-ping is opt-in (#2564) pokes only the cold quota family once the connection is switched on",
      "Antigravity auto-ping is opt-in (#2564) records the governing reset, not whichever family came first in the map",
      "Antigravity auto-ping is opt-in (#2564) ships a settings-gated entry rather than an always-on one",
      "resolveQuotaEntry (#2564) does not let the order of the key list decide the governing window",
      "resolveQuotaEntry (#2564) keeps the single-key path free of the reset filter",
      "resolveQuotaEntry (#2564) picks the earliest reset when the quota key is a set of models",
      "resolveQuotaEntry (#2564) resolves Claude's single named window exactly as a plain lookup did",
      "resolveQuotaEntry (#2564) resolves Codex's single named window exactly as a plain lookup did",
      "resolveQuotaEntry (#2564) resolves nothing when no family in the set has a reset",
      "resolveQuotaEntry (#2564) skips a family whose reset is missing or unparseable"
    ],
    "tests/unit/antigravity-cache.test.js": [
      "Antigravity cache behavior (real API) codex-style sessionId vs random sessionId on unique prompt",
      "Antigravity cache behavior (real API) cross-account → cache SHARED (content-based global cache)",
      "Antigravity cache behavior (real API) different sessionId (same account) → cache still hits (session-independent)",
      "Antigravity cache behavior (real API) has at least one active AG connection with refreshToken",
      "Antigravity cache behavior (real API) same sessionId → cache hit on repeated call",
      "Antigravity cache behavior (real API) unique prompt (never seen) → explore when cache starts hitting"
    ],
    "tests/unit/antigravity-chat-validation-2632.test.js": [
      "Antigravity chat-host verification 403 (#2632) classifies a VALIDATION_REQUIRED 403 raised by the chat host",
      "Antigravity chat-host verification 403 (#2632) still refuses a 403 from an unrelated domain",
      "Antigravity chat-host verification 403 (#2632) still refuses a non-verification 403 from the chat host",
      "Antigravity chat-host verification 403 (#2632) surfaces the recovery path through the executor's parseError",
      "Antigravity chat-host verification 403 (#2632) the chat transport really is the daily host"
    ],
    "tests/unit/antigravity-claude-image-3148.test.js": [
      "a Claude image survives the Antigravity envelope (#3148) carries a url image as fileData",
      "a Claude image survives the Antigravity envelope (#3148) carries an OpenAI-shaped image on the claude path too (#2014)",
      "a Claude image survives the Antigravity envelope (#3148) carries base64 image data through",
      "a Claude image survives the Antigravity envelope (#3148) drops nothing when the image block carries no usable source",
      "a Claude image survives the Antigravity envelope (#3148) keeps the accompanying text",
      "a Claude image survives the Antigravity envelope (#3148) leaves a non-claude model on the gemini path untouched",
      "a Claude image survives the Antigravity envelope (#3148) the gemini-model path already carried images, which is why this was model-specific",
      "a Claude image survives the Antigravity envelope (#3148) uses the inlineData shape the rest of the translator builds"
    ],
    "tests/unit/antigravity-diagnostic-sinks.test.js": [
      "Antigravity diagnostic sinks does not log an opaque credential-refresh diagnostic",
      "Antigravity diagnostic sinks does not retain an opaque parsed upstream diagnostic"
    ],
    "tests/unit/antigravity-executor-coverage.test.js": [
      "AntigravityExecutor.cloakTools github-copilot mode drops client copies of native/decoy names entirely",
      "AntigravityExecutor.cloakTools returns the body untouched when no tools are present",
      "AntigravityExecutor.cloakTools suffixes client tools, preserves native names, appends deduped decoys, and maps back",
      "buildUrl forces non-streaming for image models and falls back past a bad urlIndex",
      "buildUrl streams via streamGenerateContent, non-stream via generateContent",
      "computeRetryDelay backs off exponentially on transient statuses",
      "computeRetryDelay uses body reset time when headers carry none, vetoing beyond the cap",
      "computeRetryDelay vetoes non-transient statuses with non-transient messages",
      "id generators generateProjectId yields adj-noun-hex5 and generateSessionId is uuid+epoch",
      "parseError replaces upstream text with the safe message and attaches no validation for plain 500",
      "parseError tolerates unparseable JSON bodies",
      "parseRetryHeaders parses retry-after as an HTTP date (future and past)",
      "parseRetryHeaders parses x-ratelimit-reset-after seconds and x-ratelimit-reset epoch",
      "parseRetryHeaders returns null for header-less objects and empty headers",
      "refreshCredentials (mocked token endpoint) exchanges the refresh token and keeps the old one if none returned",
      "refreshCredentials (mocked token endpoint) returns null without a refresh token, on non-ok, and on fetch throw",
      "transformRequest — image models allowlists only text and well-formed inlineData parts",
      "transformRequest — image models derives aspect ratio from a WxH resolution suffix and strips it from the model",
      "transformRequest — image models takes a small WxH suffix as a literal aspect ratio",
      "transformRequest — standard requests forces functionResponse turns to role user and backfills thoughtSignature on functionCall",
      "transformRequest — standard requests merges tool groups, sanitizes names, and defaults empty parameters",
      "transformRequest — standard requests passes through a client requestId already in IDE format"
    ],
    "tests/unit/antigravity-gemini-36-flash-2757.test.js": [
      "ag/gemini-3.6-flash (#2757) does not become the provider default",
      "ag/gemini-3.6-flash (#2757) is a routable model id",
      "ag/gemini-3.6-flash (#2757) is listed once, so the catalogue shows it beside the tiers",
      "ag/gemini-3.6-flash (#2757) leaves gemini-3.6-flash-high resolving to gemini-3.6-flash-tiered(high)",
      "ag/gemini-3.6-flash (#2757) leaves gemini-3.6-flash-low resolving to gemini-3.6-flash-tiered(low)",
      "ag/gemini-3.6-flash (#2757) leaves gemini-3.6-flash-medium resolving to gemini-3.6-flash-tiered(medium)",
      "ag/gemini-3.6-flash (#2757) never reaches upstream under the bare GA name"
    ],
    "tests/unit/antigravity-hotreload-sinks.test.js": [
      "Antigravity hot-reload diagnostic sink does not expose an opaque refresh diagnostic"
    ],
    "tests/unit/antigravity-ide-version-sync.test.js": [
      "Antigravity IDE version single source mitm override version matches the provider pin",
      "Antigravity IDE version single source user agent template carries the pinned version"
    ],
    "tests/unit/antigravity-image-editing.test.js": [
      "Antigravity image editing requests drops a non-array contents wrapper without throwing",
      "Antigravity image editing requests drops a non-array nested contents wrapper without throwing",
      "Antigravity image editing requests drops invalid text and inline image payloads",
      "Antigravity image editing requests drops non-array message parts without throwing",
      "Antigravity image editing requests drops non-object messages without throwing",
      "Antigravity image editing requests drops non-object parts without throwing",
      "Antigravity image editing requests drops unsupported parts and strips every field outside the outbound allowlist",
      "Antigravity image editing requests normalizes both MIME spellings and prefers valid camelCase",
      "Antigravity image editing requests preserves an OpenAI data URI through Gemini translation and Antigravity execution",
      "Antigravity image editing requests preserves decoded inline image data in the outbound image request",
      "Antigravity image editing requests preserves text and inline image order from nested request contents",
      "Antigravity image editing requests preserves valid parts across multiple messages",
      "Antigravity image editing requests sanitizes nested inline data while preferring valid request contents"
    ],
    "tests/unit/antigravity-image-size-2a9213.test.js": [
      "antigravity image adapter model resolution appends the aspect-ratio suffix the executor parses",
      "antigravity image adapter model resolution does not double-append a suffix the model already carries",
      "antigravity image adapter model resolution keeps the plain model when no size is given",
      "antigravity image adapter model resolution keeps the plain model when size is not a usable string",
      "antigravity image adapter model resolution maps a portrait size to its own ratio",
      "antigravity image adapter model resolution rejects a non-image selection before any provider dispatch",
      "antigravity image adapter model resolution still passes the prompt and input image through untouched"
    ],
    "tests/unit/antigravity-mitm-router-auth-1295.test.js": [
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) forwards upstream when the request body cannot be parsed",
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) forwards upstream when the router refuses this proxy's key (401)",
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) forwards upstream when the router refuses this proxy's key (403)",
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) pipes a served response through instead of passing it through",
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) relays a provider error unchanged, since only 401/403 are this proxy's own gate",
      "Antigravity MITM handler never hands the IDE an auth failure (#1295) still answers when no forwarder is supplied, rather than hanging"
    ],
    "tests/unit/antigravity-model-key-2846.test.js": [
      "Antigravity routes its friendly synonym onto the real model key (#2846) gemini-3.5-flash-high resolves upstream to gemini-3-flash-agent",
      "Antigravity routes its friendly synonym onto the real model key (#2846) the mitm synonym map still declares the same mapping",
      "Antigravity routes its friendly synonym onto the real model key (#2846) the real key still routes to itself",
      "Antigravity routes its friendly synonym onto the real model key (#2846) the synonym is still selectable — nothing was removed",
      "Antigravity routes its friendly synonym onto the real model key (#2846) tiered entries keep their preset suffix"
    ],
    "tests/unit/antigravity-nonstream-usage-3260.test.js": [
      "#3260 non-streaming usage extraction for enveloped Gemini responses leaves the OpenAI and Claude shapes alone",
      "#3260 non-streaming usage extraction for enveloped Gemini responses prefers the top-level metadata when both are present",
      "#3260 non-streaming usage extraction for enveloped Gemini responses reads usageMetadata out of the antigravity { response } envelope",
      "#3260 non-streaming usage extraction for enveloped Gemini responses returns null when there is no usage anywhere",
      "#3260 non-streaming usage extraction for enveloped Gemini responses still reads a top-level usageMetadata"
    ],
    "tests/unit/antigravity-normalize-contents-3214.test.js": [
      "Antigravity thought-filtered conversation normalization merges same-role turns made adjacent when a thought-only turn is removed"
    ],
    "tests/unit/antigravity-oauth-client.test.js": [
      "antigravity oauth client (deduped) google client shared by gemini + gemini-cli",
      "antigravity oauth client (deduped) registry transport keeps clientId/clientSecret",
      "antigravity oauth client (deduped) shared source holds the canonical credentials",
      "antigravity oauth client (deduped) src oauth.js imports shared client + keeps full shape"
    ],
    "tests/unit/antigravity-oauth-provider-coverage.test.js": [
      "buildAuthUrl builds the authorize URL from the config with offline consent params",
      "exchangeToken POSTs the code with client credentials to the config token URL",
      "exchangeToken throws with the upstream body text on a non-ok response",
      "mapTokens maps snake_case token fields and folds in postExchange extras",
      "mapTokens tolerates a missing extras bag",
      "postExchange accepts a string cloudaicompanionProject with the default tier",
      "postExchange onboarding loop breaks on a thrown onboard fetch without surfacing",
      "postExchange onboarding retries when the onboard response is non-ok",
      "postExchange returns an empty projectId when loadCodeAssist is non-ok, and swallows a thrown load",
      "postExchange returns userInfo and the default-tier projectId, then onboards until done"
    ],
    "tests/unit/antigravity-opencode-branding-dff64849.test.js": [
      "Antigravity system prompt: opencode branding rewrite (dff648496) leaves a prompt without the brand untouched",
      "Antigravity system prompt: opencode branding rewrite (dff648496) preserves TitleCase: OpenCode -> Antigravity",
      "Antigravity system prompt: opencode branding rewrite (dff648496) preserves UPPERCASE: OPENCODE -> ANTIGRAVITY",
      "Antigravity system prompt: opencode branding rewrite (dff648496) rewrites every occurrence in one prompt, each keeping its own casing",
      "Antigravity system prompt: opencode branding rewrite (dff648496) rewrites lowercase opencode to antigravity",
      "Antigravity system prompt: opencode branding rewrite (dff648496) still applies the Hermes sanitizer alongside the rewrite",
      "Antigravity system prompt: opencode branding rewrite (dff648496) still strips the Zed Claude-agent prompt alongside the rewrite"
    ],
    "tests/unit/antigravity-prefill-strip-2321.test.js": [
      "Antigravity Claude never receives a trailing assistant turn (#2321) closes a trailing assistant tool_use with a user tool_result",
      "Antigravity Claude never receives a trailing assistant turn (#2321) handles an empty conversation without throwing",
      "Antigravity Claude never receives a trailing assistant turn (#2321) leaves a conversation already ending with a user turn alone",
      "Antigravity Claude never receives a trailing assistant turn (#2321) reaches the Antigravity envelope with a user turn last",
      "Antigravity Claude never receives a trailing assistant turn (#2321) resolves a trailing assistant text prefill to a user turn"
    ],
    "tests/unit/antigravity-project-outcome.test.js": [
      "Antigravity project verification outcomes delivers one typed validation outcome and observation to late waiters",
      "Antigravity project verification outcomes does not let a released same-ID operation overwrite its replacement cache",
      "Antigravity project verification outcomes does not notify hooks from a cached project result",
      "Antigravity project verification outcomes does not notify hooks when project lookup fails",
      "Antigravity project verification outcomes does not report success after its connection has been released",
      "Antigravity project verification outcomes evicts cached project values at the connection lifecycle boundary",
      "Antigravity project verification outcomes isolates a throwing validation waiter from another waiter",
      "Antigravity project verification outcomes keeps typed outcomes independent for different connections",
      "Antigravity project verification outcomes notifies every waiter when a project probe succeeds",
      "Antigravity project verification outcomes reports a strict loadCodeAssist challenge to its initiator",
      "Antigravity project verification outcomes uses the first initiator challenge snapshot for project success",
      "Antigravity project verification outcomes uses the fixed Antigravity message for an opaque loadCodeAssist 500 diagnostic",
      "Antigravity project verification outcomes uses the fixed Antigravity message for an opaque project transport diagnostic"
    ],
    "tests/unit/antigravity-quota-gemini-3.6.test.js": [
      "Antigravity quota tracker: Gemini 3.6 Flash usage bars returns Gemini 3.6 Flash tier quotas so the dashboard can render usage bars",
      "Antigravity quota tracker: Gemini 3.6 Flash usage bars still filters out internal and non-important models"
    ],
    "tests/unit/antigravity-quota-gemini-3.7.test.js": [
      "Antigravity quota tracker: Gemini 3.7 Flash usage bars returns Gemini 3.7 Flash tier quotas so the dashboard can render usage bars"
    ],
    "tests/unit/antigravity-quota-registry-1609.test.js": [
      "Antigravity quota tracker reads the registry (#1609) admits every registry model id, so the two cannot drift again",
      "Antigravity quota tracker reads the registry (#1609) bills the quota read against the project stored on the connection",
      "Antigravity quota tracker reads the registry (#1609) does not invent a bucket for an alias whose wire id upstream never reports",
      "Antigravity quota tracker reads the registry (#1609) falls back to the loadCodeAssist project when the connection has none",
      "Antigravity quota tracker reads the registry (#1609) keeps a bucket for a registry id the hand-copied list had dropped",
      "Antigravity quota tracker reads the registry (#1609) mirrors an alias id onto the upstream bucket it shares",
      "Antigravity quota tracker reads the registry (#1609) still drops internal and unrouted models",
      "Antigravity quota tracker reads the registry (#1609) still refuses an upstream display name that is not the registry name"
    ],
    "tests/unit/antigravity-quota-skip-3561.test.js": [
      "getExhaustedQuotaWindow fails open with no snapshot, a malformed snapshot, or no model",
      "getExhaustedQuotaWindow reports the exhausted pair with the upstream reset time",
      "getExhaustedQuotaWindow stays silent at 0% with no reset time, the shape a model with no quotaInfo takes",
      "getExhaustedQuotaWindow stays silent for a provider whose quota windows are not model-keyed",
      "getExhaustedQuotaWindow stays silent for another model on the same account",
      "getExhaustedQuotaWindow stays silent on an unlimited window",
      "getExhaustedQuotaWindow stays silent once the reset time has passed",
      "selection reads the exhausted pair as unavailable lets a standing timed lock keep precedence over the quota reading",
      "selection reads the exhausted pair as unavailable reports retry timing from the earliest cached reset when every account is out",
      "selection reads the exhausted pair as unavailable routes past the exhausted account to one that still has the model",
      "selection reads the exhausted pair as unavailable skips only the exhausted model and answers with its reset time",
      "selection reads the exhausted pair as unavailable still serves the exhausted account for a model it has quota for",
      "the quota key is derivable without a live account carries the registry model id from the usage payload into the snapshot key"
    ],
    "tests/unit/antigravity-retry-hook.test.js": [
      "antigravity computeRetryDelay hook (D3) 503 without retry info → transient backoff",
      "antigravity computeRetryDelay hook (D3) buildHeaders matches official IDE stream headers",
      "antigravity computeRetryDelay hook (D3) deduplicates sanitized tool names",
      "antigravity computeRetryDelay hook (D3) does not hold an aborted request while inspecting retry diagnostics",
      "antigravity computeRetryDelay hook (D3) does not retry non-transient 400 errors",
      "antigravity computeRetryDelay hook (D3) exponential backoff for 429 when no retry info",
      "antigravity computeRetryDelay hook (D3) parses retry time from error body when no header",
      "antigravity computeRetryDelay hook (D3) registry uses the daily IDE cloudcode host and user agent",
      "antigravity computeRetryDelay hook (D3) retries Antigravity agent terminated body even when status is not 429",
      "antigravity computeRetryDelay hook (D3) retries high traffic body",
      "antigravity computeRetryDelay hook (D3) transforms chat requests with official IDE requestId shape and 64000 token cap",
      "antigravity computeRetryDelay hook (D3) uses Retry-After header (seconds → ms) when within cap",
      "antigravity computeRetryDelay hook (D3) vetoes (false) when Retry-After exceeds cap"
    ],
    "tests/unit/antigravity-retry-response.test.js": [
      "Antigravity refreshed response replacement maps a retry abort",
      "Antigravity refreshed response replacement maps a retry typed timeout",
      "Antigravity refreshed response replacement maps other retry transport failures without resurrecting the original 401",
      "Antigravity refreshed response replacement preserves ordinary-provider transport error formatting",
      "Antigravity refreshed response replacement replaces the original response with a generic retry HTTP error",
      "Antigravity refreshed response replacement replaces the original response with a retry 403 for typed error parsing",
      "Antigravity refreshed response replacement replaces URL headers body format and target log after retry success",
      "Antigravity refreshed response replacement uses the fixed Antigravity message for parsed upstream diagnostics in every chatCore sink",
      "Antigravity refreshed response replacement uses the fixed Antigravity message for transport diagnostics in every chatCore sink"
    ],
    "tests/unit/antigravity-schema-cleaner.test.js": [
      "cleanJSONSchemaForAntigravity preserves schema-keyword names used as object properties"
    ],
    "tests/unit/antigravity-sse-validation-coverage.test.js": [
      "classifyAntigravityJsonValidation classifies a validation payload and returns null for safe or invalid JSON",
      "classifyAntigravityJsonValidation falls back to the transport status when the payload carries none",
      "classifyAntigravitySseOutcome / classifyAntigravitySseValidation classifies a generic error frame and returns null for safe traffic",
      "classifyAntigravitySseOutcome / classifyAntigravitySseValidation finds a validation event across complete frames, CRLF included",
      "classifyAntigravitySseOutcome / classifyAntigravitySseValidation ignores frames without data lines and non-JSON data",
      "classifyAntigravitySseOutcome / classifyAntigravitySseValidation inspects a trailing partial frame only when includeTrailing is on",
      "createAntigravitySseValidationGate classifies trailing bytes at EOF: validation, error, and safe passthrough",
      "createAntigravitySseValidationGate fails safe on an oversized complete frame and on an oversized unbounded buffer",
      "createAntigravitySseValidationGate fails safe when the upstream reader throws mid-stream",
      "createAntigravitySseValidationGate passes safe events through and closes at upstream EOF",
      "createAntigravitySseValidationGate propagates cancel to the upstream reader",
      "createAntigravitySseValidationGate terminates with the safe message on a generic error event and calls the hook",
      "createAntigravitySseValidationGate terminates with the verification message on a validation event and calls the hook",
      "createSseTextStream emits the text once and closes",
      "readBoundedAntigravityJson concatenates chunks to EOF and decodes them",
      "readBoundedAntigravityJson skips empty chunks and reports an oversized body as exceeded"
    ],
    "tests/unit/antigravity-stream-options.test.js": [
      "AntigravityExecutor stream_options normalization preserves stream_options for a streaming request",
      "AntigravityExecutor stream_options normalization removes stream_options from a non-streaming request"
    ],
    "tests/unit/antigravity-terminal-verification.test.js": [
      "Antigravity terminal verification success classifies a complete Antigravity JSON RPC validation body before stream sinks",
      "Antigravity terminal verification success classifies a split Antigravity JSON RPC validation body before returning a stream",
      "Antigravity terminal verification success defers Antigravity account-health success until terminal stream completion",
      "Antigravity terminal verification success does not clear Antigravity account health before rejecting empty 200 JSON",
      "Antigravity terminal verification success does not notify at empty EOF",
      "Antigravity terminal verification success does not notify from abandoned streams",
      "Antigravity terminal verification success does not notify from an aborted completion",
      "Antigravity terminal verification success does not notify from an empty non-stream response",
      "Antigravity terminal verification success does not notify from malformed forced SSE-to-JSON output",
      "Antigravity terminal verification success does not notify from malformed non-stream JSON",
      "Antigravity terminal verification success gates a later split Antigravity validation frame before stream sinks",
      "Antigravity terminal verification success gates Antigravity 200 JSON validation before non-stream sinks",
      "Antigravity terminal verification success gates Antigravity validation before dynamic non-stream SSE sinks",
      "Antigravity terminal verification success gates Antigravity validation before forced SSE-to-JSON conversion sinks",
      "Antigravity terminal verification success notifies at non-aborted terminal output-token completion",
      "Antigravity terminal verification success notifies at non-aborted terminal text completion",
      "Antigravity terminal verification success notifies at non-aborted terminal thinking completion",
      "Antigravity terminal verification success notifies terminal verification at most once",
      "Antigravity terminal verification success notifies verification after useful forced SSE-to-JSON output",
      "Antigravity terminal verification success notifies verification only after useful non-stream output",
      "Antigravity terminal verification success preflights a later Antigravity generic SSE error before every stream sink",
      "Antigravity terminal verification success preflights a normal Antigravity data frame before it can reach stream sinks",
      "Antigravity terminal verification success preflights an initial Antigravity generic SSE error before every stream sink",
      "Antigravity terminal verification success redacts disguised HTTP-200 structured errors before non-stream sinks",
      "Antigravity terminal verification success rejects a thought-only Antigravity STOP frame before it reaches the client",
      "Antigravity terminal verification success replays buffered Antigravity thought frames once visible output follows",
      "Antigravity terminal verification success treats a forced Antigravity structural candidates-empty SSE frame as ordinary empty output",
      "Antigravity terminal verification success uses a fixed Antigravity error for an opaque non-SSE title",
      "Antigravity terminal verification success uses a fixed Antigravity error for an opaque stream-read exception",
      "Antigravity terminal verification success uses a fixed Antigravity error for an opaque structured streaming failure",
      "Antigravity terminal verification success uses the fixed Antigravity message for a 200 non-stream JSON diagnostic",
      "Antigravity terminal verification success uses the fixed Antigravity message for a forced SSE opaque error",
      "Antigravity terminal verification success uses the fixed Antigravity message when forced SSE reading throws an opaque diagnostic",
      "Antigravity terminal verification success uses the fixed Antigravity message when non-stream SSE reading throws an opaque diagnostic"
    ],
    "tests/unit/antigravity-think-shell-1061.test.js": [
      "Antigravity loop-detection shells (#1061) never emits a visible part that is only a marker, even with no text block",
      "Antigravity loop-detection shells (#1061) persists no <think> shell for a single thinking block",
      "Antigravity loop-detection shells (#1061) persists no accumulated shells across four thinking blocks",
      "Antigravity loop-detection shells (#1061) still delivers the reasoning itself on the thought channel"
    ],
    "tests/unit/antigravity-to-openai-request.test.js": [
      "generationConfig → OpenAI sampling and token fields bumps max_tokens to DEFAULT_MIN_TOKENS when tools are in play (truncated tool args cost a retry)",
      "generationConfig → OpenAI sampling and token fields maps maxOutputTokens to max_tokens verbatim when no tools are present",
      "generationConfig → OpenAI sampling and token fields maps thinkingBudget tiers to reasoning_effort and omits it at budget 0",
      "generationConfig → OpenAI sampling and token fields temperature 0 survives (falsy value must not be dropped)",
      "system instruction and contents accepts an unwrapped body (no request envelope)",
      "system instruction and contents inlineData becomes an image_url data URI part",
      "system instruction and contents maps model role to assistant and keeps user as user",
      "system instruction and contents string and parts-shaped systemInstruction both land as a system message first",
      "system instruction and contents thought parts become reasoning_content, never silently dropped",
      "tool calls and tool results a content mixing functionResponses with text and calls yields tool messages plus one assistant message",
      "tool calls and tool results a content with no parts is skipped without throwing",
      "tool calls and tool results derives a deterministic id from the name when upstream sent none, so the functionResponse pairs",
      "tool calls and tool results functionCall becomes an assistant tool_call with args serialized, id preserved",
      "tool calls and tool results remembers the thoughtSignature keyed on the upstream id (#3646)",
      "tool declarations → OpenAI tools a declaration without parameters gets the empty-object schema, not undefined",
      "tool declarations → OpenAI tools lowercases Gemini schema types recursively and strips enumDescriptions"
    ],
    "tests/unit/antigravity-token-import-1611.test.js": [
      "#1611 pasted Antigravity token JSON imports folds the other snake_case fields the same way",
      "#1611 pasted Antigravity token JSON imports infers apikey from a pasted key and access_token from a bare bearer",
      "#1611 pasted Antigravity token JSON imports lets an item override the list's provider rather than being forced into it",
      "#1611 pasted Antigravity token JSON imports never echoes a pasted token back",
      "#1611 pasted Antigravity token JSON imports still refuses a paste with no credential, and one with no provider to inherit",
      "#1611 pasted Antigravity token JSON imports takes the report's array paste, mixing both key spellings",
      "#1611 pasted Antigravity token JSON imports takes the report's single-object paste"
    ],
    "tests/unit/antigravity-usage-headers.test.js": [
      "Antigravity usage headers uses the official IDE user agent and omits router-only source headers"
    ],
    "tests/unit/antigravity-usage-validation.test.js": [
      "Antigravity usage verification does not clear a malformed successful quota payload",
      "Antigravity usage verification does not clear a message-only quota result",
      "Antigravity usage verification does not clear a transport failure",
      "Antigravity usage verification does not clear an HTTP quota error",
      "Antigravity usage verification does not clear from subscription data alone",
      "Antigravity usage verification does not expose a classified 2xx validation payload through the usage endpoint",
      "Antigravity usage verification does not expose a non-JSON quota diagnostic through the usage endpoint",
      "Antigravity usage verification does not expose an arbitrary quota exception through the usage endpoint",
      "Antigravity usage verification does not expose an opaque probe diagnostic through the Antigravity usage route",
      "Antigravity usage verification does not expose an opaque refresh diagnostic through the Antigravity usage route",
      "Antigravity usage verification does not log an opaque subscription diagnostic",
      "Antigravity usage verification exposes only plan and quotas for a successful mixed subscription payload",
      "Antigravity usage verification fails open when a validation callback rejects and keeps the URL redacted",
      "Antigravity usage verification fails open when the Gemini quota request throws",
      "Antigravity usage verification marks a usable models response and terminally clears its snapshot",
      "Antigravity usage verification preserves a subscription validation challenge without probing quota or clearing it",
      "Antigravity usage verification projects unallowlisted successful labels through the usage route without upstream diagnostics",
      "Antigravity usage verification reads the quota response exactly once as text",
      "Antigravity usage verification reads the subscription response exactly once as text",
      "Antigravity usage verification reports a strict quota 403 challenge",
      "Antigravity usage verification reports a strict subscription 403 challenge",
      "Antigravity usage verification reports a successful subscription validation challenge"
    ],
    "tests/unit/antigravity-validation.test.js": [
      "redaction redacts Help links when error details carry validation",
      "redaction redacts malformed raw accounts links",
      "redaction redacts the camel-case validation field",
      "redaction redacts the snake-case validation field",
      "redaction redacts uppercase raw accounts links without changing URL acceptance",
      "redaction redacts validation_link",
      "structured contracts accepts a successful loadCodeAssist validation tier",
      "structured contracts accepts the exact 403 RPC contract for autopush-cloudcode-pa.googleapis.com",
      "structured contracts accepts the exact 403 RPC contract for cloudcode-pa.googleapis.com",
      "structured contracts accepts the exact 403 RPC contract for staging-cloudcode-pa.googleapis.com",
      "structured contracts rejects a Help detail without ErrorInfo",
      "structured contracts rejects a later Help link",
      "structured contracts rejects a load response with currentTier",
      "structured contracts rejects a load tier with another reason",
      "structured contracts rejects a load tier without its action link",
      "structured contracts rejects a loosely named metadata field",
      "structured contracts rejects a mismatched RPC body code",
      "structured contracts rejects a non-403 RPC response",
      "structured contracts rejects a non-successful load response",
      "structured contracts rejects a raw message link",
      "structured contracts rejects a root action link",
      "structured contracts rejects a successful tier from another source",
      "structured contracts rejects an appeal link",
      "structured contracts rejects an ErrorInfo detail with a different type",
      "structured contracts rejects an ErrorInfo detail with another domain",
      "structured contracts rejects an RPC detail with another reason",
      "structured contracts rejects an RPC reason with whitespace",
      "structured contracts rejects invalid first Help link even with valid metadata",
      "structured contracts uses the first Help link before metadata",
      "structured contracts uses validation_link metadata when Help is absent",
      "URL validation accepts exact HTTPS",
      "URL validation canonicalizes explicit default HTTPS port",
      "URL validation rejects alternate host",
      "URL validation rejects C0",
      "URL validation rejects DEL",
      "URL validation rejects empty",
      "URL validation rejects HTTP",
      "URL validation rejects leading space",
      "URL validation rejects malformed",
      "URL validation rejects non-default port",
      "URL validation rejects non-string",
      "URL validation rejects overlong canonical href",
      "URL validation rejects overlong UTF-8 input",
      "URL validation rejects password",
      "URL validation rejects subdomain",
      "URL validation rejects trailing dot",
      "URL validation rejects trailing space",
      "URL validation rejects username",
      "URL validation retains opaque path query and fragment"
    ],
    "tests/unit/antigravity-verification-access.test.js": [
      "Antigravity verification access enforces mutation CSRF for 'alternate textual origin'",
      "Antigravity verification access enforces mutation CSRF for 'CLI token without browser headers'",
      "Antigravity verification access enforces mutation CSRF for 'cross-origin'",
      "Antigravity verification access enforces mutation CSRF for 'cross-site'",
      "Antigravity verification access enforces mutation CSRF for 'malformed Origin'",
      "Antigravity verification access enforces mutation CSRF for 'missing Origin'",
      "Antigravity verification access enforces mutation CSRF for 'missing Sec-Fetch-Site'",
      "Antigravity verification access enforces mutation CSRF for 'none'",
      "Antigravity verification access enforces mutation CSRF for 'opaque null'",
      "Antigravity verification access enforces mutation CSRF for 'origin credentials'",
      "Antigravity verification access enforces mutation CSRF for 'origin path'",
      "Antigravity verification access enforces mutation CSRF for 'same-origin browser'",
      "Antigravity verification access enforces mutation CSRF for 'same-site'",
      "Antigravity verification access enforces mutation CSRF for 'trailing slash'",
      "Antigravity verification access requireLogin=false authorizes 'CLI token' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'JWT cookie' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'proxied loopback' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'trusted IPv4 loopback' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'trusted IPv4-mapped loopback' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'trusted IPv6 loopback' only through approved identity",
      "Antigravity verification access requireLogin=false authorizes 'unstamped forged loopback' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'CLI token' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'JWT cookie' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'proxied loopback' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'trusted IPv4 loopback' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'trusted IPv4-mapped loopback' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'trusted IPv6 loopback' only through approved identity",
      "Antigravity verification access requireLogin=true authorizes 'unstamped forged loopback' only through approved identity"
    ],
    "tests/unit/antigravity-verification-callers.test.js": [
      "Antigravity verification callers classifies a direct hot-reload 403 from one text read without false clears",
      "Antigravity verification callers does not abort or suppress late quota-wrapper callbacks after the race",
      "Antigravity verification callers forwards only force and trusted hook fields through one usage attempt",
      "Antigravity verification callers gives the cold Antigravity chat project lookup a hook snapshot",
      "Antigravity verification callers keeps proactive Antigravity project refresh non-blocking while supplying hooks",
      "Antigravity verification callers keeps quota guard's three-second race on the verification wrapper",
      "Antigravity verification callers keeps the exact four generic Antigravity-capable usage callers wrapped",
      "Antigravity verification callers keeps the exact two project callers wrapped with Antigravity hooks",
      "Antigravity verification callers releases engine and verification lifetimes only after provider deletion succeeds",
      "Antigravity verification callers takes a fresh Antigravity hook snapshot immediately before chatCore",
      "Antigravity verification callers uses a fresh wrapper for every hot-reload quota verification attempt",
      "Antigravity verification callers uses a fresh wrapper for the initial Antigravity usage read",
      "Antigravity verification callers uses another fresh wrapper for the post-refresh Antigravity usage read",
      "Antigravity verification callers uses the submitted challenge ID for the dedicated forced recheck"
    ],
    "tests/unit/antigravity-verification-privacy.test.js": [
      "Antigravity verification privacy boundaries classifies and bounds raw project and chat diagnostics before public sinks",
      "Antigravity verification privacy boundaries does not use process-global storage for challenge state",
      "Antigravity verification privacy boundaries keeps action URLs out of getActiveRequests output",
      "Antigravity verification privacy boundaries keeps action URLs out of getUsageStats output",
      "Antigravity verification privacy boundaries keeps action URLs out of public error construction",
      "Antigravity verification privacy boundaries keeps action URLs out of request detail and request-log persistence",
      "Antigravity verification privacy boundaries keeps action URLs out of the general usage SSE stream",
      "Antigravity verification privacy boundaries keeps sensitive snapshots URL-free for both realistic validation URLs",
      "Antigravity verification privacy boundaries keeps sensitive SSE deltas URL-free for both realistic validation URLs",
      "Antigravity verification privacy boundaries keeps sensitive-route headers, logs, and redirects free of action URLs"
    ],
    "tests/unit/antigravity-verification-routes.test.js": [
      "Antigravity verification routes detail returns exactly the private detail shape",
      "Antigravity verification routes detail shares one 404 body for a missing connection",
      "Antigravity verification routes detail shares that 404 body for a non-Antigravity connection",
      "Antigravity verification routes detail shares that 404 body when expiry has removed the challenge",
      "Antigravity verification routes detail shares that 404 body when no challenge is live",
      "Antigravity verification routes dismissal leaves the provider connection row untouched",
      "Antigravity verification routes dismissal returns 204 for the matching current challenge",
      "Antigravity verification routes dismissal returns 404 when there is no live challenge to dismiss",
      "Antigravity verification routes dismissal returns 409 for a stale challenge without returning the replacement",
      "Antigravity verification routes dismissal runs CSRF before reading bodies across every mutation fixture",
      "Antigravity verification routes recheck forwards a selected strict route into the recheck probe",
      "Antigravity verification routes recheck rejects every denied identity on all four sensitive methods",
      "Antigravity verification routes recheck returns 409 before probing a stale submitted challenge",
      "Antigravity verification routes recheck returns a bounded 502 for a generic provider failure",
      "Antigravity verification routes recheck returns a bounded 502 for a malformed unmarked result",
      "Antigravity verification routes recheck returns a bounded 502 for transport failure",
      "Antigravity verification routes recheck returns false for a usable probe that records a concurrent replacement",
      "Antigravity verification routes recheck returns required-proxy unavailable before a recheck probe",
      "Antigravity verification routes recheck returns the shared 404 before probing a non-Antigravity connection",
      "Antigravity verification routes recheck returns verified only when a usable probe clears the submitted challenge",
      "Antigravity verification routes stream cleans up on request abort",
      "Antigravity verification routes stream cleans up once on stream cancel without growing listeners",
      "Antigravity verification routes stream sends an initial sanitized snapshot with exact SSE headers",
      "Antigravity verification routes stream sends sanitized upsert and remove deltas",
      "Antigravity verification routes stream writes a 25-second comment heartbeat"
    ],
    "tests/unit/antigravity-verification-state.test.js": [
      "Antigravity verification state allows an older matching success until a newer hook records an outcome",
      "Antigravity verification state allows an older validation callback until a newer hook records an outcome",
      "Antigravity verification state assigns globally increasing safe-integer generations",
      "Antigravity verification state bounds the observation ledger and evicts its oldest pair",
      "Antigravity verification state clears a matching current challenge",
      "Antigravity verification state deleting a connection clears its entry and observation ledger",
      "Antigravity verification state does not let a delayed old observation replace a newer challenge",
      "Antigravity verification state does not let a dismissed observation resurrect a challenge",
      "Antigravity verification state does not let an older success clear a newer challenge",
      "Antigravity verification state evicts the oldest observed entry when the live cap is exceeded",
      "Antigravity verification state expires a challenge in the one-minute sweep",
      "Antigravity verification state expires a challenge lazily after ten minutes",
      "Antigravity verification state expires observation-ledger entries after ten minutes",
      "Antigravity verification state ignores released chat hook callbacks after same-ID replacement",
      "Antigravity verification state ignores released project hook callbacks after same-ID replacement",
      "Antigravity verification state ignores released usage hook callbacks after same-ID replacement",
      "Antigravity verification state keeps challenges isolated by connection",
      "Antigravity verification state records one observation only once without refreshing expiry or emitting",
      "Antigravity verification state rejects a stale dismissal without exposing the current challenge",
      "Antigravity verification state rejects an older hook callback after a newer hook has observed its challenge",
      "Antigravity verification state starts empty after module reload",
      "Antigravity verification state unreferences the cleanup timer"
    ],
    "tests/unit/antigravity-web-search-2f17352c.test.js": [
      "a search routed to antigravity posts the grounding model and endpoint the registry declares",
      "a search routed to antigravity refuses a connection with no projectId before spending an upstream call",
      "antigravity registry declares webSearch via chat carries the grounding model and endpoint on searchViaChat",
      "antigravity registry declares webSearch via chat lists webSearch among its service kinds",
      "antigravity registry declares webSearch via chat survives the registry → AI_PROVIDERS projection",
      "chat-search config for antigravity is registered",
      "chat-search config for antigravity merges repeated chunk URLs into one citation carrying snippet and content",
      "chat-search config for antigravity reports a missing projectId instead of letting upstream 403 misleadingly",
      "chat-search config for antigravity resolves its endpoint from the registry rather than a literal"
    ],
    "tests/unit/api-key-candidate-resolution-2926.test.js": [
      "gateway API-key candidate resolution (PR #2926) attributes a Claude messages request to the valid x-api-key after a stale Bearer",
      "gateway API-key candidate resolution (PR #2926) collects every supported credential once in protocol precedence order",
      "gateway API-key candidate resolution (PR #2926) keeps a valid Authorization credential when an extra header is invalid",
      "gateway API-key candidate resolution (PR #2926) keeps Google-header and query credentials in the same resolver",
      "gateway API-key candidate resolution (PR #2926) selects the valid x-api-key after a stale Claude Bearer credential",
      "gateway API-key candidate resolution (PR #2926) uses the selected x-api-key at every claimed modality gate"
    ],
    "tests/unit/api-key-checksum-not-auth.test.js": [
      "the API key checksum is never an authentication decision no production file imports the checksum helpers",
      "the API key checksum is never an authentication decision the checksum secret's fallback is documented as a non-secret",
      "the API key checksum is never an authentication decision validateApiKey decides on an exact key match, not on the key's shape"
    ],
    "tests/unit/api-key-entropy.test.js": [
      "API key parsing survives the widening refuses anything that is not a key",
      "API key parsing survives the widening rejects a checksum of the wrong length rather than throwing",
      "API key parsing survives the widening rejects a key whose checksum was tampered with",
      "API key parsing survives the widening rejects a key whose keyId was tampered with",
      "API key parsing survives the widening round-trips a freshly minted key",
      "API key parsing survives the widening still accepts a 6-character keyId minted before the widening",
      "API key parsing survives the widening still parses the two-part legacy format",
      "API key secret material comes from the CSPRNG does not draw from Math.random",
      "API key secret material comes from the CSPRNG keeps the keyId free of the format delimiter",
      "API key secret material comes from the CSPRNG mints a 128-bit keyId, hex encoded",
      "API key secret material comes from the CSPRNG mints distinct keys across a large sample",
      "the display width leaves the key unguessable shows far less than it withholds"
    ],
    "tests/unit/api-key-expiry-2351.test.js": [
      "a key with no stamp keeps working, and the column is there from install no migration reaches back into apiKeys rows",
      "a key with no stamp keeps working, and the column is there from install the column is declared nullable in the schema a clean install creates",
      "a key with no stamp keeps working, and the column is there from install the versions stay ordered and unique as the chain grows",
      "an expired key stops working (#2351) a stamp in the future is not",
      "an expired key stops working (#2351) a stamp in the past is expired",
      "an expired key stops working (#2351) an unparseable stamp is NOT treated as expired",
      "an expired key stops working (#2351) no stamp means never expires, which is every key issued before this",
      "an expired key stops working (#2351) the boundary counts as expired, so a key never outlives its own stamp",
      "rejection happens at request-auth time no sweep is required for a key to stop working",
      "rejection happens at request-auth time validateApiKey reads the stamp and refuses before the active check",
      "the creation surface carries it without breaking older callers a caller that does not send it keeps exactly the behaviour it had",
      "the creation surface carries it without breaking older callers POST accepts an optional expiresAt",
      "the creation surface carries it without breaking older callers the created key reports its own expiry back",
      "the stored shape is normalized a row is read back with both the stamp and a derived flag",
      "the stored shape is normalized an unparseable input becomes never-expires rather than an arbitrary time",
      "the stored shape is normalized creation and update both go through the normalizer"
    ],
    "tests/unit/api-key-limits-3371.test.js": [
      "a ceiling stops the key at request-auth time another key's spend does not count against this one",
      "a ceiling stops the key at request-auth time clearing the ceiling to null makes the key unlimited again",
      "a ceiling stops the key at request-auth time completion tokens are counted separately from prompt tokens",
      "a ceiling stops the key at request-auth time getExceededLimit names which ceiling was hit",
      "a ceiling stops the key at request-auth time money: the USD ceiling refuses once the summed cost reaches it",
      "a ceiling stops the key at request-auth time prompt tokens: under the ceiling passes, at or over it refuses",
      "a ceiling stops the key at request-auth time raising the ceiling revives the key without reissuing it",
      "a fumbled ceiling does not become an arbitrary budget a token ceiling is stored as a whole number",
      "a fumbled ceiling does not become an arbitrary budget an unparseable or negative value stores as no ceiling",
      "a fumbled ceiling does not become an arbitrary budget zero IS a real ceiling, which freezes the key without deleting it",
      "a key with no ceiling behaves exactly as before creating without limits leaves all three null",
      "a key with no ceiling behaves exactly as before it validates however much it has already spent",
      "spend is reported next to the ceiling a key that has never been used reports zeros rather than nothing",
      "spend is reported next to the ceiling getApiKeyUsage sums the key's own history",
      "spend is reported next to the ceiling getApiKeyUsageTotals answers for every key in one pass",
      "the API surface carries it without breaking older callers a body with no ceiling in it creates exactly the key it always did",
      "the API surface carries it without breaking older callers GET pairs each key's ceilings with what it has already spent",
      "the API surface carries it without breaking older callers POST accepts the three optional ceilings and reports them back",
      "the API surface carries it without breaking older callers PUT sets each ceiling independently and leaves omitted ones alone",
      "the pure comparison reports null when every ceiling is absent",
      "the pure comparison reports the first ceiling reached, and reaching it exactly counts",
      "the schema change is additive (#3371) an install that already has the columns re-syncs without a failure",
      "the schema change is additive (#3371) SCHEMA_VERSION is stamped, which is what arms the pre-change backup",
      "the schema change is additive (#3371) the columns and the lookup index actually exist on a booted DB",
      "the schema change is additive (#3371) the enforcement query has an index, so it is not a scan of an unpruned table",
      "the schema change is additive (#3371) the three ceilings are declared nullable, so an existing row keeps NULL",
      "the schema change is additive (#3371) the versioned migration chain stays ordered and unique"
    ],
    "tests/unit/api-key-mask-distinguishes-2206.test.js": [
      "the API key mask distinguishes keys on one install (#2206) never returns the whole key",
      "the API key mask distinguishes keys on one install (#2206) never shows the machineId, which would make the key reconstructible",
      "the API key mask distinguishes keys on one install (#2206) reports null for no key at all",
      "the API key mask distinguishes keys on one install (#2206) shows the per-key half",
      "the API key mask distinguishes keys on one install (#2206) still masks an old-format key, which has no keyId to show",
      "the API key mask distinguishes keys on one install (#2206) the keyName fallback uses the mask too, not a raw prefix slice",
      "the API key mask distinguishes keys on one install (#2206) two keys from the same install do not mask identically"
    ],
    "tests/unit/api-key-model-allowlist-1154.test.js": [
      "a key with an allowlist admits only what is on it a bare entry admits the provider-qualified request for the same model, and back",
      "a key with an allowlist admits only what is on it a key the table does not hold is unrestricted, since the key check itself already refused it",
      "a key with an allowlist admits only what is on it admits a listed model and refuses an unlisted one",
      "a key with an allowlist admits only what is on it an empty request model is refused rather than admitted by default",
      "a key with an allowlist admits only what is on it matching ignores case and surrounding space, which is how a pasted list arrives",
      "a key with an allowlist admits only what is on it two qualified names must match exactly, so one provider's entry never admits another's",
      "a key with no allowlist behaves exactly as it always did local mode, where there is no key at all, is not restricted",
      "a key with no allowlist behaves exactly as it always did may route anything",
      "a key with no allowlist behaves exactly as it always did reports null",
      "nothing about the schema changes (#1154) a bulk revoke clears them too",
      "nothing about the schema changes (#1154) deleting a key takes its allowlist with it, so a reused id cannot inherit one",
      "nothing about the schema changes (#1154) the allowlist lives in kv, the way disabled models and pricing already do",
      "nothing about the schema changes (#1154) unrestricted is the absence of a row, not a sentinel",
      "the keys API carries it the same way it carries a ceiling an omitted field is left as it was; an explicit null is a clear",
      "the keys API carries it the same way it carries a ceiling pickLimits takes allowedModels alongside the three ceilings",
      "the stored value is normalized on the way in a value that is not a list at all is read as no restriction rather than locking the operator out",
      "the stored value is normalized on the way in an empty list means every model, not no model, so a key is never bricked by one",
      "the stored value is normalized on the way in blanks and duplicates are dropped",
      "the stored value is normalized on the way in null clears an allowlist back to every model",
      "the stored value is normalized on the way in setting the allowlist leaves the spend ceilings alone, and the other way round"
    ],
    "tests/unit/api-key-provider-scope-854.test.js": [
      "provider-scoped allowlist entries (#854) a bare '*' is not a wildcard entry, so it cannot silently open a scoped key",
      "provider-scoped allowlist entries (#854) admits every model of the named provider",
      "provider-scoped allowlist entries (#854) admits the bare provider string the search and fetch handlers check",
      "provider-scoped allowlist entries (#854) compares whole provider segments, so a lookalike prefix is not admitted",
      "provider-scoped allowlist entries (#854) is case-insensitive on both sides, like every other entry",
      "provider-scoped allowlist entries (#854) refuses an unqualified model name, because it names no provider to check",
      "provider-scoped allowlist entries (#854) refuses another provider's model",
      "provider-scoped allowlist entries (#854) still honours exact entries listed beside a provider scope",
      "the existing enforcement seam carries the provider scope (#854) is exported and reachable from the shared refusal helper",
      "the existing enforcement seam carries the provider scope (#854) refuseDisallowedModel answers 403 for a provider outside the scope"
    ],
    "tests/unit/api-keys-bulk-delete-2120.test.js": [
      "bulk API key revocation (#2120) an id that is already gone is a partial success, not a failure",
      "bulk API key revocation (#2120) no id at all is rejected rather than treated as delete-everything",
      "bulk API key revocation (#2120) removes every named key in one call and leaves the rest alone",
      "bulk API key revocation (#2120) the repo call is one statement and dedupes a repeated id",
      "bulk API key revocation (#2120) the single-key route still works on its own"
    ],
    "tests/unit/apikey-allowlist-all-modalities-448-2833.test.js": [
      "every public modality enforces it (#448, #2833) chat.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) embeddings.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) fetch.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) finds the handler set at all",
      "every public modality enforces it (#448, #2833) imageGeneration.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) jsonProxy.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) rerank.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) search.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) stt.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) tts.js consults the key's model allowlist",
      "every public modality enforces it (#448, #2833) videoGeneration.js consults the key's model allowlist",
      "the guard itself (#448, #2833) is inert when the key carries no allowlist",
      "the guard itself (#448, #2833) is inert with no key, which is local mode",
      "the guard itself (#448, #2833) is inert with no model rather than refusing the request",
      "the guard itself (#448, #2833) names the refused model, so the caller can see why",
      "the guard itself (#448, #2833) passes a model the key is allowed to use",
      "the guard itself (#448, #2833) refuses a model the key is not allowed to use, with 403"
    ],
    "tests/unit/apikey-device-tracking-930.test.js": [
      "the window (#930) caps one key rather than growing without bound between sweeps",
      "the window (#930) drops a client that has been silent past the TTL",
      "the window (#930) forgets a key entirely once its last client expires",
      "the window (#930) keeps a client that keeps calling",
      "what counts as one client (#930) counts the same address and agent once, however often it calls",
      "what counts as one client (#930) counts two addresses as two clients",
      "what counts as one client (#930) counts two agents from one address as two clients",
      "what counts as one client (#930) keeps keys apart",
      "what counts as one client (#930) reports 0 for a key nothing has used",
      "what it refuses to trust (#930) collapses to one bucket when the address cannot be trusted",
      "what it refuses to trust (#930) never throws on a malformed request, since it sits on the request path",
      "what it refuses to trust (#930) records nothing without a key or a request",
      "what it refuses to trust (#930) stores no address, only a hash"
    ],
    "tests/unit/apikey-endpoint-override-2504.test.js": [
      "per-connection endpoint override for an API-key provider (#2504) a compatible provider still takes its endpoint from its node, not the caller",
      "per-connection endpoint override for an API-key provider (#2504) a connection that names no endpoint keeps the registry default",
      "per-connection endpoint override for an API-key provider (#2504) an edit changes the override, and an empty value clears it",
      "per-connection endpoint override for an API-key provider (#2504) an edit rejects a new bad URL without touching what is stored",
      "per-connection endpoint override for an API-key provider (#2504) echoing a value written before this check does not block editing the rest",
      "per-connection endpoint override for an API-key provider (#2504) refuses a base URL that is not an absolute http(s) URL",
      "per-connection endpoint override for an API-key provider (#2504) refuses an API type outside the vocabulary the executor understands",
      "per-connection endpoint override for an API-key provider (#2504) stores a base URL and an API type the caller names",
      "per-connection endpoint override for an API-key provider (#2504) the nested shape the existing baseUrlField form sends is accepted too"
    ],
    "tests/unit/apikey-endpoint-visibility-2504.test.js": [
      "the default endpoint is visible (#2504) covers the api-key providers the report names, not just one",
      "the default endpoint is visible (#2504) omits the field rather than inventing one where the registry has none",
      "the default endpoint is visible (#2504) projects it for most of the api-key catalogue rather than a handful",
      "the default endpoint is visible (#2504) projects the registry base URL and format for an api-key provider",
      "the override can be read back (#2504) exposes apiType beside baseUrl, so the pair is not half-readable",
      "the override can be read back (#2504) still withholds the credential fields"
    ],
    "tests/unit/app-updater-state-machine.test.js": [
      "isUpdateDisabled (#1563 opt-out) is off when the flag is unset, '0', or 'false', on for any other value",
      "killAppProcesses on POSIX does nothing when the pid file is absent or holds no pid",
      "killAppProcesses on POSIX falls back to process.kill when sudo kill is refused, and survives ps failure",
      "killAppProcesses on POSIX kills the MITM pid from the pid file and every matching app process, then waits",
      "killAppProcesses on Windows uses taskkill with the PowerShell fallback and collects node + helper pids",
      "spawnUpdaterAndExit copies the bundled updater into DATA_DIR runtime so npm -g can overwrite node_modules",
      "spawnUpdaterAndExit falls back to cwd-relative updater paths when no override is set",
      "spawnUpdaterAndExit preserves tray mode in the relaunch arguments",
      "spawnUpdaterAndExit relaunches the launcher's own path when it exists (#2186) and honors the live PORT (#2575)",
      "spawnUpdaterAndExit reuses an existing runtime copy of the same size instead of recopying",
      "spawnUpdaterAndExit schedules the server exit after the configured delay",
      "spawnUpdaterAndExit spawns the detached updater with the full config contract and unrefs it"
    ],
    "tests/unit/assemblyai-stt.test.js": [
      "AssemblyAI STT maps an explicit language field to AssemblyAI language_code",
      "AssemblyAI STT uses language detection when no language is provided",
      "AssemblyAI STT uses raw Authorization header for AssemblyAI upload/submit/poll"
    ],
    "tests/unit/async-log-output.test.js": [
      "a closed output descriptor is counted without an unhandled rejection",
      "a genuinely unread stdout pipe keeps the event loop responsive and shutdown bounded",
      "actual asynchronous stdout preserves line order and redacts before admission"
    ],
    "tests/unit/auth-status.test.js": [
      "GET /api/auth/status does not assert a default password when status is unreadable",
      "GET /api/auth/status fails closed when status dependencies throw",
      "GET /api/auth/status fails during session initialization without a JWT secret (\"\")",
      "GET /api/auth/status fails during session initialization without a JWT secret (undefined)",
      "GET /api/auth/status reports an authenticated session when the auth cookie is valid",
      "GET /api/auth/status reports effective password source without exposing secrets (null, configured env)",
      "GET /api/auth/status reports effective password source without exposing secrets (null, configured env)",
      "GET /api/auth/status reports effective password source without exposing secrets (stored-hash-value, configured env)",
      "GET /api/auth/status reports unauthenticated when the auth cookie is invalid"
    ],
    "tests/unit/auto-router-1386.test.js": [
      "model selection (#1386) an explicit rule beats the tier pick",
      "model selection (#1386) claims auto-router but never the bare \"auto\"",
      "model selection (#1386) falls to the next tier when the preferred one is empty",
      "model selection (#1386) ignores a rule that is not a provider/model id",
      "model selection (#1386) never routes to a combo, which would recurse",
      "model selection (#1386) returns null rather than guessing when nothing is routable",
      "model selection (#1386) routes a greeting to the cheapest budget model",
      "model selection (#1386) routes coding to the standard tier",
      "model selection (#1386) routes reasoning to the top tier",
      "model selection (#1386) skips a model availability reports exhausted",
      "task classification (#1386) a code fence is coding",
      "task classification (#1386) a greeting is simple",
      "task classification (#1386) a tool-bearing request is coding whatever the text says",
      "task classification (#1386) a very long prompt is reasoning whatever it says",
      "task classification (#1386) an architecture question is reasoning",
      "task classification (#1386) reads a Responses-shaped body too",
      "task classification (#1386) reads multimodal content blocks"
    ],
    "tests/unit/auto-router-listed-1386.test.js": [
      "the auto router is discoverable (#1386) appears in the client-facing listing",
      "the auto router is discoverable (#1386) is attributed to the router, not to a provider that does not own it",
      "the auto router is discoverable (#1386) is listed exactly once",
      "the auto router is discoverable (#1386) is the id the router accepts",
      "where it must not appear (#1386) is absent from an install with no connections",
      "where it must not appear (#1386) is not offered to a Claude-compat client, in any spelling",
      "where it must not appear (#1386) stays out of buildModelsList, which the router itself reads"
    ],
    "tests/unit/auto-routing-policy.test.js": [
      "refuses anonymous and nonlocal mutation before state access",
      "rolls back settings when the receipt cannot be recorded",
      "survives disk reopen and reports post-commit flush uncertainty separately",
      "validates all three rules, records an atomic receipt and preserves unrelated settings"
    ],
    "tests/unit/autoping-early-quota-lift.test.js": [
      "early quota lift: fresh usage outranks the lock timestamp an already-elapsed lock is left alone (it lapses by itself, no write to learn nothing)",
      "early quota lift: fresh usage outranks the lock timestamp clears a future lock when the governing window reads healthy, spending no request",
      "early quota lift: fresh usage outranks the lock timestamp the clear never touches backoffLevel, so a wrong lift re-locks harder",
      "no early lift while the governing window is exhausted an exhausted window re-marks to the provider reset instead of clearing",
      "no early lift while the governing window is exhausted an unreadable usage payload clears nothing (absence of evidence is not a lift)"
    ],
    "tests/unit/autoping-exhausted-reset-cycle.test.js": [
      "a spent warm is recorded; a refused one is not a rate-limited ping records no warm, so the window is not falsely marked started",
      "a spent warm is recorded; a refused one is not a successful ping persists lastPingAt and per-window warm state",
      "an account already counting time is never pinged sends nothing while every tracked window is running, and holds the usage read too",
      "exhausted governing window: skipped until the reset timestamp, then re-pinged re-pings once the reset timestamp has passed and the window reads stopped",
      "exhausted governing window: skipped until the reset timestamp, then re-pinged the mark is exactly what account fallback filters on, and it lapses at the reset",
      "exhausted governing window: skipped until the reset timestamp, then re-pinged writes rateLimitedUntil from the provider reset and does not spend a ping"
    ],
    "tests/unit/autoping-provider-coverage.test.js": [
      "every configured provider can actually be warmed antigravity is reachable from the dashboard toggle",
      "every configured provider can actually be warmed antigravity resolves a ping model",
      "every configured provider can actually be warmed antigravity resolves a ping model without a configured one",
      "every configured provider can actually be warmed claude is reachable from the dashboard toggle",
      "every configured provider can actually be warmed claude resolves a ping model",
      "every configured provider can actually be warmed claude resolves a ping model without a configured one",
      "every configured provider can actually be warmed codex is reachable from the dashboard toggle",
      "every configured provider can actually be warmed codex resolves a ping model",
      "every configured provider can actually be warmed codex resolves a ping model without a configured one",
      "every configured provider can actually be warmed gates an auth type the provider does not carry",
      "every configured provider can actually be warmed kimi is reachable from the dashboard toggle",
      "every configured provider can actually be warmed kimi resolves a ping model",
      "every configured provider can actually be warmed kimi resolves a ping model without a configured one"
    ],
    "tests/unit/autoping-provider-list-2564.test.js": [
      "the auto-ping provider list has one source (#2564) derives every settings key from the table",
      "the auto-ping provider list has one source (#2564) pairs each key with its provider id",
      "the auto-ping provider list has one source (#2564) the boot check reads all of them",
      "the auto-ping provider list has one source (#2564) the launcher menu is generated from what the server reports",
      "the auto-ping provider list has one source (#2564) the menu helper accepts a generated item list without breaking a static one",
      "the auto-ping provider list has one source (#2564) the settings write reconfigures on any of them, not two named ones"
    ],
    "tests/unit/autoping-tick-reach.test.js": [
      "a cold connection actually gets warmed, on every provider antigravity pokes one model per quota family, not just the governing one",
      "a cold connection actually gets warmed, on every provider antigravity sends an upstream warm request",
      "a cold connection actually gets warmed, on every provider claude sends an upstream warm request",
      "a cold connection actually gets warmed, on every provider codex sends an upstream warm request",
      "a cold connection actually gets warmed, on every provider kimi sends an upstream warm request",
      "a cold connection actually gets warmed, on every provider kimi warms through the generic executor with its cheapest model",
      "quota warming tick reaches every configured provider a provider with no setting written is skipped, not enumerated",
      "quota warming tick reaches every configured provider antigravity is enumerated when its setting is on",
      "quota warming tick reaches every configured provider claude is enumerated when its setting is on",
      "quota warming tick reaches every configured provider codex is enumerated when its setting is on",
      "quota warming tick reaches every configured provider kimi is enumerated when its setting is on"
    ],
    "tests/unit/autostart-headless-linux-1431.test.js": [
      "autostart falls back to a systemd --user unit when headless (#1431) does not report success, and cleans up the unit, when the manager cannot link it",
      "autostart falls back to a systemd --user unit when headless (#1431) does not trust a leftover unit file without confirming the manager has it enabled",
      "autostart falls back to a systemd --user unit when headless (#1431) reports enabled via the systemd unit and removes it on disable",
      "autostart falls back to a systemd --user unit when headless (#1431) still uses the desktop-autostart file on a graphical session (DISPLAY set)",
      "autostart falls back to a systemd --user unit when headless (#1431) writes a systemd user unit instead of the inert desktop-autostart file"
    ],
    "tests/unit/autostart-opt-out-3628.test.js": [
      "autostart preserves a user's choice across Hide to Tray (#3628) does not recreate a startup entry manually removed after automatic setup",
      "autostart preserves a user's choice across Hide to Tray (#3628) keeps an explicit re-enable across later automatic hides",
      "autostart preserves a user's choice across Hide to Tray (#3628) retries a failed automatic enable because no decision was recorded",
      "autostart preserves a user's choice across Hide to Tray (#3628) routes Hide to Tray through the once-only automatic path",
      "autostart preserves a user's choice across Hide to Tray (#3628) treats the legacy explicit opt-out as a decided state",
      "autostart preserves a user's choice across Hide to Tray (#3628) uses the CLI data-directory convention on Windows",
      "autostart preserves a user's choice across Hide to Tray (#3628) writes the first automatic entry and records that automatic choice"
    ],
    "tests/unit/azure-executor.test.js": [
      "AzureExecutor registry is registered on executor map",
      "AzureExecutor.requiresMaxCompletionTokens does not match non-reasoning deployments",
      "AzureExecutor.requiresMaxCompletionTokens matches gpt-5 family deployments",
      "AzureExecutor.requiresMaxCompletionTokens matches o1/o3/o4 reasoning deployments",
      "AzureExecutor.transformRequest is a no-op when max_tokens is absent",
      "AzureExecutor.transformRequest keeps an explicit max_completion_tokens and still deletes max_tokens",
      "AzureExecutor.transformRequest leaves max_tokens untouched for non-reasoning deployments",
      "AzureExecutor.transformRequest renames max_tokens to max_completion_tokens for reasoning deployments"
    ],
    "tests/unit/background-token-refresh.test.js": [
      "runBackgroundTokenRefreshTick calls refresh only for due connections and swallows per-connection errors",
      "runBackgroundTokenRefreshTick does not call refresh when nothing is due",
      "runBackgroundTokenRefreshTick keeps exactly one DEBUG tick summary under LOG_LEVEL=DEBUG",
      "runBackgroundTokenRefreshTick stays silent at INFO on a fully successful tick",
      "runBackgroundTokenRefreshTick still names the provider on a failed refresh and never logs connection ids",
      "runBackgroundTokenRefreshTick swallows top-level load errors",
      "selectConnectionsNeedingRefresh never selects apikey connections",
      "selectConnectionsNeedingRefresh selects already-expired oauth connection",
      "selectConnectionsNeedingRefresh selects oauth grok-cli connection expiring in 10 minutes",
      "selectConnectionsNeedingRefresh skips connection expiring in 2 hours",
      "selectConnectionsNeedingRefresh skips oauth connection without refreshToken",
      "start/stop guards honors DISABLE_BACKGROUND_TOKEN_REFRESH kill-switch",
      "start/stop guards is idempotent: second start is no-op"
    ],
    "tests/unit/bare-model-connected-owner-710.test.js": [
      "bare model with several static owners prefers a connected one (#710) ignores a connection to a provider that does not declare the model",
      "bare model with several static owners prefers a connected one (#710) keeps the import-order owner when IT is the connected one",
      "bare model with several static owners prefers a connected one (#710) keeps the import-order owner when nothing is connected",
      "bare model with several static owners prefers a connected one (#710) leaves a single-owner bare model alone",
      "bare model with several static owners prefers a connected one (#710) routes to the owner the user actually connected"
    ],
    "tests/unit/bare-model-resolution.test.js": [
      "bare model resolution declares the active xAI API model IDs in the catalog",
      "bare model resolution lets user-defined model aliases win over static catalog owners",
      "bare model resolution never blind-routes bare -free names to openrouter",
      "bare model resolution preserves explicitly prefixed Grok subscription routing",
      "bare model resolution rejects an unknown bare model instead of guessing a provider from its prefix",
      "bare model resolution rejects an unknown bare model that matches none of the prefix rules (closes the openai default too)",
      "bare model resolution resolves prefix-collision models to the prefix-matching provider",
      "bare model resolution resolves single-owner models without admin data",
      "bare model resolution routes a bare declared xAI model through its catalog owner",
      "bare model resolution routes bare opencode free models via the synced free catalog",
      "bare model resolution routes bare static-registry names without touching the free catalog",
      "bare model resolution routes duplicate bare Grok IDs to xAI",
      "bare model resolution still resolves a known catalog id (guards the fix below against regressing this)",
      "canonicalEchoModel keeps prefixed requests and non-catalog bare names exactly as sent",
      "canonicalEchoModel passes through missing requested models",
      "canonicalEchoModel re-injects the listing prefix for bare names on connection-less catalog providers"
    ],
    "tests/unit/base-executor-connect-timeout.test.js": [
      "BaseExecutor response-header timeout clears the deadline on headers and on network rejection",
      "BaseExecutor response-header timeout creates a fresh full deadline after an explicitly replay-safe rejection",
      "BaseExecutor response-header timeout does not resend an ambiguous 502 even when a status retry is configured",
      "BaseExecutor response-header timeout preserves caller cancellation",
      "BaseExecutor response-header timeout restores caller cancellation when transport wraps the rejection",
      "BaseExecutor response-header timeout uses provider override and bypasses ordinary 502 retry"
    ],
    "tests/unit/base-executor-contract.test.js": [
      "BaseExecutor.buildHeaders — auth placement Accept: text/event-stream only when streaming",
      "BaseExecutor.buildHeaders — auth placement anthropic-compatible: apiKey goes to x-api-key, never Authorization, and version header is set",
      "BaseExecutor.buildHeaders — auth placement anthropic-compatible: OAuth-only credentials fall back to Bearer",
      "BaseExecutor.buildHeaders — auth placement config headers are merged but do not clobber Content-Type semantics",
      "BaseExecutor.buildHeaders — auth placement standard provider: accessToken wins over apiKey, Bearer scheme",
      "BaseExecutor.buildUrl anthropic-compatible: appends /messages",
      "BaseExecutor.buildUrl openai-compatible: appends /chat/completions to the stored base, trailing slash normalized",
      "BaseExecutor.buildUrl openai-compatible: falls back to OPENAI_COMPAT_BASE without a stored base",
      "BaseExecutor.buildUrl plain provider: urlIndex selects among baseUrls, out-of-range falls back to [0]",
      "BaseExecutor.execute — non-2xx propagation, no silent success a 401 with a body is returned to the caller untouched (no retry, no swallow)",
      "BaseExecutor.execute — non-2xx propagation, no silent success sends the serialized transformed body and the built headers to the provider URL",
      "BaseExecutor.parseError — status and body reach the caller a past quotaResetTimeStamp is ignored (no stale resetsAtMs)",
      "BaseExecutor.parseError — status and body reach the caller extracts error.message from a JSON body and keeps the status",
      "BaseExecutor.parseError — status and body reach the caller falls back to raw text for a non-JSON body, and to HTTP <status> for an empty one",
      "BaseExecutor.parseError — status and body reach the caller parses \"Resets in ...\" wording from a 429 message string only",
      "BaseExecutor.parseError — status and body reach the caller parses Google RPC ErrorInfo quotaResetDelay into resetsAtMs and appends the reason",
      "BaseExecutor.parseError — status and body reach the caller parses RetryInfo retryDelay in h/m/s composite form",
      "passes the exact serialized payload and destination to the dispatch boundary"
    ],
    "tests/unit/base-executor-last-retry-error-2722.test.js": [
      "BaseExecutor.execute — last useful retry error survives exhaustion (#2722) does not touch response doubles that lack clone() (existing retry tests)",
      "BaseExecutor.execute — last useful retry error survives exhaustion (#2722) keeps an earlier attempt's error body when the final retry comes back empty",
      "BaseExecutor.execute — last useful retry error survives exhaustion (#2722) leaves a final response with its own body untouched"
    ],
    "tests/unit/base-executor-retry.test.js": [
      "BaseExecutor.execute — baseUrls fallback falls over to the next url on 429 (shouldRetry)",
      "BaseExecutor.execute — computeRetryDelay hook veto hook returning false skips retry (uses fallback path)",
      "BaseExecutor.execute — computeRetryDelay hook veto only invokes computeRetryDelay when status has retry config",
      "BaseExecutor.execute — network error retry/fallback does not map an uncertain POST exception to a replayable HTTP rejection",
      "BaseExecutor.execute — network error retry/fallback throws when the only url fails with network error and no retries left",
      "BaseExecutor.execute — retry by status (config-driven) retries 502 `attempts` times then succeeds",
      "BaseExecutor.execute — retry by status (config-driven) stops after exhausting 502 attempts on a single url and throws"
    ],
    "tests/unit/blackforestlabs-adapter-coverage.test.js": [
      "blackForestLabs adapter buildBody maps prompt, size and image",
      "blackForestLabs adapter buildHeaders prefers apiKey, falls back to accessToken, tolerates no creds",
      "blackForestLabs adapter is async and builds the submit URL from the registry imageConfig",
      "blackForestLabs adapter normalize returns the sample URL, or empty data without one",
      "blackForestLabs adapter parseResponse throws when no polling_url is returned",
      "blackForestLabs adapter polls until Ready and forwards the poll auth header",
      "blackForestLabs adapter throws on a non-ok poll response",
      "blackForestLabs adapter throws the upstream error message on Error/Failed status",
      "blackForestLabs adapter times out when the deadline passes without a terminal status"
    ],
    "tests/unit/board-kit.test.js": [
      "composes a board with summary chips, toolbar, group, card and evidence lines"
    ],
    "tests/unit/body-read-deadline.test.js": [
      "reader-owned response body deadline cancels the owned reader with a typed body deadline and never resolves late EOF",
      "reader-owned response body deadline decodes split UTF-8 through its acquired reader and releases it once",
      "reader-owned response body deadline gives the consumer an owned reader instead of the raw stream",
      "reader-owned response body deadline parses JSON only after its reader has fully decoded the body",
      "reader-owned response body deadline preserves the caller abort reason as the first terminal cause"
    ],
    "tests/unit/bounded-log-retention.test.js": [
      "bounded redaction before retention admission omits incomplete, opaque and oversized frames and clears cancellation state",
      "bounded redaction before retention admission omits oversized values without splitting their credentials and never invokes getters",
      "bounded redaction before retention admission redacts JSON secret fields across every possible stream chunk split",
      "bounded redaction before retention admission stops traversing large arrays and handles cycles and prototype keys"
    ],
    "tests/unit/bounded-log-sink.test.js": [
      "bounded asynchronous diagnostics bounded shutdown discards pending items while accurately reporting a hung write",
      "bounded asynchronous diagnostics cancellation discards pending records without double accounting",
      "bounded asynchronous diagnostics failed writes are counted without rejection or retry amplification",
      "bounded asynchronous diagnostics never invokes a sink on the producer stack and counts stalled in-flight bytes",
      "never evicts a queued session initializer to make room for completion evidence",
      "reserves completion evidence by evicting pending content without exceeding either cap",
      "shares flush and close promises so a stalled sink cannot grow a waiter set"
    ],
    "tests/unit/budget-admin-api.test.js": [
      "GET rejects duplicate or unknown filters and never initializes history on read",
      "mutation size is bounded independently of its declared length",
      "pages operator-scoped reservations and requires evidence to release one",
      "rejects before reading a mutation body or touching budget state"
    ],
    "tests/unit/budget-crash-recovery.test.js": [
      "separate native process admissions and abrupt death admits one concurrent unknown exposure and preserves it after the owner is killed"
    ],
    "tests/unit/budget-dispatch-hooks.test.js": [
      "a rejected admission causes zero transport calls and no response callback",
      "crosses admission, exact wire send and response observation in order",
      "overridden execute cannot silently inherit the Base coverage claim"
    ],
    "tests/unit/budget-dispatch-pipeline.test.js": [
      "API key through real admission, transport, ledger and query client cancellation retains partial exposure after streaming output",
      "API key through real admission, transport, ledger and query direct authentication rejection releases only its own attempt before the retry",
      "API key through real admission, transport, ledger and query does not release exposure forforged local refusal proof",
      "API key through real admission, transport, ledger and query does not release exposure forrevoked local refusal proof",
      "API key through real admission, transport, ledger and query keeps budget exposure consistent with a proven429 retry",
      "API key through real admission, transport, ledger and query keeps budget exposure consistent with a proven503 retry",
      "API key through real admission, transport, ledger and query no structural or compatible endpoint assumption manufactures a token bound",
      "API key through real admission, transport, ledger and query releases an owned pre-transport refusaltransport_pool_capacity with a local503 receipt",
      "API key through real admission, transport, ledger and query releases an owned pre-transport refusaltransport_pool_cleanup with a local503 receipt",
      "API key through real admission, transport, ledger and query releases an owned pre-transport refusaltransport_pools_closed with a local503 receipt",
      "API key through real admission, transport, ledger and query retains ambiguous5xx exposure and makes exactly one dispatch ({\"x-tokenproxy-replay-safe\":\"false\"})",
      "API key through real admission, transport, ledger and query retains ambiguous5xx exposure and makes exactly one dispatch ({\"x-tokenproxy-replay-safe\":\"true\",\"x-should-retry\":\"false\"})",
      "API key through real admission, transport, ledger and query retains ambiguous5xx exposure and makes exactly one dispatch ({})",
      "API key through real admission, transport, ledger and query strict default admits a documented output bound and joins one exact request",
      "API key through real admission, transport, ledger and query strict missing prompt bound refuses before transport with an explicit terminal error",
      "API key through real admission, transport, ledger and query the field-strip retry owns final401 status, reset, body cleanup and exposure",
      "API key through real admission, transport, ledger and query the field-strip retry owns final429 status, reset, body cleanup and exposure",
      "API key through real admission, transport, ledger and query the field-strip retry owns final503 status, reset, body cleanup and exposure",
      "API key through real admission, transport, ledger and query unknown best-effort exposure permits one concurrent wire and retains failed exposure"
    ],
    "tests/unit/budget-driver-qualification.test.js": [
      "accounting on better-sqlite3 preserves recorded usage and counters after reopening the same file",
      "accounting on sql.js preserves recorded usage and counters after reopening the same file",
      "better-sqlite3 keeps uncertain exposure when usage recording fails after dispatch",
      "sql.js cannot admit capped dispatch and writes nothing, while an unlimited key keeps its fast path",
      "sql.js surfaces a failed publication instead of reporting the write as durable"
    ],
    "tests/unit/budget-explicit-outcomes.test.js": [
      "explicit task outcome API a successful request without a report has no outcome, and the admin read shows exactly what was reported",
      "explicit task outcome API links an outcome only to an exact request that belongs to the same key",
      "explicit task outcome API refuses an outcome without a task, an outcome value outside the contract, and an outcome on a non-outcome event",
      "explicit task outcome API refuses malformed ids and bodies before any write",
      "explicit task outcome API requires authentication and never records an anonymous outcome",
      "explicit task outcome API retains an explicit cancelled outcome under its exact event id",
      "explicit task outcome API retains an explicit failure outcome under its exact event id",
      "explicit task outcome API retains an explicit success outcome under its exact event id",
      "explicit task outcome API retains an explicit unknown outcome under its exact event id",
      "explicit task outcome API treats an exact resend as a duplicate and a reused id with different evidence as a conflict"
    ],
    "tests/unit/budget-identity-shedding.test.js": [
      "exhausted presented key cannot become anonymous local traffic chat refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic embeddings refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic fetch refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic imageGeneration refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic jsonProxy refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic keeps public budget validation unchanged and attaches a terminal budget refusal",
      "exhausted presented key cannot become anonymous local traffic rerank refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic search refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic stt refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic tts refuses before selecting any provider even with requireApiKey=false",
      "exhausted presented key cannot become anonymous local traffic videoGeneration refuses before selecting any provider even with requireApiKey=false"
    ],
    "tests/unit/budget-media-pipeline.test.js": [
      "embeddings reserves separately after a proven auth rejection and settles exact input usage",
      "embeddings strict prompt bounds refuse before fetch",
      "rerank reserves separately after a proven auth rejection and settles exact input usage",
      "rerank strict prompt bounds refuse before fetch"
    ],
    "tests/unit/budget-outcome-qualification.test.js": [
      "priceUsage keeps conflicting reported USD values as evidence, not as an amount agreeing duplicate spellings and a single spelling both report normally",
      "priceUsage keeps conflicting reported USD values as evidence, not as an amount leaves the reported amount unknown and labels the estimate separately",
      "priceUsage keeps conflicting reported USD values as evidence, not as an amount without a rate card the conflict leaves cost unknown",
      "receipt reconciliation refuses ambiguity before any write accepts a receipt that spells one quantity twice with the same value",
      "receipt reconciliation refuses ambiguity before any write preserves an explicit reported USD zero as provider-reported, not confirmed payment",
      "receipt reconciliation refuses ambiguity before any write rejects a receipt where completion_tokens and output_tokens disagree",
      "receipt reconciliation refuses ambiguity before any write rejects a receipt where cost_usd and cost_in_usd disagree",
      "receipt reconciliation refuses ambiguity before any write rejects a receipt where prompt_tokens and input_tokens disagree"
    ],
    "tests/unit/budget-recovery-policy.test.js": [
      "persistent budget policy and conservative recovery API rejects unsupported policy before any creation or mutation",
      "persistent budget policy and conservative recovery configuration import refuses ambiguous raw-key reassignment atomically",
      "persistent budget policy and conservative recovery configuration roundtrip preserves ceilings, policy and existing outstanding exposure",
      "persistent budget policy and conservative recovery legacy configuration import cannot erase an existing stable key ceiling",
      "persistent budget policy and conservative recovery native admission flushes durably and restores the previous SQLite sync setting",
      "persistent budget policy and conservative recovery new keys default strict while untouched historical policies are labeled best effort",
      "persistent budget policy and conservative recovery SQL.js cannot admit capped requests, while unlimited retains its fast path"
    ],
    "tests/unit/budget-reservations.test.js": [
      "durable atomic budget reservations a later provider receipt settles partial usage exactly once without another request or charge",
      "durable atomic budget reservations a new retry gets its own identity after proven provider nonacceptance",
      "durable atomic budget reservations an unlimited in-flight request keeps stable ownership across key rotation",
      "durable atomic budget reservations estimates remain recorded but cannot erase unknown historical quantities",
      "durable atomic budget reservations grouped key summaries separate held exposure from recorded counts without raw secrets",
      "durable atomic budget reservations idempotent reserve never crosses a key or logical identity",
      "durable atomic budget reservations missing usage and unknown media costs retain the remaining allowance",
      "durable atomic budget reservations only one of concurrent unknown-bound admissions can reserve the allowance",
      "durable atomic budget reservations partial stream usage preserves residual exposure without double counting",
      "durable atomic budget reservations raw-key rotation retains lifetime usage and uncertain exposure under stable key ID",
      "durable atomic budget reservations retries cannot reuse an already dispatched attempt or its uncertain allowance",
      "durable atomic budget reservations settles one exact attempt once and frees only the unused reservation",
      "durable atomic budget reservations strict bounded parallel admissions use only their verified completion exposure",
      "durable atomic budget reservations strict never treats missing bounds or old missing costs as zero",
      "durable atomic budget reservations uncapped dimensions do not prevent settlement of a complete capped USD amount",
      "durable atomic budget reservations unlimited keys avoid accounts and reservation writes"
    ],
    "tests/unit/budget-uncovered-paths.test.js": [
      "fetch refuses capped keys before any uncovered generation path",
      "imageGeneration refuses capped keys before any uncovered generation path",
      "jsonProxy refuses capped keys before any uncovered generation path",
      "search refuses capped keys before any uncovered generation path",
      "stt refuses capped keys before any uncovered generation path",
      "tts refuses capped keys before any uncovered generation path",
      "videoGeneration refuses capped keys before any uncovered generation path"
    ],
    "tests/unit/build-sha-stamp-order.test.js": [
      "BUILD_SHA is stamped before the bundle is built git HEAD wins over the stamp files when a .git dir exists (worktree freshness)",
      "BUILD_SHA is stamped before the bundle is built next.config.mjs is what makes the ordering matter",
      "BUILD_SHA is stamped before the bundle is built slices an env-provided sha to 12 chars like every other path",
      "BUILD_SHA is stamped before the bundle is built stamps exactly once, so no later write can shadow the pre-build one",
      "BUILD_SHA is stamped before the bundle is built still removes the file rather than writing a placeholder off a checkout",
      "BUILD_SHA is stamped before the bundle is built writes cli/BUILD_SHA before it invokes the Next build"
    ],
    "tests/unit/buildOutputFilter.test.js": [
      "PR #1175 - buildOutput compression behavior compresses cargo build output",
      "PR #1175 - buildOutput compression behavior compresses npm install with deprecations",
      "PR #1175 - buildOutput compression behavior keeps cargo errors verbatim",
      "PR #1175 - buildOutput compression behavior keeps maven BUILD FAILED as error",
      "PR #1175 - buildOutput filter detection detects cargo build output (no longer misdetected as git-status)",
      "PR #1175 - buildOutput filter detection detects npm install output",
      "PR #1175 - false positive risks generic 'Compiling templates' (non-build context) triggers buildOutput",
      "PR #1175 - false positive risks generic app log with 'ERROR:' triggers buildOutput (potential false positive)",
      "PR #1175 - false positive risks plain text with no patterns falls through (no false positive)",
      "PR #1175 - porcelain regex fix edge cases cargo Compiling lines NOT detected as git-status (porcelain false positive fix)",
      "PR #1175 - porcelain regex fix edge cases git status --porcelain with staged (status code first char) still detects",
      "PR #1175 - porcelain regex fix edge cases git status --porcelain workdir-only (space first char) STILL detects as gitStatus (minimal fix preserved old regex)",
      "PR #1175 - porcelain regex fix edge cases long-form git status with 'On branch' always detects",
      "PR #1175 - safety: no data corruption empty input returns input",
      "PR #1175 - safety: no data corruption input with no recognized patterns returns input (fallback)",
      "PR #1175 - safety: no data corruption input with only errors preserves all errors",
      "PR #1175 - safety: no data corruption limits warnings to 5 + summary line"
    ],
    "tests/unit/buildOutputFilterAdversarial.test.js": [
      "git-log priority git-log chosen over build-output when commit header present in first window",
      "git-log priority pure git diff still stays git-diff",
      "PR #1175 - adversarial: user code containing build strings file content with 'BUILD SUCCESS' on its own line triggers buildOutput",
      "PR #1175 - adversarial: user code containing build strings real cargo error spanning multiple lines preserves context",
      "PR #1175 - adversarial: user code containing build strings user JS code with console.log('npm warn ...') triggers buildOutput",
      "PR #1175 - corruption safety input with ONLY a single ERROR: line",
      "PR #1175 - corruption safety input with only Downloading lines",
      "PR #1175 - corruption safety input with only progress lines (no errors/warnings/summary) returns input fallback",
      "PR #1175 - corruption safety more than 3 deprecations: keep first 3 verbatim + count rest",
      "PR #1175 - corruption safety safeApply wraps buildOutput against panics",
      "PR #1175 - corruption safety unicode/emoji in deprecation warning preserved (minimal fix keeps first 3 verbatim)",
      "PR #1175 - DETECT_WINDOW boundary build pattern at very start: detected",
      "PR #1175 - DETECT_WINDOW boundary build pattern beyond DETECT_WINDOW chars: NOT detected",
      "PR #1175 - integration with compressMessages compressed output never grows input (safety guard)",
      "PR #1175 - integration with compressMessages input below MIN_COMPRESS_SIZE → NOT compressed",
      "PR #1175 - integration with compressMessages npm install output above MIN_COMPRESS_SIZE → compressed",
      "PR #1175 - integration with compressMessages tool_result with is_error:true is NOT compressed (preserve error traces)",
      "PR #1175 - line endings & whitespace Compiling without leading spaces",
      "PR #1175 - line endings & whitespace CRLF line endings still detect",
      "PR #1175 - line endings & whitespace Tab-prefixed Compiling (real cargo output uses leading spaces, not tab)",
      "PR #1175 - pathological 10000 Compiling lines don't crash",
      "PR #1175 - pathological input with only newlines",
      "PR #1175 - pathological null/undefined safety via safeApply",
      "PR #1175 - pathological very long single line (no newlines) with build pattern",
      "PR #1175 - porcelain regression deeper 100% workdir-only porcelain → STILL detects gitStatus (minimal fix preserved old regex)",
      "PR #1175 - porcelain regression deeper manual gitStatus() call on workdir-only porcelain still parses correctly",
      "PR #1175 - porcelain regression deeper mixed staged + workdir + untracked porcelain → detected (has status code first char)",
      "PR #1175 - priority with overlapping patterns git-diff wins over buildOutput when both present",
      "PR #1175 - priority with overlapping patterns git-status (long form) wins over buildOutput"
    ],
    "tests/unit/builtin-server-tool-drop-3133.test.js": [
      "dropping a built-in server tool is announced (#3133) leaves Anthropic alone — there the tool is supported and must survive",
      "dropping a built-in server tool is announced (#3133) reports every dropped tool, not just the first",
      "dropping a built-in server tool is announced (#3133) stays quiet when the request carries only ordinary function tools",
      "dropping a built-in server tool is announced (#3133) still strips it, since forwarding a tool the provider cannot run is worse",
      "dropping a built-in server tool is announced (#3133) warns, naming the tool and the provider that cannot run it"
    ],
    "tests/unit/bulk-add-names.test.js": [
      "planBulkAdd: auto-named gap-fill (the replace bug) continues past the highest existing index when low slots are taken",
      "planBulkAdd: auto-named gap-fill (the replace bug) gap-fills around existing names — never reuses an existing name",
      "planBulkAdd: auto-named gap-fill (the replace bug) skips blank/whitespace-only lines but keeps indexing contiguous",
      "planBulkAdd: auto-named gap-fill (the replace bug) uses Key 1..N by paste index when nothing exists",
      "planBulkAdd: auto-named gap-fill (the replace bug) within-batch names are unique even for the same free slot",
      "planBulkAdd: cloudflare-ai (name|apiKey|accountId) 1-part cloudflare line is auto-named Key N",
      "planBulkAdd: cloudflare-ai (name|apiKey|accountId) 2-part cloudflare line is name|apiKey (no accountId)",
      "planBulkAdd: cloudflare-ai (name|apiKey|accountId) parses 3-part lines into name + apiKey + accountId",
      "planBulkAdd: custom name|apiKey apiKey containing pipes is preserved (parts after first rejoined)",
      "planBulkAdd: custom name|apiKey custom name avoids an existing same-base name",
      "planBulkAdd: custom name|apiKey uses the literal base name with a gap-filled index",
      "planBulkAdd: robustness coerces non-array existingNames gracefully",
      "planBulkAdd: robustness falls back to base 'Key' when name part is empty",
      "planBulkAdd: robustness returns [] for no input",
      "planBulkAdd: robustness trims names and apiKeys"
    ],
    "tests/unit/bulk-model-id-entry-2035.test.js": [
      "pasting a list of model ids (#2035) drops duplicates within the paste, keeping first position",
      "pasting a list of model ids (#2035) keeps a single id intact",
      "pasting a list of model ids (#2035) returns nothing for blank input",
      "pasting a list of model ids (#2035) splits on newlines and commas, trimming and dropping empties"
    ],
    "tests/unit/bun-disconnect-warning-3559.test.js": [
      "the Bun disconnect gap is announced, not silent (#3559) it names the runtime that does work, so the warning is actionable",
      "the Bun disconnect gap is announced, not silent (#3559) nothing bridges req or socket events into a synthetic response close",
      "the Bun disconnect gap is announced, not silent (#3559) start-up warns when the runtime is Bun",
      "the Bun disconnect gap is announced, not silent (#3559) the warning is emitted once at module scope, not per request"
    ],
    "tests/unit/bypass-handler-null-content.test.js": [
      "bypass handler null content parts ignores a null system content part while detecting a naming request",
      "bypass handler null content parts ignores a null user content part while detecting Warmup",
      "bypass handler treats the request shape the same for any client bypasses a bare count probe regardless of what called it",
      "bypass handler treats the request shape the same for any client bypasses a Warmup probe with no user-agent involved at all",
      "bypass handler treats the request shape the same for any client leaves an ordinary multi-message conversation alone"
    ],
    "tests/unit/bypass-handler-responses.test.js": [
      "bypass detection patterns bypasses a title-extraction probe (trailing assistant '{')",
      "bypass detection patterns bypasses on every configured SKIP_PATTERN",
      "bypass detection patterns ignores non-text and non-array content shapes when extracting text",
      "bypass detection patterns returns null when messages are absent or empty",
      "ccFilterNaming title path builds a 3-word title from the user message (non-streaming, OpenAI shape)",
      "ccFilterNaming title path builds a Claude-format naming response without throwing (non-streaming)",
      "ccFilterNaming title path does nothing without the ccFilterNaming flag",
      "ccFilterNaming title path reads an array-of-blocks body.system (Claude shape)",
      "ccFilterNaming title path reads the system prompt from an in-messages system role too",
      "ccFilterNaming title path streams the naming payload when stream is not disabled",
      "non-streaming response construction merges translated chunks into a complete Claude message with usage",
      "non-streaming response construction returns a direct OpenAI JSON body when source format is OpenAI",
      "streaming response construction emits SSE chunks ending with [DONE] for an OpenAI-shaped request",
      "streaming response construction translates chunks to the Claude event stream when the body is Claude-shaped"
    ],
    "tests/unit/cache-epoch-saturation.test.js": [
      "cache-epoch saturation on oversized bodies (#trackCacheEpoch) byte-identical bodies over 128 KiB should not report a saturated ce or a false compact hint"
    ],
    "tests/unit/cache-prefix-affinity.test.js": [
      "cache-prefix routing key ignores a breakpoint that only appears in messages",
      "cache-prefix routing key is stable across the turns of one agent",
      "cache-prefix routing key never returns anything but a digest",
      "cache-prefix routing key reads the system block even when the tools carry a breakpoint too",
      "cache-prefix routing key returns the empty string when the body carries no breakpoint",
      "cache-prefix routing key separates agents that differ only in their tool set",
      "cache-prefix routing key separates two subagents that inherit one session id",
      "cache-prefix routing key stops at the first breakpoint and ignores what follows it",
      "cache-prefix routing key survives a body it cannot serialize instead of throwing"
    ],
    "tests/unit/cache-pricing.test.js": [
      "cacheMultiplierFor anthropic and claude resolve to the 0.1/1.25 (5m) multipliers",
      "cacheMultiplierFor openai and gemini carry no fallback discount: unstated cache rates degrade to plain pricing",
      "cacheMultiplierFor unknown providers degrade to plain pricing (1.0/1.0)",
      "costForUsage a 0.0 cache rate is a real rate, not a fallback trigger",
      "costForUsage clamps cache quantities to the prompt total and negatives to zero",
      "costForUsage default multipliers charge cache tokens at the plain input rate",
      "costForUsage explicit per-model cache rates win over multipliers",
      "costForUsage splits a canonical usage into the four billed parts",
      "costForUsage tolerates a missing usage or pricing"
    ],
    "tests/unit/cache-usage-accounting-parity.test.js": [
      "conservation: input + cache_read + cache_creation === prompt_tokens kiro-to-claude: flat spelling conserves prompt_tokens",
      "conservation: input + cache_read + cache_creation === prompt_tokens kiro-to-claude: nested spelling conserves prompt_tokens",
      "conservation: input + cache_read + cache_creation === prompt_tokens openai-to-claude: flat spelling conserves prompt_tokens",
      "conservation: input + cache_read + cache_creation === prompt_tokens openai-to-claude: nested spelling conserves prompt_tokens",
      "openai-to-claude: input_tokens is cache-exclusive no cache reported: input_tokens stays the full prompt",
      "openai-to-claude: input_tokens is cache-exclusive reads a gateway's top-level cached_tokens when no details block exists",
      "openai-to-claude: input_tokens is cache-exclusive subtracts both cache subsets from the inclusive prompt_tokens (flat Anthropic spelling)",
      "openai-to-claude: input_tokens is cache-exclusive subtracts the nested prompt_tokens_details spelling too",
      "translator parity on identical usage payloads identical final usage: cache-free stream",
      "translator parity on identical usage payloads identical final usage: flat Anthropic spelling",
      "translator parity on identical usage payloads identical final usage: nested details spelling"
    ],
    "tests/unit/cached-token-e2e.test.js": [
      "cached-token end-to-end (persist + aggregate + cost) Claude cache usage: canonical prompt is inclusive, cached persisted, cost correct",
      "cached-token end-to-end (persist + aggregate + cost) OpenAI cache usage: inclusive prompt passes through, cached counted once",
      "cached-token end-to-end (persist + aggregate + cost) persists a provider exact total when the model has no local price",
      "cached-token end-to-end (persist + aggregate + cost) Today stats count input/output-shaped usage"
    ],
    "tests/unit/cached-token-usage.test.js": [
      "Anthropic streaming usage (message_start carries cache, message_delta output-only) does not forward provider cost metadata in public OpenAI usage",
      "Anthropic streaming usage (message_start carries cache, message_delta output-only) does not let a NaN field poison the running max-merge",
      "Anthropic streaming usage (message_start carries cache, message_delta output-only) extractUsage reads input + cache from message_start",
      "Anthropic streaming usage (message_start carries cache, message_delta output-only) keeps provider exact totals through both usage extraction paths",
      "Anthropic streaming usage (message_start carries cache, message_delta output-only) merges message_start cache with message_delta output without clobbering",
      "calculateCostFromTokens (canonical inclusive convention) applies only the reasoning-rate delta when reasoning is priced above output",
      "calculateCostFromTokens (canonical inclusive convention) does not double-bill reasoning_tokens that are a subset of output",
      "calculateCostFromTokens (canonical inclusive convention) does not let cache_creation drive nonCached negative",
      "calculateCostFromTokens (canonical inclusive convention) matches plain input pricing when no cache present",
      "calculateCostFromTokens (canonical inclusive convention) prefers a valid provider exact total over local token pricing",
      "calculateCostFromTokens (canonical inclusive convention) prices cached + cache_creation as subsets of an inclusive prompt without double-counting",
      "calculateCostFromTokens (canonical inclusive convention) rejects invalid provider totals before falling back to local pricing",
      "canonicalizeUsage extracts and persists cached Responses input tokens for internal accounting",
      "canonicalizeUsage folds a Claude cache-miss first write (cache_creation only, no cache_read yet)",
      "canonicalizeUsage folds Claude exclusive cache into an inclusive prompt count",
      "canonicalizeUsage handles no-cache usage",
      "canonicalizeUsage is idempotent (running twice yields the same canonical shape)",
      "canonicalizeUsage keeps finite nonnegative provider totals and drops invalid ones",
      "canonicalizeUsage passes through Gemini inclusive prompt (cachedContent already counted)",
      "canonicalizeUsage passes through OpenAI inclusive prompt unchanged",
      "canonicalizeUsage reads Responses-API nested detail shapes (input_tokens_details / output_tokens_details)",
      "canonicalizeUsage returns null for invalid input",
      "Kiro usage pass-through forward-compat: surfaces cache fields if Kiro event shape grows them",
      "Kiro usage pass-through passes through plain input/output when no cache fields are present"
    ],
    "tests/unit/caller-abort-propagation.test.js": [
      "caller abort propagation forwards only safe model failure metadata into account state",
      "caller abort propagation keeps a selected all-locked chat account separate from a preceding failed account",
      "caller abort propagation preserves caller identity through the request-buffer replay candidate and does not mutate an aborted account",
      "caller abort propagation preserves the original signal through /api/v1/api/chat and short-circuits typed abort",
      "caller abort propagation preserves the original signal through /api/v1/chat/completions and short-circuits typed abort",
      "caller abort propagation preserves the original signal through /api/v1/messages and short-circuits typed abort",
      "caller abort propagation preserves the original signal through /api/v1/responses and short-circuits typed abort",
      "caller abort propagation preserves the original signal through /api/v1/responses/compact and short-circuits typed abort",
      "caller abort propagation projects the selected model client status without changing raw credential state",
      "caller abort propagation takes one ordinary account-failure transition for a body deadline",
      "caller abort propagation uses a generic unavailable response for a legacy lock without metadata"
    ],
    "tests/unit/capabilities-openrouter-lane-window.test.js": [
      "OpenRouter lane models resolve their real provider ceiling keeps deepseek/deepseek-v4-flash-0731 at 1048576 with its deepseek thinking caps",
      "OpenRouter lane models resolve their real provider ceiling keeps z-ai/glm-5.3-flash at 1048576 with its vision and zai thinking caps",
      "OpenRouter lane models resolve their real provider ceiling meets the lane context_exact pins of 1048575 and 1048576",
      "OpenRouter lane models resolve their real provider ceiling resolves deepseek/deepseek-v4-flash-0731(medium) to the same 1048576 window as its bare id",
      "OpenRouter lane models resolve their real provider ceiling resolves z-ai/glm-5.3-flash(high) to the same 1048576 window as its bare id"
    ],
    "tests/unit/capabilities-opus-context.test.js": [
      "Claude Opus 1M context capabilities keeps the older Opus 4.5 at the standard 200k context",
      "Claude Opus 1M context capabilities resolves claude-opus-4-6 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-4-7 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-4-8 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-4.7 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-4.8 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-5 to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-5-agentic to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-5-thinking to a 1M context window",
      "Claude Opus 1M context capabilities resolves claude-opus-5-thinking-agentic to a 1M context window"
    ],
    "tests/unit/capabilities-service-kind.test.js": [
      "capabilitiesFromServiceKind maps imageToText custom models to vision-capable runtime models",
      "capabilitiesFromServiceKind maps media output/input custom model kinds to runtime capabilities"
    ],
    "tests/unit/capabilities.test.js": [
      "getCapabilitiesForModel marks DeepSeek V4 Flash Vision as vision-capable",
      "getCapabilitiesForModel reports Kiro Claude Opus 4.8 as a 1M context model",
      "getCapabilitiesForModel reports Kiro Claude Opus 5 variants as 1M adaptive-thinking models",
      "getCapabilitiesForModel reports Kiro Claude Sonnet 5 as a 1M adaptive-thinking model",
      "getCapabilitiesForModel reports Kiro GPT 5.6 models with the Kiro 272k context window",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) bare id without a provider keeps default (no vision)",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) caps oc/ox-alpha-free report image+reasoning via opencode format",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) caps ocg/ox-alpha-free report image+reasoning via opencode format",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) caps opencode-go/ox-alpha-free report image+reasoning via opencode format",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) caps opencode/ox-alpha-free report image+reasoning via opencode format",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) generic claude-sonnet-4.6(max) equals its base caps (existing behavior kept)",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) mismatched providers do not pick up Ox Alpha caps",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) numeric suffix '(8192)' resolves to identical caps",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) suffix '(max)' resolves to identical caps for all 4 pairs",
      "Ox Alpha capability entries (provider-scoped, upstream #3483) x-preview-f-free keeps fork videoInput + opencode format"
    ],
    "tests/unit/capacity-activity-ui.test.js": [
      "bounds both tracks to the full covered minute when only one bucket remains",
      "does not turn entirely unattributed history into a known zero requests count",
      "includes the final bucket end across multiple sparse buckets without changing observations",
      "keeps data failures explicit and retryable",
      "keeps logical request totals, attempts and paired cache token share distinct",
      "keeps multi-day ticks distinct and supports seconds only for sub-minute buckets",
      "leaves empty or overflowing time domains unset",
      "leaves the time domain unset for invalid bucket width -60000",
      "leaves the time domain unset for invalid bucket width 0",
      "leaves the time domain unset for invalid bucket width Infinity",
      "leaves the time domain unset for invalid bucket width NaN",
      "leaves the time domain unset for invalid bucket width null",
      "leaves the time domain unset for invalid bucket width undefined",
      "offers a scale and a style for the time charts, and a grouped metric for the calendar",
      "preserves missing token samples and sparse gaps instead of drawing measured zeros",
      "retains dates across UTC midnight even within a short interval",
      "selects a clipped interval from a chart click and a whole day from a calendar cell",
      "sums a calendar metric per UTC day from measured buckets only and scopes a chosen day"
    ],
    "tests/unit/capacity-adapter-config-and-stripping.test.js": [
      "augmentModelsWithCapacityAdapter handles bare model strings without a provider slash",
      "augmentModelsWithCapacityAdapter leaves models untouched when a member already satisfies, or the pool cannot",
      "augmentModelsWithCapacityAdapter no-ops on empty hard caps, non-array models, or empty models",
      "augmentModelsWithCapacityAdapter prepends capable pool models when no original model satisfies, dedupes originals",
      "getCapacityAdapterConfig missing or malformed entry disables the capability",
      "getCapacityAdapterConfig normalizes the legacy array form: entries via .model, falsy dropped, enabled fallback",
      "getCapacityAdapterConfig substitutes the default fallback model when enabled with an empty pool",
      "getCapacityAdapterModels flattens enabled pools in capability order, deduped, skipping disabled",
      "strategy selection getActiveAdapterStrategy picks the first hard capability with a usable pool",
      "strategy selection round-robin only when enabled AND roundRobin",
      "stripHistoryForContext keeps system + head + trailing user run, drops the middle",
      "stripHistoryForContext returns body unchanged when head keep covers all older turns within budget",
      "stripHistoryForContext returns body unchanged when no message-array key exists or the array is empty",
      "stripHistoryForContext returns body unchanged when only system messages exist, or no older turns",
      "stripHistoryForContext supports the input and contents keys and array-block content lengths",
      "withCapacityAdapterStripping returns the handler itself when the pool is empty",
      "withCapacityAdapterStripping strips history only for adapter models, passing extra args through"
    ],
    "tests/unit/capacity-adapter-vision-fallback-1419.test.js": [
      "capacity adapter vision fallback declarations (#1419) declares the adapter's default vision fallback model as vision-capable",
      "capacity adapter vision fallback declarations (#1419) leaves the model list untouched when a member already covers vision",
      "capacity adapter vision fallback declarations (#1419) prepends the vision-capable fallback ahead of a non-vision target when enabled"
    ],
    "tests/unit/capacity-controls.test.js": [
      "per-account drain confirmation keeps a confirmed success and later stale refusal separate",
      "per-account drain confirmation never reports an HTTP success with a different readback version as persisted",
      "per-account drain confirmation opens only an exact physical activity record with an explicit retained session",
      "per-account drain confirmation preserves uncertain network outcomes and does not retry an unknown mutation",
      "per-account drain confirmation uses the query version when reversing and refuses missing versions before mutation"
    ],
    "tests/unit/capacity-economics-binding-repair.test.js": [
      "guarded existing synthetic fixture completion binding binds only the known six usage and three cost rows, preserves two legacy rows, and is idempotent",
      "guarded existing synthetic fixture completion binding refuses receipt identity drift before writing",
      "guarded existing synthetic fixture completion binding refuses timestamp drift before mutating any completion identity",
      "guarded existing synthetic fixture completion binding requires an explicit synthetic fixture declaration"
    ],
    "tests/unit/capacity-economics-fixture.test.js": [
      "versioned retained Capacity and Economics scenarios exports the exact selected historical series with explicit capture bounds",
      "versioned retained Capacity and Economics scenarios exposes only selected matching cost rows and never emits the private session identifier",
      "versioned retained Capacity and Economics scenarios projects captured-rate arithmetic, reported zero, negative modeling and refused joins",
      "versioned retained Capacity and Economics scenarios retains separate same-scope units, a fresh reset observation and stale evidence"
    ],
    "tests/unit/capacity-fixture-guard.test.js": [
      "disposable Capacity fixture guard permits encrypted synthetic configuration on a subsequent run without reading auth files",
      "disposable Capacity fixture guard permits exact plain fixture readback and temporary-directory compatibility",
      "disposable Capacity fixture guard permits names in their schema column with empty plain or encrypted extras",
      "disposable Capacity fixture guard refuses a different explicit root and a symlinked root",
      "disposable Capacity fixture guard refuses encrypted rows without the matching explicitly supplied key",
      "disposable Capacity fixture guard refuses encrypted rows without the matching explicitly supplied key",
      "disposable Capacity fixture guard rejects a changed id in the persisted fixture",
      "disposable Capacity fixture guard rejects a changed name in the persisted fixture",
      "disposable Capacity fixture guard rejects a changed provider in the persisted fixture",
      "disposable Capacity fixture guard rejects an incompatible manifest count",
      "disposable Capacity fixture guard rejects an incompatible manifest name",
      "disposable Capacity fixture guard rejects an incompatible manifest provider",
      "disposable Capacity fixture guard rejects an incompatible manifest version",
      "disposable Capacity fixture guard rejects an invalid schema name despite a Synthetic name in extras",
      "disposable Capacity fixture guard rejects an invalid schema name despite a Synthetic name in extras",
      "disposable Capacity fixture guard rejects an invalid schema name despite a Synthetic name in extras",
      "disposable Capacity fixture guard rejects deeply nested usable credentials after decryption without exposing them"
    ],
    "tests/unit/cascade-policy-api.test.js": [
      "returns the route-local admin refusal before reads, parsing or writes",
      "validates bounded fields before repository mutation"
    ],
    "tests/unit/cascade-policy.test.js": [
      "normalizes each valid unique mapping without reordering it",
      "persists reviewed mappings and immutable receipts without touching other policy, then restores through a fresh revision",
      "refuses memory-backed persistence without writing settings or receipts",
      "rejects malformed, duplicate, self and oversized mappings before mutation"
    ],
    "tests/unit/catalog-controls-ui.test.js": [
      "asks inline before disabling a model and sends nothing until the pair is confirmed",
      "commits an alias from the row itself on Enter, with no dialog anywhere",
      "deletes an alias when the field is emptied",
      "registers a custom model from the inline add row rather than a dialog",
      "reverts an alias edit on Escape without sending anything",
      "saves the sticky round-robin limit alone, leaving the visible strategy untouched",
      "writes the whole four-kind capability object when one capability is toggled"
    ],
    "tests/unit/catalog-tools-model.test.js": [
      "catalog batch contracts accepts both import envelopes without discarding advanced fields",
      "catalog batch contracts checks sync status and exact saved plan order",
      "catalog batch contracts does not treat an already-registered entry as a capability overwrite",
      "catalog batch contracts encodes each exact deletion identity and repeats the id parameter",
      "catalog batch contracts keeps failed diagnostics and measured zero latency in both response shapes",
      "catalog batch contracts requires all matching account cooldowns to disappear without claiming failure repair",
      "catalog batch contracts verifies deletion only within the requested provider and kind",
      "catalog batch contracts verifies successful partial import entries and their persisted capability fields"
    ],
    "tests/unit/catalog-tools-ui.test.js": [
      "does not send on mount and keeps partial import results plus the original draft after readback",
      "keeps a successful write with failed readback unconfirmed and never repeats it",
      "keeps suggested membership editable and verifies exact saved order",
      "names provider-wide cooldown scope and verifies every matching lock is absent",
      "requires explicit confirmation for provider diagnostics and renders each mixed result",
      "sends a single repeated-id delete after showing exact selected identities",
      "synchronizes metadata only after confirmation and verifies the persisted check"
    ],
    "tests/unit/caveman-prompts.test.js": [
      "Caveman internal consistency no level uses Unicode arrow (SHARED_NO_DECORATION bans arrow shorthand)",
      "Caveman prompt coverage adds no-decorative-emoji guidance to every level",
      "Caveman prompt coverage adds no-invented-abbreviations guidance to every level",
      "Caveman prompt coverage adds no-self-reference guidance to every level",
      "Caveman prompt coverage adds preserve-user-language guidance to every level",
      "Caveman prompt coverage every level key has matching prompt and vice versa",
      "Caveman prompt coverage has a prompt string for every level",
      "Caveman ULTRA targeted sync does not encourage arrow shorthand",
      "Caveman ULTRA targeted sync does not encourage invented abbreviations"
    ],
    "tests/unit/caveman-tools-unaffected-1811.test.js": [
      "the terseness prompt never suppresses tool calls (#1811) arguments are exempted from compression too",
      "the terseness prompt never suppresses tool calls (#1811) every level carries the carve-out",
      "the terseness prompt never suppresses tool calls (#1811) it says calls happen, not merely that they stay short",
      "the terseness prompt never suppresses tool calls (#1811) the instruction that motivated this is still present",
      "the terseness prompt never suppresses tool calls (#1811) the pre-existing exact-preservation boundaries survive",
      "the terseness prompt never suppresses tool calls (#1811) ultra is still the most compressed level"
    ],
    "tests/unit/cert-install-error-558.test.js": [
      "the macOS cert install reports what actually failed (#558) bounds the detail rather than pasting an unbounded blob",
      "the macOS cert install reports what actually failed (#558) carries the underlying error text through",
      "the macOS cert install reports what actually failed (#558) falls back to the plain message when there is no detail",
      "the macOS cert install reports what actually failed (#558) still names a user cancellation specifically",
      "the macOS cert install reports what actually failed (#558) the surfaced text is stderr, which never carries the sudo password"
    ],
    "tests/unit/chat-connect-timeout-propagation.test.js": [
      "chat connect timeout propagation carries caller cancellation into the credential-refresh executor attempt",
      "chat connect timeout propagation carries caller cancellation into the field-strip executor attempt",
      "chat connect timeout propagation carries caller cancellation into the initial executor attempt",
      "chat connect timeout propagation forbids another replay after an accepted field-strip retry returns a synthetic failure",
      "chat connect timeout propagation forbids replay after a field-strip retry loses its transport outcome",
      "chat connect timeout propagation maps an initial caller abort to 499",
      "chat connect timeout propagation maps an initial typed timeout to 502",
      "chat connect timeout propagation maps an unrelated credential-refresh retry error to 502",
      "chat connect timeout propagation maps credential-refresh retry transport error to AbortError: client left",
      "chat connect timeout propagation maps credential-refresh retry transport error to ConnectTimeoutError: Upstream response headers exceeded 8000ms",
      "chat connect timeout propagation maps field-strip retry transport error to AbortError: client left",
      "chat connect timeout propagation maps field-strip retry transport error to ConnectTimeoutError: Upstream response headers exceeded 8000ms",
      "chat connect timeout propagation passes the same context to initial and credential-refresh attempts",
      "chat connect timeout propagation passes the same context to initial and field-strip attempts",
      "chat connect timeout propagation preserves non-replayable provenance on an executor's synthetic HTTP 400",
      "chat connect timeout propagation preserves non-replayable provenance on an executor's synthetic HTTP 401",
      "chat connect timeout propagation preserves non-replayable provenance on an executor's synthetic HTTP 403",
      "chat connect timeout propagation preserves non-replayable provenance on an executor's synthetic HTTP 503",
      "Codex Sol Fast policy applies priority after Claude translation for gpt-5.6-sol-review(ultra)",
      "Codex Sol Fast policy applies priority after Claude translation for gpt-5.6-sol(max)",
      "Codex Sol Fast policy does not apply outside the enabled Sol scope",
      "Codex Sol Fast policy does not apply outside the enabled Sol scope",
      "Codex Sol Fast policy does not apply outside the enabled Sol scope",
      "Codex Sol Fast policy is provider-scoped and leaves the input body untouched",
      "Codex Sol Fast policy preserves explicit service tier \"default\" with Fast=false for gpt-5.6-luna",
      "Codex Sol Fast policy preserves explicit service tier \"default\" with Fast=true for gpt-5.6-sol",
      "Codex Sol Fast policy preserves explicit service tier \"priority\" with Fast=false for gpt-5.6-sol",
      "Codex Sol Fast policy preserves explicit service tier \"priority\" with Fast=true for gpt-5.6-sol",
      "Codex Sol Fast policy preserves explicit service tier \"priority\" with Fast=true for gpt-5.6-terra",
      "Codex Sol Fast policy preserves explicit service tier \"unsupported\" with Fast=true for gpt-5.6-sol"
    ],
    "tests/unit/chat-core-client-status-metadata.test.js": [
      "chat core model failure metadata keeps a generic structured ModelError at its raw status",
      "chat core model failure metadata keeps a selected all-locked account's reason and raw status separate from a failed account",
      "chat core model failure metadata projects selected metadata through every credentialed all-rate-limited handler",
      "chat core model failure metadata retains a verified payload only long enough to attach safe model metadata"
    ],
    "tests/unit/chat-handler-dispatch-branches.test.js": [
      "admission gates in order refuses a disabled model with 404 naming it",
      "admission gates in order returns the allowlist refusal as-is",
      "admission gates in order short-circuits a bypass (naming/warmup) request before rotation",
      "auto router answers 503 when nothing is routable",
      "auto router lets a user combo named \"auto\" outrank the router",
      "auto router substitutes the routed model and continues the ordinary path",
      "body and model validation refuses a body without a model with 400",
      "body and model validation refuses an unparseable JSON body with 400",
      "body and model validation tolerates a clientRawRequest with no headers field",
      "capacity adapter on the solo path drops an adapter whose provider is not reachable and stays solo",
      "capacity adapter on the solo path spreads over a combo when a reachable adapter is added",
      "combo expansion expands a nested combo with its own fusion strategy",
      "combo expansion fusion strategy strips tools from panel members",
      "combo expansion narrows a combo to the agent-role group and reports the narrowing",
      "combo expansion pins a combo member to its configured connection, strictly",
      "combo expansion refuses a combo cycle instead of recursing",
      "dispatch-scoped configuration snapshot reads the settings row the same number of times however many accounts one dispatch burns",
      "model-name rewrites before routing applies the claude-compat rewrite when the toggle is on",
      "model-name rewrites before routing strips the [1m] context suffix before any lookup",
      "providerConcurrencyOverflow fails open when active requests are unreadable",
      "providerConcurrencyOverflow fails open when settings are unreadable",
      "providerConcurrencyOverflow ignores a malformed cap",
      "providerConcurrencyOverflow refuses at the cap with the live count in the message",
      "readAttemptCeiling accepts only a positive safe integer",
      "the single-model loop answers 499 when the caller signal is already aborted",
      "the single-model loop cold-fills the antigravity projectId, persists it, and wires the callbacks"
    ],
    "tests/unit/chat-request-replay.test.js": [
      "chat request replay does not replay a buffer error without proof the generation was rejected",
      "chat request replay passes invalid imported timeout values through without coercion",
      "chat request replay passes only an exact persisted Fast boolean 0",
      "chat request replay passes only an exact persisted Fast boolean 1",
      "chat request replay passes only an exact persisted Fast boolean 2",
      "chat request replay passes only an exact persisted Fast boolean 3",
      "chat request replay passes unresolved provider and global timeouts to chat core",
      "chat request replay replays once on the same account before returning success",
      "chat request replay returns a repeated overflow without looping"
    ],
    "tests/unit/chat-search-extractors-coverage.test.js": [
      "antigravity grounding edges skips chunks without urls and supports pointing at missing indices",
      "antigravity grounding edges windows a mid-text segment with ellipses on both cut edges",
      "gemini config extracts text and citations, dropping chunks without a web url",
      "gemini config sends the config-built endpoint, body and headers",
      "gemini config survives an empty payload",
      "kimi tool-call edges skips calls without arguments",
      "minimax config builds the request from its own config",
      "minimax config falls back to tool_calls, tolerating bad JSON and url-less items",
      "minimax config prefers direct web_search_results and skips entries without urls",
      "openai fallback citations falls back to top-level citations when no annotations exist, coercing shapes",
      "perplexity citation coercion coerces strings and objects, drops the rest",
      "perplexity-agent config builds the request from its own config",
      "perplexity-agent config collects text, annotation citations and result items from output blocks",
      "perplexity-agent config falls back to top-level citations when output carried none",
      "the request timeout timer actually fires aborts a hung upstream and answers 504"
    ],
    "tests/unit/chat-search-handler.test.js": [
      "error propagation keeps upstream status intact a non-string upstream error object is serialized, not [object Object]",
      "error propagation keeps upstream status intact maps a network error to 502 with the message",
      "error propagation keeps upstream status intact maps AbortError to 504",
      "error propagation keeps upstream status intact non-JSON upstream body is 502, never a throw",
      "error propagation keeps upstream status intact upstream 429 passes through as 429 with the upstream error message",
      "request building and the unified success envelope antigravity: sends project + IDE user agent, unwraps { response }, dedupes repeated sources by URL",
      "request building and the unified success envelope every provider's extractAnswer survives an empty object without throwing",
      "request building and the unified success envelope falls back to the registry default model when none is given",
      "request building and the unified success envelope glm: reads the side-channel web_search array with its `link` spelling",
      "request building and the unified success envelope kimi: citations parsed out of $web_search tool_call arguments; malformed JSON args skipped, not thrown",
      "request building and the unified success envelope maxResults caps the citation list; a bogus maxResults falls back to the default 10",
      "request building and the unified success envelope openai: search-preview model gets no tools, other models get web_search; usage totals flow to llm_tokens",
      "request building and the unified success envelope xai: concatenates responses-API output text and collects annotation citations",
      "trust-boundary validation (no fetch fired) antigravity without a projectId is a 401 naming the reconnect, not an upstream 403",
      "trust-boundary validation (no fetch fired) rejects a missing or non-string query with 400",
      "trust-boundary validation (no fetch fired) rejects an unknown provider with 400",
      "trust-boundary validation (no fetch fired) rejects missing credentials with 401"
    ],
    "tests/unit/chatcore-cache-epoch-isolation.test.js": [
      "cache-epoch tracking per session finds a shared prefix when the next request extends the previous body",
      "cache-epoch tracking per session handles a rewritten history that shares no prefix with the previous body",
      "cache-epoch tracking per session reports no shared prefix on a session's first request",
      "cache-epoch tracking per session shrinking a session body below half its previous size still dispatches",
      "progressive tool disclosure drops tools the static filter excludes",
      "progressive tool disclosure keeps the client's own non-MCP tools whatever maxTools says",
      "progressive tool disclosure leaves the tool list alone when the filter excludes nothing",
      "progressive tool disclosure runs neither stage when the request opts out of token saving",
      "progressive tool disclosure skips both stages when the tools array is empty",
      "progressive tool disclosure trims an MCP catalogue to maxTools once BM25 disclosure is on",
      "request-body isolation does not hand the caller's own message objects to the executor",
      "request-body isolation isolates a Responses-API input array as well as messages",
      "request-body isolation isolates conversationState and survives a non-cloneable member",
      "request-body isolation leaves a body with neither messages nor input alone"
    ],
    "tests/unit/chatcore-module-helpers-coverage.test.js": [
      "isTokenExpiringSoon accepts an epoch-millisecond expiry, not only an ISO string",
      "isTokenExpiringSoon honours an explicit buffer and treats an already-past expiry as expiring",
      "isTokenExpiringSoon is true inside the default buffer and false outside it",
      "isTokenExpiringSoon treats '' as not expiring",
      "isTokenExpiringSoon treats +0 as not expiring",
      "isTokenExpiringSoon treats null as not expiring",
      "isTokenExpiringSoon treats undefined as not expiring",
      "logProxySelection does not throw when no log sink is supplied",
      "logProxySelection falls back to the connection id, then to unknown, for the conn= tag",
      "logProxySelection logs no PROXY line when neither a relay nor an enabled connection proxy is set",
      "logProxySelection logs the no_proxy list on debug when the connection proxy carries one",
      "REQ summary listener feeding sessionCalibrationFor ignores a non-string rid",
      "REQ summary listener feeding sessionCalibrationFor ignores a rid that was never registered against a session",
      "REQ summary listener feeding sessionCalibrationFor ignores a summary with no rid field",
      "REQ summary listener feeding sessionCalibrationFor ignores an ok summary whose token fields sum to zero",
      "REQ summary listener feeding sessionCalibrationFor leaves calibration alone on a non-ok verdict",
      "REQ summary listener feeding sessionCalibrationFor returns the identity factor for a falsy sid",
      "stripContinuityFields drops encrypted continuity fields even with no provider or model given",
      "stripContinuityFields returns the body untouched when it has no messages array"
    ],
    "tests/unit/chatcore-preparation-cancellation.test.js": [
      "gateway preparation cancellation boundary does no preparation or provider work for an already-aborted caller",
      "gateway preparation cancellation boundary returns an explicit size refusal before dispatch when aggregate media is too large",
      "gateway preparation cancellation boundary returns caller cancellation during embedding without invoking a provider",
      "gateway preparation cancellation boundary returns caller cancellation during headroom without invoking a provider",
      "gateway preparation cancellation boundary returns caller cancellation during media without invoking a provider",
      "gateway preparation cancellation boundary returns caller cancellation during pxpipe without invoking a provider"
    ],
    "tests/unit/chatCore-token-saver.test.js": [
      "chatCore tokenSaver callback failure never affects request — executor still runs",
      "chatCore tokenSaver emits at most once per request for RTK regardless of input shape",
      "chatCore tokenSaver header off (case-insensitive) emits and persists zero events",
      "chatCore tokenSaver Headroom event NOT emitted when diagnostics.after missing",
      "chatCore tokenSaver Headroom event only after diagnostics.after exists and uses proxy-reported tokens",
      "chatCore tokenSaver optional onTokenSaverEvent signature: omitting callback still succeeds",
      "chatCore tokenSaver regressions caveman injection claims CAVEMAN flag when the body actually changed",
      "chatCore tokenSaver regressions failed requests keep save= and path= on the REQ.failed line (audit finding 20)",
      "chatCore tokenSaver regressions no phantom XFORM.injected claim when the prompt was already injected",
      "chatCore tokenSaver regressions privacy measures and emits a row under its own flag with the saver header off",
      "chatCore tokenSaver regressions token-saver rows carry the 8-hex rid",
      "chatCore tokenSaver RTK event uses characters before/after/saved (no bytes fields)"
    ],
    "tests/unit/chenzk-provider.test.js": [
      "Chenzk API provider builds into the runtime PROVIDERS map with the openai format default",
      "Chenzk API provider exposes its seed models",
      "Chenzk API provider is registered as an OpenAI-compatible apikey provider",
      "Chenzk API provider keeps every registry id unique after adding chenzk"
    ],
    "tests/unit/chutes-model-catalog-983.test.js": [
      "#983 chutes registry auto-load prerequisite declares a modelsFetcher pointing at the public catalog",
      "#983 chutes registry auto-load prerequisite leaves the pinned transport (baseUrl/validateUrl/category) unchanged",
      "#983 chutes registry auto-load prerequisite opts into passthroughModels so any fetched id can be selected",
      "#983 chutes registry auto-load prerequisite uses a filter type that suggested-models/filters.js actually implements"
    ],
    "tests/unit/claude-auto-mode-classifier.test.js": [
      "classifier alternatives remain fail-closed rejects a hidden forced Gemini SSE candidate 1 contradiction",
      "classifier alternatives remain fail-closed rejects a hidden Gemini JSON candidate 1 contradiction",
      "classifier alternatives remain fail-closed rejects hidden forced Chat SSE choice 1 with malformed choice",
      "classifier alternatives remain fail-closed rejects hidden forced Chat SSE choice 1 with tool call",
      "classifier alternatives remain fail-closed rejects hidden forced Chat SSE choice 1 with unknown choice field",
      "classifier alternatives remain fail-closed rejects hidden OpenAI Chat JSON choice 1 with malformed choice",
      "classifier alternatives remain fail-closed rejects hidden OpenAI Chat JSON choice 1 with tool call",
      "classifier alternatives remain fail-closed rejects hidden OpenAI Chat JSON choice 1 with unknown choice field",
      "Claude classifier request detector detects exact prefix boundary colon",
      "Claude classifier request detector detects exact prefix boundary end",
      "Claude classifier request detector detects exact prefix boundary period",
      "Claude classifier request detector detects exact prefix boundary whitespace",
      "Claude classifier request detector detects stage one first system block",
      "Claude classifier request detector detects stage one string",
      "Claude classifier request detector detects stage two empty stops",
      "Claude classifier request detector detects stage two string",
      "Claude classifier request detector does not scan assistant block",
      "Claude classifier request detector does not scan image block",
      "Claude classifier request detector does not scan later system block",
      "Claude classifier request detector does not scan role-system message",
      "Claude classifier request detector does not scan tool-result block",
      "Claude classifier request detector does not scan unknown block",
      "Claude classifier request detector does not scan user block",
      "Claude classifier request detector rejects a prefix quoted later in an ordinary prompt",
      "Claude classifier request detector rejects a sentinel without the prefix",
      "Claude classifier request detector rejects invalid system array empty array",
      "Claude classifier request detector rejects invalid system array missing first text",
      "Claude classifier request detector rejects invalid system array non-string first text",
      "Claude classifier request detector rejects invalid system array non-text first block",
      "Claude classifier request detector rejects non-object body array",
      "Claude classifier request detector rejects non-object body boolean",
      "Claude classifier request detector rejects non-object body null",
      "Claude classifier request detector rejects non-object body number",
      "Claude classifier request detector rejects non-object body string",
      "Claude classifier request detector rejects stop_sequences case variant",
      "Claude classifier request detector rejects stop_sequences null",
      "Claude classifier request detector rejects stop_sequences number",
      "Claude classifier request detector rejects stop_sequences object",
      "Claude classifier request detector rejects stop_sequences string",
      "Claude classifier request detector rejects stop_sequences substring",
      "Claude classifier request detector rejects stop_sequences wrong array",
      "Claude classifier request detector rejects stream value \"false\"",
      "Claude classifier request detector rejects stream value {}",
      "Claude classifier request detector rejects stream value 0",
      "Claude classifier request detector rejects stream value null",
      "Claude classifier request detector rejects stream value true",
      "Claude classifier request detector rejects stream value undefined",
      "Claude classifier request detector rejects stream value undefined",
      "Claude classifier request detector rejects system near miss alphanumeric continuation",
      "Claude classifier request detector rejects system near miss changed case",
      "Claude classifier request detector rejects system near miss generic phrase",
      "Claude classifier request detector rejects system near miss leading whitespace",
      "Claude Code response classifier validation accepts empty response.output metadata from data-only JSON type",
      "Claude Code response classifier validation accepts empty response.output metadata from explicit event",
      "Claude Code response classifier validation canonicalizes forced Chat SSE reasoning beside a decision",
      "Claude Code response classifier validation keeps a non-Claude forced Chat SSE near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps a non-Claude Gemini SSE near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps a non-Claude native Claude JSON near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps a non-Claude OpenAI JSON near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps a non-Claude Responses JSON near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps a non-Claude unexpected Responses SSE near miss unchanged without classifier calls",
      "Claude Code response classifier validation keeps an ordinary Claude Responses SSE unprojected and unteed",
      "Claude Code response classifier validation keeps Gemini SSE thinking beside a classifier decision canonical",
      "Claude Code response classifier validation keeps ordinary forced Responses incomplete status cache-aware without teeing",
      "Claude Code response classifier validation keeps the forced Chat SSE decision <block>no</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the forced Chat SSE decision <block>yes</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the Gemini SSE decision <block>no</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the Gemini SSE decision <block>yes</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the native Claude JSON decision <block>no</block> with its Claude identity",
      "Claude Code response classifier validation keeps the native Claude JSON decision <block>yes</block> with its Claude identity",
      "Claude Code response classifier validation keeps the OpenAI Chat JSON decision <block>no</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the OpenAI Chat JSON decision <block>yes</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the Responses JSON decision <block>no</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the Responses JSON decision <block>yes</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the unexpected Responses SSE decision <block>no</block> as a canonical Claude Message",
      "Claude Code response classifier validation keeps the unexpected Responses SSE decision <block>yes</block> as a canonical Claude Message",
      "Claude Code response classifier validation preserves ordinary Chat reasoning when no text is present",
      "Claude Code response classifier validation preserves ordinary JSON fence removal and empty-content fallback",
      "Claude Code response classifier validation preserves ordinary Responses reasoning and custom tool conversion",
      "Claude Code response classifier validation rejects a blank explicit SSE event instead of falling back to JSON type",
      "Claude Code response classifier validation rejects a hidden custom tool from unexpected Responses SSE",
      "Claude Code response classifier validation rejects a legacy OpenAI Chat JSON function_call beside a classifier decision",
      "Claude Code response classifier validation rejects a Responses JSON item dropped by final conversion",
      "Claude Code response classifier validation rejects actionable output hidden in recognized metadata",
      "Claude Code response classifier validation rejects Gemini SSE executableCode beside a classifier decision",
      "Claude Code response classifier validation rejects Gemini SSE unknown actionable content beside a classifier decision",
      "Claude Code response classifier validation rejects Gemini SSE with a function call beside a decision",
      "Claude Code response classifier validation rejects hidden forced Chat SSE images beside a classifier decision",
      "Claude Code response classifier validation rejects hidden forced Chat SSE legacy function_call beside a classifier decision",
      "Claude Code response classifier validation rejects hidden forced Chat SSE unknown delta fields beside a classifier decision",
      "Claude Code response classifier validation rejects invalid forced Chat SSE with decision plus tool use",
      "Claude Code response classifier validation rejects invalid forced Chat SSE with prose",
      "Claude Code response classifier validation rejects invalid forced Chat SSE with two text segments with surrounding prose",
      "Claude Code response classifier validation rejects invalid native Claude JSON with decision plus unknown block",
      "Claude Code response classifier validation rejects invalid native Claude JSON with malformed thinking",
      "Claude Code response classifier validation rejects invalid native Claude JSON with missing content",
      "Claude Code response classifier validation rejects invalid native Claude JSON with thinking only",
      "Claude Code response classifier validation rejects invalid native Claude JSON with tool use",
      "Claude Code response classifier validation rejects invalid native Claude JSON with two text blocks",
      "Claude Code response classifier validation rejects invalid OpenAI Chat JSON with decision plus tool call",
      "Claude Code response classifier validation rejects invalid OpenAI Chat JSON with no choice",
      "Claude Code response classifier validation rejects invalid OpenAI Chat JSON with prose",
      "Claude Code response classifier validation rejects invalid OpenAI Chat JSON with tool call",
      "Claude Code response classifier validation rejects invalid Responses JSON with additional tools",
      "Claude Code response classifier validation rejects invalid Responses JSON with allow then deny",
      "Claude Code response classifier validation rejects invalid Responses JSON with custom tool call",
      "Claude Code response classifier validation rejects invalid Responses JSON with custom tool output",
      "Claude Code response classifier validation rejects invalid Responses JSON with deny then allow",
      "Claude Code response classifier validation rejects invalid Responses JSON with duplicate decisions",
      "Claude Code response classifier validation rejects invalid Responses JSON with earlier prose then decision",
      "Claude Code response classifier validation rejects invalid Responses JSON with empty message then decision",
      "Claude Code response classifier validation rejects invalid Responses JSON with empty output",
      "Claude Code response classifier validation rejects invalid Responses JSON with function call",
      "Claude Code response classifier validation rejects invalid Responses JSON with function call output",
      "Claude Code response classifier validation rejects invalid Responses JSON with malformed reasoning",
      "Claude Code response classifier validation rejects invalid Responses JSON with missing output",
      "Claude Code response classifier validation rejects invalid Responses JSON with non-array output",
      "Claude Code response classifier validation rejects invalid Responses JSON with two text blocks",
      "Claude Code response classifier validation rejects invalid Responses JSON with unknown item",
      "Claude Code response classifier validation rejects invalid Responses JSON with unknown nested block",
      "Claude Code response classifier validation rejects invalid unexpected Responses SSE with duplicate output index",
      "Claude Code response classifier validation rejects invalid unexpected Responses SSE with EOF before terminal",
      "Claude Code response classifier validation rejects invalid unexpected Responses SSE with hidden custom tool",
      "Claude Code response classifier validation rejects invalid unexpected Responses SSE with terminal mismatch",
      "Claude Code response classifier validation rejects malformed classifier prose from forced Chat SSE",
      "Claude Code response classifier validation rejects malformed classifier prose from forced Responses SSE",
      "Claude Code response classifier validation rejects malformed classifier prose from Gemini SSE",
      "Claude Code response classifier validation rejects malformed classifier prose from native Claude JSON",
      "Claude Code response classifier validation rejects malformed classifier prose from OpenAI Chat JSON",
      "Claude Code response classifier validation rejects raw Responses SSE ambiguity from a custom tool item dropped by conversion",
      "Claude Code response classifier validation rejects raw Responses SSE ambiguity from an earlier prose message before a final decision",
      "Claude Code response classifier validation rejects raw Responses SSE ambiguity from an unknown item dropped by conversion",
      "Claude Code response classifier validation rejects raw Responses SSE ambiguity from an unknown message content block dropped by conversion",
      "Claude Code response classifier validation rejects unresolved Responses transport evidence from a custom-tool input fragment without an authoritative custom tool",
      "Claude Code response classifier validation rejects unresolved Responses transport evidence from a function-call argument fragment without an authoritative call",
      "Claude Code response classifier validation rejects unresolved Responses transport evidence from an added custom-tool item without an authoritative terminal item",
      "Claude Code response classifier validation rejects unresolved Responses transport evidence from an added unknown content part without an authoritative terminal block",
      "Claude Code response classifier validation rejects unresolved Responses transport evidence from an output-text fragment with a conflicting item identity",
      "direct Claude classifier Message validation rejects unresolved frozen projection evidence without mutating caller data",
      "direct Claude classifier Message validation validates classifier Messages directly: allow",
      "direct Claude classifier Message validation validates classifier Messages directly: allow outer whitespace",
      "direct Claude classifier Message validation validates classifier Messages directly: bypasses a non-classifier by identity",
      "direct Claude classifier Message validation validates classifier Messages directly: deny",
      "direct Claude classifier Message validation validates classifier Messages directly: deny outer whitespace",
      "direct Claude classifier Message validation validates classifier Messages directly: redacted thinking plus decision",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects array Message",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects case variant",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects decision plus tool",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects decision plus unknown",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects document",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects empty content",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects empty text",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects image",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects malformed redacted thinking",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects malformed thinking",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects missing content",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects missing Message",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects non-array content",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects prefix text",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects prose",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects suffix text",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects thinking only",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects tool result",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects tool use",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects two tags",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects two text blocks",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects unknown block",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects whitespace text",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects wrong role",
      "direct Claude classifier Message validation validates classifier Messages directly: rejects wrong type",
      "direct Claude classifier Message validation validates classifier Messages directly: thinking plus decision",
      "lossless Responses classifier projection accepts terminal-only and matching done-plus-terminal SSE sources without duplicate entries",
      "lossless Responses classifier projection classifies JSON additional tools as actionable",
      "lossless Responses classifier projection classifies JSON custom tool call as actionable",
      "lossless Responses classifier projection classifies JSON custom tool output as actionable",
      "lossless Responses classifier projection classifies JSON function call as actionable",
      "lossless Responses classifier projection classifies JSON function call output as actionable",
      "lossless Responses classifier projection classifies JSON unknown tool shape as actionable",
      "lossless Responses classifier projection decodes split UTF-8 and data-only CRLF terminal SSE frames",
      "lossless Responses classifier projection does not inspect JSON output or consume an SSE stream for a non-classifier",
      "lossless Responses classifier projection ignores content-free Responses metadata",
      "lossless Responses classifier projection keeps every JSON message and content block distinct in output order",
      "lossless Responses classifier projection keeps unknown JSON items and message blocks visible",
      "lossless Responses classifier projection marks malformed JSON shape empty message",
      "lossless Responses classifier projection marks malformed JSON shape empty output",
      "lossless Responses classifier projection marks malformed JSON shape malformed output text",
      "lossless Responses classifier projection marks malformed JSON shape malformed reasoning summary",
      "lossless Responses classifier projection marks malformed JSON shape missing output",
      "lossless Responses classifier projection marks malformed JSON shape non-array output",
      "lossless Responses classifier projection marks malformed JSON shape non-assistant message",
      "lossless Responses classifier projection marks malformed JSON shape null reasoning summary",
      "lossless Responses classifier projection marks malformed terminal SSE hidden item on unknown event",
      "lossless Responses classifier projection marks malformed terminal SSE invalid done index",
      "lossless Responses classifier projection marks malformed terminal SSE malformed JSON",
      "lossless Responses classifier projection marks malformed terminal SSE missing done item",
      "lossless Responses classifier projection preserves duplicate and gap terminal indexes before marking reconciliation malformed",
      "lossless Responses classifier projection projects ordered JSON reasoning and decision blocks without conversion loss",
      "lossless Responses classifier projection projects terminal Responses SSE items before the converter selects a message",
      "lossless Responses classifier projection reconciles matching Responses fragments against authoritative terminal items",
      "Task 3 caller-abort terminality keeps a 502 core Response on the existing account fallback path",
      "Task 3 caller-abort terminality returns a 499 core Response without account mutation",
      "Task 3 classifier request and model invariants keeps a frozen classifier handler request byte-equal after a decision",
      "Task 3 classifier request and model invariants keeps frozen classifier request fields and canonical Message metadata",
      "Task 3 classifier request and model invariants preserves real devin-cli model claude-sonnet-4.6-thinking-1m",
      "Task 3 classifier request and model invariants preserves real windsurf model claude-sonnet-4.6-1m",
      "Task 3 classifier request and model invariants preserves real windsurf model claude-sonnet-4.6-thinking-1m",
      "Task 3 Codex Fast tier before classifier validation keeps client service tier absent at undefined through the real classifier 502 path",
      "Task 3 Codex Fast tier before classifier validation keeps client service tier default at default through the real classifier 502 path",
      "Task 3 Codex Fast tier before classifier validation keeps client service tier priority at priority through the real classifier 502 path",
      "Task 3 combo classifier terminality keeps classifier-adjacent 401 errors distinct from a decision",
      "Task 3 combo classifier terminality keeps classifier-adjacent 429 errors distinct from a decision",
      "Task 3 combo classifier terminality keeps classifier-adjacent 503 errors distinct from a decision",
      "Task 3 combo classifier terminality keeps classifier-adjacent 504 errors distinct from a decision",
      "Task 3 combo classifier terminality returns a first 499 Response without scheduling a fallback",
      "Task 3 combo classifier terminality returns a first canonical decision without scheduling a fallback",
      "Task 3 combo classifier terminality returns an accepted malformed classifier result without generating on a different model"
    ],
    "tests/unit/claude-autoping-model-walk-2592.test.js": [
      "Claude auto-ping candidate list (#2592) falls back to the models this fork actually routes for Claude",
      "Claude auto-ping candidate list (#2592) lists each model once",
      "Claude auto-ping candidate list (#2592) prefers a haiku before the larger models, so the ping stays cheap",
      "Claude auto-ping candidate list (#2592) tries the configured model first",
      "only a model-level refusal walks the catalogue (#2592) does not walk on a 400 that is about the request rather than the model",
      "only a model-level refusal walks the catalogue (#2592) does not walk on an auth or rate-limit failure",
      "only a model-level refusal walks the catalogue (#2592) treats a 400 naming the model as a model refusal",
      "only a model-level refusal walks the catalogue (#2592) treats a 404 as this model being refused"
    ],
    "tests/unit/claude-cache-anchor-translated-2808.test.js": [
      "claude cache anchor on the translated path (#2808) leaves a non-Claude upstream alone",
      "claude cache anchor on the translated path (#2808) re-anchors when the final body is Claude format but the client is not the Claude CLI"
    ],
    "tests/unit/claude-cli-ua-version-floor.test.js": [
      "spoofed claude-cli version floor (eaa10ad4) billing-header cc_version matches the same floor",
      "spoofed claude-cli version floor (eaa10ad4) CLAUDE_CLI_SPOOF_HEADERS (autoping + agentrouter fingerprint) is >= 2.1.251",
      "spoofed claude-cli version floor (eaa10ad4) registry transport User-Agent is claude-cli >= 2.1.251",
      "spoofed claude-cli version floor (eaa10ad4) registry UA and shared spoof headers agree on one version"
    ],
    "tests/unit/claude-client-cache-anchors.test.js": [
      "adversarial-review cache anchor regressions F1: abandons a 4-anchor plan when the first-turn tail fallback would add a 5th breakpoint",
      "adversarial-review cache anchor regressions F2: forces legacy re-anchor when a thinking block carries cache_control",
      "adversarial-review cache anchor regressions F3: forces legacy re-anchor when a defer_loading tool carries cache_control",
      "adversarial-review cache anchor regressions F4: prepareClaudeRequest alone anchors the final message on a first turn with no assistant",
      "client cache_control preservation abandons the preserve when backfilling would exceed the 4-breakpoint ceiling",
      "client cache_control preservation anchorClaudeCache alone preserves a valid plan",
      "client cache_control preservation applies unchanged legacy anchors when the client sends none",
      "client cache_control preservation backfills the tool-tail 1h anchor when the client anchored only messages",
      "client cache_control preservation falls back to legacy anchors when a client cache_control type is wrong",
      "client cache_control preservation falls back to legacy anchors when a client ttl is malformed",
      "client cache_control preservation falls back to legacy anchors when the client sends more than 4 breakpoints",
      "client cache_control preservation keeps a valid 2-breakpoint mixed-ttl plan verbatim",
      "client cache_control preservation keeps a valid 4-breakpoint client plan verbatim through the full pipeline",
      "client cache_control preservation keeps empty-system-block filtering working alongside a preserved plan (#2047)",
      "client cache_control preservation prepareClaudeRequest alone preserves a valid plan (no anchorClaudeCache pass)"
    ],
    "tests/unit/claude-cloaking-billing-header.test.js": [
      "applyCloaking absent-session fallback (F5) derives a stable per-account seed when sessionId is null, for header and user_id",
      "applyCloaking billing header determinism differs across accounts",
      "applyCloaking billing header determinism differs across sessions for the same account",
      "applyCloaking billing header determinism does not touch non-OAuth credentials",
      "applyCloaking billing header determinism is stable when the session id is absent",
      "applyCloaking billing header determinism produces a byte-identical header across calls with the same key+session and different bodies"
    ],
    "tests/unit/claude-cloaking.test.js": [
      "cloakClaudeTools does not suffix a forced choice that targets a non-client (decoy/built-in) tool",
      "cloakClaudeTools leaves non-forced tool_choice untouched",
      "cloakClaudeTools renames tool_use names in message history",
      "cloakClaudeTools returns the body unchanged when there are no tools",
      "cloakClaudeTools suffixes a forced tool_choice to match the renamed tool",
      "cloakClaudeTools suffixes client tool names and maps them back",
      "cloakClaudeTools suffixes only the chosen tool when several are present"
    ],
    "tests/unit/claude-code-model-discovery-2947.test.js": [
      "Claude Code model discovery (#2947) is detected once and reused for the Codex catalog branch",
      "Claude Code model discovery (#2947) leaves an OpenAI client's list untouched",
      "Claude Code model discovery (#2947) still honours the kill switch",
      "Claude Code model discovery (#2947) the client is recognised from its user agent alone",
      "Claude Code model discovery (#2947) the detector runs before the gate that now depends on it",
      "Claude Code model discovery (#2947) the rewrite gate accepts the detector, not only the version header",
      "the rewrite is protocol-keyed first, identity only as the one-call fallback (#2947) any client sending anthropic-version gets it, not only claude-cli",
      "the rewrite is protocol-keyed first, identity only as the one-call fallback (#2947) once the header is present, clientTool cannot change the outcome",
      "the rewrite is protocol-keyed first, identity only as the one-call fallback (#2947) the identity fallback only ever fires when the header is absent"
    ],
    "tests/unit/claude-compat-layer.test.js": [
      "looksLikeClaudeWrappedModel detects [1m]/[1M] suffix",
      "looksLikeClaudeWrappedModel detects claude- prefix (any case)",
      "looksLikeClaudeWrappedModel ignores plain names",
      "looksLikeClaudeWrappedModel rejects non-strings",
      "normalizeClaudeModelName handles bare claude-prefixed pair form",
      "normalizeClaudeModelName handles degenerate inputs",
      "normalizeClaudeModelName keeps official claude models untouched",
      "normalizeClaudeModelName keeps unknown claude-* names (likely official Anthropic models)",
      "normalizeClaudeModelName maps known bare names (combos, alias keys)",
      "normalizeClaudeModelName returns non-claude names unchanged after suffix strip",
      "normalizeClaudeModelName strips lone [1m] suffix without prefix",
      "normalizeClaudeModelName strips prefix+suffix from derived route form",
      "readClaudeCompat defaults to enabled + auto mode",
      "readClaudeCompat honors explicit config and sanitizes garbage",
      "rewriteModelsListForClaude appends bare official Anthropic ids so Claude Code window discovery matches (#compaction)",
      "rewriteModelsListForClaude does not double-suffix ids that already carry [1m]",
      "rewriteModelsListForClaude keywords mode matches model part case-insensitively",
      "rewriteModelsListForClaude off mode never suffixes; combos get prefixed too",
      "rewriteModelsListForClaude passes through malformed entries",
      "rewriteModelsListForClaude prefixes ids, sets display_name, appends [1m] per policy"
    ],
    "tests/unit/claude-compat-routing-index.test.js": [
      "buildClaudeRoutingIndex adds alias-map keys as bare names and slash-values as pairs",
      "buildClaudeRoutingIndex adds combo names as bare, skipping nameless combos",
      "buildClaudeRoutingIndex adds custom models under their alias and the mapped alias, skipping malformed rows",
      "buildClaudeRoutingIndex indexes the whole static catalog under alias and provider-id spellings",
      "buildClaudeRoutingIndex survives every db read failing — static catalog alone remains",
      "stripContextSuffix passes non-strings through",
      "stripContextSuffix removes a trailing [1m] case-insensitively",
      "stripContextSuffix returns the input unchanged when stripping would leave an empty id"
    ],
    "tests/unit/claude-custom-tools-isolation.test.js": [
      "Claude custom tools through compatible-provider preparation compresses fresh tool output on repeated attempts without rewriting the caller",
      "Claude custom tools through compatible-provider preparation isolates caller containers before translation with RTK enabled=false",
      "Claude custom tools through compatible-provider preparation isolates caller containers before translation with RTK enabled=true",
      "Claude custom tools through compatible-provider preparation isolates nested containers around opaque direct-engine handles",
      "Claude custom tools through compatible-provider preparation preserves custom tool schema and choice for glm",
      "Claude custom tools through compatible-provider preparation preserves custom tool schema and choice for kimi",
      "Claude custom tools through compatible-provider preparation preserves custom tool schema and choice for minimax",
      "Claude custom tools through compatible-provider preparation retains the typed schema and its complete call/result transaction at dispatch"
    ],
    "tests/unit/claude-default-models.test.js": [
      "claudeEnv pure helpers maps the four roles + subagent to env keys",
      "claudeEnv pure helpers mergeClaudeEnv creates env when settings.json has none",
      "claudeEnv pure helpers mergeClaudeEnv strictly key-overwrites env and never deletes",
      "claudeEnv pure helpers omits keys for empty roles and missing display names",
      "claudeEnv pure helpers sanitizeDefaultModels repairs garbage into the clean shape",
      "claudeEnv pure helpers sanitizeEnvOverrides keeps strings, stringifies scalars, drops the rest"
    ],
    "tests/unit/claude-empty-system-block-2047.test.js": [
      "empty system block on the Claude request path (#2047) does not anchor the prompt cache on the empty block",
      "empty system block on the Claude request path (#2047) does not send a system block with empty text",
      "empty system block on the Claude request path (#2047) drops system entirely when every block was empty",
      "empty system block on the Claude request path (#2047) keeps a system prompt that actually has text",
      "empty system block on the Claude request path (#2047) leaves the rest of the reported request intact"
    ],
    "tests/unit/claude-fable-registry-guard.test.js": [
      "claude registry declares the Fable lane (ec62de2b) keeps claude-fable-5-1 in the static model catalog",
      "claude registry declares the Fable lane (ec62de2b) keeps every routed Claude lane declared, so no sibling row silently drops",
      "claude registry declares the Fable lane (ec62de2b) resolves bare claude-fable-5-1 to the claude provider",
      "claude registry declares the Fable lane (ec62de2b) routes bare claude-fable-5-1 through getModelInfoCore without an alias map",
      "unknown bare model rejection keeps the 404 contract the error class itself carries the contract fields for any model name",
      "unknown bare model rejection keeps the 404 contract throws ModelNotFoundError with status 404 / invalid_request_error / model_not_found"
    ],
    "tests/unit/claude-header-forwarding.test.js": [
      "DefaultExecutor.buildHeaders() — anthropic provider includes context-management and sonnet beta flags for claude-sonnet-4-20250514",
      "DefaultExecutor.buildHeaders() — anthropic provider merges client-supplied raw beta headers",
      "DefaultExecutor.buildHeaders() — anthropic-compatible stripping does NOT strip headers when baseUrl is api.anthropic.com",
      "DefaultExecutor.buildHeaders() — anthropic-compatible stripping does NOT strip headers when baseUrl is empty (defaults to Anthropic)",
      "DefaultExecutor.buildHeaders() — anthropic-compatible stripping keeps other beta flags intact after stripping",
      "DefaultExecutor.buildHeaders() — anthropic-compatible stripping removes claude-code-20250219 from anthropic-beta for non-Anthropic host",
      "DefaultExecutor.buildHeaders() — anthropic-compatible stripping strips x-app and anthropic-dangerous-direct-browser-access for non-Anthropic host",
      "DefaultExecutor.buildHeaders() — claude provider does not throw when no model is given",
      "DefaultExecutor.buildHeaders() — claude provider includes Accept: text/event-stream when stream=true",
      "DefaultExecutor.buildHeaders() — claude provider includes heavy-agent beta flags for claude-opus-5",
      "DefaultExecutor.buildHeaders() — claude provider includes heavy-agent beta flags for claude-sonnet-5",
      "DefaultExecutor.buildHeaders() — claude provider omits Accept: text/event-stream when stream=false",
      "DefaultExecutor.buildHeaders() — claude provider omits heavy-agent beta flags for claude-fable-5",
      "DefaultExecutor.buildHeaders() — claude provider omits heavy-agent beta flags for claude-haiku-4-5-20251001",
      "DefaultExecutor.buildHeaders() — claude provider sets Bearer Authorization when only accessToken is provided",
      "DefaultExecutor.buildHeaders() — claude provider sets x-api-key auth when apiKey is provided",
      "DefaultExecutor.buildHeaders() — claude provider uses static provider defaults when no model is given",
      "proxyAwareFetch — api.anthropic.com routing does NOT route non-Anthropic hosts through gotScraping",
      "proxyAwareFetch — api.anthropic.com routing falls back gracefully when got-scraping throws on non-streaming path",
      "proxyAwareFetch — api.anthropic.com routing uses native fetch for api.anthropic.com — got-scraping routing is disabled in proxyFetch"
    ],
    "tests/unit/claude-native-no-fabricated-signature-2227.test.js": [
      "nothing is invented for the upstream that can verify it (#2227) a dropped foreign signature is not replaced by the default one",
      "nothing is invented for the upstream that can verify it (#2227) a genuine Claude signature is still forwarded untouched",
      "nothing is invented for the upstream that can verify it (#2227) a tool-use turn that never had thinking gets none invented either",
      "nothing is invented for the upstream that can verify it (#2227) and DeepSeek still gets its unsigned one",
      "nothing is invented for the upstream that can verify it (#2227) the drop that this repair broke still works: foreign signatures do not survive",
      "nothing is invented for the upstream that can verify it (#2227) the lenient anthropic-compatible family still gets its placeholder"
    ],
    "tests/unit/claude-nonstream-usage-cache-split.test.js": [
      "non-streaming usage cache split clamps input_tokens at zero when cached exceeds prompt_tokens",
      "non-streaming usage cache split emits no cache fields when the upstream reported no cache activity",
      "non-streaming usage cache split keeps an exclusive input_tokens source as-is",
      "non-streaming usage cache split subtracts cache creation tokens too",
      "non-streaming usage cache split subtracts cached tokens from inclusive prompt_tokens (1000/900 -> 100/900)"
    ],
    "tests/unit/claude-nonstreaming-usage.test.js": [
      "Claude non-streaming usage translation does not add empty prompt details when Claude reports no cache usage",
      "Claude non-streaming usage translation includes cache reads and cache writes in OpenAI prompt totals"
    ],
    "tests/unit/claude-passthrough-decloak.test.js": [
      "Claude passthrough tool-name decloaking (#2392) decloaks a mapped final unterminated Claude tool_use frame",
      "Claude passthrough tool-name decloaking (#2392) does not apply a Claude name map to a final non-Claude passthrough",
      "Claude passthrough tool-name decloaking (#2392) does not apply a Claude name map to a non-Claude passthrough",
      "Claude passthrough tool-name decloaking (#2392) keeps a native suffix-like name in a final Claude tool_use frame",
      "Claude passthrough tool-name decloaking (#2392) leaves Claude tool names outside the cloaking map untouched",
      "Claude passthrough tool-name decloaking (#2392) restores a cloaked tool name in native Claude tool_use start events"
    ],
    "tests/unit/claude-passthrough-no-done-sentinel-1733.test.js": [
      "the OpenAI sentinel stays off Anthropic streams (#1733) an OpenAI client still gets its sentinel",
      "the OpenAI sentinel stays off Anthropic streams (#1733) passthrough skips it when the client speaks Claude",
      "the OpenAI sentinel stays off Anthropic streams (#1733) the decision is made on the client format, not the provider",
      "the OpenAI sentinel stays off Anthropic streams (#1733) the Gemini-family exclusion it sits beside is unchanged"
    ],
    "tests/unit/claude-passthrough-tool-reconciliation-2663.test.js": [
      "native Claude passthrough tool-result reconciliation (#2663) does not mutate caller state reused by an account fallback",
      "native Claude passthrough tool-result reconciliation (#2663) does not re-pair a result across an intervening assistant turn",
      "native Claude passthrough tool-result reconciliation (#2663) does not treat a folded system message as the tool-result turn",
      "native Claude passthrough tool-result reconciliation (#2663) keeps matching media and never stringifies orphaned media",
      "native Claude passthrough tool-result reconciliation (#2663) keeps only the first duplicate result structured and salvages the later text",
      "native Claude passthrough tool-result reconciliation (#2663) normalizes only known nested models at the native executor boundary without mutating fallback input",
      "native Claude passthrough tool-result reconciliation (#2663) reconciles valid, missing, and orphaned results before the native Claude executor"
    ],
    "tests/unit/claude-stream-terminal-1733.test.js": [
      "kiro → claude stream terminal (#1733) does not replay the buffered tool arguments twice",
      "kiro → claude stream terminal (#1733) emits nothing after message_stop when finish_reason repeats",
      "kiro → claude stream terminal (#1733) still emits a complete single-pass stream",
      "openai → claude stream terminal (#1733) emits nothing after message_stop when a provider sends a trailing content frame",
      "openai → claude stream terminal (#1733) emits nothing after message_stop when the provider repeats finish_reason",
      "openai → claude stream terminal (#1733) still emits the normal stream when nothing trails the terminal"
    ],
    "tests/unit/claude-thinking-usage-2770.test.js": [
      "#2770 Claude thinking-token usage does not let the stream usage accumulator flatten Claude thinking on the client terminal",
      "#2770 Claude thinking-token usage keeps Claude thinking as a nested reasoning subset in a Chat non-stream response",
      "#2770 Claude thinking-token usage keeps Claude thinking as a nested reasoning subset in a Responses non-stream response",
      "#2770 Claude thinking-token usage keeps Claude thinking as a nested reasoning subset on the OpenAI stream terminal",
      "#2770 Claude thinking-token usage limits Claude thinking to the nonnegative output-token subset",
      "#2770 Claude thinking-token usage persists the numeric thinking subset internally without public Claude fields"
    ],
    "tests/unit/claude-to-openai-request-coverage.test.js": [
      "claudeToOpenAIRequest system handling emits no system message when every block is empty, and accepts a plain string",
      "claudeToOpenAIRequest system handling joins blocks to a string without markers, and strips the billing header line",
      "claudeToOpenAIRequest system handling keeps block shape when a cache_control marker is present, dropping empty blocks",
      "claudeToOpenAIRequest system handling skips a system prompt whose blocks all collapse when a marker sits on an empty block",
      "claudeToOpenAIRequest top-level fields carries temperature, reasoning_effort (both spellings), reasoning and passthrough fields",
      "claudeToOpenAIRequest top-level fields maps tool_choice any/auto/string/unknown",
      "claudeToOpenAIRequest top-level fields normalizes tool schemas: missing, malformed, and object-without-properties",
      "fixMissingToolResponsesOpenAI behaviour inserts [No response received] for tool calls without a following tool reply",
      "max_tokens is present only when the body carries it",
      "message conversion an image-only tool_result gets a placeholder, and non-string non-array content is stringified",
      "message conversion converts thinking blocks to reasoning_content on tool-call, text and thinking-only turns",
      "message conversion empty content array yields an empty message, and unknown message shapes are dropped",
      "message conversion lifts images out of tool_result content and keeps the text in the tool turn",
      "message conversion preserves both inline and remote user images",
      "message conversion tool-call turn without thinking still carries empty reasoning_content (#1480)",
      "message conversion wraps a mid-conversation system message as instructions, dropping an empty one"
    ],
    "tests/unit/claude-tool-model-prefix-2642.test.js": [
      "a TokenProxy alias is stripped from a tool's model (#2642) a bare model with no prefix is left alone",
      "a TokenProxy alias is stripped from a tool's model (#2642) a non-string model is not coerced",
      "a TokenProxy alias is stripped from a tool's model (#2642) a prefix this router does not own is left alone",
      "a TokenProxy alias is stripped from a tool's model (#2642) a tool without a model is unaffected",
      "a TokenProxy alias is stripped from a tool's model (#2642) several tools are each handled",
      "a TokenProxy alias is stripped from a tool's model (#2642) the reported value is de-prefixed",
      "a TokenProxy alias is stripped from a tool's model (#2642) the rest of the tool is untouched"
    ],
    "tests/unit/claude-tool-schema-normalize-2875.test.js": [
      "a malformed Claude tool schema is normalized (#2875) does not mutate the caller's schema",
      "a malformed Claude tool schema is normalized (#2875) fills in a missing type",
      "a malformed Claude tool schema is normalized (#2875) fills in missing properties on an object schema",
      "a malformed Claude tool schema is normalized (#2875) keeps sibling keywords rather than rebuilding the schema",
      "a malformed Claude tool schema is normalized (#2875) leaves a non-object schema type as declared",
      "a malformed Claude tool schema is normalized (#2875) leaves a well-formed schema alone",
      "a malformed Claude tool schema is normalized (#2875) replaces a non-object schema entirely"
    ],
    "tests/unit/claude-tool-terminal-1490.test.js": [
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) does not emit a second terminal when the stream already finished",
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) emits message_delta and message_stop after closing the tool block",
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) leaves a flush with no tool call alone",
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) opens, closes and terminates a call whose name never arrived",
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) reports the usage the provider did send",
      "a tool-call stream that ends without finish_reason still ends the Claude message (#1490) still delivers the buffered arguments intact (the #3416 contract)"
    ],
    "tests/unit/claude-tool-type-default.test.js": [
      "defaultClaudeToolType defaults a missing type to custom",
      "defaultClaudeToolType defaults falsy types (null, undefined, empty string) to custom",
      "defaultClaudeToolType leaves a tool that already declares a type untouched",
      "defaultClaudeToolType returns a non-array unchanged",
      "handleChatCore: Claude-format dispatch does not rewrite a tool that already declares a type",
      "handleChatCore: Claude-format dispatch stamps type:custom on a type-less tool before dispatch"
    ],
    "tests/unit/claude-usage-cache-2848.test.js": [
      "Claude usage cache lifetime (#2848) bounds the cache so a rotating token cannot grow it without limit",
      "Claude usage cache lifetime (#2848) does not serve one transient failure forever",
      "Claude usage cache lifetime (#2848) drops the dead token's entries so the next read is a real fetch",
      "Claude usage cache lifetime (#2848) reports the expiry on 401 instead of serving the last good quota"
    ],
    "tests/unit/claude-usage-cache-fold-3075.test.js": [
      "Claude cache tokens are folded exactly once (#3075) does not let the usage DB fold the cache a second time",
      "Claude cache tokens are folded exactly once (#3075) folds a cache-creation-only first write exactly once too",
      "Claude cache tokens are folded exactly once (#3075) marks the message_start snapshot as folded too",
      "Claude cache tokens are folded exactly once (#3075) reports a cache-inclusive prompt on the message_stop exit as well",
      "Claude cache tokens are folded exactly once (#3075) still agrees with the message_delta exit"
    ],
    "tests/unit/claude-window-discovery-bare-ids.test.js": [
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) a catalog carrying claude-fable-5-1 yields a bare claude-fable-5-1 row (fleet outage pin)",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) an entry with no context_length never outranks one with a real window",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) bare id survives prefixed and thinking/effort variants crowding the catalog",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) does not duplicate the bare row when the input already lists a bare id",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) every prefixed row still ships alongside the bare rows",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) non-official ids never gain a bare row",
      "bare official Anthropic ids in the /v1/models rewrite (#compaction) widest window wins across duplicate entries, regardless of catalog order"
    ],
    "tests/unit/cli-autoping-toggle-2349.test.js": [
      "the CLI can toggle quota auto-ping (#2349) it follows the existing toggle shape in this menu",
      "the CLI can toggle quota auto-ping (#2349) no new endpoint was added: it reuses updateSettings",
      "the CLI can toggle quota auto-ping (#2349) offers a toggle for each provider the scheduler knows",
      "the CLI can toggle quota auto-ping (#2349) writes the keys the settings API already owns",
      "the CLI toggle writes the shape the scheduler reads keeps the per-connection map, which is the only gate the tick has",
      "the CLI toggle writes the shape the scheduler reads preserves sibling keys it does not own",
      "the CLI toggle writes the shape the scheduler reads reads ON from the connection map, not from the unread `enabled` flag",
      "the CLI toggle writes the shape the scheduler reads refuses to write when no account is enrolled",
      "the CLI toggle writes the shape the scheduler reads turns every enrolled account off, and back on again"
    ],
    "tests/unit/cli-build-artifacts.test.js": [
      "CLI build server artifacts merges complete API routes and provider chunks for the legacy nested app layout",
      "CLI build server artifacts merges complete API routes and provider chunks for the Next 16 workspace layout",
      "CLI build server artifacts merges idempotently without removing standalone-generated files",
      "CLI build server artifacts reports the missing required API route artifact path"
    ],
    "tests/unit/cli-build-cleanup.test.js": [
      "CLI build cleanup does not package the isolated build home from standalone output",
      "CLI build cleanup removes stale staged output before the compiler runs",
      "CLI build cleanup stops recursive copies when a standalone dependency symlink points to an ancestor"
    ],
    "tests/unit/cli-headless-and-stop-1191-967.test.js": [
      "the launcher does not wait on a prompt without a TTY (#1191) the menu loop is guarded by a TTY check",
      "the launcher does not wait on a prompt without a TTY (#1191) the pre-existing update-relaunch fallback is untouched",
      "the launcher does not wait on a prompt without a TTY (#1191) the tray still starts, because a detached desktop launch has no TTY either",
      "tokenproxy stop (#967) dispatches on the subcommand and exits",
      "tokenproxy stop (#967) is listed in --help",
      "tokenproxy stop (#967) runs after argument parsing so --port is honoured",
      "tokenproxy stop (#967) runs before the server is started, so stop never spawns one"
    ],
    "tests/unit/cli-heap-flags-3365.test.js": [
      "resolveHeapFlags (#3365) emits no flag at all for 0, leaving the sizing to node",
      "resolveHeapFlags (#3365) falls back to the default on a junk value, and says so",
      "resolveHeapFlags (#3365) honours an explicit cap",
      "resolveHeapFlags (#3365) ignores unrelated NODE_OPTIONS",
      "resolveHeapFlags (#3365) keeps the 6144 default when nothing is configured",
      "resolveHeapFlags (#3365) prefers the dedicated var over NODE_OPTIONS",
      "resolveHeapFlags (#3365) stands aside when NODE_OPTIONS already caps the heap",
      "resolveHeapFlags (#3365) treats an empty or whitespace value as unset, without warning"
    ],
    "tests/unit/cli-hide-shutdown-flag-1197.test.js": [
      "the hide handoff does not resurrect the server it just killed (#1197) every deliberate shutdown path sets it",
      "the hide handoff does not resurrect the server it just killed (#1197) sets the shutdown flag before cleanup on the hide path",
      "the hide handoff does not resurrect the server it just killed (#1197) the exit path still does the same thing, in the same order",
      "the hide handoff does not resurrect the server it just killed (#1197) the restart really is gated on that flag, which is why it matters"
    ],
    "tests/unit/cli-node-engine-floor-2795.test.js": [
      "the CLI declares the Node floor it actually needs (#2795) agrees with the native-module support set, which already starts at 20",
      "the CLI declares the Node floor it actually needs (#2795) File really is the global the floor is about",
      "the CLI declares the Node floor it actually needs (#2795) requires at least Node 20"
    ],
    "tests/unit/cli-nonjson-error-body-1696.test.js": [
      "a non-JSON error body reports the status, not the parser (#1696) a 2xx with an unparseable body still reports a parse failure",
      "a non-JSON error body reports the status, not the parser (#1696) an error status is reported as HTTP <code> with the body",
      "a non-JSON error body reports the status, not the parser (#1696) parsing no longer wraps the status check",
      "a non-JSON error body reports the status, not the parser (#1696) the body is collapsed and bounded before being shown",
      "a non-JSON error body reports the status, not the parser (#1696) the JSON success and JSON error paths are unchanged",
      "the branch chosen for each body shape a JSON error body keeps its own message",
      "the branch chosen for each body shape a normal success is untouched",
      "the branch chosen for each body shape an empty body is not an error",
      "the branch chosen for each body shape an HTML error page is truncated rather than dumped",
      "the branch chosen for each body shape the reported case now names the status"
    ],
    "tests/unit/cli-process-ownership-3026.test.js": [
      "CLI process ownership (#3026) bounds a byte-drip health response instead of waiting indefinitely",
      "CLI process ownership (#3026) does not kill a next-server-named local HTTP process that fails TokenProxy liveness signature",
      "CLI process ownership (#3026) stop does not kill a next-server process outside its selected port",
      "CLI process ownership (#3026) takeover rejects an unknown port owner without waiting to replace it"
    ],
    "tests/unit/cli-server-stdout-log-1753.test.js": [
      "background server stdout reaches the crash log (#1753) stdout and stderr keep the same tail, and relay only with --log",
      "background server stdout reaches the crash log (#1753) stdout is always piped, never discarded",
      "background server stdout reaches the crash log (#1753) the crash tail is still what gets printed on exit",
      "background server stdout reaches the crash log (#1753) the pipe is drained whether or not --log is on (#2447 still holds)",
      "background server stdout reaches the crash log (#1753) the tail stays bounded by the existing limit"
    ],
    "tests/unit/cli-stdio-relay-3562.test.js": [
      "the server child never inherits a console handle (#3562) a piped, relayed child actually delivers its output",
      "the server child never inherits a console handle (#3562) does not use inherit",
      "the server child never inherits a console handle (#3562) drains stderr unconditionally, since it is always piped",
      "the server child never inherits a console handle (#3562) drains stdout whenever it is piped",
      "the server child never inherits a console handle (#3562) keeps the crash tail even while --log is on"
    ],
    "tests/unit/cli-xai-video.test.js": [
      "downloadToFile downloads via .part and renames atomically",
      "downloadToFile follows redirects",
      "downloadToFile removes the .part file when the download fails",
      "imageInputToUrl converts a local file to a base64 data URL",
      "imageInputToUrl passes URLs and data URLs through",
      "parseArgs applies defaults",
      "parseArgs parses all documented flags",
      "parseArgs rejects unknown flags",
      "run (against a mock gateway) creates, polls to done, downloads the MP4, and exits 0",
      "run (against a mock gateway) exits non-zero when polling exceeds the timeout",
      "run (against a mock gateway) exits non-zero when the job fails, without leaving files",
      "run (against a mock gateway) reports a helpful error when no xAI account is connected",
      "sanitizeText redacts bearer tokens from error output"
    ],
    "tests/unit/client-disconnect-guard-3564.test.js": [
      "a client disconnect is not reported as a server error (#3564) catches the handler rejection instead of leaving it unhandled",
      "a client disconnect is not reported as a server error (#3564) handles clientError without leaving the socket open",
      "a client disconnect is not reported as a server error (#3564) leaves the spoofable-header defence exactly as it was",
      "a client disconnect is not reported as a server error (#3564) parses as valid JavaScript",
      "a client disconnect is not reported as a server error (#3564) recognises every shape Node reports an abandoned request as"
    ],
    "tests/unit/client-header-passthrough-2413.test.js": [
      "a client's custom headers reach the upstream (#2413) forwards the two attribution headers providers actually read",
      "a client's custom headers reach the upstream (#2413) forwards x- prefixed headers",
      "a client's custom headers reach the upstream (#2413) preserves the client's own casing",
      "the default executor applies it last after every header it builds itself",
      "what it refuses to forward never a header this router already set, whatever the casing",
      "what it refuses to forward never framing or hop-by-hop headers",
      "what it refuses to forward never the caller's cookies, which are its session with us",
      "what it refuses to forward never the caller's credential",
      "what it refuses to forward never the headers with deliberate handling elsewhere",
      "what it refuses to forward never this router's internal stamps",
      "what it refuses to forward no headers at all is a no-op",
      "what it refuses to forward nothing outside the x- prefix and those two names",
      "what it refuses to forward skips a non-string or empty value rather than sending it"
    ],
    "tests/unit/client-metadata-strip-1157.test.js": [
      "client_metadata never reaches an OpenAI-shaped upstream (#1157 #1442) covers the providers that reported the 400, which never declared the quirk",
      "client_metadata never reaches an OpenAI-shaped upstream (#1157 #1442) is keyed on the upstream format, not on a per-provider quirk",
      "client_metadata never reaches an OpenAI-shaped upstream (#1157 #1442) leaves the field alone for an Anthropic upstream, which understands it",
      "client_metadata never reaches an OpenAI-shaped upstream (#1157 #1442) the openai-to-openai passthrough is the gap the executor now closes",
      "client_metadata never reaches an OpenAI-shaped upstream (#1157 #1442) the translator half still drops it on every openai-shaped target"
    ],
    "tests/unit/client-model-error-status.test.js": [
      "client model error status projection classifies a 403 carrying an unrelated error type",
      "client model error status projection classifies a 403 with no model in it, which must stay account-wide",
      "client model error status projection classifies an Anthropic entitlement refusal naming the model",
      "client model error status projection classifies an Anthropic entitlement refusal phrased without the model id",
      "client model error status projection classifies an OpenAI entitlement refusal naming the model",
      "client model error status projection keeps failure metadata internal to the result and client error body",
      "client model error status projection projects a non-model authentication failure only from a verified structured model signature",
      "client model error status projection projects a request parameter failure only from a verified structured model signature",
      "client model error status projection projects a verified Anthropic not_found_error naming the model only from a verified structured model signature",
      "client model error status projection projects a verified Gemini unknown-model payload only from a verified structured model signature",
      "client model error status projection projects a verified OpenAI invalid_request_error with param=model on 400 only from a verified structured model signature",
      "client model error status projection projects a verified OpenAI model_not_found code only from a verified structured model signature",
      "client model error status projection projects an absent payload only from a verified structured model signature",
      "client model error status projection projects an Anthropic not_found_error naming a DIFFERENT model only from a verified structured model signature",
      "client model error status projection projects an OpenAI 400 that does not name the requested model only from a verified structured model signature",
      "client model error status projection projects generic ModelError prose only from a verified structured model signature",
      "client model error status projection projects the anthropic api-key twin via the claude predicate only from a verified structured model signature",
      "client model error status projection projects the openai api-key twin via the codex predicate only from a verified structured model signature"
    ],
    "tests/unit/client-retry-permission.test.js": [
      "client retry permission handles immutable response headers without mutating the source",
      "client retry permission lets a client wait on a busy account without permitting account rotation",
      "client retry permission overrides an upstream retry hint when the accepted outcome is uncertain",
      "client retry permission retains an explicit upstream refusal even when the gateway knows rejection was safe"
    ],
    "tests/unit/client-setup-connectivity.test.js": [
      "checks configuration as a pure function with no key and no network",
      "fails the configuration tier on an expired key, a paused key and a disallowed model",
      "generates client endpoints without disclosing the key",
      "reaches only this gateway on the authentication tier and says so",
      "refuses an unauthorized caller at both entry points",
      "refuses an unnamed or unrecognized tier rather than guessing one",
      "refuses the inference tier instead of downgrading it",
      "reports a superseded key as a warning, not an error",
      "retains the check as an audit event carrying no key material",
      "runs the configuration tier without touching the network",
      "separates a timeout from a refused connection",
      "skips the network when the configuration already fails"
    ],
    "tests/unit/cloudflare-ai-max-tokens-1645.test.js": [
      "cloudflare-ai default max_tokens (#1645) does not inject max_tokens for other providers",
      "cloudflare-ai default max_tokens (#1645) fills a default max_tokens when the client omits it",
      "cloudflare-ai default max_tokens (#1645) leaves an explicit client max_tokens untouched"
    ],
    "tests/unit/cloudflare-embeddings.test.js": [
      "cloudflare-ai embeddings support exposes an embedding adapter with the account-scoped URL",
      "cloudflare-ai embeddings support refuses to build a URL without accountId (no silent fallback)",
      "cloudflare-ai embeddings support registry lists the three BGE models under embedding service kind",
      "cloudflare-ai embeddings support sends the OpenAI-shaped body with bearer auth"
    ],
    "tests/unit/cloudflare-flatten-media-2749.test.js": [
      "the Cloudflare flatten is keyed on the model, not the message (#2749) a text-only model cannot use the image either way, so dropping it is graceful",
      "the Cloudflare flatten is keyed on the model, not the message (#2749) does not touch another provider's messages",
      "the Cloudflare flatten is keyed on the model, not the message (#2749) keeps the array for a vision model, so the image survives",
      "the Cloudflare flatten is keyed on the model, not the message (#2749) leaves a plain string message alone on both",
      "the Cloudflare flatten is keyed on the model, not the message (#2749) still flattens for a text-only model, which is why the rule exists",
      "the Cloudflare flatten is keyed on the model, not the message (#2749) the two models really do differ in vision, or this test is vacuous"
    ],
    "tests/unit/cloudflare-glm52-2215.test.js": [
      "Cloudflare Workers AI offers GLM 5.2 (#2215) goes upstream under its own id",
      "Cloudflare Workers AI offers GLM 5.2 (#2215) inherits the gateway's thinking ladder, which excludes xhigh",
      "Cloudflare Workers AI offers GLM 5.2 (#2215) is in the catalog and accepted as a model id",
      "Cloudflare Workers AI offers GLM 5.2 (#2215) needs no pricing row — its sibling has none either",
      "Cloudflare Workers AI offers GLM 5.2 (#2215) resolves the same capabilities as its already-working sibling"
    ],
    "tests/unit/cloudflare-thinking-levels-2665.test.js": [
      "cloudflare-ai thinking levels stop at high (#2665) a request asking for xhigh goes out at the highest level the gateway takes",
      "cloudflare-ai thinking levels stop at high (#2665) it applies to every cloudflare-ai model, not just the kimi ones",
      "cloudflare-ai thinking levels stop at high (#2665) the levels the gateway does take are passed through untouched",
      "cloudflare-ai thinking levels stop at high (#2665) the OpenAI provider itself still offers xhigh",
      "cloudflare-ai thinking levels stop at high (#2665) the same request on the OpenAI provider keeps xhigh",
      "cloudflare-ai thinking levels stop at high (#2665) xhigh is not offered"
    ],
    "tests/unit/codebuddy-cn-usage-handler.test.js": [
      "getCodeBuddyCnUsage classifies one-shot packs as bonus, sorted soonest-expiring first, recurring:false",
      "getCodeBuddyCnUsage disambiguates duplicate cadence labels with a counter",
      "getCodeBuddyCnUsage falls back to apiKey when accessToken is missing",
      "getCodeBuddyCnUsage falls back to SubProductName then a default plan name",
      "getCodeBuddyCnUsage labels a ~30d refill pack Monthly with cycle balances and recurring:true",
      "getCodeBuddyCnUsage labels a ~7d cycle Weekly and a ~1d cycle Daily",
      "getCodeBuddyCnUsage maps 401 to an invalid-credential message",
      "getCodeBuddyCnUsage maps 403 to an invalid-credential message",
      "getCodeBuddyCnUsage maps other non-ok statuses to an API error message with the status",
      "getCodeBuddyCnUsage names the plan from the first refill pack when both kinds exist",
      "getCodeBuddyCnUsage POSTs the registry usage URL with a bearer token",
      "getCodeBuddyCnUsage reports no credit package when accounts are empty",
      "getCodeBuddyCnUsage returns a message when no credential is available",
      "getCodeBuddyCnUsage surfaces a non-zero code without msg as unknown",
      "getCodeBuddyCnUsage surfaces a non-zero upstream code with its msg",
      "getCodeBuddyCnUsage treats a pack with unparsable cycle end as bonus with null resetAt",
      "getCodeBuddyCnUsage wraps a thrown parse error into a message",
      "getCodeBuddyIntlUsage scopes messages to the intl provider id"
    ],
    "tests/unit/codebuddy-intl-system-prompt-3344.test.js": [
      "CodeBuddyIntlExecutor system prompt handling (#3344) keeps caller system and developer instructions after the required CodeBuddy prompt",
      "CodeBuddyIntlExecutor system prompt handling (#3344) keeps the required CodeBuddy system prompt when the caller has no instructions",
      "CodeBuddyIntlExecutor system prompt handling (#3344) preserves text from typed system and developer content blocks"
    ],
    "tests/unit/codebuddy-reasoning-optin.test.js": [
      "CodeBuddyExecutor reasoning params are opt-in (#2071) does NOT force reasoning when the client did not request it",
      "CodeBuddyExecutor reasoning params are opt-in (#2071) mirrors reasoning_summary:auto when the client explicitly requested reasoning",
      "CodeBuddyExecutor reasoning params are opt-in (#2071) omits reasoning_effort for none/off and adds no reasoning_summary"
    ],
    "tests/unit/codebuddy-system-prompt-len-3339.test.js": [
      "CodeBuddyExecutor system prompt length gate (#3339) does not touch user or assistant messages",
      "CodeBuddyExecutor system prompt length gate (#3339) falls back to the default on a junk or negative value",
      "CodeBuddyExecutor system prompt length gate (#3339) honours a raised limit so a long project prompt survives",
      "CodeBuddyExecutor system prompt length gate (#3339) keeps the 2000-char default when the env var is unset",
      "CodeBuddyExecutor system prompt length gate (#3339) leaves a short project prompt alone and stays quiet",
      "CodeBuddyExecutor system prompt length gate (#3339) names the rule that fired so a false positive is diagnosable",
      "CodeBuddyExecutor system prompt length gate (#3339) preserves typed-block content shape on replacement",
      "CodeBuddyExecutor system prompt length gate (#3339) still replaces an agent identity prompt",
      "CodeBuddyExecutor system prompt length gate (#3339) treats 0 as 'identity markers only'"
    ],
    "tests/unit/codex-account-export-1590.test.js": [
      "credential egress gate allows the CLI token, and a signed-in session on the loopback socket",
      "credential egress gate refuses a loopback caller whose session cookie does not verify",
      "credential egress gate refuses a loopback caller with no signed-in session, even though /api/oauth allows requireLogin=false",
      "credential egress gate refuses a remote caller",
      "exported account carries the credential and the account identity, but not the install's proxy binding",
      "exported account is never cached and never leaks through a referrer",
      "exported account rejects a connection that is not a Codex account, and one holding no credential",
      "round trip through the existing importer imports one account from a bare object, so a per-account import needs no new route",
      "round trip through the existing importer re-imports an exported account as one account, with its auth type intact",
      "round trip through the existing importer reports a malformed account instead of writing an empty one"
    ],
    "tests/unit/codex-autoping-catalog-3212.test.js": [
      "codex auto-ping model selection is wired (#3212) does not fetch the catalog on a tick that would not ping anyway",
      "codex auto-ping model selection is wired (#3212) keeps the configured model when the catalog is unreachable",
      "codex auto-ping model selection is wired (#3212) keeps the configured model when the catalog request is rejected",
      "codex auto-ping model selection is wired (#3212) pings the live catalog's model, not the configured one",
      "codex auto-ping model selection is wired (#3212) skips the upstream ping when the catalog offers nothing callable",
      "selectCodexPingModel (#3212) filters out entries the catalog says are not API-supported",
      "selectCodexPingModel (#3212) prefers an explicit catalog default over position",
      "selectCodexPingModel (#3212) respects catalog order when nothing is marked default",
      "selectCodexPingModel (#3212) returns null when the catalog offers nothing callable",
      "selectCodexPingModel (#3212) returns undefined for a payload that is not a catalog at all",
      "selectCodexPingModel (#3212) treats a missing supported_in_api flag as supported"
    ],
    "tests/unit/codex-callback-bind.test.js": [
      "codex callback proxy bind answers on BOTH loopback families, which is what makes the localhost redirect land",
      "codex callback proxy bind completes the exchange when the callback arrives over IPv6",
      "codex callback proxy bind does NOT widen past loopback — a code-bearing callback stays off the LAN",
      "codex callback proxy bind frees the port on stop, so the next sign-in does not hit a half-closed listener",
      "codex callback proxy bind still reports port_busy when the fixed port is held, preserving the existing contract",
      "xai proxy keeps the same bind shape answers on both loopback families"
    ],
    "tests/unit/codex-executor-edge-coverage.test.js": [
      "_peekSseTransientError stream mechanics propagates cancel through the replacement body to the upstream reader",
      "_peekSseTransientError stream mechanics reassembles peeked bytes plus the unread remainder into the replacement body",
      "_peekSseTransientError stream mechanics survives a body that errors mid-read and reassembles what it saw",
      "execute SSE verdicts converts an SSE account-capacity error into a 503 with the extracted message",
      "execute SSE verdicts does not regenerate after an accepted structured SSE overloaded error",
      "execute SSE verdicts does not retry a ciphertext 400 when the body carries no strippable input",
      "execute SSE verdicts parses an error split into single-byte CRLF frames without replay",
      "execute SSE verdicts passes malformed or untyped content through without fabrication",
      "execute SSE verdicts passes malformed or untyped content through without fabrication",
      "execute SSE verdicts passes malformed or untyped content through without fabrication",
      "execute SSE verdicts passes malformed or untyped content through without fabrication",
      "execute SSE verdicts preserves ordinary output containing model_at_capacity byte for byte without regeneration",
      "execute SSE verdicts preserves ordinary output containing Selected model is at capacity byte for byte without regeneration",
      "execute SSE verdicts preserves ordinary output containing server_is_overloaded byte for byte without regeneration",
      "execute SSE verdicts preserves ordinary output containing service_unavailable_error byte for byte without regeneration",
      "execute SSE verdicts retains partial output before a later error in the same chunk",
      "execute SSE verdicts returns the 400 untouched when the error body cannot be read",
      "headers and credential plumbing injects the default originator when the base headers lack one",
      "headers and credential plumbing prefetchImages leaves an image part with no resolvable url untouched",
      "headers and credential plumbing refreshCredentials returns null without a refreshToken",
      "parseError usage_limit_reached defers to the base parser when no reset hint, on bad JSON, and on non-429",
      "parseError usage_limit_reached falls back to resets_in_seconds when resets_at is stale",
      "parseError usage_limit_reached uses resets_at when it is in the future",
      "transformRequest normalization guards converts system role to developer, skipping null items",
      "transformRequest normalization guards drops tool_choice referencing an unknown function and keeps a known one",
      "transformRequest normalization guards filters null tools, unknown named types, and nameless function tools; keeps hosted types",
      "transformRequest normalization guards keeps a paired tool output while tolerating null input items",
      "transformRequest normalization guards leaves a role-bearing non-message item and typed assistant parts repaired",
      "transformRequest normalization guards removes any field outside the Responses allowlist",
      "transformRequest normalization guards restores the typed placeholder when input normalizes to nothing",
      "transformRequest normalization guards returns an unrecognized effort value unchanged for a model with no thinking levels",
      "transformRequest normalization guards strips a recognized effort suffix off the model and applies it as reasoning effort",
      "transformRequest normalization guards strips server-generated ids: string refs, item_reference objects, and embedded ids"
    ],
    "tests/unit/codex-fast-capacity.test.js": [
      "Codex fast tier and capacity handling classifies 200-SSE context overflow as a terminal client error",
      "Codex fast tier and capacity handling classifies structured 200-SSE model capacity",
      "Codex fast tier and capacity handling detects a structured context overflow split across stream chunks",
      "Codex fast tier and capacity handling does not carry a failure event type into a later output event",
      "Codex fast tier and capacity handling does not classify normal output as context overflow: The request used too many tokens in the example.",
      "Codex fast tier and capacity handling does not classify normal output as context overflow: This explains what exceeds the context window.",
      "Codex fast tier and capacity handling does not inspect non-2xx upstream responses as SSE",
      "Codex fast tier and capacity handling forwards supported service tier default verbatim",
      "Codex fast tier and capacity handling forwards supported service tier priority verbatim",
      "Codex fast tier and capacity handling forwards supported service tier ultrafast verbatim",
      "Codex fast tier and capacity handling keeps HTTP 413 terminal through shared error and fallback handling",
      "Codex fast tier and capacity handling leaves untyped non-JSON SSE words unclassified",
      "Codex fast tier and capacity handling maps Codex fast tier to priority and max reasoning to xhigh",
      "Codex fast tier and capacity handling omits an unsupported service tier rather than remapping it",
      "Codex fast tier and capacity handling reassembles normal SSE after peeking",
      "Codex fast tier and capacity handling returns context overflow as HTTP 413 without retrying",
      "Codex fast tier and capacity handling treats a non-overflow error code as authoritative",
      "Codex fast tier and capacity handling uses ChatGPT workspace header fallback",
      "Codex reasoning normalization normalizes gpt-5.6-luna effort max to max",
      "Codex reasoning normalization normalizes gpt-5.6-luna effort ultra to max",
      "Codex reasoning normalization normalizes gpt-5.6-sol effort max to max",
      "Codex reasoning normalization normalizes gpt-5.6-sol effort ultra to ultra",
      "Codex reasoning normalization normalizes gpt-5.6-terra effort max to max",
      "Codex reasoning normalization normalizes gpt-5.6-terra effort ultra to ultra",
      "Codex reasoning normalization resolves review models before applying the reasoning matrix"
    ],
    "tests/unit/codex-image-adapter-stream.test.js": [
      "codex adapter request shaping buildBody omits empty optional params and keeps unsuffixed model names",
      "codex adapter request shaping buildBody strips the image suffix, streams, and forwards optional tool params",
      "codex adapter request shaping buildBody wraps reference images as data URLs with framing text",
      "codex adapter request shaping buildHeaders decodes the account id from the idToken JWT payload",
      "codex adapter request shaping buildHeaders degrades to empty strings on missing/garbage credentials",
      "codex adapter request shaping buildHeaders prefers providerSpecificData account id and bears the access token",
      "codex adapter request shaping buildUrl returns the configured provider base URL",
      "codex adapter SSE parsing (non-streaming) collects the final image b64 from response.output_item.done",
      "codex adapter SSE parsing (non-streaming) handles chunk boundaries splitting an SSE block and ignores malformed data",
      "codex adapter SSE parsing (non-streaming) throws when the stream ends without an image",
      "codex adapter SSE streaming to client emits an error event when reading the upstream stream throws",
      "codex adapter SSE streaming to client emits an error event when the stream carries no image, without onRequestSuccess",
      "codex adapter SSE streaming to client pipes progress, partial images, and done to the client and fires onRequestSuccess"
    ],
    "tests/unit/codex-image-fetch.test.js": [
      "CodexExecutor image handling execute() prefetches images before sending to upstream",
      "CodexExecutor image handling falls back to original URL when remote fetch fails",
      "CodexExecutor image handling fetches 1MB remote image and inlines it as base64 data URI",
      "CodexExecutor image handling passes through existing data URIs without calling fetch"
    ],
    "tests/unit/codex-image-to-chat-kimi-k3-3392.test.js": [
      "a Codex image survives the Responses to Chat translation (#3392) input_image becomes an image_url block rather than staying Responses-shaped",
      "a Codex image survives the Responses to Chat translation (#3392) the base64 payload is not flattened into text, which is the inflation",
      "a Codex image survives the Responses to Chat translation (#3392) the requested detail is carried, not reset",
      "opencode-go kimi-k3 is pinned to the Chat wire (#3392) it declares openai only, which is what forces the translation",
      "opencode-go kimi-k3 is pinned to the Chat wire (#3392) its sibling k2.6 is pinned the same way, so this is the family convention",
      "opencode-go kimi-k3 is pinned to the Chat wire (#3392) the model is registered"
    ],
    "tests/unit/codex-manual-code.test.js": [
      "manual-code route covers codex as well as xai, which was the only provider allowed before",
      "manual-code route REFUSES a pasted URL whose state disagrees with the registered sign-in",
      "manual-code route reports an expired or missing session as \"start the sign-in again\" rather than obscurely",
      "manual-code route still refuses a provider that has no manual fallback",
      "parseManualCallback accepts a bare code, falling back to the state the row already holds",
      "parseManualCallback accepts a bare query fragment pasted without its origin",
      "parseManualCallback pulls the code and state out of a whole pasted callback URL",
      "parseManualCallback refuses a callback URL that carries no code",
      "parseManualCallback surfaces a provider error carried in the URL instead of exchanging it"
    ],
    "tests/unit/codex-model-catalog-1908.test.js": [
      "Codex clients get a catalog they can decode (#1908) a bare id with no provider prefix still gets a display name",
      "Codex clients get a catalog they can decode (#1908) describes exactly the same set as the OpenAI list it was built from",
      "Codex clients get a catalog they can decode (#1908) every entry carries the identity fields",
      "Codex clients get a catalog they can decode (#1908) omits the two fields whose value domain is not derivable here",
      "Codex clients get a catalog they can decode (#1908) search support is read from the capability catalog, not invented",
      "Codex clients get a catalog they can decode (#1908) survives an empty or malformed list without inventing entries",
      "Codex clients get a catalog they can decode (#1908) the envelope is `models`, which is the field the decoder demands",
      "only a Codex client sees it both branches read the same `out`, so Codex never sees a different set",
      "only a Codex client sees it that detector recognises the Codex clients and not the others",
      "only a Codex client sees it the OpenAI list shape is still what everyone else gets",
      "only a Codex client sees it the reshape happens after sorting, so both views agree on order",
      "only a Codex client sees it the route reuses the request path's own detector"
    ],
    "tests/unit/codex-native-passthrough-thinking.test.js": [
      "native Codex passthrough thinking suffixes converts unsupported Luna Ultra to Max without dropping reasoning metadata",
      "native Codex passthrough thinking suffixes forwards Ultra for Sol",
      "native Codex passthrough thinking suffixes forwards Ultra through a Terra review alias"
    ],
    "tests/unit/codex-orphaned-tool-outputs.test.js": [
      "Codex orphaned tool outputs does not mutate retained native items or log call IDs",
      "Codex orphaned tool outputs drops duplicate outputs after the first valid result",
      "Codex orphaned tool outputs drops unmatched function, custom, and malformed output items",
      "Codex orphaned tool outputs keeps correctly paired function and custom outputs with structured content",
      "Codex orphaned tool outputs leaves input unchanged when no output items are present",
      "Codex orphaned tool outputs recognizes Chat Completions assistant tool call IDs",
      "Codex orphaned tool outputs restores the typed placeholder after custom-only orphan cleanup",
      "Codex orphaned tool outputs restores the typed placeholder after function-only orphan cleanup"
    ],
    "tests/unit/codex-per-request-state-3164.test.js": [
      "codex keeps per-request state off the shared executor (#3164) a request carrying neither field still gets a header and a plain URL",
      "codex keeps per-request state off the shared executor (#3164) compact routing follows the flag on the request's own credentials",
      "codex keeps per-request state off the shared executor (#3164) no instance fields carry request state any more",
      "codex keeps per-request state off the shared executor (#3164) the compact flag is resolved before the URL is built",
      "codex keeps per-request state off the shared executor (#3164) the executor really is a process-wide singleton",
      "codex keeps per-request state off the shared executor (#3164) two concurrent requests keep their own session id"
    ],
    "tests/unit/codex-reauth-state-3194.test.js": [
      "Codex permanently invalid OAuth state (#3194) keeps an ordinary Codex 401 model-scoped",
      "Codex permanently invalid OAuth state (#3194) keeps non-Codex invalidation markers model-scoped",
      "Codex permanently invalid OAuth state (#3194) quarantines a resolved Codex 401 containing authentication token has been invalidated",
      "Codex permanently invalid OAuth state (#3194) quarantines a resolved Codex 401 containing invalidated oauth token",
      "Codex permanently invalid OAuth state (#3194) quarantines a resolved Codex 401 containing refresh token already used",
      "Codex permanently invalid OAuth state (#3194) quarantines a resolved Codex 401 containing refresh_token_invalidated",
      "Codex permanently invalid OAuth state (#3194) quarantines a resolved Codex 401 containing refresh_token_reused",
      "Codex permanently invalid OAuth state (#3194) restores the same Codex OAuth profile and clears stale failure state"
    ],
    "tests/unit/codex-refresh-token.test.js": [
      "Codex Refresh Token CodexExecutor credential lifecycle should de-duplicate concurrent refreshes for the same Codex connection",
      "Codex Refresh Token CodexExecutor credential lifecycle should refresh Codex credentials and preserve omitted id_token",
      "Codex Refresh Token CodexExecutor credential lifecycle should refresh Codex when lastRefreshAt is older than the upstream stale window",
      "Codex Refresh Token getRefreshLeadMs (early refresh config) codex lead should be greater than default buffer",
      "Codex Refresh Token getRefreshLeadMs (early refresh config) should fallback to default buffer for unknown providers",
      "Codex Refresh Token getRefreshLeadMs (early refresh config) should return provider-specific lead time for OAuth providers",
      "Codex Refresh Token refreshCodexToken should keep old refresh_token when server does not return new one",
      "Codex Refresh Token refreshCodexToken should return new refresh_token when server provides one (token rotation)"
    ],
    "tests/unit/codex-reset-credits.test.js": [
      "Codex reset credits GET force-refreshes OAuth credentials when reset credit fetch reports expired auth",
      "Codex reset credits GET refreshes OAuth credentials before returning reset credit details",
      "Codex reset credits POST returns 409 when there are no reset credits to consume",
      "Codex reset credits returns normalized reset credit expiry details"
    ],
    "tests/unit/codex-responses-system-injection.test.js": [
      "CodexExecutor Responses normalization converts assistant input_text array parts without rewriting other Responses history",
      "CodexExecutor Responses normalization does not rewrite typed tool-call history",
      "CodexExecutor Responses normalization preserves the typed injection through the final Codex transform",
      "CodexExecutor Responses normalization repairs a legacy role/content item before the Codex request is dispatched",
      "Responses system-prompt injection adds a typed message item when a Responses request has no instructions",
      "Responses system-prompt injection allows Caveman and Ponytail to append to the same Responses message",
      "Responses system-prompt injection keeps Responses developer content typed when appending another instruction",
      "Responses system-prompt injection retains the Chat Completions string shape"
    ],
    "tests/unit/codex-stale-ciphertext-2667.test.js": [
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) also recognises a bare decrypt failure",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) cancels both diagnostic branches and never resends when the caller aborts inspection",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) cancels the discarded rejected body before the repaired dispatch",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) does not repair or resend when x-should-retry denies replay",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) does not repair or resend when x-tokenproxy-replay-safe denies replay",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) does not retry when the request carries no ciphertext to drop",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) does not reuse the original400 after the repaired attempt loses its transport outcome",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) does not use an oversized diagnostic as proof of a repairable rejection",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) leaves an unrelated 400 untouched and readable",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) retries once only — a second stale 400 is surfaced",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) returns a stalled diagnostic after the inspection deadline without a second dispatch",
      "Codex recovers from a stale encrypted-reasoning 400 (#2667) strips the ciphertext and retries once"
    ],
    "tests/unit/codex-subscription-entitlement.test.js": [
      "getCodexSubscriptionEntitlement accessToken hint beats stale idToken and PSD",
      "getCodexSubscriptionEntitlement accounts/check requires Cockpit web headers Referer User-Agent x-openai-target",
      "getCodexSubscriptionEntitlement cache 6h success TTL avoids network",
      "getCodexSubscriptionEntitlement CRLF in account id is stripped from ChatGPT-Account-Id header",
      "getCodexSubscriptionEntitlement expiry exactly nowMs is rejected (strict >)",
      "getCodexSubscriptionEntitlement fail-open on network error, non-2xx, malformed json keeps last-known-good",
      "getCodexSubscriptionEntitlement failed-attempt retry 30m avoids network when recent failure",
      "getCodexSubscriptionEntitlement fallback to /subscriptions when entitlement missing/expired",
      "getCodexSubscriptionEntitlement force bypasses 6h and 30m caches but not valid JWT claim",
      "getCodexSubscriptionEntitlement forwards proxyOptions exactly and queries timezone_offset_min",
      "getCodexSubscriptionEntitlement fresh 30m attempt cache with past activeUntil must NOT return past",
      "getCodexSubscriptionEntitlement fresh 6h cache with past activeUntil and no future network falls back to null",
      "getCodexSubscriptionEntitlement fresh 6h cache with past activeUntil must NOT return past (proceeds to network)",
      "getCodexSubscriptionEntitlement future accounts snapshot beats past /subscriptions fallback",
      "getCodexSubscriptionEntitlement future last-known-good cache beats past /subscriptions fallback when accounts snapshot not future",
      "getCodexSubscriptionEntitlement handles numeric-string epoch seconds and milliseconds for JWT claim",
      "getCodexSubscriptionEntitlement ignores generic exp and quota expiry, falls through to network",
      "getCodexSubscriptionEntitlement malformed JWT remains fail-open and exposes only safe cache fields",
      "getCodexSubscriptionEntitlement nested chatgpt_subscription_active_until wins (ISO) and avoids network even with force",
      "getCodexSubscriptionEntitlement nested object map preserves outer org key and account_ordering",
      "getCodexSubscriptionEntitlement nested object-map with no inner id falls back to outer key for subscriptions",
      "getCodexSubscriptionEntitlement network throw with past cache returns null, future cache preserved",
      "getCodexSubscriptionEntitlement never returns token or raw payload in result or throw",
      "getCodexSubscriptionEntitlement no accessToken with past cache returns null activeUntil and no past persist",
      "getCodexSubscriptionEntitlement non-2xx also fail-open",
      "getCodexSubscriptionEntitlement normalizes epoch seconds and ms from JWT claim",
      "getCodexSubscriptionEntitlement paid non-free workspace beats free default when no org/account hints",
      "getCodexSubscriptionEntitlement paid workspace with top-level plan: 'plus' beats free default",
      "getCodexSubscriptionEntitlement parses accounts as array or object robustly and selects non-free then first",
      "getCodexSubscriptionEntitlement past /subscriptions expiry is rejected, not returned or persisted (falls back to null when no future snapshot/cache)",
      "getCodexSubscriptionEntitlement prefers providerSpecificData organization over stale idToken when accessToken has no hint",
      "getCodexSubscriptionEntitlement rejected past subscriptions plan does not overwrite/persist",
      "getCodexSubscriptionEntitlement repeated valid idToken same expiry/plan with fresh fetchedAt does not request patch or network",
      "getCodexSubscriptionEntitlement same valid idToken after 6h may refresh fetchedAt once",
      "getCodexSubscriptionEntitlement selects org ID via nested claim > providerSpecificData > default > non-free > first",
      "getCodexSubscriptionEntitlement subscriptions fallback non-2xx preserves accounts snapshot expiry (fail-open)",
      "getCodexSubscriptionEntitlement subscriptions fallback uses web headers encoded id ChatGPT-Account-Id",
      "getCodexSubscriptionEntitlement subscriptions network error preserves accounts snapshot expiry",
      "getCodexSubscriptionEntitlement supports providerSpecificData organizationId/chatgptOrganizationId and matches workspace_id",
      "getCodexSubscriptionEntitlement top-level chatgpt_subscription_active_until wins",
      "getCodexSubscriptionEntitlement uses provider registry via U('codex') not hardcoded endpoint"
    ],
    "tests/unit/codex-subscription-route.test.js": [
      "GET /api/usage/[connectionId] codex subscription calls helper for Codex OAuth and merges safe fields without overwriting plan with null",
      "GET /api/usage/[connectionId] codex subscription does not persist when patch has no changes (no-change path)",
      "GET /api/usage/[connectionId] codex subscription helper failure is fail-open and does not affect quota response",
      "GET /api/usage/[connectionId] codex subscription merges subscription fields into JSON and persists patch only when changed (changed path)",
      "GET /api/usage/[connectionId] codex subscription preserves all providerSpecificData and quotas on merge",
      "GET /api/usage/[connectionId] codex subscription skips helper for Codex API-key (api_key)",
      "GET /api/usage/[connectionId] codex subscription skips helper for Codex API-key (apikey spelling)",
      "GET /api/usage/[connectionId] codex subscription skips helper for non-codex provider",
      "GET /api/usage/[connectionId] codex subscription skips helper for OpenCode Go"
    ],
    "tests/unit/codex-tier-log-3239.test.js": [
      "Codex effective service tier logging (#3239) logs the final transformed tier for default",
      "Codex effective service tier logging (#3239) logs the final transformed tier for fast",
      "Codex effective service tier logging (#3239) logs the final transformed tier for priority",
      "Codex effective service tier logging (#3239) logs the final transformed tier for undefined"
    ],
    "tests/unit/codex-token-expired-refresh-1821.test.js": [
      "Codex token_expired refresh classification (#1821) classifies a 401 token_expired body as permanent",
      "Codex token_expired refresh classification (#1821) keeps a rate-limit body transient",
      "Codex token_expired refresh classification (#1821) returns an unrecoverable result instead of null so the retry loop stops"
    ],
    "tests/unit/codex-tool-fidelity-2512.test.js": [
      "#2512 Codex request fidelity for tool-calling fields does not invent strict when the client omitted it or asked for true",
      "#2512 Codex request fidelity for tool-calling fields forwards the client's parallel_tool_calls instead of stripping it",
      "#2512 Codex request fidelity for tool-calling fields keeps strict:false on a function tool whose optional params are not all required",
      "#2512 Codex request fidelity for tool-calling fields omits parallel_tool_calls when the client did not send one",
      "#2512 Codex request fidelity for tool-calling fields reads strict:false off the nested Chat-Completions tool shape too",
      "#2512 Codex request fidelity for tool-calling fields still strips fields the Codex upstream rejects"
    ],
    "tests/unit/codex-tool-normalization.test.js": [
      "CodexExecutor tool normalization preserves custom freeform tools with format payloads",
      "CodexExecutor tool normalization preserves hosted Responses tools",
      "CodexExecutor tool normalization preserves Responses text.format for structured outputs",
      "CodexExecutor tool normalization preserves Responses-native tool_search tools"
    ],
    "tests/unit/codex-usage-error-paths.test.js": [
      "consumeCodexRateLimitResetCredit rejects a missing or non-string redeem request id",
      "consumeCodexRateLimitResetCredit rejects without an access token",
      "consumeCodexRateLimitResetCredit reports noCredit on code=no_credit without throwing",
      "consumeCodexRateLimitResetCredit succeeds on code=reset and posts the redeem id to the consume URL",
      "consumeCodexRateLimitResetCredit wraps a network failure",
      "getCodexRateLimitResetCredits forwards the account id header from providerSpecificData",
      "getCodexRateLimitResetCredits non-OK with a structured message surfaces that message",
      "getCodexRateLimitResetCredits non-OK with unparseable body throws the fallback availability message",
      "getCodexRateLimitResetCredits rejects without an access token",
      "getCodexSubscriptionEntitlement cache and normalization fresh fetchedAt with a future cached expiry returns the cache without network",
      "getCodexSubscriptionEntitlement cache and normalization fresh fetchedAt, no cache, no token records only the attempt",
      "getCodexSubscriptionEntitlement cache and normalization malformed JWT payload decodes to null and falls through",
      "getCodexSubscriptionEntitlement cache and normalization no token but a stale future cache returns the cache with an attempt patch",
      "getCodexSubscriptionEntitlement cache and normalization numeric epoch-seconds JWT claim normalizes to ISO without network",
      "getCodexSubscriptionEntitlement cache and normalization numeric-string claim normalizes; non-finite and object claims are rejected",
      "getCodexSubscriptionEntitlement cache and normalization out-of-range now falls back to a real clock for nowIso",
      "getCodexSubscriptionEntitlement cache and normalization recent attempt with a future cache returns the cache; without cache and no token records the attempt",
      "getCodexSubscriptionEntitlement network path empty accounts list yields the null attempt result",
      "getCodexSubscriptionEntitlement network path expired accounts snapshot triggers the subscriptions fallback (array shape)",
      "getCodexSubscriptionEntitlement network path failed subscriptions fallback with only an expired snapshot returns the null attempt result",
      "getCodexSubscriptionEntitlement network path malformed accounts JSON falls back to the cached snapshot",
      "getCodexSubscriptionEntitlement network path selects by idToken account id when no org hint matches",
      "getCodexSubscriptionEntitlement network path selects the account by idToken org hint, honors account_ordering, uses entitlement expiry",
      "getCodexSubscriptionEntitlement network path subscriptions fallback unwraps { data: [...] } and { subscriptions: [...] }",
      "getCodexUsage error paths network failure is wrapped with a Codex-specific error",
      "getCodexUsage error paths non-OK response returns an availability message, not a throw"
    ],
    "tests/unit/codex-vision-capability-1302.test.js": [
      "codex family resolves vision (#1302, #1201) blackbox/gpt-5.3-codex reads images",
      "codex family resolves vision (#1302, #1201) cu/gpt-5.2-codex reads images",
      "codex family resolves vision (#1302, #1201) cu/gpt-5.3-codex reads images",
      "codex family resolves vision (#1302, #1201) does not hand vision to an image-OUTPUT model by widening gpt-5",
      "codex family resolves vision (#1302, #1201) gh/gpt-5.2-codex reads images",
      "codex family resolves vision (#1302, #1201) gpt-5-codex reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.1-codex reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.1-codex-max reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.1-codex-mini reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.2-codex reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.3-codex reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) gpt-5.3-codex-spark reads images on opencode-zen",
      "codex family resolves vision (#1302, #1201) keeps the rest of the codex row intact",
      "codex family resolves vision (#1302, #1201) opencode-zen/gpt-5.2-codex reads images",
      "meta provider entry keeps Muse Spark multimodal meta/muse-spark-1.1 keeps vision, audio and video input",
      "meta provider entry keeps Muse Spark multimodal meta/muse-spark-1.2 keeps vision, audio and video input",
      "meta provider entry keeps Muse Spark multimodal meta/muse-spark-1.2-contributor keeps vision, audio and video input",
      "provider aliases resolve to the same entry as the canonical id an unknown provider still falls through to the pattern table",
      "provider aliases resolve to the same entry as the canonical id cbcn/codebuddy-cn matches glm-5v-turbo",
      "provider aliases resolve to the same entry as the canonical id cbcn/glm-5v-turbo reads images (GLM-5V is a vision model)",
      "provider aliases resolve to the same entry as the canonical id cx/codex matches gpt-5.6-sol",
      "provider aliases resolve to the same entry as the canonical id kr/kiro matches gpt-5.6-terra",
      "provider aliases resolve to the same entry as the canonical id qd/* no longer falls to the 200K text-only floor",
      "provider aliases resolve to the same entry as the canonical id qd/qoder matches kmodel_latest"
    ],
    "tests/unit/codex-workspace-connections-1172.test.js": [
      "codex workspace connections does not rename a connection the caller named itself",
      "codex workspace connections keeps a second workspace of the same account as its own connection",
      "codex workspace connections names the two workspaces apart so connection management can tell them apart",
      "codex workspace connections still merges a re-login to the workspace that is already connected"
    ],
    "tests/unit/cohere-provider-compatibility.test.js": [
      "Cohere compatibility transport uses the documented OpenAI-compatible chat and validation endpoints"
    ],
    "tests/unit/combo-advances-past-missing-model-1946.test.js": [
      "a combo advances past a model the provider does not serve (#1946) a bare 404 with no such body still falls back at account level",
      "a combo advances past a model the provider does not serve (#1946) it joins the existing escape hatch rather than a second branch",
      "a combo advances past a model the provider does not serve (#1946) the account-level predicate deliberately does NOT fall back on it",
      "a combo advances past a model the provider does not serve (#1946) the combo now treats it as a model limitation",
      "a combo advances past a model the provider does not serve (#1946) the generic phrasing is deliberately not matched",
      "a combo advances past a model the provider does not serve (#1946) the pre-existing limitation matches still work"
    ],
    "tests/unit/combo-api-validation-1423.test.js": [
      "combo API cycle validation (#1423) rejects a direct cycle before creating the combo",
      "combo API cycle validation (#1423) rejects an update that closes an indirect cycle",
      "combo graph validation (#1423) accepts a chain ending at real provider models",
      "combo graph validation (#1423) rejects an indirect cycle reached through a provider-prefixed combo member"
    ],
    "tests/unit/combo-autoswitch.test.js": [
      "detectRequiredCapabilities antigravity request.contents image -> vision",
      "detectRequiredCapabilities gemini inlineData image -> vision",
      "detectRequiredCapabilities openai file -> pdf",
      "detectRequiredCapabilities openai image_url -> vision",
      "detectRequiredCapabilities responses input_image -> vision",
      "detectRequiredCapabilities text-only -> empty",
      "detectRequiredCapabilities web_search tool -> no capability (search detection disabled)",
      "detectRequiredCapabilities web_search tool alongside an image still detects vision",
      "reorderByCapabilities floats vision-capable model to front, keeps fallback",
      "reorderByCapabilities keeps order when no model matches",
      "reorderByCapabilities no required -> unchanged",
      "reorderByCapabilities single model -> unchanged"
    ],
    "tests/unit/combo-body-isolation-3619.test.js": [
      "each combo attempt gets its own body (#3619) a body that cannot be cloned is passed through rather than failing the request",
      "each combo attempt gets its own body (#3619) a translator that mutates the first attempt does not reach the second",
      "each combo attempt gets its own body (#3619) the caller's own body is left untouched, so a retry above this layer is clean",
      "each combo attempt gets its own body (#3619) the copy still carries everything the attempt needs"
    ],
    "tests/unit/combo-capabilities-2805.test.js": [
      "combo entries expose intersected capabilities (#2805) intersects rather than unions: one text-only member clears the flag",
      "combo entries expose intersected capabilities (#2805) keeps the per-member caps instead of dropping them",
      "combo entries expose intersected capabilities (#2805) only intersects booleans, never thinkingFormat or a window",
      "combo entries expose intersected capabilities (#2805) uses the same emission condition a single-model entry uses"
    ],
    "tests/unit/combo-context-length-1089.test.js": [
      "handleComboChat prioritizes the context-fitting model first (#1089) tries the large-context model before the undersized one for a big prompt",
      "reorderByContextFit (#1089) floats a large-context model ahead of an undersized one for a big request",
      "reorderByContextFit (#1089) leaves order unchanged when nothing is required (0 / falsy)",
      "reorderByContextFit (#1089) never drops a model even when NONE fit — the combo must still get a candidate to try",
      "reorderByContextFit (#1089) single model -> unchanged"
    ],
    "tests/unit/combo-cycle-guard-1235.test.js": [
      "a self-referencing combo is refused, not recursed (#1235) answers with a client error naming the cycle, not a crash",
      "a self-referencing combo is refused, not recursed (#1235) leaves the solo capacity-adapter path unseeded, since it is not a combo",
      "a self-referencing combo is refused, not recursed (#1235) passes a COPY of the chain to each member, so siblings do not poison each other",
      "a self-referencing combo is refused, not recursed (#1235) records the combo before expanding its members",
      "a self-referencing combo is refused, not recursed (#1235) refuses to re-enter a combo already being expanded",
      "a self-referencing combo is refused, not recursed (#1235) seeds the chain at the top-level dispatch, catching self-reference at the first hop"
    ],
    "tests/unit/combo-default-model-2114.test.js": [
      "default model for a new combo (#2114) fails open when the availability list cannot be read",
      "default model for a new combo (#2114) matches an unavailability reported under the provider id or its alias",
      "default model for a new combo (#2114) never seeds a combo with another combo",
      "default model for a new combo (#2114) reports no-available-model when every candidate is exhausted",
      "default model for a new combo (#2114) returns the first listed model when nothing is exhausted",
      "default model for a new combo (#2114) skips a model that is on cooldown",
      "default model for a new combo (#2114) skips every model of a provider reported unavailable account-wide"
    ],
    "tests/unit/combo-empty-stream-3463.test.js": [
      "combo empty-stream guard is time-bounded (#3463) gives up on a keepalive-only stream instead of blocking forever",
      "combo refuses replay of empty accepted streams (#3463) accepts a Claude-format content block as meaningful",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is claude input_json_delta",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is claude text_delta",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is claude tool_use block start",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is gemini functionCall part",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is gemini text part",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is ollama message content",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is reasoning-only delta",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is responses output_text delta",
      "combo refuses replay of empty accepted streams (#3463) accepts a stream whose only frame is usage with output tokens",
      "combo refuses replay of empty accepted streams (#3463) accepts a tool-call-only stream as meaningful",
      "combo refuses replay of empty accepted streams (#3463) does not consume a non-stream JSON completion body",
      "combo refuses replay of empty accepted streams (#3463) leaves non-SSE responses alone so tts/search/image combos are unaffected",
      "combo refuses replay of empty accepted streams (#3463) passes the stream through once the peek byte budget is exhausted",
      "combo refuses replay of empty accepted streams (#3463) preserves a frame split across two network chunks",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: claude content_block_start text envelope only",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: claude message_start envelope only",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: empty tool_calls array",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: empty-string content delta",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: gemini candidate with empty text part",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: gemini candidate with no parts",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: role + finish_reason only",
      "combo refuses replay of empty accepted streams (#3463) refuses replay on a metadata-only accepted stream: usage frame reporting zero output tokens",
      "combo refuses replay of empty accepted streams (#3463) replays every byte, including frames that precede the first meaningful one",
      "combo refuses replay of empty accepted streams (#3463) replays upstream bytes verbatim when the preamble is not valid UTF-8",
      "combo refuses replay of empty accepted streams (#3463) reports 502 after the first accepted empty stream",
      "combo refuses replay of empty accepted streams (#3463) returns the first model untouched when it does send content",
      "combo refuses replay of empty accepted streams (#3463) stops when the accepted stream closes without sending a single byte",
      "combo refuses replay of empty accepted streams (#3463) stops when the first model accepts but returns zero meaningful frames",
      "combo refuses replay of empty accepted streams (#3463) treats a stream carrying only [DONE] as a failure"
    ],
    "tests/unit/combo-fusion-branches.test.js": [
      "combo graph helpers findComboCycle detects a mutual cycle and clears a diamond via the visited set",
      "combo graph helpers getComboModelsFromData resolves full name, basename, and rejects non-strings",
      "combo graph helpers validateComboAcyclic requires a name and reports the cycle path",
      "combo token-saver and member-connection resolution resolveComboMemberConnection: pin found, guards reject non-strings and inherited keys",
      "combo token-saver and member-connection resolution resolveComboTokenSaver: outermost boolean-false gate, per-key override, guards",
      "detectRequiredCapabilities modality shapes attachments: mime-typed audio, and bare url fallback -> vision",
      "detectRequiredCapabilities modality shapes document with data-URI source, string pdf, and generic file fallback -> pdf",
      "detectRequiredCapabilities modality shapes input_audio block, message-level audio_url, and data:audio string -> audioInput",
      "detectRequiredCapabilities modality shapes video block and document with video media_type -> videoInput",
      "extractPanelText openai chat, text-completion, gemini parts, and responses output shapes",
      "handleComboChat 499 client abort is returned as-is without trying further members",
      "handleComboChat a rejected structured error permits fallback, but a thrown next member stops further replay",
      "handleComboChat an accepted empty stream is terminal before any later member is attempted",
      "handleComboChat an accepted stream carrying an upstream error returns its error without rotating",
      "handleComboChat reports the earliest reset when all members explicitly reject for quota depletion",
      "handleComboChat retries an explicitly rejected 503 after its HTTP-date delay on the same member",
      "handleComboChat round-robin fallback success advances the rotation cursor past the served member",
      "handleFusionChat empty panel returns 400; single-member panel answers directly",
      "handleFusionChat logs timeout, throw, failure, empty and unparseable panel members; single survivor answers directly",
      "handleFusionChat two answers reach the judge; a bodyless request gets a synthesized user turn",
      "reorder helpers reorderByCapabilities soft-cap tier splits satisfied hard caps by soft caps",
      "reorder helpers reorderByContextFit guards: no tokens, non-array, single model",
      "rotation round-robin advances, peek does not, other strategies pass through"
    ],
    "tests/unit/combo-fusion.test.js": [
      "fusion combo answers directly with a single-model panel (nothing to fuse)",
      "fusion combo defaults the judge to the first panel model when none is set",
      "fusion combo fans out to the panel then routes a synthesis turn to the judge",
      "fusion combo flattens Anthropic-style tool_use and tool_result blocks in arrays",
      "fusion combo flattens previous tool history and assistant tool_calls into prose for panel calls",
      "fusion combo proceeds on quorum without waiting for a straggler (grace window)",
      "fusion combo returns 503 when the whole panel fails",
      "fusion combo returns the lone survivor directly when only one panel model succeeds"
    ],
    "tests/unit/combo-member-account-1477.test.js": [
      "a combo can pin one member to one account (#1477) a plain request never pays the settings read",
      "a combo can pin one member to one account (#1477) a replay pin still wins, since it is about reaching the account that just failed",
      "a combo can pin one member to one account (#1477) an inherited property is not a configured pin",
      "a combo can pin one member to one account (#1477) ignores a malformed map rather than throwing on it",
      "a combo can pin one member to one account (#1477) returns nothing for a member the combo does not name",
      "a combo can pin one member to one account (#1477) returns nothing outside a combo, so a plain request is unchanged",
      "a combo can pin one member to one account (#1477) returns the account named for that member",
      "a combo can pin one member to one account (#1477) the handler pins strictly, because a named account must not be substituted",
      "a combo can pin one member to one account (#1477) the outermost combo wins, so a nested one cannot override the entry point"
    ],
    "tests/unit/combo-peek-rotation-3404.test.js": [
      "peekRotatedModels (#3404 combo batch test) documents that getRotatedModels advances the live cursor on every call",
      "peekRotatedModels (#3404 combo batch test) honours a sticky limit that is holding the cursor in place",
      "peekRotatedModels (#3404 combo batch test) leaves live routing on the model it was already going to serve",
      "peekRotatedModels (#3404 combo batch test) passes non-rotating input straight through",
      "peekRotatedModels (#3404 combo batch test) reads a rotation state that getRotatedModels advanced twice",
      "peekRotatedModels (#3404 combo batch test) reports the order live traffic would actually get next",
      "peekRotatedModels (#3404 combo batch test) returns the same order repeatedly without advancing the cursor"
    ],
    "tests/unit/combo-priority-2467.test.js": [
      "`fallback` already is the requested priority strategy (#2467) fails over to the next member on a rolling usage limit",
      "`fallback` already is the requested priority strategy (#2467) is distinct from round-robin, which does NOT hold priority",
      "`fallback` already is the requested priority strategy (#2467) keeps the stored order untouched, request after request",
      "`fallback` already is the requested priority strategy (#2467) returns to the preferred member on the next request once its window resets",
      "`fallback` already is the requested priority strategy (#2467) sends every request to the highest-priority member while it is healthy"
    ],
    "tests/unit/combo-provider-agnostic-1584.test.js": [
      "#1584 a combo is walked by position, not by provider keeps walking past several failing members of different providers",
      "#1584 a combo is walked by position, not by provider reaches the non-codex members when the codex member fails",
      "#1584 a combo is walked by position, not by provider stops after accepted empty generation independently of provider identity",
      "#1584 a combo is walked by position, not by provider stops at the first healthy member whatever its provider — that is fallback, not a codex rule"
    ],
    "tests/unit/combo-rename-check-2763.test.js": [
      "editing a combo without renaming it (#2763) allows a rename to a name nobody holds",
      "editing a combo without renaming it (#2763) saves a combo whose name is the one the lookup returns",
      "editing a combo without renaming it (#2763) saves even when another row already holds the same name",
      "editing a combo without renaming it (#2763) still refuses a rename onto a name another combo holds",
      "editing a combo without renaming it (#2763) still rejects an invalid name",
      "editing a combo without renaming it (#2763) still reports a missing combo rather than a name collision"
    ],
    "tests/unit/combo-routing.test.js": [
      "combo round-robin routing continues after the model that succeeds through a round-robin fallback",
      "combo round-robin routing continues after the successful duplicate model occurrence",
      "combo round-robin routing does not rotate fallback combos",
      "combo round-robin routing keeps existing one-request round-robin behavior by default",
      "combo round-robin routing keeps normal round-robin advancement when the first model succeeds",
      "combo round-robin routing resets sticky rotation after a successful fallback",
      "combo round-robin routing sticks to each combo model for the configured number of requests",
      "combo round-robin routing tracks sticky rotation independently per combo",
      "combo round-robin routing uses the successful model's original position after auto-switch reorders fallbacks"
    ],
    "tests/unit/combo-skips-disabled-1521.test.js": [
      "a combo skips members the operator disabled (#1521) does not resurrect disabled members when the entire combo is disabled",
      "a combo skips members the operator disabled (#1521) drops a disabled member",
      "a combo skips members the operator disabled (#1521) matches the exact provider alias, not a prefix or a superstring",
      "a combo skips members the operator disabled (#1521) never fails a route because the disabled list is unreadable",
      "a combo skips members the operator disabled (#1521) resolves a bare model name before checking its provider policy",
      "a combo skips members the operator disabled (#1521) returns the list untouched when nothing is disabled",
      "a combo skips members the operator disabled (#1521) survives a malformed disabled map"
    ],
    "tests/unit/combo-slash-resolve.test.js": [
      "getComboModelsFromData prefers exact full-name match over basename",
      "getComboModelsFromData resolves a bare combo name",
      "getComboModelsFromData resolves a provider-prefixed combo name (openrouter/lordx.1 -> lordx.1)",
      "getComboModelsFromData returns null when no combo matches"
    ],
    "tests/unit/combo-suggest-1033.test.js": [
      "model tiers come from published price, not a guess (#1091) calls an unpriced model unpriced rather than free",
      "model tiers come from published price, not a guess (#1091) carries the capabilities the resolver already knows",
      "model tiers come from published price, not a guess (#1091) puts an expensive model in the top tier and a cheap one in the budget tier",
      "model tiers come from published price, not a guess (#1091) splits a model id at the first slash only",
      "the suggested chain spreads across providers (#1033) breaks a tier tie on the larger context window",
      "the suggested chain spreads across providers (#1033) does not put two models of one provider next to each other",
      "the suggested chain spreads across providers (#1033) is deterministic, so the same connections suggest the same chain",
      "the suggested chain spreads across providers (#1033) prefers the capable tiers before the cheap one",
      "the suggested chain spreads across providers (#1033) puts an unpriced model after a priced one rather than first",
      "the suggested chain spreads across providers (#1033) respects the limit and never repeats a model",
      "the suggested chain spreads across providers (#1033) returns what it has when fewer models exist than asked for"
    ],
    "tests/unit/combo-test-peeks-3404.test.js": [
      "combo test routes do not consume a rotation turn (#3404) src/app/api/combos/[id]/test/route.js peeks instead of rotating",
      "combo test routes do not consume a rotation turn (#3404) src/app/api/combos/test/route.js peeks instead of rotating"
    ],
    "tests/unit/combo-test-runner.test.js": [
      "Combo Test Runner API executes fallback sequence when model 1 fails and model 2 succeeds",
      "Combo Test Runner API handles ad-hoc unsaved combo testing via /api/combos/test",
      "Combo Test Runner API returns 404 if combo ID is not found and no inline models provided",
      "Combo Test Runner API tests all models in full diagnostic mode (mode='all')"
    ],
    "tests/unit/combo-test-step-isolation-1874.test.js": [
      "one failing combo member does not 500 the whole test (#1874) ad-hoc: a throwing ping becomes a failed step",
      "one failing combo member does not 500 the whole test (#1874) ad-hoc: self-calls the port the request arrived on",
      "one failing combo member does not 500 the whole test (#1874) ad-hoc: the guard is inside the per-model loop, not around it",
      "one failing combo member does not 500 the whole test (#1874) saved: a throwing ping becomes a failed step",
      "one failing combo member does not 500 the whole test (#1874) saved: self-calls the port the request arrived on",
      "one failing combo member does not 500 the whole test (#1874) saved: the guard is inside the per-model loop, not around it",
      "one failing combo member does not 500 the whole test (#1874) the individual test really does take a different path, which is the asymmetry"
    ],
    "tests/unit/combo-token-saver-2289.test.js": [
      "#2289 per-combo token-saver overrides accepts the boolean shorthand the report asks for",
      "#2289 per-combo token-saver overrides falls through to a member combo when the outer one declares nothing",
      "#2289 per-combo token-saver overrides gives the outermost combo in a nested chain the decision",
      "#2289 per-combo token-saver overrides ignores a malformed override rather than throwing",
      "#2289 per-combo token-saver overrides ignores an inherited property, since a combo may legally be named constructor",
      "#2289 per-combo token-saver overrides keeps the strategy fields the same bag already carries working",
      "#2289 per-combo token-saver overrides leaves the globals alone for enabled: true, the way the master gate does",
      "#2289 per-combo token-saver overrides lets a per-saver key outrank the combo's own enabled: false",
      "#2289 per-combo token-saver overrides returns the global flags unchanged when nothing declares an override",
      "#2289 per-combo token-saver overrides turns a single saver on for a combo while the global stays off",
      "#2289 per-combo token-saver overrides turns every saver off for a combo that says enabled: false"
    ],
    "tests/unit/combo-token-saver-wiring-2289.test.js": [
      "a combo's token-saver overrides reach the handler (#2289, #2037) a combo can turn the whole saver off in one flag",
      "a combo's token-saver overrides reach the handler (#2289, #2037) a combo overrides only the flags it names",
      "a combo's token-saver overrides reach the handler (#2289, #2037) a combo that declares nothing behaves exactly as today",
      "a combo's token-saver overrides reach the handler (#2289, #2037) a request outside a combo resolves to the global settings unchanged",
      "a combo's token-saver overrides reach the handler (#2289, #2037) the expensive lazy load follows the resolved flag, not the global one",
      "a combo's token-saver overrides reach the handler (#2289, #2037) the handler resolves them instead of reading the global flags directly",
      "schema distillation is combo-wired (#2289) a combo can override the schema distiller independently",
      "schema distillation is combo-wired (#2289) the combo wiring test's flag list is exactly COMBO_TOKEN_SAVER_KEYS (no drift)"
    ],
    "tests/unit/combo-transient-retry-337.test.js": [
      "combo retries a transient member before advancing (#337) advances immediately when the provider asks for a wait longer than the cap",
      "combo retries a transient member before advancing (#337) advances immediately when the provider states no delay at all",
      "combo retries a transient member before advancing (#337) does not retry a rate limit, which belongs to the account not the member",
      "combo retries a transient member before advancing (#337) falls through to the next member once the retry budget is spent",
      "combo retries a transient member before advancing (#337) never retries a client error that stops the combo outright",
      "combo retries a transient member before advancing (#337) retries the same member after a short transient cooldown",
      "combo retries a transient member before advancing (#337) waits exactly as long as the provider's own Retry-After"
    ],
    "tests/unit/combo-upstream-error-reason-1996.test.js": [
      "a combo member's in-content upstream error keeps its reason (#1996) a real answer is untouched",
      "a combo member's in-content upstream error keeps its reason (#1996) preserves an accepted stream error without replaying generation",
      "a combo member's in-content upstream error keeps its reason (#1996) the peek reports the error instead of counting it as content",
      "a combo member's in-content upstream error keeps its reason (#1996) the status carried out is the upstream's, not a blanket 503"
    ],
    "tests/unit/combos-import-export.test.js": [
      "combos import/export export returns version 2 payload with combos and strategies",
      "combos import/export import merges capacityAdapter over existing keys",
      "combos import/export import rejects invalid payloads",
      "combos import/export import replaces combos and persists strategies"
    ],
    "tests/unit/combos-repo-models.test.js": [
      "combo repository model normalization returns arrays to repository and API consumers for malformed persisted model values"
    ],
    "tests/unit/commandcode-glm52-2334.test.js": [
      "commandcode lists GLM 5.2 (#2334) declares the id in the vendor's own naming",
      "commandcode lists GLM 5.2 (#2334) inherits GLM capabilities from the pattern table, not a hand-written entry",
      "commandcode lists GLM 5.2 (#2334) keeps the existing catalogue and its default model"
    ],
    "tests/unit/commandcode-provider-api-1528.test.js": [
      "#1528 commandcode uses the documented Provider API posts to /provider/v1/chat/completions, never the CLI's /alpha/generate",
      "#1528 commandcode uses the documented Provider API sends no CLI-impersonation headers",
      "#1528 commandcode uses the documented Provider API speaks OpenAI chat completions, so no commandcode-format hop remains",
      "#1528 commandcode uses the documented Provider API still authenticates with the Bearer key and still honours ZDR"
    ],
    "tests/unit/commandcode-to-openai.test.js": [
      "commandcode-to-openai — error event does not open with a role when content already went out",
      "commandcode-to-openai — error event opens with the assistant role when the error is the first event",
      "commandcode-to-openai — error event sends the role exactly once when text follows the error",
      "commandcode-to-openai — error event stringifies object errors so client sees readable message",
      "commandcode-to-openai — final tool-call event does NOT re-emit tool_calls when tool-input-* deltas already fired",
      "commandcode-to-openai — final tool-call event emits a consolidated tool_calls when only the final tool-call event arrives",
      "commandcode-to-openai — finish emits a final chunk with finish_reason=tool_calls when finishReason is tool-calls",
      "commandcode-to-openai — finish includes usage on the final chunk when totalUsage provided",
      "commandcode-to-openai — reasoning-delta maps reasoning-delta to reasoning_content delta",
      "commandcode-to-openai — state pre-populated by initState (Responses API client) advances the tool index for a second distinct tool call",
      "commandcode-to-openai — state pre-populated by initState (Responses API client) does not crash on the first tool-call event (original crash repro)",
      "commandcode-to-openai — state pre-populated by initState (Responses API client) is idempotent — re-feeding the same tool-call event is a no-op",
      "commandcode-to-openai — state pre-populated by initState (Responses API client) still handles a text-delta under pre-init state",
      "commandcode-to-openai — text-delta emits assistant role on first delta then content-only",
      "commandcode-to-openai — tool-input-* with id field (live schema) ignores tool-input-delta when id is unknown (no prior start)",
      "commandcode-to-openai — tool-input-* with id field (live schema) registers tool index using event.id (NOT toolCallId)"
    ],
    "tests/unit/commandcode-zdr-connection.test.js": [
      "Command Code ZDR connection persistence persists the strict boolean policy at creation and updates it without replacing other connection data"
    ],
    "tests/unit/commandcode-zdr-validation.test.js": [
      "Command Code API-key validation keeps a ZDR-unavailable model distinct from an invalid API key",
      "Command Code API-key validation uses the connection ZDR policy for the validation request"
    ],
    "tests/unit/commandcode-zdr.test.js": [
      "Command Code ZDR upstream header adds x-cmd-zdr only for a connection that explicitly enables ZDR"
    ],
    "tests/unit/compact-response-headers.test.js": [
      "live combo response headers marks an exhausted combo without claiming a failed value is a served model",
      "live combo response headers tracks a streamed response and preserves the replayed body",
      "live combo response headers tracks the model that returns a terminal client error"
    ],
    "tests/unit/compatibility-evidence.test.js": [
      "a newer or identical run is not a prior regression baseline",
      "a repeated failed outcome does not create a fresh regression",
      "cancelled is never regression evidence",
      "contradictory succeeded/failed outcomes remain unknown",
      "different exact fixtureVersion is not comparable",
      "different exact model is not comparable",
      "different exact provider is not comparable",
      "different exact scenario is not comparable",
      "different fixtureHash cannot manufacture regression",
      "different scope cannot manufacture regression",
      "implementation revisions form separate capability rows but comparable baselines",
      "interrupted is never regression evidence",
      "invalid retained identity remains unknown",
      "invalid retained identity remains unknown",
      "invalid retained identity remains unknown",
      "invalid retained identity remains unknown",
      "malformed retained checks cannot throw or establish support",
      "malformed retained checks cannot throw or establish support",
      "malformed retained checks cannot throw or establish support",
      "malformed retained checks cannot throw or establish support",
      "malformed retained checks cannot throw or establish support",
      "missing and unknown check outcomes remain unknown rather than matrix support",
      "only comparable completed exact evidence produces a linked passed-to-failed regression",
      "queued is never regression evidence",
      "result scope disagreement cannot establish regression",
      "running is never regression evidence",
      "timed-out is never regression evidence",
      "unrecognized scopes cannot establish capability support"
    ],
    "tests/unit/compatibility-model.test.js": [
      "maintained translator evidence a malformed source envelope cannot become a passed check",
      "maintained translator evidence actually converts ordered stream chunks with final flush and completion",
      "maintained translator evidence actually converts tool request, retains exact source, and distinguishes schema limits",
      "maintained translator evidence reports registered direct/pivot/passthrough and missing edges truthfully",
      "retained local input boundary bounds stream event count and nesting",
      "retained local input boundary refuses credential-like text and complete byte overflow without truncation",
      "retained local input boundary refuses nested sensitive key __proto__",
      "retained local input boundary refuses nested sensitive key apiKey",
      "retained local input boundary refuses nested sensitive key authorization",
      "retained local input boundary refuses nested sensitive key refresh_token",
      "retained local input boundary requires explicit suitable provenance and rejects unsupported scopes"
    ],
    "tests/unit/compatibility-schema.test.js": [
      "ties retained compatibility runs to an existing immutable fixture revision"
    ],
    "tests/unit/compatibility-scopes-migration.test.js": [
      "compatibility scope migration on better-sqlite3 preserves all historical runs, backup, indexes and triggers and retains controlled scopes after reopen",
      "compatibility scope migration on better-sqlite3 rolls back a failure after copying rows and can retry on the same adapter",
      "compatibility scope migration on sql.js preserves all historical runs, backup, indexes and triggers and retains controlled scopes after reopen",
      "compatibility scope migration on sql.js rolls back a failure after copying rows and can retry on the same adapter"
    ],
    "tests/unit/compatibility-ui.test.js": [
      "a disconnected submission reports unknown acceptance and never replays the run",
      "archives an exact fixture revision with confirmation and readback without running it",
      "cancel is offered on a live run, calls the cancel route, and a cancelled receipt loses it",
      "missing retained measurements remain unknown and do not crash the result inspector",
      "mounts the fixture book, queue contract and empty inspector from retained state",
      "queue refusal surfaces as an explicit notice and schedules nothing",
      "renders honest terminal failures: deadline, missing edge and result_unretainable",
      "submits a pinned revision, renders the queued run identity, then the terminal receipt",
      "unknown identity fields render unknown, never a guessed value"
    ],
    "tests/unit/compatibility-worker.test.js": [
      "bounded local worker a new process interrupts the previous owner's pending runs without replaying them",
      "bounded local worker a result over the retained byte cap becomes a failed terminal receipt, not a stuck run",
      "bounded local worker admits one active plus four queued and refuses the sixth without scheduling",
      "bounded local worker cancels a queued run before any worker starts and kills a running one at cancel",
      "bounded local worker kills a worker at the deadline with an explicit timed-out terminal state and pumps the queue",
      "bounded local worker worker error and premature exit both land on failed, never silent",
      "honest missing-edge failure an unsupported route refuses execution instead of passing the payload through",
      "owner-scoped persistence refuses cross-owner reads and terminal transitions without any write",
      "owner-scoped persistence terminal states are immutable receipts"
    ],
    "tests/unit/compatible-key-validate-994.test.js": [
      "adding a hand-added OpenAI-compatible endpoint accepts a working key when the gateway serves no /models",
      "adding a hand-added OpenAI-compatible endpoint keeps the single-call path when the listing works",
      "adding a hand-added OpenAI-compatible endpoint still fails when the chat endpoint is missing too, so a wrong base URL is caught",
      "adding a hand-added OpenAI-compatible endpoint treats 401 from the listing as definitive and does not probe further",
      "testing a saved connection to that endpoint reports the connection healthy when only the listing is missing",
      "testing a saved connection to that endpoint still reports a rejected key as an error"
    ],
    "tests/unit/compatible-node-thinking-format-2752.test.js": [
      "an openai-compatible node gets OpenAI thinking fields (#2752) a registry provider with its own declared format still wins",
      "an openai-compatible node gets OpenAI thinking fields (#2752) an anthropic-compatible node is not affected",
      "an openai-compatible node gets OpenAI thinking fields (#2752) its level set is OpenAI's too",
      "an openai-compatible node gets OpenAI thinking fields (#2752) no qwen-native fields are emitted for a qwen-looking model",
      "an openai-compatible node gets OpenAI thinking fields (#2752) the direct qwen provider still gets its native fields",
      "an openai-compatible node gets OpenAI thinking fields (#2752) the OpenAI field is emitted instead"
    ],
    "tests/unit/compatible-provider-connections.test.js": [
      "compatible provider connections API allows multiple connections on the same compatible node",
      "compatible provider connections API copies multi-protocol transports into the connection",
      "compatible provider connections API creates one API-key connection for an Anthropic-compatible node",
      "compatible provider connections API creates one API-key connection for an OpenAI-compatible node"
    ],
    "tests/unit/completion-identity-migration.test.js": [
      "schema 20 to 21 exact completion binding backs up before additive migration and preserves legacy identity as unavailable"
    ],
    "tests/unit/completion-identity-pipeline.test.js": [
      "server completion binding through the real final usage writer binds exactly one completion despite colliding client header prefixes",
      "server completion binding through the real final usage writer does not bind partial usage or turn legacy request IDs into completion evidence",
      "server completion binding through the real final usage writer keeps a later completion unavailable when its ledger write fails and the old rid row survives"
    ],
    "tests/unit/configuration/configuration-domains-api.test.js": [
      "authenticates before any domain read and never accepts an inference credential",
      "refuses secret fields, oversized input, wrong scope, and unsupported paths without copying request data",
      "runs real domain snapshot/draft/activation/selection/rollback and rejects stale writes"
    ],
    "tests/unit/configuration/configuration-domains-persistence.test.js": [
      "flush failure before staging persistence leaves routing unchanged",
      "native readers never see partially replaced stores and an intervening connection defeats expectedCurrent",
      "post-commit flush failure reports partial and preserves recoverable memory plus durable staged receipt",
      "propagates POSIX directory fsync failure after atomic rename without discarding memory",
      "strict sql.js flush is immediately reloadable with private permissions and no pending timer"
    ],
    "tests/unit/configuration/configuration-domains.test.js": [
      "a patch touching no covered surface records no history",
      "allowlists all domains without serializing secret URLs, headers, account identity or private shaping text",
      "an explicit providerSpecificData replacement holding no covered key still records the removed allowlist",
      "disabled-model cache refresh and explicit empty account inheritance survive activation and rollback",
      "history insertion failure aborts the ordinary policy mutation and preserves the prior version",
      "invalid selection paths, prototype segments, missing entities and stale hashes are refused",
      "ordinary account, provider, disabled-model, pool and saver writes share same-transaction history",
      "pool restoration rotates bound credential revision while retaining current transport secrets",
      "selectively restores exact changed fields and leaves current credentials, URLs and unrelated policies intact",
      "shaping restoration requires current explicit consent and preserves private configuration"
    ],
    "tests/unit/configuration/versioned-routing-api.test.js": [
      "bounds bytes before parsing and never reflects a secret-bearing malformed payload",
      "does not expose raw database failure messages",
      "executes actual authenticated create/revise/validate/activate/conflict/rollback/read/audit",
      "has no mutable history endpoint",
      "refuses auth class false/false loopback true for GET  before config access",
      "refuses auth class false/false loopback true for POST drafts before config access",
      "refuses auth class false/true loopback true for GET versions before config access",
      "refuses auth class false/true loopback true for POST drafts before config access",
      "refuses auth class true/false loopback false for POST drafts before config access",
      "refuses malformed fields before state access",
      "refuses malformed fields before state access",
      "refuses malformed fields before state access"
    ],
    "tests/unit/configuration/versioned-routing-persistence.test.js": [
      "flush failure before staging persistence leaves routing unchanged",
      "native readers never see partially replaced stores and an intervening connection defeats expectedCurrent",
      "post-commit flush failure reports partial and preserves recoverable memory plus durable staged receipt",
      "propagates POSIX directory fsync failure after atomic rename without discarding memory",
      "strict sql.js flush is immediately reloadable with private permissions and no pending timer"
    ],
    "tests/unit/configuration/versioned-routing.test.js": [
      "independent validation and redaction boundaries hashes semantic object order canonically but preserves member ordering",
      "independent validation and redaction boundaries refuses excluded strategy credential before retaining a draft",
      "independent validation and redaction boundaries refuses member endpoint before retaining a draft",
      "independent validation and redaction boundaries refuses missing optimistic tokens and unpublished rollback sources",
      "independent validation and redaction boundaries refuses nested alias credential before retaining a draft",
      "independent validation and redaction boundaries refuses prototype key before retaining a draft",
      "independent validation and redaction boundaries refuses top-level secret before retaining a draft",
      "independent validation and redaction boundaries stores an editable alias cycle draft but refuses activation",
      "independent validation and redaction boundaries stores an editable cycle through qualified combo basename draft but refuses activation",
      "independent validation and redaction boundaries stores an editable empty combo draft but refuses activation",
      "independent validation and redaction boundaries stores an editable missing connection draft but refuses activation",
      "independent validation and redaction boundaries stores an editable missing member draft but refuses activation",
      "real stored draft lifecycle appends immutable revisions and refuses stale concurrent draft editors",
      "real stored draft lifecycle fails the original direct write if its audit insert cannot commit",
      "real stored draft lifecycle keeps unrelated settings, secrets and excluded shaping overrides through activation and rollback",
      "real stored draft lifecycle permits only one of two concurrent activations from the same prior hash",
      "real stored draft lifecycle records delete operations and excludes provider-only changes from hash and history",
      "real stored draft lifecycle rejects a stale activation after an existing writer changes one covered setting",
      "real stored draft lifecycle reports partial after committed config if rotation invalidation fails, without replay",
      "real stored draft lifecycle rolls back all store writes when a later write fails and records a generic failure",
      "real stored draft lifecycle saves, diffs, validates, activates, reads gateway stores and rolls back as a new version",
      "real stored draft lifecycle uses actual defaults when a covered property is omitted and preserves exclusions on a removed plan",
      "redacts malformed legacy URLs in keys and values, and refuses to activate their projection",
      "refuses corrupted settings before overwriting outside-scope state",
      "reports committed state when final audit persistence fails instead of inviting replay",
      "validates nested membership and rejects an account from a different provider",
      "versioned existing routing writes records effective combo and alias changes without copying unrelated credentials"
    ],
    "tests/unit/connect-timeout-config.test.js": [
      "connect timeout resolution accepts bounded integer 1000",
      "connect timeout resolution accepts bounded integer 120000",
      "connect timeout resolution accepts bounded integer 15000",
      "connect timeout resolution rejects invalid literal -Infinity",
      "connect timeout resolution rejects invalid literal 120001",
      "connect timeout resolution rejects invalid literal 15000",
      "connect timeout resolution rejects invalid literal 15000.5",
      "connect timeout resolution rejects invalid literal 999",
      "connect timeout resolution rejects invalid literal Infinity",
      "connect timeout resolution rejects invalid literal NaN",
      "connect timeout resolution rejects invalid literal null",
      "connect timeout resolution rejects invalid literal true",
      "connect timeout resolution skips invalid imported candidates",
      "connect timeout resolution uses provider, registry, global, then env precedence"
    ],
    "tests/unit/connection-apikey-collapse-917.test.js": [
      "adding a second API key under the default name (#917) does not overwrite the first account's credential",
      "adding a second API key under the default name (#917) keeps the second key as its own connection",
      "adding a second API key under the default name (#917) keys the match on the provider, not on the name alone",
      "adding a second API key under the default name (#917) names the two apart so the list can distinguish them",
      "adding a second API key under the default name (#917) still updates in place when the same key is saved again",
      "adding a second API key under the default name (#917) treats a credential-less compatible endpoint as one connection, not many"
    ],
    "tests/unit/connection-default-model-474.test.js": [
      "connection default model routing (PR #474) does not call an upstream core for all-rate-limited chat credentials",
      "connection default model routing (PR #474) does not call an upstream core for all-rate-limited embeddings credentials",
      "connection default model routing (PR #474) does not call an upstream core without eligible chat credentials",
      "connection default model routing (PR #474) does not call an upstream core without eligible embeddings credentials",
      "connection default model routing (PR #474) does not override an explicit chat model",
      "connection default model routing (PR #474) does not override an explicit embeddings model",
      "connection default model routing (PR #474) exposes the selected connection default model to the request handlers",
      "connection default model routing (PR #474) locks the physical model that failed after resolving a bare default",
      "connection default model routing (PR #474) sends the selected connection default for a bare chat alias",
      "connection default model routing (PR #474) sends the selected connection default for a bare embeddings alias"
    ],
    "tests/unit/connection-endpoint-override-3253.test.js": [
      "per-connection endpoint override (#3253) appends the provider's operation path to a base",
      "per-connection endpoint override (#3253) does not double the operation path when the full endpoint is given",
      "per-connection endpoint override (#3253) ignores a trailing slash on the stored value",
      "per-connection endpoint override (#3253) keeps the registry URL when no override is stored",
      "per-connection endpoint override (#3253) redirects the Anthropic wire to its own operation path"
    ],
    "tests/unit/connection-reorder-determinism-1181.test.js": [
      "renumbering connections is deterministic (#1181) a missing timestamp does not make the comparator unstable",
      "renumbering connections is deterministic (#1181) priority still wins over everything",
      "renumbering connections is deterministic (#1181) recency is still preferred when the timestamps actually differ",
      "renumbering connections is deterministic (#1181) the result does not depend on the order the rows came back from the DB",
      "renumbering connections is deterministic (#1181) two rows sharing a priority and a timestamp always land the same way",
      "the repo uses that comparator reorderInTx breaks a full tie on the id",
      "the repo uses that comparator the id tie-break comes after the recency one, not instead of it"
    ],
    "tests/unit/connection-status.test.js": [
      "filterActiveConnections excludes explicitly disabled connections",
      "filterActiveConnections returns an empty list for invalid input"
    ],
    "tests/unit/connection-test-rotated-token-1444.test.js": [
      "a connection test never strands a rotated token (#1444) carries the rotation on the failure path as well as the success path",
      "a connection test never strands a rotated token (#1444) keeps the genuine no-refresh path reporting an invalid token",
      "a connection test never strands a rotated token (#1444) persists a rotation regardless of whether the test passed",
      "a connection test never strands a rotated token (#1444) reports the retry's own error rather than blaming the renewed token",
      "the OAuth probe is bounded (#1449) gives the initial probe and the post-refresh retry a timeout signal",
      "the OAuth probe is bounded (#1449) uses the constant the rest of the file already uses"
    ],
    "tests/unit/connection-validation-feedback.test.js": [
      "allows fractional quota thresholds through native form validation and preserves their exact value",
      "clears only the selected provider ceiling through the supported field patch",
      "edits the union of exact snapshot keys and saved thresholds without losing configured-only windows",
      "keeps missing canonical validation unknown despite an old legacy success field",
      "renders a check result separately from generation and independent capacity limits",
      "renders a selected pool ahead of a retained direct marker and restores direct after clearing it",
      "shows unknown quota windows without fabricating a 5h input and keeps priority editable"
    ],
    "tests/unit/connections-auth-capabilities.test.js": [
      "connection authentication choices does not invent a credential for 'coqui'",
      "connection authentication choices does not invent a credential for 'ddgs'",
      "connection authentication choices does not invent a credential for 'edge-tts'",
      "connection authentication choices does not invent a credential for 'firecrawl_custom'",
      "connection authentication choices does not invent a credential for 'google-tts'",
      "connection authentication choices does not invent a credential for 'local-device'",
      "connection authentication choices does not invent a credential for 'mimo-free'",
      "connection authentication choices does not invent a credential for 'opencode'",
      "connection authentication choices does not invent a credential for 'searxng'",
      "connection authentication choices does not invent a credential for 'tortoise'",
      "connection authentication choices exposes the registered OAuth mechanism for 'antigravity'",
      "connection authentication choices exposes the registered OAuth mechanism for 'claude'",
      "connection authentication choices exposes the registered OAuth mechanism for 'codebuddy-cn'",
      "connection authentication choices exposes the registered OAuth mechanism for 'codebuddy-intl'",
      "connection authentication choices exposes the registered OAuth mechanism for 'codex'",
      "connection authentication choices exposes the registered OAuth mechanism for 'cursor'",
      "connection authentication choices exposes the registered OAuth mechanism for 'devin'",
      "connection authentication choices exposes the registered OAuth mechanism for 'gemini-cli'",
      "connection authentication choices exposes the registered OAuth mechanism for 'github'",
      "connection authentication choices exposes the registered OAuth mechanism for 'gitlab'",
      "connection authentication choices exposes the registered OAuth mechanism for 'grok-cli'",
      "connection authentication choices exposes the registered OAuth mechanism for 'iflow'",
      "connection authentication choices exposes the registered OAuth mechanism for 'kilocode'",
      "connection authentication choices exposes the registered OAuth mechanism for 'kimchi'",
      "connection authentication choices exposes the registered OAuth mechanism for 'kimi'",
      "connection authentication choices exposes the registered OAuth mechanism for 'kiro'",
      "connection authentication choices exposes the registered OAuth mechanism for 'qoder'",
      "connection authentication choices exposes the registered OAuth mechanism for 'xai'",
      "connection authentication choices exposes the registered OAuth mechanism for 'zed'",
      "connection authentication choices preserves declared modes for 'api-airforce'",
      "connection authentication choices preserves declared modes for 'baidu'",
      "connection authentication choices preserves declared modes for 'bazaarlink'",
      "connection authentication choices preserves declared modes for 'bluesminds'",
      "connection authentication choices preserves declared modes for 'cloudflare-ai'",
      "connection authentication choices preserves declared modes for 'codebuddy-cn'",
      "connection authentication choices preserves declared modes for 'codebuddy-intl'",
      "connection authentication choices preserves declared modes for 'devin'",
      "connection authentication choices preserves declared modes for 'gemini'",
      "connection authentication choices preserves declared modes for 'gitlawb-opengateway'",
      "connection authentication choices preserves declared modes for 'grok-cli'",
      "connection authentication choices preserves declared modes for 'kilo-gateway'",
      "connection authentication choices preserves declared modes for 'kimchi'",
      "connection authentication choices preserves declared modes for 'kimi'",
      "connection authentication choices preserves declared modes for 'llm7'",
      "connection authentication choices preserves declared modes for 'modelscope'",
      "connection authentication choices preserves declared modes for 'morph'",
      "connection authentication choices preserves declared modes for 'nous'",
      "connection authentication choices preserves declared modes for 'nvidia'",
      "connection authentication choices preserves declared modes for 'ollama-search'",
      "connection authentication choices preserves declared modes for 'ollama'",
      "connection authentication choices preserves declared modes for 'openrouter'",
      "connection authentication choices preserves declared modes for 'poolside'",
      "connection authentication choices preserves declared modes for 'qoder'",
      "connection authentication choices preserves declared modes for 'sambanova'",
      "connection authentication choices preserves declared modes for 'sensenova'",
      "connection authentication choices preserves declared modes for 'sumopod'",
      "connection authentication choices preserves declared modes for 'tencent'",
      "connection authentication choices preserves declared modes for 'x5lab'",
      "connection authentication choices preserves declared modes for 'xai'"
    ],
    "tests/unit/connections-investigation.test.js": [
      "does not present disabled or unqualified accounts as observed unhealthy",
      "keeps selected evidence visible outside current account filters",
      "keeps unknown health separate from recorded unhealthy accounts",
      "opens the retained exact account and creates analysis links with the same interval, model and comparison",
      "reserves no empty Network inspector when no account is selected",
      "selects into the shared workspace and filters current inventory only by the supported account dimensions",
      "uses the retained account for the Network inspector without inventing observed reachability"
    ],
    "tests/unit/connections-parity-ui.test.js": [
      "creates a dynamic node account with exact provider identity and verifies the created row",
      "does not report an accepted account option write as verified when readback differs",
      "keeps registry OAuth-only providers out of the API-key creation branch",
      "keeps the nonsecret import draft on refusal and clears its credential",
      "passes an explicit model through the client configuration check without invoking inference",
      "refuses Kiro callbacks from a different sign-in before extracting an authorization code",
      "requires an explicit proxy test target before making any request",
      "restores a named release record with its concurrency version and verifies the exact stored record",
      "saves edited SAML mappings while preserving an untouched certificate and nonsecret defaults",
      "updates only the selected provider timeout and verifies its field without overwriting strategy siblings"
    ],
    "tests/unit/connections-repo-maintenance.test.js": [
      "bulk delete and explicit reorder breaks a full priority+updatedAt tie deterministically by id",
      "bulk delete and explicit reorder deletes every row of a provider and reports the count",
      "bulk delete and explicit reorder reorderProviderConnections renumbers priorities from 1",
      "cleanupProviderConnections strips explicit nulls and empty providerSpecificData, counting each removal",
      "degradation cause taxonomy classifies each persisted failure into its stable cause class",
      "github-derived connection names prefers githubLogin, then githubEmail, then email, then githubName",
      "mergeProviderConnectionData merges providerSpecificData key by key and keeps unnamed fields",
      "mergeProviderConnectionData returns null for an unknown id",
      "mergeProviderConnectionData treats a non-object patch and a missing name as no-ops on those fields",
      "oauth dedup workspace tie-breaks keeps a workspace-bearing login separate from a bare-email row",
      "oauth dedup workspace tie-breaks keeps username-bearing and username-less identities apart, matches equal usernames",
      "oauth dedup workspace tie-breaks matches workspace rows on chatgptAccountId when both sides carry one",
      "updateConnectionProxyPoolSnapshotIfBound coerces a non-true strictProxy to false",
      "updateConnectionProxyPoolSnapshotIfBound refuses a row with no providerSpecificData and an unknown id",
      "updateConnectionProxyPoolSnapshotIfBound writes the pair only while the expected pool still owns the row"
    ],
    "tests/unit/connections-row-cache-pf1.test.js": [
      "connections row decrypt cache (P-F1) a foreign direct-DB write busts the cache too",
      "connections row decrypt cache (P-F1) a repo write path busts the cache",
      "connections row decrypt cache (P-F1) sees a delete immediately (no resurrected cached row)",
      "connections row decrypt cache (P-F1) serves repeated reads from the cache"
    ],
    "tests/unit/context-analytics-worker.test.js": [
      "bounded analytics admission coalesces identical reads and rejects excess queued work",
      "bounded analytics admission removes cancelled queued jobs without cancelling a shared active reader",
      "bounded analytics admission terminates a timed-out worker before restarting and closes pending work",
      "read-only Context analytics accepts projections only and never caller SQL, paths or unbounded filters",
      "read-only Context analytics aggregates an entire long session into bounded buckets independent of turn pages",
      "read-only Context analytics holds a consistent WAL snapshot while the writer continues and rejects mutation",
      "read-only Context analytics invalidates the same completed projection on committed WAL writes",
      "read-only Context analytics partitions adjacent time scopes without duplicating boundary attempts",
      "read-only Context analytics reads fresh sql.js snapshots after atomic replacement without writing them",
      "read-only Context analytics reports filtered unattributed coverage without manufacturing sessions",
      "read-only Context analytics requires unambiguous valid timestamps and mutually exclusive end bounds",
      "read-only Context analytics returns a controlled failure and recovers for the next valid query",
      "read-only Context analytics returns identical filtered totals and explicit projection/freshness"
    ],
    "tests/unit/context-budget-calibration.test.js": [
      "context budget calibration a request that fits uncalibrated can be over budget once calibrated",
      "context budget calibration factor 1 by default and bounded to [0.5, 4]",
      "context budget calibration scales the projected size and the character deficit together"
    ],
    "tests/unit/context-budget.test.js": [
      "contextBudget: the estimator is honest about its own units assumes fewer characters per token than prose, because this is code",
      "contextBudget: the window is a budget, not a suggestion aims BELOW the trigger so one prune does not re-fire next turn",
      "contextBudget: the window is a budget, not a suggestion charges a media block flat instead of by its encoded length",
      "contextBudget: the window is a budget, not a suggestion counts instructions and tools, not only the conversation",
      "contextBudget: the window is a budget, not a suggestion falls back to the engine default rather than assuming a megatoken",
      "contextBudget: the window is a budget, not a suggestion holds back a reserve and never spends the whole window",
      "contextBudget: the window is a budget, not a suggestion lets the operator override the window and the reserve",
      "contextBudget: the window is a budget, not a suggestion never lets a small window produce a reserve one reply would overrun",
      "contextBudget: the window is a budget, not a suggestion refuses a reserve fraction that would prune a conversation that fits",
      "count_tokens: a client decides when to compact from this number charges an image nested inside a tool_result flat too",
      "count_tokens: a client decides when to compact from this number does not report a screenshot as a third of a megatoken window",
      "count_tokens: a client decides when to compact from this number still counts text, system and tools the way it always did",
      "toolPruner: nothing happens while the request fits keeps the flat path for a caller that never measured",
      "toolPruner: nothing happens while the request fits leaves a whole session untouched inside the budget",
      "toolPruner: pressure is progressive and stops when it is enough reports what it could not find instead of eating the protected turns",
      "toolPruner: pressure is progressive and stops when it is enough stops as soon as the overflow is covered rather than cutting everything",
      "toolPruner: pressure is progressive and stops when it is enough trims the OLDEST turns and leaves the recent working set whole",
      "toolPruner: pressure is progressive and stops when it is enough uses only the gentlest tier it needs, and escalates when it must"
    ],
    "tests/unit/context-compactor-2187.test.js": [
      "Context Compactor: claude targets, coercion, and marker preservation claude target: summary+notice ship as one labeled user note, never role system",
      "Context Compactor: claude targets, coercion, and marker preservation coerces NaN threshold and non-positive recentTurnsToKeep instead of misfiring",
      "Context Compactor: claude targets, coercion, and marker preservation keeps the rtk elide marker visible in the summary and never fabricates the word output",
      "Context Compactor: claude targets, coercion, and marker preservation mid-conversation system messages are preserved verbatim, not silently dropped",
      "Context Compactor: summary is system-scoped, not a fabricated dialogue (#2187) carries the summary in a system-scoped block",
      "Context Compactor: summary is system-scoped, not a fabricated dialogue (#2187) keeps the pre-existing shape the rest of the pipeline relies on (system head, 2-block summary, recent tail)",
      "Context Compactor: summary is system-scoped, not a fabricated dialogue (#2187) never injects a synthetic user or assistant turn to carry the summary"
    ],
    "tests/unit/context-evidence-export.test.js": [
      "Context evidence export applies request-time bounds and dimensions to the entire population, including comparison accounts",
      "Context evidence export exports complete populations beyond the page size and preserves ordered stages",
      "Context evidence export exports exact owned relations without raw content and keeps explicit reports separate from inference",
      "Context evidence export exports two exact attempt/session tuples independently of account comparison and shared filters",
      "Context evidence export refuses related event overflow without returning a partial artifact",
      "Context evidence export requires exact attempt and session selection and labels selected scope semantics",
      "Context evidence export withholds malformed or mismatched boundaries and preserves historical absence"
    ],
    "tests/unit/context-evidence-pipeline.test.js": [
      "actual request capture, persistence and readonly query anonymous and malformed client reports never produce inferred client identities",
      "actual request capture, persistence and readonly query disabled measurements preserve authenticated identity without serializing or inventing structures",
      "actual request capture, persistence and readonly query exports the actual captured attempt and its authenticated client report through the protected read worker",
      "actual request capture, persistence and readonly query isolates rejected structural metrics from authoritative partial usage",
      "actual request capture, persistence and readonly query keeps client and gateway evidence with each retry but never carries forward old dispatch evidence",
      "actual request capture, persistence and readonly query keeps physical refusal identity and missing usage honest",
      "actual request capture, persistence and readonly query keeps stored structure private on corrupted rows and survives reader restarts",
      "actual request capture, persistence and readonly query links exact physical UUIDs, content-free boundaries and explicit identity through the worker",
      "actual request capture, persistence and readonly query restores a saved exact comparison and exports both actual attempts and reports in one worker snapshot",
      "actual request capture, persistence and readonly query reuses a matching prepared body across boundaries without reserializing it",
      "explicit client-reported event boundary accepts unpaid outcome reporting after the authenticated key exhausts its inference budget",
      "explicit client-reported event boundary bounds actual request bytes independently of declared content length",
      "explicit client-reported event boundary filters all retained events before pagination, preserves unknown routing and authorizes admin reads",
      "explicit client-reported event boundary persists linked events, exact duplicates and signed client estimates without claiming verification",
      "explicit client-reported event boundary prunes expired event evidence through the indexed observation time",
      "explicit client-reported event boundary records explicit handoff targets and task outcomes with independent provenance",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"afterTokens\":1.5}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"beforeTokens\":-1}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"occurredAt\":\"2026-01-01\"}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"prompt\":\"private\"}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"sessionId\":1}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"targetClientId\":\"target\"}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"tokenMeasurementMethod\":null}",
      "explicit client-reported event boundary rejects unsupported or ambiguous evidence {\"type\":\"prefix_change\"}",
      "explicit client-reported event boundary requires a live API key even on loopback and does not leak foreign request links"
    ],
    "tests/unit/context-evidence-ui.test.js": [
      "Content-free Context evidence views compares all ordered stages and signed quantities without deriving cash savings",
      "Content-free Context evidence views distinguishes baseline loading, failure and missing exact identity without substitution",
      "Content-free Context evidence views distinguishes empty reports from a failed read",
      "Content-free Context evidence views exposes the exact completion identity before navigation to Economics",
      "Content-free Context evidence views paginates exact linked reports independently of scope and separates observations from reports",
      "Content-free Context evidence views preserves unknown cost versus zero and identifies the exact ledger denominator",
      "Content-free Context evidence views shows measured boundaries, role and overlapping subset denominators without raw prompts",
      "Content-free Context evidence views shows unavailable transport or historical boundaries without fabricated zero counts"
    ],
    "tests/unit/context-investigations-controls.test.js": [
      "Context investigation controls compares explicit UTC periods without changing shared selection or treating absent usage as zero",
      "Context investigation controls keeps the exact session when routing continues and reports a refused page without replacing evidence",
      "Context investigation controls pages and searches server project options while retaining an off-page selected label",
      "distinguishes recorded shaping failures and cancellation from historical byte measurements",
      "Quota schedule evidence marks retained scheduling data as stale when a refresh fails and never displays cancelled eligibility",
      "Quota schedule evidence opens exact schedule history with a fixed end, paginates and retains failure feedback"
    ],
    "tests/unit/context-investigations-population.test.js": [
      "Context complete investigation projections applies an exact durable project filter and refuses mismatched attempt links",
      "Context complete investigation projections compares whole half-open periods with common dimensions, sample denominators and unknowns",
      "Context complete investigation projections continues beyond 100 tied switch receipts without duplication when newer rows arrive",
      "Context complete investigation projections continues stored pins and refuses malformed or wrong-kind cursors",
      "Context complete investigation projections rejects incomplete periods and admits new projections through the worker validator",
      "Context complete investigation projections round-trips explicit interval definitions in version 5 without changing old definitions",
      "Context complete investigation projections searches all project labels before pagination and retains an off-page selection",
      "Context complete investigation projections uses literal project substrings and complete time/client scope rather than SQL wildcards"
    ],
    "tests/unit/context-length-exposure-1089.test.js": [
      "#1089 context length is exposed per model resolves a model-specific contextWindow that overrides the default",
      "#1089 context length is exposed per model resolves a positive contextWindow for a model with no explicit override"
    ],
    "tests/unit/context-persistence-delta.test.js": [
      "better-sqlite3 checks a delayed pending write against the committed terminal state",
      "better-sqlite3 does not cache a rolled-back optional ledger or suppress its later valid restoration",
      "better-sqlite3 keeps each changed intermediate boundary, shrinking stage order and growing structural evidence",
      "better-sqlite3 keeps native and sql.js persisted snapshots after repeated terminal updates and reload",
      "better-sqlite3 persists and exports every effective context control, including false values",
      "better-sqlite3 persists changed risk and same-boundary fingerprints even when byte counts match",
      "better-sqlite3 repeated identical snapshots cause zero durable changes and keep every signed byte",
      "better-sqlite3 retains first pricing attribution and request identity when later metadata changes",
      "better-sqlite3 updates final usage and time without rewriting an unchanged ordered stage or HMAC ledger",
      "node:sqlite checks a delayed pending write against the committed terminal state",
      "node:sqlite does not cache a rolled-back optional ledger or suppress its later valid restoration",
      "node:sqlite keeps each changed intermediate boundary, shrinking stage order and growing structural evidence",
      "node:sqlite keeps native and sql.js persisted snapshots after repeated terminal updates and reload",
      "node:sqlite persists and exports every effective context control, including false values",
      "node:sqlite persists changed risk and same-boundary fingerprints even when byte counts match",
      "node:sqlite repeated identical snapshots cause zero durable changes and keep every signed byte",
      "node:sqlite retains first pricing attribution and request identity when later metadata changes",
      "node:sqlite updates final usage and time without rewriting an unchanged ordered stage or HMAC ledger",
      "sql.js checks a delayed pending write against the committed terminal state",
      "sql.js does not cache a rolled-back optional ledger or suppress its later valid restoration",
      "sql.js keeps each changed intermediate boundary, shrinking stage order and growing structural evidence",
      "sql.js keeps native and sql.js persisted snapshots after repeated terminal updates and reload",
      "sql.js persists and exports every effective context control, including false values",
      "sql.js persists changed risk and same-boundary fingerprints even when byte counts match",
      "sql.js repeated identical snapshots cause zero durable changes and keep every signed byte",
      "sql.js retains first pricing attribution and request identity when later metadata changes",
      "sql.js updates final usage and time without rewriting an unchanged ordered stage or HMAC ledger"
    ],
    "tests/unit/context-status-read-errors.test.js": [
      "exposes a corrupt operator read without changing best-effort callers for {\"entries\":null}",
      "exposes a corrupt operator read without changing best-effort callers for {broken",
      "propagates an I/O failure to operators while retaining best-effort request reads",
      "serves a missing telemetry file as an empty successful observation",
      "serves valid retained estimates and observed tokens through the actual strict route"
    ],
    "tests/unit/context-status-route.test.js": [
      "context-status route caps entries at 100, keeping the newest",
      "context-status route keeps observed input distinct from estimates for -1",
      "context-status route keeps observed input distinct from estimates for 0",
      "context-status route keeps observed input distinct from estimates for 18000",
      "context-status route keeps observed input distinct from estimates for 18000",
      "context-status route keeps observed input distinct from estimates for Infinity",
      "context-status route keeps observed input distinct from estimates for null",
      "context-status route keeps observed input distinct from estimates for undefined",
      "context-status route null-sanitizes garbage fields and caps strings at 64 chars",
      "context-status route replies 503 when the store read throws",
      "context-status route returns 200 with an empty entries array for an empty store",
      "context-status route returns entries newest-first with an ISO generatedAt"
    ],
    "tests/unit/context-status-store.test.js": [
      "contextStatusStore a dollarsSaved-only write merges over the rid-stamped completion row",
      "contextStatusStore corrupt file recovers to empty without throwing, then accepts writes",
      "contextStatusStore drops updatedAt that is not ISO-shaped and parseable",
      "contextStatusStore entries with wrong shape are dropped on read",
      "contextStatusStore LRU eviction drops the oldest entry at the 512 cap",
      "contextStatusStore omitted fields stay absent; saveBytes of exactly 0 is kept",
      "contextStatusStore rejects malformed sids and never touches disk",
      "contextStatusStore retries once on a rename ENOENT from an interleaved writer, keeping the update",
      "contextStatusStore truncated JSON mid-array also recovers empty",
      "contextStatusStore uses per-process unique tmp names for concurrent writers",
      "contextStatusStore write/read roundtrip preserves allowlisted fields",
      "contextStatusStore writes atomically with mode 0600 and no leftover tmp file"
    ],
    "tests/unit/context-structure-equivalence.test.js": [
      "preserves every count and HMAC byte from the version-1 pre-optimization oracle"
    ],
    "tests/unit/context-structure.test.js": [
      "content-free structural evidence captures compatible tool transactions from each JSON protocol",
      "content-free structural evidence captures compatible tool transactions from each JSON protocol",
      "content-free structural evidence captures compatible tool transactions from each JSON protocol",
      "content-free structural evidence does not classify schema examples and tool-argument data as attachments",
      "content-free structural evidence keeps history-prefix fingerprints stable when only the latest user message changes",
      "content-free structural evidence keeps the versioned role DTO aligned with protocol roles",
      "content-free structural evidence partitions UTF-8 JSON bytes exactly and preserves the caller",
      "content-free structural evidence retains explicit zeros for an observed empty body and refuses unbounded capture",
      "content-free structural evidence reuses an internal serialized body and handles JSON-omitted optional fields",
      "content-free structural evidence separates overlapping tool and attachment subsets without double-counting nested results",
      "content-free structural evidence streams multi-chunk bodies to the same digest as whole-string encoding",
      "content-free structural evidence strips unknown fields at persistence and rejects contradictory boundaries"
    ],
    "tests/unit/context-suffix-strip-2531.test.js": [
      "the [1m] context suffix never reaches an upstream (#2531) leaves an ordinary id alone",
      "the [1m] context suffix never reaches an upstream (#2531) matches case-insensitively and only at the end",
      "the [1m] context suffix never reaches an upstream (#2531) never returns an empty id",
      "the [1m] context suffix never reaches an upstream (#2531) runs in chat.js before, and outside, the compat-gated block",
      "the [1m] context suffix never reaches an upstream (#2531) still recognises a wrapped model for the gated rewrite",
      "the [1m] context suffix never reaches an upstream (#2531) strips the suffix",
      "the [1m] context suffix never reaches an upstream (#2531) survives a non-string"
    ],
    "tests/unit/context-telemetry-api.test.js": [
      "context operator API boundary rejects anonymous and inference keys without disclosing or changing context",
      "context operator API boundary requires a verified loopback peer for mutation, returns no-store and hides hashes",
      "context operator API boundary returns explicit summary projection and overload without leaking worker errors",
      "context operator API boundary runs project, interval and routing projections through the real bounded worker",
      "context operator API boundary validates bounds, absent sessions and unknown mutation fields"
    ],
    "tests/unit/context-telemetry-persistence.test.js": [
      "private persisted context lifecycle joins pins only by full session hash and excludes private identity or prompt data",
      "private persisted context lifecycle never guesses a conversation from shared connection or missing session",
      "private persisted context lifecycle normalizes cache-exclusive Claude input without double-counting",
      "private persisted context lifecycle prunes bounded history and stage/session orphans",
      "private persisted context lifecycle records partial failures once, keeps estimates out of provider totals, and distinguishes missing cache",
      "private persisted context lifecycle rejects broken stage order and malformed filters",
      "private persisted context lifecycle retains billed usage when optional context metrics are invalid",
      "private persisted context lifecycle scopes filtering, pagination, models, accounts and retries to identical rows",
      "private persisted context lifecycle survives close/reopen and upgrades an old schema without inventing history",
      "private persisted context lifecycle updates one row, rejects a late pending placeholder, and reconciles stage bytes"
    ],
    "tests/unit/context-telemetry-pipeline.test.js": [
      "drives a real SSE stream through completion without a second placeholder row",
      "persists partial stream usage once across abandonment and late completion",
      "persists the actual nonstream provider usage at the shared attempt id",
      "records a malformed accepted body as failure and forbids replay",
      "records effective epoch and cache controls as explicit booleans",
      "records transport failures without fabricated usage and forbids uncertain replay",
      "records usage-less completed streams as estimates rather than observed counts",
      "scopes cache-prefix measurements by session, provider, model and account"
    ],
    "tests/unit/context-telemetry-usage-provenance.test.js": [
      "context usage provenance distinguishes explicit identity from content-inferred locality",
      "context usage provenance keeps absent output and cache fields absent across partial provider responses",
      "context usage provenance never adds a cache-inclusive input count twice to calibration",
      "context usage provenance retains estimate provenance and preserves observed zero cache"
    ],
    "tests/unit/context-workspace.test.js": [
      "Context projection contracts clips focused buckets to the shared half-open range",
      "Context projection contracts includes dates in track ticks when a session spans days",
      "Context projection contracts maps every shared filter and exclusive end without timezone conversion",
      "Context projection contracts retains null measurements and never stacks cache onto inclusive input",
      "Context projection contracts uses the retained bucket extent for a single-bucket session without inventing a multi-day domain",
      "Context workspace drops previous evidence immediately when the shared scope changes",
      "Context workspace exposes a failed read without presenting fabricated zero coverage",
      "Context workspace focuses the full-session trend through shared scope, not the visible attempt page",
      "Context workspace keeps pagination available for an empty page of a nonempty cohort",
      "Context workspace keeps provider usage on failures and labels prefix changes as observations",
      "Context workspace keeps the initial session when a shared filter changes the cohort order",
      "Context workspace keeps the selected attempt trigger mounted and restores its focus after inspection",
      "Context workspace labels retained exact-attempt evidence after a failed refresh and retries only that identity",
      "Context workspace leaves the identity unselected until an operator chooses a session",
      "Context workspace paginates attempts on the server without replacing a still-returned selected identity",
      "Context workspace preserves the investigated session and request when a focused interval excludes them",
      "Context workspace refreshes both projections only after a successful project label save",
      "Context workspace renders isolated SQLite writer and read-worker projections without inventing unavailable fields",
      "Context workspace renders real v9 rows, preserves unknown cache versus observed zero, and opens all 14 ordered stages",
      "Context workspace renders the cohort as compact cards grouped by identity at the Everyday level",
      "Context workspace renders the same cohort as a dense row table at the Advanced level",
      "Context workspace retains an explicitly chosen comparison baseline after another attempt and scope are selected",
      "Context workspace retains the project-label draft when an acknowledged write cannot be verified",
      "Context workspace returns the cohort to page one when a shared project scope changes",
      "Context workspace saves an operator label with a PATCH and exposes an authorization refusal",
      "Context workspace shows historical coverage without synthesizing sessions or requesting their details",
      "Context workspace shows recorded controls and routing receipt scope without claiming active pins",
      "Context workspace shows requested and served model evidence together",
      "Context workspace uses client navigation for recorded controls without resetting the shared scope",
      "exposes all attempt measurements directly without an inner disclosure",
      "retains the direct history-policy draft across task and shared-scope changes",
      "shows retention semantics for 45 without inventing a finite window",
      "shows retention semantics for null without inventing a finite window",
      "shows retention semantics for undefined without inventing a finite window"
    ],
    "tests/unit/continuity-strip.test.js": [
      "stripContinuityFields (outbound boundary) end-to-end: Responses multi-turn translation stripped before dispatch",
      "stripContinuityFields (outbound boundary) is a no-op for bodies without a messages array",
      "stripContinuityFields (outbound boundary) null/undefined guard with provider args",
      "stripContinuityFields (outbound boundary) removes continuity blobs from assistant messages",
      "stripContinuityFields (outbound boundary) skips adaptive strip when cache empty",
      "stripContinuityFields (outbound boundary) strips adaptive rejected fields via provider/model blocklist"
    ],
    "tests/unit/control-feedback.test.js": [
      "Access uses the configuration check verdict and states its scope does not diagnose a public default password from an absent stored hash (environment)",
      "Access uses the configuration check verdict and states its scope does not diagnose a public default password from an absent stored hash (null)",
      "Access uses the configuration check verdict and states its scope does not diagnose a public default password from an absent stored hash (undefined)",
      "Access uses the configuration check verdict and states its scope does not infer acceptance when client-secret validity is unknown (tested=false)",
      "Access uses the configuration check verdict and states its scope does not infer acceptance when client-secret validity is unknown (tested=true)",
      "Access uses the configuration check verdict and states its scope does not show success for incomplete or malformed application evidence {\"ok\":\"true\",\"discoveryOk\":true}",
      "Access uses the configuration check verdict and states its scope does not show success for incomplete or malformed application evidence {\"ok\":true}",
      "Access uses the configuration check verdict and states its scope does not show success for incomplete or malformed application evidence {}",
      "Access uses the configuration check verdict and states its scope does not show success for incomplete or malformed application evidence null",
      "Access uses the configuration check verdict and states its scope identifies SAML success as a local format check without claiming provider acceptance",
      "Access uses the configuration check verdict and states its scope limits a positive OIDC result to discovery and the credential check, without claiming sign-in",
      "Access uses the configuration check verdict and states its scope renders HTTP 200 with ok=false as a rejection and preserves the provider reason",
      "Access uses the configuration check verdict and states its scope still warns when the effective password source is the built-in default",
      "System version read failures ends all loading placeholders after HTTP 500, shows the reason, and retries only the version read",
      "System version read failures ends loading and exposes a retry after an interrupted version read"
    ],
    "tests/unit/copilot-catalog-cache-contracts.test.js": [
      "catalog cache cache key is per credential",
      "catalog cache forceRefresh bypasses a warm cache",
      "catalog cache serves a second call from cache within the TTL, expires after it",
      "catalog filtering empty catalog resolves null and is not cached",
      "catalog filtering keeps only enabled chat models, dedupes ids, requires an id",
      "catalog filtering non-array payload resolves null",
      "credential handling 401 refreshes the Copilot token, persists it, and retries once",
      "credential handling 401 with no GitHub accessToken cannot refresh and resolves null",
      "credential handling no token at all skips the live fetch",
      "credential handling non-auth failure resolves null without a refresh attempt",
      "credential handling persist-callback failure does not sink the retry",
      "credential handling refresh without a token resolves null",
      "credential handling retry failure after a successful refresh resolves null"
    ],
    "tests/unit/copilot-catalog-limits-2756.test.js": [
      "Copilot live catalog carries its own limits (#2756) does not advertise the direct-Claude window for a Copilot-hosted model",
      "Copilot live catalog carries its own limits (#2756) exposes the published context window and output ceiling",
      "Copilot live catalog carries its own limits (#2756) falls back to the static table when Copilot publishes no limits",
      "Copilot live catalog carries its own limits (#2756) keeps id and name, and still filters non-chat and disabled entries"
    ],
    "tests/unit/copilot-live-models-1596.test.js": [
      "Copilot claude models come from the live catalog (#1596) discovers the rest live, which is how newer models arrive",
      "Copilot claude models come from the live catalog (#1596) records why the static list is deliberately incomplete",
      "Copilot claude models come from the live catalog (#1596) resolves the static list by alias, not by id",
      "Copilot claude models come from the live catalog (#1596) still ships the claude models it does list statically"
    ],
    "tests/unit/copilot-model-not-supported-3477.test.js": [
      "a model-scoped rejection does not cost the account (#3477) a rate limit still backs off rather than passing",
      "a model-scoped rejection does not cost the account (#3477) a real credential failure still locks, which is the line this must not cross",
      "a model-scoped rejection does not cost the account (#3477) and the prose form",
      "a model-scoped rejection does not cost the account (#3477) the bare code does too, so a body without the type field is still safe",
      "a model-scoped rejection does not cost the account (#3477) the exact Copilot body does not lock",
      "a model-scoped rejection does not cost the account (#3477) the neighbouring model-scoped codes still pass, unchanged"
    ],
    "tests/unit/copilot-proxy-host-1240.test.js": [
      "both Copilot hosts are redirected (#1240) cli/hooks/cleanupMitmHosts.js registers the proxy host beside the api host",
      "both Copilot hosts are redirected (#1240) every file that knows one host knows both, so cleanup matches setup",
      "both Copilot hosts are redirected (#1240) maps the proxy host to the copilot tool, not just to the host list",
      "both Copilot hosts are redirected (#1240) open-sse/utils/proxyFetch.js registers the proxy host beside the api host",
      "both Copilot hosts are redirected (#1240) src/mitm/config.js registers the proxy host beside the api host",
      "both Copilot hosts are redirected (#1240) src/shared/constants/mitmToolHosts.js registers the proxy host beside the api host"
    ],
    "tests/unit/cost-ledger-pipeline.test.js": [
      "cost ledger pipeline estimated usage (provider omitted it) writes no ledger row",
      "cost ledger pipeline ledger failure never blocks usage persistence",
      "cost ledger pipeline savers off: baseline equals actual, savedUsd is zero",
      "cost ledger pipeline savers on: baseline exceeds actual, savedUsd is positive and lands in the saver component",
      "cost ledger pipeline streaming path: usage with cache fields lands one ledger row with the saver/cache split",
      "cost ledger pipeline the current request's saver dollars land on its own context-status entry (no one-request lag)"
    ],
    "tests/unit/cost-ledger-repo.test.js": [
      "computeCostLedgerEntry attributes a shrunk body to the saver component, not the cache one",
      "computeCostLedgerEntry canonicalizes raw provider usage spellings before costing",
      "computeCostLedgerEntry costs the baseline as fully uncached and the actual with cache rates",
      "computeCostLedgerEntry refuses inconsistent or invalid canonical cache accounting 0",
      "computeCostLedgerEntry refuses inconsistent or invalid canonical cache accounting 1",
      "computeCostLedgerEntry refuses inconsistent or invalid canonical cache accounting 2",
      "computeCostLedgerEntry refuses inconsistent or invalid canonical cache accounting 3",
      "computeCostLedgerEntry reports saver growth honestly when savers grew the body",
      "computeCostLedgerEntry skips a provider-reported zero-zero usage as unmeasurable",
      "computeCostLedgerEntry skips estimated usage (not provider-reported)",
      "computeCostLedgerEntry skips missing rid/model/body/usage",
      "computeCostLedgerEntry skips unknown models (no rate card), never guessing",
      "costLedger schema registration retains attribution and completion bindings from schema version 21",
      "estimateBaselineTokens tokenizes at 4 chars/token, rounded up",
      "recordCostLedger + sumSavedUsdSince persists, upserts on rid conflict, and rolls up per sid with the saver/cache split",
      "recordCostLedger + sumSavedUsdSince returns null for bad inputs instead of a fake zero",
      "recordCostLedgerForRequest writes one row end-to-end and skips unpriceable requests"
    ],
    "tests/unit/cost-locale-2976.test.js": [
      "cost is formatted for the reader's locale (#2976) converts and relabels only when a rate is supplied",
      "cost is formatted for the reader's locale (#2976) disambiguates the dollar for a locale that has its own",
      "cost is formatted for the reader's locale (#2976) falls back to USD for a locale whose currency the tag does not pin",
      "cost is formatted for the reader's locale (#2976) ignores a rate that cannot be a rate",
      "cost is formatted for the reader's locale (#2976) keeps 4 decimals for a per-call cost, the way the chart needs",
      "cost is formatted for the reader's locale (#2976) keeps the plain dollar for English",
      "cost is formatted for the reader's locale (#2976) never converts the base currency against itself",
      "cost is formatted for the reader's locale (#2976) renders something for a corrupt locale cookie instead of throwing",
      "cost is formatted for the reader's locale (#2976) treats a missing or unusable amount as zero, as every call site did",
      "cost is formatted for the reader's locale (#2976) uses the reader's own grouping and decimal marks"
    ],
    "tests/unit/count-tokens-boundaries.test.js": [
      "count endpoint provenance and admission counts verified model text locally but labels provider framing unverified",
      "count endpoint provenance and admission does not tokenize encoded media and exposes its unbounded heuristic error",
      "count endpoint provenance and admission labels Claude and unknown models as estimates with no verified tokenizer",
      "count endpoint provenance and admission preserves preflight headers",
      "count endpoint provenance and admission rejects deeply nested content before recursion overflows",
      "count endpoint provenance and admission rejects oversized JSON and text, malformed JSON, arrays and null",
      "count endpoint provenance and admission releases stalled body admissions on cancellation and permits a later request"
    ],
    "tests/unit/count-tokens-overload.test.js": [
      "count endpoint body chunk overhead accepts a body trickled one byte at a time without holding one Buffer per chunk",
      "count endpoint body chunk overhead reassembles chunk boundaries that split multi-byte characters",
      "count endpoint body chunk overhead still refuses a trickled body past 4 MiB and one past the tokenizer text bound",
      "tokenizer worker cancellation and replacement under load a worker that exceeds its heap limit fails the job without taking the process down",
      "tokenizer worker cancellation and replacement under load aborting active work replaces the worker and later queued and new requests still complete",
      "tokenizer worker cancellation and replacement under load keeps resident memory bounded across repeated maximum-size counts"
    ],
    "tests/unit/count-tokens.test.js": [
      "Anthropic count_tokens estimator counts system prompts and tool definitions",
      "Anthropic count_tokens estimator counts tool and thinking content blocks that carry context",
      "Anthropic count_tokens estimator preserves the existing plain text estimate"
    ],
    "tests/unit/counterfactual-evidence.test.js": [
      "counterfactual evidence boundary does not accept an overflowing decomposition as reconciled",
      "counterfactual evidence boundary keeps historical zero/zero decomposition unknown even when the total is zero",
      "counterfactual evidence boundary refuses unsupported attribution 0",
      "counterfactual evidence boundary refuses unsupported attribution 1",
      "counterfactual evidence boundary refuses unsupported attribution 10",
      "counterfactual evidence boundary refuses unsupported attribution 11",
      "counterfactual evidence boundary refuses unsupported attribution 12",
      "counterfactual evidence boundary refuses unsupported attribution 13",
      "counterfactual evidence boundary refuses unsupported attribution 14",
      "counterfactual evidence boundary refuses unsupported attribution 2",
      "counterfactual evidence boundary refuses unsupported attribution 3",
      "counterfactual evidence boundary refuses unsupported attribution 4",
      "counterfactual evidence boundary refuses unsupported attribution 5",
      "counterfactual evidence boundary refuses unsupported attribution 6",
      "counterfactual evidence boundary refuses unsupported attribution 7",
      "counterfactual evidence boundary refuses unsupported attribution 8",
      "counterfactual evidence boundary refuses unsupported attribution 9",
      "counterfactual evidence boundary retains signed modeled differences without claiming realized savings",
      "exact indexed completion binding batches exports within SQLite parameter bounds without scanning historical rows",
      "exact indexed completion binding does not query history for legacy rid/request-id links or expose freeform identity",
      "exact indexed completion binding enforces unique bindings and refuses duplicates from an imported database without its index",
      "exact indexed completion binding uses indexed equality and grouped counts for only the selected UUIDs"
    ],
    "tests/unit/cowork-lians-memory.test.js": [
      "Lians Cowork memory plugin declares the published Lians 0.5.0 MCP tool surface",
      "Lians Cowork memory plugin does not duplicate a local plugin name or tool name",
      "Lians Cowork memory plugin launches the published local MCP server through uvx"
    ],
    "tests/unit/crash-report-visibility-1891.test.js": [
      "a crash says what happened even when stderr is congested (#1891, #1814) console.error loses the message here, which is the bug being fixed",
      "a crash says what happened even when stderr is congested (#1891, #1814) does not report the exit twice when a crash already named it",
      "a crash says what happened even when stderr is congested (#1891, #1814) names an uncaught exception",
      "a crash says what happened even when stderr is congested (#1891, #1814) names an unhandled rejection",
      "a crash says what happened even when stderr is congested (#1891, #1814) reports a non-zero exit that nothing else accounted for",
      "a crash says what happened even when stderr is congested (#1891, #1814) stays quiet on a clean exit",
      "one client hanging up does not take the gateway down (#1814) still dies on a real fault, so nothing is being swallowed wholesale",
      "one client hanging up does not take the gateway down (#1814) survives an uncaught \"aborted\", which is how Node words the same event",
      "one client hanging up does not take the gateway down (#1814) survives an uncaught ECONNABORTED",
      "one client hanging up does not take the gateway down (#1814) survives an uncaught ECONNRESET",
      "one client hanging up does not take the gateway down (#1814) survives an uncaught EPIPE",
      "one client hanging up does not take the gateway down (#1814) survives an uncaught ERR_STREAM_PREMATURE_CLOSE"
    ],
    "tests/unit/cred-refresh-decisions.test.js": [
      "CRED.chain-diverged does not fire when the held fp differs from the issued fp",
      "CRED.chain-diverged fires once per held fp and lists capped peers",
      "CRED.no-refresh-path emits which=token when the refresh token is missing",
      "CRED.no-refresh-path emits which=url when the provider has no refresh URL",
      "CRED.refresh-failed discriminates invalid_client and http",
      "CRED.refresh-failed emits conn, prov, status, why, fp0, age with the closed discriminator",
      "CRED.refresh-failed emits why=network when the fetch itself throws",
      "CRED.refresh-failed folds repeats exponentially and never folds across a why change",
      "CRED.rotated emits only when the refresh token fingerprint changes",
      "CRED.rotated stays silent when the provider returns the same token",
      "issue-record age rendering renders minutes, hours, and days compactly",
      "issue-record age rendering uses the persisted firstSeen for age on failure"
    ],
    "tests/unit/credential-metadata-isolation-3170.test.js": [
      "PR #3170 credential metadata isolation does not cache a resolved Vertex project ID on frozen direct-executor credentials",
      "PR #3170 credential metadata isolation keeps a refresh retry local while reporting rotated credentials",
      "PR #3170 credential metadata isolation keeps frozen shared credentials isolated across independent chat requests"
    ],
    "tests/unit/crof-provider-config.test.js": [
      "Crof AI provider configuration registers Crof as an API-key OpenAI-compatible passthrough provider"
    ],
    "tests/unit/cumulative-tool-args-2869.test.js": [
      "cumulative tool-argument streaming (#2869) collapses an exact restatement of a complete argument string",
      "cumulative tool-argument streaming (#2869) replaces the buffer when a chunk restates it, instead of doubling",
      "cumulative tool-argument streaming (#2869) still appends genuine incremental fragments"
    ],
    "tests/unit/cursor-agent-exec-request.test.js": [
      "CursorExecutor AgentService exec_request handling acknowledges a request-context exec request without ending the turn",
      "CursorExecutor AgentService exec_request handling does not render an unsupported exec request as assistant content",
      "CursorExecutor AgentService exec_request handling drops frames batched behind an unsupported exec request in the same read",
      "CursorExecutor AgentService exec_request handling returns a non-200 error body for an unsupported exec request when not streaming"
    ],
    "tests/unit/cursor-agent-mode-tools-1008.test.js": [
      "Cursor agent mode triggers on the request, not the client name (#1008) also triggers when the request carries tools",
      "Cursor agent mode triggers on the request, not the client name (#1008) can only widen, never narrow",
      "Cursor agent mode triggers on the request, not the client name (#1008) reads tools from the body it is about to send",
      "Cursor agent mode triggers on the request, not the client name (#1008) reproduces the trigger's contract",
      "Cursor agent mode triggers on the request, not the client name (#1008) still honours the Claude Code user agents"
    ],
    "tests/unit/cursor-agent-proto.test.js": [
      "Cursor AgentService codec (cursorProtobuf.js) encodeMcpTools produces empty bytes for no tools",
      "Cursor AgentService codec (cursorProtobuf.js) encodeMcpTools wraps multiple tool defs as repeated field 1",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes array of mixed",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes bool false",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes bool true",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes deeply nested",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes empty object",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes flat object",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes float",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes integer",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes nested object",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes null",
      "Cursor AgentService codec (cursorProtobuf.js) google.protobuf.Value round-trip encodes/decodes string",
      "Cursor AgentService codec (cursorProtobuf.js) McpArgs decode decodes name, toolName, toolCallId, and typed args map",
      "Cursor AgentService codec (cursorProtobuf.js) McpArgs decode handles empty args map",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult error / toolNotFound builds error result (field 2)",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult error / toolNotFound builds toolNotFound result (field 5)",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult image content builds image item with raw bytes + mime type",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult image content builds mixed text + image content",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult success builds success with multiple text items",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult success builds success with single text content",
      "Cursor AgentService codec (cursorProtobuf.js) McpResult success marks is_error=true",
      "Cursor AgentService codec (cursorProtobuf.js) McpToolDefinition accepts flat tool shape (no .function wrapper)",
      "Cursor AgentService codec (cursorProtobuf.js) McpToolDefinition encodes name, description, input_schema (Value), provider, tool_name",
      "Cursor AgentService codec (cursorProtobuf.js) McpToolDefinition preserves nested JSON-schema types",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes a text-only run request with system + model",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes conversation_history from prior turns including tool calls/results",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes mcp_tools (field 4) when tools are provided",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for case-insensitive Fable prefix",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for final Fable fast",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for GPT fast",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for Grok fast",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for non-final Fable fast",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame encodes RequestedModel model_id only for Opus fast",
      "Cursor AgentService executor helpers (cursor.js) buildAgentRunFrame omits mcp_tools when no tools provided",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest accepts array text content",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest accepts history with assistant tool_calls + tool results",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest accepts plain text content",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest accepts request with tools declared",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest rejects missing messages",
      "Cursor AgentService executor helpers (cursor.js) isAgentCapableRequest rejects non-text (image) content"
    ],
    "tests/unit/cursor-composer-thinking.test.js": [
      "CursorExecutor Composer thinking-field responses does not expose opaque AgentService terminal reasoning in JSON",
      "CursorExecutor Composer thinking-field responses does not expose opaque AgentService terminal reasoning in SSE",
      "CursorExecutor Composer thinking-field responses does not treat thinking as visible output for non-Composer models",
      "CursorExecutor Composer thinking-field responses streams only visible content after </think> for Composer responses",
      "CursorExecutor Composer thinking-field responses uses visible content after </think> for non-streaming Composer responses"
    ],
    "tests/unit/cursor-connect-timeout.test.js": [
      "Cursor response-header deadlines a 120000 override cannot extend Cursor's existing 60000 ceiling",
      "Cursor response-header deadlines agent HTTP/2 rejects a pre-aborted caller before opening a client",
      "Cursor response-header deadlines AgentService cleans up an adapter connect failure",
      "Cursor response-header deadlines AgentService cleans up an adapter frame failure",
      "Cursor response-header deadlines AgentService cleans up an adapter request failure",
      "Cursor response-header deadlines AgentService cleans up an adapter write failure",
      "Cursor response-header deadlines AgentService HTTP/2 rejects with the provider response-header timeout",
      "Cursor response-header deadlines AgentService preserves caller abort after headers",
      "Cursor response-header deadlines AgentService preserves caller abort while a non-200 body is pending",
      "Cursor response-header deadlines AgentService removes its full-stream abort listener once on caller abort",
      "Cursor response-header deadlines AgentService removes its full-stream abort listener once on normal end",
      "Cursor response-header deadlines AgentService removes its full-stream abort listener once on protocol error",
      "Cursor response-header deadlines an unrelated HTTP/2 protocol error retains the public 500 response",
      "Cursor response-header deadlines closes the AgentService SessionLease exactly once on caller abort",
      "Cursor response-header deadlines closes the AgentService SessionLease exactly once on normal end",
      "Cursor response-header deadlines closes the AgentService SessionLease exactly once on request error",
      "Cursor response-header deadlines executeAgent removes its caller forwarding listener on normal completion",
      "Cursor response-header deadlines fetch mode clears its deadline before reading the body",
      "Cursor response-header deadlines fetch mode preserves the exact caller abort",
      "Cursor response-header deadlines fetch mode rejects with the provider response-header timeout",
      "Cursor response-header deadlines forwards execute proxy options as the resolved route and composed deadline signal to the AgentService adapter",
      "Cursor response-header deadlines rejects a direct AgentService setup with the response-header deadline",
      "Cursor response-header deadlines rejects a strict proxy AgentService setup with the response-header deadline",
      "Cursor response-header deadlines rejects an AgentService request close before headers and closes its lease",
      "Cursor response-header deadlines rejects an AgentService request end before headers and closes its lease",
      "Cursor response-header deadlines standard HTTP/2 cleans up a synchronous connect failure",
      "Cursor response-header deadlines standard HTTP/2 cleans up a synchronous end failure",
      "Cursor response-header deadlines standard HTTP/2 cleans up a synchronous request failure",
      "Cursor response-header deadlines standard HTTP/2 cleans up a synchronous write failure",
      "Cursor response-header deadlines standard HTTP/2 clears only the header deadline and keeps the original whole-request ceiling",
      "Cursor response-header deadlines standard HTTP/2 completes before the legacy ceiling with no timers",
      "Cursor response-header deadlines standard HTTP/2 preserves caller abort before and after headers",
      "Cursor response-header deadlines standard HTTP/2 rejects a pre-aborted caller before opening a client",
      "Cursor response-header deadlines standard HTTP/2 rejects with the provider response-header timeout"
    ],
    "tests/unit/cursor-default-composer-1077.test.js": [
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) prefers real text content over the thinking fallback",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) returns the visible answer for non-streaming cu/default",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) returns the visible answer for non-streaming default",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) still hides thinking for a named non-Composer model",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) streams the visible answer for cu/default",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) streams the visible answer for default",
      "Cursor Auto (Server Picks) decodes a Composer reply (#1077) surfaces nothing when the server pick sends plain reasoning"
    ],
    "tests/unit/cursor-dispatch-hooks.test.js": [
      "denies fetch admission before any transport and keeps binary encoding honest",
      "denies http2 admission before any transport and keeps binary encoding honest",
      "does not invent a physical boundary around opaque AgentService work",
      "execute preserves legacy dispatch refusal and does not turn it into upstream500",
      "fetch observes headers before consuming binary response and cancels on hook failure",
      "HTTP2 accounting rejection closes the stream and remains the original exception",
      "HTTP2 observes actual header event and waits for accounting before returning buffered output",
      "keeps opaque service/subprocess coverage explicitly unsupported for class CursorExecutor extends BaseExecutor {\n  constructor({ connectHttp2 = __vite_ssr_import_13__.connectHttp2 } = {}) {\n    super(\"cursor\", __vite_ssr_import_4__.PROVIDERS.cursor);\n    this.connectHttp2 = connectHttp2;\n  }\n\n  buildUrl() {\n    return `${this.config.baseUrl}${this.config.chatPath}`;\n  }\n\n  buildHeaders(credentials) {\n    const accessToken = credentials.accessToken;\n    const machineId = credentials.providerSpecificData?.machineId;\n    const ghostMode = credentials.providerSpecificData?.ghostMode !== false;\n\n    if (!machineId) {\n      throw new Error(\"Machine ID is required for Cursor API\");\n    };\n\n    return (0,__vite_ssr_import_7__.buildCursorHeaders)(accessToken, machineId, ghostMode);\n  }\n\n  transformRequest(model, body, stream, credentials) {\n    // Messages are already translated by chatCore (claude→openai→cursor)\n    // Do NOT call openaiToCursorRequest again — double-translation drops tool_results\n    const messages = body.messages || [];\n    const tools = body.tools || [];\n    const reasoningEffort = body.reasoning_effort || null;\n    // Force Agent mode, or Cursor answers \"Switch to Agent mode to apply all\n    // changes\" and the flow stops.\n    //\n    // This was a Claude Code user-agent allowlist (issue #643), which meant every\n    // other agentic client hit the same wall and needed its own UA added —\n    // opencode reported exactly that (#1008). The client's NAME was only ever a\n    // proxy for the real signal: a request carrying tools is agentic by\n    // definition. Both are checked, so this can only widen and no client that\n    // worked before stops working.\n    const ua = credentials?.rawHeaders?.[\"user-agent\"] || \"\";\n    const agenticUA = ua.includes(\"claude-cli\") || ua.includes(\"claude-code\") || ua.includes(\"Claude Code\");\n    const forceAgentMode = agenticUA || tools.length > 0;\n    return (0,__vite_ssr_import_6__.generateCursorBody)(messages, model, tools, reasoningEffort, forceAgentMode);\n  }\n\n  async makeFetchRequest(url, headers, body, signal, proxyOptions = null, connectTimeout = null, dispatchHooks = {}) {\n    if (dispatchHooks.beforeDispatch) await dispatchHooks.beforeDispatch({ body: null, serialized: body, url, structuralEncoding: \"protobuf\", byteLength: body.byteLength });\n    signal?.throwIfAborted?.();\n    const deadline = (0,__vite_ssr_import_14__.createExecutorResponseHeaderTimeout)({\n      connectTimeout,\n      registryTimeout: this.config?.timeoutMs,\n      envTimeout: __vite_ssr_import_5__.FETCH_CONNECT_TIMEOUT_MS,\n      signal,\n    });\n    let response;\n    try {\n      response = await (0,__vite_ssr_import_12__.proxyAwareFetch)(url, {\n        method: \"POST\",\n        headers,\n        body,\n        signal: deadline.signal,\n      }, proxyOptions);\n    } catch (error) {\n      throw deadline.classify(error);\n    } finally {\n      deadline.clear();\n    };\n\n    await (0,__vite_ssr_import_2__.notifyDispatchResponse)(dispatchHooks.afterDispatch, response);\n    return {\n      status: response.status,\n      headers: Object.fromEntries(response.headers.entries()),\n      body: __vite_ssr_import_3__.Buffer.from(await response.arrayBuffer())\n    };\n  }\n\n  async makeHttp2Request(url, headers, body, signal, connectTimeout = null, dispatchHooks = {}) {\n    if (!__vite_ssr_import_17__) {\n      throw new Error(\"http2 module not available\");\n    };\n    if (signal?.aborted) {\n      return Promise.reject(signal.reason || new DOMException(\"Request aborted\", \"AbortError\"));\n    };\n\n    if (dispatchHooks.beforeDispatch) await dispatchHooks.beforeDispatch({ body: null, serialized: body, url, structuralEncoding: \"protobuf\", byteLength: body.byteLength });\n    signal?.throwIfAborted?.();\n\n    const HTTP2_TIMEOUT_MS = 60000; // 60s max — prevent hung sessions\n\n    return new Promise((resolve, reject) => {\n      const urlObj = new URL(url);\n      const deadline = (0,__vite_ssr_import_14__.createExecutorResponseHeaderTimeout)({\n        connectTimeout,\n        registryTimeout: this.config?.timeoutMs,\n        envTimeout: __vite_ssr_import_5__.FETCH_CONNECT_TIMEOUT_MS,\n        signal,\n      });\n      const chunks = [];\n      let responseHeaders = {};\n      let headerNotification = Promise.resolve();\n      let settled = false;\n      let client;\n      let req;\n      let hangTimeout;\n\n      const onHeaderAbort = () => finish(reject)(deadline.classify(deadline.signal.reason));\n      const onCallerAbort = () => finish(reject)(signal.reason || new DOMException(\"Request aborted\", \"AbortError\"));\n\n      // Ensure client is always closed on settle\n      const finish = (fn) => (...args) => {\n        if (settled) return;\n        settled = true;\n        deadline.clear();\n        clearTimeout(hangTimeout);\n        deadline.signal.removeEventListener(\"abort\", onHeaderAbort);\n        signal?.removeEventListener(\"abort\", onCallerAbort);\n        try { req?.destroy(); } catch {};\n        try { client?.close(); } catch {};\n        fn(...args);\n      };\n\n      try {\n        client = __vite_ssr_import_17__.connect(`https://${urlObj.host}`);\n\n        // Hard timeout: close session if server never responds\n        hangTimeout = setTimeout(finish(() => {\n          reject(new Error(\"HTTP/2 request timed out\"));\n        }), HTTP2_TIMEOUT_MS);\n\n        client.on(\"error\", finish(reject));\n\n        req = client.request({\n          \":method\": \"POST\",\n          \":path\": urlObj.pathname,\n          \":authority\": urlObj.host,\n          \":scheme\": \"https\",\n          ...headers\n        });\n\n        deadline.signal.addEventListener(\"abort\", onHeaderAbort, { once: true });\n        signal?.addEventListener(\"abort\", onCallerAbort, { once: true });\n\n        req.on(\"response\", (hdrs) => {\n          responseHeaders = hdrs;\n          deadline.clear();\n          deadline.signal.removeEventListener(\"abort\", onHeaderAbort);\n          if (dispatchHooks.afterDispatch) {\n            req.pause?.();\n            const response = {\n              status: Number(hdrs[\":status\"]),\n              headers: new Headers(Object.entries(hdrs).filter(([key, value]) => !key.startsWith(\":\") && value !== undefined)),\n              body: { cancel: () => req.destroy() },\n            };\n            headerNotification = (0,__vite_ssr_import_2__.notifyDispatchResponse)(dispatchHooks.afterDispatch, response);\n            headerNotification.then(() => { if (!settled) req.resume?.(); }, finish(reject));\n          }\n        });\n        req.on(\"data\", (chunk) => { chunks.push(chunk); });\n        req.on(\"end\", () => {\n          headerNotification.then(finish(() => {\n            resolve({\n              status: responseHeaders[\":status\"],\n              headers: responseHeaders,\n              body: __vite_ssr_import_3__.Buffer.concat(chunks)\n            });\n          }), finish(reject));\n        });\n        req.on(\"error\", finish(reject));\n\n        req.write(body);\n        req.end();\n      } catch (error) {\n        finish(reject)(error);\n      }\n    });\n  }\n\n  /**\n   * AgentService (agent.api5.cursor.sh) is HTTP/2-only. Node's fetch/undici speaks\n   * HTTP/1.1 and fails with HTTPParserError on the h2 preface — use http2 duplex.\n   */\n  async openAgentHttp2Stream(url, headers, signal, proxyOptions = null, connectTimeout = null) {\n    if (signal?.aborted) {\n      throw signal.reason || new DOMException(\"Request aborted\", \"AbortError\");\n    };\n\n    const urlObj = new URL(url);\n    const deadline = (0,__vite_ssr_import_14__.createExecutorResponseHeaderTimeout)({\n      connectTimeout,\n      registryTimeout: this.config?.timeoutMs,\n      envTimeout: __vite_ssr_import_5__.FETCH_CONNECT_TIMEOUT_MS,\n      signal,\n    });\n    const route = (0,__vite_ssr_import_12__.resolveEffectiveProxyRoute)(url, proxyOptions || {});\n    const chunkQueue = [];\n    let waiting = null;\n    let ended = false;\n    let streamError = null;\n    let lease = null;\n    let client = null;\n    let req = null;\n    let closed = false;\n    let headersSettled = false;\n    let resolveHeaders;\n    let rejectHeaders;\n    let onHeaderAbort;\n    let onCallerAbort;\n\n    const wake = (result) => {\n      if (!waiting) return;\n      const resolve = waiting;\n      waiting = null;\n      resolve(result);\n    };\n\n    const close = () => {\n      if (closed) return;\n      closed = true;\n      deadline.clear();\n      if (onHeaderAbort) deadline.signal.removeEventListener(\"abort\", onHeaderAbort);\n      if (onCallerAbort) signal?.removeEventListener(\"abort\", onCallerAbort);\n      try { req?.destroy(); } catch {};\n      try { lease?.close(); } catch {}\n    };\n\n    const fail = (error) => {\n      if (streamError) return;\n      streamError = error;\n      ended = true;\n      if (!headersSettled) {\n        headersSettled = true;\n        rejectHeaders(error);\n      };\n      wake(null);\n      close();\n    };\n\n    onHeaderAbort = () => fail(deadline.classify(deadline.signal.reason));\n    onCallerAbort = () => fail(signal.reason || new DOMException(\"Request aborted\", \"AbortError\"));\n\n    const responseHeaders = new Promise((resolve, reject) => {\n      resolveHeaders = resolve;\n      rejectHeaders = reject;\n    });\n\n    try {\n      lease = await this.connectHttp2(url, { route, signal: deadline.signal });\n      if (!lease?.session || typeof lease.close !== \"function\") {\n        throw new Error(\"Cursor AgentService adapter did not return a SessionLease\");\n      };\n      if (deadline.signal.aborted) {\n        throw deadline.classify(deadline.signal.reason);\n      };\n      client = lease.session;\n      client.on(\"error\", fail);\n\n      req = client.request({\n        \":method\": \"POST\",\n        \":path\": urlObj.pathname,\n        \":authority\": urlObj.host,\n        \":scheme\": \"https\",\n        ...headers,\n      });\n\n      req.on(\"error\", fail);\n      req.on(\"response\", (hdrs) => {\n        if (headersSettled) return;\n        headersSettled = true;\n        deadline.clear();\n        deadline.signal.removeEventListener(\"abort\", onHeaderAbort);\n        resolveHeaders(hdrs);\n      });\n      req.on(\"data\", (chunk) => {\n        if (waiting) wake({ value: chunk, done: false });\n        else chunkQueue.push(chunk);\n      });\n      req.on(\"end\", () => {\n        if (closed || ended) return;\n        if (!headersSettled) {\n          fail(agentConnectionClosedError());\n          return;\n        };\n        ended = true;\n        wake({ value: undefined, done: true });\n        close();\n      });\n      req.once(\"close\", () => {\n        if (!closed && !ended) fail(agentConnectionClosedError());\n      });\n\n      deadline.signal.addEventListener(\"abort\", onHeaderAbort, { once: true });\n      signal?.addEventListener(\"abort\", onCallerAbort, { once: true });\n    } catch (error) {\n      close();\n      throw deadline.classify(error);\n    };\n\n    return {\n      responseHeaders,\n      write(frame) {\n        if (req && !req.destroyed) req.write(__vite_ssr_import_3__.Buffer.from(frame));\n      },\n      end() {\n        try { if (req && !req.destroyed) req.end(); } catch {}\n      },\n      close,\n      async read() {\n        if (chunkQueue.length) return { value: chunkQueue.shift(), done: false };\n        if (ended) {\n          if (streamError) throw streamError;\n          return { value: undefined, done: true };\n        };\n        const result = await new Promise((resolve) => { waiting = resolve; });\n        if (streamError) throw streamError;\n        return result || { value: undefined, done: true };\n      },\n    };\n  }\n\n  async executeAgent({ model, body, stream, credentials, signal, proxyOptions = null, connectTimeout = null }) {\n    const agentEndpoint = __vite_ssr_import_4__.PROVIDER_OAUTH.cursor?.agentEndpoint;\n    if (!agentEndpoint) throw new Error(\"Cursor AgentService endpoint is not configured\");\n\n    const url = `${agentEndpoint}${AGENT_RUN_PATH}`;\n    const headers = this.buildHeaders(credentials);\n    const requestController = new AbortController();\n    const onParentAbort = () => requestController.abort(signal.reason);\n    let parentAbortAttached = false;\n    const removeParentAbort = () => {\n      if (!parentAbortAttached) return;\n      parentAbortAttached = false;\n      signal.removeEventListener(\"abort\", onParentAbort);\n    };\n    if (signal?.aborted) {\n      requestController.abort(signal.reason);\n    } else if (signal?.addEventListener) {\n      parentAbortAttached = true;\n      signal.addEventListener(\"abort\", onParentAbort, { once: true });\n    };\n\n    let session;\n    try {\n      session = await this.openAgentHttp2Stream(\n        url,\n        headers,\n        requestController.signal,\n        proxyOptions,\n        connectTimeout,\n      );\n      const closeSession = session.close.bind(session);\n      session.close = () => {\n        removeParentAbort();\n        closeSession();\n      };\n      session.write(buildAgentRunFrame(body.messages || [], model));\n    } catch (error) {\n      session?.close();\n      removeParentAbort();\n      if (error?.name === \"AbortError\" || (0,__vite_ssr_import_14__.isConnectTimeoutError)(error)) throw error;\n      throw new Error(`Cursor AgentService request failed: ${error.message}`);\n    };\n\n    let responseHeaders;\n    try {\n      responseHeaders = await session.responseHeaders;\n    } catch (error) {\n      session.close();\n      if (error?.name === \"AbortError\" || (0,__vite_ssr_import_14__.isConnectTimeoutError)(error)) throw error;\n      throw new Error(`Cursor AgentService request failed: ${error.message}`);\n    };\n\n    const status = Number(responseHeaders[\":status\"] || 0);\n    if (status !== 200) {\n      let errorText = \"\";\n      try {\n        while (true) {\n          const { done, value } = await session.read();\n          if (done) break;\n          errorText += __vite_ssr_import_3__.Buffer.from(value).toString(\"utf8\");\n        }\n      } catch (error) {\n        session.close();\n        if (error?.name === \"AbortError\" || (0,__vite_ssr_import_14__.isConnectTimeoutError)(error)) throw error;\n      };\n      session.close();\n      return {\n        response: new Response(JSON.stringify({\n          error: { message: `Cursor AgentService ${status}: ${errorText || \"request failed\"}`, type: \"api_error\" },\n        }), { status: status || __vite_ssr_import_5__.HTTP_STATUS.SERVER_ERROR, headers: { \"Content-Type\": \"application/json\" } }),\n        url,\n        headers,\n        transformedBody: body,\n        responseFormat: __vite_ssr_import_11__.FORMATS.OPENAI,\n      };\n    };\n\n    // The Claude SSE translator derives Anthropic's message ID by stripping\n    // `chatcmpl-`. Keep the remaining ID in Anthropic's required `msg_` form\n    // so strict clients such as Claude Code accept the completed stream.\n    const responseId = `chatcmpl-msg_${Date.now()}`;\n    const created = Math.floor(Date.now() / 1000);\n    let pending = __vite_ssr_import_3__.Buffer.alloc(0);\n    let finished = false;\n\n    const consume = async (onEvent) => {\n      try {\n        while (!finished) {\n          const { done, value } = await session.read();\n          if (done) break;\n          pending = __vite_ssr_import_3__.Buffer.concat([pending, __vite_ssr_import_3__.Buffer.from(value)]);\n          pending = decodeAgentFrames(pending, (payload) => {\n            // A single read can carry several frames; once the turn is over the\n            // rest of the batch must not reach the already-closed controller.\n            if (finished) return;\n            const serverMessage = (0,__vite_ssr_import_6__.decodeMessage)(payload);\n\n            // agent.v1.AgentServerMessage.interaction_update\n            if (serverMessage.has(1)) {\n              const update = (0,__vite_ssr_import_6__.decodeMessage)(serverMessage.get(1)[0].value);\n              if (update.has(1)) {\n                const textDelta = extractAgentString((0,__vite_ssr_import_6__.decodeMessage)(update.get(1)[0].value), 1);\n                if (textDelta) onEvent({ type: \"text\", value: textDelta });\n              };\n              // Cursor's AgentService emits internal reasoning without the\n              // cryptographic signature required by Anthropic thinking blocks.\n              // Forwarding it makes strict Anthropic clients (Claude Code)\n              // discard or wait on an otherwise complete response. Keep the\n              // reasoning upstream-only and emit the normal answer text.\n              if (update.has(14)) {\n                finished = true;\n                onEvent({ type: \"done\" });\n              }\n            };\n\n            // AgentService requests IDE context before producing a response.\n            // Return an empty context; tokenproxy is not coupled to an editor.\n            if (serverMessage.has(2)) {\n              const execRequest = (0,__vite_ssr_import_6__.decodeMessage)(serverMessage.get(2)[0].value);\n              if (execRequest.has(10)) {\n                session.write(createRequestContextResponse());\n              } else {\n                // Every other ExecServerMessage variant is an editor-backed tool\n                // (shell, read, write, …) that tokenproxy cannot service. Fail the\n                // turn rather than narrating protocol state as assistant text.\n                debugLog(`[CURSOR AGENT] Unsupported exec request fields: ${[...execRequest.keys()].join(\",\")}`);\n                finished = true;\n                onEvent({ type: \"error\", value: \"Cursor AgentService requested an unsupported IDE tool\" });\n              }\n            }\n          });\n        }\n      } finally {\n        try { session.end(); } catch {};\n        try { session.close(); } catch {};\n        if (!finished) onEvent({ type: \"done\" });\n      }\n    };\n\n    if (stream === false) {\n      let content = \"\";\n      let agentError = null;\n      await consume((event) => {\n        if (event.type === \"text\") content += event.value;\n        else if (event.type === \"error\") agentError = event.value;\n      });\n      if (agentError) {\n        return {\n          response: new Response(JSON.stringify({ error: { message: agentError, type: \"api_error\" } }), {\n            status: __vite_ssr_import_5__.HTTP_STATUS.BAD_REQUEST,\n            headers: { \"Content-Type\": \"application/json\" },\n          }),\n          url,\n          headers,\n          transformedBody: body,\n          responseFormat: __vite_ssr_import_11__.FORMATS.OPENAI,\n        };\n      };\n      return {\n        response: new Response(JSON.stringify({\n          id: responseId,\n          object: \"chat.completion\",\n          created,\n          model,\n          choices: [{ index: 0, message: { role: \"assistant\", content: content || null }, finish_reason: \"stop\" }],\n          usage: (0,__vite_ssr_import_8__.estimateUsage)(body, content.length, __vite_ssr_import_11__.FORMATS.OPENAI),\n        }), { headers: { \"Content-Type\": \"application/json\" } }),\n        url,\n        headers,\n        transformedBody: body,\n        responseFormat: __vite_ssr_import_11__.FORMATS.OPENAI,\n      };\n    };\n\n    const encoder = new TextEncoder();\n    const responseStream = new ReadableStream({\n      start(controller) {\n        consume((event) => {\n          if (event.type === \"text\") {\n            controller.enqueue(encoder.encode((0,__vite_ssr_import_10__.chatChunkSse)({ id: responseId, created, model, delta: { content: event.value } })));\n          } else if (event.type === \"error\") {\n            // An SSE error frame, not a content delta: a protocol failure must not\n            // be rendered to the user as the assistant's reply, and downstream\n            // usage tracking must not record the turn as a success.\n            controller.enqueue(encoder.encode((0,__vite_ssr_import_10__.sseChunk)({ error: { message: event.value, type: \"api_error\" } })));\n            controller.enqueue(encoder.encode((0,__vite_ssr_import_9__.SSE_DONE)));\n            controller.close();\n          } else if (event.type === \"done\") {\n            controller.enqueue(encoder.encode((0,__vite_ssr_import_10__.chatChunkSse)({ id: responseId, created, model, delta: {}, finishReason: \"stop\" })));\n            controller.enqueue(encoder.encode((0,__vite_ssr_import_9__.SSE_DONE)));\n            controller.close();\n          }\n        }).catch((error) => controller.error(error));\n      },\n      cancel() {\n        requestController.abort();\n      },\n    });\n\n    return {\n      response: new Response(responseStream, { headers: __vite_ssr_import_9__.SSE_HEADERS }),\n      url,\n      headers,\n      transformedBody: body,\n      responseFormat: __vite_ssr_import_11__.FORMATS.OPENAI,\n    };\n  }\n\n  get supportsBudgetDispatch() { return false; }\n  get budgetDispatchUnsupportedReason() { return \"AgentService can perform opaque internal generations; this executor also supports protobuf transport.\"; }\n\n  async execute({ model, body, stream, credentials, signal, log, proxyOptions = null, connectTimeout = null, beforeDispatch, afterDispatch }) {\n    if (isAgentTextRequest(body)) {\n      try {\n        return await this.executeAgent({ model, body, stream, credentials, signal, proxyOptions, connectTimeout });\n      } catch (error) {\n        if (error?.name === \"AbortError\" || (0,__vite_ssr_import_14__.isConnectTimeoutError)(error)) throw error;\n        return {\n          response: new Response(JSON.stringify({\n            error: { message: error.message, type: \"connection_error\", code: \"\" },\n          }), { status: __vite_ssr_import_5__.HTTP_STATUS.SERVER_ERROR, headers: { \"Content-Type\": \"application/json\", \"x-tokenproxy-replay-safe\": \"false\" } }),\n          url: `${__vite_ssr_import_4__.PROVIDER_OAUTH.cursor?.agentEndpoint || \"\"}${AGENT_RUN_PATH}`,\n          headers: {},\n          transformedBody: body,\n        };\n      }\n    };\n\n    const url = this.buildUrl();\n    const headers = this.buildHeaders(credentials);\n    const transformedBody = this.transformRequest(model, body, stream, credentials);\n    let hookFailed = false;\n    const guard = hook => hook && (async info => {\n      try { await hook(info); } catch (error) { hookFailed = true; throw error; }\n    });\n    const dispatchHooks = { beforeDispatch: guard(beforeDispatch), afterDispatch: guard(afterDispatch) };\n\n    try {\n      const shouldForceFetch = proxyOptions?.enabled === true || proxyOptions?.connectionProxyEnabled === true || !!proxyOptions?.vercelRelayUrl;\n      const response = (__vite_ssr_import_17__ && !shouldForceFetch)\n        ? await this.makeHttp2Request(url, headers, transformedBody, signal, connectTimeout, dispatchHooks)\n        : await this.makeFetchRequest(url, headers, transformedBody, signal, proxyOptions, connectTimeout, dispatchHooks);\n\n      if (response.status !== 200) {\n        const errorText = response.body?.toString() || \"Unknown error\";\n        const errorResponse = new Response(JSON.stringify({\n          error: {\n            message: `[${response.status}]: ${errorText}`,\n            type: \"invalid_request_error\",\n            code: \"\"\n          }\n        }), {\n          status: response.status,\n          headers: (0,__vite_ssr_import_1__.rejectionHeaders)({ headers: new Headers(Object.entries(response.headers || {}).filter(([key]) => !key.startsWith(\":\"))) })\n        });\n        return { response: errorResponse, url, headers, transformedBody: body };\n      };\n\n      if (!response.body || response.body.length === 0) {\n        const errorResponse = new Response(JSON.stringify({\n          error: {\n            message: \"Cursor returned an empty response body\",\n            type: \"upstream_error\",\n            code: \"missing_response_body\",\n          },\n        }), {\n          status: __vite_ssr_import_5__.HTTP_STATUS.BAD_GATEWAY,\n          headers: { \"Content-Type\": \"application/json\", \"x-tokenproxy-replay-safe\": \"false\" },\n        });\n        return { response: errorResponse, url, headers, transformedBody: body };\n      };\n\n      const transformedResponse = stream !== false\n        ? this.transformProtobufToSSE(response.body, model, body)\n        : this.transformProtobufToJSON(response.body, model, body);\n\n      return { response: transformedResponse, url, headers, transformedBody: body };\n    } catch (error) {\n      if (hookFailed) throw error;\n      if (error?.name === \"AbortError\" || (0,__vite_ssr_import_14__.isConnectTimeoutError)(error)) throw error;\n      const errorResponse = new Response(JSON.stringify({\n        error: {\n          message: error.message,\n          type: \"connection_error\",\n          code: \"\"\n        }\n      }), {\n        status: __vite_ssr_import_5__.HTTP_STATUS.SERVER_ERROR,\n        headers: { \"Content-Type\": \"application/json\", \"x-tokenproxy-replay-safe\": \"false\" }\n      });\n      return { response: errorResponse, url, headers, transformedBody: body };\n    }\n  }\n\n  transformProtobufToJSON(buffer, model, body) {\n    const responseId = `chatcmpl-cursor-${Date.now()}`;\n    const created = Math.floor(Date.now() / 1000);\n\n    let offset = 0;\n    let totalContent = \"\";\n    let totalThinking = \"\";\n    const toolCalls = [];\n    const toolCallsMap = new Map(); // Track streaming tool calls by ID\n    const finalizedIds = new Set();\n    let frameCount = 0;\n\n    debugLog(`[CURSOR BUFFER] Total length: ${buffer.length} bytes`);\n\n    while (offset < buffer.length) {\n      const frame = readCursorFrame(buffer, offset, frameCount, \"\");\n      if (frame.status === \"done\") break;\n      offset = frame.offset;\n      frameCount++;\n      if (frame.status === \"skip\") continue;\n      const payload = frame.payload;\n\n      // Check for JSON error frames (byte guard: skip toString on non-JSON frames)\n      if (payload.length > 0 && payload[0] === 0x7b) {\n        try {\n          const text = payload.toString(\"utf-8\");\n          if (text.includes('\"error\"')) {\n            const hasContent = totalContent || toolCallsMap.size > 0;\n            debugLog(\n              `[CURSOR BUFFER] Error frame (hasContent=${hasContent}): ${text.slice(0, 500)}`\n            );\n            if (hasContent) {\n              break;\n            };\n            return createErrorResponse(JSON.parse(text));\n          }\n        } catch {}\n      };\n\n      const result = (0,__vite_ssr_import_6__.extractTextFromResponse)(new Uint8Array(payload));\n      debugLog(`[CURSOR DECODED] Frame ${frameCount}:`, result);\n\n      if (result.error) {\n        const hasContent = totalContent || toolCallsMap.size > 0;\n        debugLog(`[CURSOR BUFFER] Decoded error (hasContent=${hasContent}): ${result.error}`);\n        if (hasContent) {\n          break;\n        };\n        return new Response(\n          JSON.stringify({\n            error: {\n              message: result.error,\n              type: \"rate_limit_error\",\n              code: \"rate_limited\"\n            }\n          }),\n          {\n            status: __vite_ssr_import_5__.HTTP_STATUS.RATE_LIMITED,\n            headers: { \"Content-Type\": \"application/json\" }\n          }\n        );\n      };\n\n      if (result.toolCall) {\n        const tc = result.toolCall;\n\n        if (toolCallsMap.has(tc.id)) {\n          // Accumulate arguments for existing tool call\n          const existing = toolCallsMap.get(tc.id);\n          existing.function.arguments += tc.function.arguments;\n          existing.isLast = tc.isLast;\n        } else {\n          // New tool call\n          toolCallsMap.set(tc.id, { ...tc });\n        };\n\n        // Push to final array when isLast is true\n        if (tc.isLast) {\n          const finalToolCall = toolCallsMap.get(tc.id);\n          finalizedIds.add(tc.id);\n          toolCalls.push({\n            id: finalToolCall.id,\n            type: finalToolCall.type,\n            function: {\n              name: finalToolCall.function.name,\n              arguments: finalToolCall.function.arguments\n            }\n          });\n        }\n      };\n\n      if (result.text) totalContent += result.text;\n      if (result.thinking) totalThinking += result.thinking;\n    };\n\n    const visibleComposerContent = isComposerModel(model)\n      ? visibleComposerContentFromThinking(totalThinking)\n      : \"\";\n    const finalContent = totalContent || visibleComposerContent;\n\n    debugLog(\n      `[CURSOR BUFFER] Parsed ${frameCount} frames, toolCallsMap size: ${toolCallsMap.size}, finalized toolCalls: ${toolCalls.length}`\n    );\n\n    // Finalize all remaining tool calls in map (in case stream ended without isLast=true)\n    for (const [id, tc] of toolCallsMap.entries()) {\n      // Check if already in final array\n      if (!finalizedIds.has(id)) {\n        debugLog(`[CURSOR BUFFER] Finalizing incomplete tool call: ${id}, isLast=${tc.isLast}`);\n        toolCalls.push({\n          id: tc.id,\n          type: tc.type,\n          function: {\n            name: tc.function.name,\n            arguments: tc.function.arguments\n          }\n        });\n      }\n    };\n\n    debugLog(`[CURSOR BUFFER] Final toolCalls count: ${toolCalls.length}`);\n\n\n    const message = {\n      role: \"assistant\",\n      content: finalContent || null\n    };\n\n    if (toolCalls.length > 0) {\n      message.tool_calls = toolCalls;\n    };\n\n    const usage = (0,__vite_ssr_import_8__.estimateUsage)(body, finalContent.length, __vite_ssr_import_11__.FORMATS.OPENAI);\n\n    const completion = {\n      id: responseId,\n      object: \"chat.completion\",\n      created,\n      model,\n      choices: [{\n        index: 0,\n        message,\n        finish_reason: toolCalls.length > 0 ? \"tool_calls\" : \"stop\"\n      }],\n      usage\n    };\n\n    return new Response(JSON.stringify(completion), {\n      status: 200,\n      headers: { \"Content-Type\": \"application/json\" }\n    });\n  }\n\n  transformProtobufToSSE(buffer, model, body) {\n    const responseId = `chatcmpl-cursor-${Date.now()}`;\n    const created = Math.floor(Date.now() / 1000);\n\n    const chunks = [];\n    let offset = 0;\n    let totalContent = \"\";\n    let totalThinking = \"\";\n    let emittedComposerThinkingContentLength = 0;\n    const toolCalls = [];\n    const toolCallsMap = new Map(); // Track streaming tool calls by ID\n    const finalizedIds = new Set();\n    const emittedToolCallIds = new Set();\n    let frameCount = 0;\n    const emitComposerContent = (visible) => {\n      if (visible.length <= emittedComposerThinkingContentLength) return;\n      const deltaContent = visible.slice(emittedComposerThinkingContentLength);\n      emittedComposerThinkingContentLength = visible.length;\n      totalContent += deltaContent;\n      chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({\n        id: responseId, created, model,\n        delta:\n          chunks.length === 0 && toolCalls.length === 0\n            ? { role: \"assistant\", content: deltaContent }\n            : { content: deltaContent }\n      }));\n    };\n\n    debugLog(`[CURSOR BUFFER SSE] Total length: ${buffer.length} bytes`);\n\n    while (offset < buffer.length) {\n      const frame = readCursorFrame(buffer, offset, frameCount, \" SSE\");\n      if (frame.status === \"done\") break;\n      offset = frame.offset;\n      frameCount++;\n      if (frame.status === \"skip\") continue;\n      const payload = frame.payload;\n\n      // Check for JSON error frames (byte-guard: only decode if starts with '{')\n      if (payload[0] === 0x7b) {\n        try {\n          const text = payload.toString(\"utf-8\");\n          if (text.includes('\"error\"')) {\n            const hasContent = chunks.length > 0 || totalContent || toolCallsMap.size > 0;\n            debugLog(\n              `[CURSOR BUFFER SSE] Error frame (hasContent=${hasContent}): ${text.slice(0, 500)}`\n            );\n            if (hasContent) {\n              break;\n            };\n            return createErrorResponse(JSON.parse(text));\n          }\n        } catch {}\n      };\n\n      const result = (0,__vite_ssr_import_6__.extractTextFromResponse)(new Uint8Array(payload));\n      debugLog(`[CURSOR DECODED SSE] Frame ${frameCount}:`, result);\n\n      if (result.error) {\n        const hasContent = chunks.length > 0 || totalContent || toolCallsMap.size > 0;\n        debugLog(`[CURSOR BUFFER SSE] Decoded error (hasContent=${hasContent}): ${result.error}`);\n        if (hasContent) {\n          break;\n        };\n        return new Response(\n          JSON.stringify({\n            error: {\n              message: result.error,\n              type: \"rate_limit_error\",\n              code: \"rate_limited\"\n            }\n          }),\n          {\n            status: __vite_ssr_import_5__.HTTP_STATUS.RATE_LIMITED,\n            headers: { \"Content-Type\": \"application/json\" }\n          }\n        );\n      };\n\n      if (result.toolCall) {\n        const tc = result.toolCall;\n\n        if (chunks.length === 0) {\n          chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({ id: responseId, created, model, delta: { role: \"assistant\", content: \"\" } }));\n        };\n\n        if (toolCallsMap.has(tc.id)) {\n          // Accumulate arguments for existing tool call\n          const existing = toolCallsMap.get(tc.id);\n          const oldArgsLen = existing.function.arguments.length;\n          existing.function.arguments += tc.function.arguments;\n          existing.isLast = tc.isLast;\n\n          // Stream the delta arguments\n          if (tc.function.arguments) {\n            emittedToolCallIds.add(tc.id);\n            chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({\n              id: responseId, created, model,\n              delta: {\n                tool_calls: [\n                  {\n                    index: existing.index,\n                    id: tc.id,\n                    type: \"function\",\n                    function: {\n                      name: tc.function.name,\n                      arguments: tc.function.arguments\n                    }\n                  }\n                ]\n              }\n            }));\n          }\n        } else {\n          // New tool call - assign index and add to map\n          const toolCallIndex = toolCalls.length;\n          finalizedIds.add(tc.id);\n          toolCalls.push({ ...tc, index: toolCallIndex });\n          toolCallsMap.set(tc.id, { ...tc, index: toolCallIndex });\n\n          // Stream initial tool call with name\n          emittedToolCallIds.add(tc.id);\n          chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({\n            id: responseId, created, model,\n            delta: {\n              tool_calls: [\n                {\n                  index: toolCallIndex,\n                  id: tc.id,\n                  type: \"function\",\n                  function: {\n                    name: tc.function.name,\n                    arguments: tc.function.arguments\n                  }\n                }\n              ]\n            }\n          }));\n        }\n      };\n\n      if (result.text) {\n        totalContent += result.text;\n        chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({\n          id: responseId, created, model,\n          delta:\n            chunks.length === 0 && toolCalls.length === 0\n              ? { role: \"assistant\", content: result.text }\n              : { content: result.text }\n        }));\n      };\n\n      if (isComposerModel(model) && result.thinking) {\n        totalThinking += result.thinking;\n        emitComposerContent(\n          emittableComposerContent(visibleComposerContentFromThinking(totalThinking))\n        );\n      }\n    };\n\n    debugLog(\n      `[CURSOR BUFFER SSE] Parsed ${frameCount} frames, toolCallsMap size: ${toolCallsMap.size}, toolCalls array: ${toolCalls.length}`\n    );\n\n    // Finalize all remaining tool calls in map (stream may have ended without isLast=true)\n    for (const [id, tc] of toolCallsMap.entries()) {\n      if (!finalizedIds.has(id)) {\n        debugLog(`[CURSOR BUFFER SSE] Finalizing incomplete tool call: ${id}, isLast=${tc.isLast}`);\n        const toolCallIndex = toolCalls.length;\n        toolCalls.push({\n          id: tc.id,\n          type: tc.type,\n          index: toolCallIndex,\n          function: {\n            name: tc.function.name,\n            arguments: tc.function.arguments\n          }\n        });\n\n        // Emit SSE chunk for the finalized tool call if not already emitted\n        if (!emittedToolCallIds.has(tc.id)) {\n          chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({\n            id: responseId, created, model,\n            delta: {\n              tool_calls: [\n                {\n                  index: toolCallIndex,\n                  id: tc.id,\n                  type: \"function\",\n                  function: {\n                    name: tc.function.name,\n                    arguments: tc.function.arguments\n                  }\n                }\n              ]\n            }\n          }));\n        }\n      }\n    };\n\n    // A tail held back as a possible sentinel prefix is ordinary content once\n    // the stream is over, so flush it rather than swallowing it (#1316).\n    if (isComposerModel(model)) {\n      emitComposerContent(visibleComposerContentFromThinking(totalThinking));\n    };\n\n    if (chunks.length === 0 && toolCalls.length === 0) {\n      chunks.push((0,__vite_ssr_import_10__.chatChunkSse)({ id: responseId, created, model, delta: { role: \"assistant\", content: \"\" } }));\n    };\n\n    const usage = (0,__vite_ssr_import_8__.estimateUsage)(body, totalContent.length, __vite_ssr_import_11__.FORMATS.OPENAI);\n\n    chunks.push(\n      `data: ${JSON.stringify({\n        id: responseId,\n        object: \"chat.completion.chunk\",\n        created,\n        model,\n        choices: [\n          {\n            index: 0,\n            delta: {},\n            finish_reason: toolCalls.length > 0 ? \"tool_calls\" : \"stop\"\n          }\n        ],\n        usage\n      })}\\n\\n`\n    );\n    chunks.push((0,__vite_ssr_import_9__.SSE_DONE));\n\n    return new Response(chunks.join(\"\"), {\n      status: 200,\n      headers: { ...__vite_ssr_import_9__.SSE_HEADERS }\n    });\n  }\n\n  async refreshCredentials() {\n    return null;\n  }\n}",
      "keeps opaque service/subprocess coverage explicitly unsupported for class DevinCliExecutor extends BaseExecutor {\n  constructor() {\n    super(\"devin-cli\", { id: \"devin-cli\", baseUrl: \"devin://acp/stdio\" });\n  }\n\n  buildUrl() {\n    return \"devin://acp/stdio\";\n  }\n\n  buildHeaders() {\n    return {};\n  }\n\n  transformRequest() {\n    return null;\n  }\n\n  get supportsBudgetDispatch() { return false; }\n  get budgetDispatchUnsupportedReason() { return \"The subprocess owns opaque model calls and tool-driven generation loops.\"; }\n\n  async execute({ model, body, credentials, signal, log }) {\n    const b = body ?? {};\n    const messages = Array.isArray(b.messages)\n      ? b.messages\n      : Array.isArray(b.input)\n        ? b.input\n        : [];\n    const promptText = buildPromptText(messages);\n    const workspaceCwd = resolveWorkspaceCwd(b);\n    const devinBin = resolveDevinBin();\n\n    log?.info?.(\n      \"DEVIN\",\n      `devin acp → model=${model}, bin=${devinBin}, cwd=${workspaceCwd}`\n    );\n\n    // Optional MCP servers via DEVIN_MCP_SERVERS (JSON object, devin config format):\n    //   {\"echo\":{\"command\":\"/abs/node\",\"args\":[\"/srv/echo.js\"],\"env\":{\"K\":\"V\"}}}\n    // Plus body.tools (OpenAI schema) → exposed as a \"clientTools\" MCP\n    // server so devin can invoke client-defined tools (bridged back in Phase 2).\n    // When any are present, a throwaway XDG_CONFIG_HOME holds devin/config.json so\n    // the agent auto-connects them (session/new mcpServers alone doesn't spawn\n    // them — see ACP mcp/connect, still unstable). Cleaned up on finish.\n    // NOTE: this replaces the user's global devin MCP config for the subprocess.\n    let mcpConfigDir = null;\n    const mcpServers = {};\n    const mcpJson = process.env.DEVIN_MCP_SERVERS?.trim();\n    if (mcpJson) {\n      try {\n        Object.assign(mcpServers, JSON.parse(mcpJson));\n      } catch (e) {\n        log?.info?.(\"DEVIN\", `DEVIN_MCP_SERVERS parse failed: ${e.message}`);\n      }\n    };\n    const clientTools = Array.isArray(b.tools) ? b.tools.filter(Boolean) : [];\n    const clientToolResults = extractClientToolResults(messages);\n    const clientToolsMcp = buildClientToolsMcp(clientTools, clientToolResults);\n    const hasClientTools = !!clientToolsMcp;\n    if (clientToolsMcp) {\n      mcpServers[\"clientTools\"] = clientToolsMcp;\n      const seeded = Object.keys(clientToolResults).length;\n      log?.info?.(\n        \"DEVIN\",\n        `exposing ${clientTools.length} client tool(s) as MCP` +\n          (seeded ? ` (seeded ${seeded} result(s))` : \"\")\n      );\n    };\n    if (Object.keys(mcpServers).length) {\n      try {\n        mcpConfigDir = __vite_ssr_import_3__.default.mkdtempSync(__vite_ssr_import_1__.default.join(__vite_ssr_import_2__.default.tmpdir(), \"devin-mcp-\"));\n        const cfgDev = __vite_ssr_import_1__.default.join(mcpConfigDir, \"devin\");\n        __vite_ssr_import_3__.default.mkdirSync(cfgDev, { recursive: true });\n        __vite_ssr_import_3__.default.writeFileSync(\n          __vite_ssr_import_1__.default.join(cfgDev, \"config.json\"),\n          JSON.stringify({ mcpServers })\n        );\n        log?.info?.(\"DEVIN\", `mcp config written → ${mcpConfigDir}`);\n      } catch (e) {\n        log?.info?.(\"DEVIN\", `mcp config write failed: ${e.message}`);\n        mcpConfigDir = null;\n      }\n    };\n    const cleanupMcp = () => {\n      if (!mcpConfigDir) return;\n      try {\n        __vite_ssr_import_3__.default.rmSync(mcpConfigDir, { recursive: true, force: true });\n      } catch {\n        /* ignore */\n      };\n      mcpConfigDir = null;\n    };\n\n    const sseStream = new ReadableStream({\n      start(controller) {\n        const enc = new TextEncoder();\n        const emit = (data) => controller.enqueue(enc.encode(data));\n\n        // Inherit the parent environment so devin resolves stored CLI credentials\n        // (~/.local/share/devin/credentials.toml from `devin auth login`). Do NOT\n        // inject WINDSURF_API_KEY: this provider is noAuth, and a bogus/leaked key\n        // overrides stored creds and makes devin return -32000 \"invalid api key\".\n        const env = { ...process.env };\n        // Auto-approve tool execution so the agent doesn't block waiting for a\n        // session/request_permission response we never send (default mode would\n        // hang the stream on the first shell/exec tool call). Override via env.\n        // WARNING: bypass lets the agent run shell/modify FS unattended — local only.\n        env.DEVIN_PERMISSION_MODE = process.env.DEVIN_PERMISSION_MODE || \"bypass\";\n        if (mcpConfigDir) env.XDG_CONFIG_HOME = mcpConfigDir;\n\n        // Agent type: default (omitted) = full agent with built-in tools\n        // (fs/shell/search) so the model can actually perform tasks. Override to\n        // `summarizer` (no tools, text-only) via CLI_DEVIN_AGENT_TYPE for a safer,\n        // tool-less mode. WARNING: the default agent can run shell commands and\n        // modify the filesystem on the host running tokenproxy — only expose locally.\n        const agentType = process.env.CLI_DEVIN_AGENT_TYPE?.trim();\n        const acpArgs = [\"acp\"];\n        if (agentType) acpArgs.push(\"--agent-type\", agentType);\n\n        // Spawn in the client workspace cwd (from <cwd> env context) so built-in\n        // file tools create/delete relative paths in the user's project.\n        // MCP config still comes from XDG_CONFIG_HOME (throwaway), not project .devin/.\n        const child = (0,__vite_ssr_import_0__.spawn)(devinBin, acpArgs, {\n          env,\n          cwd: workspaceCwd,\n          stdio: [\"pipe\", \"pipe\", \"pipe\"],\n          // On Windows, devin.exe may need shell resolution\n          shell: process.platform === \"win32\",\n        });\n\n        let spawnError = null;\n        let stdinClosed = false;\n\n        child.on(\"error\", (err) => {\n          spawnError = err;\n          const msg =\n            err.message.includes(\"ENOENT\") || err.message.includes(\"not found\")\n              ? `Devin CLI not found: ${devinBin}. Install via https://cli.devin.ai or set CLI_DEVIN_BIN env var.`\n              : `Devin CLI spawn error: ${err.message}`;\n          emit(\n            `data: ${JSON.stringify({ error: { message: msg, type: \"devin_cli_error\", code: \"spawn_failed\" } })}\\n\\n`\n          );\n          emit(\"data: [DONE]\\n\\n\");\n          controller.close();\n        });\n\n        if (signal) {\n          signal.addEventListener(\"abort\", () => {\n            if (!child.killed) child.kill(\"SIGTERM\");\n          });\n        };\n\n        // ── JSON-RPC state machine ──────────────────────────────────────────\n        let idCounter = 1;\n        let sessionId = null;\n        let initDone = false;\n        let sessionCreated = false;\n        let promptSent = false;\n        const responseId = `chatcmpl-devin-${Date.now()}`;\n        const created = Math.floor(Date.now() / 1000);\n        let roleEmitted = false;\n        let totalText = \"\";\n        let finished = false;\n\n        const sendRpc = (method, params) => {\n          if (stdinClosed || child.stdin.destroyed) return;\n          const id = idCounter++;\n          try {\n            child.stdin.write(rpc(method, params, id));\n          } catch {\n            /* ignore write errors after close */\n          };\n          return id;\n        };\n\n        // Emit a content delta as an OpenAI-compatible SSE chunk (handles the\n        // leading role chunk once).\n        const emitDelta = (delta) => {\n          if (!roleEmitted) {\n            emit(\n              `data: ${JSON.stringify({\n                id: responseId,\n                object: \"chat.completion.chunk\",\n                created,\n                model,\n                choices: [{ index: 0, delta: { role: \"assistant\", content: \"\" }, finish_reason: null }],\n              })}\\n\\n`\n            );\n            roleEmitted = true;\n          };\n          totalText += delta;\n          emit(\n            `data: ${JSON.stringify({\n              id: responseId,\n              object: \"chat.completion.chunk\",\n              created,\n              model,\n              choices: [{ index: 0, delta: { content: delta }, finish_reason: null }],\n            })}\\n\\n`\n          );\n        };\n\n        // Emit an OpenAI tool_call delta (function calling). Ends the turn with\n        // finish_reason \"tool_calls\" so the client executes and returns tool_result.\n        let toolUseEmitted = false;\n        // ACP tool_call is upsert-by-id: the first event has title, a later update\n        // may only carry rawInput (title omitted). Track pending client-tool calls.\n        const pendingClientTools = new Map(); // toolCallId → original tool name\n        const emitToolUse = (toolName, args, toolCallId) => {\n          const argsStr = typeof args === \"string\" ? args : JSON.stringify(args ?? {});\n          if (!roleEmitted) {\n            emit(\n              `data: ${JSON.stringify({\n                id: responseId,\n                object: \"chat.completion.chunk\",\n                created,\n                model,\n                choices: [{ index: 0, delta: { role: \"assistant\", content: null }, finish_reason: null }],\n              })}\\n\\n`\n            );\n            roleEmitted = true;\n          };\n          emit(\n            `data: ${JSON.stringify({\n              id: responseId,\n              object: \"chat.completion.chunk\",\n              created,\n              model,\n              choices: [\n                {\n                  index: 0,\n                  delta: {\n                    tool_calls: [\n                      {\n                        index: 0,\n                        id: toolCallId,\n                        type: \"function\",\n                        function: { name: toolName, arguments: argsStr },\n                      },\n                    ],\n                  },\n                  finish_reason: null,\n                },\n              ],\n            })}\\n\\n`\n          );\n        };\n\n        const finish = (error, finishReason = \"stop\") => {\n          if (finished) return;\n          finished = true;\n\n          if (error) {\n            emit(\n              `data: ${JSON.stringify({ error: { message: error, type: \"devin_cli_error\" } })}\\n\\n`\n            );\n          } else {\n            // Emit finish chunk\n            emit(\n              `data: ${JSON.stringify({\n                id: responseId,\n                object: \"chat.completion.chunk\",\n                created,\n                model,\n                choices: [{ index: 0, delta: {}, finish_reason: finishReason }],\n                usage: {\n                  prompt_tokens: Math.ceil(promptText.length / 4),\n                  completion_tokens: Math.ceil(totalText.length / 4),\n                  total_tokens: Math.ceil((promptText.length + totalText.length) / 4),\n                  estimated: true,\n                },\n              })}\\n\\n`\n            );\n          };\n          emit(\"data: [DONE]\\n\\n\");\n\n          // Gracefully close stdin → devin will exit\n          try {\n            if (!stdinClosed) {\n              stdinClosed = true;\n              child.stdin.end();\n            }\n          } catch {\n            /* ignore */\n          };\n\n          // Give it 2s to exit cleanly, then SIGKILL\n          const killTimer = setTimeout(() => {\n            if (!child.killed) child.kill(\"SIGKILL\");\n          }, 2000);\n          killTimer.unref?.();\n\n          controller.close();\n          cleanupMcp();\n        };\n\n        // ── stdout reader (NDJSON) ──────────────────────────────────────────\n        let buffer = \"\";\n\n        child.stdout.on(\"data\", (chunk) => {\n          buffer += chunk.toString(\"utf8\");\n          let nl;\n          // Each ACP message is a newline-terminated JSON line\n          while ((nl = buffer.indexOf(\"\\n\")) !== -1) {\n            const line = buffer.slice(0, nl).trim();\n            buffer = buffer.slice(nl + 1);\n            if (!line) continue;\n\n            let msg;\n            try {\n              msg = JSON.parse(line);\n            } catch {\n              continue; // ignore non-JSON lines (banner text, etc.)\n            };\n\n            // ── Initialize response ───────────────────────────────────────\n            if (!initDone && msg.result !== undefined && !msg.method) {\n              initDone = true;\n              // Create session with the client workspace cwd so agent file tools\n              // resolve relative paths against the project (not /tmp).\n              // `mcpServers` is required by devin 3000.2.x (must be a sequence);\n              // omitting it returns -32602 \"Invalid params: missing field mcpServers\".\n              sendRpc(\"session/new\", {\n                cwd: workspaceCwd,\n                mcpServers: [],\n                model: model || undefined,\n              });\n              continue;\n            };\n\n            // ── session/new response → get sessionId ──────────────────────\n            if (initDone && !sessionCreated && msg.result !== undefined && !msg.method) {\n              const res = msg.result || {};\n              sessionId = res.sessionId || null;\n              if (!sessionId) {\n                finish(\"Devin ACP: session/new returned no sessionId\");\n                return;\n              };\n              sessionCreated = true;\n              // Send the prompt. devin 3000.2.x expects `prompt` (a sequence),\n              // not `content` — using `content` returns -32602 \"missing field prompt\".\n              promptSent = true;\n              sendRpc(\"session/prompt\", {\n                sessionId,\n                prompt: [{ type: \"text\", text: promptText }],\n              });\n              continue;\n            };\n\n            // ── session/prompt response (ack / final result) ────────────\n            if (sessionCreated && promptSent && msg.result !== undefined && !msg.method) {\n              // Devin 3000.2.x only resolves session/prompt with the final result\n              // (stopReason) after streaming completes. Streaming notifications are\n              // handled below; nothing to do here unless we never streamed.\n              if (!roleEmitted) {\n                const res = msg.result || undefined;\n                const content = extractResultText(res);\n                if (content) {\n                  totalText = content;\n                  emitDelta(content);\n                };\n                const stopReason = (res && res.stopReason) || \"\";\n                if (stopReason && stopReason !== \"cancelled\") {\n                  finish();\n                  return;\n                }\n              };\n              continue;\n            };\n\n            // ── Permission requests → auto-approve the first allow option ──\n            // Devi asks before running shell/exec tools; as a headless proxy we\n            // grant once. (DEVIN_PERMISSION_MODE=bypass usually prevents these,\n            // but some tool kinds still prompt, so handle them here too.)\n            if (msg.method === \"session/request_permission\" && msg.id !== undefined) {\n              const options = msg.params?.options || [];\n              const allow =\n                options.find((o) => /allow/i.test(String(o.kind || \"\"))) || options[0];\n              if (allow) {\n                child.stdin.write(\n                  JSON.stringify({\n                    jsonrpc: \"2.0\",\n                    id: msg.id,\n                    result: { outcome: { outcome: \"selected\", optionId: allow.optionId } },\n                  }) + \"\\n\"\n                );\n              };\n              continue;\n            };\n\n            // ── Agent stopped notification (devin 3000.2.x stop signal) ───\n            if (msg.method === \"_cognition.ai/agent_stopped\" || msg.method === \"$/agent_stopped\") {\n              const cause = msg.params?.cause;\n              if (cause === \"error\") {\n                // devin uses errorMessage on this notification (not message/error).\n                const errText =\n                  msg.params?.errorMessage ||\n                  msg.params?.message ||\n                  msg.params?.error ||\n                  \"Devin agent error\";\n                finish(String(errText));\n              } else {\n                finish();\n              };\n              return;\n            };\n\n            // ── Streaming notifications (session/update) ──────────────────\n            if (msg.method === \"session/update\" || msg.method === \"$/update\") {\n              const params = msg.params;\n              if (!params) continue;\n\n              // devin 3000.2.x nests the payload under params.update.sessionUpdate;\n              // older devin used a flat params.type.\n              const update = params.update || {};\n              const type = update.sessionUpdate || params.type;\n              const contentField = update.content !== undefined ? update.content : params.content;\n              const deltaText =\n                typeof contentField === \"string\"\n                  ? contentField\n                  : contentField?.text ?? params.delta ?? params.text ?? \"\";\n\n              // ── Client-tool bridge: devin calling a tool from our exposed MCP ──\n              // ACP title shape: \"Calling mcp_<name> from clientTools\".\n              // tool_call is upsert-by-id: title may only appear on the first event,\n              // rawInput on a later tool_call_update. Track pending ids so we don't\n              // require both fields on the same notification.\n              if (\n                hasClientTools &&\n                !toolUseEmitted &&\n                (type === \"tool_call\" || type === \"tool_call_update\")\n              ) {\n                const tcId = update.toolCallId;\n                if (typeof update.title === \"string\" && update.title.startsWith(\"Calling mcp_\") && /from clientTools\\b/.test(update.title)) {\n                  const nameMatch = update.title.match(/^Calling (mcp_\\S+)\\b/);\n                  const mcpName = nameMatch ? nameMatch[1] : \"\";\n                  const origName = fromMcpToolName(mcpName);\n                  if (tcId && origName) pendingClientTools.set(tcId, origName);\n                };\n                const origName = tcId ? pendingClientTools.get(tcId) : null;\n                if (origName && update.rawInput) {\n                  toolUseEmitted = true;\n                  pendingClientTools.delete(tcId);\n                  emitToolUse(origName, update.rawInput, tcId || `call_${Date.now()}`);\n                  finish(null, \"tool_calls\");\n                  return;\n                };\n                continue;\n              };\n\n              if (type === \"agent_message_chunk\" || type === \"message_delta\" || type === \"text_delta\" || type === \"content_delta\") {\n                if (deltaText) emitDelta(deltaText);\n              } else if (type === \"agent_thought_chunk\") {\n                // Internal reasoning — not surfaced to the client.\n              } else if (type === \"message_stop\" || type === \"stop\" || type === \"done\") {\n                finish();\n                return;\n              } else if (type === \"error\") {\n                finish(String(params.message || params.error || \"Devin ACP error\"));\n                return;\n              };\n              continue;\n            };\n\n            // ── Error responses ───────────────────────────────────────────\n            if (msg.error) {\n              finish(`Devin ACP error ${msg.error.code}: ${msg.error.message}`);\n              return;\n            }\n          }\n        });\n\n        child.stderr.on(\"data\", (chunk) => {\n          log?.debug?.(\"DEVIN\", `stderr: ${chunk.toString(\"utf8\").slice(0, 200)}`);\n        });\n\n        child.on(\"close\", (code) => {\n          if (!finished) {\n            if (code !== 0 && !spawnError) {\n              finish(roleEmitted ? undefined : `Devin CLI exited with code ${code}`);\n            } else {\n              finish();\n            }\n          } else {\n            cleanupMcp();\n          }\n        });\n\n        // ── Send initialize ───────────────────────────────────────────────\n        sendRpc(\"initialize\", {\n          protocolVersion: \"0.3\",\n          clientInfo: { name: \"tokenproxy\", version: \"1.0\" },\n          capabilities: {},\n        });\n      },\n    });\n\n    return {\n      response: new Response(sseStream, {\n        status: 200,\n        headers: {\n          \"Content-Type\": \"text/event-stream\",\n          \"Cache-Control\": \"no-cache\",\n          Connection: \"keep-alive\",\n        },\n      }),\n      url: \"devin://acp/stdio\",\n      headers: {},\n      transformedBody: {\n        model,\n        cwd: workspaceCwd,\n        clientTools: clientTools.map((t) => t?.function?.name || t?.name).filter(Boolean),\n        clientToolResults: Object.keys(clientToolResults),\n        mcpServers: Object.keys(mcpServers),\n        promptLength: Array.isArray(body?.messages)\n          ? body.messages.length\n          : Array.isArray(body?.input)\n            ? body.input.length\n            : 0,\n      },\n    };\n  }\n}",
      "keeps opaque service/subprocess coverage explicitly unsupported for class TraeExecutor extends BaseExecutor {\n  constructor() {\n    super(\"trae\", __vite_ssr_import_2__.PROVIDERS.trae);\n  }\n\n  base() {\n    return (this.config.baseUrl || \"https://core-normal.trae.ai/api/remote/v1\").replace(/\\/$/, \"\");\n  }\n\n  buildHeaders(credentials, stream = true) {\n    const token = credentials?.accessToken || \"\";\n    const psd = credentials?.providerSpecificData || {};\n    return {\n      Authorization: `Cloud-IDE-JWT ${token}`,\n      \"Content-Type\": \"application/json\",\n      \"X-Trae-Client-Type\": \"web\",\n      \"X-Preferenced-Language\": psd.appLanguage || \"en\",\n      \"x-user-region\": psd.userRegion || \"US\",\n      Referer: \"https://solo.trae.ai/\",\n      \"User-Agent\": TRAE_UA,\n      Accept: stream ? \"text/event-stream\" : \"application/json\",\n    };\n  }\n\n  // SOLO session modes: \"code\" (model picker) vs \"work\" (fast auto lane).\n  resolveMode(model) {\n    const m = (model || \"\").trim().toLowerCase();\n    if (m === \"work\" || m === \"auto-work\" || m === \"solo-work\") {\n      return { mode: \"work\", strategy: \"auto\", modelName: \"\" };\n    };\n    const auto = !m || m === \"auto\";\n    return { mode: \"code\", strategy: auto ? \"auto\" : \"manual\", modelName: auto ? \"\" : model };\n  }\n\n  // common_params is a JSON-encoded string embedded inside initial_message.\n  commonParams(psd, mode, sessionId) {\n    const cp = {\n      language: \"en-us\",\n      app_language: psd.appLanguage || \"en\",\n      quality: \"stable\",\n      app_version: psd.appVersion || \"1.0.0.1229\",\n      web_id: psd.webId || \"\",\n      user_identity: psd.userIdentity || \"Free\",\n      is_freshman: \"0\",\n      biz_user_id: psd.bizUserId || \"\",\n      user_unique_id: psd.userUniqueId || \"\",\n      scope: psd.scope || \"marscode-us\",\n      tenant: psd.tenant || \"marscode\",\n      region: psd.region || \"US-East\",\n      aiRegion: psd.aiRegion || psd.region || \"US-East\",\n      is_privacy_mode: 0,\n      privacy_mode: \"off\",\n      solo_chat_mode: mode,\n    };\n    if (sessionId) cp.biz_session_id = sessionId;\n    return JSON.stringify(cp);\n  }\n\n  // POST /chat_sessions — creates a session and submits the first turn.\n  async createSession(headers, query, model, psd, signal, connectTimeout = null) {\n    const { mode, strategy, modelName } = this.resolveMode(model);\n    const body = {\n      mode,\n      environment_id: \"default\",\n      initial_message: {\n        chat_session_id: \"\",\n        content: [],\n        query,\n        model_name: modelName,\n        agent_type: \"solo_agent_remote\",\n        model_selection_strategy: strategy,\n        common_params: this.commonParams(psd, mode),\n      },\n      env: \"remote\",\n      auto_create_project: false,\n      origin: \"web\",\n    };\n    const deadline = (0,__vite_ssr_import_4__.createExecutorResponseHeaderTimeout)({\n      connectTimeout,\n      registryTimeout: this.config?.timeoutMs,\n      envTimeout: __vite_ssr_import_3__.FETCH_CONNECT_TIMEOUT_MS,\n      signal,\n    });\n    let res;\n    try {\n      res = await (0,__vite_ssr_import_1__.proxyAwareFetch)(`${this.base()}/chat_sessions`, {\n        method: \"POST\",\n        headers,\n        body: JSON.stringify(body),\n        signal: deadline.signal,\n      }, null);\n    } catch (error) {\n      throw deadline.classify(error);\n    } finally {\n      deadline.clear();\n    };\n    const text = await res.text();\n    if (!res.ok) throw new Error(`[${res.status}] ${text}`);\n    const json = JSON.parse(text);\n    if (json?.code !== 0) throw new Error(`Trae create_session: ${JSON.stringify(json)}`);\n    return { sessionId: json.data.chat_session_id, messageId: json.data.message_id };\n  }\n\n  // GET /events SSE → invoke onEvent(eventType, dataObj) per frame.\n  // Resolves when `done`/`error` arrives, the stream ends, or timeout fires.\n  async streamEvents(headers, sessionId, replyTo, onEvent, signal, connectTimeout = null) {\n    const url = `${this.base()}/chat_sessions/${sessionId}/events?reply_to_message_id=${encodeURIComponent(replyTo)}`;\n    const streamController = new AbortController();\n    const deadline = (0,__vite_ssr_import_4__.createExecutorResponseHeaderTimeout)({\n      connectTimeout,\n      registryTimeout: this.config?.timeoutMs,\n      envTimeout: __vite_ssr_import_3__.FETCH_CONNECT_TIMEOUT_MS,\n      signal,\n    });\n    const fetchSignal = AbortSignal.any([deadline.signal, streamController.signal]);\n    let streamTimer;\n    try {\n      let res;\n      try {\n        res = await (0,__vite_ssr_import_1__.proxyAwareFetch)(url, { method: \"GET\", headers, signal: fetchSignal }, null);\n      } catch (error) {\n        throw deadline.classify(error);\n      };\n      deadline.clear();\n      streamTimer = setTimeout(\n        () => streamController.abort(new Error(\"trae stream timeout\")),\n        STREAM_TIMEOUT_MS,\n      );\n      if (!res.ok || !res.body) throw new Error(`[${res.status}] events stream failed`);\n      const reader = res.body.getReader();\n      const decoder = new TextDecoder();\n      let buf = \"\";\n      let ev = null;\n      for (;;) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        buf += decoder.decode(value, { stream: true });\n        let nl;\n        while ((nl = buf.indexOf(\"\\n\")) >= 0) {\n          const line = buf.slice(0, nl).replace(/\\r$/, \"\");\n          buf = buf.slice(nl + 1);\n          if (line.startsWith(\"event:\")) ev = line.slice(6).trim();\n          else if (line.startsWith(\"data:\")) {\n            const payload = line.slice(5).trim();\n            let data;\n            try { data = JSON.parse(payload); } catch { data = { _raw: payload }; };\n            if (onEvent(ev, data)) {\n              await reader.cancel().catch(() => {});\n              return;\n            }\n          } else if (line === \"\") ev = null;\n        }\n      }\n    } finally {\n      deadline.clear();\n      clearTimeout(streamTimer);\n    }\n  }\n\n  get supportsBudgetDispatch() { return false; }\n  get budgetDispatchUnsupportedReason() { return \"Opaque remote-agent sessions can perform internal generations after acceptance.\"; }\n\n  async execute({ model, body, stream, credentials, signal, connectTimeout = null }) {\n    const headers = this.buildHeaders(credentials, stream !== false);\n    const psd = credentials?.providerSpecificData || {};\n    const query = flattenQuery(body?.messages || []);\n    const responseId = `chatcmpl-trae-${Date.now()}`;\n    const created = Math.floor(Date.now() / 1000);\n\n    const errResponse = (status, message) => new Response(\n      JSON.stringify({ error: { message, type: \"api_error\", code: \"\" } }),\n      { status, headers: { \"Content-Type\": \"application/json\", \"x-tokenproxy-replay-safe\": \"false\" } }\n    );\n\n    let session;\n    try {\n      session = await this.createSession(headers, query, model, psd, signal, connectTimeout);\n    } catch (err) {\n      if (err?.name === \"AbortError\") throw err;\n      return { response: errResponse(502, err?.message ? String(err.message) : String(err)), url: this.base(), headers, transformedBody: body };\n    };\n\n    // Shared per-turn state: plan_item thoughts (cumulative, longest wins).\n    const order = [];\n    const thoughts = {};\n    let sent = 0;\n    let usage = null;\n    let errorEvent = null;\n    const renderNewText = (data) => {\n      const pid = data.id;\n      if (!pid) return \"\";\n      if (!(pid in thoughts)) order.push(pid);\n      const t = data.thought || \"\";\n      if (t.length >= (thoughts[pid] || \"\").length) thoughts[pid] = t;\n      const full = order.map((i) => thoughts[i]).join(\"\");\n      const piece = full.slice(sent);\n      sent = full.length;\n      return piece;\n    };\n\n    if (stream !== false) {\n      const enc = new TextEncoder();\n      const sse = new ReadableStream({\n        start: async (controller) => {\n          const emit = (obj) => controller.enqueue(enc.encode(`data: ${JSON.stringify(obj)}\\n\\n`));\n          emit({\n            id: responseId,\n            object: \"chat.completion.chunk\",\n            created,\n            model,\n            choices: [{ index: 0, delta: { role: \"assistant\" }, finish_reason: null }],\n          });\n          try {\n            await this.streamEvents(headers, session.sessionId, session.messageId, (ev, data) => {\n              if (ev === \"error\") { errorEvent = data; return true; };\n              if (ev === \"token_usage\") usage = data;\n              if (ev === \"plan_item\") {\n                const piece = renderNewText(data);\n                if (piece) {\n                  emit({\n                    id: responseId,\n                    object: \"chat.completion.chunk\",\n                    created,\n                    model,\n                    choices: [{ index: 0, delta: { content: piece }, finish_reason: null }],\n                  });\n                }\n              };\n              return ev === \"done\";\n            }, signal, connectTimeout);\n            if (errorEvent) {\n              emit({\n                id: responseId,\n                object: \"chat.completion.chunk\",\n                created,\n                model,\n                choices: [],\n                error: { message: `trae ${errorEvent.code || \"\"}: ${errorEvent.message || \"\"}`, type: \"api_error\" },\n              });\n            } else {\n              emit({\n                id: responseId,\n                object: \"chat.completion.chunk\",\n                created,\n                model,\n                choices: [{ index: 0, delta: {}, finish_reason: \"stop\" }],\n              });\n              if (usage) {\n                emit({\n                  id: responseId,\n                  object: \"chat.completion.chunk\",\n                  created,\n                  model,\n                  choices: [],\n                  usage: {\n                    prompt_tokens: usage.prompt_tokens || 0,\n                    completion_tokens: usage.completion_tokens || 0,\n                    total_tokens: usage.total_tokens || 0,\n                  },\n                });\n              }\n            };\n            controller.enqueue(enc.encode(\"data: [DONE]\\n\\n\"));\n            controller.close();\n          } catch (err) {\n            controller.error(err);\n          }\n        },\n      });\n      return {\n        response: new Response(sse, {\n          status: 200,\n          headers: {\n            \"Content-Type\": \"text/event-stream\",\n            \"Cache-Control\": \"no-cache\",\n            \"Connection\": \"keep-alive\",\n          },\n        }),\n        url: this.base(),\n        headers,\n        transformedBody: body,\n      };\n    };\n\n    // Non-streaming: drive to completion, return chat.completion JSON.\n    try {\n      await this.streamEvents(headers, session.sessionId, session.messageId, (ev, data) => {\n        if (ev === \"error\") { errorEvent = data; return true; };\n        if (ev === \"token_usage\") usage = data;\n        if (ev === \"plan_item\") renderNewText(data);\n        return ev === \"done\";\n      }, signal, connectTimeout);\n    } catch (err) {\n      if (err?.name === \"AbortError\") throw err;\n      return { response: errResponse(502, err?.message ? String(err.message) : String(err)), url: this.base(), headers, transformedBody: body };\n    };\n    if (errorEvent) {\n      return { response: errResponse(502, `trae ${errorEvent.code || \"\"}: ${errorEvent.message || \"\"}`), url: this.base(), headers, transformedBody: body };\n    };\n    const content = order.map((i) => thoughts[i]).join(\"\");\n    const out = {\n      id: responseId,\n      object: \"chat.completion\",\n      created,\n      model,\n      choices: [{ index: 0, message: { role: \"assistant\", content }, finish_reason: \"stop\" }],\n    };\n    if (usage) {\n      out.usage = {\n        prompt_tokens: usage.prompt_tokens || 0,\n        completion_tokens: usage.completion_tokens || 0,\n        total_tokens: usage.total_tokens || 0,\n      };\n    };\n    return {\n      response: new Response(JSON.stringify(out), { status: 200, headers: { \"Content-Type\": \"application/json\" } }),\n      url: this.base(),\n      headers,\n      transformedBody: body,\n    };\n  }\n\n  // Refresh hook placeholder — Cloud-IDE-JWT is long-lived (~14d); refresh via\n  // ExchangeToken (refresh→access) is wired in services/tokenRefresh/providers.js.\n  async refreshCredentials() {\n    return null;\n  }\n}"
    ],
    "tests/unit/cursor-empty-body.test.js": [
      "CursorExecutor empty successful transport bodies continues to transform a non-empty successful HTTP/2 body",
      "CursorExecutor empty successful transport bodies returns a bounded 502 for empty HTTP/2 responses without transforming protobuf",
      "CursorExecutor empty successful transport bodies returns a bounded 502 for empty HTTP/2 responses without transforming protobuf",
      "CursorExecutor empty successful transport bodies returns a bounded 502 for empty proxy-forced fetch responses without transforming protobuf",
      "CursorExecutor empty successful transport bodies returns a bounded 502 for empty proxy-forced fetch responses without transforming protobuf"
    ],
    "tests/unit/cursor-final-sentinel-1316.test.js": [
      "Cursor Composer strips the final-answer sentinel (#1316) drops the sentinel from a non-streaming reply",
      "Cursor Composer strips the final-answer sentinel (#1316) drops the sentinel when it is separated by whitespace",
      "Cursor Composer strips the final-answer sentinel (#1316) flushes a trailing '<' the stream never completes",
      "Cursor Composer strips the final-answer sentinel (#1316) never emits a sentinel fragment when the marker straddles two frames",
      "Cursor Composer strips the final-answer sentinel (#1316) streams an ordinary '<' in the answer instead of buffering it"
    ],
    "tests/unit/cursor-mitm-passthrough-2420.test.js": [
      "Cursor MITM declines without breaking Cursor (#2420) forwards to the real upstream instead of answering 501",
      "Cursor MITM declines without breaking Cursor (#2420) only chat paths reach the handler at all",
      "Cursor MITM declines without breaking Cursor (#2420) pins only Cursor's API host, not a site the user browses",
      "Cursor MITM declines without breaking Cursor (#2420) still answers explicitly when no forwarder is supplied"
    ],
    "tests/unit/cursor-models-http2-post.test.js": [
      "parseCursorUsableModels display-name fallbacks prefers field 4, then 5, then 3, then the id; skips duplicates and blanks",
      "resolveCursorModels via the built-in http2 POST aborts a request in flight when the signal fires",
      "resolveCursorModels via the built-in http2 POST forceRefresh bypasses the cache and reconnects",
      "resolveCursorModels via the built-in http2 POST posts over the fake session, parses models, and caches the result",
      "resolveCursorModels via the built-in http2 POST rejects immediately on a pre-aborted signal",
      "resolveCursorModels via the built-in http2 POST returns null and logs when the credential lacks token or machine id",
      "resolveCursorModels via the built-in http2 POST returns null and warns on a non-200 status",
      "resolveCursorModels via the built-in http2 POST returns null on an empty catalog payload",
      "resolveCursorModels via the built-in http2 POST returns null when session.request itself throws",
      "resolveCursorModels via the built-in http2 POST returns null when the connector yields no effective route",
      "resolveCursorModels via the built-in http2 POST returns null when the stream errors"
    ],
    "tests/unit/cursor-models.test.js": [
      "Cursor live model catalog bypasses every catalog cache read and overwrites the effective route on force refresh",
      "Cursor live model catalog closes a non-strict fallback-to-direct lease on the effective direct cache hit",
      "Cursor live model catalog closes a strict proxied lease after warming its route-specific cache",
      "Cursor live model catalog decodes the GetUsableModels protobuf response",
      "Cursor live model catalog does not expose a seeded direct cache through relay unavailable catalog routes",
      "Cursor live model catalog does not expose a seeded direct cache through required-unavailable unavailable catalog routes",
      "Cursor live model catalog fails open on an ordinary catalog post failure while closing its lease",
      "Cursor live model catalog force refresh replaces a seeded direct entry through a non-strict proxy fallback",
      "Cursor live model catalog force refresh replaces a seeded strict proxy entry without opening a cache-hit lease",
      "Cursor live model catalog maps a propagated required-unavailable model error to the typed kind response",
      "Cursor live model catalog partitions direct and distinct proxied catalog cache entries",
      "Cursor live model catalog returns typed relay without catalog transport",
      "Cursor live model catalog returns typed required-unavailable without catalog transport",
      "Cursor live model catalog uses a direct cache hit without opening a second lease or posting"
    ],
    "tests/unit/cursor-protobuf-codec-coverage.test.js": [
      "agent Value codec decodeMcpArgs round-trips a struct-of-values argument map",
      "agent Value codec round-trips every JSON shape",
      "ConnectRPC framing keeps the raw payload when gzip decompression fails",
      "ConnectRPC framing returns null on truncated headers and short frames",
      "ConnectRPC framing round-trips a compressed frame",
      "default export exposes the codec surface used by the executor",
      "encodeMessage carries tool results, server bubble id, and agent-mode markers",
      "encodeRequest mixed-assistant normalization does not duplicate results when the next message already carries the same ids",
      "encodeRequest mixed-assistant normalization splits a mixed assistant message into call + result messages",
      "encodeRequest mixed-assistant normalization splits when the next message carries different result ids",
      "extractTextFromResponse extracts thinking text and empty results",
      "extractTextFromResponse falls back to raw_args when mcp params carry no nested args",
      "extractTextFromResponse prefers the nested MCP tool name and params when present",
      "extractTextFromResponse returns null toolCall when the id or name is missing, then falls through",
      "generateCursorBody emits one uncompressed frame whose payload is the chat request",
      "MCP result encoding error and tool-not-found variants use their own fields",
      "MCP result encoding success carries text and image items plus an explicit is_error",
      "MCP tool definition encoding encodeMcpTool omits absent name/desc/schema and always sets a server",
      "MCP tool definition encoding encodeMcpTools returns empty for no tools and concatenates entries otherwise",
      "MCP tool definition encoding encodes the OpenAI wrapper shape and the flat MCP shape identically per contract",
      "tool result requests buildToolResultRequest strips name prefixes and splits the model_call_id delimiter",
      "tool result requests encodeToolResult defaults name/args/result and formats mcp__ names",
      "tool result requests generateToolResultBody frames the request",
      "tool result requests strips a bare mcp_ prefix and passes raw names through",
      "wire primitives decodes FIXED64 and FIXED32 wire types as raw byte slices",
      "wire primitives encodeField returns empty for unsupported wire types and handles Buffer/garbage values",
      "wire primitives encodeInstruction of empty text is empty",
      "wire primitives returns a null value for an unknown wire type and null field at end of buffer",
      "wire primitives round-trips varints across the multi-byte boundary"
    ],
    "tests/unit/cursor-protobuf-framing.test.js": [
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips array",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips boolean false",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips boolean true",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips empty object",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips null",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips number",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips object",
      "agent.v1 Value codec (encodeAgentValue / decodeAgentValue) round-trips string",
      "encodeToolResult / encodeRequest shape sanity encodeRequest splits a mixed assistant tool-calls+results message into two",
      "encodeToolResult / encodeRequest shape sanity encodeToolResult formats a raw tool name and preserves the model_call_id split",
      "extractTextFromResponse extracts a tool call with id/name/args",
      "extractTextFromResponse extracts plain response text (field 2 -> field 1)",
      "extractTextFromResponse returns a decodeError shape rather than throwing on garbage input",
      "extractTextFromResponse returns nulls when neither known field is present",
      "field encode/decode round-trip decodeMessage collects repeated fields under the same key",
      "field encode/decode round-trip round-trips a string LEN field",
      "field encode/decode round-trip round-trips a varint field",
      "MCP tool definition and args codec decodeMcpArgs extracts name, tool_call_id and the args map",
      "MCP tool definition and args codec encodeMcpResultError and encodeMcpResultToolNotFound use distinct field numbers",
      "MCP tool definition and args codec encodeMcpResultSuccess always emits is_error even when false",
      "MCP tool definition and args codec encodeMcpToolDefinition accepts the OpenAI wrapper shape",
      "varint round-trip encodes and decodes 0",
      "varint round-trip encodes and decodes 1",
      "varint round-trip encodes and decodes 127",
      "varint round-trip encodes and decodes 128",
      "varint round-trip encodes and decodes 16384",
      "varint round-trip encodes and decodes 2147483647",
      "varint round-trip encodes and decodes 300",
      "wrapConnectRPCFrame / parseConnectRPCFrame an oversized length prefix does not read past the buffer (32-bit sign wrap yields an empty payload, never OOB access)",
      "wrapConnectRPCFrame / parseConnectRPCFrame falls back to raw payload on a corrupt gzip body instead of throwing",
      "wrapConnectRPCFrame / parseConnectRPCFrame handles a frame split across two chunks by waiting for the tail (classic tail-drop bug)",
      "wrapConnectRPCFrame / parseConnectRPCFrame returns null on a truncated header (fewer than 5 bytes)",
      "wrapConnectRPCFrame / parseConnectRPCFrame returns null when the declared length exceeds the available buffer (truncated tail)",
      "wrapConnectRPCFrame / parseConnectRPCFrame round-trips a gzip-compressed frame",
      "wrapConnectRPCFrame / parseConnectRPCFrame round-trips an uncompressed frame"
    ],
    "tests/unit/cursor-sse-usage-abort.test.js": [
      "execute — error/abort mid-stream a JSON error frame with resource_exhausted and no prior content returns a 429 rather than a truncated stream",
      "execute — error/abort mid-stream a non-abort transport error is converted into a 500 connection_error JSON body, not thrown",
      "execute — error/abort mid-stream an AbortError thrown by the transport propagates out of execute rather than becoming a 500",
      "execute — error/abort mid-stream an error frame arriving after real content is preserved rather than discarding the partial reply",
      "execute — upstream non-200 on the legacy ChatService path wraps a non-200 transport status into an invalid_request_error JSON body without transforming",
      "executor-level gzip frame decoding (readCursorFrame/decompressPayload) decodes a gzip-flagged frame transparently in the SSE path",
      "executor-level gzip frame decoding (readCursorFrame/decompressPayload) falls back through inflate when a TRAILER-flagged frame is raw zlib deflate, not gzip",
      "transformProtobufToJSON — usage accounting matches the non-streaming path prompt_tokens/completion_tokens/total_tokens agree, and completion scales with reply length",
      "transformProtobufToSSE — multi-frame assembly assembles content across multiple frames into ordered deltas ending in [DONE]",
      "transformProtobufToSSE — multi-frame assembly final chunk carries usage that is internally consistent (prompt+completion=total)"
    ],
    "tests/unit/cursor-transport-coverage.test.js": [
      "buildHeaders requires a machineId",
      "executeAgent transport failures decodes gzip agent frames and frames split across reads",
      "executeAgent transport failures returns an api_error response for a non-200 AgentService status",
      "executeAgent transport failures still reports the status when reading the error body fails",
      "executeAgent transport failures wraps a responseHeaders rejection as a request failure",
      "executeAgent transport failures wraps an AgentService connection failure via execute() as connection_error",
      "isAgentCapableRequest / buildAgentRunFrame edges encodes array content, drops empty history entries, and frames correctly",
      "isAgentCapableRequest / buildAgentRunFrame edges rejects null content and empty message lists",
      "openAgentHttp2Stream adapter contract fails with http2_connection_closed when the stream ends before headers",
      "openAgentHttp2Stream adapter contract rejects an adapter that does not return a SessionLease",
      "refreshCredentials is a no-op returning null",
      "transformProtobufToJSON accumulates streamed tool-call arguments and finalizes on isLast",
      "transformProtobufToJSON decodes text frames across the gzip / deflate / raw-deflate fallback chain",
      "transformProtobufToJSON finalizes a tool call the stream never marked last",
      "transformProtobufToJSON keeps decoded content when a JSON error frame arrives after content",
      "transformProtobufToJSON maps a JSON error frame to a structured error response",
      "transformProtobufToJSON returns the undecompressed payload when every decompressor fails",
      "transformProtobufToJSON stops at an incomplete trailing frame without throwing",
      "transformProtobufToSSE emits a finalizing chunk for a tool call the stream never marked last",
      "transformProtobufToSSE keeps streamed content when a JSON error frame arrives after content",
      "transformProtobufToSSE maps a JSON error frame to a structured error response",
      "transformProtobufToSSE streams tool-call deltas: initial name chunk, argument deltas, tool_calls finish"
    ],
    "tests/unit/custom-adapters-3356.test.js": [
      "compiled adapters are what the engine already routes (#3356) resolveTransport picks the compiled transport for the client's source format",
      "compiled adapters are what the engine already routes (#3356) round-trips a stored node back into an importable document",
      "custom adapter compilation (#3356) accepts a per-endpoint url and a urlSuffix",
      "custom adapter compilation (#3356) carries static headers onto every transport",
      "custom adapter compilation (#3356) compiles a minimal document into an executable provider node",
      "custom adapter compilation (#3356) derives the canonical path per declared format and attaches the format's auth",
      "custom adapter compilation (#3356) honours an explicit auth header and scheme",
      "custom adapter compilation (#3356) keeps the openai-compatible id prefix, which is what carries transports to the executor",
      "custom adapter compilation (#3356) normalizes a base URL pasted with its canonical path",
      "custom adapter input validation (#3356) rejects a header name that is not an HTTP token",
      "custom adapter input validation (#3356) rejects a header value carrying CR or LF",
      "custom adapter input validation (#3356) rejects a non-http(s) base URL",
      "custom adapter input validation (#3356) rejects a non-object document",
      "custom adapter input validation (#3356) rejects a prefix already owned by another node",
      "custom adapter input validation (#3356) rejects a prefix with a slash, which would break prefix/model routing",
      "custom adapter input validation (#3356) rejects an unknown or duplicated endpoint format",
      "custom adapter input validation (#3356) rejects framing headers",
      "custom adapters refuse executable content (#3356) rejects \"hooks\" rather than loading it",
      "custom adapters refuse executable content (#3356) rejects \"requestTransformer\" rather than loading it",
      "custom adapters refuse executable content (#3356) rejects \"responseTransformer\" rather than loading it",
      "custom adapters refuse executable content (#3356) rejects \"script\" rather than loading it",
      "custom adapters refuse executable content (#3356) rejects \"streamTransformer\" rather than loading it",
      "custom adapters refuse executable content (#3356) rejects a function-valued field",
      "custom adapters refuse executable content (#3356) rejects an environment interpolation in a header value"
    ],
    "tests/unit/custom-adapters-validation-coverage.test.js": [
      "adapterFromProviderNode returns null for a non-object and round-trips a compiled node",
      "adapterFromProviderNode tolerates a node without transports",
      "compileCustomAdapter rejection paths rejects a non-http(s) or missing baseUrl",
      "compileCustomAdapter rejection paths rejects a non-object document",
      "compileCustomAdapter rejection paths rejects executable fields and function values",
      "compileCustomAdapter rejection paths rejects missing/oversized name and bad prefix, and a taken prefix",
      "compileCustomAdapter rejection paths reports endpoints resolving to nothing when only invalid entries survive validation without errors",
      "compileCustomAdapter rejection paths validates auth: shape, header token, reserved header, scheme",
      "compileCustomAdapter rejection paths validates endpoints: array shape, entry shape, format, duplicates, url, urlSuffix",
      "compileCustomAdapter rejection paths validates headers: shape, count, name, reserved, type, size, newline, interpolation",
      "compileCustomAdapter success paths compiles per-endpoint auth defaults from the format table and hoists headers",
      "compileCustomAdapter success paths derives node baseUrl from the first endpoint url when no baseUrl was given",
      "compileCustomAdapter success paths falls back apiType to chat when neither chat nor responses formats are declared",
      "compileCustomAdapter success paths uses declared auth on every transport and honours anthropicVersion",
      "normalizeAdapterBaseUrl strips trailing slashes and pasted canonical paths case-insensitively"
    ],
    "tests/unit/custom-model-vision-1904.test.js": [
      "custom models can declare capabilities (#1904) a custom model declaring nothing produces no map entry",
      "custom models can declare capabilities (#1904) an existing recognised model is untouched when no override is set",
      "custom models can declare capabilities (#1904) an explicit model-context edit still wins on contextWindow",
      "custom models can declare capabilities (#1904) an unrecognised id is text-only without an override",
      "custom models can declare capabilities (#1904) maxInputTokens and maxOutputTokens finally have a reader",
      "custom models can declare capabilities (#1904) the declared vision flag reaches the resolver",
      "custom models can declare capabilities (#1904) the override is provider-scoped and does not leak to another provider serving the same id",
      "custom models can declare capabilities (#1904) the write path validates the flag and re-publishes the map"
    ],
    "tests/unit/custom-node-image-provider-2197.test.js": [
      "custom OpenAI-compatible nodes generate images (#2197) leaves a built-in provider on its own adapter",
      "custom OpenAI-compatible nodes generate images (#2197) resolves an adapter for a custom node id",
      "custom OpenAI-compatible nodes generate images (#2197) still sends an OpenAI-shaped body and bearer auth",
      "custom OpenAI-compatible nodes generate images (#2197) takes the endpoint from the connection, not a registry entry",
      "custom OpenAI-compatible nodes generate images (#2197) tolerates a trailing slash or a pasted full endpoint"
    ],
    "tests/unit/custom-provider-model-list-3115.test.js": [
      "compatible-provider public model allowlists applies the same allowlist rule to Anthropic-compatible providers",
      "compatible-provider public model allowlists distinguishes expired credential value 1577836800 from a generic authorization failure",
      "compatible-provider public model allowlists distinguishes expired credential value 1577836800000 from a generic authorization failure",
      "compatible-provider public model allowlists distinguishes expired credential value 2020-01-01T00:00:00.000Z from a generic authorization failure",
      "compatible-provider public model allowlists does not leak stale enabledModels when the explicit list is empty",
      "compatible-provider public model allowlists keeps an empty Available Models list empty without upstream discovery",
      "compatible-provider public model allowlists lists only models explicitly saved under the provider id",
      "compatible-provider public model allowlists maps upstream status 401 to an actionable unauthorized catalog failure",
      "compatible-provider public model allowlists maps upstream status 404 to an actionable not_found catalog failure",
      "compatible-provider public model allowlists maps upstream status 429 to an actionable rate_limited catalog failure",
      "compatible-provider public model allowlists preserves built-in provider static catalogs",
      "compatible-provider public model allowlists preserves Claude-compatible rewriting and final sorting",
      "compatible-provider public model allowlists preserves explicit import discovery on the provider models route",
      "compatible-provider public model allowlists reports a provider catalog timeout separately from other transport failures",
      "compatible-provider public model allowlists reports a provider without catalog support as a manual-import state",
      "compatible-provider public model allowlists reports an unreachable compatible catalog as a retryable non-success response without exposing transport details",
      "compatible-provider public model allowlists supports legacy display-prefix custom models and explicit aliases"
    ],
    "tests/unit/custom-server-peer-headers.test.js": [
      "custom-server peer header sanitizing drops a client-supplied peer trust token",
      "custom-server peer header sanitizing drops a client-supplied x-tp-via-proxy marker",
      "custom-server peer header sanitizing generates a peer trust token at boot",
      "custom-server peer header sanitizing keeps the peer token out of persisted request details",
      "custom-server peer header sanitizing marks via-proxy and adopts the forwarded IP for a loopback proxy hop",
      "custom-server peer header sanitizing replaces a client-supplied x-tp-real-ip with the socket address",
      "custom-server peer header sanitizing stamps the trust token so downstream can tell the wrapper ran"
    ],
    "tests/unit/dashboard-guard.test.js": [
      "dashboard guard auto-import local-only access allows only verified direct-loopback auto-import when login is disabled",
      "dashboard guard helpers extracts bearer API keys before x-api-key",
      "dashboard guard helpers extracts Google API keys after x-api-key",
      "dashboard guard local-only access allows /api/context-status on loopback when requireLogin=false",
      "dashboard guard local-only access allows /api/headroom/status on loopback when requireLogin=false",
      "dashboard guard local-only access allows headroom routes on loopback with valid auth_token cookie",
      "dashboard guard local-only access allows headroom routes with valid CLI token",
      "dashboard guard local-only access allows local-only route on loopback when requireLogin=false",
      "dashboard guard local-only access allows local-only route with valid CLI token",
      "dashboard guard local-only access allows pxpipe route from remote host with valid CLI token",
      "dashboard guard local-only access allows pxpipe route on loopback when requireLogin=false",
      "dashboard guard local-only access allows remote settings PATCH with valid CLI token when requireLogin=false",
      "dashboard guard local-only access allows settings PATCH on loopback with valid JWT when requireLogin=false",
      "dashboard guard local-only access denies pxpipe route /api/pxpipe/install from remote host even when requireLogin=false",
      "dashboard guard local-only access denies pxpipe route /api/pxpipe/status from remote host even when requireLogin=false",
      "dashboard guard local-only access keeps settings GET open when requireLogin=false (dashboard read)",
      "dashboard guard local-only access rejects /api/context-status remotely even when requireLogin=false",
      "dashboard guard local-only access rejects /api/headroom/status from remote even when requireLogin=false (403)",
      "dashboard guard local-only access rejects headroom routes from remote when requireLogin=false (PR 3503 gap)",
      "dashboard guard local-only access rejects local-only route from non-loopback host without CLI token",
      "dashboard guard local-only access rejects local-only route from tunnel host even when requireLogin=false",
      "dashboard guard local-only access rejects local-only route on loopback when requireLogin=true and no JWT",
      "dashboard guard local-only access rejects local-only route when Origin is non-loopback (CSRF block)",
      "dashboard guard local-only access rejects remote settings PATCH when requireLogin=false (PR 3499)",
      "dashboard guard local-only access still allows /api/headroom/status with valid CLI token from anywhere",
      "dashboard guard public LLM API access accepts a valid x-api-key after a stale Bearer credential",
      "dashboard guard public LLM API access allows loopback peer IP regardless of Host",
      "dashboard guard public LLM API access allows loopback public LLM API without API key",
      "dashboard guard public LLM API access allows loopback rewritten public LLM API without API key",
      "dashboard guard public LLM API access allows remote beta public LLM API with valid Google API key header",
      "dashboard guard public LLM API access allows remote beta public LLM API with valid Google key query parameter",
      "dashboard guard public LLM API access allows remote codex rewrite with valid API key",
      "dashboard guard public LLM API access allows remote public LLM API with valid bearer API key",
      "dashboard guard public LLM API access allows remote public LLM API with valid x-api-key",
      "dashboard guard public LLM API access allows remote rewritten beta public LLM API with valid API key",
      "dashboard guard public LLM API access keeps remote public LLM POST API-key-protected",
      "dashboard guard public LLM API access rejects remote beta public LLM API without API key",
      "dashboard guard public LLM API access rejects remote codex rewrite without API key",
      "dashboard guard public LLM API access rejects remote Host-spoof when real peer IP is non-loopback",
      "dashboard guard public LLM API access rejects remote rewritten beta public LLM API without API key",
      "dashboard guard public LLM API access rejects remote rewritten public LLM API without API key",
      "dashboard guard public LLM API access returns a bodyless CORS preflight without API-key lookup"
    ],
    "tests/unit/data-dir-permission-message-1983.test.js": [
      "a permission failure on the data directory says so (#1983) covers the three codes a wrong owner or a read-only mount produces",
      "a permission failure on the data directory says so (#1983) gives the command that fixes it, pointed at the real data directory",
      "a permission failure on the data directory says so (#1983) keeps the code and the original error, so callers can still branch on it",
      "a permission failure on the data directory says so (#1983) names sudo, which is the cause in nearly every report of this",
      "a permission failure on the data directory says so (#1983) names the path, the code and the user it is running as",
      "a permission failure on the data directory says so (#1983) passes anything else straight through, unwrapped"
    ],
    "tests/unit/data-dir-test-isolation.test.js": [
      "ordinary application startup retains its existing directory contract preserves production permission fallback (EACCES)",
      "ordinary application startup retains its existing directory contract preserves production permission fallback (EPERM)",
      "ordinary application startup retains its existing directory contract preserves the default path in development",
      "ordinary application startup retains its existing directory contract preserves the default path in production",
      "test database isolation before module initialization accepts the explicit per-file directory supplied by the repository recipe",
      "test database isolation before module initialization never redirects a denied test directory to the home database (EACCES)",
      "test database isolation before module initialization never redirects a denied test directory to the home database (EPERM)",
      "test database isolation before module initialization rejects a Unix-only test path on Windows before selecting the default directory",
      "test database isolation before module initialization rejects an unspecified test DATA_DIR (   ) without filesystem writes",
      "test database isolation before module initialization rejects an unspecified test DATA_DIR () without filesystem writes",
      "test database isolation before module initialization rejects an unspecified test DATA_DIR (undefined) without filesystem writes"
    ],
    "tests/unit/database-import-partial.test.js": [
      "reports committed import and failed process refresh separately without repeating import",
      "reports complete local phases when both succeed, and refuses missing identity before mutation"
    ],
    "tests/unit/db-adapter-shutdown.test.js": [
      "sql.js adapter shutdown flushes a pending write through the registry before exit",
      "sql.js adapter shutdown registerShutdownFlusher rejects non-functions",
      "sql.js adapter shutdown SIGINT is handled exactly once through the shutdown registry",
      "sql.js adapter shutdown SIGTERM is handled exactly once through the shutdown registry"
    ],
    "tests/unit/db-benchmark.test.js": [
      "DB Benchmark — SQLite vs Lowdb getUsageStats(24h) repeat 50x",
      "DB Benchmark — SQLite vs Lowdb INSERT 500 provider connections",
      "DB Benchmark — SQLite vs Lowdb READ 200 by id (point lookup)",
      "DB Benchmark — SQLite vs Lowdb READ 200 filtered queries",
      "DB Benchmark — SQLite vs Lowdb saveRequestUsage 500 entries"
    ],
    "tests/unit/db-concurrent.test.js": [
      "DB Concurrency — atomic safety 100 parallel saveRequestUsage → no count loss",
      "DB Concurrency — atomic safety 200 parallel saveRequestDetail → all flushed",
      "DB Concurrency — atomic safety addCustomModel race: parallel duplicate adds → only 1 inserted",
      "DB Concurrency — atomic safety daily summary aggregates correctly under parallel writes",
      "DB Concurrency — atomic safety mixed concurrent: usage + details + connections + aliases",
      "DB Concurrency — atomic safety OAuth refresh race: parallel updateProviderConnection on same id",
      "DB Concurrency — atomic safety updatePricing race: parallel adds different models → all merged",
      "DB Concurrency — atomic safety updateSettings parallel → no merge loss"
    ],
    "tests/unit/db-driver-chain.test.js": [
      "Driver fallback chain default → picks better-sqlite3 when available",
      "Driver fallback chain falls back to node:sqlite when better-sqlite3 unavailable",
      "Driver fallback chain falls back to sql.js when both native drivers unavailable"
    ],
    "tests/unit/db-file-permissions.test.js": [
      "DB file permissions creates the data dir and DB file with owner-only permissions",
      "DB file permissions keeps WAL sidecar files owner-only",
      "DB file permissions repairs world-readable permissions left by an existing install",
      "DB file permissions writes schema-migration backups owner-only"
    ],
    "tests/unit/db-migration-chain.test.js": [
      "Schema migrations a foreign predecessor state directory is outside the search path entirely",
      "Schema migrations auto-sync re-creates missing index when DB lacks it",
      "Schema migrations existing DB at older schemaVersion → re-applies pending migrations on restart",
      "Schema migrations fresh DB → applies migrations & stamps schemaVersion",
      "Schema migrations predecessor state sitting beside the namespace is neither read nor changed"
    ],
    "tests/unit/db-migration-safety.test.js": [
      "atomic additive schema migration backs up missing schema objects even when the stored version already matches",
      "atomic additive schema migration refuses an unenforceable unique index without deleting duplicates or stamping completion",
      "atomic additive schema migration rolls back failed column expansion and can retry on the same adapter",
      "atomic additive schema migration upgrades an unversioned populated predecessor without losing rows using better-sqlite3",
      "atomic additive schema migration upgrades an unversioned populated predecessor without losing rows using node:sqlite",
      "atomic additive schema migration upgrades an unversioned populated predecessor without losing rows using sql.js",
      "complete pre-schema backup publishes all data and schema independently with better-sqlite3",
      "complete pre-schema backup publishes all data and schema independently with native-without-serialize",
      "complete pre-schema backup publishes all data and schema independently with node:sqlite",
      "complete pre-schema backup publishes all data and schema independently with sql.js",
      "complete pre-schema backup stops before any schema write if the required backup cannot be published"
    ],
    "tests/unit/db-sqlite-vs-lowdb.test.js": [
      "DB SQLite layer — public API parity apiKeys: create/get/validate/delete",
      "DB SQLite layer — public API parity combos: CRUD",
      "DB SQLite layer — public API parity customModels: add/list/delete with dedupe",
      "DB SQLite layer — public API parity disabledModels: add/remove per provider",
      "DB SQLite layer — public API parity exportDb / importDb roundtrip",
      "DB SQLite layer — public API parity getChartData: 24h buckets",
      "DB SQLite layer — public API parity getChartData: 7d buckets",
      "DB SQLite layer — public API parity isCloudEnabled reflects settings",
      "DB SQLite layer — public API parity mitmAlias: get/set per tool",
      "DB SQLite layer — public API parity modelAliases: KV ops",
      "DB SQLite layer — public API parity pricing: user pricing merged with constants",
      "DB SQLite layer — public API parity providerConnections: CRUD + reorder by priority",
      "DB SQLite layer — public API parity providerConnections: GitHub OAuth uses account identity as fallback name",
      "DB SQLite layer — public API parity providerConnections: optional fields persisted via JSON column",
      "DB SQLite layer — public API parity providerNodes: CRUD",
      "DB SQLite layer — public API parity proxyPools: CRUD with sort by updatedAt desc",
      "DB SQLite layer — public API parity requestDetails: save → query with paging",
      "DB SQLite layer — public API parity settings: get → defaults; update → merge",
      "DB SQLite layer — public API parity usage: pending tracking in-memory",
      "DB SQLite layer — public API parity usage: saveRequestUsage + getUsageHistory + getUsageStats"
    ],
    "tests/unit/ddgs-search-provider-3401.test.js": [
      "DDGS provider entry defaults to the loopback ddgs api port and honours DDGS_URL",
      "DDGS provider entry has a builder and a normalizer wired for its id",
      "DDGS provider entry is a credential-free webSearch provider, like SearXNG",
      "DDGS request builder does not double the path when the base URL already names an endpoint",
      "DDGS request builder drops the year timelimit on news, which DDGS documents as d/w/m only",
      "DDGS request builder joins country and language into the DDGS region token",
      "DDGS request builder maps time_range onto the single-letter timelimit",
      "DDGS request builder omits region unless both halves are present",
      "DDGS request builder passes a chosen backend engine through and omits it otherwise",
      "DDGS request builder posts JSON to /search/text for a web search",
      "DDGS request builder posts to /search/news for a news search",
      "DDGS request builder reads safesearch and backend from providerSpecificData too",
      "DDGS request builder sends no auth header (the ddgs api server has none)",
      "DDGS request builder turns offset into a 1-indexed page",
      "DDGS response normalizer maps news() rows, whose link field is url, plus date/image/source",
      "DDGS response normalizer maps text() rows, whose link field is href",
      "DDGS response normalizer returns an empty set rather than throwing on a malformed payload"
    ],
    "tests/unit/decision-log-auth-emissions.test.js": [
      "auth.js LOCK emissions (markAccountUnavailable) LOCK.applied class=credential on a 401 has expect_reset=false (the timed-backoff misreport)",
      "auth.js LOCK emissions (markAccountUnavailable) LOCK.applied class=rate on a temporary 429 has expect_reset=true",
      "auth.js LOCK emissions (markAccountUnavailable) LOCK.clamped reports requested vs applied when a provider reset hits the cap",
      "auth.js LOCK emissions (markAccountUnavailable) LOCK.monthly-reset on the GitHub usage-limit path",
      "auth.js LOCK emissions (markAccountUnavailable) LOCK.permanent on the Codex deactivation path",
      "auth.js LOCK emissions (markAccountUnavailable) omits rid when markAccountUnavailable has no log context",
      "auth.js selection emissions emits LEASE.refused with held/cap/next/retry_after when the pinned slot is full",
      "auth.js selection emissions emits LEASE.ungated when capacity was never registered (fail-open speaks)",
      "auth.js selection emissions emits SEL.operator-pinned when the operator named the connection",
      "auth.js selection emissions emits SEL.proxy-unusable with pool and resolution kind, releasing the slot",
      "auth.js selection emissions emits SEL.quota-paused and SEL.quota-unknown for the two quota exits",
      "auth.js selection emissions emits SEL.repin with from/to/trigger/rcpt when the pin moves",
      "auth.js selection emissions emits SEL.skipped and SEL.refused when nothing can serve",
      "auth.js selection emissions emits SEL.win with the worked-example field shape and the repin receipt as rcpt",
      "auth.js selection emissions keeps a healthy pin silent (no pin-hit line) but reports it on the return",
      "auth.js selection emissions never emits SEL.model-locked: a timed lock no longer removes an account",
      "auth.js selection emissions omits rid when no log context was threaded (background selection)"
    ],
    "tests/unit/decision-log-incident-replay.test.js": [
      "incident replay: diagnose the quota lock + failover from the log alone plays beats 1-5 and matches the golden capture",
      "incident replay: diagnose the quota lock + failover from the log alone rotates a rate limit across the pool instead of ending on the first account"
    ],
    "tests/unit/decision-log-req-summary.test.js": [
      "ACCT.alias-dropped names the dropped alias with conn/model context",
      "detail-write ACCT phases on the non-stream handlers forced-SSE detail-write failure: ACCT phase=save-json",
      "detail-write ACCT phases on the non-stream handlers non-stream detail-write failure: ACCT phase=save",
      "detail-write ACCT phases on the non-stream handlers non-stream success: REQ.ok with row= and x-tp-rid header",
      "handleChatCore REQ.failed at terminal provider errors emits REQ.failed with status and upstream why, and no REQ.ok",
      "handleChatCore REQ.ok one-liner carries the rid into the request detail and usage rows",
      "handleChatCore REQ.ok one-liner echoes x-tp-rid on the success response",
      "handleChatCore REQ.ok one-liner emits exactly one REQ.ok with rid, route, conn, tokens and row=",
      "handleChatCore XFORM cache fork notes notes cache-keep when existing claude anchors survive re-anchoring",
      "handleChatCore XFORM cache fork notes notes cache-legacy when the body carried no valid anchors",
      "handleChatCore XFORM.headroom-phantom emits delta and shrunk_pct when savings are phantom",
      "LOG.boot (custom-server) emits a boot line with sha/version/node/db/logdecisions and never throws",
      "REQ.ok save=/ce= with a composed saver pipeline ce= reports the shared prefix on a repeat request of the same sid",
      "REQ.ok save=/ce= with a composed saver pipeline no saver running keeps the line byte-identical to the pre-telemetry shape",
      "REQ.ok save=/ce= with a composed saver pipeline save=/save_tok= appear with the rtk stage delta; ce= silent on first sight of a sid",
      "reqSummary path= assembly a 7-code saver path retains cache-keep under the widened render budget",
      "reqSummary path= assembly an 8-stage save list renders complete, not mid-delta cut",
      "reqSummary path= assembly countCacheAnchors counts only valid ephemeral breakpoints",
      "reqSummary path= assembly joins fork codes in order and clears them after the emit",
      "reqSummary path= assembly omits path= when the request took no annotated forks",
      "request-log lifecycle integration closes bounded retention after forced-stream JSON completion",
      "request-log lifecycle integration does not turn asynchronous logger failure into a failed request",
      "request-log lifecycle integration keeps retention open while an SSE response lives and closes on consumption",
      "streaming: buildOnStreamComplete emissions emits REQ.failed status=499 why=aborted on caller abort",
      "streaming: buildOnStreamComplete emissions flags estimated usage, locks the empty stream, and still emits REQ.ok",
      "streaming: buildOnStreamComplete emissions routes detail-write failures to ACCT with phases save-stream/update/finalize",
      "streaming: buildOnStreamComplete emissions stall abandonment locks and reports STREAM.stalled",
      "streaming: handleStreamingResponse non-SSE guard rejects a 200 HTML upstream as STREAM.non-sse and echoes rid",
      "streaming: placeholder detail write routes the placeholder write failure to ACCT phase=save-stream",
      "x-tp-rid header echo createErrorResult carries rid through to the response",
      "x-tp-rid header echo errorResponse stamps the rid when given one",
      "x-tp-rid header echo unavailableResponse stamps the rid when given one"
    ],
    "tests/unit/decision-log-sink.test.js": [
      "sink failure and recovery (D-9, SEC-5b) fails soft once, re-probes only after 5 minutes, and emits LOG.resumed on recovery",
      "sink rotation (D-2) keeps three generations .1/.2/.3 and never a .4"
    ],
    "tests/unit/decision-log-up-lines.test.js": [
      "UP lines (chat.js admission/retry loop) names UP.attempt-ceiling when x-max-attempts is exhausted",
      "UP lines (chat.js admission/retry loop) names UP.failover with the advertised lock after confirmed quota depletion",
      "UP lines (chat.js admission/retry loop) names UP.no-replay when an accepted generation returns an empty stream",
      "UP lines (chat.js admission/retry loop) names UP.no-replay while a healthy account waits through a cooldown",
      "UP lines (chat.js admission/retry loop) names UP.replay-overflow when the same account fails again"
    ],
    "tests/unit/decision-log.test.js": [
      "chat.js admission refusal (the 73% line) D-10: the rate-limit refusal emits REQ.refused carrying the same rid",
      "chat.js admission refusal (the 73% line) names the key, the limit, the window and the reset, and folds the repeats",
      "chat.js admission refusal (the 73% line) no longer writes the reasonless warn it replaced",
      "decision log schema drops null and undefined rather than writing 'k=null'",
      "decision log schema is '<iso8601> CLASS.verdict rid=<8hex> k=v...' to the byte",
      "decision log schema keeps fold bookkeeping last, where it is about the line not the event",
      "decision log schema puts the identity keys first whatever order the caller wrote them",
      "field keys SEC-4: keys outside the k=v grammar never reach the line",
      "helpers puts an identity in a line without the line carrying the credential",
      "helpers states a reset relatively, because an instant makes the reader do arithmetic",
      "path/save truncation cuts at whole codes (audit finding 19) a 9-code path renders only whole codes plus an ellipsis, never a mid-code cut",
      "path/save truncation cuts at whole codes (audit finding 19) save= gets the same whole-code treatment",
      "repeat folding D-6: never folds across a change in status",
      "repeat folding emits on 1,2,4,8,...,128 then every 128",
      "repeat folding forces a roll-up once an hour so a slow burn cannot go quiet",
      "repeat folding is bounded, so the folding state cannot itself become the leak",
      "repeat folding names how many occurrences a folded line stands for, and when the run began",
      "repeat folding NEVER folds across a change in why",
      "repeat folding turns the measured 154 OAuth failures into 8 lines without losing one",
      "request id adopts the front proxy's x-tp-rid so a 503 at admission joins the gateway lines",
      "request id is 8 hex and does not repeat inside a process",
      "request id refuses an inbound rid that could forge a second line, and mints instead",
      "silence policy is structural, not a review habit cannot be split into a second line by a value (log injection)",
      "silence policy is structural, not a review habit never spreads or stringifies an object into a line",
      "silence policy is structural, not a review habit routes free text through the shared redaction chokepoint",
      "silence policy is structural, not a review habit truncates free text at 60 characters",
      "storm backstop converts an unbounded log storm into a bounded couple of lines a minute",
      "storm backstop D-1: counts folded lines, not decide() calls — 300 identical calls emit 9 and never trip it",
      "storm backstop D-8: storm-throttled occurrences still enter foldState, so the post-resume rep is whole",
      "storm backstop never throttles the nominal REQ line, which is one per request already",
      "the closed verdict enum holds every verdict the shipped call sites use",
      "the closed verdict enum is frozen, class and list",
      "the closed verdict enum makes a typo a red test rather than a new string in production",
      "the two incidents the design must make greppable loopback satisfied an inference-class gate: an invisible bypass becomes one line",
      "the two incidents the design must make greppable OAuth chain rotated underneath us: 169 undiscoverable lines become one"
    ],
    "tests/unit/decision-trace-ranking.test.js": [
      "rankAccounts trace: the gates a mixed-shape cohort ranks on each account own deadline, not a shape refusal",
      "rankAccounts trace: the gates depleted names every account, its band, and the soonest reset",
      "rankAccounts trace: the gates empty cohort degrades with the reason, not a fake winner",
      "rankAccounts trace: the gates invalid-record fires for a malformed window inside a cohort",
      "rankAccounts trace: the gates invalid-record names the offending id and the reason, and still serves it",
      "rankAccounts trace: the ordered path folds stale evidence age into alt tokens (band:age)",
      "rankAccounts trace: the ordered path names the ordering key that decided (reset-horizon) with winner fields",
      "rankAccounts trace: the ordered path never prints: the ranker is pure",
      "rankAccounts trace: the ordered path reports configured-priority when priority breaks the tie",
      "rankAccounts trace: the ordered path reports fallback-order for a singleton cohort and omits unknown fields",
      "rankAccounts trace: the ordered path reports pinned-continuity when the previous pin decides between equals"
    ],
    "tests/unit/decision-trace-repin.test.js": [
      "decideRepin exposes trigger/reason/from/to for every outcome a healthy pin is held even when an earlier account has restocked",
      "decideRepin exposes trigger/reason/from/to for every outcome degraded cohort with a fallback winner keeps the pin",
      "decideRepin exposes trigger/reason/from/to for every outcome empty cohort -> none, no-accounts",
      "decideRepin exposes trigger/reason/from/to for every outcome never prints: the policy is pure",
      "decideRepin exposes trigger/reason/from/to for every outcome no existing pin -> repin with initial-pin from nothing",
      "decideRepin exposes trigger/reason/from/to for every outcome pinned account gone and nothing can serve -> none with the ranker reason",
      "decideRepin exposes trigger/reason/from/to for every outcome pinned account left the cohort -> repin on unavailable",
      "decideRepin exposes trigger/reason/from/to for every outcome pinned window exhausted -> repin on exhaustion (the pin-expired verdict)"
    ],
    "tests/unit/decision-trace-scheduler.test.js": [
      "lease registry verdicts a gated admission carries no ungated marker",
      "lease registry verdicts a lease carries the seq a double-release line prints, and release stays idempotent",
      "lease registry verdicts a malformed capacity fails open too, but says capacity-malformed",
      "lease registry verdicts a refused reservation reports held, cap and retry hint via lastRefusal",
      "lease registry verdicts an ungated admission says why: unregistered capacity fails open visibly",
      "selectAndReserve trace a mixed-shape cohort serves and emits no refusal",
      "selectAndReserve trace carries the refusal enum for the no-accounts and none-eligible exits",
      "selectAndReserve trace folds slot-walk skips, max 3 then +N, and refuses when every slot is taken",
      "selectAndReserve trace maps a healthy pin to the silent pin-hit verdict (not printed, but returned)",
      "selectAndReserve trace maps an exhausted pin to the pin-expired verdict with from/to",
      "selectAndReserve trace names the healthy pinned connection whose capacity requires a wait",
      "selectAndReserve trace never prints: the scheduler is pure",
      "selectAndReserve trace returns the ranking win plus an initial-pin verdict and no skips on a clean selection"
    ],
    "tests/unit/decloak-forced-sse-2693.test.js": [
      "cloaked tool names are restored in every response shape (#2693) a name that was never cloaked is left alone",
      "cloaked tool names are restored in every response shape (#2693) a shape it does not recognise is returned untouched rather than mangled",
      "cloaked tool names are restored in every response shape (#2693) an empty or absent map is a no-op, and the body comes back identical",
      "cloaked tool names are restored in every response shape (#2693) Claude content blocks",
      "cloaked tool names are restored in every response shape (#2693) it does not mutate the response it was given",
      "cloaked tool names are restored in every response shape (#2693) OpenAI tool_calls, which is what the forced-SSE path parses into",
      "cloaked tool names are restored in every response shape (#2693) Responses output items",
      "the forced-SSE path actually calls it and chatCore passes the map at both call sites",
      "the forced-SSE path actually calls it restores names on both of its branches",
      "the forced-SSE path actually calls it takes the map"
    ],
    "tests/unit/deepseek-thinking-echo-2397.test.js": [
      "DeepSeek thinking mode gets its thinking echoed back (#2397, #1786) a plain-text assistant turn after a tool result carries a thinking block",
      "DeepSeek thinking mode gets its thinking echoed back (#2397, #1786) nothing is added when thinking is not enabled",
      "DeepSeek thinking mode gets its thinking echoed back (#2397, #1786) real thinking the client sent is kept, not replaced by a placeholder",
      "DeepSeek thinking mode gets its thinking echoed back (#2397, #1786) the placeholder DeepSeek gets is unsigned, since it does not want Anthropic's signature",
      "the widening is scoped to DeepSeek a Claude tool-use turn without thinking gets NO fabricated block",
      "the widening is scoped to DeepSeek a plain-text assistant turn to Claude native is left alone"
    ],
    "tests/unit/default-executor-anthropic-header.test.js": [
      "DefaultExecutor Anthropic version headers adds the default Anthropic version for combined auth when absent",
      "DefaultExecutor Anthropic version headers adds the default Anthropic version for split auth when absent",
      "DefaultExecutor Anthropic version headers preserves an existing Anthropic version for combined auth",
      "DefaultExecutor Anthropic version headers preserves an existing Anthropic version for split auth"
    ],
    "tests/unit/default-executor-transport.test.js": [
      "applyEndpointOverride — degenerate registry URL an empty registry URL leaves the trimmed stored base standing alone",
      "applyEndpointOverride carries the registry operation path onto a bare stored base",
      "applyEndpointOverride does not double the path when the stored base already ends in it",
      "applyEndpointOverride empty/blank override leaves the registry URL; unrecognized registry tail stands alone",
      "applyEndpointOverride longest path wins: /images/generations is not mistaken for /generations (#3253)",
      "DefaultExecutor.applyJsonSchemaFallback — openai-compatible without Structured Output appends to an existing string system message instead of adding a second one",
      "DefaultExecutor.applyJsonSchemaFallback — openai-compatible without Structured Output downgrades json_schema to json_object and injects the schema as a system prompt",
      "DefaultExecutor.applyJsonSchemaFallback — openai-compatible without Structured Output leaves a non-json_schema response_format alone",
      "DefaultExecutor.applyJsonSchemaFallback — openai-compatible without Structured Output pushes a text part onto an array system content",
      "DefaultExecutor.buildHeaders — anthropic official vs compatible client-supplied anthropic-beta flags are merged into the base set, not dropped",
      "DefaultExecutor.buildHeaders — anthropic official vs compatible official anthropic: x-api-key auth, version header, and an Anthropic-Beta for the model",
      "DefaultExecutor.buildHeaders — anthropic official vs compatible third-party anthropic-compatible upstream: identity headers stripped, claude-code beta removed, Bearer added beside x-api-key",
      "DefaultExecutor.buildHeaders — registry-declared hooks run before auth kilocodeOrg: org header only when the connection carries an orgId",
      "DefaultExecutor.buildHeaders — registry-declared hooks run before auth kimiHeaders: device id carried from the connection, auth token not clobbered",
      "DefaultExecutor.buildHeaders — runtimeTransport auth descriptor a transport beta header reduced to only the claude-code flag is deleted, not left empty",
      "DefaultExecutor.buildHeaders — runtimeTransport auth descriptor combined descriptor with anthropicVersion sets both token header and version",
      "DefaultExecutor.buildHeaders — split descriptor OAuth branch anthropic-compatible with only an access token authenticates via Bearer, not x-api-key",
      "DefaultExecutor.buildUrl — remaining branches a registry urlSuffix is appended to the provider base URL",
      "DefaultExecutor.buildUrl — remaining branches anthropic-compatible: /messages appended to the stored base, default base without one",
      "DefaultExecutor.buildUrl {accountId} substitution: filled from providerSpecificData, throws without it",
      "DefaultExecutor.buildUrl gemini format: model-templated path split by stream",
      "DefaultExecutor.buildUrl per-connection endpoint override keeps the provider operation path",
      "DefaultExecutor.buildUrl runtimeTransport baseUrl wins over everything, with optional urlSuffix",
      "DefaultExecutor.execute — floor probe never costs a second call on an unreadable body a non-OK response whose body cannot be re-read is returned as-is, one upstream call",
      "DefaultExecutor.execute — max_tokens floor retry (#1702) a refusal naming a floor triggers exactly one retry at that floor",
      "DefaultExecutor.execute — max_tokens floor retry (#1702) a request that sent no numeric max_tokens never retries on a refusal",
      "DefaultExecutor.execute — max_tokens floor retry (#1702) an OK response returns as-is with a single upstream call",
      "DefaultExecutor.refreshCredentials — grant plumbing a failed refresh (non-2xx) yields null, never a fabricated credential",
      "DefaultExecutor.refreshCredentials — grant plumbing a refresher that throws is caught and reported as null with an error log",
      "DefaultExecutor.refreshCredentials — grant plumbing claude refresh posts grant_type=refresh_token and maps the token response",
      "DefaultExecutor.refreshCredentials — grant plumbing refreshKiro posts the JSON refresh body with the kiro-cli UA",
      "DefaultExecutor.refreshCredentials — grant plumbing refreshWithForm keeps the old refresh token when the response omits one",
      "DefaultExecutor.refreshCredentials — grant plumbing returns null with no refreshToken and for providers without a refresher",
      "DefaultExecutor.refreshCredentials — provider-specific refreshers a non-2xx from a provider-specific refresher yields null (iflow, kimi)",
      "DefaultExecutor.refreshCredentials — provider-specific refreshers iflow: Basic auth from registry clientId:clientSecret, token response mapped",
      "DefaultExecutor.refreshCredentials — provider-specific refreshers kilocode: device-code flow has no refresh, returns null without any call",
      "DefaultExecutor.refreshCredentials — provider-specific refreshers kimi: posts the form grant to the registry refreshUrl with the stable device id",
      "DefaultExecutor.refreshCredentials — remaining grant rows codex: registry grant posts refresh_token, or fails closed without one",
      "DefaultExecutor.refreshCredentials — remaining grant rows gemini: registry grant posts refresh_token, or fails closed without one",
      "DefaultExecutor.refreshCredentials — remaining grant rows kimi-coding: shares its sibling's refresher and maps the token response",
      "DefaultExecutor.refreshCredentials — remaining grant rows refreshWithForm and refreshKiro return null on a non-2xx without fabricating credentials",
      "DefaultExecutor.transformRequest — cloudflare-ai max_tokens default (#1645) fills a positive default when the client omits max_tokens, and never overrides an explicit one",
      "DefaultExecutor.transformRequest — official OpenAI usage accounting does not inject stream_options when the client itself asked to stream",
      "DefaultExecutor.transformRequest — official OpenAI usage accounting drops client_metadata for a non-claude-format provider but keeps it for anthropic",
      "DefaultExecutor.transformRequest — official OpenAI usage accounting forces stream:true and injects stream_options.include_usage for a JSON client",
      "DefaultExecutor.transformRequest — official OpenAI usage accounting renames max_tokens → max_completion_tokens for gpt-5/o-series, value preserved",
      "DefaultExecutor.transformRequest — overlong tool-call IDs (official OpenAI) leaves an id at or under the limit untouched",
      "DefaultExecutor.transformRequest — overlong tool-call IDs (official OpenAI) normalizes an over-64-char id identically on the call and its tool result",
      "DefaultExecutor.transformRequest — remaining provider scopes gpt-5.6 chat models that reject tools+reasoning get reasoning_effort none, others keep it",
      "DefaultExecutor.transformRequest — remaining provider scopes mistral: final assistant message is marked prefix:true without mutating the caller's body",
      "DefaultExecutor.transformRequest — remaining provider scopes muse-spark models: a forced tool_choice is demoted to auto, with and without an effort suffix",
      "DefaultExecutor.transformRequest — remaining provider scopes openai-compatible responses transport: text object without format gets the API default",
      "DefaultExecutor.transformRequest — remaining provider scopes openai: a body without a messages array passes the tool-call id normalizer untouched"
    ],
    "tests/unit/defer-loading-cache-anchor-3567.test.js": [
      "a deferred tool never carries a cache anchor (#3567) anchors exactly one tool",
      "a deferred tool never carries a cache anchor (#3567) anchors nothing when every tool defers",
      "a deferred tool never carries a cache anchor (#3567) anchors the last NON-deferred tool when the last tool defers",
      "a deferred tool never carries a cache anchor (#3567) skips a deferred tool in the middle without disturbing it",
      "a deferred tool never carries a cache anchor (#3567) still anchors the last tool when none defer",
      "a deferred tool never carries a cache anchor (#3567) the prepare path applies the same rule"
    ],
    "tests/unit/dev-test-server-process-ownership.test.js": [
      "dev test server process and data ownership aborts credential sync when process ownership cannot be established",
      "dev test server process and data ownership allocates fresh state and only stops the process recorded with matching start time and command",
      "dev test server process and data ownership does not accept another process's healthy listener",
      "dev test server process and data ownership keeps credential cloning behind an explicit source and opt-in",
      "dev test server process and data ownership refuses an inherited DATA_DIR during normal startup",
      "dev test server process and data ownership refuses to signal an unrelated live PID",
      "dev test server process and data ownership rejects a credential clone target outside the isolated state namespace",
      "dev test server process and data ownership uses an allowlisted fake home and data directory for both build and server",
      "dev test server process and data ownership uses curl with config disabled and bounded deadlines"
    ],
    "tests/unit/developer-role-normalized-1028.test.js": [
      "a developer role never reaches a chat-completions target (#1028 #1038) keeps the instruction text, not just the role",
      "a developer role never reaches a chat-completions target (#1028 #1038) leaves user and assistant roles alone",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for kimi",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for ollama",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for openai",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for qwen",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for step",
      "a developer role never reaches a chat-completions target (#1028 #1038) normalises developer to system for zai",
      "a developer role never reaches a chat-completions target (#1028 #1038) still routes a top-level instructions string to a system turn"
    ],
    "tests/unit/devin-cli-executor.test.js": [
      "DevinCliExecutor — failure paths a JSON-RPC error response finishes with its code and message",
      "DevinCliExecutor — failure paths a non-ENOENT spawn error is reported verbatim",
      "DevinCliExecutor — failure paths a nonzero exit before any output finishes with the exit code as the error",
      "DevinCliExecutor — failure paths a session/update of type error finishes with the upstream message",
      "DevinCliExecutor — failure paths agent_stopped cause=error prefers errorMessage over the generic fallback",
      "DevinCliExecutor — failure paths an aborted signal kills the subprocess",
      "DevinCliExecutor — failure paths session/new without a sessionId is a terminal protocol error",
      "DevinCliExecutor — failure paths spawn ENOENT surfaces an install hint and terminates the stream",
      "DevinCliExecutor — message and workspace shapes a non-existent <cwd> candidate falls back to os.tmpdir",
      "DevinCliExecutor — message and workspace shapes an empty message list yields the (empty) sentinel prompt",
      "DevinCliExecutor — message and workspace shapes inlines claude-style content blocks: text, tool_use, tool_result, and system role",
      "DevinCliExecutor — message and workspace shapes resolves <cwd> from Responses-style input message items",
      "DevinCliExecutor — message and workspace shapes seeds claude-style tool_use/tool_result pairs into the MCP result map",
      "DevinCliExecutor — protocol fallbacks and noise tolerance a final session/prompt result with no streamed chunks is emitted from the result text",
      "DevinCliExecutor — protocol fallbacks and noise tolerance banner text and blank stdout lines are ignored, message_stop ends the turn",
      "DevinCliExecutor — protocol fallbacks and noise tolerance extracts the final text from the messages-array result shape",
      "DevinCliExecutor — protocol fallbacks and noise tolerance invalid DEVIN_MCP_SERVERS JSON is logged and does not create an MCP config",
      "DevinCliExecutor — transport surface CLI_DEVIN_BIN overrides binary discovery for the spawn",
      "DevinCliExecutor — transport surface declares the stdio pseudo-URL, empty headers, and a null transformRequest",
      "DevinCliExecutor ACP session/new auto-approves session/request_permission with the first allow option",
      "DevinCliExecutor ACP session/new bridges a client-tool MCP call to an OpenAI tool_use",
      "DevinCliExecutor ACP session/new completes the prompt without a -32602 Invalid params error",
      "DevinCliExecutor ACP session/new defaults session/new cwd to os.tmpdir when request has no workspace cwd",
      "DevinCliExecutor ACP session/new does not inject WINDSURF_API_KEY — devin-cli uses stored CLI creds (devin auth login)",
      "DevinCliExecutor ACP session/new does not set XDG_CONFIG_HOME when DEVIN_MCP_SERVERS is absent",
      "DevinCliExecutor ACP session/new emits agent_message_chunk content and skips agent_thought_chunk",
      "DevinCliExecutor ACP session/new exposes body.tools as an MCP server (sets XDG_CONFIG_HOME + writes script)",
      "DevinCliExecutor ACP session/new overrides the agent type via CLI_DEVIN_AGENT_TYPE",
      "DevinCliExecutor ACP session/new respects an explicit DEVIN_PERMISSION_MODE override",
      "DevinCliExecutor ACP session/new seeds MCP with tool_result from prior client round-trip",
      "DevinCliExecutor ACP session/new sends session/new with mcpServers as an array",
      "DevinCliExecutor ACP session/new sends session/prompt with prompt (not content) as an array",
      "DevinCliExecutor ACP session/new sets DEVIN_PERMISSION_MODE=bypass so tool calls don't hang on permission prompts",
      "DevinCliExecutor ACP session/new sets XDG_CONFIG_HOME when DEVIN_MCP_SERVERS is provided",
      "DevinCliExecutor ACP session/new spawns the default agent (with built-in tools) by default",
      "DevinCliExecutor ACP session/new uses client <cwd> env context for session/new and spawn"
    ],
    "tests/unit/devin-executor.test.js": [
      "Devin executor calls GetUserJwt before streaming GetChatMessage",
      "Devin executor emits a normalized error for a trailer frame",
      "Devin executor is registered as a specialized executor",
      "Devin executor maps tool calls and usage into the SSE stream",
      "Devin executor returns non-2xx chat responses for standard error handling"
    ],
    "tests/unit/devin-models.test.js": [
      "Devin model discovery decodes enabled model configs and filters disabled entries",
      "Devin model discovery decodes gzip-compressed discovery payloads",
      "Devin model discovery posts the Devin auth metadata to the discovery endpoint"
    ],
    "tests/unit/devin-oauth.test.js": [
      "Devin OAuth accepts a full pasted callback URL and validates state",
      "Devin OAuth builds a stateful PKCE authorization URL",
      "Devin OAuth exchanges the code as JSON and never creates a refresh token",
      "Devin registry exposes OAuth metadata and static fallback models"
    ],
    "tests/unit/devin-protocol.test.js": [
      "Devin protocol decodes text and thinking deltas",
      "Devin protocol decodes tool, usage, stop, and message deltas",
      "Devin protocol encodes OpenAI assistant tool_calls into the Devin prompt",
      "Devin protocol includes the IDE version required by GetUserJwt",
      "Devin protocol keeps incomplete frames for the next network read",
      "Devin protocol normalizes the session token once",
      "Devin protocol preserves multiple deltas carried in one protobuf payload",
      "Devin protocol reads JSON trailer errors",
      "Devin protocol rejects oversized Connect frames",
      "Devin protocol round-trips a compressed Connect frame"
    ],
    "tests/unit/diet-prune.test.js": [
      "chatCore diet wiring accepts diet events through the events allowlist",
      "chatCore diet wiring emits a truthful applied:false row when the epoch boundary blocks the stage",
      "chatCore diet wiring emits an applied:true diet row with truthful byte savings",
      "chatCore diet wiring keeps referenced tool_results (assistant quotes a long substring)",
      "chatCore diet wiring leaves tool_results untouched by default (flag off)",
      "chatCore diet wiring prunes expired tool_results below the cut when diet is on",
      "pruneExpiredToolResults keeps @@-only hunks too",
      "pruneExpiredToolResults keeps a block a later assistant turn quotes verbatim (>=64-char substring)",
      "pruneExpiredToolResults keeps a block whose tool_use id a later assistant turn mentions",
      "pruneExpiredToolResults keeps blocks carrying diff hunks",
      "pruneExpiredToolResults keeps blocks younger than minAgeTurns assistant turns",
      "pruneExpiredToolResults keeps error traces from failed tools (is_error + error text)",
      "pruneExpiredToolResults keeps payloads below minBlockChars",
      "pruneExpiredToolResults keeps the most recent assistant turn's own tool_results",
      "pruneExpiredToolResults never mutates blocks carrying cache_control",
      "pruneExpiredToolResults never mutates messages at or before the epoch cut",
      "pruneExpiredToolResults passes non-recognizable bodies through unchanged",
      "pruneExpiredToolResults preserves pair structure: message count, block counts, id linkage, tool_use untouched",
      "pruneExpiredToolResults prunes an expired, unreferenced tool_result to the expired stub",
      "pruneExpiredToolResults prunes both duplicates' older copies when three identical payloads exist",
      "pruneExpiredToolResults prunes error-text payloads when the tool did not report is_error",
      "pruneExpiredToolResults prunes older duplicates and names the retained turn; newest copy survives",
      "pruneExpiredToolResults prunes OpenAI-style role:'tool' string messages",
      "pruneExpiredToolResults skips unrecognized content shapes without throwing",
      "pruneExpiredToolResults skips with applied:false when the epoch cut is 0"
    ],
    "tests/unit/direct-connection-controls-ui.test.js": [
      "acknowledges header clearing so a later unrelated account save does not repeat it",
      "adopts a profile from its own field and reads the key back",
      "binds __clear__ directly and verifies the returned account snapshot",
      "binds __none__ directly and verifies the returned account snapshot",
      "binds strict-pool directly and verifies the returned account snapshot",
      "does not mount client setup before the key is opened on that task",
      "edits a quota threshold from its own field and takes a polled change back",
      "edits profile fields on row selection and preserves the reviewed version in the final write",
      "mounts client setup only when the key is open on that task, and saves expiry in place",
      "opens auth and model controls in one task step without calling providers",
      "reports an account control write whose saved value does not read back",
      "retains custom-proxy semantics without rendering the credential and blocks an incomplete readback",
      "sends only the field that changed when the allowlist is edited",
      "shows account policy directly and preserves both priority meanings without duplicating provider concurrency",
      "splits network tasks without opening dialogs or dispatching operations",
      "updates clean account options from saved state and preserves conflicting edited fields until reset"
    ],
    "tests/unit/direct-executor-connect-timeout.test.js": [
      "direct executor response-header deadlines 'github messages' clears after success",
      "direct executor response-header deadlines 'github messages' clears after unrelated network rejection",
      "direct executor response-header deadlines 'github messages' preserves the exact caller abort",
      "direct executor response-header deadlines 'github messages' uses provider override and clears",
      "direct executor response-header deadlines 'github responses' clears after success",
      "direct executor response-header deadlines 'github responses' clears after unrelated network rejection",
      "direct executor response-header deadlines 'github responses' preserves the exact caller abort",
      "direct executor response-header deadlines 'github responses' uses provider override and clears",
      "direct executor response-header deadlines 'Grok Web' applies the provider override to its generation fetch",
      "direct executor response-header deadlines 'Grok Web' clears on success and retains unrelated 502 conversion",
      "direct executor response-header deadlines 'Grok Web' preserves exact caller cancellation",
      "direct executor response-header deadlines 'Perplexity Web' applies the provider override to its generation fetch",
      "direct executor response-header deadlines 'Perplexity Web' clears on success and retains unrelated 502 conversion",
      "direct executor response-header deadlines 'Perplexity Web' preserves exact caller cancellation",
      "direct executor response-header deadlines 'vertex generation' clears after success",
      "direct executor response-header deadlines 'vertex generation' clears after unrelated network rejection",
      "direct executor response-header deadlines 'vertex generation' preserves the exact caller abort",
      "direct executor response-header deadlines 'vertex generation' uses provider override and clears",
      "direct executor response-header deadlines 'windsurf generation' clears after success",
      "direct executor response-header deadlines 'windsurf generation' clears after unrelated network rejection",
      "direct executor response-header deadlines 'windsurf generation' preserves the exact caller abort",
      "direct executor response-header deadlines 'windsurf generation' uses provider override and clears",
      "direct executor response-header deadlines Devin clears its deadline on success and unrelated rejection",
      "direct executor response-header deadlines Devin preserves caller abort on the final chat request",
      "direct executor response-header deadlines Devin times only the final chat request",
      "direct executor response-header deadlines GitHub escalation creates a fresh deadline for /responses",
      "direct executor response-header deadlines MiMo auth retry creates a fresh deadline",
      "direct executor response-header deadlines MiMo leaves bootstrap outside the deadline and times chat",
      "direct executor response-header deadlines MiMo preserves caller abort and clears on success or unrelated rejection",
      "direct executor response-header deadlines Zed preserves caller abort and clears on success or unrelated rejection",
      "direct executor response-header deadlines Zed refresh retry creates a fresh full deadline",
      "direct executor response-header deadlines Zed times only completions after discovery and token acquisition"
    ],
    "tests/unit/disabled-model-direct-request-577.test.js": [
      "a disabled model is refused on the direct path (#577) does not match the same model id under another provider",
      "a disabled model is refused on the direct path (#577) fails OPEN when the disabled list cannot be read",
      "a disabled model is refused on the direct path (#577) is false for a non-string, rather than throwing on the request path",
      "a disabled model is refused on the direct path (#577) leaves a sibling model of the same provider alone",
      "a disabled model is refused on the direct path (#577) reports a disabled alias/model as disabled",
      "a disabled model is refused on the direct path (#577) treats a bare id with no provider as not disabled",
      "the chat handler enforces it (#577) checks before combo expansion and returns a 404"
    ],
    "tests/unit/disabled-models-per-account-1527.test.js": [
      "disabled models are per account (#1527) a connection with no entries of its own inherits rather than seeing nothing",
      "disabled models are per account (#1527) a connection-scoped write does not disturb another connection",
      "disabled models are per account (#1527) an install with only provider-scoped entries still sees them",
      "disabled models are per account (#1527) clearing a connection's last entry does not fall back and re-disable it",
      "disabled models are per account (#1527) keeps the two accounts independent as each is edited",
      "disabled models are per account (#1527) leaves the whole-map read that /v1/models uses keyed by alias",
      "disabled models are per account (#1527) provider-wide writes still behave exactly as before",
      "disabled models are per account (#1527) re-enabling on one connection leaves the provider-wide set alone"
    ],
    "tests/unit/dispatch-response-hook.test.js": [
      "cancels unused output promptly while preserving the hook failure",
      "keeps a cancellation failure from replacing the accounting failure",
      "passes the exact response and optional verified nonacceptance without consuming it"
    ],
    "tests/unit/dispatcher-cache-lifecycle.test.js": [
      "dispatcher cache lifecycle brands only owned local refusals and can revoke proof after an earlier dispatch",
      "dispatcher cache lifecycle evicts an idle entry while preserving an older active stream",
      "dispatcher cache lifecycle gracefully waits for active shutdown and refuses subsequent admission",
      "dispatcher cache lifecycle quarantines failed idle cleanup without losing ownership or leaking its secret error",
      "dispatcher cache lifecycle rejects new identities while an acquisition or body is active without closing either",
      "dispatcher cache lifecycle releases failed construction so a later attempt can construct successfully",
      "dispatcher cache lifecycle reserves one cold construction synchronously and shares its exact result",
      "dispatcher cache lifecycle waits for graceful close before allocating a replacement and retains the slot"
    ],
    "tests/unit/dns-config-entry-management.test.js": [
      "addDNSEntry appends one 127.0.0.1 line per missing host and flushes DNS",
      "addDNSEntry is idempotent: no child process when every host is already present",
      "addDNSEntry maps a rejected sudo password onto an actionable message",
      "addDNSEntry pins the substring-match gap: a partial antigravity entry blocks the missing host",
      "addDNSEntry throws on an unknown tool without touching anything",
      "checkAllDNSStatus reports a tool active only when every one of its hosts is present",
      "checkAllDNSStatus reports every tool inactive when the hosts file is unreadable",
      "execWithPassword falls back to plain sh (no password on stdin) when sudo is absent",
      "execWithPassword pipes the password to `sudo -S sh -c <cmd>` and resolves stdout",
      "execWithPassword rejects with the child's stderr on nonzero exit",
      "execWithPassword rejects with the exit code when the child wrote nothing",
      "removeAllDNSEntries keeps going when one tool's removal fails",
      "removeAllDNSEntriesSync is silent when the hosts file does not exist (shutdown path)",
      "removeAllDNSEntriesSync rewrites the hosts file without any tool host and flushes synchronously",
      "removeAllDNSEntriesSync writes nothing when the file already carries no tool host",
      "removeDNSEntry filters the tool's lines out and flushes DNS",
      "removeDNSEntry is idempotent: no child process when nothing matches",
      "removeDNSEntry throws on an unknown tool"
    ],
    "tests/unit/dns-config-shell-availability.test.js": [
      "MITM DNS shell availability rejects cleanly when the no-sudo fallback has no sh shell"
    ],
    "tests/unit/docker-dev-compose-2828.test.js": [
      "docker dev override (#2828) declares a dev stage that the dev override targets",
      "docker dev override (#2828) docker-compose.dev.yml publishes every port on loopback only",
      "docker dev override (#2828) docker-compose.yml publishes every port on loopback only",
      "docker dev override (#2828) does not tag the dev build as the published image",
      "docker dev override (#2828) keeps production as the last stage, so a plain `docker build .` is unchanged",
      "docker dev override (#2828) shadows node_modules and .next so the container never writes them to the host checkout"
    ],
    "tests/unit/docker-healthcheck-3096.test.js": [
      "the image reports its own health (#3096) declares a HEALTHCHECK",
      "the image reports its own health (#3096) gives the standalone server time to boot before failing",
      "the image reports its own health (#3096) honours PORT so a remapped container still reports healthy",
      "the image reports its own health (#3096) probes the health route that actually exists",
      "the image reports its own health (#3096) uses node, not a tool the image deliberately removed"
    ],
    "tests/unit/docker-jwt-secret-startup-1544.test.js": [
      "JWT_SECRET is checked at startup (#1544) refuses to start and says why, instead of serving a dashboard that 500s",
      "JWT_SECRET is checked at startup (#1544) starts when the secret is in the environment",
      "JWT_SECRET is checked at startup (#1544) starts when the secret is only in the .env beside the server"
    ],
    "tests/unit/dockerignore-nested-2828.test.js": [
      ".dockerignore excludes nested copies too (#2828) excludes .next at any depth, not only the root one",
      ".dockerignore excludes nested copies too (#2828) excludes node_modules at any depth, not only the root one",
      ".dockerignore excludes nested copies too (#2828) keeps the .git exclusion in both forms, which it already had"
    ],
    "tests/unit/drain-decision-log.test.js": [
      "DRAIN decision lines (D-11) DELETE on a connection that is not draining stays silent",
      "DRAIN decision lines (D-11) DELETE that completes a drain emits DRAIN.end",
      "DRAIN decision lines (D-11) DRAIN.begin and DRAIN.end for one connection carry the same conn prefix",
      "DRAIN decision lines (D-11) idempotent POST on an already-draining connection stays silent",
      "DRAIN decision lines (D-11) POST that starts a drain emits DRAIN.begin with the connection prefix"
    ],
    "tests/unit/drain-swap-atomicity.test.js": [
      "manual drain verbs against a concurrent automation write an unraced POST then DELETE still round-trips with the ifMatch contract",
      "manual drain verbs against a concurrent automation write DELETE refuses with 412 when the document changed after its read, so a fresh automation drain is not ended blindly",
      "manual drain verbs against a concurrent automation write POST refuses with 412 when automation drained the account after the precondition read, and keeps the automation document",
      "swapDrainDoc writes only against the exact document it was given"
    ],
    "tests/unit/duplicate-finish-tool-input-3416.test.js": [
      "repeated finish_reason must not duplicate the tool input (refs #3416) closes the message exactly once when finish_reason repeats",
      "repeated finish_reason must not duplicate the tool input (refs #3416) emits the tool input exactly once for a well-behaved stream",
      "repeated finish_reason must not duplicate the tool input (refs #3416) emits the tool input exactly once when finish_reason repeats",
      "repeated finish_reason must not duplicate the tool input (refs #3416) still closes a plain text response exactly once"
    ],
    "tests/unit/economics-chart-visibility.test.js": [
      "keeps isolated observations visible after twenty rows while preserving unknown gaps"
    ],
    "tests/unit/economics-evidence.test.js": [
      "authoritative Economics evidence classifies additional attempts only with physical coverage and a logical identity",
      "authoritative Economics evidence does not decompose unknown or inconsistent cache detail",
      "authoritative Economics evidence filters entire explicit-reference populations and paginates all cohorts",
      "authoritative Economics evidence joins actual persisted capture to exact request metrics and captured rates",
      "authoritative Economics evidence keeps unknown identity cohorts selectable and shared filters conjunctive",
      "authoritative Economics evidence keeps USD provider reports and estimates separate, including reported zero",
      "authoritative Economics evidence preserves costs when retained request evidence expires without inventing identity",
      "authoritative Economics evidence refuses inconsistent identity enrichment without discarding the cost row",
      "authoritative Economics evidence refuses mismatched connectionId identity on an exact request id",
      "authoritative Economics evidence refuses mismatched model identity on an exact request id",
      "authoritative Economics evidence refuses mismatched provider identity on an exact request id",
      "authoritative Economics evidence rejects unsafe and contradictory filters before query execution",
      "authoritative Economics evidence retains an explicit counterfactual identifier through the real usage writer and reads exact evidence"
    ],
    "tests/unit/economics-lens.test.js": [
      "Economics ledger interactions compares selected named cohorts using explicit usable-sample denominators",
      "Economics ledger interactions does not silently display the full ledger under a selected cohort while its rows are unavailable",
      "Economics ledger interactions does not turn an unassigned identity into an unfiltered request",
      "Economics ledger interactions exposes pinning and switches grouping through controlled props",
      "Economics ledger interactions groups cohorts into compact pricing cards at the Everyday level and filters by a state chip",
      "Economics ledger interactions holds compared cohort positions during live updates without requiring an inspected row",
      "Economics ledger interactions inspects a cohort without filtering and exposes a separate exact drilldown",
      "Economics ledger interactions keeps aborted model evidence as an attempted identity",
      "Economics ledger interactions keeps cancelled model evidence as an attempted identity",
      "Economics ledger interactions keeps error model evidence as an attempted identity",
      "Economics ledger interactions keeps null model evidence as an attempted identity",
      "Economics ledger interactions keeps overview measures compact and exposes full token columns with server sorting",
      "Economics ledger interactions keeps pending model evidence as an attempted identity",
      "Economics ledger interactions labels persisted pending status and avoids an inverted page range when the result page is empty",
      "Economics ledger interactions renders load, error and empty states without stale economics",
      "Economics ledger interactions requests another server page and opens the exact record in the inspection dock",
      "Economics ledger interactions restores the exact record highlight and holds row positions while live quantities change",
      "Economics ledger interactions retains chosen token-column modes while a server sort or scope read temporarily unloads the rows",
      "Economics ledger interactions retains comparison snapshots across server pages and loading, but clears a changed population",
      "Economics ledger interactions retains unknown and inconsistent record data without inventing cache savings or a context join",
      "Economics ledger interactions shows an aggregate with no usable input samples as unknown even when its empty sum is zero",
      "Economics ledger interactions sorts the cohort book numerically and keeps named provider/model identities distinct",
      "Economics ledger interactions uses server sorting for the complete ledger and never reorders only the loaded page",
      "Economics numerical and identity boundaries does not coerce missing or unsupported numeric quantities into zero",
      "Economics numerical and identity boundaries keeps shared model names distinct across providers",
      "Economics numerical and identity boundaries uses persisted account names and a compact historical account fallback",
      "Economics numerical and identity boundaries warns separately about missing detail and invalid quantities"
    ],
    "tests/unit/economics-task-surface-ui.test.js": [
      "keeps identity fields direct and retains unapplied edits until Apply or Discard",
      "offers one task-tab level, preserving analytics, query scope, and pricing drafts across tabs"
    ],
    "tests/unit/economics-tools-model.test.js": [
      "operator Economics drafts keeps unknown rates absent and explicit zero, rejecting invalid monetary quantities",
      "operator Economics drafts targets one exact reservation with measured quantities and nonsecret evidence reference",
      "operator Economics drafts validates explicit attribution without inventing raw client identities"
    ],
    "tests/unit/economics-tools-ui.test.js": [
      "keeps separate pricing drafts when provider and model choices change",
      "reads a known budget key directly and preserves exact reservation drafts across reads and keys",
      "retains the pricing draft after refusal, then verifies a confirmed write by separate readback",
      "reviews exact reservation reconciliation and retains the draft after a refused write",
      "settles only after final confirmation and a separate matching budget readback"
    ],
    "tests/unit/economics-trend.test.js": [
      "economic time observations clamps partial buckets to the current exact scope without expanding or emptying a selection",
      "economic time observations preserves observed zero, unknown samples, invalid quantities and unobserved time gaps",
      "economic time observations uses actual health account display names instead of mislabeling configured accounts as historical",
      "reports separated years and prefers actual timestamps to bucket boundaries"
    ],
    "tests/unit/edge-tts-provider.test.js": [
      "fetchEdgeTtsVoices fetches the voice list once and serves the cache afterwards",
      "fetchEdgeTtsVoices throws when the voices endpoint fails",
      "synthesize defaults the voice and reuses the cached token on a second call",
      "synthesize does not re-bootstrap or resend before an upstream Retry-After deadline",
      "synthesize does not regenerate when x-should-retry denies replay",
      "synthesize does not regenerate when x-tokenproxy-replay-safe denies replay",
      "synthesize exposes noAuth",
      "synthesize marks a male voice as Male in the SSML",
      "synthesize omits the Cookie header when no cookies were set",
      "synthesize re-scrapes the token and retries once on 429",
      "synthesize rejects audio smaller than 1KiB as empty",
      "synthesize scrapes the token, posts SSML with cookie, and returns base64 mp3",
      "synthesize throws on a non-retryable TTS failure",
      "synthesize throws when the abuse-prevention token cannot be parsed",
      "synthesize throws when the translator page fetch fails",
      "synthesize throws with status and body when the retry also fails"
    ],
    "tests/unit/elevenlabs-tts-provider-options-3132.test.js": [
      "ElevenLabs honours caller-supplied provider options (#3132) a request cannot rewrite the text or the model the router resolved",
      "ElevenLabs honours caller-supplied provider options (#3132) behaves as before when no options are passed",
      "ElevenLabs honours caller-supplied provider options (#3132) carries a caller's voice_settings and output_format into the request body",
      "ElevenLabs honours caller-supplied provider options (#3132) keeps the hardcoded defaults for settings the caller did not send",
      "ElevenLabs honours caller-supplied provider options (#3132) leaves the query string clean when no output_format is given"
    ],
    "tests/unit/embed-reorder-role-alternation.test.js": [
      "reorderByRelevance role alternation keeps every user entry immediately followed by its original answer",
      "reorderByRelevance role alternation never places two consecutive entries with the same role"
    ],
    "tests/unit/embed-reorder.test.js": [
      "reorderByRelevance fails open on HTTP 500",
      "reorderByRelevance fails open on malformed response",
      "reorderByRelevance fails open on timeout",
      "reorderByRelevance fails open on zero vectors",
      "reorderByRelevance handles empty input",
      "reorderByRelevance keeps roles alternating and every answer next to its original question",
      "reorderByRelevance keys cached vectors on the endpoint, not the model name alone",
      "reorderByRelevance never moves the protected tail, extending it to keep a split pair whole",
      "reorderByRelevance orders most relevant pairs nearest to the tail boundary",
      "reorderByRelevance passes through on short queries without fetching",
      "reorderByRelevance passes through when no movable run reaches length 2",
      "reorderByRelevance pins tool-bearing entries and reorders segments independently",
      "reorderByRelevance reorders within a segment on both sides of a pinned entry",
      "reorderByRelevance replays a memoised order for known pairs and puts a newly appended pair last",
      "reorderByRelevance skips the fetch entirely on full cache hit",
      "reorderByRelevance truncates notes beyond 5 moved pairs"
    ],
    "tests/unit/embedding-required-input-type-1378.test.js": [
      "a required embedding parameter is defaulted, not dropped (#1378) a client that knows the field still wins",
      "a required embedding parameter is defaulted, not dropped (#1378) a model that does not declare it gets no input_type at all",
      "a required embedding parameter is defaulted, not dropped (#1378) accepts the bare id as well as the vendor-prefixed one",
      "a required embedding parameter is defaulted, not dropped (#1378) an unknown model on a declaring provider is left alone",
      "a required embedding parameter is defaulted, not dropped (#1378) defaults input_type for the model that declares it required",
      "a required embedding parameter is defaulted, not dropped (#1378) the rest of the body is unchanged"
    ],
    "tests/unit/embedding-usage-persistence.test.js": [
      "embedding usage persistence does not record inexact usage 0",
      "embedding usage persistence does not record inexact usage 1",
      "embedding usage persistence does not record inexact usage 2",
      "embedding usage persistence does not record inexact usage 3",
      "embedding usage persistence does not record inexact usage 4",
      "embedding usage persistence does not record inexact usage 5",
      "embedding usage persistence does not record inexact usage 6",
      "embedding usage persistence records exact provider usage for successful embedding requests"
    ],
    "tests/unit/embeddings-combo-1379.test.js": [
      "embeddings combo support (#1379) expands a combo name and answers from the first member that works",
      "embeddings combo support (#1379) falls through to the next member when the first provider fails",
      "embeddings combo support (#1379) gives each member its own copy of the request body",
      "embeddings combo support (#1379) reports a failure once every member has failed",
      "embeddings combo support (#1379) still 400s on a name that is neither a model nor a combo",
      "embeddings combo support (#1379) still runs a plain provider/model request without consulting combos"
    ],
    "tests/unit/embeddingsCore.test.js": [
      "buildEmbeddingsBody array input — passes array as-is",
      "buildEmbeddingsBody custom encoding_format is forwarded",
      "buildEmbeddingsBody gemini batch input forwards dimensions on each request",
      "buildEmbeddingsBody gemini single input forwards dimensions as outputDimensionality",
      "buildEmbeddingsBody no encoding_format in body → defaults to float",
      "buildEmbeddingsBody single string input — includes model and input, default encoding_format=float",
      "buildEmbeddingsHeaders openai — uses accessToken when apiKey is absent",
      "buildEmbeddingsHeaders openai → Authorization: Bearer, Content-Type: application/json",
      "buildEmbeddingsHeaders openai-compatible-* → Authorization: Bearer only (no extra headers)",
      "buildEmbeddingsHeaders openrouter → adds HTTP-Referer and X-Title headers",
      "buildEmbeddingsUrl antigravity (non-openai-compatible, no URL mapping) → 400",
      "buildEmbeddingsUrl ollama-local custom host → respects baseUrl from providerSpecificData",
      "buildEmbeddingsUrl ollama-local custom host with trailing slashes → strips trailing slashes cleanly",
      "buildEmbeddingsUrl ollama-local default host → http://localhost:11434/v1/embeddings",
      "buildEmbeddingsUrl openai → https://api.openai.com/v1/embeddings",
      "buildEmbeddingsUrl openai-compatible-* → uses baseUrl from providerSpecificData",
      "buildEmbeddingsUrl openai-compatible-* strips trailing slash from baseUrl",
      "buildEmbeddingsUrl openai-compatible-* without baseUrl → falls back to api.openai.com",
      "buildEmbeddingsUrl openrouter → https://openrouter.ai/api/v1/embeddings",
      "buildEmbeddingsUrl unsupported provider (e.g. gemini-cli) → 400 error, no fetch called",
      "buildEmbeddingsUrl vercel-ai-gateway → https://ai-gateway.vercel.sh/v1/embeddings",
      "handleEmbeddingsCore — input validation empty array input passes validation and reaches provider",
      "handleEmbeddingsCore — input validation empty string input passes validation",
      "handleEmbeddingsCore — input validation input is a number → 400 Bad Request",
      "handleEmbeddingsCore — input validation input is an object → 400 Bad Request",
      "handleEmbeddingsCore — input validation input is null → 400 Bad Request",
      "handleEmbeddingsCore — input validation missing input → 400 Bad Request",
      "handleEmbeddingsCore — provider error handling error result response has OpenAI-format error body",
      "handleEmbeddingsCore — provider error handling invalid JSON from provider → returns 502",
      "handleEmbeddingsCore — provider error handling network error (fetch throws) → returns 502 Bad Gateway",
      "handleEmbeddingsCore — provider error handling provider 400 → returns success=false with status 400",
      "handleEmbeddingsCore — provider error handling provider 429 → returns success=false with status 429",
      "handleEmbeddingsCore — provider error handling provider 500 → returns success=false with status 500",
      "handleEmbeddingsCore — success path calls onRequestSuccess callback on success",
      "handleEmbeddingsCore — success path does not call onRequestSuccess on provider error",
      "handleEmbeddingsCore — success path provider response with non-standard format is passed through as-is",
      "handleEmbeddingsCore — success path response body is valid OpenAI-format JSON",
      "handleEmbeddingsCore — success path response Content-Type is application/json",
      "handleEmbeddingsCore — success path response includes CORS header Access-Control-Allow-Origin: *",
      "handleEmbeddingsCore — success path returns success=true with Response on 200",
      "handleEmbeddingsCore — token refresh on 401/403 on 401 with no refresh token, falls back gracefully (no crash)",
      "handleEmbeddingsCore — token refresh on 401/403 on 401, attempts retry after refresh; succeeds if refresh gives new token"
    ],
    "tests/unit/empty-content-no-account-lock.test.js": [
      "an empty response fails the request without benching the account chat.js arms no empty-content lock either",
      "an empty response fails the request without benching the account does not tell the decision log it locked",
      "an empty response fails the request without benching the account keeps the lock on the upstream-error-in-content path",
      "an empty response fails the request without benching the account locates the empty-content branch it is asserting on",
      "an empty response fails the request without benching the account passes no forced cooldown deadline on the empty-content return",
      "an empty response fails the request without benching the account sseToJsonHandler arms no empty-content lock either"
    ],
    "tests/unit/empty-stream-single-model-2535.test.js": [
      "an empty single-model stream is refused, not forwarded (#2535) a non-SSE reply is passed through untouched (body stays null)",
      "an empty single-model stream is refused, not forwarded (#2535) a stream that carries only a role envelope reports no content",
      "an empty single-model stream is refused, not forwarded (#2535) a stream that carries text reports content and replays every byte",
      "an empty single-model stream is refused, not forwarded (#2535) accepted empty content returns a terminal error without dispatching another account",
      "an empty single-model stream is refused, not forwarded (#2535) an upstream error carried as content keeps its own reason and status",
      "an empty single-model stream is refused, not forwarded (#2535) the probe is the non-streaming path, which is why it stayed green",
      "an empty single-model stream is refused, not forwarded (#2535) the single-model success path peeks before returning"
    ],
    "tests/unit/empty-upstream-not-200-2727.test.js": [
      "an upstream that answered with nothing is not a success (#2727) accepts a tool call and no text",
      "an upstream that answered with nothing is not a success (#2727) accepts array content",
      "an upstream that answered with nothing is not a success (#2727) accepts reasoning only, which a thinking model can legitimately return alone",
      "an upstream that answered with nothing is not a success (#2727) accepts text content",
      "an upstream that answered with nothing is not a success (#2727) applies to every provider, not just the one that prompted it",
      "an upstream that answered with nothing is not a success (#2727) rejects a choice with no message",
      "an upstream that answered with nothing is not a success (#2727) rejects an empty choices array",
      "an upstream that answered with nothing is not a success (#2727) rejects an empty tool_calls array",
      "an upstream that answered with nothing is not a success (#2727) rejects empty string content",
      "an upstream that answered with nothing is not a success (#2727) rejects no choices key at all",
      "an upstream that answered with nothing is not a success (#2727) rejects null content and no tool calls",
      "an upstream that answered with nothing is not a success (#2727) rejects the exact reported body",
      "an upstream that answered with nothing is not a success (#2727) rejects whitespace-only content",
      "an upstream that answered with nothing is not a success (#2727) the failure it produces is a gateway error, not a success",
      "the Claude and Responses shapes keep their own emptiness rules a Claude tool_use block alone is useful",
      "the Claude and Responses shapes keep their own emptiness rules an empty Claude content array is not useful",
      "the Claude and Responses shapes keep their own emptiness rules an empty Responses output is not useful"
    ],
    "tests/unit/english-only-ui.test.js": [
      "English-only interface does not ship the language selector, locale endpoint, translation runtime or catalogs",
      "English-only interface ignores a saved %invalid locale while retaining user text",
      "English-only interface ignores a saved ar locale while retaining user text",
      "English-only interface ignores a saved fa locale while retaining user text",
      "English-only interface ignores a saved zh-CN locale while retaining user text",
      "English-only interface keeps number and relative-time formatting English after an external language change",
      "English-only interface renders sign-in in English and preserves multilingual SSO labels without loading catalogs",
      "English-only interface waits for a request before producing the operator document"
    ],
    "tests/unit/epoch-compact.test.js": [
      "autocompact aligns the drop window outward so a straddling pair is fully dropped",
      "autocompact does not mutate the frozen input",
      "autocompact extends the drop window outward rather than splitting a pair at the tail boundary",
      "autocompact fails closed when summarizeFn throws or returns no text",
      "autocompact fires at exactly 75% and replaces the dropped region with one synthetic summary",
      "autocompact refuses to fire when a pair straddles the epoch cut itself",
      "autocompact reports a distinct skip cause",
      "autocompact returns the input reference below the 75% trigger",
      "autocompact returns the input reference when windowTokens is unusable",
      "autocompact returns unchanged when there is nothing between the cut and the tail",
      "autocompact tracks OpenAI-style tool_calls/tool_call_id pairs across the tail boundary",
      "chatCore epoch cascade wiring accepts epochMicro/epochAuto events through the events allowlist",
      "chatCore epoch cascade wiring emits a truthful applied:false row when the epoch boundary blocks the stage",
      "chatCore epoch cascade wiring leaves messages untouched by default (flags off)",
      "chatCore epoch cascade wiring reports epoch_boundary when a stable epoch blocks epochAuto",
      "chatCore epoch cascade wiring skips epochAuto below the window trigger and reports window_pressure",
      "chatCore epoch cascade wiring stubs tool_result payloads below the cut when epochMicro is on",
      "computeEpochCutIndex returns 0 for a stable epoch (ce >= prevBytes * 0.5)",
      "computeEpochCutIndex returns 0 when ce or prevBytes is missing/non-finite",
      "computeEpochCutIndex returns the first index whose cumulative bytes exceed ce",
      "computeEpochCutIndex returns the last index when only the final message falls outside the prefix",
      "microcompact counts stubbed blocks, not messages",
      "microcompact does nothing when cut 0 leaves no mutable window",
      "microcompact leaves cache_control markers alone",
      "microcompact never touches messages at or before the epoch cut",
      "microcompact passes non-recognizable bodies through unchanged",
      "microcompact protects the last keepLastTurns messages",
      "microcompact returns the input reference when nothing is eligible",
      "microcompact stubs a tool_result payload at exactly minBlockChars and preserves pair structure",
      "microcompact stubs Anthropic text blocks and OpenAI-style string content",
      "placeholderEpochSummarizer builds a deterministic digest without a network call",
      "placeholderEpochSummarizer omits sections that have no entries"
    ],
    "tests/unit/error-config-abuse-rule.test.js": [
      "abuse-prevention backoff rule backs off with escalating level on 'to prevent abuse' error text",
      "abuse-prevention backoff rule unrelated 403 text uses the plain 403 status rule, no backoff escalation"
    ],
    "tests/unit/estimated-usage-honest.test.js": [
      "estimated usage is reported without padding keeps the estimate self-consistent so total equals its parts",
      "estimated usage is reported without padding reports the Claude shape exactly as counted",
      "estimated usage is reported without padding reports the OpenAI shape exactly as counted"
    ],
    "tests/unit/everyday-navigation.test.js": [
      "everyday navigation exposes every retained destination in advanced without duplicate or obsolete entries",
      "everyday navigation falls back to daily work for unknown preferences and paths",
      "everyday navigation keeps current specialist destinations reachable without changing the selected page"
    ],
    "tests/unit/evidence-export-size.test.js": [
      "Final UTF-8 evidence size accepts exactly 8 MiB and refuses the next byte without shortening evidence",
      "Final UTF-8 evidence size counts multibyte values as UTF-8 rather than string code units",
      "Final UTF-8 evidence size includes indentation and preview metadata in the download boundary",
      "Final UTF-8 evidence size refuses a worker result pushed over the cap by the freshness envelope",
      "Final UTF-8 evidence size returns the exact allowed serialized response with no-store"
    ],
    "tests/unit/executor-const-guard.test.js": [
      "antigravity retry (intentional change: 429=6, 503=3) 429 attempts = 6",
      "antigravity retry (intentional change: 429=6, 503=3) 503 attempts = 3",
      "compat base URLs / version ANTHROPIC_API_VERSION",
      "compat base URLs / version ANTHROPIC_COMPAT_BASE",
      "compat base URLs / version OPENAI_COMPAT_BASE",
      "default token limits max/min",
      "provider baseUrl const (full path, no trailing slash) mimo-free full path",
      "provider baseUrl const (full path, no trailing slash) opencode no trailing slash"
    ],
    "tests/unit/executor-request-state-3169.test.js": [
      "executor request state (#3169) derives Gemini CLI headers from the request model rather than shared executor state",
      "executor request state (#3169) derives OpenCode Zen authentication from the request model rather than shared executor state",
      "executor request state (#3169) does not send one Grok connection's device id on the next anonymous request"
    ],
    "tests/unit/executors/xai-executor.test.js": [
      "XaiExecutor suffix parsing keeps none reasoning_effort for grok-4",
      "XaiExecutor suffix parsing keeps reasoning_effort for grok-4.3",
      "XaiExecutor suffix parsing parses -high suffix and strips it from model",
      "XaiExecutor suffix parsing parses -low suffix",
      "XaiExecutor suffix parsing strips none reasoning_effort for Grok Composer",
      "XaiExecutor suffix parsing strips reasoning_effort for grok-build"
    ],
    "tests/unit/fal-apikey-validate-993.test.js": [
      "POST /api/providers/validate for image-only providers accepts a fal key instead of answering 'validation not supported'",
      "POST /api/providers/validate for image-only providers accepts the same shape for the other image providers with no declared auth scheme",
      "POST /api/providers/validate for image-only providers still refuses a request with no key at all",
      "POST /api/providers/validate for image-only providers still rejects a bad key for a media provider that declares its auth scheme"
    ],
    "tests/unit/fetch-provider-error-branches.test.js": [
      "exa provider maps a transport failure to 502",
      "exa provider propagates upstream error with fallback message on non-json body",
      "exa provider returns normalized data with title and x-api-key header",
      "generic dispatch guards maps a thrown provider error to a 502 and logs it",
      "generic dispatch guards rejects a missing provider",
      "generic dispatch guards rejects a missing url",
      "generic dispatch guards rejects an unsupported provider",
      "jina-reader branches maps a transport failure to 502",
      "jina-reader branches parses a markdown heading as the title when no Title: line exists",
      "jina-reader branches returns a null title when neither metadata nor heading is present",
      "ollama bounded-body edge paths classifies a caller abort fired during transport dispatch as client-aborted",
      "ollama bounded-body edge paths classifies a synchronously-throwing body reader as a transport error",
      "ollama bounded-body edge paths rejects a JSON array payload as an invalid response",
      "ollama bounded-body edge paths rejects a links entry that is not a parseable URL",
      "ollama bounded-body edge paths rejects a success body whose content-length exceeds the cap",
      "ollama bounded-body edge paths treats a bodyless response as invalid JSON",
      "ollama bounded-body edge paths truncates an oversized error body and keeps the generic message",
      "tavily provider handles an empty results array",
      "tavily provider maps a timeout abort to 504",
      "tavily provider propagates an upstream error status and message",
      "tavily provider returns normalized data and truncates to maxCharacters"
    ],
    "tests/unit/fetch-success-clears-account.test.js": [
      "web fetch account state clears a stale provider lock after a successful fetch"
    ],
    "tests/unit/fetch-with-timeout.test.js": [
      "fetchWithTimeout aborts the request when the deadline passes, not just the wait",
      "fetchWithTimeout clears its timer once the response arrives",
      "fetchWithTimeout clears its timer when fetch rejects",
      "fetchWithTimeout defaults to a ten second deadline",
      "fetchWithTimeout forwards method, headers and body unchanged",
      "fetchWithTimeout leaves a caller-supplied signal in charge",
      "provider validation routes have no un-deadlined probe src/app/api/provider-nodes/validate/route.js calls fetch only through fetchWithTimeout",
      "provider validation routes have no un-deadlined probe src/app/api/providers/validate/route.js calls fetch only through fetchWithTimeout"
    ],
    "tests/unit/file-block-routing.test.js": [
      "file/document block support claude: non-pdf file is dropped (not a document)",
      "file/document block support claude: openai file (pdf) -> document block",
      "file/document block support gemini: ignores non-data-uri file",
      "file/document block support gemini: openai file block -> inlineData",
      "file/document block support schema: file is a valid openai content type"
    ],
    "tests/unit/finish-reason-concern.test.js": [
      "enum literals (catch drift) CLAUDE_STOP literals",
      "enum literals (catch drift) GEMINI_FINISH literals",
      "enum literals (catch drift) OPENAI_FINISH literals",
      "fromOpenAIFinish round-trip - claude length -> max_tokens",
      "fromOpenAIFinish round-trip - claude tool_calls -> tool_use",
      "toOpenAIFinish - claude end_turn -> stop",
      "toOpenAIFinish - claude max_tokens -> length",
      "toOpenAIFinish - claude tool_use -> tool_calls",
      "toOpenAIFinish - commandcode tool-calls -> tool_calls",
      "toOpenAIFinish - commandcode unknown passthrough",
      "toOpenAIFinish - gemini BLOCKLIST -> content_filter",
      "toOpenAIFinish - gemini MAX_TOKENS -> length",
      "toOpenAIFinish - gemini OTHER -> stop",
      "toOpenAIFinish - gemini PROHIBITED_CONTENT -> content_filter",
      "toOpenAIFinish - gemini RECITATION -> content_filter",
      "toOpenAIFinish - gemini SAFETY -> content_filter",
      "toOpenAIFinish - gemini STOP -> stop",
      "toOpenAIFinish - gemini UNKNOWN_XYZ -> stop",
      "toOpenAIFinish - kiro tool_use -> tool_calls",
      "toOpenAIFinish - ollama length -> length",
      "toOpenAIFinish - ollama max_tokens -> length",
      "toOpenAIFinish - ollama tool_calls -> tool_calls",
      "toOpenAIFinish - ollama unknown_xyz -> stop"
    ],
    "tests/unit/firecrawl-selfhosted.test.js": [
      "Firecrawl providers environment variable overrides respects FIRECRAWL_DEFAULT_FORMAT",
      "Firecrawl providers environment variable overrides respects FIRECRAWL_TIMEOUT_MS",
      "Firecrawl providers firecrawl_custom provider does not send an Authorization header",
      "Firecrawl providers firecrawl_custom provider falls back to the default local URL when FIRECRAWL_BASE_URL is not set",
      "Firecrawl providers firecrawl_custom provider returns a clear error when the instance is unreachable",
      "Firecrawl providers firecrawl_custom provider returns a clear error when the instance returns a non-2xx response",
      "Firecrawl providers firecrawl_custom provider uses FIRECRAWL_BASE_URL and hits /v2/scrape",
      "Firecrawl providers official firecrawl provider rejects when API key is missing",
      "Firecrawl providers official firecrawl provider uses cloud URL and sends Authorization: Bearer <key>"
    ],
    "tests/unit/fish-audio-tts.test.js": [
      "Fish Audio TTS provider exposes the four documented models",
      "Fish Audio TTS provider is registered as a TTS-only apikey provider",
      "Fish Audio TTS provider is visible to the generic dispatcher, which reads AI_PROVIDERS",
      "Fish Audio TTS provider keeps registry ids and aliases unique",
      "Fish Audio TTS request shape defaults to the free model when none is given",
      "Fish Audio TTS request shape maps the voice onto reference_id, and omits it when unset",
      "Fish Audio TTS request shape returns base64 audio with its format",
      "Fish Audio TTS request shape sends the model as an HTTP header, not in the body",
      "Fish Audio TTS request shape surfaces the upstream error message"
    ],
    "tests/unit/force-stream-config.test.js": [
      "forceStream provider config keeps forced-stream providers streaming for JSON clients when body.stream is false",
      "forceStream provider config keeps forced-stream providers streaming for JSON clients when body.stream is undefined",
      "forceStream provider config only openai/codex/kimi force streaming",
      "forceStream provider config syncs negotiated stream:true into a same-format openai→kimi JSON-client body"
    ],
    "tests/unit/free-model-sync-scheduler.test.js": [
      "configureFreeModelSync scheduler does nothing when disabled",
      "configureFreeModelSync scheduler runs immediately and again on the configured cadence when enabled",
      "configureFreeModelSync scheduler startFreeModelSync reads the interval from settings; stop cancels the timer",
      "FREE_MODEL_SYNC_FIXTURE_BASE redirect falls back to the original url when the base is unparseable",
      "FREE_MODEL_SYNC_FIXTURE_BASE redirect rewrites catalog origins to the fixture base, preserving paths",
      "getFreeModelSyncStatus reports config, targets and per-provider catalog state",
      "runFreeModelSync failure paths isolates an auto-combo update failure and skips deleted combos",
      "runFreeModelSync failure paths records per-provider HTTP failures without stopping the pass",
      "runFreeModelSync failure paths reports a top-level error when settings cannot be read"
    ],
    "tests/unit/free-model-sync.test.js": [
      "buildModelsList free-catalog merge hides disabled free models and non-LLM kinds under llm kindFilter",
      "buildModelsList free-catalog merge lists synced free-provider models without any stored connection",
      "getSyncTargets excludes apikey-category providers even when they ship modelsFetcher",
      "getSyncTargets includes an oauth-category provider only when its own fetcher is a *-free contract (#1995)",
      "getSyncTargets orders targets by registry priority (openrouter before opencode)",
      "getSyncTargets selects free/freeTier providers that expose a filterable modelsFetcher",
      "normalizeFreeModelSyncConfig coerces enabled to boolean and rejects non-whitelisted intervals",
      "normalizeFreeModelSyncConfig defaults to disabled / 4h / no combos on empty input",
      "normalizeFreeModelSyncConfig keeps only string combo ids",
      "runFreeModelSync isolates per-provider failures — one dead endpoint keeps the others",
      "runFreeModelSync never deletes customs on first run (no previous snapshot)",
      "runFreeModelSync persists filtered catalogs, diffs customs and rewrites auto-combos",
      "runFreeModelSync sends registry transport headers to the opencode endpoint",
      "runFreeModelSync skips when a run is already in flight"
    ],
    "tests/unit/free-tier-docs-current-2661.test.js": [
      "free-tier provider documentation is current (#2661) Gemini CLI is marked shut down and names its replacement",
      "free-tier provider documentation is current (#2661) Kiro is documented as paid with its credit cap, not as unlimited free",
      "free-tier provider documentation is current (#2661) Qwen Code's free OAuth tier is marked discontinued with its date",
      "free-tier provider documentation is current (#2661) the README does not carry a second, drifting copy",
      "free-tier provider documentation is current (#2661) they sit under a heading that says not to plan around them"
    ],
    "tests/unit/fusion-media-preserved-3378.test.js": [
      "fusion panel bodies keep image blocks (#3378) carries an image nested inside a tool_result's own content",
      "fusion panel bodies keep image blocks (#3378) carries an image on an OpenAI-format tool-role message",
      "fusion panel bodies keep image blocks (#3378) carries an image that shares a message with a tool_result",
      "fusion panel bodies keep image blocks (#3378) leaves a pure-text tool turn as a plain string"
    ],
    "tests/unit/fusion-strip-stream-options-3024.test.js": [
      "Fusion strips stream_options (#3024) does not throw for a 2-model fusion with stream_options present",
      "Fusion strips stream_options (#3024) removes stream_options before fanning out to panel models"
    ],
    "tests/unit/fusion-trailing-turn-3425.test.js": [
      "a fusion panel body never ends on a non-user turn (#3425) closes a trailing assistant turn on openai messages",
      "a fusion panel body never ends on a non-user turn (#3425) closes a trailing assistant turn on responses input",
      "a fusion panel body never ends on a non-user turn (#3425) closes a trailing model turn on gemini contents",
      "a fusion panel body never ends on a non-user turn (#3425) closes a trailing system turn",
      "a fusion panel body never ends on a non-user turn (#3425) leaves a body that already ends on a user turn alone"
    ],
    "tests/unit/gateway-401-source-1160.test.js": [
      "a gateway-raised 401 says so (#1160) declares one constant rather than repeating the literal",
      "a gateway-raised 401 says so (#1160) every 401 the guard raises carries it",
      "a gateway-raised 401 says so (#1160) the field is additive, never replacing error",
      "a gateway-raised 401 says so (#1160) the messages that already distinguished the cases are unchanged"
    ],
    "tests/unit/gemini-3.7-antigravity.test.js": [
      "Gemini 3.7 Flash Support & Config (#3286, #3281) defines pricing matching gemini-3.6-flash baseline",
      "Gemini 3.7 Flash Support & Config (#3286, #3281) registers gemini-3.7-flash in gemini provider registry",
      "Gemini 3.7 Flash Support & Config (#3286, #3281) registers gemini-3.7-flash tiered models in antigravity provider registry",
      "Gemini 3.7 Flash Support & Config (#3286, #3281) resolves capabilities correctly for gemini-3.7 models with official limits"
    ],
    "tests/unit/gemini-36-integration.test.js": [
      "Gemini 3.6 Antigravity tiers maps the high tier to the shared upstream model with matching thinking level",
      "Gemini 3.6 Antigravity tiers maps the low tier to the shared upstream model with matching thinking level",
      "Gemini 3.6 Antigravity tiers maps the medium tier to the shared upstream model with matching thinking level",
      "Gemini 3.6 catalogs and pricing exposes the direct Gemini API models and their pricing",
      "Gemini 3.6 catalogs and pricing keeps the standalone CLI Gemini catalog synchronized",
      "Gemini 3.6 MITM model extraction defaults invalid or missing thinking levels to medium",
      "Gemini 3.6 MITM model extraction exports the model extractor from the side-effect-free MITM config module",
      "Gemini 3.6 MITM model extraction extracts the high thinking tier",
      "Gemini 3.6 MITM model extraction extracts the low thinking tier",
      "Gemini 3.6 MITM model extraction extracts the medium thinking tier",
      "Gemini Cloud Code endpoint isolation fails fast when onboardUser returns done:true without a project id",
      "Gemini Cloud Code endpoint isolation keeps Gemini CLI on the official cloudcode host",
      "Gemini Cloud Code endpoint isolation uses the prod cloudcode host for Antigravity discovery but daily for chat"
    ],
    "tests/unit/gemini-cli-ansi-sanitization.test.js": [
      "Gemini CLI ANSI stream sanitization keeps a Gemini CLI function call beside ANSI-only text",
      "Gemini CLI ANSI stream sanitization leaves generic Gemini parsing and translation unchanged",
      "Gemini CLI ANSI stream sanitization parses a Gemini CLI data frame after terminal controls before data",
      "Gemini CLI ANSI stream sanitization recognizes Gemini CLI DONE after a terminal control prefix",
      "Gemini CLI ANSI stream sanitization recognizes Gemini CLI DONE after single-character and C1 controls",
      "Gemini CLI ANSI stream sanitization removes all raw controls from Gemini CLI JSON before parsing",
      "Gemini CLI ANSI stream sanitization removes decoded terminal controls from unsigned and signed thought text",
      "Gemini CLI ANSI stream sanitization removes terminal controls embedded in Gemini CLI JSON before parsing",
      "Gemini CLI ANSI stream sanitization sanitizes a forced non-stream Gemini CLI response through the shared parser"
    ],
    "tests/unit/gemini-cli-sse-framing-2273.test.js": [
      "gemini-cli SSE framing (#2273) keeps ordinary whitespace, which SSE JSON-escapes anyway",
      "gemini-cli SSE framing (#2273) leaves generic gemini text byte-preserved",
      "gemini-cli SSE framing (#2273) parses a gc/ data line that carries wire control bytes",
      "gemini-cli SSE framing (#2273) strips ANSI and C0/C1 controls out of gc/ text",
      "gemini-cli SSE framing (#2273) terminates every emitted frame with a blank line"
    ],
    "tests/unit/gemini-colocated-function-response-2394.test.js": [
      "gemini to openai keeps parts co-located with a functionResponse (#2394) emits the tool result before the message it was co-located with",
      "gemini to openai keeps parts co-located with a functionResponse (#2394) keeps a functionCall that shares a content with a functionResponse",
      "gemini to openai keeps parts co-located with a functionResponse (#2394) keeps text co-located with a functionResponse under the original turn role",
      "gemini to openai keeps parts co-located with a functionResponse (#2394) still flattens several parallel functionResponses into one message each"
    ],
    "tests/unit/gemini-format-helpers.test.js": [
      "cleanJSONSchemaForAntigravity — remaining phases collapses a type array to its first non-null entry, or string when all null",
      "cleanJSONSchemaForAntigravity — remaining phases converts const to enum with string values and inferred type",
      "cleanJSONSchemaForAntigravity — remaining phases exposes the default safety settings with every category set to OFF",
      "cleanJSONSchemaForAntigravity — remaining phases filters required entries whose property was dropped, deleting an empty list",
      "cleanJSONSchemaForAntigravity — remaining phases flattens anyOf to the best non-null branch, preferring object > array > scalar",
      "cleanJSONSchemaForAntigravity — remaining phases flattens oneOf the same way",
      "cleanJSONSchemaForAntigravity — remaining phases gives an empty object schema nested in properties the same placeholder",
      "cleanJSONSchemaForAntigravity — remaining phases gives an empty schema {} an object type with a reason placeholder",
      "cleanJSONSchemaForAntigravity — remaining phases handles array-valued schema nodes (items as tuple) without touching property names",
      "cleanJSONSchemaForAntigravity — remaining phases infers type:object when properties exist without a type",
      "cleanJSONSchemaForAntigravity — remaining phases keeps an explicit type when stringifying enum values",
      "cleanJSONSchemaForAntigravity — remaining phases merges allOf properties and required lists without duplicates",
      "cleanJSONSchemaForAntigravity — remaining phases passes through non-object input",
      "cleanJSONSchemaForAntigravity — remaining phases strips every unsupported keyword and x- vendor fields, but not property NAMES",
      "convertOpenAIContentToParts converts AI SDK image parts, data URI and http variants",
      "convertOpenAIContentToParts converts an http(s) image URL to fileData",
      "convertOpenAIContentToParts converts audio_url and file data URIs to inlineData",
      "convertOpenAIContentToParts converts input_audio with mp3 mapped to audio/mpeg and default wav",
      "convertOpenAIContentToParts converts text and data-URI image blocks",
      "convertOpenAIContentToParts drops unrecognized blocks",
      "convertOpenAIContentToParts returns no parts for non-string non-array content",
      "convertOpenAIContentToParts wraps a plain string as one text part",
      "extractTextContent joins text blocks with the separator and ignores other blocks",
      "extractTextContent passes a string through",
      "extractTextContent returns empty string for anything else",
      "id generators generateProjectId is adjective-noun-hex5",
      "id generators generateRequestId is agent-prefixed and unique",
      "id generators generateSessionId is a UUID followed by a timestamp",
      "sanitizeFunctionResponseResult / tryParseJSON returns primitives untouched",
      "sanitizeFunctionResponseResult / tryParseJSON rewrites keys with $ # / and definitions, recursively and in arrays",
      "sanitizeFunctionResponseResult / tryParseJSON tryParseJSON parses and sanitizes; returns null-ish on invalid JSON"
    ],
    "tests/unit/gemini-native-endpoint.test.js": [
      "Gemini native v1beta endpoint accepts Google-style client keys without forwarding them upstream",
      "Gemini native v1beta endpoint does not forward stale compression headers from native upstream responses",
      "Gemini native v1beta endpoint does not hijack provider-prefixed non-Gemini audio requests",
      "Gemini native v1beta endpoint does not mark Gemini credentials unavailable when the native client aborts",
      "Gemini native v1beta endpoint falls back to the next Gemini credential when native fetch times out before headers",
      "Gemini native v1beta endpoint keeps non-audio Gemini requests on the existing chat conversion path",
      "Gemini native v1beta endpoint lists Gemini TTS models using standard Google model names",
      "Gemini native v1beta endpoint passes Gemini AUDIO generateContent requests through to Google's native endpoint",
      "Gemini native v1beta endpoint preserves an already-aborted caller signal when rebuilding a Gemini request",
      "Gemini native v1beta endpoint returns 502 for native fetch failures when credential fallback is not allowed"
    ],
    "tests/unit/gemini-parallel-tool-results-2393.test.js": [
      "parallel Gemini tool results all survive (#2393) a single result still produces a single message, unchanged",
      "parallel Gemini tool results all survive (#2393) carries each result's own payload, not the first repeated",
      "parallel Gemini tool results all survive (#2393) falls back to a name-derived id when the part carries none",
      "parallel Gemini tool results all survive (#2393) keeps every functionResponse in one content",
      "parallel Gemini tool results all survive (#2393) leaves ordinary text and tool calls alone"
    ],
    "tests/unit/gemini-response-format-2003.test.js": [
      "openai response_format maps onto Gemini generationConfig (#2003) a malformed json_schema falls through rather than sending a null schema",
      "openai response_format maps onto Gemini generationConfig (#2003) json_object asks for JSON without inventing a schema",
      "openai response_format maps onto Gemini generationConfig (#2003) json_schema sets both the mime type and the schema",
      "openai response_format maps onto Gemini generationConfig (#2003) no response_format leaves generationConfig untouched",
      "openai response_format maps onto Gemini generationConfig (#2003) the caller's schema is not mutated"
    ],
    "tests/unit/gemini-retired-models-2788.test.js": [
      "Gemini catalog carries no id Google has already shut down (#2788) embedding-001 (shut down 2025-10-30) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) every image entry is a GA id, not a retired preview",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-2.0-flash (shut down 2026-06-01) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-2.5-flash is kept — Google announces no shutdown date for it",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-2.5-flash-lite is kept — Google announces no shutdown date for it",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-2.5-pro is kept — Google announces no shutdown date for it",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3-pro-image is offered in its place",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3-pro-image-preview (shut down 2026-06-25) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3.1-flash-image is offered in its place",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3.1-flash-image-preview (shut down 2026-06-25) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3.1-flash-lite is offered in its place",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3.1-flash-lite-preview (shut down 2026-05-25) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-3.6-flash is offered in its place",
      "Gemini catalog carries no id Google has already shut down (#2788) gemini-embedding-2 is offered in its place",
      "Gemini catalog carries no id Google has already shut down (#2788) text-embedding-004 (shut down 2026-01-14) is gone",
      "Gemini catalog carries no id Google has already shut down (#2788) the STT list still offers a live model per tier"
    ],
    "tests/unit/gemini-schema-name-map-2336.test.js": [
      "gemini schema cleaner treats properties as a name map (#2336) does not inject type:string into a parameter map holding a key named enum",
      "gemini schema cleaner treats properties as a name map (#2336) does not read a parameter named const as the const keyword",
      "gemini schema cleaner treats properties as a name map (#2336) does not turn an empty properties map into a schema",
      "gemini schema cleaner treats properties as a name map (#2336) is idempotent",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named allOf as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named anyOf as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named const as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named description as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named enum as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named items as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named oneOf as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named properties as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named required as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) keeps a parameter named type as a parameter",
      "gemini schema cleaner treats properties as a name map (#2336) still applies the placeholder to a genuinely empty object schema",
      "gemini schema cleaner treats properties as a name map (#2336) still converts a real const keyword to an enum"
    ],
    "tests/unit/gemini-schema-string-property-1564.test.js": [
      "a property whose value is not a schema is repaired (#1564) a known type name becomes the schema it was meant to be",
      "a property whose value is not a schema is repaired (#1564) a property legitimately NAMED like a keyword is untouched",
      "a property whose value is not a schema is repaired (#1564) a well-formed schema is unchanged by the new phase",
      "a property whose value is not a schema is repaired (#1564) an unusable value is dropped rather than guessed at",
      "a property whose value is not a schema is repaired (#1564) dropping a property also drops it from required",
      "a property whose value is not a schema is repaired (#1564) every JSON Schema type name is accepted",
      "a property whose value is not a schema is repaired (#1564) it reaches nested properties, not just the top level"
    ],
    "tests/unit/gemini-thought-signature-3646.test.js": [
      "Gemini thoughtSignature survives the tool-call round trip (#3646) carries it through the Antigravity Cloud Code envelope too",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) does not leak the signature into the client-visible tool call id",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) does the same on the non-streaming path",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) keeps a signature a Gemini-format client already persisted in its history",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) replays the streamed call under its own signature, not the placeholder",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) still falls back to the placeholder for a call it never signed",
      "Gemini thoughtSignature survives the tool-call round trip (#3646) the sanity check: the fixture signature is not one of the placeholders"
    ],
    "tests/unit/gemini-tool-name-collision-3622.test.js": [
      "Gemini function name collisions (#3622) is deterministic, so a redeclared tool keeps the same name",
      "Gemini function name collisions (#3622) keeps every emitted name inside Gemini's 64-character limit",
      "Gemini function name collisions (#3622) keeps two long names that share a 64-char prefix distinct",
      "Gemini function name collisions (#3622) leaves a short name untouched and adds no map entry for it",
      "Gemini function name collisions (#3622) maps each emitted name back to the one the client declared",
      "Gemini function name collisions (#3622) uses inputs that genuinely collide under plain truncation"
    ],
    "tests/unit/gemini-tool-result-turns-3055.test.js": [
      "Gemini tool-result turns (#3055) keeps a functionResponse turn separate from the user text that follows",
      "Gemini tool-result turns (#3055) keeps an empty-string tool result instead of dropping the response",
      "Gemini tool-result turns (#3055) still merges ordinary consecutive user text turns"
    ],
    "tests/unit/gemini-tts.test.js": [
      "Gemini TTS exposes current Gemini TTS models in the TTS catalog",
      "Gemini TTS preserves an explicit Gemini TTS model and voice pair",
      "Gemini TTS uses the default Gemini TTS model when only a voice is provided"
    ],
    "tests/unit/gemini-usage-projectid.test.js": [
      "Gemini CLI usage project id resolution normalizes project objects returned by loadCodeAssist",
      "Gemini CLI usage project id resolution returns actionable guidance when no project id is available",
      "Gemini CLI usage project id resolution uses the projectId stored on the provider connection"
    ],
    "tests/unit/gemini-veo-video-3656.test.js": [
      "create and poll (#3656) does not report a job it cannot address",
      "create and poll (#3656) never re-sends a creation POST after a network error",
      "create and poll (#3656) passes an upstream error status and text through",
      "create and poll (#3656) polls the operation name it was given, not a base plus an id",
      "create and poll (#3656) posts predictLongRunning with the api-key header and returns the operation as the request id",
      "create and poll (#3656) refuses without a key instead of calling upstream",
      "reading an operation (#3656) calls a done operation with no video a failure, not a completed job",
      "reading an operation (#3656) reads the other shapes the preview API returns",
      "reading an operation (#3656) reports pending while it is not done",
      "reading an operation (#3656) returns the video URI in the envelope xAI already uses",
      "reading an operation (#3656) surfaces an operation error rather than reporting completion",
      "routing (#3656) is registered under the provider the registry names",
      "routing (#3656) owns its own poll ids and no one else's",
      "routing (#3656) the registry declares video for gemini with the three Veo models",
      "the request body Veo takes (#3656) carries duration and negative prompt when asked",
      "the request body Veo takes (#3656) derives an aspect ratio from an OpenAI-style size",
      "the request body Veo takes (#3656) omits parameters entirely when nothing was asked for",
      "the request body Veo takes (#3656) passes an explicit aspect ratio through",
      "the request body Veo takes (#3656) puts the prompt on an instance, as the API expects",
      "the request body Veo takes (#3656) takes an inline data URI as bytes rather than a URI"
    ],
    "tests/unit/gemma4-thinking-replay-2480.test.js": [
      "Gemma 4 on the Gemini API (#2480) asks for level-based thinking, not a budget",
      "Gemma 4 on the Gemini API (#2480) keeps both for a regular Gemini route",
      "Gemma 4 on the Gemini API (#2480) leaves the older Gemma line on its budget-free defaults",
      "Gemma 4 on the Gemini API (#2480) replays tool history without synthetic thoughts or signatures"
    ],
    "tests/unit/generic-format-tts-handlers.test.js": [
      "binary decoding via responseToBase64 honours the response content-type over the handler default",
      "binary decoding via responseToBase64 rejects a payload under 100 bytes as empty audio",
      "cartesia authenticates via X-API-Key and wraps the voice as { mode: \"id\" }",
      "cartesia omits the voice and defaults the model when unset",
      "coqui and tortoise (local, noAuth) coqui omits speaker_id when no voice is given",
      "coqui and tortoise (local, noAuth) coqui posts { text, speaker_id } without auth",
      "coqui and tortoise (local, noAuth) tortoise posts { text, voice } and defaults the voice",
      "coqui and tortoise (local, noAuth) tortoise surfaces upstream failure",
      "deepgram falls back to a default model when none is given",
      "deepgram sends the model as a query param with Token auth and decodes binary",
      "fish-audio omits reference_id and defaults the model when unset",
      "fish-audio sends the model in a header and the voice as reference_id",
      "huggingface-tts appends the model to the URL and posts { inputs }",
      "huggingface-tts rejects a missing or traversal model id without fetching",
      "hyperbolic posts { text } with Bearer auth and returns the audio field",
      "hyperbolic throws the upstream error message on failure",
      "inworld defaults voice and model when unset",
      "inworld throws when the response has no audioContent",
      "inworld uses Basic auth and returns audioContent",
      "nvidia-tts defaults the voice when none is given",
      "nvidia-tts posts { input: { text }, voice, model } and decodes wav binary",
      "nvidia-tts surfaces an upstream failure",
      "openai-compatible omits Authorization and defaults the voice when unset",
      "openai-compatible posts the OpenAI speech shape with Bearer auth",
      "playht falls back to the whole key when there is no colon, with defaults",
      "playht splits \"userId:apiKey\" into X-USER-ID and Bearer"
    ],
    "tests/unit/github-endpoint-streams.test.js": [
      "GithubExecutor — /responses endpoint stream transform a fetch failure on /responses is classified and rethrown",
      "GithubExecutor — /responses endpoint stream transform converts Responses API deltas to OpenAI chunks and forwards [DONE]",
      "GithubExecutor — /responses endpoint stream transform flushes an unterminated trailing data line as a converted chunk",
      "GithubExecutor — /responses endpoint stream transform passes a non-OK response through and handles a bodyless OK",
      "GithubExecutor — /v1/messages Claude shim a fetch failure is classified by the deadline and rethrown",
      "GithubExecutor — /v1/messages Claude shim an OK response with no body yields an empty same-status Response",
      "GithubExecutor — /v1/messages Claude shim forwards upstream [DONE] when the client asked to stream",
      "GithubExecutor — /v1/messages Claude shim passes a non-OK response through untouched",
      "GithubExecutor — /v1/messages Claude shim routes a claude-named model to messagesUrl and translates the Claude SSE to OpenAI chunks",
      "GithubExecutor — token refresh chain refreshCopilotToken maps token/expires_at, nulls on non-OK with a log, nulls on throw",
      "GithubExecutor — token refresh chain refreshCredentials falls back GitHub→Copilot when the direct Copilot refresh fails",
      "GithubExecutor — token refresh chain refreshGitHubToken posts the form grant to the registry token endpoint and keeps the old refresh token when omitted",
      "GithubExecutor — token refresh chain returns null when everything fails, and short-circuits on a direct Copilot success",
      "GithubExecutor — token refresh chain returns the bare GitHub tokens when the post-refresh Copilot exchange also fails",
      "GithubExecutor.needsRefresh — expiry parsing falls through to the base check when the copilot token is comfortably fresh",
      "GithubExecutor.needsRefresh — expiry parsing true without a copilotToken; true inside the 5-minute lead for seconds and ISO forms",
      "GithubExecutor.sanitizeMessagesForChatCompletions — content part branches keeps text/image_url parts, serializes tool parts to text, drops empty ones",
      "GithubExecutor.sanitizeMessagesForChatCompletions — content part branches nulls content when every part is stripped, and leaves null-content tool_call messages alone",
      "GithubExecutor.sanitizeMessagesForChatCompletions — content part branches returns the body untouched when there are no messages"
    ],
    "tests/unit/github-executor-transport.test.js": [
      "GithubExecutor token refresh refreshCopilotToken exchanges the GitHub token with `token` scheme at copilot_internal, null on failure",
      "GithubExecutor token refresh refreshCredentials falls back to the GitHub refresh token chain when the Copilot exchange fails",
      "GithubExecutor token refresh refreshCredentials returns null when every path fails — caller must see the failure",
      "GithubExecutor.buildHeaders falls back to accessToken when no copilotToken, and carries the Copilot identity headers",
      "GithubExecutor.buildHeaders prefers copilotToken over accessToken — the GitHub OAuth token must not go upstream when a Copilot token exists",
      "GithubExecutor.execute — endpoint routing an unrelated 400 is NOT escalated: the real error body reaches the caller",
      "GithubExecutor.execute — endpoint routing claude-* models go to /v1/messages, never /chat/completions",
      "GithubExecutor.execute — endpoint routing escalates a 400 'not accessible via /chat/completions' to /responses, but caches the route ONLY on success (#3477)",
      "GithubExecutor.executeWithResponsesEndpoint — non-2xx pass-through returns the upstream error response without wrapping it in a translated stream",
      "GithubExecutor.needsRefresh true without a copilotToken; true within the 5-minute expiry window; false when fresh",
      "GithubExecutor.sanitizeMessagesForChatCompletions drops content down to null when everything is stripped, and keeps assistant tool_calls messages",
      "GithubExecutor.sanitizeMessagesForChatCompletions serializes tool_result/thinking parts as text and keeps text/image_url",
      "GithubExecutor.transformRequest does not mutate the caller's body",
      "GithubExecutor.transformRequest renames max_tokens → max_completion_tokens for gpt-5/o-series only",
      "GithubExecutor.transformRequest strips reasoning_effort:\"none\" but keeps real values"
    ],
    "tests/unit/github-monthly-usage-lock.test.js": [
      "GitHub monthly usage exhaustion keeps unrelated GitHub 402 errors model-scoped",
      "GitHub monthly usage exhaustion locks the whole account until the next UTC month"
    ],
    "tests/unit/github-prefill-sanitize.test.js": [
      "GithubExecutor prefill sanitization drops a trailing assistant prefill message",
      "GithubExecutor prefill sanitization drops multiple consecutive trailing assistant messages",
      "GithubExecutor prefill sanitization is null/empty safe",
      "GithubExecutor prefill sanitization leaves a conversation ending in a tool message untouched",
      "GithubExecutor prefill sanitization leaves a conversation ending in a user message untouched",
      "GithubExecutor prefill sanitization never empties an assistant-only conversation (keeps at least one message)"
    ],
    "tests/unit/github-responses-escalation-3477.test.js": [
      "GitHub Copilot /responses escalation cache (#3477) does not pin a model to /responses when the escalation also fails",
      "GitHub Copilot /responses escalation cache (#3477) honors x-should-retry before changing the generation endpoint",
      "GitHub Copilot /responses escalation cache (#3477) honors x-tokenproxy-replay-safe before changing the generation endpoint",
      "GitHub Copilot /responses escalation cache (#3477) pins a model to /responses once that endpoint serves it"
    ],
    "tests/unit/github-responses-routing.test.js": [
      "GithubExecutor.execute cached-route guard (#1062) does NOT use /responses for a Gemini model even if it was wrongly cached as codex",
      "GithubExecutor.supportsResponsesEndpoint allows OpenAI/codex models on the /responses endpoint",
      "GithubExecutor.supportsResponsesEndpoint excludes Claude models from the /responses endpoint",
      "GithubExecutor.supportsResponsesEndpoint excludes Gemini models from the /responses endpoint",
      "GithubExecutor.supportsResponsesEndpoint is null-safe"
    ],
    "tests/unit/gitlab-pat-import-receipt.test.js": [
      "returns the exact created GitLab account identity for readback without echoing its credential"
    ],
    "tests/unit/gitlawb-opengateway.test.js": [
      "Gitlawb OpenGateway provider keeps a small documented seed while allowing the live catalog",
      "Gitlawb OpenGateway provider normalizes the public OpenAI-shaped model catalogue",
      "Gitlawb OpenGateway provider registers the documented API-key OpenAI endpoint"
    ],
    "tests/unit/gitlawb-provider-routes.test.js": [
      "Gitlawb OpenGateway API-key validation does not accept a public models 200 as proof that a new key works",
      "Gitlawb OpenGateway API-key validation returns bounded diagnostics for an unavailable saved probe",
      "Gitlawb OpenGateway API-key validation uses the authenticated, non-billable credits probe through a saved proxy"
    ],
    "tests/unit/glm-cn-anthropic-transport-1746.test.js": [
      "glm-cn offers the vendor's Anthropic endpoint (#1746) a claude client routes to the anthropic-compatible endpoint",
      "glm-cn offers the vendor's Anthropic endpoint (#1746) an openai client still routes to the coding endpoint",
      "glm-cn offers the vendor's Anthropic endpoint (#1746) it mirrors the international sibling rather than inventing a shape",
      "glm-cn offers the vendor's Anthropic endpoint (#1746) the claude transport carries Anthropic auth and headers",
      "glm-cn offers the vendor's Anthropic endpoint (#1746) the default transport is unchanged, so existing users are not moved",
      "glm-cn offers the vendor's Anthropic endpoint (#1746) the usage endpoint is untouched"
    ],
    "tests/unit/glm-usage-multiple-limits-3596.test.js": [
      "GLM usage keeps every interval limit (#3596) a non-token limit is still skipped, and does not consume a key",
      "GLM usage keeps every interval limit (#3596) remaining is derived per limit, not from the last one seen",
      "GLM usage keeps every interval limit (#3596) the plan still comes through",
      "GLM usage keeps every interval limit (#3596) the single-window case is byte-for-byte what it was",
      "GLM usage keeps every interval limit (#3596) two windows produce two entries"
    ],
    "tests/unit/google-pse-cx-field-3402.test.js": [
      "Google PSE can be given its search engine id (#3402) composes with a base URL rather than replacing it",
      "Google PSE can be given its search engine id (#3402) declares the field on the registry entry",
      "Google PSE can be given its search engine id (#3402) does not disturb the other shapes the builder produces",
      "Google PSE can be given its search engine id (#3402) stores a typed value where the search caller reads it",
      "Google PSE can be given its search engine id (#3402) trims, and drops an empty value rather than saving a blank credential"
    ],
    "tests/unit/google-tts-chunking-2287.test.js": [
      "long text is split before it reaches Google TTS (#2287) concatenates the decoded segments rather than the base64 strings",
      "long text is split before it reaches Google TTS (#2287) cuts mid-token only when one token exceeds the cap on its own",
      "long text is split before it reaches Google TTS (#2287) falls back to a word boundary when there is no sentence break",
      "long text is split before it reaches Google TTS (#2287) handles empty and non-string input",
      "long text is split before it reaches Google TTS (#2287) leaves short text as one piece",
      "long text is split before it reaches Google TTS (#2287) loses no words when rejoined",
      "long text is split before it reaches Google TTS (#2287) never emits a piece over the limit",
      "long text is split before it reaches Google TTS (#2287) prefers a sentence boundary and keeps the punctuation"
    ],
    "tests/unit/google-tts-provider.test.js": [
      "googleTts synthesize chunks long input and concatenates the audio",
      "googleTts synthesize defaults the language to en and reuses the cached token",
      "googleTts synthesize rejects audio shorter than 100 chars as empty",
      "googleTts synthesize scrapes the token, posts the RPC, and returns base64 mp3",
      "googleTts synthesize strips special characters before synthesis",
      "googleTts synthesize throws when the RPC fails",
      "googleTts synthesize throws when the token cannot be parsed from the page",
      "googleTts synthesize throws when the token page fetch fails"
    ],
    "tests/unit/gpt54-effort-levels-2359.test.js": [
      "GPT 5.4 and 5.5 reach every effort level (#2359) declares each requested level",
      "GPT 5.4 and 5.5 reach every effort level (#2359) resolves the model(level) form to the same capable model",
      "GPT 5.4 and 5.5 reach every effort level (#2359) ships the models",
      "GPT 5.4 and 5.5 reach every effort level (#2359) uses the openai effort format, which is what carries the level"
    ],
    "tests/unit/gpt56-sol-function-tools-reasoning-2540.test.js": [
      "gpt-5.6-sol function-tool reasoning compatibility (#2540) does not touch the client-facing Responses path, which the upstream told us to use",
      "gpt-5.6-sol function-tool reasoning compatibility (#2540) forces reasoning_effort:none when function tools are declared",
      "gpt-5.6-sol function-tool reasoning compatibility (#2540) leaves a gpt-5 model the upstream has not rejected alone",
      "gpt-5.6-sol function-tool reasoning compatibility (#2540) leaves a sol request without function tools alone"
    ],
    "tests/unit/grok-build-config.test.js": [
      "grokBuildConfig blank override restores previous subagent mapping and removes owned slot",
      "grokBuildConfig creates independent main and per-type subagent model slots",
      "grokBuildConfig is idempotent and updates owned slots without duplicate sections",
      "grokBuildConfig legacy callers without subagentModels leave existing overrides untouched",
      "grokBuildConfig preserves unrelated config sections",
      "grokBuildConfig removes mappings that were originally unset",
      "grokBuildConfig reset restores previous default and all previous subagent mappings",
      "grokBuildConfig returns stable slot names only for supported subagent types"
    ],
    "tests/unit/grok-cli-bulk-import-abb20d.test.js": [
      "grok-cli bulk import — accepted shapes accepts a single bare object",
      "grok-cli bulk import — accepted shapes accepts camelCase token fields as well as snake_case",
      "grok-cli bulk import — accepted shapes accepts the { accounts: [...] } wrapper the modal posts",
      "grok-cli bulk import — accepted shapes creates one connection per item in a bare array",
      "grok-cli bulk import — rejection 400s on a body that is not JSON",
      "grok-cli bulk import — rejection 400s on an empty account list",
      "grok-cli bulk import — rejection never echoes a token back to the caller",
      "grok-cli bulk import — rejection reports the failing item by index and still imports the rest",
      "grok-cli bulk import — stored connection backfills the email from the access token when no id_token is given",
      "grok-cli bulk import — stored connection backfills the email from the id_token",
      "grok-cli bulk import — stored connection derives expiresAt from expires_in",
      "grok-cli bulk import — stored connection ignores a caller-supplied provider or id",
      "grok-cli bulk import — stored connection keeps an explicit email over anything decoded from a token",
      "grok-cli bulk import — stored connection matches the device_code login flow"
    ],
    "tests/unit/grok-cli-executor.test.js": [
      "grok-cli registry is listed as oauth provider for dashboard",
      "grok-cli registry maps effort virtual models to upstream grok-4.5",
      "grok-cli registry registers transport + oauth + models",
      "grok-cli registry resolves aliases to provider id",
      "grok-cli registry routes bare grok-build to the subscription provider",
      "GrokCliExecutor bounds per-session turn state",
      "GrokCliExecutor buildHeaders falls back to top-level email/userId (OAuth mapTokens shape)",
      "GrokCliExecutor buildHeaders sets CLI fingerprint + session headers",
      "GrokCliExecutor buildUrl points at cli-chat-proxy responses",
      "GrokCliExecutor countGrokCliUserTurns / resolveGrokCliTurnIdx helpers",
      "GrokCliExecutor does not advance turn index when retrying the same request body",
      "GrokCliExecutor drops stale tool_choice and normalizes converted custom choices",
      "GrokCliExecutor forwards reasoning effort for grok-4.6 (#3514)",
      "GrokCliExecutor is registered on executor map (id + aliases)",
      "GrokCliExecutor keeps fallback session stable when assistant history appears",
      "GrokCliExecutor keeps the allowlist closed around grok-4.5 / grok-4.6",
      "GrokCliExecutor normalizes Codex cross-provider tool and reasoning history",
      "GrokCliExecutor normalizes official effort aliases",
      "GrokCliExecutor omits reasoning effort for models that reject it",
      "GrokCliExecutor parseError surfaces 402 spending-limit",
      "GrokCliExecutor preserves native Grok encrypted reasoning and item ids",
      "GrokCliExecutor resolves a stable session and monotonic turn index without executor fields",
      "GrokCliExecutor stringifies structured outputs and removes orphaned output items",
      "GrokCliExecutor transformRequest keeps role:system (HAR parity) and strips server ids",
      "GrokCliExecutor transformRequest normalizes Responses body like official CLI"
    ],
    "tests/unit/grok-cli-expiresat-2546.test.js": [
      "Grok CLI (xAI) token expiry propagation (#2546) proactive refresh does NOT fire for a far-future grok-cli token",
      "Grok CLI (xAI) token expiry propagation (#2546) proactive refresh fires for a near-expiry grok-cli token (expiresAt present)"
    ],
    "tests/unit/grok-cli-models.test.js": [
      "Grok CLI live models normalizes official model metadata",
      "Grok CLI live models refreshes and retries through selected proxy"
    ],
    "tests/unit/grok-cli-oauth-probe.test.js": [
      "classifyOAuthProbeResult (grok-cli) Codex-style acceptStatuses 400 stays silent success (no soft warning)",
      "classifyOAuthProbeResult (grok-cli) treats 200 as hard success",
      "classifyOAuthProbeResult (grok-cli) treats 401 as hard auth failure",
      "classifyOAuthProbeResult (grok-cli) treats 402 spending-limit as soft success (connected, out of credits)",
      "classifyOAuthProbeResult (grok-cli) treats 403 as access denied"
    ],
    "tests/unit/grok-cli-quota-frame.test.js": [
      "decodeGrokCreditsFrame clamps usage ratio above 1.0 to percentUsed 100",
      "decodeGrokCreditsFrame decodes raw unframed protobuf payload",
      "decodeGrokCreditsFrame decodes real GetGrokCreditsConfig shape (nested, fixed32, Timestamp, trailer)",
      "decodeGrokCreditsFrame ignores trailing gRPC-web trailer frame (flag 0x80)",
      "decodeGrokCreditsFrame returns null for empty buffer",
      "decodeGrokCreditsFrame returns null for negative usage ratio",
      "decodeGrokCreditsFrame returns null for trailer-only body",
      "decodeGrokCreditsFrame returns null for truncated buffer",
      "decodeGrokCreditsFrame returns null when nested usage-ratio has unexpected wire type",
      "decodeGrokCreditsFrame returns null when top-level field 1 is not length-delimited",
      "decodeGrokCreditsFrame returns null when top-level has no field 1",
      "decodeGrokCreditsFrame treats omitted usage-ratio as 0% (proto3 default)",
      "probeFrameHeader accepts trailer frame header (flag 0x80)",
      "probeFrameHeader reads frame header at non-zero offset",
      "probeFrameHeader rejects declared length that exceeds body",
      "probeFrameHeader rejects invalid compression flag"
    ],
    "tests/unit/grok-daily-usage-route.test.js": [
      "Grok daily usage route preserves owner text when Grok does not report subscribed access",
      "Grok daily usage route replaces the no-numeric-quota text with today's request meter"
    ],
    "tests/unit/grok-web-executor.test.js": [
      "GrokWebExecutor — non-streaming synthesis accumulates deltas, lets a final modelResponse override, carries the fingerprint, fills usage",
      "GrokWebExecutor — non-streaming synthesis an error event with only a code falls back to a code-bearing message",
      "GrokWebExecutor — non-streaming synthesis an upstream error event becomes a 502 GROK_ERROR",
      "GrokWebExecutor — prompt assembly and outbound request shape logs and falls back to a default mapping for an unmapped model",
      "GrokWebExecutor — prompt assembly and outbound request shape normalizes the sso cookie: with and without the pasted 'sso=' prefix",
      "GrokWebExecutor — prompt assembly and outbound request shape prefixes history with roles, maps developer→system, leaves the last user turn bare",
      "GrokWebExecutor — streaming synthesis a body stream that errors mid-read produces a [Stream error:] chunk and [DONE]",
      "GrokWebExecutor — streaming synthesis a pre-aborted signal prevents generation dispatch",
      "GrokWebExecutor — streaming synthesis emits role chunk, token deltas with fingerprint, finish stop, then [DONE], under no-buffer SSE headers",
      "GrokWebExecutor — streaming synthesis surfaces an upstream error event inline and still terminates the stream",
      "GrokWebExecutor — upstream failure classification a generic fetch failure becomes a 502 upstream_error, not a throw",
      "GrokWebExecutor — upstream failure classification an AbortError propagates as a throw for the retry layer",
      "GrokWebExecutor — upstream failure classification an OK response with no body is a 502 upstream_error",
      "GrokWebExecutor — upstream failure classification non-OK statuses pass through with an HTTP_<status> code; 401/403/429 get guidance",
      "GrokWebExecutor.execute — request validation 400s on a missing or empty messages array without calling upstream",
      "GrokWebExecutor.execute — request validation 400s when every message reduces to empty text"
    ],
    "tests/unit/groq-strip-thinking-3014.test.js": [
      "groq drops thinking fields its schema rejects (#3014) enable_thinking is removed",
      "groq drops thinking fields its schema rejects (#3014) it applies to every groq model, since the schema is the gateway's",
      "groq drops thinking fields its schema rejects (#3014) nothing else in the body is disturbed",
      "groq drops thinking fields its schema rejects (#3014) reasoning_effort survives, because Groq accepts it on gpt-oss",
      "groq drops thinking fields its schema rejects (#3014) the Anthropic-shaped thinking object is removed too",
      "groq drops thinking fields its schema rejects (#3014) the rule is provider-scoped and does not touch another provider's qwen"
    ],
    "tests/unit/header-timeout-shared-path.test.js": [
      "our own response-header timeout is a shared-path failure is classified as shared-path, not as a credential fault",
      "our own response-header timeout is a shared-path failure matches whatever timeout value the deadline reports",
      "our own response-header timeout is a shared-path failure rotates without locking the key or escalating backoff",
      "our own response-header timeout is a shared-path failure still locks a genuine per-key failure",
      "the claude header deadline fits its own context window clears the measured worst-case prefill",
      "the claude header deadline fits its own context window declares a registry timeout that survives validation",
      "the claude header deadline fits its own context window is still overridable per provider from settings"
    ],
    "tests/unit/headroom-branch-coverage.test.js": [
      "callCompress response gates keeps the original on conflicting token metrics (string-encoded)",
      "callCompress response gates keeps the original on phantom token savings (>95%)",
      "callCompress response gates keeps the original on tokens_saved <= 0",
      "callCompress response gates passes a skip_reason without messages[] through as the diagnostic",
      "claude branch guards and entry fallbacks keeps the original when the claude compression saves no bytes",
      "claude branch guards and entry fallbacks skips on a missing proxy URL",
      "claude branch guards and entry fallbacks skips when no oldest-first claude slice fits under the byte cap",
      "claude branch guards and entry fallbacks treats an unserializable body as zero bytes instead of throwing",
      "Claude branch guards rejects a compressed message whose content is neither string nor array",
      "Claude branch guards rejects a compressed set that loses tool pairing ids",
      "Claude branch guards reports an unsupported claude shape when messages is absent",
      "entry gates reports an unsupported shape for a body with no message container",
      "entry gates skips on a missing body",
      "entry gates skips when within context budget",
      "formatters formatHeadroomLog renders the token delta line",
      "formatters formatHeadroomSizeLog needs both snapshots",
      "formatters isHeadroomPhantomSavings flags claimed savings without byte shrink",
      "Gemini projection compresses and writes back only text parts",
      "Gemini projection keeps the original on phantom projected savings",
      "Gemini projection reads the antigravity-nested request object",
      "Gemini projection skips a gemini body with no text parts",
      "Kiro projection compresses and writes text back through the projection targets",
      "Kiro projection keeps the original on phantom projected-size savings",
      "Kiro projection rejects a reply message with no extractable text",
      "Kiro projection rejects a role-mismatched projection reply",
      "Kiro projection skips a Kiro body that projects to no messages",
      "OpenAI shape guard keeps the original on phantom byte savings on the openai path",
      "OpenAI shape guard reads commandcode messages one level down under params",
      "OpenAI shape guard rejects a candidate that drops tool_calls pairing",
      "OpenAI shape guard rejects a candidate that rewrites tool_call_id",
      "OpenAI shape guard rejects a candidate whose null content has no tool_calls on either side",
      "openai-responses branch skips an untyped item with no role",
      "openai-responses branch skips when a message item would vanish in projection (empty content)",
      "openai-responses branch skips when input carries a non-message item type",
      "transport failures and circuit breaker cooldown opens the breaker after repeat failures and reports the cooldown",
      "transport failures and circuit breaker cooldown rejects a response carrying CCR hashes",
      "transport failures and circuit breaker cooldown rejects a response with a CCR marker inside a message",
      "transport failures and circuit breaker cooldown reports a rejected config.mode on HTTP 400 and a plain status otherwise",
      "transport failures and circuit breaker cooldown survives an unparseable endpoint URL via the string fallback"
    ],
    "tests/unit/headroom-chat-core.test.js": [
      "handleChatCore Headroom diagnostics keeps original body (no commit) and logs skip reason when Headroom reports phantom savings",
      "handleChatCore Headroom diagnostics logs why Headroom was skipped on chat completions",
      "handleChatCore Headroom diagnostics masks credentials and query strings in Headroom endpoint diagnostics",
      "handleChatCore Headroom diagnostics pxpipe applies transform when no opt-out header is present",
      "handleChatCore Headroom diagnostics pxpipe honors token-saver header off on claude body above threshold",
      "handleChatCore Headroom diagnostics pxpipe honors token-saver header OFF on claude body above threshold",
      "handleChatCore Headroom diagnostics scrubs credentials and query strings from Headroom fetch errors",
      "handleChatCore Headroom diagnostics sends Headroom-compressed messages to the provider executor"
    ],
    "tests/unit/headroom-context-pressure-gate.test.js": [
      "Headroom is gated on context pressure calls the proxy once the request is over the budget",
      "Headroom is gated on context pressure does not call the proxy for a request inside the budget",
      "Headroom is gated on context pressure keeps the prefix byte-identical across turns that stay inside the budget",
      "Headroom is gated on context pressure reports the budget skip distinctly from the toggle being off"
    ],
    "tests/unit/headroom-dashboard-lifetime-2330.test.js": [
      "headroom dashboard reaches its own data through the proxy (#2330) rewrites every root-relative call the shipped dashboard/settings pages make",
      "headroom dashboard reaches its own data through the proxy (#2330) rewrites the settings page and its sub-paths",
      "headroom dashboard reaches its own data through the proxy (#2330) sends the lifetime savings history to Headroom, not to the tokenproxy origin",
      "headroom dashboard reaches its own data through the proxy (#2330) still refuses paths outside the allowlist, and never double-prefixes"
    ],
    "tests/unit/headroom-detect.test.js": [
      "headroom detect recognizes loopback URLs",
      "headroom detect treats a reachable external proxy as running without any local probing"
    ],
    "tests/unit/headroom-extras-restricted-2965.test.js": [
      "headroom paths are gated identically (#2965) gates every headroom path identically, so the asymmetry is gone"
    ],
    "tests/unit/headroom-proxy-route.test.js": [
      "forwardedHeaders injects Bearer from trimmed HEADROOM_API_KEY only",
      "forwardedHeaders never forwards HEADROOM_PROXY_TOKEN as a credential",
      "forwardedHeaders prefers HEADROOM_API_KEY and ignores HEADROOM_PROXY_TOKEN fallback",
      "forwardedHeaders strips host and hop-by-hop headers always; forwards viewer credentials to loopback targets (fork exception)",
      "forwardedHeaders strips viewer credentials for external targets too",
      "proxy handler forwards viewer cookies to the loopback target and injects HEADROOM_API_KEY Bearer",
      "proxy handler leaves non-HTML bodies (json/sse/binary) untouched",
      "proxy handler rejects non-http(s) configured targets with a bounded error",
      "proxy handler returns a generic bounded error when upstream fails - no secrets, no upstream body echo",
      "proxy handler rewrites 3xx Location headers without duplication",
      "proxy handler rewrites text/html bodies regardless of catch-all path and drops stale content-length",
      "proxy handler uses manual redirects upstream",
      "rewriteHeadroomHtml does not rewrite axios calls (no evidence Headroom uses it)",
      "rewriteHeadroomHtml does not rewrite escaped or dynamic template expressions",
      "rewriteHeadroomHtml is idempotent - second pass changes nothing",
      "rewriteHeadroomHtml leaves absolute, protocol-relative, scheme URLs and unknown root paths untouched",
      "rewriteHeadroomHtml returns non-string input unchanged",
      "rewriteHeadroomHtml rewrites allow-listed src/href/action and fetch literals, preserving query/hash and quote type",
      "rewriteLocation handles null/empty/malformed safely",
      "rewriteLocation leaves external, protocol-relative, non-http and already-prefixed locations untouched",
      "rewriteLocation maps same-origin redirects when the target has a base pathname",
      "rewriteLocation rewrites root-relative locations preserving query/hash",
      "rewriteLocation rewrites same-origin absolute locations through the proxy"
    ],
    "tests/unit/headroom-responses-format.test.js": [
      "compressWithHeadroom openai-responses format (#1998) keeps body.input in Responses format after compressing an openai-responses request",
      "compressWithHeadroom openai-responses format (#1998) skips Responses tool/reasoning history instead of collapsing it into a message (#2132)"
    ],
    "tests/unit/headroom-responses-guard-3571.test.js": [
      "the Responses compression guard stays conservative (#3571) carries no unreachable second branch",
      "the Responses compression guard stays conservative (#3571) records why widening it is wrong, not just that it is blunt",
      "the Responses compression guard stays conservative (#3571) reproduces the predicate's contract",
      "the Responses compression guard stays conservative (#3571) still ignores a body with no input array",
      "the Responses compression guard stays conservative (#3571) still treats every non-message item as unsafe"
    ],
    "tests/unit/headroom-responses-instruction-items-2132.test.js": [
      "#2132 headroom must not drop Responses instruction items fails open when the round trip loses a non-instruction item",
      "#2132 headroom must not drop Responses instruction items keeps developer and current user items while compressing historical output",
      "#2132 headroom must not drop Responses instruction items keeps every instruction item, not only the first, and at its own index",
      "#2132 headroom must not drop Responses instruction items leaves an instruction-free body compressing exactly as before",
      "#2132 headroom must not drop Responses instruction items still refuses a body carrying non-message input items"
    ],
    "tests/unit/headroom-responses-translate-diagnostic.test.js": [
      "compressWithHeadroom openai-responses translation failure records why it bailed instead of returning a bare null"
    ],
    "tests/unit/headroom-status-enabled-1956.test.js": [
      "headroom status reports the toggle, not only the probe (#1956) a reachable proxy with the toggle off is not active",
      "headroom status reports the toggle, not only the probe (#1956) is active only when the toggle is on and the proxy answers",
      "headroom status reports the toggle, not only the probe (#1956) is not active when the toggle is on but the proxy is down",
      "headroom status reports the toggle, not only the probe (#1956) treats a missing setting as off rather than as on",
      "the handler says why it skipped compression (#1956) warns when the saver is on regardless of the headroom toggle"
    ],
    "tests/unit/headroom-target-format-shapes-2620.test.js": [
      "#2620 commandcode params.messages compresses the nested message list and leaves the envelope alone",
      "#2620 commandcode params.messages preserves an error trace: is_error under params.messages skips before fetch",
      "#2620 commit preserves fields outside the OpenAI contract keeps an OpenAI message name and reverts cleanly when the byte guard trips",
      "#2620 commit preserves fields outside the OpenAI contract keeps Ollama tool_name / images / thinking that the proxy does not echo",
      "#2620 gemini-family contents[] projection fails open when the proxy does not preserve the projected order",
      "#2620 gemini-family contents[] projection never rewrites functionCall / functionResponse / inlineData parts",
      "#2620 gemini-family contents[] projection preserves systemInstruction and compresses opted-in contents text parts",
      "#2620 gemini-family contents[] projection reads antigravity's nested body.request and vertex's snake_case system_instruction",
      "#2620 gemini-family contents[] projection still skips a contents[] body when the format is not a Gemini one",
      "R-F1: Kiro/Gemini projections never write into caller-shared objects antigravity: the caller's nested request.contents is untouched",
      "R-F1: Kiro/Gemini projections never write into caller-shared objects Gemini: the caller's contents array is untouched; the body carries the compressed copy",
      "R-F1: Kiro/Gemini projections never write into caller-shared objects Kiro: on proxy failure the caller's conversationState is still untouched",
      "R-F1: Kiro/Gemini projections never write into caller-shared objects Kiro: the caller's conversationState object is untouched; the body carries the compressed copy",
      "R-F3: part-level isError is an error trace Claude tool_result part with isError:true skips before fetch"
    ],
    "tests/unit/headroom-timeout-setting.test.js": [
      "handleChatCore threads the timeout to the compression call falls back to the built-in default when nothing is configured",
      "handleChatCore threads the timeout to the compression call uses the configured value",
      "headroomTimeoutMs setting drops a value AbortSignal.timeout would misread back to unset",
      "headroomTimeoutMs setting is preferred over the env fallback by the chat handler",
      "headroomTimeoutMs setting is unset by default so the env var and the built-in default still decide",
      "headroomTimeoutMs setting keeps a configured integer inside the accepted window"
    ],
    "tests/unit/headroom-windows-loopback-2476.test.js": [
      "headroom compress call forces IPv4 for the default localhost endpoint leaves a non-localhost endpoint (an explicit IP or remote host) alone",
      "headroom compress call forces IPv4 for the default localhost endpoint passes a connection dispatcher when the endpoint host is the literal 'localhost'",
      "headroom health probe forces IPv4 for the default localhost endpoint does not attach a dispatcher for a remote headroom URL",
      "headroom health probe forces IPv4 for the default localhost endpoint passes a connection dispatcher to the /health fetch for localhost"
    ],
    "tests/unit/headroom.test.js": [
      "CCR marker rejection does not reject on unrelated metadata mentioning ccr outside message content",
      "CCR marker rejection rejects <<ccr: prefix inside block part without touching body",
      "CCR marker rejection rejects <<ccr: prefix inside content without touching body",
      "CCR marker rejection rejects <<ccr: prefix inside non-text tool-result part without touching body",
      "CCR marker rejection rejects <<ccr: prefix inside tool_calls function arguments without touching body",
      "CCR marker rejection rejects ccr_hashes nonempty without touching body",
      "Claude direct path (no OpenAI pivot) older proxy 404 on Claude direct fails open after one call, no pivot retry",
      "Claude direct path (no OpenAI pivot) response count/role/content-shape mismatch → null, body unchanged, one call",
      "Claude direct path (no OpenAI pivot) sends Claude messages directly; preserves system and tools byte-for-byte locally",
      "compression_skipped contract 200 with compression_skipped=true returns null and preserves body verbatim",
      "compression_skipped contract bounds a remote-provided skip_reason so it cannot flood logs",
      "compression_skipped contract missing messages in a 200 response is invalid → null",
      "compressWithHeadroom activates circuit breaker after consecutive failures without repeated fetch",
      "compressWithHeadroom compresses Kiro conversationState history/currentMessage in-place",
      "compressWithHeadroom compresses messages in-place",
      "compressWithHeadroom compresses responses input in-place",
      "compressWithHeadroom fails open on bad response",
      "compressWithHeadroom fails open when Kiro Headroom output does not preserve message order",
      "compressWithHeadroom no-ops when disabled",
      "compressWithHeadroom skips unknown shapes",
      "compressWithHeadroom uses a 15s default timeout so large prompt compression can finish",
      "error tool result skip (pre-fetch) Claude tool_result is_error:true skips before fetch",
      "error tool result skip (pre-fetch) Kiro toolResults status:error skips before fetch",
      "error tool result skip (pre-fetch) OpenAI explicit error shapes skip; plain text never triggers inference",
      "error tool result skip (pre-fetch) Responses function_call_output status error or is_error skips before fetch",
      "formatHeadroomLog formats reported token deltas without implying provider billing savings",
      "formatHeadroomLog reports effective payload, tool-schema, and tool-history sizes",
      "headroom outbound auth sends the outbound key as Bearer when set; never the inbound proxy token; no secret in diagnostics",
      "no-gain / phantom / conflicting metrics guard conflicting metrics (after > before) keep original body",
      "no-gain / phantom / conflicting metrics guard exception thrown by proxy leaves original body unchanged",
      "no-gain / phantom / conflicting metrics guard phantom gain (tokens shrink claimed, bytes grow) keeps original body",
      "no-gain / phantom / conflicting metrics guard real gain commits replacement",
      "no-gain / phantom / conflicting metrics guard zero reported token gain keeps original body",
      "older proxy rejection is one-call fail-open HTTP 400 → null, body untouched, no retry pivot",
      "older proxy rejection is one-call fail-open HTTP 404 → null, body untouched, no retry pivot",
      "one-call + payload contract config omits compress_user_messages when disabled but keeps lossy_inline mode",
      "one-call + payload contract sends exactly one POST with model/messages/config.mode=lossy_inline and never frozen_message_count",
      "OpenAI structural validation (adversarial proxy) assistant tool_calls (ids/names/arguments) mutated → null, body untouched",
      "OpenAI structural validation (adversarial proxy) dropped messages (3→1) with claimed savings → null, body untouched, one fetch",
      "OpenAI structural validation (adversarial proxy) generic body.input through default branch enforces same contract",
      "OpenAI structural validation (adversarial proxy) invalid content shape (null content replacing text) → null, body untouched",
      "OpenAI structural validation (adversarial proxy) reordered roles (same count) → null, body untouched",
      "OpenAI structural validation (adversarial proxy) tool_call_id mutated → null; faithful tool_call_id commits",
      "OpenAI structural validation (adversarial proxy) valid same-shape compression with real shrink still commits (not blanket reject)",
      "parseHeadroomTimeoutMs accepts finite integers in (0, 600000) and rejects the boundary",
      "parseHeadroomTimeoutMs defaults to 30000 on missing/invalid values",
      "string-number token metric gates string tokens_before/after still trip the phantom-savings gate",
      "string-number token metric gates string tokens_saved:'0' still trips the no-gain gate",
      "string-number token metric gates valid string-encoded metrics with real gains still commit"
    ],
    "tests/unit/health-detail-3097.test.js": [
      "GET /api/health/detail gives an anonymous caller readiness only, and never scans connections",
      "GET /api/health/detail groups degraded connections per provider for an authenticated caller",
      "GET /api/health/detail proves the database with a real query rather than a handle check",
      "GET /api/health/detail reports ok when every enabled connection is healthy",
      "GET /api/health/detail reports status error, still on HTTP 200, when the database is unreachable",
      "GET /api/health/detail treats requireLogin=false as an operator, matching the rest of /api/*"
    ],
    "tests/unit/hermes-cloaking.test.js": [
      "Hermes Cloaking / System Prompt Sanitization Anthropic API Surface -> Antigravity sanitizes system array blocks when client connects via Anthropic format",
      "Hermes Cloaking / System Prompt Sanitization Anthropic API Surface -> Antigravity sanitizes system prompt when client connects via Anthropic format (/v1/messages)",
      "Hermes Cloaking / System Prompt Sanitization AntigravityExecutor.transformRequest sanitizes Hermes system prompt in transformRequest",
      "Hermes Cloaking / System Prompt Sanitization AntigravityExecutor.transformRequest strips Zed Claude agent system prompt while also sanitizing",
      "Hermes Cloaking / System Prompt Sanitization openaiToAntigravityRequest sanitizes Hermes system prompt in Claude-backed Antigravity envelope",
      "Hermes Cloaking / System Prompt Sanitization openaiToAntigravityRequest sanitizes Hermes system prompt in Gemini-backed Antigravity envelope",
      "Hermes Cloaking / System Prompt Sanitization sanitizeAntigravitySystemPrompt handles null, undefined, and non-string values safely",
      "Hermes Cloaking / System Prompt Sanitization sanitizeAntigravitySystemPrompt handles variations in whitespace and casing",
      "Hermes Cloaking / System Prompt Sanitization sanitizeAntigravitySystemPrompt leaves non-matching system prompts untouched",
      "Hermes Cloaking / System Prompt Sanitization sanitizeAntigravitySystemPrompt replaces exact Hermes Agent opening sentence"
    ],
    "tests/unit/hermes-vision-detection.test.js": [
      "Hermes Vision Image Detection auto-switches non-vision model (deepseek-v4-pro) to Vision Adapter model (Kimi-K3)",
      "Hermes Vision Image Detection detects vision from embedded data:image URI in string content",
      "Hermes Vision Image Detection detects vision from Hermes attachments array",
      "Hermes Vision Image Detection detects vision from Ollama / Hermes images array",
      "Hermes Vision Image Detection detects vision from Vercel AI SDK / Hermes experimental_attachments",
      "Hermes Vision Image Detection strips msg.images and attachments when model does not support vision"
    ],
    "tests/unit/hf-model-routing.test.js": [
      "HuggingFace model alias parsing resolves hf alias to huggingface provider",
      "HuggingFace STT support (issue #2548) advertises the two whisper STT presets",
      "HuggingFace STT support (issue #2548) exposes an sttConfig so the STT dispatch layer recognizes the provider"
    ],
    "tests/unit/hide-to-tray-macos-sigterm-1284.test.js": [
      "Hide to Tray on macOS survives more than SIGHUP (#1284) also ignores SIGTERM so a session/process manager cannot kill the hidden server",
      "Hide to Tray on macOS survives more than SIGHUP (#1284) ignores SIGHUP, same as before",
      "Hide to Tray on macOS survives more than SIGHUP (#1284) registers the SIGTERM override in the same darwin branch as SIGHUP, not globally"
    ],
    "tests/unit/history-retention-ui.test.js": [
      "fails closed on incomplete stored policy instead of inventing a default",
      "fails closed on the initial settings read and allows a read retry",
      "reads current policy without discarding a draft and returns focus after an explicit discard",
      "requires explicit deletion acknowledgement and verifies persisted policy before reporting success",
      "requires renewed acknowledgement after changing the selected policy",
      "retains the opt-in draft after mismatch without claiming persistence",
      "retains the opt-in draft after refused without claiming persistence",
      "saves preservation without requiring deletion acknowledgement",
      "shows the stored policy directly without a modal or an implicit write"
    ],
    "tests/unit/history-retention.test.js": [
      "explicit history retention policy applies an explicit window to event-only ingestion and rejects expired incoming reports",
      "explicit history retention policy carries indefinite retention through analytics and requires a deliberate window mode",
      "explicit history retention policy deletes expired Context evidence only after explicit window selection while retaining usage",
      "explicit history retention policy keeps every old evidence row byte-for-byte on the first request with {\"statsRetentionDays\":45}",
      "explicit history retention policy keeps every old evidence row byte-for-byte on the first request with {\"statsRetentionMode\":\"preserve\",\"statsRetentionDays\":1}",
      "explicit history retention policy keeps every old evidence row byte-for-byte on the first request with {}",
      "explicit history retention policy preserves old events in event-only ingestion and accepts historical reports without attribution guesses"
    ],
    "tests/unit/http2-connect-abort-paths.test.js": [
      "abort and write failures during HTTP CONNECT destroys the target TLS socket when its handshake errors",
      "abort and write failures during HTTP CONNECT rejects and destroys the proxy socket when aborted mid-CONNECT",
      "abort and write failures during HTTP CONNECT rejects when writing the CONNECT request itself throws",
      "abort handling on the socks path (waitWithAbort) propagates an agent connect failure with the signal still pending",
      "abort handling on the socks path (waitWithAbort) rejects with the abort reason and destroys a late-arriving socks socket",
      "abort handling on the socks path (waitWithAbort) resolves normally when the agent answers before any abort",
      "default node primitives routes a direct connect through http2.connect",
      "default node primitives routes a socks proxy through SocksProxyAgent",
      "default node primitives routes an http proxy through net.connect then tls.connect",
      "route validation rejects a proxy route on a non-tunnel protocol",
      "route validation rejects a proxy route whose URL does not parse",
      "route validation rejects an unrecognised route kind"
    ],
    "tests/unit/http2-connect.test.js": [
      "Cursor HTTP/2 effective egress route keeps connection-proxy-direct direct without environment resolution",
      "Cursor HTTP/2 effective egress route keeps pool-none direct without environment resolution",
      "Cursor HTTP/2 effective egress route makes zero generic fetch calls for required-unavailable provenance",
      "Cursor HTTP/2 effective egress route rejects a required-unavailable persisted selection before environment resolution",
      "Cursor HTTP/2 effective egress route rejects a selected relay before proxy or environment transport selection",
      "Cursor HTTP/2 effective egress route rejects malformed selected proxy strict=false without environment fallback",
      "Cursor HTTP/2 effective egress route rejects malformed selected proxy strict=true without environment fallback",
      "Cursor HTTP/2 effective egress route rejects malformed selected proxy strict=true without environment fallback",
      "Cursor HTTP/2 effective egress route selects a connection proxy before environment and redacts its cache identity",
      "Cursor HTTP/2 effective egress route treats matching selected connectionNoProxy as intentional direct egress",
      "Cursor HTTP/2 effective egress route uses environment policy only for an unselected connection",
      "Cursor HTTP/2 tunnel SessionLease closes failed proxy resources once before non-strict direct fallback",
      "Cursor HTTP/2 tunnel SessionLease destroys a returned proxy tunnel once when its HTTP/2 session errors",
      "Cursor HTTP/2 tunnel SessionLease does not direct-fallback from a strict proxy failure",
      "Cursor HTTP/2 tunnel SessionLease opens a direct session and closes it only once",
      "Cursor HTTP/2 tunnel SessionLease preserves a caller abort before a pending proxy connection and destroys it once",
      "Cursor HTTP/2 tunnel SessionLease preserves a caller abort while waiting for an HTTP/2 session",
      "Cursor HTTP/2 tunnel SessionLease preserves bytes after a split CONNECT header",
      "Cursor HTTP/2 tunnel SessionLease rejects a non-200 CONNECT response without opening HTTP/2",
      "Cursor HTTP/2 tunnel SessionLease rejects and cleans a CONNECT tunnel peer that ends before its response",
      "Cursor HTTP/2 tunnel SessionLease rejects and cleans a direct H2 session that closes before readiness",
      "Cursor HTTP/2 tunnel SessionLease rejects and cleans a proxy socket that closes before CONNECT starts",
      "Cursor HTTP/2 tunnel SessionLease rejects relay and unavailable routes without constructing a transport",
      "Cursor HTTP/2 tunnel SessionLease uses socks-proxy-agent for socks: routes",
      "Cursor HTTP/2 tunnel SessionLease uses socks-proxy-agent for socks4: routes",
      "Cursor HTTP/2 tunnel SessionLease uses socks-proxy-agent for socks4a: routes",
      "Cursor HTTP/2 tunnel SessionLease uses socks-proxy-agent for socks5: routes",
      "Cursor HTTP/2 tunnel SessionLease uses socks-proxy-agent for socks5h: routes",
      "Cursor HTTP/2 tunnel SessionLease uses verified http: CONNECT before target TLS",
      "Cursor HTTP/2 tunnel SessionLease uses verified https: CONNECT before target TLS"
    ],
    "tests/unit/huggingface-provider-routes.test.js": [
      "HuggingFace API-key validation does not expose a saved connection proxy failure",
      "HuggingFace API-key validation does not expose an upstream response as an invalid-key diagnostic",
      "HuggingFace API-key validation uses the same auth-only probe through a saved connection proxy",
      "HuggingFace API-key validation validates a new key with HuggingFace whoami"
    ],
    "tests/unit/iflow-executor.test.js": [
      "IFlowExecutor authorization and header generation sets Authorization header and signature when apiKey is provided",
      "IFlowExecutor authorization and header generation sets Authorization header fallback and signature when only accessToken is provided"
    ],
    "tests/unit/image-connect-timeout.test.js": [
      "image connect timeout propagation does not try the second combo member after an ambiguous image503",
      "image connect timeout propagation maps an uncertain response-header timeout to 502 without replay or account rotation",
      "image connect timeout propagation passes one provider/global context and the request signal into Antigravity",
      "image connect timeout propagation rejects an explicitly selected chat model without substituting an image model",
      "image connect timeout propagation requires a model instead of silently selecting an image default",
      "image connect timeout propagation returns 499 for caller cancellation without disabling the account",
      "image connect timeout propagation reuses the same settings snapshot across combo model fallback",
      "image connect timeout propagation stops an image combo after an uncertain response-header timeout",
      "image connect timeout propagation stops an image combo after caller cancellation"
    ],
    "tests/unit/image-core-orchestrator-paths.test.js": [
      "direct fetch path binaryOutput on the direct path decodes b64_json",
      "direct fetch path binaryOutput on the direct path falls back to JSON when the url fetch fails",
      "direct fetch path passes through an sseResponse from adapter.parseResponse",
      "direct fetch path refreshes credentials on 401 and retries with the new headers",
      "direct fetch path returns 400 when adapter build throws",
      "direct fetch path returns 502 when adapter.parseResponse throws",
      "direct fetch path returns 502 when fetch rejects",
      "direct fetch path returns a terminal failure when building the refreshed attempt throws",
      "direct fetch path returns the upstream error when refresh yields no credentials",
      "direct fetch path serializes string request bodies as-is",
      "direct fetch path skips refresh for noAuth adapters",
      "executor-delegating adapters binaryOutput decodes b64_json to bytes with the requested mime",
      "executor-delegating adapters binaryOutput fetches url when b64_json is absent",
      "executor-delegating adapters keeps raw body when normalize output is not OpenAI-shaped",
      "executor-delegating adapters maps AbortError to 499",
      "executor-delegating adapters maps other executor errors to 502 with formatted message",
      "executor-delegating adapters returns JSON response from executeViaExecutor and calls onRequestSuccess"
    ],
    "tests/unit/image-edits-2718.test.js": [
      "buildImageEditBody — JSON accepts a bare base64 `image` string",
      "buildImageEditBody — JSON accepts images[].image_url as a data URL",
      "buildImageEditBody — JSON refuses an oversized request before reading the body",
      "buildImageEditBody — JSON rejects a malformed JSON body",
      "buildImageEditBody — JSON rejects a remote URL rather than fetching it (SSRF)",
      "buildImageEditBody — JSON rejects an unsupported Content-Type",
      "buildImageEditBody — JSON rejects file_id explicitly",
      "buildImageEditBody — JSON requires model, prompt and at least one image",
      "buildImageEditBody — multipart accepts a single `image` file and preserves the sniffed MIME type",
      "buildImageEditBody — multipart accepts the `image[]` convention and splits refs so codex sees no duplicate",
      "buildImageEditBody — multipart coerces numeric multipart fields",
      "buildImageEditBody — multipart rejects a mask instead of silently dropping it",
      "buildImageEditBody — multipart rejects a non-image upload",
      "buildImageEditBody — multipart sniffs the real format rather than trusting the declared Content-Type",
      "handleImageEdits forwards a JSON body and the caller's auth headers to the generation path",
      "handleImageEdits returns the validation error without touching the generation path"
    ],
    "tests/unit/image-fetch-hardening.test.js": [
      "fetchImageAsBase64 hardening accepts valid PNG from public host",
      "fetchImageAsBase64 hardening rejects disguised non-image payload (magic byte mismatch)",
      "fetchImageAsBase64 hardening rejects non-http url",
      "fetchImageAsBase64 hardening rejects payload over size cap",
      "fetchImageAsBase64 hardening returns null when fetch not ok",
      "fetchImageAsBase64 hardening SSRF: rejects blocked hostname localhost",
      "fetchImageAsBase64 hardening SSRF: rejects cloud metadata 169.254.169.254",
      "fetchImageAsBase64 hardening SSRF: rejects IPv6 loopback",
      "fetchImageAsBase64 hardening SSRF: rejects private IP (10.x)"
    ],
    "tests/unit/image-generation.test.js": [
      "handleImageGenerationCore calls onRequestSuccess callback on success",
      "handleImageGenerationCore generates image with Cloudflare Workers AI JSON response",
      "handleImageGenerationCore generates image with Codex gpt-5.5-image using current Codex version header",
      "handleImageGenerationCore generates image with Gemini format",
      "handleImageGenerationCore generates image with Minimax format",
      "handleImageGenerationCore generates image with NanoBanana format",
      "handleImageGenerationCore generates image with OpenAI format",
      "handleImageGenerationCore generates image with SD WebUI format",
      "handleImageGenerationCore handles HuggingFace binary response",
      "handleImageGenerationCore handles network errors",
      "handleImageGenerationCore handles OpenRouter with HTTP-Referer header",
      "handleImageGenerationCore handles provider error responses",
      "handleImageGenerationCore handles Vercel AI Gateway image generation as OpenAI-compatible",
      "handleImageGenerationCore rejects unsupported provider",
      "handleImageGenerationCore resolves Cloudflare img2img and inpainting URL inputs before sending",
      "handleImageGenerationCore uses multipart form data for Cloudflare FLUX.2 models",
      "handleImageGenerationCore validates required prompt field"
    ],
    "tests/unit/import-authtype-2968.test.js": [
      "restoring an older backup keeps a connection's auth mode honest (#2968) agrees with the shipped registry, not just the fixture",
      "restoring an older backup keeps a connection's auth mode honest (#2968) does not hand an api-key-only provider an OAuth mode it has no flow for",
      "restoring an older backup keeps a connection's auth mode honest (#2968) keeps oauth when the registry says nothing, which is the old behaviour",
      "restoring an older backup keeps a connection's auth mode honest (#2968) keeps oauth wherever the provider can actually do oauth",
      "restoring an older backup keeps a connection's auth mode honest (#2968) never overrides an authType the backup actually carried",
      "restoring an older backup keeps a connection's auth mode honest (#2968) uses the provider's only declared mode when it is neither oauth nor apikey"
    ],
    "tests/unit/inband-upstream-error-1640.test.js": [
      "#1640 an oversized-request error carried in the stream fails the request falls back to 502 when the in-band error names no status",
      "#1640 an oversized-request error carried in the stream fails the request leaves a genuine answer alone",
      "#1640 an oversized-request error carried in the stream fails the request records the failure so it is inspectable like any other failed request",
      "#1640 an oversized-request error carried in the stream fails the request returns the upstream's own status instead of piping the error as content"
    ],
    "tests/unit/inspect-error-body.test.js": [
      "does not treat invalid UTF-8 as complete evidence",
      "finishes bounded inspection without waiting for the unconsumed tee branch",
      "propagates cancellation and removes its pending reader",
      "reads exact complete Unicode bytes without consuming the original response",
      "refuses to classify an oversized or incomplete body and preserves original bytes"
    ],
    "tests/unit/internal-cli-token-auth-3638.test.js": [
      "internal model-test authorization (#3638) accepts the generated token only over loopback (http://[::1]:20128/api/v1/images/generations)",
      "internal model-test authorization (#3638) accepts the generated token only over loopback (http://127.0.0.1:20128/api/v1/embeddings)",
      "internal model-test authorization (#3638) accepts the generated token only over loopback (http://localhost:20128/api/v1/chat/completions)",
      "internal model-test authorization (#3638) rejects a forgeable loopback URL with missing server peer proof",
      "internal model-test authorization (#3638) rejects a forgeable loopback URL with reverse-proxy loopback hop",
      "internal model-test authorization (#3638) rejects a forgeable loopback URL with stamped public peer",
      "internal model-test authorization (#3638) rejects a forgeable loopback URL with wrong server peer proof",
      "internal model-test authorization (#3638) rejects a valid token when the request URL is not loopback",
      "internal model-test authorization (#3638) rejects missing and forged loopback tokens but preserves valid API-key access",
      "internal model-test authorization (#3638) routes the required-key gate through loopback internal authorization (../../src/sse/handlers/chat.js)",
      "internal model-test authorization (#3638) routes the required-key gate through loopback internal authorization (../../src/sse/handlers/embeddings.js)",
      "internal model-test authorization (#3638) routes the required-key gate through loopback internal authorization (../../src/sse/handlers/imageGeneration.js)",
      "internal model-test authorization (#3638) routes the required-key gate through loopback internal authorization (../../src/sse/handlers/stt.js)"
    ],
    "tests/unit/inventory-activity-pagination.test.js": [
      "reaches every retained account group while keeping complete totals and exact selection across pages"
    ],
    "tests/unit/investigations-api.test.js": [
      "Authenticated persistent investigation boundary bounds bodies and refuses forged ownership or executable query fields",
      "Authenticated persistent investigation boundary denies anonymous reads and nonlocal writes before changing storage",
      "Authenticated persistent investigation boundary exports exact IDs through the actual bounded read worker and retains source bytes",
      "Authenticated persistent investigation boundary persists through actual routes, rejects conflicts, and returns no-store"
    ],
    "tests/unit/investigations.test.js": [
      "Complete evidence exports constrains selected cohorts to shared bounds and refuses conflicting dimensions",
      "Complete evidence exports distinguishes exact legacy ledger record IDs from request attribution",
      "Complete evidence exports does not guess missing selected identities or accept alternate worker operations",
      "Complete evidence exports exports all matching rows beyond page one with a completeness manifest",
      "Complete evidence exports exports only selected accounts and reports deleted comparison members",
      "Complete evidence exports persists version 4 identity filters and exports the same selected cohort as its lens",
      "Complete evidence exports refuses oversized populations atomically with no partial items",
      "Complete evidence exports uses shared filters and exclusive UTC bounds for the entire export",
      "Persistent operator workspaces never lets another owner read or modify a saved record",
      "Persistent operator workspaces persists a bounded definition, exact selection and multiaccount comparison",
      "Persistent operator workspaces rejects lost updates and stale deletes without changing stored bytes",
      "Persistent operator workspaces rejects unsupported payload fields/types 0",
      "Persistent operator workspaces rejects unsupported payload fields/types 1",
      "Persistent operator workspaces rejects unsupported payload fields/types 2",
      "Persistent operator workspaces rejects unsupported payload fields/types 3",
      "Persistent operator workspaces requires exact bookmarks and removes selection from named filter sets",
      "Persistent operator workspaces retains a historical quota series fingerprint with its exact account",
      "Persistent operator workspaces round trips version 2 exact baselines while preserving version 1 definitions",
      "Persistent operator workspaces round trips version 3 evidence cohorts while preserving version 1 and 2 definitions",
      "Persistent operator workspaces treats markup names as ordinary strings and parameterizes filters",
      "Persistent operator workspaces validates fixed UTC bounds and exact Context identities"
    ],
    "tests/unit/jina-reader-fetch.test.js": [
      "Jina Reader fetch returns the upstream status and error body",
      "Jina Reader fetch uses Jina's JSON POST API instead of embedding the URL in the path"
    ],
    "tests/unit/jina-return-format-2239.test.js": [
      "jina-reader honours the requested format (#2239) omits the header rather than sending a bad one",
      "jina-reader honours the requested format (#2239) only sends a format Jina accepts, so an unknown value cannot 400 the fetch",
      "jina-reader honours the requested format (#2239) reports the format it actually asked for",
      "jina-reader honours the requested format (#2239) sends the format on the header Jina reads",
      "jina-reader honours the requested format (#2239) still sends the url in the body and the key when there is one",
      "jina-reader honours the requested format (#2239) the sibling backends already forwarded the format, which is the asymmetry"
    ],
    "tests/unit/json-proxy-app.test.js": [
      "handleJsonProxy cancellation and timeout behavior does not resend a possibly accepted request after a response-body timeout",
      "handleJsonProxy cancellation and timeout behavior returns a body-phase client cancellation without marking or rotating the selected account"
    ],
    "tests/unit/json-proxy-core.test.js": [
      "handleJsonProxyCore transport safety classifies a body-phase client cancellation without treating it as an upstream success",
      "handleJsonProxyCore transport safety classifies a body-phase timeout as a retryable gateway timeout",
      "handleJsonProxyCore transport safety does not bypass a strict proxy failure with a direct request",
      "handleJsonProxyCore transport safety marks a client-aborted request without classifying the account as unavailable",
      "handleJsonProxyCore transport safety reports a bounded upstream timeout as a retryable gateway timeout",
      "handleJsonProxyCore transport safety uses the selected connection's proxy, relay, no-proxy, and strict-routing settings"
    ],
    "tests/unit/jsonFence.test.js": [
      "stripJsonFence leaves prose containing a fence untouched",
      "stripJsonFence leaves unfenced JSON untouched",
      "stripJsonFence passes through non-strings",
      "stripJsonFence unwraps a ```json fence",
      "stripJsonFence unwraps a bare ``` fence",
      "unfenceJsonChoices leaves content alone when JSON was not requested",
      "unfenceJsonChoices tolerates a tool-call message with null content",
      "unfenceJsonChoices unfences assistant content in JSON mode",
      "wantsJsonOutput is false without response_format",
      "wantsJsonOutput is true for json_schema and json_object"
    ],
    "tests/unit/key-budget-presentation.test.js": [
      "does not convert missing monetary or token values to zero",
      "explains strict incomplete records and unsupported durable storage",
      "keeps an absent projection and absent historic coverage unknown",
      "labels unknown outstanding bounds independently of a small recorded balance",
      "prioritizes disabled or expired keys and does not promise future admission",
      "recognizes a literal zero ceiling as exhausted",
      "uses the lifetime ledger and includes held allowance when explaining a capped key"
    ],
    "tests/unit/key-client-attribution.test.js": [
      "carries attribution on the keys list without disclosing the secret",
      "distinguishes a key whose requests named no client from one with no requests",
      "falls back to the last attributed client when the newest request named none",
      "names the client from the most recent attributed request",
      "never attributes one key's traffic to another",
      "reports no attribution at all for a key with no retained requests",
      "still serves the keys list when attribution cannot be read"
    ],
    "tests/unit/key-disclosure.test.js": [
      "discloses exactly the selected key only through the explicit operator action",
      "does not disclose key metadata to an anonymous or inference-only caller",
      "does not inherit future private fields through public projection",
      "redacts stored secrets from list, detail, update and device responses",
      "refreshes exact held allowance after dispatch uncertainty without disclosing the key",
      "refuses unauthorized disclosure and writes with {\"x-inference\":\"yes\"}",
      "refuses unauthorized disclosure and writes with {\"x-operator\":\"yes\",\"x-peer\":\"remote\"}",
      "refuses unauthorized disclosure and writes with {}",
      "reports a missing key without disclosing other credentials",
      "returns a newly issued credential once in the creation response"
    ],
    "tests/unit/key-reveal-audit.test.js": [
      "never returns stored key material from any enumerating response",
      "records a failed disclosure for an id that resolves to no key",
      "refuses to disclose when the audit event cannot be retained",
      "retains an audit event for the disclosure, carrying no key material",
      "writes no audit event for a disclosure the guard refused"
    ],
    "tests/unit/key-reveal-ui.test.js": [
      "clears a revealed credential when the tab becomes hidden",
      "creates a key from the inline row and shows its value once",
      "does not put a delayed reveal on screen after the tab was left",
      "expands one key in place and never opens a dialog",
      "keeps a refused save visible beside the field it came from",
      "reveals one key only after an explicit inline confirmation, then clears it",
      "saves the protection policy from its own field, sending only that field",
      "selects a key through its enclosing label without configuring or mutating it",
      "shows a refused reveal as its own sentence without producing a credential"
    ],
    "tests/unit/key-rotation-overlap.test.js": [
      "carries the predecessor's access settings and profile linkage onto the successor",
      "keeps the old secret valid for the chosen overlap window",
      "never extends a key past an expiry it already had",
      "refuses an out-of-range window and a second rotation of the same key",
      "refuses rotation from an unauthorized caller and leaves the key untouched",
      "retains an audit event for the rotation, carrying neither secret",
      "returns the successor secret once and never through the list",
      "stops the old secret once the window has passed, leaving the successor working",
      "supports a zero-hour window as an explicit choice, and requires the choice"
    ],
    "tests/unit/key-usage-analytics.test.js": [
      "accepts no caller SQL, credential or path and bounds the output population",
      "connects the real worker result through the protected listing",
      "keeps all-missing fields unknown and never infers a rotated credential match",
      "makes persisted sql.js freshness explicit without reading its writer memory",
      "queries complete recorded history off-thread with public stable IDs and unknown counts",
      "reports historical zero prices without pricing provenance separately",
      "returns current controls and budget despite a stalled optional historical query"
    ],
    "tests/unit/keys-lifecycle-render.test.js": [
      "distinguishes behind from drifted in the rendered copy",
      "does not present an edited expiry as the original overlap deadline",
      "gives a superseded key its deadline and a successor its lineage",
      "names the drifted fields and the profile it drifted from",
      "reports an unavailable baseline rather than vouching for the key",
      "says a key with nothing recorded is unknown, never zero",
      "separates a key whose requests named no client from one with no requests"
    ],
    "tests/unit/kilocode-free-models-1995.test.js": [
      "#1995 kilocode registry prerequisite category stays oauth (chat still needs a real device-code token), but now syncs",
      "#1995 kilocode registry prerequisite declares a modelsFetcher for the full gateway catalog",
      "#1995 kilocode registry prerequisite opts into passthroughModels"
    ],
    "tests/unit/kilocode-free-selector-1995.test.js": [
      "Kilo Code free models reach the combo selector (#1995) includes kilocode in the sync targets despite category oauth",
      "Kilo Code free models reach the combo selector (#1995) persists the catalog per provider id and isolates other providers' failures",
      "Kilo Code free models reach the combo selector (#1995) re-syncs without duplicating models already captured by the last snapshot",
      "Kilo Code free models reach the combo selector (#1995) writes the fetched catalog into customModels under the selector's alias"
    ],
    "tests/unit/kimchi-executor-transform.test.js": [
      "KimchiExecutor.buildHeaders bearer auth plus the live kimchi User-Agent",
      "KimchiExecutor.transformRequest — gateway drops drops every Anthropic top-level field the OpenAI gateway rejects",
      "KimchiExecutor.transformRequest — gateway drops merges a top-level system prompt into the message list instead of dropping it",
      "KimchiExecutor.transformRequest — gateway drops prepends to an existing system message rather than adding a second one",
      "KimchiExecutor.transformRequest — gateway drops strips cache_control and signature from message parts and tools",
      "KimchiExecutor.transformRequest — gateway drops strips reasoning params for claude-named models (anthropic-backed), keeps them otherwise",
      "KimchiExecutor.transformRequest — gateway drops strips reasoning when the cached catalog marks the model anthropic-backed by metadata, not name",
      "kimchiModels.buildKimchiModelsUrl defaults to the kimchi API and normalizes trailing slashes on overrides",
      "kimchiModels.getCachedKimchiModelMetadata resolves by id, case-insensitively, and through a provider/ prefix",
      "kimchiModels.normalizeKimchiModel maps limits and modalities into capabilities, and flags anthropic compat",
      "kimchiModels.normalizeKimchiModel rejects entries without an id and tolerates junk",
      "kimchiModels.resolveKimchiModels — fetch, cache, errors an empty or unparseable model list is not cached as success",
      "kimchiModels.resolveKimchiModels — fetch, cache, errors no token → null without any network call (fail closed, no anonymous request)",
      "kimchiModels.resolveKimchiModels — fetch, cache, errors non-2xx catalog → null, warns with the status, marks 429/5xx retryable",
      "kimchiModels.resolveKimchiModels — fetch, cache, errors sends the bearer token to the kimchi models endpoint and caches the catalog"
    ],
    "tests/unit/kimchi-request-headers.test.js": [
      "Kimchi request headers uses the refreshed User-Agent on later requests through the production executor"
    ],
    "tests/unit/kimchi-strip-reasoning.test.js": [
      "kimchi executor wiring default export is KimchiExecutor class",
      "kimchi executor wiring KimchiExecutor extends DefaultExecutor via prototype chain",
      "kimchi stripReasoningContent handles multi-turn: strips old turns, keeps recent one",
      "kimchi stripReasoningContent ignores assistant messages that have no reasoning_content",
      "kimchi stripReasoningContent leaves non-assistant messages untouched",
      "kimchi stripReasoningContent preserves short custom reasoning under the threshold",
      "kimchi stripReasoningContent preserves the 1-char placeholder that injectReasoningContent sets",
      "kimchi stripReasoningContent removes long reasoning_content from assistant messages but keeps content",
      "kimchi stripReasoningContent returns early on missing/empty messages array"
    ],
    "tests/unit/kimchi.test.js": [
      "kimchi oauth accepts a callback with matching state and returns the token",
      "kimchi oauth builds the cli-auth URL with encoded callback + state",
      "kimchi OAuth dedup different email never matches",
      "kimchi OAuth dedup incoming without username does not match legacy row with username",
      "kimchi OAuth dedup legacy row without username matches incoming without username (backward compat)",
      "kimchi OAuth dedup same email + different username = NO match (cross-IdP, the bug)",
      "kimchi OAuth dedup same email + same username = dedup (re-login same IdP)",
      "kimchi OAuth dedup workspaces still dedupe on workspace ID when both sides have one",
      "kimchi oauth rejects a callback whose state does not match",
      "kimchi registry entry exposes the upstream static models",
      "kimchi registry entry is an oauth provider auto-listed via byCategory",
      "kimchi registry entry passes through models not in the static list",
      "kimchi registry entry points at the OpenAI-compatible gateway with an authenticated UA",
      "kimchi registry entry uses Bearer auth",
      "kimchi validateToken 200 → valid",
      "kimchi validateToken 401 → invalid, expired message",
      "kimchi validateToken 403 → invalid, scope message",
      "kimchi validateToken unknown / network error → fail-open valid",
      "kimchiModels falls back to slug as name when display_name is empty",
      "kimchiModels maps Kimchi metadata entries to tokenproxy model shape",
      "kimchiModels returns empty array for non-array input"
    ],
    "tests/unit/kimi-apikey-2881.test.js": [
      "credential-scoped selection is opt-in (#2881) a credential kind no transport claims falls back to the unscoped entry",
      "credential-scoped selection is opt-in (#2881) a custom node's compiled transports are unaffected",
      "credential-scoped selection is opt-in (#2881) a provider declaring no authModes resolves identically for both credential kinds",
      "credential-scoped selection is opt-in (#2881) an unmatched format resolves to null unless a transport claims the credential",
      "credential-scoped selection is opt-in (#2881) every provider without an authModes transport still returns its first format match",
      "credentialAuthMode normalizes the stored authType (#2881) falls back to the credential shape when authType is absent",
      "credentialAuthMode normalizes the stored authType (#2881) reads both spellings of an API key",
      "credentialAuthMode normalizes the stored authType (#2881) treats a hand-pasted access_token as OAuth",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) a caller passing no credentials keeps the pre-change endpoint",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) an api-key connection reaches the platform anthropic endpoint",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) an api-key connection reaches the platform chat endpoint",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) the platform transports carry the auth shape their endpoint needs",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) the platform transports drop the Kimi Code CLI device headers",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) the whole upstream route follows, not just the transport lookup",
      "kimi routes an API key to the platform, not the coding endpoint (#2881) the working OAuth subscription route is untouched"
    ],
    "tests/unit/kimi-cn-2510.test.js": [
      "kimi-cn reaches the China-region platform (#2510) carries the auth shape each endpoint needs",
      "kimi-cn reaches the China-region platform (#2510) every endpoint it declares is on the China host",
      "kimi-cn reaches the China-region platform (#2510) is API-key only, since Kimi Code OAuth is international",
      "kimi-cn reaches the China-region platform (#2510) is registered exactly once and resolves as its own provider",
      "kimi-cn reaches the China-region platform (#2510) leaves the international entry alone",
      "kimi-cn reaches the China-region platform (#2510) lists the platform model ids and omits the coding-subscription ones",
      "kimi-cn reaches the China-region platform (#2510) mirrors the sibling pattern rather than inventing one",
      "kimi-cn reaches the China-region platform (#2510) picks the endpoint matching the client format"
    ],
    "tests/unit/kimi-connection-test-authmode-3190.test.js": [
      "the Kimi connection test probes the host matching the credential (#3190) leaves the subscription probe untouched for a subscription credential",
      "the Kimi connection test probes the host matching the credential (#3190) never sends an API key to the subscription host",
      "the Kimi connection test probes the host matching the credential (#3190) validates a platform API key against the platform balance endpoint"
    ],
    "tests/unit/kimi-responses-transport-943.test.js": [
      "a format kimi declares no transport for still respects the credential (#943) a Responses-API client with a platform key reaches the platform, not the subscription host",
      "a format kimi declares no transport for still respects the credential (#943) every unmatched format an API key can arrive on lands on the platform",
      "a format kimi declares no transport for still respects the credential (#943) it carries the platform's own auth shape, not the subscription host's",
      "a format kimi declares no transport for still respects the credential (#943) the target format is unchanged, so only the host and auth scheme move",
      "the subscription route and every other provider are untouched (#943) a caller passing no credentials keeps resolving to null on an unmatched format",
      "the subscription route and every other provider are untouched (#943) a custom compatible node is unaffected — its transports declare no authModes",
      "the subscription route and every other provider are untouched (#943) an OAuth subscription token on an unmatched format keeps the coding endpoint",
      "the subscription route and every other provider are untouched (#943) no other provider in the registry changes, because none opts in",
      "the subscription route and every other provider are untouched (#943) the formats kimi does declare still resolve exactly as #2881 left them"
    ],
    "tests/unit/kiro-agentic-prompt-override-2469.test.js": [
      "the chunked-write prompt is opt-in and tunable (#2469) an empty value attaches nothing at all",
      "the chunked-write prompt is opt-in and tunable (#2469) an operator can replace the rule, which is what the report wants",
      "the chunked-write prompt is opt-in and tunable (#2469) is attached only to the agentic model variant",
      "the chunked-write prompt is opt-in and tunable (#2469) is read at use, so a change needs no restart",
      "the chunked-write prompt is opt-in and tunable (#2469) the shipped default is still exported for anything that documents it",
      "the chunked-write prompt is opt-in and tunable (#2469) uses the shipped text when nothing overrides it"
    ],
    "tests/unit/kiro-api-key-endpoint-routing.test.js": [
      "Kiro auth-aware endpoint routing builds endpoint-specific headers",
      "Kiro auth-aware endpoint routing keeps Builder ID OAuth on the Kiro runtime surface",
      "Kiro auth-aware endpoint routing keeps external IdP on CodeWhisperer before Amazon Q",
      "Kiro auth-aware endpoint routing regionalizes AWS endpoints for IDC without changing Kiro runtime",
      "Kiro auth-aware endpoint routing retries only endpoint/auth-surface failures, not payload-invalid 400s",
      "Kiro auth-aware endpoint routing routes API-key inference through Amazon Q before other surfaces"
    ],
    "tests/unit/kiro-catalog-body-timeout-1357.test.js": [
      "Kiro catalog fetch body deadline (#1357) aborts a body that stalls after the headers arrive",
      "Kiro catalog fetch body deadline (#1357) aborts a stalled error body too, not just the success path",
      "Kiro catalog fetch body deadline (#1357) clears the timer on the normal path, leaving no handle behind"
    ],
    "tests/unit/kiro-connection-test-region-3497.test.js": [
      "Kiro connection-test region is validated before credentialed egress (#3497) leaves the social refresh path, which reads no region, working",
      "Kiro connection-test region is validated before credentialed egress (#3497) makes zero requests when the saved region is not an AWS region",
      "Kiro connection-test region is validated before credentialed egress (#3497) rejects a region carrying a path separator that re-hosts the token endpoint",
      "Kiro connection-test region is validated before credentialed egress (#3497) still refreshes through the real endpoint for a valid region"
    ],
    "tests/unit/kiro-conversation-canonicalization.test.js": [
      "Kiro conversation canonicalizer deduplicates extra results without losing their text",
      "Kiro conversation canonicalizer does not fabricate a continuation on a current structured tool turn for Claude",
      "Kiro conversation canonicalizer does not fabricate a continuation on a current structured tool turn for OpenAI",
      "Kiro conversation canonicalizer does not fabricate a continuation on an image-only turn for Claude",
      "Kiro conversation canonicalizer does not fabricate a continuation on an image-only turn for OpenAI",
      "Kiro conversation canonicalizer does not let session replay replace a tool-result turn",
      "Kiro conversation canonicalizer does not mutate the source conversation or tool definitions",
      "Kiro conversation canonicalizer flattens a trailing unanswered assistant tool call and creates a current user turn",
      "Kiro conversation canonicalizer flattens malformed input and tool uses missing from the current specs",
      "Kiro conversation canonicalizer flattens non-adjacent and orphaned tool results",
      "Kiro conversation canonicalizer flattens structured history when the client sent no tool specs",
      "Kiro conversation canonicalizer keeps complete parallel tool pairs structured",
      "Kiro conversation canonicalizer keeps the answered parallel call and flattens only the missing one",
      "Kiro conversation canonicalizer normalizes names and recursively removes unsupported schema fields",
      "Kiro conversation canonicalizer preserves Claude tool_result errors",
      "Kiro conversation canonicalizer remaps duplicate tool IDs together with their adjacent results",
      "Kiro conversation canonicalizer repairs a 30-call parallel turn with one missing result",
      "Kiro conversation canonicalizer repairs partial parallel results in both direct translators",
      "Kiro conversation canonicalizer retains continuation for a truly empty structural user turn for Claude",
      "Kiro conversation canonicalizer retains continuation for a truly empty structural user turn for OpenAI",
      "Kiro conversation canonicalizer uses the neutral nonempty placeholder for a historical structured tool turn for Claude",
      "Kiro conversation canonicalizer uses the neutral nonempty placeholder for a historical structured tool turn for OpenAI"
    ],
    "tests/unit/kiro-conversation-merge-and-repair.test.js": [
      "flatten-everything retry on post-reconcile validation failure a spec set that drifts between reconcile and validate flattens all structured tools",
      "normalizeKiroToolSpecs collisions and limits collision on a name at the length cap re-trims so the suffix still fits",
      "normalizeKiroToolSpecs collisions and limits two names sanitizing to the same identifier get a numeric suffix",
      "same-role turn merging consecutive assistant turns merge content and toolUses",
      "same-role turn merging consecutive user turns merge content, images and tool results",
      "same-role turn merging stale tools on a history user turn are removed",
      "validateKiroConversation direct checks flags a call without a matching result and a missing current content"
    ],
    "tests/unit/kiro-eventstream-frame-decoder.test.js": [
      "EventStream empty payloads treats a zero-length and a whitespace-only payload as no payload",
      "EventStream frame validation rejects a payload that is not valid JSON",
      "EventStream header value types decodes a frame carrying every header value type alongside the event type",
      "EventStream header value types keeps a rejected header type private without a replacement generation",
      "EventStream header value types rejects a header whose declared value runs past the header block",
      "EventStream header value types rejects an unknown header value type"
    ],
    "tests/unit/kiro-external-idp-normalize.test.js": [
      "buildExternalIdpRefreshParams builds a form-encoded refresh body from providerSpecificData, snake_case aliases included",
      "buildExternalIdpRefreshParams requires refreshToken, clientId and scope, and validates the endpoint",
      "decodeJwtPayload decodes a base64url payload and returns null for anything malformed",
      "normalizeKiroExternalIdpAuth accepts camelCase field aliases",
      "normalizeKiroExternalIdpAuth falls back through preferred_username / upn / sub for the email claim",
      "normalizeKiroExternalIdpAuth imports a valid JSON string round-trip with normalized providerSpecificData",
      "normalizeKiroExternalIdpAuth rejects a region that fails the shared AWS pattern",
      "normalizeKiroExternalIdpAuth rejects invalid JSON, non-objects, and non-external_idp auth methods",
      "normalizeKiroExternalIdpAuth requires each credential field by name",
      "normalizeKiroExternalIdpAuth resolves expiresAt from explicit timestamp, then expires_in, then JWT exp, then default",
      "normalizeScope joins arrays with trimming and blank-dropping, trims strings, empties otherwise",
      "validateMicrosoftTokenEndpoint accepts an https Microsoft login endpoint and normalizes it to a URL string",
      "validateMicrosoftTokenEndpoint rejects empty, unparseable, non-https, and non-Microsoft endpoints"
    ],
    "tests/unit/kiro-external-idp.test.js": [
      "Kiro external_idp (CLIProxyAPI) import and refresh adds CodeWhisperer external IdP headers and endpoint ordering",
      "Kiro external_idp (CLIProxyAPI) import and refresh fails closed before URL construction for a legacy unsafe external IdP region",
      "Kiro external_idp (CLIProxyAPI) import and refresh imports CLIProxyAPI external_idp JSON as a Kiro OAuth connection",
      "Kiro external_idp (CLIProxyAPI) import and refresh refreshes Microsoft external_idp tokens with form-encoded OAuth body",
      "Kiro external_idp (CLIProxyAPI) import and refresh rejects an imported external IdP region that could alter an AWS hostname",
      "Kiro external_idp (CLIProxyAPI) import and refresh rejects external_idp refresh endpoints outside Microsoft login",
      "Kiro external_idp (CLIProxyAPI) import and refresh sends TokenType for external_idp Kiro usage probes"
    ],
    "tests/unit/kiro-generic-thinking-fields-3641.test.js": [
      "Kiro rejects generic thinking fields (#3641) keeps the effort fields Kiro does accept, nested under additionalModelRequestFields",
      "Kiro rejects generic thinking fields (#3641) strips a top-level thinking object left by a Responses client",
      "Kiro rejects generic thinking fields (#3641) strips the same fields for a Gemini client"
    ],
    "tests/unit/kiro-model-id-spelling-2308.test.js": [
      "Kiro model id spelling on the wire (#2308) leaves every registry id byte-identical on the wire",
      "Kiro model id spelling on the wire (#2308) normalizes a dashed client id onto the registry spelling, then sends that",
      "Kiro model id spelling on the wire (#2308) passes claude-sonnet-5 through untouched, so no transform can explain its rejection",
      "Kiro model id spelling on the wire (#2308) sends claude-haiku-4.5 with its dot intact rather than dashing it",
      "Kiro model id spelling on the wire (#2308) sends claude-opus-4.8 with its dot intact rather than dashing it",
      "Kiro model id spelling on the wire (#2308) sends claude-sonnet-4.5 with its dot intact rather than dashing it",
      "Kiro model id spelling on the wire (#2308) strips the synthetic thinking suffix without disturbing the version separator"
    ],
    "tests/unit/kiro-models-region-validation.test.js": [
      "Kiro live model catalog region validation rejects an unsafe profile ARN region before sending its Bearer token",
      "Kiro live model catalog region validation uses a validated profile ARN region for its Bearer-authenticated catalog URL"
    ],
    "tests/unit/kiro-no-fake-continue-2182.test.js": [
      "kiro does not fabricate a 'continue' instruction (#2182) a genuinely bare turn still says continue",
      "kiro does not fabricate a 'continue' instruction (#2182) a tool-result turn is not given a user instruction",
      "kiro does not fabricate a 'continue' instruction (#2182) content is never empty, which is what the fallback is for",
      "kiro does not fabricate a 'continue' instruction (#2182) real user text is never replaced",
      "kiro does not fabricate a 'continue' instruction (#2182) the tool results themselves survive"
    ],
    "tests/unit/kiro-nonstream-error.test.js": [
      "Kiro non-streaming error propagation prefers a terminal SSE error over earlier semantic chunks",
      "Kiro non-streaming error propagation returns 502 instead of collapsing a failed Kiro SSE stream into stop"
    ],
    "tests/unit/kiro-oauth-provider-coverage.test.js": [
      "mapTokens applies config defaults for missing region, authMethod, startUrl and profileArn",
      "mapTokens maps snake_case tokens, extracts the JWT email, and keeps provider data",
      "pollToken defaults to authorization_pending when the body carries no error field",
      "pollToken falls back to the default-region token URL with no extraData",
      "pollToken POSTs the device code with stored client credentials and maps camelCase tokens",
      "pollToken rejects an invalid stored region before any network call",
      "pollToken returns pending errors with the upstream description or message",
      "pollToken wraps a non-JSON response as invalid_response with the raw text",
      "requestDeviceCode defaults the poll interval when the upstream omits it",
      "requestDeviceCode honors a custom region, startUrl and idc auth method",
      "requestDeviceCode registers a client from the config then requests device authorization",
      "requestDeviceCode rejects an invalid region before any network call",
      "requestDeviceCode throws with the upstream text when client registration fails",
      "requestDeviceCode throws with the upstream text when device authorization fails"
    ],
    "tests/unit/kiro-oauth-service.test.js": [
      "extractEmailFromJWT reads email, then preferred_username, then sub",
      "extractEmailFromJWT returns null for malformed input instead of throwing",
      "pollDeviceToken reports a terminal error as non-pending",
      "pollDeviceToken reports authorization_pending as pending, not failure",
      "pollDeviceToken reports slow_down as pending, not failure",
      "pollDeviceToken returns the token bundle on success with the device-code grant",
      "profile and model listing falls back to the first profile when no region matches, null when empty",
      "profile and model listing maps models with defaults for missing name and token limits",
      "profile and model listing prefers the profile whose ARN region matches the requested region",
      "refreshToken propagates a refresh failure from either branch",
      "refreshToken rejects an invalid stored region before fetching (AWS branch)",
      "refreshToken uses the AWS OIDC branch when clientId+clientSecret are present, in the stored region",
      "refreshToken uses the social branch without client credentials and defaults expiresIn",
      "registerClient propagates the upstream error body on non-ok",
      "registerClient rejects a region the shared AWS pattern rejects, before any fetch",
      "registerClient sends the registration contract from KIRO_CONFIG and returns the client triple",
      "social login: build URL and code exchange agree on redirect_uri exchanges the code with the SAME redirect_uri the login URL advertised",
      "social login: build URL and code exchange agree on redirect_uri maps provider to idp and carries challenge/state through",
      "social login: build URL and code exchange agree on redirect_uri throws with the upstream body when the exchange fails",
      "startDeviceAuthorization keeps a provider-supplied interval",
      "startDeviceAuthorization passes the client triple through and defaults the poll interval to 5",
      "startDeviceAuthorization throws with the upstream body on non-ok",
      "validateApiKey accepts a key only when the model catalog is non-empty, and trims it",
      "validateApiKey rejects an empty or whitespace key without network",
      "validateApiKey wraps an empty catalog as a validation failure (200-with-nothing is not proof)",
      "validateImportToken rejects a token missing the expected prefix without any network call",
      "validateImportToken validates by refreshing and tags the result as imported",
      "validateImportToken wraps a refresh failure as a validation failure"
    ],
    "tests/unit/kiro-profile-arn.test.js": [
      "kiro API-key auth (KiroService.validateApiKey) rejects a 200 response with an empty model catalog",
      "kiro API-key auth (KiroService.validateApiKey) rejects an empty API key without a network call",
      "kiro API-key auth (KiroService.validateApiKey) surfaces a validation error when the key is rejected",
      "kiro API-key auth (KiroService.validateApiKey) validates an API key against Amazon Q without inventing profileArn"
    ],
    "tests/unit/kiro-refresh-region-ssrf-3497.test.js": [
      "Kiro IDC refresh region pinning (#3497) sends no credentials anywhere but AWS for a.evil.example/b (path-split re-host)",
      "Kiro IDC refresh region pinning (#3497) sends no credentials anywhere but AWS for US-EAST-1 (wrong case)",
      "Kiro IDC refresh region pinning (#3497) sends no credentials anywhere but AWS for us-east-1.amazonaws.com.evil.example (suffix re-host)",
      "Kiro IDC refresh region pinning (#3497) sends no credentials anywhere but AWS for us-east-1/ (trailing slash)",
      "Kiro IDC refresh region pinning (#3497) still refreshes over a well-formed region"
    ],
    "tests/unit/kiro-schema-normalization.test.js": [
      "Kiro root schema normalization clones object-valued enum and const data byte-for-byte",
      "Kiro root schema normalization counts empty and reference-only alternatives when intersecting required names",
      "Kiro root schema normalization falls back to an empty object schema for \"not-a-schema\"",
      "Kiro root schema normalization falls back to an empty object schema for 42",
      "Kiro root schema normalization falls back to an empty object schema for null",
      "Kiro root schema normalization falls back to an empty object schema for true",
      "Kiro root schema normalization falls back to an empty object schema for undefined",
      "Kiro root schema normalization is deterministic, idempotent, and shares no mutable schema references",
      "Kiro root schema normalization normalizes the same schema through both real request translators",
      "Kiro root schema normalization preserves a root reference but never lifts a consumed branch reference",
      "Kiro root schema normalization preserves keyword-shaped definition names and their surviving references",
      "Kiro root schema normalization preserves keyword-shaped dependentRequired names",
      "Kiro root schema normalization preserves keyword-shaped names in root and nested property maps",
      "Kiro root schema normalization preserves supported non-composition keywords and removes empty required",
      "Kiro root schema normalization recursively folds root composition with deterministic properties and requirements",
      "Kiro root schema normalization skips invalid combinator members and prunes malformed required entries",
      "Kiro root schema normalization still strips real schema keywords at root and nested schema nodes"
    ],
    "tests/unit/kiro-system-prompt-schema-2989.test.js": [
      "kiro payload carries no top-level systemPrompt (#2989) never regains the field once tools and images are present",
      "kiro payload carries no top-level systemPrompt (#2989) omits the field: claude route, client system prompt",
      "kiro payload carries no top-level systemPrompt (#2989) omits the field: claude route, no client system prompt",
      "kiro payload carries no top-level systemPrompt (#2989) omits the field: claude route, thinking intensity with no client system prompt (#2716)",
      "kiro payload carries no top-level systemPrompt (#2989) omits the field: openai route, client system prompt",
      "kiro payload carries no top-level systemPrompt (#2989) omits the field: openai route, no client system prompt",
      "kiro payload carries no top-level systemPrompt (#2989) still delivers the text on the user message: claude route, client system prompt",
      "kiro payload carries no top-level systemPrompt (#2989) still delivers the text on the user message: claude route, thinking intensity with no client system prompt (#2716)",
      "kiro payload carries no top-level systemPrompt (#2989) still delivers the text on the user message: openai route, client system prompt"
    ],
    "tests/unit/kiro-terminal-integrity.test.js": [
      "Kiro terminal integrity recovery accepts CLI-compatible text and usage frames at clean EOF without messageStop",
      "Kiro terminal integrity recovery accepts messageStop without semantic output as explicit completion",
      "Kiro terminal integrity recovery cannot expose an error body from an undispatched retry",
      "Kiro terminal integrity recovery caps diagnostic event-type cardinality",
      "Kiro terminal integrity recovery classifies failure cancelled before validating a malformed deferred tool",
      "Kiro terminal integrity recovery classifies failure content_filtered before validating a malformed deferred tool",
      "Kiro terminal integrity recovery classifies failure max_tokens before validating a malformed deferred tool",
      "Kiro terminal integrity recovery continues past eager tool-shape errors to authoritative stop cancelled",
      "Kiro terminal integrity recovery continues past eager tool-shape errors to authoritative stop content_filtered",
      "Kiro terminal integrity recovery continues past eager tool-shape errors to authoritative stop max_tokens",
      "Kiro terminal integrity recovery counts deferred tool fragments against the private memory bound",
      "Kiro terminal integrity recovery does not let a valid tool override failure stop reason cancelled",
      "Kiro terminal integrity recovery does not let a valid tool override failure stop reason content_filtered",
      "Kiro terminal integrity recovery does not let a valid tool override failure stop reason max_tokens",
      "Kiro terminal integrity recovery does not let a valid tool override failure stop reason novel_reason",
      "Kiro terminal integrity recovery does not retry legitimate final: I'll check the logs. They show no errors and deployment succeeded.",
      "Kiro terminal integrity recovery does not retry legitimate final: Let me check: status is 200 and the checksum matches abc123.",
      "Kiro terminal integrity recovery does not retry legitimate final: The verification is complete and all tests passed.",
      "Kiro terminal integrity recovery does not retry legitimate final: Working...",
      "Kiro terminal integrity recovery does not retry legitimate final: 已完成驗證，所有測試均通過。",
      "Kiro terminal integrity recovery does not retry legitimate final: 我會檢查版本。版本是 1.2.3。",
      "Kiro terminal integrity recovery does not retry legitimate final: 接下來請你先批准部署，我會等待你的確認。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504，且主副池均未重啟。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補充兩點已確認的結果。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查 504 access log，確認 host／路徑與是否為集中流量（答案是否定的）。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查 504 access log，確認 host／路徑與有無集中流量：無集中流量。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查 504 access log，結果顯示沒有集中流量。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查，結果顯示沒有集中流量。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查：結果顯示沒有集中流量。",
      "Kiro terminal integrity recovery does not retry legitimate final: 目前證據顯示只有少量 504。最後補查結果顯示沒有集中流量。",
      "Kiro terminal integrity recovery does not spend another generation on incomplete-final repair",
      "Kiro terminal integrity recovery does not start a second generation after an accepted empty response",
      "Kiro terminal integrity recovery does not turn model-controlled parser detail into a retry instruction",
      "Kiro terminal integrity recovery fails closed for stop reason cancelled",
      "Kiro terminal integrity recovery fails closed for stop reason content_filtered",
      "Kiro terminal integrity recovery fails closed for stop reason novel_reason",
      "Kiro terminal integrity recovery fails closed for stop reason pause_turn",
      "Kiro terminal integrity recovery fails safely when the private gate exceeds its configured bound",
      "Kiro terminal integrity recovery keeps semantic output private behind a heartbeat until clean EOF",
      "Kiro terminal integrity recovery lets a complete tool call override metadata end_turn",
      "Kiro terminal integrity recovery maps max_tokens without treating it as a normal stop",
      "Kiro terminal integrity recovery parses frames split across chunks and multiple frames in one chunk",
      "Kiro terminal integrity recovery prefers a non-retryable terminal reason over an earlier retryable reason",
      "Kiro terminal integrity recovery preserves the most restrictive conflicting stop reason",
      "Kiro terminal integrity recovery preserves the most restrictive conflicting stop reason",
      "Kiro terminal integrity recovery propagates cancellation while validation is waiting for EOF",
      "Kiro terminal integrity recovery propagates EventStream error without retry or leakage",
      "Kiro terminal integrity recovery propagates EventStream exception without retry or leakage",
      "Kiro terminal integrity recovery rejects a non-string toolUseId without dispatching again",
      "Kiro terminal integrity recovery rejects a raw chunk before concatenating beyond the protocol bound",
      "Kiro terminal integrity recovery rejects accepted duplicate headers without replay or semantic leakage",
      "Kiro terminal integrity recovery rejects accepted ellipsis final ... without another generation",
      "Kiro terminal integrity recovery rejects accepted ellipsis final … without another generation",
      "Kiro terminal integrity recovery rejects accepted message CRC without replay or semantic leakage",
      "Kiro terminal integrity recovery rejects accepted out-of-bounds headers without replay or semantic leakage",
      "Kiro terminal integrity recovery rejects accepted prelude CRC without replay or semantic leakage",
      "Kiro terminal integrity recovery rejects accepted truncated frame without replay or semantic leakage",
      "Kiro terminal integrity recovery rejects incomplete future-action final without replay: Let me check the remaining failures.",
      "Kiro terminal integrity recovery rejects incomplete future-action final without replay: Next I'll verify the deployment logs.",
      "Kiro terminal integrity recovery rejects incomplete future-action final without replay: 我會重新抓取最新日誌並確認結果。",
      "Kiro terminal integrity recovery rejects incomplete future-action final without replay: 接下來我只再確認部署結果。",
      "Kiro terminal integrity recovery rejects incomplete future-action final without replay: 目前證據顯示只在 **03:48:30–03:49:00 TPE** 出現少量 NonKA 504；主池 106/106、副池 50/50，且兩池都沒有重啟。最後補查 504 access log，確認 host／路徑與是否為集中流量。",
      "Kiro terminal integrity recovery rejects malformed wrapper tools without replay or leaking the invalid call",
      "Kiro terminal integrity recovery rejects malformed_model_output without replay or semantic leakage",
      "Kiro terminal integrity recovery reports an accepted empty stream without reaching a later HTTP response",
      "Kiro terminal integrity recovery reports corrupt-frame provenance from the sole accepted attempt",
      "Kiro terminal integrity recovery requires complete direct tool input and keeps the failure private",
      "Kiro terminal integrity recovery terminates an accepted response-body read failure without replay",
      "Kiro terminal integrity recovery terminates an accepted TTFT timeout without another generation",
      "Kiro terminal integrity recovery treats validated non-semantic frames as watchdog activity"
    ],
    "tests/unit/kiro-thinking-strip.test.js": [
      "KiroExecutor thinking tag stripping handles empty content after stripping when hasReasoningContent is true",
      "KiroExecutor thinking tag stripping strips <thinking> tags from assistantResponseEvent",
      "KiroExecutor thinking tag stripping uses tool_calls finish reason for tool streams without messageStop",
      "KiroExecutor thinking tag stripping waits for clean EOF before emitting stop after messageStop"
    ],
    "tests/unit/kiro-to-openai-translation-contracts.test.js": [
      "assistant content events :event-type: header form and raw JSON line form parse",
      "assistant content events empty content yields null",
      "assistant content events model in state flows into the chunk, defaulting otherwise",
      "assistant content events non-JSON SSE data falls back to raw text and needs a typed event to emit",
      "assistant content events object event: first chunk carries the role, later chunks do not",
      "assistant content events SSE string form with event: and data: lines parses",
      "assistant content events state seeds a chatcmpl id and created seconds on first event",
      "passthrough and guards already-OpenAI chunks pass through untouched",
      "passthrough and guards null/empty chunk returns null",
      "passthrough and guards unknown event type is skipped",
      "reasoning events string reasoning payload and empty payload",
      "reasoning events surfaces reasoning_content with a leading role on the first chunk",
      "tool use events emits an OpenAI tool_calls delta with stringified arguments",
      "tool use events falls back to a generated call id and empty input",
      "usage and stop events final chunk carries stashed usage",
      "usage and stop events round-trip: content then stop share id/model and end well-formed",
      "usage and stop events stop after tool use finishes tool_calls; plain stop finishes stop",
      "usage and stop events usage event stashes OpenAI-shaped usage on state and emits nothing"
    ],
    "tests/unit/kiro-tool-name-restoration-3107.test.js": [
      "PR #3107 Kiro normalized tool-name restoration keeps a reverse map when Claude tools are normalized",
      "PR #3107 Kiro normalized tool-name restoration keeps a reverse map when OpenAI tools are normalized",
      "PR #3107 Kiro normalized tool-name restoration rejects an invalid mapped tool call without a replacement generation",
      "PR #3107 Kiro normalized tool-name restoration restores mapped names in the raw Kiro response translator",
      "PR #3107 Kiro normalized tool-name restoration restores only a mapped EventStream tool name while preserving arguments and the tool_call wrapper"
    ],
    "tests/unit/kiro-tool-result-images-2521.test.js": [
      "both Kiro translators use it claude-to-kiro no longer stringifies a tool result's content array",
      "both Kiro translators use it claude-to-kiro.js hoists tool-result images into pendingImages",
      "both Kiro translators use it openai-to-kiro.js hoists tool-result images into pendingImages",
      "images inside a tool result reach Kiro's vision channel (#2521) handles several images in one result",
      "images inside a tool result reach Kiro's vision channel (#2521) keeps ordinary text-only results exactly as they were",
      "images inside a tool result reach Kiro's vision channel (#2521) leaves a remote URL as text, since Kiro takes base64 only",
      "images inside a tool result reach Kiro's vision channel (#2521) never serialises base64 into the text channel, which is what made it expensive",
      "images inside a tool result reach Kiro's vision channel (#2521) passes a plain string result through untouched",
      "images inside a tool result reach Kiro's vision channel (#2521) pulls a Claude image block out as an image, not as text",
      "images inside a tool result reach Kiro's vision channel (#2521) pulls an OpenAI image_url data URI out the same way",
      "images inside a tool result reach Kiro's vision channel (#2521) survives a null or empty result without inventing content",
      "the MITM inbound path carries images too (#2521) converts Kiro's { format, source: { bytes } } into a data URI image_url",
      "the MITM inbound path carries images too (#2521) emits a user turn for an image even when there is no text and no tool result",
      "the MITM inbound path carries images too (#2521) reads uim.images",
      "the MITM inbound path carries images too (#2521) stops mapping tool-result parts to the empty string"
    ],
    "tests/unit/kiro-usage-and-tool-integrity.test.js": [
      "A: tool-call bytes count as output tokens does not report a tool-only turn as OUT 1",
      "A: tool-call bytes count as output tokens still counts plain text output",
      "B: truncation after output closes as length, not as a failure keeps the text and reports finish_reason length",
      "B: truncation after output closes as length, not as a failure still fails a truncation that produced nothing",
      "C: one unusable tool fragment does not take the whole turn with it keeps streamed text when the only tool call is unusable",
      "C: one unusable tool fragment does not take the whole turn with it ships the valid call and drops only the invalid one",
      "C: one unusable tool fragment does not take the whole turn with it still hard-fails when nothing usable was produced at all",
      "D: cache tokens survive the kiro -> claude translation also accepts the nested details spelling used on passthrough",
      "D: cache tokens survive the kiro -> claude translation forwards the flat Chat spelling the executor emits",
      "D: cache tokens survive the kiro -> claude translation omits the cache keys when the upstream reported none",
      "D: cache tokens survive the kiro -> claude translation preserves cache on the non-streaming path too",
      "E: the translator valid-guard is defence-in-depth canonicalizeKiroConversation heals that same conversation",
      "E: the translator valid-guard is defence-in-depth validateKiroConversation names the offending turn"
    ],
    "tests/unit/launcher-stray-rejection-974.test.js": [
      "a boot failure still ends the launcher (#974) the boot chain reports and exits instead of being swallowed",
      "the launcher outlives a stray rejection (#974) handles both crash events, not just one of them",
      "the launcher outlives a stray rejection (#974) is registered before the boot chain, not inside startServer",
      "the launcher outlives a stray rejection (#974) says which process it is, since the report has no other output",
      "the launcher outlives a stray rejection (#974) the rejection handler keeps the launcher up rather than exiting"
    ],
    "tests/unit/lingua-compress.test.js": [
      "chatCore lingua wiring accepts lingua events through the events allowlist",
      "chatCore lingua wiring compresses a natural-language tool_result below the cut when lingua is on",
      "chatCore lingua wiring emits a truthful applied:false row when the epoch boundary blocks the stage",
      "chatCore lingua wiring emits an applied:true lingua row with truthful byte savings",
      "chatCore lingua wiring leaves the body unchanged and stays truthful when the endpoint fails",
      "chatCore lingua wiring leaves tool_results untouched by default (flag off)",
      "chatCore lingua wiring reports no_backend when the stage is on but no endpoint is configured",
      "compressBlobs classifies endpoints: loopback ok, unix path ok, remote refused",
      "compressBlobs compresses a natural-language blob at minChars",
      "compressBlobs derives ratio from blob size (<=0.5, targeting 4096 chars out)",
      "compressBlobs is inert without an endpoint: no_backend, body reference unchanged",
      "compressBlobs keeps pair structure intact while compressing a tool_result payload",
      "compressBlobs leaves cache_control blocks untouched",
      "compressBlobs never compresses the live tail: the latest user message is not a candidate",
      "compressBlobs never touches messages at or before the epoch cut",
      "compressBlobs propagates caller cancellation mid-flight with the body unchanged",
      "compressBlobs refuses a remote-host endpoint without issuing any request",
      "compressBlobs reports applied:false and leaves the body unchanged on a backend failure",
      "compressBlobs reports applied:false on a schema mismatch and never partially mutates",
      "compressBlobs skips a blob at minChars-1 and compresses at minChars",
      "compressBlobs skips code-keyword blobs, fenced blobs, and diff hunks",
      "compressBlobs skips JSON blobs",
      "compressBlobs skips with applied:false when the epoch cut is 0",
      "scripts/lingua-sidecar.mjs answers 501 when llmlingua-2 is not importable",
      "scripts/lingua-sidecar.mjs validates request shape and path",
      "stage name registries contextRepo STAGE_NAMES contains the epoch-cascade saver names"
    ],
    "tests/unit/live-catalog-2654.test.js": [
      "fetchOpenAICatalog fails open returns null on 401 so the static registry stands",
      "fetchOpenAICatalog fails open returns null on an empty catalog",
      "fetchOpenAICatalog fails open returns null when the upstream throws or times out",
      "fetchOpenAICatalog fails open returns null without a credential rather than calling out",
      "fetchOpenAICatalog fails open sends the connection's own key and nothing else",
      "normalizeCodexCatalog does not double-suffix an id that already ends in -review",
      "normalizeCodexCatalog mints the deterministic -review alias for chat models",
      "normalizeCodexCatalog never twins an image model, even when upstream omits its type",
      "normalizeOpenAICatalog drops capability metadata that is not a plain object",
      "normalizeOpenAICatalog drops engines that would 400 on /v1/chat/completions",
      "normalizeOpenAICatalog keeps a live chat model the static registry has never heard of",
      "normalizeOpenAICatalog routes each non-chat family to its own kind so /v1/models stays chat-only",
      "normalizeOpenAICatalog survives a malformed body without throwing",
      "resolveLiveOpenAIModels exposes a live-only chat id and keeps the static media entries",
      "resolveLiveOpenAIModels returns null, not an empty list, when the fetch cannot answer",
      "withStaticMediaModels does not duplicate an id the live catalog already carries",
      "withStaticMediaModels keeps the static media catalog an llm-only live list never mentioned"
    ],
    "tests/unit/local-device-tts-provider.test.js": [
      "fetchLocalDeviceVoices parses macOS say -v ? output and caches it",
      "fetchLocalDeviceVoices parses Windows SAPI voices via powershell",
      "fetchLocalDeviceVoices returns [] when voice discovery fails",
      "fetchLocalDeviceVoices wraps a single (non-array) powershell object and defaults missing culture",
      "synthesize omits -v when no voice is given",
      "synthesize propagates a failure from say",
      "synthesize runs say then ffmpeg and returns the mp3 as base64"
    ],
    "tests/unit/local-request-peer-trust-3294.test.js": [
      "login limiter client IP buckets spoofed peer IPs together so lockout cannot be rotated away",
      "login limiter client IP keys on the stamped peer IP when the wrapper proved it",
      "login limiter client IP still honours TRUST_PROXY for operators fronting tokenproxy with a reverse proxy",
      "peer header trust accepts the legacy Host fallback only in development",
      "peer header trust blocks spoofed local-only routes that would otherwise spawn processes",
      "peer header trust keeps serving a genuinely local request stamped by the wrapper",
      "peer header trust refuses ::ffff:10.204.111.34 as a peer",
      "peer header trust refuses [2001:db8::1] as a peer",
      "peer header trust refuses 10.204.111.34 as a peer",
      "peer header trust refuses 2001:db8::1 as a peer",
      "peer header trust rejects a spoofed loopback Host in production",
      "peer header trust rejects a spoofed loopback peer IP carrying a wrong trust token",
      "peer header trust rejects a spoofed loopback peer IP that carries no trust proof",
      "peer header trust rejects a spoofed loopback peer IP when the wrapper never booted",
      "peer header trust still refuses a stamped non-loopback peer IP",
      "peer header trust treats ::1 as a loopback peer",
      "peer header trust treats ::ffff:127.0.0.1 as a loopback peer",
      "peer header trust treats ::FFFF:127.0.0.1 as a loopback peer",
      "peer header trust treats [::1] as a loopback peer",
      "peer header trust treats 127.0.0.1 as a loopback peer"
    ],
    "tests/unit/local-tokenizer.test.js": [
      "verified bounded local tokenizer bounds empty jobs by count and drains shutdown idempotently",
      "verified bounded local tokenizer bounds input by UTF-8 bytes before dispatch",
      "verified bounded local tokenizer bounds queue bytes including the active request and cancels all admissions",
      "verified bounded local tokenizer cl100k_base matches independent official tiktoken token IDs and worker counts",
      "verified bounded local tokenizer does not infer routed aliases, unknown models or unsupported encodings",
      "verified bounded local tokenizer expires stalled work, rejects pre-abort and recovers from worker failure",
      "verified bounded local tokenizer keeps main event loop responsive during a cold actual worker count",
      "verified bounded local tokenizer o200k_base matches independent official tiktoken token IDs and worker counts"
    ],
    "tests/unit/logger-surface.test.js": [
      "logger.js import surface keeps the live helpers and the dead lifecycle methods stay gone",
      "logger.js import surface maskKey still masks to a prefix form"
    ],
    "tests/unit/long-context-depletion-handler.test.js": [
      "long-context credit 429 through the chat handler: one account depleted rotates A→B on the FIRST \"extra usage is required for long context\" and locks A for the model",
      "long-context credit 429 through the chat handler: one account depleted rotates A→B on the FIRST \"usage credits are required for long context\" and locks A for the model",
      "long-context credit 429 through the chat handler: pool depleted answers 429 without \"extra usage is required for long context\", with the substitute message and retry information",
      "long-context credit 429 through the chat handler: pool depleted answers 429 without \"usage credits are required for long context\", with the substitute message and retry information",
      "temporary rate limit preserves the agent pin returns the cooldown after one attempt without rotating to another account"
    ],
    "tests/unit/long-context-depletion-latch.test.js": [
      "long-context credit 429: classification exports the two latch phrases and a lock longer than the same-account retry window",
      "long-context credit 429: classification fails open on non-string input",
      "long-context credit 429: classification leaves an ordinary 429 on the backoff schedule",
      "long-context credit 429: classification only applies on 429; the phrase on another status is not reclassified",
      "long-context credit 429: classification treats a 429 carrying \"extra usage is required for long context\" as account depletion",
      "long-context credit 429: classification treats a 429 carrying \"usage credits are required for long context\" as account depletion",
      "long-context credit 429: client-bound bodies never carry the latch phrase createErrorResult (chatCore path) rewrites \"extra usage is required for long context\"",
      "long-context credit 429: client-bound bodies never carry the latch phrase createErrorResult (chatCore path) rewrites \"usage credits are required for long context\"",
      "long-context credit 429: client-bound bodies never carry the latch phrase errorResponse rewrites \"extra usage is required for long context\" and keeps 429 + Retry-After",
      "long-context credit 429: client-bound bodies never carry the latch phrase errorResponse rewrites \"usage credits are required for long context\" and keeps 429 + Retry-After",
      "long-context credit 429: client-bound bodies never carry the latch phrase scrub keeps surrounding context and is a no-op on clean messages",
      "long-context credit 429: client-bound bodies never carry the latch phrase unavailableResponse (pool exhausted) rewrites \"extra usage is required for long context\"",
      "long-context credit 429: client-bound bodies never carry the latch phrase unavailableResponse (pool exhausted) rewrites \"usage credits are required for long context\""
    ],
    "tests/unit/loopback-proxy-bypass-3424.test.js": [
      "loopback proxy bypass (#3424) bypasses http://[::1]:1234",
      "loopback proxy bypass (#3424) bypasses http://[0:0:0:0:0:0:0:1]",
      "loopback proxy bypass (#3424) bypasses http://127.0.0.1:8080",
      "loopback proxy bypass (#3424) bypasses http://127.1.2.3/api",
      "loopback proxy bypass (#3424) bypasses http://foo.localhost:3000",
      "loopback proxy bypass (#3424) bypasses https://localhost/v1/chat/completions",
      "loopback proxy bypass (#3424) bypasses IPv4-mapped loopback in both spellings",
      "loopback proxy bypass (#3424) does not throw on a malformed target",
      "loopback proxy bypass (#3424) still proxies http://[::ffff:192.168.1.1]",
      "loopback proxy bypass (#3424) still proxies http://10.0.0.5:8080",
      "loopback proxy bypass (#3424) still proxies http://127.0.0.1.evil.com",
      "loopback proxy bypass (#3424) still proxies http://192.168.1.10:11434/api/chat",
      "loopback proxy bypass (#3424) still proxies http://localhost.evil.com",
      "loopback proxy bypass (#3424) still proxies http://notlocalhost.com",
      "loopback proxy bypass (#3424) still proxies https://api.openai.com/v1/chat/completions",
      "loopback proxy bypass (#3424) treats the ollama-local default endpoint as loopback",
      "proxyAwareFetch honours the bypass (#3424) does not attach a proxy dispatcher for a loopback target when HTTP_PROXY is set",
      "proxyAwareFetch honours the bypass (#3424) ignores a connection-level proxy for a loopback target",
      "proxyAwareFetch honours the bypass (#3424) still routes a NON-loopback target through the proxy, so the bypass is not a blanket opt-out"
    ],
    "tests/unit/max-tokens-floor-retry-1702.test.js": [
      "max_tokens floor refusal is retried once (#1702) does not mutate the caller's body",
      "max_tokens floor refusal is retried once (#1702) does not retry when the client sent no max_tokens",
      "max_tokens floor refusal is retried once (#1702) does not retry when the request already met the floor",
      "max_tokens floor refusal is retried once (#1702) leaves a successful response alone, body unread",
      "max_tokens floor refusal is retried once (#1702) leaves an unrelated failure alone",
      "max_tokens floor refusal is retried once (#1702) reads 'at least N' as N rather than N+1",
      "max_tokens floor refusal is retried once (#1702) retries at the floor the upstream named and returns that response",
      "max_tokens floor refusal is retried once (#1702) retries once only — a second refusal is returned as is"
    ],
    "tests/unit/max-tokens-mutation.test.js": [
      "adjustMaxTokens bumps max_tokens strictly above thinking.budget_tokens",
      "adjustMaxTokens clamps a value above a custom ceiling",
      "adjustMaxTokens clamps to the given ceiling even after the thinking bump",
      "adjustMaxTokens defaults to DEFAULT_MAX_TOKENS when max_tokens absent",
      "adjustMaxTokens does not clamp a value exactly at the ceiling",
      "adjustMaxTokens does not raise max_tokens for tools when already at or above the floor",
      "adjustMaxTokens does not touch max_tokens when thinking has no budget_tokens",
      "adjustMaxTokens ignores a non-array tools field",
      "adjustMaxTokens ignores an empty tools array",
      "adjustMaxTokens keeps an explicit max_tokens under the default ceiling",
      "adjustMaxTokens leaves max_tokens alone when already strictly greater than budget_tokens",
      "adjustMaxTokens leaves max_tokens untouched once strictly above the floor (kills always-raise mutant)",
      "adjustMaxTokens raises max_tokens to DEFAULT_MIN_TOKENS when tools present and below the floor"
    ],
    "tests/unit/mcp-bridge-output-integrity.test.js": [
      "MCP session isolation bounds process count and rejects capacity before spawning",
      "MCP session isolation cancellation kills only the owner, and late output cannot reach a replacement",
      "MCP session isolation closes oversized output atomically (terminated=false)",
      "MCP session isolation closes oversized output atomically (terminated=true)",
      "MCP session isolation escalates a stuck owning child once, and never starts a replacement",
      "MCP session isolation isolates identical wire IDs, capabilities, private responses and notifications",
      "MCP session isolation refuses a second browser controller and never disturbs the first",
      "MCP session isolation refuses missing, unknown, cross-plugin and closed session IDs without spawning",
      "MCP session isolation rejects a full input queue before dispatch and never resends accepted data",
      "MCP session isolation signals only the dedicated POSIX process group belonging to the cancelled session",
      "MCP stdio output is a transparent transport ignores empty framing lines while retaining JSON whitespace",
      "MCP stdio output is a transparent transport preserves code, empty values and indentation byte for byte",
      "MCP stdio output is a transparent transport preserves deep children without invented sibling counts byte for byte",
      "MCP stdio output is a transparent transport preserves distinct references and action parameters byte for byte",
      "MCP stdio output is a transparent transport preserves isError, signed metadata, structured values and numeric lexemes",
      "MCP stdio output is a transparent transport preserves long code and trailing error evidence byte for byte",
      "MCP stdio output is a transparent transport preserves message order and escaped newlines across fragmented/coalesced frames",
      "MCP stdio output is a transparent transport retains exact Unicode at every possible UTF-8 chunk boundary"
    ],
    "tests/unit/mcp-local-only-1114.test.js": [
      "MCP routes refuse non-local callers on their own (#1114) lets a genuine loopback caller past the guard",
      "MCP routes refuse non-local callers on their own (#1114) message returns 403 for a remote caller",
      "MCP routes refuse non-local callers on their own (#1114) refuses a loopback peer that arrived through a reverse proxy",
      "MCP routes refuse non-local callers on their own (#1114) refuses a spoofed peer ip that carries no proof token",
      "MCP routes refuse non-local callers on their own (#1114) sse returns 403 for a remote caller"
    ],
    "tests/unit/mcp-session-routes.test.js": [
      "MCP session routes with actual bridge and mocked child processes closes the owning child on abort before any POST and rejects pre-aborted GET",
      "MCP session routes with actual bridge and mocked child processes closes the SSE stream on child failure without leaking error text or replaying",
      "MCP session routes with actual bridge and mocked child processes missing and stale sessions return 404 and never create a child",
      "MCP session routes with actual bridge and mocked child processes returns 409 for an occupied Browser controller, releases it only after child close",
      "MCP session routes with actual bridge and mocked child processes routes colliding IDs through the advertised sessions without cross-delivery"
    ],
    "tests/unit/memory-enhancements.test.js": [
      "ApplyMemoryEnhancements: leaves a conversation that fits entirely alone",
      "ApplyMemoryEnhancements: prunes progressively once the window is full",
      "Context Compactor: compresses long conversation history exceeding threshold",
      "Handoff insertion preserves approved content and is stable on repeated preparation",
      "Media Pruner: removes older base64 media while preserving trailing user media",
      "Tool Pruner: preserves recent tool turns and truncates older ones"
    ],
    "tests/unit/meta-ai.test.js": [
      "meta capabilities exposes minimal/low/medium/high/xhigh levels (no none, no max)",
      "meta capabilities Muse Spark reasons and cannot disable thinking",
      "meta registry is registered with the OpenAI-compatible base URL",
      "meta thinking mapping clamps max to xhigh (Muse Spark has no max)",
      "meta thinking mapping clamps the none mode to minimal (cannot disable thinking)",
      "meta thinking mapping omits reasoning_effort for a literal none level (upstream rejects none)",
      "meta thinking mapping passes supported levels through"
    ],
    "tests/unit/mid-prefix-inject.test.js": [
      "composeBoundaryNote flattens whitespace and clips each entry to noteChars",
      "composeBoundaryNote joins entries with the header and middle dot separator",
      "composeBoundaryNote keeps only the last maxNotes entries",
      "composeBoundaryNote returns an empty string for empty, missing, or all-empty notes",
      "injectBoundaryNote appends a text block to block-array user content",
      "injectBoundaryNote converts string content to a block array with the note appended",
      "injectBoundaryNote does not mutate a deep-frozen input and leaves it intact",
      "injectBoundaryNote returns the same reference for empty or whitespace-only noteText",
      "injectBoundaryNote returns the same reference when insertIndex is out of range",
      "injectBoundaryNote returns the same reference when no user message exists at all",
      "injectBoundaryNote walks backward when no user message exists at or after insertIndex",
      "injectBoundaryNote walks forward to the next user message when insertIndex is an assistant"
    ],
    "tests/unit/middleware-json-error-3441.test.js": [
      "middleware answers in JSON even when the guard throws (#3441) does not leak the thrown message to the caller",
      "middleware answers in JSON even when the guard throws (#3441) passes a normal response straight through",
      "middleware answers in JSON even when the guard throws (#3441) returns a 500 whose body parses as JSON"
    ],
    "tests/unit/mimo-free.live.test.js": [
      "MiMo Free bootstrap (live) bootstrap returns 200 with JWT",
      "MiMo Free ended channel (live) chat with valid JWT → 400 Unsupported model (channel ended)"
    ],
    "tests/unit/mimo-free.test.js": [
      "bootstrapJwt caches the jwt and does not re-fetch while still valid",
      "bootstrapJwt re-fetches once the cached jwt is within the expiry buffer",
      "bootstrapJwt returns the jwt from the bootstrap response",
      "bootstrapJwt sends the machine fingerprint as the bootstrap client",
      "bootstrapJwt throws when the bootstrap response has no jwt",
      "bootstrapJwt throws when the bootstrap response is not ok",
      "generateFingerprint is stable per machine (deterministic across calls)",
      "generateFingerprint returns a 64-char hex sha256 digest",
      "generateSessionId only emits lowercase alphanumeric characters in the suffix",
      "generateSessionId produces a fresh id on each call",
      "generateSessionId uses the ses_ prefix and a 24-char random suffix",
      "injectSystemMarker does not duplicate the marker when already present",
      "injectSystemMarker keeps a caller-provided system prompt alongside the marker",
      "injectSystemMarker leaves a body without a messages array untouched",
      "injectSystemMarker prepends a system message with the marker when none is present",
      "injectSystemMarker preserves the original user message after injection",
      "MiMo Free provider registration advertises no static model, because the free channel is gone",
      "MiMo Free provider registration lists mimo-free in the dashboard FREE_PROVIDERS catalog",
      "MiMo Free provider registration maps the mimo-free alias to mmf",
      "MiMo Free provider registration registers a specialized executor for mimo-free and the mmf alias",
      "MiMo Free provider registration registers mimo-free as a no-auth provider in open-sse config",
      "MimoFreeExecutor buildHeaders includes the MiMo source and session affinity",
      "MimoFreeExecutor buildUrl returns the free-ai chat endpoint",
      "MimoFreeExecutor does not re-bootstrap or resend when x-should-retry denies replay",
      "MimoFreeExecutor does not re-bootstrap or resend when x-tokenproxy-replay-safe denies replay",
      "MimoFreeExecutor execute injects the marker and sends a Bearer JWT to the chat endpoint",
      "MimoFreeExecutor re-bootstraps and retries once on a 403 from the chat endpoint",
      "MimoFreeExecutor transformRequest injects the system marker",
      "MimoFreeExecutor uses connectionId when present and does not leak session affinity between credentials",
      "parseJwtExp derives the expiry from the JWT exp claim (ms)",
      "parseJwtExp falls back to a future timestamp when the JWT is unparseable"
    ],
    "tests/unit/minimax-image-adapter-2482.test.js": [
      "MiniMax image generation uses its own API (#2482) clamps the count to the documented range",
      "MiniMax image generation uses its own API (#2482) passes an already-OpenAI-shaped body straight through",
      "MiniMax image generation uses its own API (#2482) passes an explicit aspect ratio through, and rejects one the API does not accept",
      "MiniMax image generation uses its own API (#2482) posts to the image generation path, not the OpenAI one",
      "MiniMax image generation uses its own API (#2482) raises when the status envelope reports a failure on an HTTP 200",
      "MiniMax image generation uses its own API (#2482) reads an array of bare strings, because the shape is not consistent across MiniMax and its proxies",
      "MiniMax image generation uses its own API (#2482) reads an array of objects with a url, because the shape is not consistent across MiniMax and its proxies",
      "MiniMax image generation uses its own API (#2482) reads an object of image urls, because the shape is not consistent across MiniMax and its proxies",
      "MiniMax image generation uses its own API (#2482) returns no images rather than throwing on an unrecognised body",
      "MiniMax image generation uses its own API (#2482) sends an aspect ratio, because the API takes no WxH size",
      "MiniMax image generation uses its own API (#2482) sends the key as a bearer token",
      "MiniMax image generation uses its own API (#2482) treats a non-url string as base64 rather than as a url"
    ],
    "tests/unit/minimax-m3-claude-wire-reasoning-2705.test.js": [
      "MiniMax reasoning_content stays off the Anthropic wire (#2705) does not inject reasoning_content when the resolved transport is claude",
      "MiniMax reasoning_content stays off the Anthropic wire (#2705) keeps injecting when no transport was resolved",
      "MiniMax reasoning_content stays off the Anthropic wire (#2705) skips the model-level deepseek rule on a non-OpenAI wire too",
      "MiniMax reasoning_content stays off the Anthropic wire (#2705) still injects reasoning_content on the OpenAI transport"
    ],
    "tests/unit/minimax-m3-stream-2047.test.js": [
      "MiniMax-M3 streaming to an OpenAI client (#2047) delivers the answer, not a bare terminator",
      "MiniMax-M3 streaming to an OpenAI client (#2047) drops everything when the wire format and the declared target disagree",
      "MiniMax-M3 streaming to an OpenAI client (#2047) minimax-cn/MiniMax-M3 sends an OpenAI client to the endpoint its body is written for",
      "MiniMax-M3 streaming to an OpenAI client (#2047) minimax/MiniMax-M3 sends an OpenAI client to the endpoint its body is written for",
      "MiniMax-M3 streaming to an OpenAI client (#2047) survives an upstream that frames with CRLF"
    ],
    "tests/unit/minimax-public-tts-exposure.test.js": [
      "public MiniMax TTS exposure ignores a forged Host header when dialling the internal voices API",
      "public MiniMax TTS exposure links minimax TTS models to its public voices endpoint",
      "public MiniMax TTS exposure links minimax-cn TTS models to its public voices endpoint",
      "public MiniMax TTS exposure lists voices through the public audio API for minimax",
      "public MiniMax TTS exposure lists voices through the public audio API for minimax-cn"
    ],
    "tests/unit/minimax-search-default-model.test.js": [
      "MiniMax chat-backed search sends the M3 default model through the /v1/search chat request"
    ],
    "tests/unit/minimax-thinking-signature.test.js": [
      "MiniMax Anthropic thinking stream adds a deserializable signature field for minimax thinking block starts",
      "MiniMax Anthropic thinking stream adds a deserializable signature field for minimax-cn thinking block starts",
      "MiniMax Anthropic thinking stream does not modify unsigned thinking starts from unrelated providers",
      "MiniMax Anthropic thinking stream enables thinking signature normalization for minimax",
      "MiniMax Anthropic thinking stream enables thinking signature normalization for minimax-cn",
      "MiniMax Anthropic thinking stream preserves a MiniMax signature already present on the block start",
      "MiniMax Anthropic thinking stream preserves real signature events across fragmented chunks"
    ],
    "tests/unit/minimax-tts.test.js": [
      "MiniMax TTS sends MiniMax T2A payload and converts hex audio to base64 JSON",
      "MiniMax TTS surfaces MiniMax base_resp errors",
      "MiniMax TTS uses the default MiniMax voice when no voice is provided"
    ],
    "tests/unit/minimax-usage.test.js": [
      "MiniMax usage includes M-series percent-only buckets that have no count totals",
      "MiniMax usage keeps non-TTS MiniMax quota rows instead of filtering to text only",
      "MiniMax usage normalizes M-series percent-only buckets on the coding_plan (countMeansRemaining) endpoint too",
      "MiniMax usage parses coding-plan TTS quota counts as remaining counts",
      "MiniMax usage parses token-plan TTS quota counts as used counts",
      "MiniMax usage prefers the upstream-provided remaining percent when counts are also present",
      "MiniMax usage renders the M3-era MiniMax-M* wildcard as a friendly series label"
    ],
    "tests/unit/minimax-voices.test.js": [
      "MiniMax voices API fetches China MiniMax voices when provider=minimax-cn",
      "MiniMax voices API fetches global MiniMax voices with stored API key"
    ],
    "tests/unit/mistral-assistant-after-tool-1152.test.js": [
      "a tool message is followed by an assistant message (#1152) a conversation that already has the assistant turn is not padded",
      "a tool message is followed by an assistant message (#1152) a non-Mistral model on the same provider is untouched",
      "a tool message is followed by an assistant message (#1152) a trailing tool message is left alone",
      "a tool message is followed by an assistant message (#1152) an assistant turn is inserted between tool results and the next user turn",
      "a tool message is followed by an assistant message (#1152) consecutive tool results are left alone",
      "a tool message is followed by an assistant message (#1152) it applies on the direct Mistral provider too, not only through NVIDIA",
      "a tool message is followed by an assistant message (#1152) the user's own content is untouched"
    ],
    "tests/unit/mistral-message-normalization-2129.test.js": [
      "Mistral trailing-assistant normalization (PR 2129) does not alter a final user turn for Mistral",
      "Mistral trailing-assistant normalization (PR 2129) does not leak Mistral normalization into a later combo fallback",
      "Mistral trailing-assistant normalization (PR 2129) does not mark a trailing assistant in another default executor",
      "Mistral trailing-assistant normalization (PR 2129) leaves a non-Mistral request body byte-for-byte unchanged before dispatch",
      "Mistral trailing-assistant normalization (PR 2129) marks the trailing assistant after Claude-to-Mistral translation"
    ],
    "tests/unit/mistral-ocr-moderation-discovery.test.js": [
      "Mistral OCR and moderation discovery carries both endpoint configs into the dashboard media registry",
      "Mistral OCR and moderation discovery keeps non-chat models out of the default list and exposes them through their explicit kinds",
      "Mistral OCR and moderation discovery publishes the endpoint that matches each discovered kind"
    ],
    "tests/unit/mistral-ocr-moderation-handlers.test.js": [
      "Mistral OCR and moderation handlers forwards batch moderation input and strips the internal provider prefix",
      "Mistral OCR and moderation handlers forwards OCR through the registered config and strips the internal provider prefix",
      "Mistral OCR and moderation handlers redacts the serving credential when an upstream moderation error echoes it",
      "Mistral OCR and moderation handlers rejects malformed OCR and moderation payload fields before selecting credentials",
      "Mistral OCR and moderation handlers rejects OCR providers without a registered OCR endpoint before selecting credentials",
      "Mistral OCR and moderation handlers rejects OCR requests without a document before selecting credentials",
      "Mistral OCR and moderation handlers retries moderation with the next account when the failure is eligible for fallback",
      "Mistral OCR and moderation handlers returns a body-phase client cancellation without marking or rotating its account"
    ],
    "tests/unit/mitm-admin-check.test.js": [
      "Windows Admin Privilege Check (winElevated.js) returns false when fltmc throws an error (non-elevated process)",
      "Windows Admin Privilege Check (winElevated.js) returns true when fltmc succeeds (elevated process)"
    ],
    "tests/unit/mitm-antigravity-auth-passthrough-1884.test.js": [
      "Antigravity MITM auth passthrough (#1884) does not flag account-setup/session URLs as chat turns",
      "Antigravity MITM auth passthrough (#1884) does not flag other Cloud Code Assist calls (quota, models list)",
      "Antigravity MITM auth passthrough (#1884) flags real Antigravity chat-turn URLs",
      "Antigravity MITM auth passthrough (#1884) handles missing/non-string URLs without throwing"
    ],
    "tests/unit/mitm-manager-lifecycle.test.js": [
      "enableToolDNS / disableToolDNS adds the DNS entry and persists tool state when running",
      "enableToolDNS / disableToolDNS refuses to enable DNS when the server is not running",
      "enableToolDNS / disableToolDNS removes the DNS entry and persists tool state regardless of running state",
      "getMitmStatus clears a dead pid file and reports not running",
      "getMitmStatus reports running with the live pid from the pid file",
      "hasDnsPrivilege denies privilege when a password is required and none is cached or stored",
      "hasDnsPrivilege grants privilege when this platform never requires a sudo password",
      "restoreToolDNS logs and continues past a tool whose DNS restore fails, rather than throwing",
      "startServer early branches refuses a concurrent start while the lock file is held by a live pid",
      "startServer early branches reuses an already-running process found via the pid file",
      "trustCert handles system trust without sudo on darwin",
      "trustCert handles system trust without sudo on linux",
      "trustCert installs the cert when sudo is available and no password is required",
      "trustCert refuses when a sudo password is required and none is available",
      "trustCert refuses when the Root CA has never been generated"
    ],
    "tests/unit/mitm-resolve-ipv6-760.test.js": [
      "MITM upstream resolution on IPv6-only networks (#760) caches a resolved address instead of re-querying per request",
      "MITM upstream resolution on IPv6-only networks (#760) falls back to the AAAA record when A resolution fails",
      "MITM upstream resolution on IPv6-only networks (#760) falls back to the AAAA record when A returns no addresses",
      "MITM upstream resolution on IPv6-only networks (#760) queries an IPv6 nameserver as well as the IPv4 one",
      "MITM upstream resolution on IPv6-only networks (#760) reports a real failure rather than caching undefined when neither family resolves",
      "MITM upstream resolution on IPv6-only networks (#760) still prefers IPv4 when an A record exists, so dual-stack is unchanged"
    ],
    "tests/unit/mitm-root-ca.test.js": [
      "MITM Root CA generation creates Root CA files synchronously for direct server startup"
    ],
    "tests/unit/mitm-stale-handle-1462.test.js": [
      "a dead MITM process does not block restart (#1462) asks the OS whether the pid is alive",
      "a dead MITM process does not block restart (#1462) clears a stale handle instead of waiting for an exit event",
      "a dead MITM process does not block restart (#1462) still refuses when the process really is running",
      "a dead MITM process does not block restart (#1462) the liveness predicate distinguishes a live pid from a dead one",
      "a dead MITM process does not block restart (#1462) treats EPERM as alive, since the process exists"
    ],
    "tests/unit/mitm-start-failure-taxonomy-876.test.js": [
      "bind errno classification does not silently fold an unknown errno into either of them",
      "bind errno classification names the port and the errno in the privilege message",
      "bind errno classification separates a held port from a privilege failure",
      "MITM_PORT override defaults to 443",
      "MITM_PORT override falls back to 443 rather than binding nonsense",
      "MITM_PORT override moves the interception port so a co-resident server can keep 443 (#1069)",
      "start-failure summary does not blame the sudo password or the port when the child said nothing",
      "start-failure summary gives every cause a distinct sentence",
      "start-failure summary reports a child that could not load its modules as a packaging problem",
      "start-failure summary reports a losing race for the port as a port problem, naming the holder",
      "start-failure summary reports a privilege failure with the remedies that actually apply",
      "start-failure summary reports a rejected sudo password as exactly that"
    ],
    "tests/unit/mitm-stop-dns-order-1809.test.js": [
      "a cleanup that could not remove the entries leaves the server up (#1809) refuses to kill the listener while a tool is still redirected to loopback",
      "a cleanup that could not remove the entries leaves the server up (#1809) the failure names the tool and says the server was left running",
      "stopServer removes the DNS redirect before it kills the listener (#1809) cleanup is not conditional on there being a process to kill",
      "stopServer removes the DNS redirect before it kills the listener (#1809) the docstring's promised order is what the code now does",
      "stopServer removes the DNS redirect before it kills the listener (#1809) the hosts entries go first, so nothing points at a dead port"
    ],
    "tests/unit/mmf-provider-3035.test.js": [
      "mmf provider (#3035) does not advertise the dead free-tier model in its catalog",
      "mmf provider (#3035) keeps the pinned dead transport untouched (baseline still governs it)"
    ],
    "tests/unit/modality-direct-replay-boundaries.test.js": [
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP408",
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP409",
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP500",
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP502",
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP503",
      "dedicated search through its own fallback dispatcher does not dispatch a chat generation after ambiguous HTTP504",
      "dedicated search through its own fallback dispatcher honors x-should-retry even for retryable status429",
      "dedicated search through its own fallback dispatcher honors x-tokenproxy-replay-safe even for retryable status429",
      "dedicated search through its own fallback dispatcher preserves the latest uncertain outcome after an explicitly rejected dedicated request",
      "exa fetch response evidence classifies actual HTTP429 ({\"x-should-retry\":\"false\"})",
      "exa fetch response evidence classifies actual HTTP429 ({})",
      "exa fetch response evidence classifies actual HTTP503 ({\"x-tokenproxy-replay-safe\":\"true\"})",
      "exa fetch response evidence classifies actual HTTP503 ({})",
      "executor image error preserves HTTP429 evidence ({\"x-should-retry\":\"false\"})",
      "executor image error preserves HTTP503 evidence ({\"x-tokenproxy-replay-safe\":\"true\"})",
      "executor image error preserves HTTP503 evidence ({})",
      "firecrawl fetch response evidence classifies actual HTTP429 ({\"x-should-retry\":\"false\"})",
      "firecrawl fetch response evidence classifies actual HTTP429 ({})",
      "firecrawl fetch response evidence classifies actual HTTP503 ({\"x-tokenproxy-replay-safe\":\"true\"})",
      "firecrawl fetch response evidence classifies actual HTTP503 ({})",
      "jina-reader fetch response evidence classifies actual HTTP429 ({\"x-should-retry\":\"false\"})",
      "jina-reader fetch response evidence classifies actual HTTP429 ({})",
      "jina-reader fetch response evidence classifies actual HTTP503 ({\"x-tokenproxy-replay-safe\":\"true\"})",
      "jina-reader fetch response evidence classifies actual HTTP503 ({})",
      "specialized normalization preserves both denial headers and omits secrets",
      "tavily fetch response evidence classifies actual HTTP429 ({\"x-should-retry\":\"false\"})",
      "tavily fetch response evidence classifies actual HTTP429 ({})",
      "tavily fetch response evidence classifies actual HTTP503 ({\"x-tokenproxy-replay-safe\":\"true\"})",
      "tavily fetch response evidence classifies actual HTTP503 ({})",
      "video cancels the rejected response before refresh replay and returns the latest uncertain outcome",
      "video does not refresh or resubmit when x-should-retry denies",
      "video does not refresh or resubmit when x-tokenproxy-replay-safe denies"
    ],
    "tests/unit/modality-file-block-image-1201.test.js": [
      "every shape routing counts as an image is also strippable (#1269) drops message-level image_url / image properties",
      "every shape routing counts as an image is also strippable (#1269) leaves those properties alone on a model that can see",
      "every shape routing counts as an image is also strippable (#1269) responses: input_file carrying an image follows vision",
      "every shape routing counts as an image is also strippable (#1269) responses: input_image is still stripped when vision is off",
      "every shape routing counts as an image is also strippable (#1269) strips input_image blocks sent on chat messages",
      "routing and stripping agree on what a file block is (#1302, #1201) a file block with no mime keeps the old document answer",
      "routing and stripping agree on what a file block is (#1302, #1201) claude: a document block carrying an image follows vision, not pdf",
      "routing and stripping agree on what a file block is (#1302, #1201) combo.js requires vision, not pdf, for an image in a file block",
      "routing and stripping agree on what a file block is (#1302, #1201) keeps an image file block on a vision model that cannot read PDFs",
      "routing and stripping agree on what a file block is (#1302, #1201) still strips a real PDF from a model that cannot read one",
      "routing and stripping agree on what a file block is (#1302, #1201) strips an image file block from a model with no vision, as an image"
    ],
    "tests/unit/modality-strip.test.js": [
      "stripUnsupportedModalities antigravity: strips inside request.contents",
      "stripUnsupportedModalities claude: strips image + document by capability",
      "stripUnsupportedModalities fast-exits when model supports all modalities",
      "stripUnsupportedModalities gemini: keeps inlineData pdf when pdf:true, strips image when vision:false",
      "stripUnsupportedModalities gemini: strips inlineData image by mime when vision:false",
      "stripUnsupportedModalities handles missing/empty body safely",
      "stripUnsupportedModalities openai: keeps image when vision:true",
      "stripUnsupportedModalities openai: strips file when pdf:false",
      "stripUnsupportedModalities openai: strips image when vision:false, leaves placeholder",
      "stripUnsupportedModalities openai: strips input_audio when audioInput:false",
      "stripUnsupportedModalities reports false when nothing was actually removed",
      "stripUnsupportedModalities reports true when a block was removed, per source format",
      "stripUnsupportedModalities responses: strips input_image when vision:false"
    ],
    "tests/unit/model-availability-scope-2568.test.js": [
      "an unavailable connection names the models that failed (#2568) a cleared failure record is not counted",
      "an unavailable connection names the models that failed (#2568) an account-level rejection still reports the whole connection",
      "an unavailable connection names the models that failed (#2568) no failure record at all falls back to the whole connection",
      "an unavailable connection names the models that failed (#2568) one bad model does not take the whole connection down",
      "an unavailable connection names the models that failed (#2568) several failed models are each reported, with their own error",
      "the route uses that scoping active locks are still reported per model, as before",
      "the route uses that scoping only falls back to __all when nothing is scoped",
      "the route uses that scoping reads the per-model failure records"
    ],
    "tests/unit/model-capability-overrides-3455.test.js": [
      "user-editable model capability overrides (#3455, #3472) accepts a bare model id, which applies wherever it is served",
      "user-editable model capability overrides (#3455, #3472) accepts a glob, so a whole cloaked family can be corrected at once",
      "user-editable model capability overrides (#3455, #3472) does not leak onto another provider serving the same id",
      "user-editable model capability overrides (#3455, #3472) fails open on a malformed value rather than taking the gateway down",
      "user-editable model capability overrides (#3455, #3472) ignores an entry whose value is not a capability object",
      "user-editable model capability overrides (#3455, #3472) leaves the catalog alone when the variable is unset",
      "user-editable model capability overrides (#3455, #3472) reshapes an existing provider's model, vendor prefix and all",
      "user-editable model capability overrides (#3455, #3472) resolves an injected key whose model id carries a slash",
      "user-editable model capability overrides (#3455, #3472) still yields contextWindow to the dedicated model-context surface",
      "user-editable model capability overrides (#3455, #3472) yields to a per-model entry injected by the app"
    ],
    "tests/unit/model-catalog-sync.test.js": [
      "a sync does not erase its own output counts one modality vote per provider, not one per id variant",
      "a sync does not erase its own output writes the same delta on a second run",
      "catalog layers strictly below the hand-written tables does fill a gap the tables leave open",
      "catalog layers strictly below the hand-written tables keeps a MODEL_CAPABILITIES entry when the catalog contradicts it",
      "catalog layers strictly below the hand-written tables keeps a PROVIDER_CAPABILITIES entry when the catalog contradicts it",
      "catalog layers strictly below the hand-written tables only ever turns a capability on, never off",
      "catalog layers strictly below the hand-written tables still lets the user's own overrides win over the catalog",
      "failure paths are closed and silent restores the catalog reader after a mid-sync failure",
      "failure paths are closed and silent returns null instead of throwing when the catalog file is absent",
      "failure paths are closed and silent survives a corrupt catalog file",
      "failure paths are closed and silent swallows a models.dev outage and leaves capability lookup working",
      "failure paths are closed and silent swallows a non-200 response",
      "name-based vision fallback cannot demote a model the tables already declared",
      "name-based vision fallback never claims vision for image generation, embeddings or speech",
      "name-based vision fallback recognises modality words in a model id",
      "the scheduler does not arm a timer during a Next build phase",
      "the scheduler does not arm a timer when MODEL_CATALOG_SYNC=off"
    ],
    "tests/unit/model-context-bulk.test.js": [
      "prepares scoped exact keys and preserves absent versus observed values",
      "refuses partial parsing, ambiguous repeated keys, unsafe numbers and empty input"
    ],
    "tests/unit/model-context-editor.test.js": [
      "context-window editor contracts matches exact keys in engine precedence before case-insensitive wildcard insertion order",
      "context-window editor contracts preserves exact positive whole tokens",
      "context-window editor contracts rejects invalid or imprecise token input \" \"",
      "context-window editor contracts rejects invalid or imprecise token input \"-1\"",
      "context-window editor contracts rejects invalid or imprecise token input \"\"",
      "context-window editor contracts rejects invalid or imprecise token input \"0\"",
      "context-window editor contracts rejects invalid or imprecise token input \"1.5\"",
      "context-window editor contracts rejects invalid or imprecise token input \"1e6\"",
      "context-window editor contracts rejects invalid or imprecise token input \"9007199254740992\"",
      "context-window editor contracts rejects invalid or imprecise token input \"Infinity\"",
      "context-window editor contracts retains vendor-prefixed raw identity"
    ],
    "tests/unit/model-context-inspector-ui.test.js": [
      "a removal asks inline beside the entry and sends nothing until it is confirmed",
      "groups every entry by state and edits its window in place, never in a layer",
      "only the expanded entry carries its precedence evidence, inline and linked"
    ],
    "tests/unit/model-context-route.test.js": [
      "/api/model-context keeps the public model listing dynamic by default",
      "/api/model-context passes reviewed-key expectations and exposes conflict or persistence uncertainty",
      "/api/model-context preserves bulk validation and counts while mutating the map atomically",
      "/api/model-context removes the same canonical key that PUT stores",
      "/api/model-context stores a trimmed positive safe integer override and rejects unsafe values",
      "/api/model-context uses local persisted/static inventory without resolving a provider or proxy sidecar"
    ],
    "tests/unit/model-credit-depletion.test.js": [
      "model credit 429: classification leaves an ordinary 429 on the backoff schedule",
      "model credit 429: classification locks longer than the same-account retry window so the loop rotates",
      "model credit 429: classification only applies on 429; the phrase on another status is not reclassified",
      "model credit 429: classification survives a non-string body without throwing",
      "model credit 429: classification treats a 429 carrying \"extra usage is required for this model\" as (account, model) depletion",
      "model credit 429: classification treats a 429 carrying \"usage credits are required for this model\" as (account, model) depletion",
      "model credit 429: classified as quota so the request rotates classifies \"extra usage is required for this model\" as quota, not rate",
      "model credit 429: classified as quota so the request rotates classifies \"usage credits are required for this model\" as quota, not rate",
      "model credit 429: classified as quota so the request rotates only applies on 429",
      "model credit 429: classified as quota so the request rotates still classifies an ordinary burst 429 as rate, which does hold the request",
      "model credit 429: relayed, not scrubbed is disjoint from the client latch list",
      "model credit 429: relayed, not scrubbed passes \"extra usage is required for this model\" through the scrubber untouched",
      "model credit 429: relayed, not scrubbed passes \"usage credits are required for this model\" through the scrubber untouched"
    ],
    "tests/unit/model-import-batch-1030.test.js": [
      "custom models can be imported as a batch (#1030) applies the same field validation the single shape applies",
      "custom models can be imported as a batch (#1030) counts a model that was already registered as imported-but-not-added",
      "custom models can be imported as a batch (#1030) refreshes the capability overrides once, not once per model",
      "custom models can be imported as a batch (#1030) refuses an empty or oversized batch outright",
      "custom models can be imported as a batch (#1030) registers every model in one request and reports the count",
      "custom models can be imported as a batch (#1030) reports a bad entry against that entry, keeping the rest of the import",
      "custom models can be imported as a batch (#1030) still answers a single model the way it always did"
    ],
    "tests/unit/model-lock-metadata.test.js": [
      "model-keyed failure metadata clears lock metadata with every legacy clear-all lock update",
      "model-keyed failure metadata clears the successful and expired pairs while retaining another active pair",
      "model-keyed failure metadata does not persist a lock for a generic HTTP 422 request error",
      "model-keyed failure metadata keeps an account with a stored failure pair selectable",
      "model-keyed failure metadata locks only the refused model when an account lacks its entitlement",
      "model-keyed failure metadata locks the (account, model) pair and rotates on a verified unknown-model signature",
      "model-keyed failure metadata selects only the requested model pair and gives account-wide state precedence",
      "model-keyed failure metadata serves a legacy lock without a matching pair rather than refusing on it",
      "model-keyed failure metadata serves a strict preferred connection that carries an account-wide failure pair",
      "model-keyed failure metadata still passes an UNVERIFIED model_not_found through without locking (#2032)",
      "model-keyed failure metadata stores a bounded, whitespace-sanitized reason with its selected pair",
      "model-keyed failure metadata writes independent alpha and beta lock pairs with the same exact expiry per pair"
    ],
    "tests/unit/model-lock-precedence.test.js": [
      "isModelLockActive precedence does not let an expired per-model lock mask an active account-wide lock",
      "isModelLockActive precedence keeps an active per-model lock",
      "isModelLockActive precedence releases a connection once both locks have expired",
      "isModelLockActive precedence returns false when the connection carries no lock at all",
      "isModelLockActive precedence treats an unparseable timestamp as no lock rather than a permanent one"
    ],
    "tests/unit/model-name-regex.test.js": [
      "model name regex fallback (C2) derives display name from id per family",
      "model name regex fallback (C2) falls back to id verbatim when no pattern matches",
      "model name regex fallback (C2) normalizeModel: explicit name always wins over regex",
      "model name regex fallback (C2) normalizeModel: terse string id becomes object with derived name",
      "model name regex fallback (C2) normalizeModelId: dash between digits becomes a dot (version separator)",
      "model name regex fallback (C2) normalizeModelId: leaves ids with no digit-digit hyphen untouched",
      "model name regex fallback (C2) normalizeModelId: non-string input passes through",
      "model name regex fallback (C2) normalizeModelId: preserves word-suffix hyphens (-thinking, -agentic)"
    ],
    "tests/unit/model-not-found-no-lock-2032.test.js": [
      "a wrong model name does not lock the account (#2032) covers the phrasings providers actually use",
      "a wrong model name does not lock the account (#2032) does not match a bare 'does not exist', which is too generic",
      "a wrong model name does not lock the account (#2032) leaves the account-level failures alone",
      "a wrong model name does not lock the account (#2032) passes the reported body through without a cooldown",
      "a wrong model name does not lock the account (#2032) sits above the status rules, since first match wins",
      "a wrong model name does not lock the account (#2032) still locks on a bare 404, which is an endpoint problem not a model one",
      "a wrong model name does not lock the account (#2032) the Check classifier recognises the same phrasings as the rules",
      "a wrong model name does not lock the account (#2032) the Check message names the model, not the endpoint"
    ],
    "tests/unit/model-pattern-cache.test.js": [
      "does not reuse results between different models or changed policy expressions",
      "preserves wildcard escaping and case behavior across reuse and eviction"
    ],
    "tests/unit/model-quota-unknown-evidence.test.js": [
      "model-keyed quota evidence keeps unknown \"   \" out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown \"\" out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown \"Infinity\" out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown \"NaN\" out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown \"unknown\" out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown {} out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown 0 out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown false out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown null out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown null out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown null out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown null out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown true out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown undefined out of model exhaustion gates",
      "model-keyed quota evidence keeps unknown undefined out of model exhaustion gates",
      "model-keyed quota evidence recovers at the stated reset and does not create a hard gate for unlimited or non-model-keyed providers",
      "model-keyed quota evidence retains measured zero \" 0 \" as a model-specific exhaustion gate",
      "model-keyed quota evidence retains measured zero \"0.0\" as a model-specific exhaustion gate",
      "model-keyed quota evidence retains measured zero \"0\" as a model-specific exhaustion gate",
      "model-keyed quota evidence retains measured zero 0 as a model-specific exhaustion gate"
    ],
    "tests/unit/model-reconcile-3398.test.js": [
      "classifyModels — a single failure is never a verdict refuses to condemn an unconfirmed failure, even a 410",
      "classifyModels — a single failure is never a verdict treats a first failure that succeeds on confirmation as present",
      "classifyModels — account-level errors abort the run condemns nothing already classified when a 429 aborts mid-run",
      "classifyModels — account-level errors abort the run never condemns on an account-level status attached to a model",
      "classifyModels — account-level errors abort the run recognises 401, 402 and 429 as account-level and 404/410 as not",
      "classifyModels — an unusable live list classifies nothing condemns nothing when the live list fetch failed",
      "classifyModels — an unusable live list classifies nothing condemns nothing when the live list is empty",
      "classifyModels — present and the report-only diff accepts bare string ids on both sides",
      "classifyModels — present and the report-only diff classifies an unprobed model still in the live list as present",
      "classifyModels — present and the report-only diff matches on upstreamModelId when the registry id is namespaced",
      "classifyModels — present and the report-only diff reports upstream ids missing from the registry without inferring their kind",
      "classifyModels — present and the report-only diff returns an empty report for an empty registry rather than throwing",
      "classifyModels — purity does not mutate its inputs",
      "classifyModels — purity is deterministic across repeated calls with the same input",
      "classifyModels — retirement evidence classifies a confirmed 404 whose id is ABSENT from the live list as retired",
      "classifyModels — retirement evidence classifies a confirmed 404 whose id IS still in the live list as unreachable-for-account, not retired",
      "classifyModels — retirement evidence classifies a confirmed 410 as retired",
      "classifyModels — retirement evidence does not condemn on a failure that is neither 410 nor 404",
      "classifyModels — retirement evidence never retires on absence from the live list alone",
      "classifyModels — retirement evidence retires a 410 even while the id is still in the live list"
    ],
    "tests/unit/model-routing.test.js": [
      "model routing routes multi-protocol node prefixes",
      "model routing routes to custom node when its prefix collides with a built-in provider alias",
      "model routing routes to custom node when prefix matches built-in tokenrouter alias (tr)",
      "model routing still routes non-reserved compatible node prefixes"
    ],
    "tests/unit/model-test-routing.test.js": [
      "model test route kind routing fails embedding model tests when provider returns no embedding data",
      "model test route kind routing refuses an external model-test target before attaching the CLI token",
      "model test route kind routing returns formatted HTTP errors for non-2xx embedding responses",
      "model test route kind routing routes embedding model tests to /api/v1/embeddings",
      "model test route kind routing routes image model tests to /api/v1/images/generations",
      "model test route kind routing routes stt model tests to /api/v1/audio/transcriptions"
    ],
    "tests/unit/models-batch-ops-3430.test.js": [
      "custom model deletion accepts a batch (#3430) deletes every id given and reports the count",
      "custom model deletion accepts a batch (#3430) refreshes the capability overrides once, not once per id",
      "custom model deletion accepts a batch (#3430) rejects a request with no id at all",
      "custom model deletion accepts a batch (#3430) reports a partial batch per id rather than as one failure",
      "custom model deletion accepts a batch (#3430) still answers a single id the way it always did",
      "model test accepts a batch (#3430) keeps going when one model throws",
      "model test accepts a batch (#3430) keeps the single-model response shape unchanged",
      "model test accepts a batch (#3430) pings sequentially, because a burst is what trips the upstream limiter",
      "model test accepts a batch (#3430) rejects a request naming nothing",
      "model test accepts a batch (#3430) returns one result per model, tagged with its id",
      "the same endpoint runs a custom prompt (#3438, #3140) falls back to the fixed probe for an empty or whitespace prompt",
      "the same endpoint runs a custom prompt (#3438, #3140) passes the caller's prompt to a single model and returns its answer",
      "the same endpoint runs a custom prompt (#3438, #3140) runs one prompt across several models so the answers can be compared"
    ],
    "tests/unit/models-catalog-aliases.test.js": [
      "projects all direct aliases from alias-to-target storage with deterministic primary spelling",
      "uses the real global disabled policy, retaining account overrides for account admission",
      "writes in alias-to-target order and refuses reuse for another target"
    ],
    "tests/unit/models-context-window-alias.test.js": [
      "/v1/models mirrors context_length into context_window carries context_window === context_length wherever the window is known",
      "/v1/models mirrors context_length into context_window omits the key entirely rather than emitting a zero for an unknown window"
    ],
    "tests/unit/models-info-capabilities.test.js": [
      "models info runtime capabilities keeps explicit LLM context ahead of runtime context",
      "models info runtime capabilities leaves non-LLM metadata unchanged",
      "models info runtime capabilities merges object overrides without dropping runtime fields",
      "models info runtime capabilities preserves legacy capability arrays while deriving context",
      "models info runtime capabilities publishes runtime LLM capabilities and context",
      "models info runtime capabilities serves runtime metadata through GET"
    ],
    "tests/unit/models-info-custom-models-1347.test.js": [
      "custom model metadata through /v1/models/info (#1347) resolves persisted custom limits for its provider-qualified id",
      "custom model metadata through /v1/models/info (#1347) selects the persisted row matching the requested kind"
    ],
    "tests/unit/models-info-upstream-id-2872.test.js": [
      "/v1/models/info exposes the upstream model id (#2872) a real registry row is the shape this relies on",
      "/v1/models/info exposes the upstream model id (#2872) emits it when the row remaps the id",
      "/v1/models/info exposes the upstream model id (#2872) omits it when the ids are the same, so the common case does not grow",
      "/v1/models/info exposes the upstream model id (#2872) omits it when the row declares none",
      "/v1/models/info exposes the upstream model id (#2872) the rest of the payload is unchanged"
    ],
    "tests/unit/models-list-missing-providers-2702.test.js": [
      "/v1/models lists every routable model (#2702) an account that selected nothing lifts the restriction rather than adding to it",
      "/v1/models lists every routable model (#2702) keeps a credential-free web-search provider as its own entry",
      "/v1/models lists every routable model (#2702) keeps credential-free providers once another provider is connected",
      "/v1/models lists every routable model (#2702) unions enabledModels across every account of one provider"
    ],
    "tests/unit/models-list-thinking-levels-2702.test.js": [
      "/v1/models exposes thinking-level variants (#2702) carries the base model's metadata onto each variant",
      "/v1/models exposes thinking-level variants (#2702) fixture check: one model has a ladder, the other has none",
      "/v1/models exposes thinking-level variants (#2702) gives a model with no ladder no extra entries",
      "/v1/models exposes thinking-level variants (#2702) keeps the variants out of the catalogue internal consumers read",
      "/v1/models exposes thinking-level variants (#2702) lists one entry per level getThinkingLevels reports, keeping the bare id"
    ],
    "tests/unit/models-list-unconfigured-1861.test.js": [
      "/v1/models on an install with nothing configured (#1861) a connection store that cannot be read still fails open to the catalogue",
      "/v1/models on an install with nothing configured (#1861) a deactivated connection is not a configured provider",
      "/v1/models on an install with nothing configured (#1861) does not advertise providers the user holds no credential for",
      "/v1/models on an install with nothing configured (#1861) still advertises what needs no credential"
    ],
    "tests/unit/models-live-resolver-parallel-2459.test.js": [
      "/v1/models resolves live catalogs concurrently (#2459) a required proxy that is unavailable still fails the whole listing",
      "/v1/models resolves live catalogs concurrently (#2459) holds two providers in flight at the same time",
      "/v1/models resolves live catalogs concurrently (#2459) isolates a failing provider: its neighbour still gets its live catalog"
    ],
    "tests/unit/models-policy.test.js": [
      "Covered policy editor transformations clears one covered override without losing unrelated bindings",
      "Covered policy editor transformations refuses duplicate plan names before replacing another plan override",
      "Covered policy editor transformations removes only the selected plan and its own override",
      "Covered policy editor transformations renames a plan and preserves its exact override and member bindings without mutating the source",
      "Covered policy editor transformations retains the focused member input while its model identifier changes",
      "keeps a visited catalog draft mounted while operators switch model tasks",
      "Policy workbench controls closes a failed activation confirmation and requires current-state reconciliation before another publication",
      "Policy workbench controls restores a prior version only after an explicit fresh review",
      "Policy workbench controls retains edits after a409 and requires explicit discard before loading the newer revision",
      "Policy workbench controls retains the exact captured packet through replay and states readiness and affinity uncertainty",
      "Policy workbench controls reviews fresh active state and reports207 partial completion without automatic replay",
      "Policy workbench controls shows a refused capture without inventing candidates or served evidence",
      "Policy workbench controls starts read-only and omits unrelated analytical export actions",
      "Policy workbench controls stores ordered edits with the exact expected draft revision, then validates that stored revision"
    ],
    "tests/unit/models-prefixed-id-lookup-3649.test.js": [
      "GET /v1/models/{provider}/{model} resolves one model (#3649) a bare id still resolves, so the #3588 behaviour is unchanged",
      "GET /v1/models/{provider}/{model} resolves one model (#3649) a kind slug is not treated as a prefix for a longer path",
      "GET /v1/models/{provider}/{model} resolves one model (#3649) a kind slug still returns a list",
      "GET /v1/models/{provider}/{model} resolves one model (#3649) a provider-prefixed id returns that model",
      "GET /v1/models/{provider}/{model} resolves one model (#3649) an unknown prefixed id 404s, naming both cases",
      "GET /v1/models/{provider}/{model} resolves one model (#3649) the last segment of a prefixed id still resolves"
    ],
    "tests/unit/models-route.test.js": [
      "buildModelsList with exposeComboOnly default-off includes Combo and provider model entries",
      "buildModelsList with exposeComboOnly enabled dedupes Combos sharing a name",
      "buildModelsList with exposeComboOnly enabled returns only kind-matching Combos, with fork enrichment intact",
      "buildModelsList with exposeComboOnly enabled with no kind-matching Combo produces empty list"
    ],
    "tests/unit/models-single-lookup-3588.test.js": [
      "GET /v1/models/{id} resolves a single model (#3588) a bare model id returns that model, not a list",
      "GET /v1/models/{id} resolves a single model (#3588) a kind slug still returns a list, so existing callers are unaffected",
      "GET /v1/models/{id} resolves a single model (#3588) an id matching nothing still 404s, and the message names both cases",
      "GET /v1/models/{id} resolves a single model (#3588) the last segment of a prefixed id also resolves",
      "GET /v1/models/{id} resolves a single model (#3588) the lookup asks for every kind, not just llm"
    ],
    "tests/unit/modelscope-provider.test.js": [
      "ModelScope API Inference provider registers the documented free-tier OpenAI endpoint and current seed models",
      "ModelScope API Inference provider sends the ModelScope access token as a Bearer credential",
      "ModelScope API Inference provider ships the official ModelScope mark as a 128px provider icon"
    ],
    "tests/unit/multi-compatible-provider-nodes.test.js": [
      "multi-protocol compatible provider nodes accepts base URLs and stores full endpoint URLs",
      "multi-protocol compatible provider nodes creates native transports for each configured endpoint",
      "multi-protocol compatible provider nodes keeps the provider valid when Responses is unsupported",
      "multi-protocol compatible provider nodes omits the optional Responses transport when support is not confirmed",
      "multi-protocol compatible provider nodes requires Chat Completions and Messages endpoint URLs",
      "multi-protocol compatible provider nodes updates endpoint transports on existing connections",
      "multi-protocol compatible provider nodes validates each configured protocol endpoint"
    ],
    "tests/unit/multimodal-drop-lock.test.js": [
      "openai→claude: image_url.detail is dropped (docs 11 §4) converts image to base64 source WITHOUT a detail field",
      "openai→claude: image_url.detail is dropped (docs 11 §4) drops input_audio entirely (claude has no audio block)",
      "openai→gemini: input_audio is mapped to inlineData (docs 11 §4) drops image_url.detail (not carried into inlineData)",
      "openai→gemini: input_audio is mapped to inlineData (docs 11 §4) maps mp3 → audio/mpeg inlineData",
      "openai→gemini: input_audio is mapped to inlineData (docs 11 §4) maps wav → audio/wav inlineData"
    ],
    "tests/unit/muse-spark-tool-choice-3662.test.js": [
      "Muse Spark never receives a forced tool_choice (#3662) covers the sibling muse ids the registry declares",
      "Muse Spark never receives a forced tool_choice (#3662) demotes on the meta provider, which actually declares the model",
      "Muse Spark never receives a forced tool_choice (#3662) does not add tool_choice to a body that never had one",
      "Muse Spark never receives a forced tool_choice (#3662) leaves a different model's forced choice untouched",
      "Muse Spark never receives a forced tool_choice (#3662) leaves an already-auto choice alone",
      "Muse Spark never receives a forced tool_choice (#3662) sees through a thinking suffix on the model name",
      "Muse Spark never receives a forced tool_choice (#3662) still demotes on opencode-go, the route the original guard named"
    ],
    "tests/unit/namespaced-alias-collision-1850.test.js": [
      "namespaced model ids that share a final path segment (#1850) imports both siblings under one provider",
      "namespaced model ids that share a final path segment (#1850) keeps the two siblings as distinct rows in the provider model list",
      "namespaced model ids that share a final path segment (#1850) resolves two aliases pointing at siblings to different models",
      "namespaced model ids that share a final path segment (#1850) routes each sibling to its own upstream id (split on the first slash)",
      "namespaced model ids that share a final path segment (#1850) stores the namespaced id verbatim rather than its last segment"
    ],
    "tests/unit/network-probe-consequence.test.js": [
      "network page probe disclosure does not present the mutable pool status as a history",
      "network page probe disclosure mounts the retained history for one pool on demand, keyed to that pool",
      "network page probe disclosure never leaks the proxy credential into the row",
      "network page probe disclosure points the Test control at that disclosure so it is announced with the button",
      "network page probe disclosure states the activation consequence in the document before the control is used"
    ],
    "tests/unit/no-account-lock-rotation.test.js": [
      "a timed lock never removes an account from selection admits an account under the account-wide lock too",
      "a timed lock never removes an account from selection admits an account whose model lock is active for another hour",
      "a timed lock never removes an account from selection exports no temporaryPinWait: a pinned session cannot be parked on a cooldown",
      "a timed lock never removes an account from selection still excludes on the PROVIDER's own exhausted window, which is not our lock",
      "markAccountUnavailable never tells the caller to wait 429 returns shouldFallback with mustWait false",
      "markAccountUnavailable never tells the caller to wait 500 returns shouldFallback with mustWait false",
      "markAccountUnavailable never tells the caller to wait 502 returns shouldFallback with mustWait false",
      "markAccountUnavailable never tells the caller to wait 529 returns shouldFallback with mustWait false",
      "rotation is bounded by wall clock, not by benching accounts declares a rotation time budget",
      "rotation is bounded by wall clock, not by benching accounts no longer bypasses a lock for the just-failed account, because none binds",
      "rotation is bounded by wall clock, not by benching accounts stops rotating once the budget is spent, on the failover path",
      "the bench cannot be reintroduced admission does not consult a timed lock",
      "the bench cannot be reintroduced markAccountUnavailable does not derive mustWait from the failure class",
      "the bench cannot be reintroduced selection carries no model-locked verdict to emit"
    ],
    "tests/unit/no-auth-providers-3269.test.js": [
      "no-auth providers are found by property, not by category (#3269) covers the other no-auth providers outside the free category",
      "no-auth providers are found by property, not by category (#3269) includes searxng, which is freeTier and so was missing from the free bucket",
      "no-auth providers are found by property, not by category (#3269) lists every provider whose registry entry sets noAuth",
      "no-auth providers are found by property, not by category (#3269) says no for a provider that needs a credential, and for junk",
      "no-auth providers are found by property, not by category (#3269) still covers the no-auth providers that are in the free category"
    ],
    "tests/unit/noauth-provider-disable-2650.test.js": [
      "#2650 an operator can take a no-auth provider out of rotation honours the setting through a provider alias, so `oc` cannot route around it",
      "#2650 an operator can take a no-auth provider out of rotation only treats an exact true as disabled, so stale junk never benches a provider",
      "#2650 an operator can take a no-auth provider out of rotation refuses credentials for a disabled no-auth provider before any proxy work",
      "#2650 an operator can take a no-auth provider out of rotation still hands out the virtual connection while nothing is disabled",
      "#2650 the disabledProviders patch is validated at the API boundary persists a boolean map",
      "#2650 the disabledProviders patch is validated at the API boundary reads back an explicit empty map on a fresh install",
      "#2650 the disabledProviders patch is validated at the API boundary rejects a non-boolean value without writing anything",
      "#2650 the disabledProviders patch is validated at the API boundary rejects a non-object patch without writing anything"
    ],
    "tests/unit/noauth-session-id-3262.test.js": [
      "#3262 no-auth free providers keep one upstream session gives the virtual no-auth connection the sentinel connectionId",
      "#3262 no-auth free providers keep one upstream session holds the session across the first tool call",
      "#3262 no-auth free providers keep one upstream session keeps the session reproducible for repeats of the same turn",
      "#3262 no-auth free providers keep one upstream session mints a new session per call when connectionId is missing",
      "#3262 no-auth free providers keep one upstream session still honours a client-supplied session id"
    ],
    "tests/unit/node-apitype-resolution-2778.test.js": [
      "an edited node's API type wins over its immutable id (#2778) a chat-born node switched to responses resolves as responses",
      "an edited node's API type wins over its immutable id (#2778) and the reverse, so switching back also takes effect",
      "an edited node's API type wins over its immutable id (#2778) defaults to chat when neither the store nor the id says anything",
      "an edited node's API type wins over its immutable id (#2778) falls back to the id for a legacy node that never stored one",
      "an edited node's API type wins over its immutable id (#2778) ignores a stored value that is not one of the two types"
    ],
    "tests/unit/node-builtins-imports.test.js": [
      "Node built-in imports loads every edited runtime module through the Node test runtime"
    ],
    "tests/unit/node-version-floor-2362.test.js": [
      "supported Node floor is stated once and consistently (#2362) executes the actual boot guard on Node 18.20.4",
      "supported Node floor is stated once and consistently (#2362) executes the actual boot guard on Node 20.18.0",
      "supported Node floor is stated once and consistently (#2362) executes the actual boot guard on Node 20.18.1",
      "supported Node floor is stated once and consistently (#2362) executes the actual boot guard on Node 20.9.0",
      "supported Node floor is stated once and consistently (#2362) executes the actual boot guard on Node 24.14.0",
      "supported Node floor is stated once and consistently (#2362) the CLI launcher declares the same floor",
      "supported Node floor is stated once and consistently (#2362) the gateway package covers both application and transport runtime requirements",
      "supported Node floor is stated once and consistently (#2362) the server refuses to boot below that floor instead of answering 500"
    ],
    "tests/unit/non-json-upstream-body-1930.test.js": [
      "a non-JSON upstream body names itself (#1930) carries the body instead of a parser error",
      "a non-JSON upstream body names itself (#1930) keeps the original parse error and the full text for a caller that wants them",
      "a non-JSON upstream body names itself (#1930) says so plainly when the body is empty",
      "a non-JSON upstream body names itself (#1930) still returns parsed JSON when the body is JSON",
      "a non-JSON upstream body names itself (#1930) treats a whitespace-only body as empty rather than quoting nothing",
      "a non-JSON upstream body names itself (#1930) truncates a large page rather than putting kilobytes in a message"
    ],
    "tests/unit/non-stream-body-timeout.test.js": [
      "non-stream body failures does not treat an unrelated reader error as client cancellation",
      "non-stream body failures keeps malformed JSON as an ordinary 502",
      "non-stream body failures maps a forced Chat SSE body deadline to 504 without success callbacks",
      "non-stream body failures maps a forced Gemini SSE body deadline to 504 without success callbacks",
      "non-stream body failures maps a forced Responses SSE body deadline to 504 without success callbacks",
      "non-stream body failures maps a ordinary JSON body deadline to 504 without success callbacks",
      "non-stream body failures maps a Responses endpoint forced-SSE body deadline to 504",
      "non-stream body failures maps an already-aborted caller to 499 without completion work",
      "non-stream body failures owns and cancels a stalled forced Chat SSE reader at the body deadline",
      "non-stream body failures owns and cancels a stalled forced Gemini SSE reader at the body deadline",
      "non-stream body failures owns and cancels a stalled forced Responses SSE reader at the body deadline",
      "non-stream body failures owns and cancels a stalled ordinary JSON reader at the body deadline"
    ],
    "tests/unit/nonstream-binary-transport-source-format-3199.test.js": [
      "non-streaming decoded binary transport replies keeps an OpenAI client completion unchanged",
      "non-streaming decoded binary transport replies leaves an unrecognized decoded binary body untouched",
      "non-streaming decoded binary transport replies returns the Anthropic message through the non-streaming handler",
      "non-streaming decoded binary transport replies translates a decoded OpenAI completion to a Responses body for a Responses client",
      "non-streaming decoded binary transport replies translates a decoded OpenAI completion to an Anthropic Message for a Claude client"
    ],
    "tests/unit/nonstream-client-envelope-2347.test.js": [
      "a gemini provider reaching a claude or ollama client gives a Claude client content blocks, not choices[]",
      "a gemini provider reaching a claude or ollama client gives an Ollama client an Ollama envelope, not choices[]",
      "a Responses-shaped provider reaching a gemini or ollama client (:305-312 fall-through) projects to the Gemini envelope instead of falling through unchanged",
      "a Responses-shaped provider reaching a gemini or ollama client (:305-312 fall-through) projects to the Ollama envelope instead of falling through unchanged",
      "hasUsefulContent judges the non-OpenAI envelopes it now sees accepts a bare (unwrapped) Gemini envelope",
      "hasUsefulContent judges the non-OpenAI envelopes it now sees accepts a Gemini envelope carrying only a function call",
      "hasUsefulContent judges the non-OpenAI envelopes it now sees accepts an Ollama envelope with tool calls and rejects an empty one",
      "hasUsefulContent judges the non-OpenAI envelopes it now sees still rejects an empty Gemini envelope so the fallback loop fires (#2727)",
      "hasUsefulContent judges the non-OpenAI envelopes it now sees still rejects an unrecognized body",
      "openai provider -> antigravity client, non-streaming carries the finish reason and usage across",
      "openai provider -> antigravity client, non-streaming delivers tool arguments as an OBJECT, not the OpenAI JSON string",
      "openai provider -> antigravity client, non-streaming hands back a Gemini candidates envelope, never choices[]",
      "openai provider -> antigravity client, non-streaming keeps reasoning, visible text and both function calls",
      "openai provider -> antigravity client, non-streaming round-trips back to the same OpenAI body",
      "openai provider -> gemini client, non-streaming carries the finish reason and usage across",
      "openai provider -> gemini client, non-streaming delivers tool arguments as an OBJECT, not the OpenAI JSON string",
      "openai provider -> gemini client, non-streaming hands back a Gemini candidates envelope, never choices[]",
      "openai provider -> gemini client, non-streaming keeps reasoning, visible text and both function calls",
      "openai provider -> gemini client, non-streaming round-trips back to the same OpenAI body",
      "openai provider -> gemini-cli client, non-streaming carries the finish reason and usage across",
      "openai provider -> gemini-cli client, non-streaming delivers tool arguments as an OBJECT, not the OpenAI JSON string",
      "openai provider -> gemini-cli client, non-streaming hands back a Gemini candidates envelope, never choices[]",
      "openai provider -> gemini-cli client, non-streaming keeps reasoning, visible text and both function calls",
      "openai provider -> gemini-cli client, non-streaming round-trips back to the same OpenAI body",
      "openai provider -> ollama client, non-streaming accepts arguments already given as an object",
      "openai provider -> ollama client, non-streaming hands back an Ollama envelope, never choices[]",
      "openai provider -> ollama client, non-streaming keeps text, thinking and both tool calls with OBJECT arguments",
      "openai provider -> ollama client, non-streaming reports usage in Ollama's own token fields",
      "openai provider -> ollama client, non-streaming round-trips back through ollamaBodyToOpenAI",
      "openai provider -> vertex client, non-streaming carries the finish reason and usage across",
      "openai provider -> vertex client, non-streaming delivers tool arguments as an OBJECT, not the OpenAI JSON string",
      "openai provider -> vertex client, non-streaming hands back a Gemini candidates envelope, never choices[]",
      "openai provider -> vertex client, non-streaming keeps reasoning, visible text and both function calls",
      "openai provider -> vertex client, non-streaming round-trips back to the same OpenAI body"
    ],
    "tests/unit/nonstream-toolcall-reasoning-1412.test.js": [
      "#1412 non-streaming reasoning_content alongside tool_calls keeps reasoning_content when the choice carries tool_calls",
      "#1412 non-streaming reasoning_content alongside tool_calls still strips reasoning_content on a plain visible-content response"
    ],
    "tests/unit/nonstreaming-handler-branches.test.js": [
      "handleNonStreamingResponse antigravity gates antigravity SSE success notifies terminal verification and swallows its rejection",
      "handleNonStreamingResponse antigravity gates antigravity SSE to a Responses client converts through the stream-to-JSON path",
      "handleNonStreamingResponse antigravity gates generic antigravity error payload fails with the safe message",
      "handleNonStreamingResponse antigravity gates JSON validation-required triggers callback (its throw is swallowed) and 403",
      "handleNonStreamingResponse antigravity gates SSE validation-required takes the SSE gate and 403s, callback throw swallowed",
      "handleNonStreamingResponse classifier mode a legacy function_call or multiple alternatives fail before validation",
      "handleNonStreamingResponse classifier mode an invalid decision fails 502 with the classifier error",
      "handleNonStreamingResponse classifier mode valid decision passes and the message is normalized to the bare decision",
      "handleNonStreamingResponse post-processing gates an upstream error framed as content fails 502 with the detected reason",
      "handleNonStreamingResponse post-processing gates HTTP 200 with empty content fails 502 and leaves the account in rotation",
      "handleNonStreamingResponse post-processing gates unwraps { data: { choices } }, repairs finish_reason, logs onRequestSuccess rejection",
      "handleNonStreamingResponse SSE and body-read failures a JSON body reader that throws fails 502 invalid-response",
      "handleNonStreamingResponse SSE and body-read failures a Responses-client SSE stream that throws mid-convert fails 502",
      "handleNonStreamingResponse SSE and body-read failures forced SSE to a Claude client is parsed then re-translated from OpenAI",
      "handleNonStreamingResponse SSE and body-read failures unparseable forced SSE fails 502 with the invalid-SSE message",
      "translateNonStreamingResponse projections claude -> openai: early return on choices, thinking and tool_use blocks, fence strip",
      "translateNonStreamingResponse projections gemini -> openai: candidate with no usable parts yields empty content string",
      "translateNonStreamingResponse projections gemini -> openai: no-candidates body passes through; inlineData becomes a markdown image",
      "translateNonStreamingResponse projections no-choice bodies pass through every fromOpenAICompletion projection unchanged",
      "translateNonStreamingResponse projections openai -> claude: bad tool arguments parse to {}, object arguments kept, cache usage subtracted",
      "translateNonStreamingResponse projections openai -> claude: fully empty message yields a single empty text block",
      "translateNonStreamingResponse projections openai -> ollama: object arguments, thinking, and done_reason mapping",
      "translateNonStreamingResponse projections openai -> responses: custom tool input unwrap, raw freeform fallback, reasoning tokens",
      "translateNonStreamingResponse projections provider responded in Responses shape: output[] normalizes to chat.completion first",
      "translateNonStreamingResponse projections unknown target with a choices array still projects; without one passes through"
    ],
    "tests/unit/nonstreaming-sse-format-3199.test.js": [
      "a non-streaming SSE body is translated from its real format (#3199) defaults to targetFormat so every other path is unchanged",
      "a non-streaming SSE body is translated from its real format (#3199) kiro is not forceStream, so it does reach this handler",
      "a non-streaming SSE body is translated from its real format (#3199) records the format the SSE parse actually produced",
      "a non-streaming SSE body is translated from its real format (#3199) the direct kiro to claude response route is registered",
      "a non-streaming SSE body is translated from its real format (#3199) translates from that format rather than from targetFormat"
    ],
    "tests/unit/notification-compatibility-regression.test.js": [
      "bounds compared metadata without copying retained request and response bodies",
      "compares the exact prior run outside the window and retains inspectable identities",
      "counts a passed-to-failed transition only once while repeated failures remain failed",
      "does not infer a regression from incomplete, unknown, changed or malformed evidence",
      "filters ownership, provider and half-open bounds before comparisons",
      "refuses account scope because controlled fixtures carry no account identity",
      "reports bounded evidence as incomplete rather than silently dropping regressions"
    ],
    "tests/unit/notification-delivery-outbox.test.js": [
      "atomically queues one delivery per retained alert and survives an adapter reopen",
      "cancels a queued target if the destination URL or subscription changed",
      "keeps a stable deduplication identity and limits overlapping drainers to two sends",
      "never exports past alerts when notifications were disabled at capture",
      "records an interrupted send as uncertain without replaying its alert",
      "recovers a pending project intent after reopening without inventing later subscriptions",
      "refuses an overflowing queue with a retained failure while preserving its alert",
      "retains a crashed sender as uncertain and rejects its late completion"
    ],
    "tests/unit/notification-idle-lifecycle.test.js": [
      "notification background ownership evaluates at startup and while completely idle without duplicate subscriptions",
      "notification background ownership stops timer and listener, and aborts outstanding evaluation before shutdown",
      "notification background ownership uses the same throttle for a timer tick and traffic at the same instant"
    ],
    "tests/unit/notification-remediation-api.test.js": [
      "does not claim a persistence barrier failure is a successful save",
      "durable bounded automation on better-sqlite3 enforces bounded batch and aborted queue without losing retained work",
      "durable bounded automation on better-sqlite3 recovers committed intent, changes state once, and retains rollback after reopening",
      "durable bounded automation on better-sqlite3 rolls back the drain and state receipt together on a receipt write failure",
      "durable bounded automation on sql.js enforces bounded batch and aborted queue without losing retained work",
      "durable bounded automation on sql.js recovers committed intent, changes state once, and retains rollback after reopening",
      "durable bounded automation on sql.js rolls back the drain and state receipt together on a receipt write failure",
      "refuses unauthenticated reads and writes before opening storage",
      "reports uncertain persistence after a local action and stops the batch",
      "requires explicit policy shape, exact revision, body bounds and mutation verb",
      "respects the global rolling action limit across different policies",
      "retrieves an exact old triggering alert independently of the newest page",
      "will not execute an intent if its pre-action durability barrier fails"
    ],
    "tests/unit/notification-remediation.test.js": [
      "acknowledgement or snooze suppresses a queued action without deleting evidence",
      "captures action intent in the same transaction as a real rule firing",
      "does not authorize historical alerts when an operator enables an action later",
      "dry-runs a queued action without touching state and applies it once transactionally",
      "enforces per-policy daily limits even after a successful rollback",
      "leaves newer operator changes intact when rollback conflicts",
      "preserves action evidence when a dry-run reviews retained history",
      "refuses a queue item after the account was manually drained and restored",
      "refuses missing or mismatched source evidence, including another account",
      "refuses stale evidence at execution even when it was fresh on enqueue",
      "requires exact account/rule authority and retained optimistic revisions",
      "revalidates policy and manual account changes before applying a queued intent",
      "rolls back only the exact applied state and does not refund the action budget"
    ],
    "tests/unit/notification-rules-api.test.js": [
      "alert disposition acknowledges and snoozes, and refuses an unknown action",
      "GET /api/admin/notification-rules returns the condition catalogue and the unavailable conditions with reasons",
      "POST /api/admin/notification-rules/dry-run rejects a malformed range rather than evaluating a partial population",
      "POST /api/admin/notification-rules/dry-run reports historical firings and writes nothing",
      "rule mutation creates a rule and rejects an unsupported condition with 400",
      "rule mutation requires the caller to state a revision",
      "rule mutation surfaces a revision conflict as 409 carrying the live rule"
    ],
    "tests/unit/notification-rules-controls.test.js": [
      "absorbs a concurrent revision bump by writing the revision it just read, then reads back",
      "checks retained snooze state after the action and does not call a provider",
      "edits a rule in place inside its own card and writes nothing when the edit is abandoned",
      "keeps the threshold control mounted after readback and writes the revision it last read",
      "keeps threshold units distinct for quota, age and counted events",
      "retains an explicit acknowledgement error without retrying or declaring success",
      "retains the local rename draft through refreshes and temporary read errors"
    ],
    "tests/unit/notification-rules-evaluate.test.js": [
      "condition honesty compression failure uses explicit execution evidence and counts failed stages",
      "condition honesty every implemented condition names a real retained source and its evaluator role",
      "cooldown semantics a rule in cooldown does not re-fire",
      "cooldown semantics fires again once the prior cooldown has elapsed",
      "cooldown semantics honours a firing recorded by an earlier evaluation run",
      "direction and dispatch a non-numeric measurement never counts as a breach",
      "direction and dispatch evaluateRule dispatches on the declared duration role",
      "direction and dispatch quota risk fires below the threshold, failures fire at or above it",
      "direction and dispatch rejects an unknown condition kind rather than defaulting to one",
      "duration semantics a gap longer than the observed cadence allowance breaks the sustain",
      "duration semantics a value that recovers restarts the sustain, so a later breach must earn the full duration again",
      "duration semantics derives the gap allowance from the samples, held between the shared floor and cap",
      "duration semantics does not fire while the condition has held for less than the duration",
      "duration semantics fires once the condition has held for exactly the duration",
      "staleness semantics does not fire before threshold plus duration has elapsed",
      "staleness semantics fires once the observation is stale for the full duration",
      "staleness semantics reports no observation rather than treating absence as fresh",
      "staleness semantics respects cooldown",
      "window semantics applies cooldown to repeated window breaches",
      "window semantics does not fire below the count threshold",
      "window semantics fires at the instant the count reaches the threshold",
      "window semantics records that have aged out of the window do not count"
    ],
    "tests/unit/notification-rules-migration.test.js": [
      "schema 16: notification rules creates the constraint that prevents duplicate concurrent firings",
      "schema 16: notification rules enforces the scope, cooldown and acknowledgement invariants in the schema itself",
      "schema 16: notification rules notification rules are present from schema 16 onwards, with 15 reserved",
      "schema 16: notification rules upgrading a version-14 database takes a backup before creating the tables"
    ],
    "tests/unit/notification-rules-repo.test.js": [
      "alert state machine a different scope key on the same rule is its own alert",
      "alert state machine acknowledging is terminal, and frees the slot for a later firing",
      "alert state machine alerts survive deletion of the rule that produced them",
      "alert state machine keeps an alert pointed at the revision that produced it after the rule moves on",
      "alert state machine records one open alert per rule and scope, never a duplicate",
      "alert state machine refuses a snooze that does not end in the future, or on a closed alert",
      "alert state machine snoozing suppresses repetition without resolving the alert",
      "dry run over the retained historical population counts fallbacks from accountSwitches, excluding a session’s first pin",
      "dry run over the retained historical population counts only failed operations, not cancelled or unresolved ones",
      "dry run over the retained historical population does not fire on history that never sustained the breach",
      "dry run over the retained historical population ignores an observation whose headroom cannot be measured",
      "dry run over the retained historical population reports absent evidence as absent, never as a pass",
      "dry run over the retained historical population reports what a rule would have fired on, linked to the causing records",
      "dry run over the retained historical population writes nothing: a dry run is a read",
      "live evaluation evaluates enabled rules only and records their alerts once",
      "optimistic concurrency increments the revision and records a version on every accepted write",
      "optimistic concurrency records enabling and disabling as their own change kind",
      "optimistic concurrency refuses a delete carrying a stale revision",
      "optimistic concurrency surfaces a conflict instead of silently overwriting a concurrent edit",
      "rule validation refuses a condition with no truthful data source behind it",
      "rule validation refuses a cooldown below the floor, so a breach cannot alert per sample",
      "rule validation refuses a scoped rule with no subject and a global rule with one",
      "rule validation refuses a threshold outside the condition’s declared range"
    ],
    "tests/unit/notification-rules-trigger-live.test.js": [
      "firing through the trigger does not fire again inside the cooldown",
      "firing through the trigger does not fire for a blip shorter than the duration",
      "firing through the trigger fires when the condition has held for the full duration",
      "firing through the trigger produces one alert when two triggers race",
      "restart does not replay a condition that was already true before the process started",
      "restart still alerts on a breach that keeps producing evidence after the restart"
    ],
    "tests/unit/notification-rules-trigger.test.js": [
      "fail-open does not wedge the throttle when an evaluation throws",
      "fail-open keeps the connection scan running when the rule scan throws",
      "fail-open swallows an evaluation throw instead of propagating it",
      "restart hands the evaluator the process start instant as its replay boundary",
      "restart passes no time window, so a long duration keeps the history it needs",
      "the trigger does not run concurrently with itself",
      "the trigger evaluates rules when the traffic tick fires",
      "the trigger keeps one traffic listener alongside the idle trigger",
      "the trigger pays one scan per interval, not one per traffic tick"
    ],
    "tests/unit/notification-rules-ui.test.js": [
      "alert state a snoozed alert is still open, never resolved",
      "alert state acknowledgement outranks a snooze that has not elapsed",
      "alert state an elapsed snooze returns to firing",
      "dry run wording does not describe an absence of firings as health",
      "dry run wording explains every state the evaluator can report",
      "evidence links links a firing to a surface this build actually ships",
      "evidence links returns no link rather than a broken one when there is nothing to point at",
      "rule self-description describes a sustained rule with the condition’s own unit and direction",
      "rule self-description describes a window rule as \"within\", not \"for\"",
      "rule self-description labels scope without inventing a subject for a global rule",
      "rule self-description says so plainly when the condition is not available in this build",
      "unknowns are never drawn as measurements renders a duration in the largest unit that stays exact",
      "unknowns are never drawn as measurements renders an absent number as an explicit unknown, but keeps a real zero",
      "unknowns are never drawn as measurements renders an absent or malformed timestamp as an explicit unknown"
    ],
    "tests/unit/notification-stage-failures.test.js": [
      "applies provider, account and half-open time bounds before counting records",
      "counts exact failed stage identities but excludes cancellation, unchanged bytes and historical inference",
      "counts preparation execution once when retry attempts retain the same stage ledger",
      "links only references carrying an exact retained session identity",
      "preserves explicitly unattributed failures without inventing an account"
    ],
    "tests/unit/notification-stage-gateway.test.js": [
      "links a real gateway transformation failure to an evaluated alert and one retained delivery"
    ],
    "tests/unit/notifications-parity-controls.test.js": [
      "separates explicit webhook evaluation from retained-rule evaluation and provider probing",
      "tests an unsaved public address only after confirmation without creating a destination"
    ],
    "tests/unit/nous-provider-routes.test.js": [
      "Nous API-key route wiring distinguishes rejected keys from inconclusive non-success responses",
      "Nous API-key route wiring does not accept missing models or upstream failures as proof of authentication",
      "Nous API-key route wiring lists authenticated text models and filters non-chat catalogue entries",
      "Nous API-key route wiring marks a saved connection inactive when Nous rejects its key",
      "Nous API-key route wiring uses the same authenticated probe for saved connection tests",
      "Nous API-key route wiring validates a new key through chat rather than the public models endpoint"
    ],
    "tests/unit/nous-provider.test.js": [
      "Nous Research provider accepts only successful inference as proof of authentication",
      "Nous Research provider builds a minimal authenticated chat probe instead of trusting public models",
      "Nous Research provider is registered with API-key-only authentication",
      "Nous Research provider maps reasoning controls to Nous's nested wire format",
      "Nous Research provider marks Hermes 4 conservatively until tool calling is advertised",
      "Nous Research provider normalizes the live catalogue and removes non-chat models",
      "Nous Research provider resolves the provider's public names without alias collisions",
      "Nous Research provider seeds both current Hermes 4 models with their advertised context",
      "Nous Research provider suggests only zero-cost text models with at least 200K context",
      "Nous Research provider uses the documented OpenAI-compatible inference endpoint"
    ],
    "tests/unit/novita-provider.test.js": [
      "Novita AI provider builds into the runtime PROVIDERS map with the openai format default",
      "Novita AI provider exposes its seed models",
      "Novita AI provider is registered as an OpenAI-compatible apikey provider",
      "Novita AI provider keeps every registry id unique after adding novita"
    ],
    "tests/unit/nube-provider.test.js": [
      "Nube.sh provider builds into the runtime PROVIDERS map with the openai format default",
      "Nube.sh provider exposes its seed models",
      "Nube.sh provider is registered as an OpenAI-compatible apikey provider",
      "Nube.sh provider keeps every registry id unique after adding nube"
    ],
    "tests/unit/nvidia-thinking-format-2268.test.js": [
      "NVIDIA NIM reasoning models use the openai thinking format (#2268) keeps every non-default capability the pattern would have supplied",
      "NVIDIA NIM reasoning models use the openai thinking format (#2268) lists models to check",
      "NVIDIA NIM reasoning models use the openai thinking format (#2268) never emits a vendor-native thinking format on an OpenAI-compatible endpoint"
    ],
    "tests/unit/oauth-authorize-grant-route.test.js": [
      "answers a codex authorize with the loopback callback its own proxy binds",
      "mints an independent state and PKCE pair on every claude authorize, bound to the caller callback",
      "returns the provider refusal for a bad code instead of writing a tokenless account"
    ],
    "tests/unit/oauth-callback-relay-dom.test.js": [
      "OAuth callback relay lifetime drops the relay on pagehide, which is the only teardown a bfcache'd tab gets",
      "OAuth callback relay lifetime drops the relay once the TTL elapses, even while the page stays open",
      "OAuth callback relay lifetime renders the provider's refusal rather than a success line, and drops the code",
      "OAuth callback relay lifetime unregisters the pagehide listener on unmount so it cannot outlive the page",
      "OAuth callback relay lifetime writes the code to the relay so a listening dashboard tab can pick it up"
    ],
    "tests/unit/oauth-callback-wait.test.js": [
      "startLocalServer frees the port once closed, and tolerates a second close",
      "waitForCallbackParams leaves no timer behind when the wait times out",
      "waitForCallbackParams resolves with the params once the callback stores them",
      "waitForCallbackParams stops polling as soon as it settles"
    ],
    "tests/unit/oauth-cursor-auto-import.test.js": [
      "GET /api/oauth/cursor/auto-import an unrecognised platform falls back to the catch-all candidate list",
      "GET /api/oauth/cursor/auto-import degrades to the manual fallback, not a 500, when every strategy fails",
      "GET /api/oauth/cursor/auto-import extracts tokens using the first-priority keys",
      "GET /api/oauth/cursor/auto-import falls through to the lower-priority keys when the first ones are empty",
      "GET /api/oauth/cursor/auto-import hands back the db path for manual entry when no strategy finds tokens",
      "GET /api/oauth/cursor/auto-import linux probes its own candidate list and shares the not-found message",
      "GET /api/oauth/cursor/auto-import reports every probed location when no macOS cursor db is accessible",
      "GET /api/oauth/cursor/auto-import unwraps JSON-encoded string values"
    ],
    "tests/unit/oauth-grant-ui.test.js": [
      "carries Kiro registration metadata through device polling without exposing it to display state",
      "opens the sign-in window inside the click and relays the callback code into the exchange",
      "refuses fixed callback renewal before opening a new-account flow and accepts one replacement document",
      "renders a grant refusal as its own sentence instead of blaming an unreachable gateway",
      "renews Cursor credentials on the selected account instead of importing another account",
      "reports a blocked sign-in window instead of waiting on one that never opened",
      "separates a closed sign-in window from a blocked one",
      "severs the opener so the provider origin cannot navigate the dashboard tab",
      "starts the codex callback proxy on the provider loopback URI and names the dashboard port",
      "waits the full proxy-session deadline rather than the five-minute grant deadline"
    ],
    "tests/unit/oauth-identity-capture.test.js": [
      "claude.mapTokens keeps two seats of one login distinguishable by account id",
      "claude.mapTokens persists identity beside the credential instead of dropping it",
      "claude.mapTokens still maps a response that carries no identity, leaving email unset",
      "connection identity persistence falls back to a stable label when the provider returned no identity",
      "connection identity persistence keeps a name the user set, and never recomputes it from identity",
      "connection identity persistence keeps a personal seat and an org seat of ONE login as two rows",
      "connection identity persistence names a new account from captured identity with nobody typing",
      "connection identity persistence writes identity into queryable columns, not only the encrypted blob",
      "deriveAccountDisplayName precedence 1. a user-set name always wins",
      "deriveAccountDisplayName precedence 1b. a whitespace-only name is not a user-set name",
      "deriveAccountDisplayName precedence 2. the email fills in when nobody typed a name",
      "deriveAccountDisplayName precedence 2b. an organisation seat is qualified so it differs from the personal seat",
      "deriveAccountDisplayName precedence 3. the organisation name carries a seat that has no email",
      "deriveAccountDisplayName precedence 4. the account id, shortened, when that is all there is",
      "deriveAccountDisplayName precedence 5. a stable fallback exists when the provider returns nothing",
      "extractClaudeAccountInfo captures the account and organisation the token response carries",
      "extractClaudeAccountInfo reads the camelCase spelling an exported Claude Code auth file uses",
      "extractClaudeAccountInfo returns an empty object rather than throwing on a response with no identity",
      "extractKimiAccountInfo falls back to sub, and yields nothing for a non-JWT token",
      "extractKimiAccountInfo recovers the upstream subject from the access token claims",
      "kimi.mapTokens records the subject without inventing an email the flow never returns",
      "normalizeAccountIdentity drops empty members so a caller can spread it without writing nulls",
      "normalizeAccountIdentity keeps a numeric id as a string and ignores unknown keys"
    ],
    "tests/unit/oauth-providers-index-dispatch.test.js": [
      "backfillAccountIdentity patches codex oauth connections missing email/account info, once per process",
      "backfillAccountIdentity recovers a kimi subject from the access token, and leaves claude alone",
      "backfillAccountIdentity resets the run-once guard when the DB read fails, so a later call retries",
      "device code flow dispatch delegates requestDeviceCode to the provider with its own config",
      "device code flow dispatch maps a token payload to success via postExchange + mapTokens",
      "device code flow dispatch refuses requestDeviceCode and pollForToken for a non-device-code provider",
      "device code flow dispatch reports pending for authorization_pending and slow_down without failing",
      "device code flow dispatch resolves a missing kiro profileArn through the profile endpoint after a token success",
      "device code flow dispatch surfaces a terminal error, defaults when no access token, and passes through non-ok results",
      "exchangeTokens threads code/verifier/state through exchangeToken, then postExchange, then mapTokens",
      "generateAuthData overrides the caller redirect_uri with the loopback URI a fixed-port provider binds",
      "generateAuthData passes state and codeChallenge into buildAuthUrl for a PKCE provider, plain state otherwise",
      "generateAuthData returns a null authUrl and provider callback metadata for a device_code provider",
      "generateAuthData runs prepareConfig with meta and honours config-provided state/verifier overrides",
      "getProvider / getProviderNames maps the legacy kimi-coding alias onto the kimi entry",
      "getProvider / getProviderNames returns the registry entry for every listed name and throws on an unknown one"
    ],
    "tests/unit/oauth-proxy-lifecycle.test.js": [
      "defect 1 — the redirect target follows the CURRENT attempt 302s Mode B to the app port of the latest start, not the first",
      "defect 1 — the redirect target follows the CURRENT attempt keeps the same guarantee for xai",
      "defect 2 — liveness answers to the kernel, not to module state arms exactly one idle timer across repeated starts, so none is orphaned",
      "defect 2 — liveness answers to the kernel, not to module state does NOT report success over servers that are no longer listening",
      "defect 3 — a pending session dies with its listener applies the same session rule to xai",
      "defect 3 — a pending session dies with its listener does NOT drop a session that already reached a terminal status",
      "defect 3 — a pending session dies with its listener drops a pending session on stop, instead of polling \"pending\" against a dead port",
      "defect 3 — a pending session dies with its listener stops a restarted proxy from exchanging against a stale session",
      "defect 4 — a retry gets its own clock, not the remainder of the first attempt keeps the same guarantee for xai",
      "defect 4 — a retry gets its own clock, not the remainder of the first attempt re-arms the idle timer when a retry reuses a listening proxy",
      "defect 4 — a retry gets its own clock, not the remainder of the first attempt still closes the retry on its OWN deadline, so renewing is not an escape from one"
    ],
    "tests/unit/oauth-proxy-servers.test.js": [
      "'Codex' fixed-port proxy 404s a path other than the callback path",
      "'Codex' fixed-port proxy edges idle timeout closes the proxy",
      "'Codex' fixed-port proxy edges Mode A fails when no authorization code arrives",
      "'Codex' fixed-port proxy edges Mode A surfaces the provider error param via error_description",
      "'Codex' fixed-port proxy edges reports port_busy when the fixed port is already bound",
      "'Codex' fixed-port proxy edges resolves immediately when the proxy is already running",
      "'Codex' fixed-port proxy Mode A exchanges tokens and renders success when a session is registered",
      "'Codex' fixed-port proxy Mode A renders failure and preserves the error when exchangeTokens rejects",
      "'Codex' fixed-port proxy Mode B redirects to the app port and stops when no session is registered",
      "'Codex' fixed-port proxy register/getStatus/clear round-trip, and refuses an incomplete session",
      "'Trae' dynamic-port proxy edges 404s an unknown path",
      "'Trae' dynamic-port proxy edges getStatus returns null on a state mismatch, and clear is state-scoped",
      "'Trae' dynamic-port proxy edges idle timeout closes the proxy",
      "'Trae' dynamic-port proxy edges renders failure and records the error when the exchange rejects",
      "'Trae' dynamic-port proxy edges resolves the same callbackUrl when already running",
      "'Trae' dynamic-port proxy exchanges tokens and renders success on a matching callback",
      "'Trae' dynamic-port proxy register/getStatus/clear round-trip, and refuses a session with no state",
      "'Trae' dynamic-port proxy rejects a cross-origin callback with 403 before ever checking state",
      "'Trae' dynamic-port proxy renders 'no active session' when nothing was registered",
      "'Trae' dynamic-port proxy tolerates a missing state but rejects a mismatched one",
      "'Windsurf' dynamic-port proxy edges 404s an unknown path",
      "'Windsurf' dynamic-port proxy edges getStatus returns null on a state mismatch, and clear is state-scoped",
      "'Windsurf' dynamic-port proxy edges idle timeout closes the proxy",
      "'Windsurf' dynamic-port proxy edges renders failure and records the error when the exchange rejects",
      "'Windsurf' dynamic-port proxy edges resolves the same callbackUrl when already running",
      "'Windsurf' dynamic-port proxy exchanges tokens and renders success on a matching callback",
      "'Windsurf' dynamic-port proxy register/getStatus/clear round-trip, and refuses a session with no state",
      "'Windsurf' dynamic-port proxy rejects a cross-origin callback with 403 before ever checking state",
      "'Windsurf' dynamic-port proxy renders 'no active session' when nothing was registered",
      "'Windsurf' dynamic-port proxy requires state and rejects a mismatch",
      "'Xai' fixed-port proxy 404s a path other than the callback path",
      "'Xai' fixed-port proxy edges idle timeout closes the proxy",
      "'Xai' fixed-port proxy edges Mode A fails when no authorization code arrives",
      "'Xai' fixed-port proxy edges Mode A surfaces the provider error param via error_description",
      "'Xai' fixed-port proxy edges reports port_busy when the fixed port is already bound",
      "'Xai' fixed-port proxy edges resolves immediately when the proxy is already running",
      "'Xai' fixed-port proxy Mode A exchanges tokens and renders success when a session is registered",
      "'Xai' fixed-port proxy Mode A renders failure and preserves the error when exchangeTokens rejects",
      "'Xai' fixed-port proxy Mode B redirects to the app port and stops when no session is registered",
      "'Xai' fixed-port proxy register/getStatus/clear round-trip, and refuses an incomplete session",
      "Devin proxy (fixed port, text/plain, requires codeVerifier) 400s a state mismatch as text/plain and stops the proxy",
      "Devin proxy (fixed port, text/plain, requires codeVerifier) 500s with plain text when the exchange throws",
      "Devin proxy (fixed port, text/plain, requires codeVerifier) exchanges tokens and returns plain-text success on a matching callback",
      "Devin proxy (fixed port, text/plain, requires codeVerifier) register requires state and codeVerifier",
      "Devin proxy edges 404s an unknown path and reports no active session as text",
      "Devin proxy edges getStatus returns null on a state mismatch",
      "Devin proxy edges idle timeout closes the proxy",
      "Devin proxy edges rejects a cross-origin callback with 403",
      "Devin proxy edges reports the fixed port as busy when already bound elsewhere",
      "Devin proxy edges resolves the fixed-port callbackUrl when already running",
      "startLocalServer 404s any path other than /callback or /auth/callback",
      "startLocalServer invokes onCallback with the query params and serves the success page on /callback",
      "startLocalServer listen errors rejects with the raw error for a synthetic non-EADDRINUSE listen failure",
      "startLocalServer rejects with a friendly message when a fixed port is already taken",
      "Zed proxy (RSA native-app, preferred port with EADDRINUSE fallback) exchanges tokens and renders success on a matching callback at /",
      "Zed proxy (RSA native-app, preferred port with EADDRINUSE fallback) falls back to a random port when the preferred port is already bound",
      "Zed proxy (RSA native-app, preferred port with EADDRINUSE fallback) register requires state and codeVerifier",
      "Zed proxy edges 404s an unknown path, reports no session, and rejects cross-origin",
      "Zed proxy edges fails outright on a synthetic non-EADDRINUSE listen error",
      "Zed proxy edges getStatus returns null on a state mismatch",
      "Zed proxy edges idle timeout closes the proxy on the EADDRINUSE fallback path too",
      "Zed proxy edges idle timeout closes the proxy on the normal listen path",
      "Zed proxy edges renders failure and records the error when the exchange rejects",
      "Zed proxy edges resolves the same callbackUrl when already running"
    ],
    "tests/unit/oauth-service-generic-coverage.test.js": [
      "authenticate rejects a callback whose state does not match the one issued",
      "authenticate runs the full flow and returns code, matching state, verifier and redirect URI",
      "buildAuthUrl carries every PKCE field from the config plus extra params",
      "exchangeCode POSTs a form body to the config token URL by default",
      "exchangeCode serializes a JSON body when asked for application/json",
      "exchangeCode throws with the upstream body text on a non-ok response",
      "startAuthFlow closes the callback server even when the wait times out",
      "startAuthFlow falls back to the error code when no description is present",
      "startAuthFlow returns the redirect URI for the allocated port and delivers callback params",
      "startAuthFlow throws the error_description when the callback carries an error",
      "startAuthFlow throws when the callback has no authorization code"
    ],
    "tests/unit/observability-enabled-3427.test.js": [
      "observability enable resolution (#3427) accepts the documented casing variants",
      "observability enable resolution (#3427) does not read a missing settings row as enabled",
      "observability enable resolution (#3427) honours the dashboard toggle when no env var is set",
      "observability enable resolution (#3427) honours the shipped .env.example combination",
      "observability enable resolution (#3427) keeps ENABLE_REQUEST_LOGS=false disabling details when it is the only signal",
      "observability enable resolution (#3427) keeps ENABLE_REQUEST_LOGS=true as an override, as before",
      "observability enable resolution (#3427) lets OBSERVABILITY_ENABLED turn details off over an enabled toggle",
      "observability enable resolution (#3427) lets OBSERVABILITY_ENABLED turn details on without touching the toggle",
      "observability enable resolution (#3427) stays off by default, with no env vars and an untouched toggle",
      "observability enable resolution (#3427) treats an empty or whitespace value as unset"
    ],
    "tests/unit/observability-state-3106.test.js": [
      "request details say whether recording is on (#3106) does not fail the request when the recording state cannot be read",
      "request details say whether recording is on (#3106) marks an empty page as disabled when nothing is being recorded",
      "request details say whether recording is on (#3106) marks the same empty page as enabled when recording is on",
      "request details say whether recording is on (#3106) reports the state alongside real rows too, not only empty ones",
      "request details say whether recording is on (#3106) still rejects a bad page before touching the DB at all"
    ],
    "tests/unit/oc-egress.test.js": [
      "applyOcEgress adds opencode.ai to NO_PROXY in direct mode",
      "applyOcEgress is a no-op when mode already matches env",
      "applyOcEgress removes opencode.ai from NO_PROXY in proxy mode and preserves others",
      "flipOcEgress flips again after cooldown expires",
      "flipOcEgress flips proxy -> direct and persists",
      "flipOcEgress respects the 60s cooldown",
      "readOcEgress defaults to proxy when no state file exists",
      "readOcEgress rejects unknown modes and normalizes"
    ],
    "tests/unit/offline-runner-isolation.test.js": [
      "offline runner isolation clears credentials, isolates paths and records a verified network boundary",
      "offline runner isolation executes the canonical runner from the repository root npm test entry point",
      "offline runner isolation executes the canonical runner from the tests package npm test entry point",
      "offline runner isolation fails the canonical verdict when a test alters the fake-production canary",
      "offline runner isolation keeps the direct explicit-config path away from a fake production home",
      "offline runner isolation refuses a nonempty artifact directory",
      "offline runner isolation refuses enabled live flag RUN_E2E",
      "offline runner isolation refuses enabled live flag RUN_LIVE_MIMO",
      "offline runner isolation refuses enabled live flag RUN_REAL",
      "offline runner isolation rejects a forged inherited network-boundary marker"
    ],
    "tests/unit/oidc-auth-mode.test.js": [
      "getOidcRuntimeConfig resolves a config when password and SSO login are both enabled",
      "getOidcRuntimeConfig resolves a config when SSO is the only login method",
      "getOidcRuntimeConfig returns null for password-only login",
      "getOidcRuntimeConfig returns null when SSO is enabled but OIDC credentials are missing",
      "isOidcAuthMode accepts every auth mode the dashboard writes for OIDC login",
      "isOidcAuthMode rejects password-only and missing auth modes"
    ],
    "tests/unit/oidc-flow-contract.test.js": [
      "buildOidcAuthorizationUrl carries the full authorization-code + PKCE parameter set",
      "claim pickers pickOidcDisplayName follows the documented precedence",
      "claim pickers pickOidcEmail returns empty string when absent",
      "exchangeOidcCode falls back to a status-coded message when the error body is not JSON",
      "exchangeOidcCode posts urlencoded grant and includes client_secret only when provided",
      "exchangeOidcCode surfaces the provider error_description on failure",
      "fetchOidcDiscovery blocks non-public issuers before any fetch fires (SSRF boundary)",
      "fetchOidcDiscovery fetches the well-known document from the trimmed issuer",
      "fetchOidcDiscovery throws when the discovery endpoint does not answer 2xx",
      "getOidcRuntimeConfig normalizes issuer, scopes, and label",
      "getOidcRuntimeConfig returns null when the auth mode is not an OIDC one",
      "getOidcRuntimeConfig returns null when the config is incomplete",
      "getPublicOrigin derives origin from forwarded proto and host when unconfigured",
      "getPublicOrigin falls back to the Host header with the request URL protocol",
      "getPublicOrigin falls back to the request URL origin when no host header exists",
      "getPublicOrigin prefers configured BASE_URL and trims trailing slashes",
      "PKCE, state, and nonce material derives the challenge as base64url SHA-256 of the verifier",
      "PKCE, state, and nonce material produces unpredictable, distinct state and nonce values",
      "probeOidcClientSecret reports invalid on invalid_client",
      "probeOidcClientSecret reports valid on a 2xx token response",
      "probeOidcClientSecret returns indeterminate on an unrecognized error",
      "probeOidcClientSecret skips when no secret is provided",
      "probeOidcClientSecret treats invalid_grant as secret accepted (only the test code is bogus)",
      "verifyOidcIdToken propagates a verification failure instead of returning a payload",
      "verifyOidcIdToken verifies against the remote JWKS with issuer, audience, and nonce enforced"
    ],
    "tests/unit/oidc-openid-scope-3642.test.js": [
      "OIDC authorization scope always carries openid (#3642) does not duplicate openid when it is already configured",
      "OIDC authorization scope always carries openid (#3642) falls back to the full default set for an empty or whitespace scope",
      "OIDC authorization scope always carries openid (#3642) keeps the admin's other scopes alongside openid",
      "OIDC authorization scope always carries openid (#3642) normalizes irregular spacing without dropping a scope",
      "OIDC authorization scope always carries openid (#3642) re-adds openid when the admin trimmed it out of the configured scopes"
    ],
    "tests/unit/oidc-ssrf-guard.test.js": [
      "assertPublicUrl (OIDC issuer guard) accepts public https issuer",
      "assertPublicUrl (OIDC issuer guard) rejects IPv6 loopback",
      "assertPublicUrl (OIDC issuer guard) rejects link-local metadata IP",
      "assertPublicUrl (OIDC issuer guard) rejects localhost hostname"
    ],
    "tests/unit/ollama-claude-transport-2475.test.js": [
      "Ollama Cloud offers its Anthropic-compatible endpoint (#2475) does not move an OpenAI-format client off the provider default",
      "Ollama Cloud offers its Anthropic-compatible endpoint (#2475) keeps the native route as the default",
      "Ollama Cloud offers its Anthropic-compatible endpoint (#2475) resolves a claude transport for a Claude client",
      "Ollama Local honours a resolved transport (#2475) falls back to the connection host's native endpoint without one",
      "Ollama Local honours a resolved transport (#2475) uses the runtimeTransport endpoint when one is resolved"
    ],
    "tests/unit/ollama-compat-stream.test.js": [
      "Ollama-compatible stream: text delivers a content line that arrived without its newline",
      "Ollama-compatible stream: text ends the stream with exactly one done:true",
      "Ollama-compatible stream: text survives a multi-byte character split across two network chunks",
      "Ollama-compatible stream: tool calls keeps the tool calls on the one terminal message"
    ],
    "tests/unit/ollama-local-connect-timeout.test.js": [
      "OllamaLocalExecutor connect timeout diagnostics does not diagnose caller cancellation as a connect timeout",
      "OllamaLocalExecutor connect timeout diagnostics preserves provider context and reports the effective typed timeout"
    ],
    "tests/unit/ollama-local-diagnostics-contracts.test.js": [
      "buildUrl defaults to <host>/api/chat with the default host",
      "buildUrl honours a resolved runtimeTransport exactly (#2475)",
      "buildUrl uses the connection's baseUrl, trailing slash stripped",
      "config disables gateway-error retry for the local host",
      "execute diagnostics breaks a large body down with top offenders and hints",
      "execute diagnostics emits pre-flight summary and success line, delegating to the base executor",
      "execute diagnostics handles an empty messages array",
      "execute diagnostics reports a non-timeout failure without the timeout diagnosis",
      "execute diagnostics summarises roles, tool calls, images and content size"
    ],
    "tests/unit/ollama-ndjson-stream.test.js": [
      "Ollama stream content type support allows application/x-ndjson and application/stream+json without blocking as non-SSE error"
    ],
    "tests/unit/ollama-nonstream-json-2348.test.js": [
      "Ollama-compatible non-streaming JSON projection carries tool calls through with parsed arguments",
      "Ollama-compatible non-streaming JSON projection keeps an upstream error body and its status instead of a 200 empty answer",
      "Ollama-compatible non-streaming JSON projection returns the assistant answer instead of an empty done message",
      "Ollama-compatible non-streaming JSON projection still streams an SSE upstream as NDJSON"
    ],
    "tests/unit/ollama-stream-final-chunk-2694.test.js": [
      "ollama stream translator, final chunk accumulates thinking from the final object",
      "ollama stream translator, final chunk closes the stream with finish_reason and usage on the last chunk",
      "ollama stream translator, final chunk keeps content that arrives in the same object as done:true",
      "ollama stream translator, final chunk reports tool_calls that arrive only in the final object",
      "ollama stream translator, final chunk still emits a single terminal chunk when the final object carries no content"
    ],
    "tests/unit/ollama-stream-tail.test.js": [
      "Ollama NDJSON stream: the tail left in the line buffer delivers a content chunk that arrived without its newline",
      "Ollama NDJSON stream: the tail left in the line buffer delivers the final chunk — finish_reason and usage — when it arrives without its newline",
      "Ollama NDJSON stream: the tail left in the line buffer is unchanged when every line is newline-terminated",
      "SSE providers keep their sentinel handling does not translate a trailing data: [DONE]"
    ],
    "tests/unit/ollama-web-fetch-transport.test.js": [
      "application signal and proxy plumbing passes the Request signal and normalized direct policy for no-auth providers",
      "application signal and proxy plumbing passes the Request signal and resolved connection proxy policy",
      "Ollama default proxy transport passes exact proxy policy to the default transport and never falls back when strict",
      "Ollama web fetch deadline and bounded body accepts a bounded body without content length",
      "Ollama web fetch deadline and bounded body accepts a successful body at exactly 4 MiB",
      "Ollama web fetch deadline and bounded body accepts empty title and content strings",
      "Ollama web fetch deadline and bounded body cancels a late Response once with the caller abort reason and contains sync cleanup failure",
      "Ollama web fetch deadline and bounded body classifies an empty object distinctly",
      "Ollama web fetch deadline and bounded body classifies body read settlement boundary caller abort",
      "Ollama web fetch deadline and bounded body classifies body read settlement boundary timeout abort",
      "Ollama web fetch deadline and bounded body classifies null body boundary caller abort",
      "Ollama web fetch deadline and bounded body classifies null body boundary timeout abort",
      "Ollama web fetch deadline and bounded body classifies wrong content type boundary caller abort",
      "Ollama web fetch deadline and bounded body classifies wrong content type boundary timeout abort",
      "Ollama web fetch deadline and bounded body decodes multibyte UTF-8 content",
      "Ollama web fetch deadline and bounded body preserves caller abort and cancels a body read once",
      "Ollama web fetch deadline and bounded body rejects a caller pre-abort without starting transport",
      "Ollama web fetch deadline and bounded body rejects a non-JSON content type and cancels the body",
      "Ollama web fetch deadline and bounded body rejects a response missing content",
      "Ollama web fetch deadline and bounded body rejects a response missing title",
      "Ollama web fetch deadline and bounded body rejects an understated content length when streamed bytes exceed 4 MiB",
      "Ollama web fetch deadline and bounded body rejects and cancels a successful body one byte over 4 MiB",
      "Ollama web fetch deadline and bounded body rejects invalid JSON",
      "Ollama web fetch deadline and bounded body rejects invalid UTF-8",
      "Ollama web fetch deadline and bounded body settles caller abort when injected transport ignores its signal and owns a late rejection",
      "Ollama web fetch deadline and bounded body settles timeout when injected transport ignores its signal and owns a late resolution",
      "Ollama web fetch deadline and bounded body times out a reader that never resolves without awaiting cancellation",
      "Ollama web fetch deadline and bounded body times out a slow body at 30000 ms and cancels once",
      "Ollama web fetch deadline and bounded body times out slow response headers at 30000 ms",
      "Ollama web fetch normalized response accepts exactly 64 KiB of links",
      "Ollama web fetch normalized response cancels an oversized diagnostic body and uses a generic message",
      "Ollama web fetch normalized response drops an unmatched high surrogate at the truncation boundary",
      "Ollama web fetch normalized response omits an empty links array",
      "Ollama web fetch normalized response omits links when the field is missing",
      "Ollama web fetch normalized response preserves 100 valid links in upstream order",
      "Ollama web fetch normalized response preserves content exactly at the requested maximum",
      "Ollama web fetch normalized response preserves link order without dereferencing any returned URL",
      "Ollama web fetch normalized response redacts canonical and decoded target variants from transport diagnostics",
      "Ollama web fetch normalized response redacts canonical and decoded target variants from upstream JSON diagnostics",
      "Ollama web fetch normalized response redacts configured proxy URL userinfo from transport failures",
      "Ollama web fetch normalized response redacts the API key and target URL from upstream diagnostics",
      "Ollama web fetch normalized response rejects 101 links",
      "Ollama web fetch normalized response rejects a non-HTTP link",
      "Ollama web fetch normalized response rejects a non-string link",
      "Ollama web fetch normalized response rejects link userinfo",
      "Ollama web fetch normalized response rejects one byte over aggregate and individual link limits",
      "Ollama web fetch normalized response rejects surrounding link whitespace",
      "Ollama web fetch normalized response returns a bounded scalar JSON upstream error",
      "Ollama web fetch normalized response truncates content over the requested maximum",
      "Ollama web fetch normalized response truncates default content at 200000 UTF-16 code units",
      "Ollama web fetch normalized response uses a generic message for non-JSON upstream text",
      "Ollama web fetch request contract accepts markdown format",
      "Ollama web fetch request contract accepts maximum maximum",
      "Ollama web fetch request contract accepts minimum maximum",
      "Ollama web fetch request contract accepts null format",
      "Ollama web fetch request contract accepts null maximum",
      "Ollama web fetch request contract accepts omitted format",
      "Ollama web fetch request contract accepts omitted maximum",
      "Ollama web fetch request contract does not add a direct attempt after an injected strict transport rejects",
      "Ollama web fetch request contract forwards the exact five-field proxy object unchanged",
      "Ollama web fetch request contract omits empty links and reports unknown cost",
      "Ollama web fetch request contract rejects an 8193-byte URL",
      "Ollama web fetch request contract rejects array format",
      "Ollama web fetch request contract rejects case variant format",
      "Ollama web fetch request contract rejects embedded whitespace or control",
      "Ollama web fetch request contract rejects empty API key",
      "Ollama web fetch request contract rejects extra advertised format",
      "Ollama web fetch request contract rejects fractional maximum",
      "Ollama web fetch request contract rejects html format",
      "Ollama web fetch request contract rejects legacy key and token fields",
      "Ollama web fetch request contract rejects missing configuration",
      "Ollama web fetch request contract rejects missing credentials",
      "Ollama web fetch request contract rejects negative maximum",
      "Ollama web fetch request contract rejects non-ASCII API key",
      "Ollama web fetch request contract rejects non-finite maximum",
      "Ollama web fetch request contract rejects number format",
      "Ollama web fetch request contract rejects over cap maximum",
      "Ollama web fetch request contract rejects string maximum",
      "Ollama web fetch request contract rejects text format",
      "Ollama web fetch request contract rejects whitespace-only API key",
      "Ollama web fetch request contract rejects wrong maximum",
      "Ollama web fetch request contract rejects wrong timeout",
      "Ollama web fetch request contract rejects zero maximum",
      "Ollama web fetch request contract sends the official request and normalizes the successful response",
      "Ollama web fetch request contract validates 8192-byte URL",
      "Ollama web fetch request contract validates embedded credentials",
      "Ollama web fetch request contract validates malformed input",
      "Ollama web fetch request contract validates missing input",
      "Ollama web fetch request contract validates surrounding whitespace",
      "Ollama web fetch request contract validates unsupported scheme"
    ],
    "tests/unit/open-package-external.test.js": [
      "open must not be bundled into the server build is declared in serverExternalPackages",
      "open must not be bundled into the server build resolves its own directory from import.meta.url, which is why it must stay external",
      "open must not be bundled into the server build stays a runtime dependency so the standalone output can resolve it"
    ],
    "tests/unit/open-sse-plain-node-imports.test.js": [
      "open-sse plain Node imports executes Cursor and machine identity after offline CJS emission",
      "open-sse plain Node imports loads Cursor and machine identity as declared ESM without typeless reparsing"
    ],
    "tests/unit/openai-compat-responses-text-format.test.js": [
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) defaults text.format to { type: 'text' } when text is an object without format",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) defaults text.format when persisted API type overrides a chat-shaped provider id",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) does not default text.format when persisted chat API type overrides a responses-shaped id",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) does not touch a non-responses openai-compatible (chat) provider",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) does nothing when there is no text object",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) ignores a non-object text value",
      "DefaultExecutor: default text.format for openai-compatible responses (#2093) preserves an existing text.format instead of overwriting it"
    ],
    "tests/unit/openai-compatible-apitype-resolution.test.js": [
      "executor buildUrl endpoint path BaseExecutor falls back to the ID substring (legacy) when apiType is absent",
      "executor buildUrl endpoint path BaseExecutor routes to /chat/completions when apiType is chat",
      "executor buildUrl endpoint path BaseExecutor routes to /responses when stored apiType is responses, despite the -chat- ID",
      "executor buildUrl endpoint path DefaultExecutor falls back to the ID substring (legacy) when apiType is absent",
      "executor buildUrl endpoint path DefaultExecutor routes to /chat/completions when apiType is chat",
      "executor buildUrl endpoint path DefaultExecutor routes to /responses when stored apiType is responses, despite the -chat- ID",
      "getTargetFormat keeps the ID-based fallback when credentials are absent",
      "getTargetFormat selects openai for chat, and honors stored chat over a -responses- ID",
      "getTargetFormat selects openai-responses when the stored apiType is responses (even on a -chat- ID)",
      "resolveOpenAICompatibleApiType falls back to the ID substring when apiType is absent",
      "resolveOpenAICompatibleApiType ignores an invalid stored apiType and falls back to the ID",
      "resolveOpenAICompatibleApiType prefers stored apiType over the ID substring (edited node on a legacy -chat- ID)"
    ],
    "tests/unit/openai-format-empty-tool-calls.test.js": [
      "filterToOpenAIFormat with an empty tool_calls array leaves a message with real tool calls untouched",
      "filterToOpenAIFormat with an empty tool_calls array still drops an assistant turn left with no usable content",
      "filterToOpenAIFormat with an empty tool_calls array still strips Claude-only blocks from the assistant turn"
    ],
    "tests/unit/openai-luna-function-tools-reasoning.test.js": [
      "official OpenAI Luna function-tool reasoning compatibility does not affect other models or providers",
      "official OpenAI Luna function-tool reasoning compatibility does not apply to the client-facing Responses API path",
      "official OpenAI Luna function-tool reasoning compatibility does not change Luna requests without a function tool declaration",
      "official OpenAI Luna function-tool reasoning compatibility does not infer current tools from tool-call history alone",
      "official OpenAI Luna function-tool reasoning compatibility forces reasoning_effort:none and preserves the existing compatibility transforms",
      "official OpenAI Luna function-tool reasoning compatibility overrides an explicit non-none effort but leaves an explicit none unchanged"
    ],
    "tests/unit/openai-responses-custom-tools.test.js": [
      "Codex Responses Lite custom tools → OpenAI Chat merges additional_tools with normal top-level function tools",
      "Codex Responses Lite custom tools → OpenAI Chat promotes additional_tools custom declarations into Chat tools",
      "Codex Responses Lite custom tools → OpenAI Chat translates custom tool call/output history into Chat assistant/tool messages",
      "Codex Responses Lite passthrough → Responses upstream appends hoisted tools after tools the client already declared",
      "Codex Responses Lite passthrough → Responses upstream drops an empty additional_tools item rather than declaring an empty tools[]",
      "Codex Responses Lite passthrough → Responses upstream hoists additional_tools out of input and unwraps tool namespaces",
      "Codex Responses Lite passthrough → Responses upstream leaves a body carrying no additional_tools item untouched",
      "Codex Responses Lite passthrough → Responses upstream stops walking namespaces a client nested past the bound",
      "OpenAI Chat stream → Codex custom_tool_call leaves normal Chat tool calls as Responses function_call events",
      "OpenAI Chat stream → Codex custom_tool_call unwraps the Chat input parameter and emits custom-tool events",
      "OpenAI Chat stream → Codex custom_tool_call waits for the function name when id and name arrive in separate chunks"
    ],
    "tests/unit/openai-responses-empty-toolcalls.test.js": [
      "OpenAI Chat stream → Responses: empty tool_calls arrays does not emit output_text.done early when every chunk carries tool_calls: []",
      "OpenAI Chat stream → Responses: empty tool_calls arrays still closes the message before a real tool call"
    ],
    "tests/unit/openai-responses-first-assistant-role.test.js": [
      "Responses to Chat assistant role framing adds one assistant role to the first visible reasoning delta",
      "Responses to Chat assistant role framing adds one assistant role to the first visible text delta",
      "Responses to Chat assistant role framing adds one assistant role to the first visible tool delta"
    ],
    "tests/unit/openai-responses-multiturn.test.js": [
      "GrokCliExecutor multi-turn input keeps native Grok reasoning and item ids",
      "openai ↔ responses multi-turn reasoning accepts reasoning_encrypted_content alias on assistant messages",
      "openai ↔ responses multi-turn reasoning openai→responses re-emits reasoning item with summary + encrypted_content",
      "openai ↔ responses multi-turn reasoning responses→openai attaches reasoning_content + encrypted_content to assistant",
      "openai ↔ responses multi-turn reasoning round-trips encrypted_content through openai → responses → openai",
      "openai ↔ responses multi-turn reasoning serializes Chat reasoning as a Responses object without dropping options",
      "openai ↔ responses multi-turn reasoning translateRequest openai→openai-responses preserves encrypted blob"
    ],
    "tests/unit/openai-responses-namespace-tools.test.js": [
      "Responses namespace tools across Chat transports carries namespace metadata through the streaming transform",
      "Responses namespace tools across Chat transports expands declared namespaces into strict-provider-safe functions with request-scoped metadata",
      "Responses namespace tools across Chat transports keeps a direct function distinct when its name collides with a flattened namespace tool",
      "Responses namespace tools across Chat transports keeps the namespace mapping through ordinary JSON and forced-SSE JSON conversions",
      "Responses namespace tools across Chat transports maps a namespace tool_choice to the safe Chat function declaration",
      "Responses namespace tools across Chat transports rebuilds namespace and custom-tool identity for streamed Chat tool calls without leaking it to another request",
      "Responses namespace tools across Chat transports recursively flattens bounded nested namespace leaves without declaring a synthetic group tool"
    ],
    "tests/unit/openai-responses-nonstream.test.js": [
      "Antigravity streaming tool calls for a Responses-API client emits a Responses function_call instead of an empty stream",
      "custom tool names supplied as the request translator's array accepts the collection the request translator actually produces",
      "custom tool names supplied as the request translator's array emits custom_tool_call when the marked name arrives in an array",
      "custom tool names supplied as the request translator's array emits function_call when the array holds a different name",
      "custom tool names supplied as the request translator's array emits function_call when the collection is an empty array",
      "custom tool names supplied as the request translator's array emits function_call when the collection is an empty Set",
      "custom tool names supplied as the request translator's array emits function_call when the collection is null",
      "custom tool names supplied as the request translator's array emits function_call when the collection is undefined",
      "custom tool names supplied as the request translator's array preserves multi-line raw input verbatim",
      "custom tool names supplied as the request translator's array treats an array and a Set identically",
      "custom tool names supplied as the request translator's array unwraps the Chat input parameter without leaking the JSON wrapper",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream converts forced Antigravity SSE into Responses JSON",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream parses chat SSE chunks and returns a Responses function_call body",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream returns a custom_tool_call for a marked tool",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream returns a custom_tool_call when the marked names arrive as an array",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream returns an Anthropic Message for a Claude-format client (forceStream provider)",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream returns an Anthropic Message with text content for a Claude-format client",
      "forced-SSE JSON path for a Responses-API client behind a chat upstream still returns chat.completion for a plain chat client",
      "non-stream Chat upstream for a Responses-API client (op-ericding bug) chains Antigravity response conversion into Responses output",
      "non-stream Chat upstream for a Responses-API client (op-ericding bug) keeps chat.completion text content as a Responses message item",
      "non-stream Chat upstream for a Responses-API client (op-ericding bug) leaves chat->chat untouched",
      "non-stream Chat upstream for a Responses-API client (op-ericding bug) translates chat.completion tool_calls into Responses function_call output",
      "non-stream Chat upstream for a Responses-API client (op-ericding bug) translates marked Chat tools into Responses custom_tool_call output",
      "non-stream Responses-API upstream for a Chat/Claude client preserves Responses cached input tokens in Chat usage details",
      "non-stream Responses-API upstream for a Chat/Claude client translates a Responses function_call into a Claude tool_use block",
      "non-stream Responses-API upstream for a Chat/Claude client translates Responses output text into chat.completion content for an OpenAI-chat client",
      "non-stream Responses-API upstream for a Chat/Claude client translates Responses output text into Claude message content for a Claude client"
    ],
    "tests/unit/openai-responses-request-roundtrip-gaps.test.js": [
      "Chat → Responses: content and tool conversions a non-function tool declaration passes through untouched",
      "Chat → Responses: content and tool conversions image blocks convert: object url, string url, input_image passthrough, AI SDK image",
      "Chat → Responses: content and tool conversions response_format converts: json_schema, schemaless json_schema, json_object",
      "Chat → Responses: content and tool conversions tool message with array content joins its text parts",
      "Chat → Responses: content and tool conversions unknown tool_choice object shape passes through",
      "Chat → Responses: reasoning continuity fields reasoning_details array joins text and content entries",
      "Chat → Responses: reasoning continuity fields string msg.reasoning becomes the summary text",
      "Responses → Chat: tool name mapping a call name with no declaration at all passes through unchanged",
      "Responses → Chat: tool name mapping a call without a namespace resolves to the single namespaced declaration",
      "Responses → Chat: tool name mapping duplicate declarations of the same tool resolve to one safe name",
      "Responses → Chat: tool_choice and misc shapes a hosted-tool tool_choice shape passes through untouched",
      "Responses → Chat: tool_choice and misc shapes max_output_tokens maps to max_tokens and text.format maps to response_format",
      "Responses → Chat: tool_choice and misc shapes reasoning item text carried in content[] attaches to the next assistant message",
      "Responses → Chat: tool_choice and misc shapes text.format json_object maps, and a formatless text field is just dropped",
      "Responses → Chat: tool_choice and misc shapes unknown content block types inside a message pass through as-is"
    ],
    "tests/unit/openai-responses-streaming-usage.test.js": [
      "OpenAI Chat Completions to Responses streaming usage forwards a trailing usage-only chunk on response.completed",
      "OpenAI Chat Completions to Responses streaming usage forwards usage carried on the finish chunk",
      "OpenAI Chat Completions to Responses streaming usage includes estimated usage when upstream usage is unavailable"
    ],
    "tests/unit/openai-responses-terminal-event.test.js": [
      "OpenAI Responses streaming termination does not add response.failed when a Responses stream already completed",
      "OpenAI Responses streaming termination does not add response.failed when a Responses stream sends response.done",
      "OpenAI Responses streaming termination emits a response.failed event when a Responses stream closes before a terminal event",
      "OpenAI Responses streaming termination emits response.failed before DONE when a Responses stream sends DONE without a terminal event"
    ],
    "tests/unit/openai-responses-usage-cost-redaction.test.js": [
      "same-format OpenAI Responses exact-cost redaction retains terminal costs internally but omits them from the client SSE event"
    ],
    "tests/unit/openai-to-antigravity-heartbeat-2431.test.js": [
      "openaiToAntigravityResponse tool-call heartbeat (#2431) does not spam a second heartbeat before the interval elapses again",
      "openaiToAntigravityResponse tool-call heartbeat (#2431) emits a heartbeat once accumulation has run long enough, without leaking a functionCall",
      "openaiToAntigravityResponse tool-call heartbeat (#2431) never heartbeats a short tool call that finishes before the interval elapses",
      "openaiToAntigravityResponse tool-call heartbeat (#2431) stays silent immediately after the first tool-call fragment",
      "openaiToAntigravityResponse tool-call heartbeat (#2431) still assembles the correct functionCall at finish despite interleaved heartbeats"
    ],
    "tests/unit/openai-to-claude-response-tools.test.js": [
      "openaiToClaudeResponse tool argument sanitization drops invalid Read pages and clamps numeric bounds",
      "openaiToClaudeResponse tool argument sanitization keeps valid PDF pages"
    ],
    "tests/unit/openai-to-claude-tools-no-type.test.js": [
      "openai→claude: tools shape fidelity flat Anthropic-shape tool (no function wrapper) is passed through with name preserved",
      "openai→claude: tools shape fidelity non-function built-in tool types are passed through (cache_control tag is OK)",
      "openai→claude: tools shape fidelity tool WITH explicit type:'function' is rewritten to Anthropic shape",
      "openai→claude: tools shape fidelity tool WITHOUT explicit type but WITH function wrapper preserves the original name (was 'undefined' in v0.5.20)"
    ],
    "tests/unit/openai-to-claude.test.js": [
      "openaiToClaudeRequest response_format handling should inject basic JSON instructions for json_object type",
      "openaiToClaudeRequest response_format handling should inject JSON schema instructions for json_schema type",
      "openaiToClaudeRequest response_format handling should not modify system prompt when response_format is missing",
      "openaiToClaudeRequest response_format handling should preserve existing system messages when adding response_format",
      "openaiToClaudeRequest tool_choice handling converts OpenAI forced tool ({type:'function'}) to Claude {type:'tool'}",
      "openaiToClaudeRequest tool_choice handling maps string tool_choice values",
      "openaiToClaudeRequest tool_choice handling never leaks an invalid type (falls back to auto)",
      "openaiToClaudeRequest tool_choice handling omits tool_choice entirely when the request has none",
      "openaiToClaudeRequest tool_choice handling passes through Claude-native tool_choice objects unchanged",
      "openaiToClaudeResponse omits empty Read pages tool argument before emitting Claude input deltas"
    ],
    "tests/unit/openai-to-commandcode.test.js": [
      "openaiToCommandCodeRequest — assistant tool_calls / tool-call converts assistant.tool_calls[] into content blocks of type tool-call",
      "openaiToCommandCodeRequest — basic envelope returns the expected top-level envelope shape",
      "openaiToCommandCodeRequest — content shape MUST always emit content as Array (never string) for user",
      "openaiToCommandCodeRequest — content shape MUST always emit content as Array for assistant",
      "openaiToCommandCodeRequest — system handling hoists system messages to params.system (string), not messages[]",
      "openaiToCommandCodeRequest — system handling joins multiple system messages with blank line",
      "openaiToCommandCodeRequest — system handling omits params.system when no system messages",
      "openaiToCommandCodeRequest — thinking suffix stripping leaves plain model ids untouched",
      "openaiToCommandCodeRequest — thinking suffix stripping strips a client thinking suffix from params.model (upstream rejects it)",
      "openaiToCommandCodeRequest — thinking suffix stripping strips the suffix from family-prefixed ids too",
      "openaiToCommandCodeRequest — tool role / tool-result (AI SDK) converts role:\"tool\" to role:\"tool\" with tool-result block; output is {type:\"text\",value}",
      "openaiToCommandCodeRequest — tools schema conversion converts OpenAI {type:\"function\", function:{...}} to Anthropic plain {name, input_schema}",
      "openaiToCommandCodeRequest — tools schema conversion does not include tools field when input has none",
      "openaiToCommandCodeRequest — tools schema conversion preserves description on converted tool",
      "openaiToCommandCodeRequest — tools schema conversion preserves image_url as an image content block"
    ],
    "tests/unit/openai-to-cursor-literals.test.js": [
      "assistant tool_calls length guard is strictly >0, not >=0 an empty tool_calls array falls through to the plain-content branch, not the tool_calls branch",
      "buildToolResultBlock: exact five-line layout, newline-joined renders every literal segment and separator exactly, with all fallbacks empty",
      "extractContent: array-of-blocks filter guard (AND, not OR) a content value that is neither string nor array yields empty text, not a crash",
      "extractContent: array-of-blocks filter guard (AND, not OR) drops null/non-object entries without throwing (would throw if the guard let them through)",
      "extractContent: array-of-blocks filter guard (AND, not OR) joins multiple text blocks with no separator, including an empty-text block",
      "extractContent: array-of-blocks filter guard (AND, not OR) requires both type===text AND text typeof string; either alone is dropped",
      "normalizeToolCallId: newline-suffixed ids dedupe to the base id a tool result referencing the base id still resolves the name recorded under the newline-suffixed id",
      "top-level request shape: exact key set survives, none extra rest-spread carries through only non-stripped fields with no injected ones",
      "user tool_result block: missing tool_use_id metadata lookup falls back to \"tool\" when the referenced tool_use_id has no recorded meta, tries both raw and normalized"
    ],
    "tests/unit/openai-to-cursor-more.test.js": [
      "ASSISTANT content-array to tool_calls conversion a content array with no tool_use blocks and no text falls through without pushing a message",
      "ASSISTANT content-array to tool_calls conversion a text block plus a tool_use block: joined text goes to content, tool_use becomes tool_calls",
      "ASSISTANT content-array to tool_calls conversion a tool_use block with an empty id is dropped by the tc.id truthy filter",
      "ASSISTANT tool_calls: index stripped, every other field kept a tool_calls entry with an index field emits without it but keeps id/type/function",
      "normalizeToolCallId: no dedup entry when the id has no newline suffix a tool_call_id with no newline registers only under its raw id, not twice",
      "plain-string message empty-content guard a user message with an empty string content is dropped, not pushed as empty",
      "plain-string message empty-content guard a user message with non-empty string content is pushed with role and content intact",
      "TOOL role toolName fallback chain an unrecognized tool_call_id with no msg.name falls all the way to \"tool\"",
      "TOOL role toolName fallback chain msg.name wins over the recorded tool meta name",
      "top-level rest-spread strips exactly the named OpenAI/Anthropic-only fields user, metadata, tool_choice, stream_options, system are removed; everything else survives"
    ],
    "tests/unit/openai-to-cursor-mutation.test.js": [
      "openaiToCursorRequest: assistant messages converts Claude-style tool_use blocks to OpenAI tool_calls, dropping calls with no id",
      "openaiToCursorRequest: assistant messages drops a plain-string message with empty content",
      "openaiToCursorRequest: assistant messages drops an assistant array-content message that has no text and no tool_use",
      "openaiToCursorRequest: assistant messages emits plain text assistant message when no tool_use blocks present",
      "openaiToCursorRequest: assistant messages strips the index field from OpenAI-style tool_calls",
      "openaiToCursorRequest: request shape defaults max_tokens via adjustMaxTokens when absent",
      "openaiToCursorRequest: request shape strips OpenAI/Anthropic-only top-level fields",
      "openaiToCursorRequest: system messages prefixes system content with the marker and moves it to a user message",
      "openaiToCursorRequest: tool role messages escapes XML-significant characters in the tool result",
      "openaiToCursorRequest: tool role messages falls back to 'tool' when no name is on record",
      "openaiToCursorRequest: tool role messages renders a tool result block with the recorded tool name from tool_calls",
      "openaiToCursorRequest: tool role messages strips control characters from the tool result text",
      "openaiToCursorRequest: user array content (Claude-style blocks) drops the message entirely when all blocks are filtered out",
      "openaiToCursorRequest: user array content (Claude-style blocks) joins text blocks and drops non-text/tool_result blocks",
      "openaiToCursorRequest: user array content (Claude-style blocks) renders tool_result blocks using the recorded assistant tool_use name"
    ],
    "tests/unit/openai-to-cursor-remaining.test.js": [
      "an all-empty parts array produces no pushed user message a content array with a text block of empty string and no tool_result contributes no message",
      "ASSISTANT tool_calls with no content defaults to empty string, not undefined an assistant message with tool_calls and no content field gets content: \"\"",
      "extractContent array filtering a non-object entry (e.g. a bare string) in the content array is skipped, not thrown on",
      "normalizeToolCallId splits on newline and keeps only the first segment an id with a newline-appended suffix registers both the raw and normalized form",
      "rememberToolMeta name fallback an empty-string function name falls back to \"tool\" in the meta map",
      "TOOL_RESULT toolMeta lookup falls back to the normalized id a tool_use_id with a newline suffix still resolves the tool name via normalization",
      "USER content-array non-object block guard a bare string block in a user content array is skipped, not thrown on"
    ],
    "tests/unit/openai-to-cursor-request.test.js": [
      "assistant tool_calls fidelity a tool_use block without an id is not emitted as a broken call",
      "assistant tool_calls fidelity an assistant message with neither content nor calls is dropped, not sent empty",
      "assistant tool_calls fidelity extracts Claude-style tool_use blocks from assistant array content into tool_calls",
      "assistant tool_calls fidelity preserves OpenAI tool_calls, strips only the streaming index field",
      "message conversion escapes XML and strips control characters from tool results (backend request errors)",
      "message conversion recovers the tool name via the newline-normalized id variant",
      "message conversion system becomes a user message tagged [System Instructions]",
      "message conversion tool message becomes a user XML tool_result block with name recovered from the earlier tool_call",
      "message conversion user array content joins text and tool_result blocks; empty output emits no message",
      "top-level field policy respects the client max_tokens instead of forcing the 32k floor",
      "top-level field policy strips user, metadata, tool_choice, stream_options and system; keeps tools",
      "top-level field policy tolerates a body without messages"
    ],
    "tests/unit/openai-to-gemini-response.test.js": [
      "openai -> gemini streaming response projection never returns a raw OpenAI chat.completion.chunk shape to the client (the original bug)",
      "openai -> gemini streaming response projection projects OpenAI chunks into the Gemini response.candidates envelope",
      "openai -> gemini-cli streaming response projection never returns a raw OpenAI chat.completion.chunk shape to the client (the original bug)",
      "openai -> gemini-cli streaming response projection projects OpenAI chunks into the Gemini response.candidates envelope",
      "openai -> vertex streaming response projection never returns a raw OpenAI chat.completion.chunk shape to the client (the original bug)",
      "openai -> vertex streaming response projection projects OpenAI chunks into the Gemini response.candidates envelope"
    ],
    "tests/unit/openai-to-kiro.test.js": [
      "openaiToKiroRequest basic message conversion should convert a simple text message",
      "openaiToKiroRequest basic message conversion should not include images field when no images are present",
      "openaiToKiroRequest image forwarding should forward base64 image from image_url content part",
      "openaiToKiroRequest image forwarding should forward multiple base64 images",
      "openaiToKiroRequest image forwarding should include both images and text content together",
      "openaiToKiroRequest image forwarding should not include images field when images array is empty",
      "openaiToKiroRequest image forwarding should treat http image URLs as text fallback (Kiro only supports base64)",
      "openaiToKiroRequest thinking budget clamps OpenAI Responses reasoning.effort xhigh to max_thinking_length 32000",
      "openaiToKiroRequest thinking budget clamps reasoning_effort max to Kiro max_thinking_length 32000",
      "openaiToKiroRequest thinking budget defaults future Kiro model ids to additionalModelRequestFields support",
      "openaiToKiroRequest thinking budget does not inject thinking prefix for reasoning_effort none",
      "openaiToKiroRequest thinking budget does not send additionalModelRequestFields for date-suffixed Claude 4 model ids",
      "openaiToKiroRequest thinking budget does not send additionalModelRequestFields for legacy Kiro model ids",
      "openaiToKiroRequest thinking budget does not send additionalModelRequestFields for pre-4 legacy Kiro model ids",
      "openaiToKiroRequest thinking budget does not send additionalModelRequestFields for prefixed pre-4 legacy Kiro model ids",
      "openaiToKiroRequest thinking budget does not send Claude-specific additionalModelRequestFields for non-Claude aliases",
      "openaiToKiroRequest thinking budget does not send Claude-specific additionalModelRequestFields for prefixed non-Claude aliases",
      "openaiToKiroRequest thinking budget keeps GPT-5.6 reasoning intentionally disabled for effort disabled",
      "openaiToKiroRequest thinking budget keeps GPT-5.6 reasoning intentionally disabled for effort none",
      "openaiToKiroRequest thinking budget keeps GPT-5.6 reasoning intentionally disabled for effort off",
      "openaiToKiroRequest thinking budget keeps the frozen session start stable across turns and keeps the clock off it",
      "openaiToKiroRequest thinking budget keeps the legacy thinking fallback for unsupported GPT-5.6 effort auto",
      "openaiToKiroRequest thinking budget keeps the legacy thinking fallback for unsupported GPT-5.6 effort minimal",
      "openaiToKiroRequest thinking budget keeps the legacy thinking fallback for unsupported GPT-5.6 effort ultra",
      "openaiToKiroRequest thinking budget keeps the thinking-alias fallback when GPT effort is blank",
      "openaiToKiroRequest thinking budget maps a supported Kiro Claude intensity suffix to native effort fields",
      "openaiToKiroRequest thinking budget maps GPT-5.6 reasoning.effort high without legacy prompt tags",
      "openaiToKiroRequest thinking budget maps GPT-5.6 reasoning.effort low without legacy prompt tags",
      "openaiToKiroRequest thinking budget maps GPT-5.6 reasoning.effort medium without legacy prompt tags",
      "openaiToKiroRequest thinking budget maps reasoning_effort high to max_thinking_length 24576",
      "openaiToKiroRequest thinking budget maps reasoning_effort low to max_thinking_length 1024",
      "openaiToKiroRequest thinking budget normalizes unsupported Kiro intensity suffix for claude-sonnet-4.5-thinking-agentic(high)",
      "openaiToKiroRequest thinking budget normalizes unsupported Kiro intensity suffix for glm-5-thinking-agentic(medium)",
      "openaiToKiroRequest thinking budget omits GPT-5.6 effort fields and legacy prompt tags when effort is absent",
      "openaiToKiroRequest thinking budget preserves GPT-5.6 effort max as supported wire effort gpt-5.6-sol",
      "openaiToKiroRequest thinking budget preserves GPT-5.6 effort xhigh as supported wire effort gpt-5.6-terra",
      "openaiToKiroRequest thinking budget replays frozen msg0 for explicit Kiro sessions while keeping current time fresh",
      "openaiToKiroRequest thinking budget uses Claude thinking.budget_tokens as max_thinking_length",
      "openaiToKiroRequest thinking budget uses the default budget for synthetic -thinking models with no explicit config",
      "openaiToKiroRequest tool interaction without client-provided tools should flatten Claude tool_use / tool_result blocks with no tools array",
      "openaiToKiroRequest tool interaction without client-provided tools should flatten OpenAI tool_calls + tool result into history text with no tools array",
      "openaiToKiroRequest tool interaction without client-provided tools should keep structured tools when the client DOES provide a tools array",
      "openaiToKiroRequest tool interaction without client-provided tools should salvage orphaned tool_result content as text instead of discarding it"
    ],
    "tests/unit/openai-to-ollama-malformed.test.js": [
      "openaiToOllamaRequest - tool_calls arguments parsing malformed JSON args -> {} (no throw)",
      "openaiToOllamaRequest - tool_calls arguments parsing valid JSON args -> parsed object"
    ],
    "tests/unit/openai-tool-call-id-normalization.test.js": [
      "official OpenAI tool-call ID normalization does not normalize IDs for the openai-compatible-custom provider",
      "official OpenAI tool-call ID normalization does not normalize IDs for the openrouter provider",
      "official OpenAI tool-call ID normalization is deterministic for the same ID across independent requests",
      "official OpenAI tool-call ID normalization is encounter-order independent when the tool result precedes its assistant call",
      "official OpenAI tool-call ID normalization keeps IDs distinct when truncation alone would collide",
      "official OpenAI tool-call ID normalization normalizes an 86-character multi-turn ID and preserves its call/result relationship",
      "official OpenAI tool-call ID normalization normalizes repeated occurrences of the same overlong ID identically",
      "official OpenAI tool-call ID normalization sends the same normalized ID on both sides of the upstream request"
    ],
    "tests/unit/opencode-deepseek-reasoning-echo-1093.test.js": [
      "deepseek thinking models get reasoning_content echoed back (#1093) does not fire for a model with no such contract",
      "deepseek thinking models get reasoning_content echoed back (#1093) does not overwrite reasoning the client already sent back",
      "deepseek thinking models get reasoning_content echoed back (#1093) leaves user and tool turns untouched",
      "deepseek thinking models get reasoning_content echoed back (#1093) opencode-go/deepseek-v4-flash-free carries it on every assistant turn",
      "deepseek thinking models get reasoning_content echoed back (#1093) opencode-zen/deepseek-v4-flash-free carries it on every assistant turn",
      "deepseek thinking models get reasoning_content echoed back (#1093) opencode/deepseek-v4-flash-free carries it on every assistant turn"
    ],
    "tests/unit/opencode-executor.test.js": [
      "OpenCodeExecutor fingerprint (free-tier 429 fix) falls back to the client-supplied x-real-ip when the server did not stamp one",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) forwards the sanitized peer IP as x-real-ip for per-IP quota buckets",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) keeps a stable session per conversation via client session headers",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) keeps sessions per-request under concurrency (no singleton bleed)",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) never forwards loopback x-real-ip (local users would share one bucket)",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) never forwards private/LAN x-real-ip either",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) omits x-real-ip when no client IP is known",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) passes through a real opencode downstream User-Agent untouched",
      "OpenCodeExecutor fingerprint (free-tier 429 fix) sends the official versioned opencode User-Agent on free-tier requests"
    ],
    "tests/unit/opencode-format-1173.test.js": [
      "opencode forceStream (#1173) forces streaming so JSON clients never hit opencode's broken stream:false branch"
    ],
    "tests/unit/opencode-go-connection-test-usage-3250.test.js": [
      "the OpenCode Go connection test does not bill the user (#3250) never touches a chat-completions path",
      "the OpenCode Go connection test does not bill the user (#3250) probes the free usage endpoint with a GET",
      "the OpenCode Go connection test does not bill the user (#3250) reports a key with zero remaining quota as valid",
      "the OpenCode Go connection test does not bill the user (#3250) still rejects the key on 401",
      "the OpenCode Go connection test does not bill the user (#3250) still rejects the key on 403",
      "the OpenCode Go connection test does not bill the user (#3250) still rejects the key on 404"
    ],
    "tests/unit/opencode-go-models.test.js": [
      "Custom multi-protocol transports resolves a custom transport matching the incoming format",
      "Custom multi-protocol transports returns null when the custom provider has no matching endpoint",
      "OpenCode Go model catalog matches the documented model IDs",
      "OpenCode Go multi-endpoint transports declares openai / claude / openai-responses transports",
      "OpenCode Go multi-endpoint transports resolveTransport picks the endpoint matching the client sourceFormat",
      "OpenCode Go multi-endpoint transports uses x-api-key + anthropicVersion on the claude transport",
      "OpenCode Go per-model supportedFormats declares [openai, claude, openai-responses] for DeepSeek vision-exp",
      "OpenCode Go per-model supportedFormats declares [openai, claude] for MiniMax + Qwen models",
      "OpenCode Go per-model supportedFormats declares [openai] only for chat-only models (GLM/Kimi/MiMo) → guards /messages routing",
      "OpenCode Go per-model supportedFormats declares [openai] only for DeepSeek chat-only models (pro/flash)",
      "OpenCode Go per-model transport guard (chatCore logic) does NOT route chat-only models to /messages on a claude-format request",
      "OpenCode Go per-model transport guard (chatCore logic) does NOT route DeepSeek pro/flash to /messages or /responses",
      "OpenCode Go per-model transport guard (chatCore logic) does NOT route MiniMax (no responses support) to /responses",
      "OpenCode Go per-model transport guard (chatCore logic) routes DeepSeek vision-exp + responses-format client to /responses",
      "OpenCode Go per-model transport guard (chatCore logic) routes MiniMax/Qwen + claude-format client to /messages"
    ],
    "tests/unit/opencode-stream-usage-3081.test.js": [
      "OpenCodeExecutor stream usage for PR #3081 adds usage for a Claude client resolved onto the OpenAI Chat wire",
      "OpenCodeExecutor stream usage for PR #3081 does not add OpenAI stream fields to a direct Claude Messages body",
      "OpenCodeExecutor stream usage for PR #3081 does not add OpenAI stream fields to a non-OpenAI wire body",
      "OpenCodeExecutor stream usage for PR #3081 does not add streaming fields to a resolved non-stream Chat Completions body",
      "OpenCodeExecutor stream usage for PR #3081 fills an undefined usage choice without discarding supplied stream options",
      "OpenCodeExecutor stream usage for PR #3081 preserves an explicit include_usage false choice",
      "OpenCodeExecutor stream usage for PR #3081 restores stream intent and requests usage for translated Chat Completions bodies"
    ],
    "tests/unit/opencode-thinking-effort.test.js": [
      "opencode thinking effort claude budget intent → reasoning_effort on x-preview-f-free (was stripped)",
      "opencode thinking effort gateway enum: xhigh/ultra clamp to max, minimal clamps to low",
      "opencode thinking effort none disables explicitly, auto omits the field (upstream default)",
      "opencode thinking effort other providers unaffected",
      "opencode thinking effort provider format wins over per-model caps: claude id on opencode gets reasoning_effort, not claude thinking blocks",
      "opencode thinking effort stealth ids resolve reasoning:true with the opencode format",
      "opencode thinking effort UI levels expose the gateway enum (incl. max) for stealth and vendor ids"
    ],
    "tests/unit/openrouter-ox-alpha-caps-3472.test.js": [
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) cannot disable thinking — the Stealth endpoint rejects that",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) carries its own window and output ceiling rather than the 200k default",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) declares tools and no vision",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) does not leak onto other OpenRouter models",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) does not leak onto the same model name under a different provider",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) is a reasoning model, which is the part that produced the blank body",
      "openrouter stealth/ox-alpha resolves its own capabilities (#3472) uses the OpenAI thinking wire, since OpenRouter is OpenAI-compatible"
    ],
    "tests/unit/openrouter-stealth-fallback.test.js": [
      "OpenRouterExecutor — body transform for provider fallback routing does not coerce a non-object provider value into an object",
      "OpenRouterExecutor — body transform for provider fallback routing injectReasoningContent is preserved (parent hook order unchanged)",
      "OpenRouterExecutor — body transform for provider fallback routing injects provider.allow_fallbacks = true on a bare body",
      "OpenRouterExecutor — body transform for provider fallback routing preserves a caller-supplied provider object and only adds allow_fallbacks when missing",
      "OpenRouterExecutor — body transform for provider fallback routing registers a specialized executor for the openrouter provider id",
      "OpenRouterExecutor — body transform for provider fallback routing respects a caller-supplied allow_fallbacks = false (does not overwrite)",
      "parseUpstreamError — annotate OpenRouter Stealth signature annotates 502 with provider_name=Stealth and Invalid URL: empty value",
      "parseUpstreamError — annotate OpenRouter Stealth signature annotates when only message matches Invalid URL: <empty> (no provider_name)",
      "parseUpstreamError — annotate OpenRouter Stealth signature does not annotate a 4xx (e.g. 401) even if message starts with Invalid URL",
      "parseUpstreamError — annotate OpenRouter Stealth signature does not annotate unrelated 502 errors",
      "parseUpstreamError — annotate OpenRouter Stealth signature falls back to raw body when JSON parsing fails"
    ],
    "tests/unit/openrouter-tts-provider.test.js": [
      "openrouter synthesize accumulates audio across reads split mid-line and skips junk lines",
      "openrouter synthesize falls back to the status code when the error body is not JSON",
      "openrouter synthesize posts to the configured baseUrl with auth and extra headers, wav output",
      "openrouter synthesize splits vendor/model/voice into model and voice",
      "openrouter synthesize throws the upstream error message on a non-ok JSON response",
      "openrouter synthesize throws when the stream carries no audio",
      "openrouter synthesize throws without an API key",
      "openrouter synthesize treats a single-slash model as a voice on the default model",
      "openrouter synthesize treats a slashless model as the voice"
    ],
    "tests/unit/operation-events-migration.test.js": [
      "schema 14: operation events keeps operationEvents present and 15 reserved for project budgets",
      "schema 14: operation events upgrading a version-13 database takes a backup before creating operationEvents"
    ],
    "tests/unit/operation-events-route.test.js": [
      "operation events query errors name their cause never echoes the caller input back in the message",
      "operation events query errors name their cause says the filter is unsupported when the parameter is unknown",
      "operation events query errors name their cause says the same for a duplicated parameter",
      "operation events query errors name their cause still points at the range when the range is what is malformed"
    ],
    "tests/unit/operation-events.test.js": [
      "operation event invariants allowlists structural details and drops credentials, URLs and free text",
      "operation event invariants an interrupted operation stays unresolved: started row only, no success",
      "operation event invariants refuses a second started and a second terminal for the same operation/phase",
      "operation event invariants rejects unknown, duplicate and oversized query parameters",
      "operation history through the analytics worker applies filters before stable pagination",
      "proxy-pool test producer a cancelled probe records cancelled and does NOT bench the pool",
      "proxy-pool test producer a late result against a changed pool records conflict and leaves the pool alone",
      "proxy-pool test producer commits the terminal receipt atomically with the activation effect",
      "proxy-pool test producer dashboard verdict treats a 200 transport with ok:false body as unreachable",
      "proxy-pool test producer forwards the caller's cancellation signal into the probe"
    ],
    "tests/unit/operation-history-inspector.test.js": [
      "activation consequence disclosure states scope, timing and reversibility beside the control",
      "probe history paging requests page two through the bounded query and renders its rows",
      "probe history paging sends a state filter to the server rather than filtering a cut page",
      "probe history rendering a cancelled probe is not a failure and reports activation untouched",
      "probe history rendering a start with no terminal receipt reads as unresolved, never as failure",
      "probe history rendering keeps the freshness and join limitations visible instead of implying completeness",
      "probe history rendering renders a missing value as a word and never as a fabricated zero",
      "probe history rendering renders a retained receipt with its identifiers, both times, actor, source and code",
      "probe history rendering says nothing was retained rather than showing an empty grid",
      "probe history rendering shows a conflict as a discarded result, naming which conflict was recorded",
      "reads an installation-wide filtered scope and preserves a key receipt without pool claims",
      "resolution pairing pairs a start against a terminal receipt only within the same operation and phase",
      "resolution pairing tolerates a missing or non-array payload without inventing rows"
    ],
    "tests/unit/operations-parity-contracts.test.js": [
      "projects failed connection scans separately from an observed empty inventory",
      "retains the admin permission refusal without reading readiness"
    ],
    "tests/unit/operations-retained-receipts.test.js": [
      "matches a terminal receipt outside the page, state filter and capture range",
      "opens an exact historical alert event without losing it beyond default pagination",
      "queue counts include all fixtures while retained history is filtered and paged"
    ],
    "tests/unit/operator-custom-headers-2660.test.js": [
      "an operator can configure a connection's headers (#2660) adds a gateway's own auth header",
      "an operator can configure a connection's headers (#2660) an empty value REMOVES a header this router would otherwise send",
      "an operator can configure a connection's headers (#2660) CAN override this router's auth, which is the whole point",
      "an operator can configure a connection's headers (#2660) ignores a malformed config rather than throwing on the request path",
      "an operator can configure a connection's headers (#2660) refuses framing headers, whoever sets them",
      "an operator can configure a connection's headers (#2660) replaces a header regardless of the casing this router used",
      "an operator can configure a connection's headers (#2660) skips a non-string value and an empty name",
      "precedence between the operator and the caller both run after this router has built its own auth",
      "precedence between the operator and the caller the caller cannot override what the operator configured",
      "precedence between the operator and the caller the caller still gets headers the operator said nothing about",
      "precedence between the operator and the caller the executor applies them in that order"
    ],
    "tests/unit/orphan-tool-results-2236.test.js": [
      "orphan repair reaches every target format (#2236) Claude target carries no unmatched tool_result",
      "orphan repair reaches every target format (#2236) Gemini target keeps the content instead of dropping it",
      "orphan repair reaches every target format (#2236) Responses target builds no unmatched function_call_output",
      "orphan tool result repair (#2236) does not leave two user turns in a row after salvage",
      "orphan tool result repair (#2236) drops an image-only orphan rather than inventing text for it",
      "orphan tool result repair (#2236) keeps a result that does have its call",
      "orphan tool result repair (#2236) keeps only the first result for a call and salvages the duplicate",
      "orphan tool result repair (#2236) leaves a body with no tool results untouched",
      "orphan tool result repair (#2236) leaves a Responses-shaped body (input[], no messages) alone",
      "orphan tool result repair (#2236) salvages a Claude tool_result block whose tool_use is gone",
      "orphan tool result repair (#2236) salvages an OpenAI tool result whose call is gone"
    ],
    "tests/unit/pair-dropper.test.js": [
      "depth cap protects, never drops (audit finding 14) a tool_use nested deeper than the walk cap makes the pair undroppable",
      "dropOldestPairs caps notes at 8 and sets notesTruncated",
      "dropOldestPairs does not mutate the frozen input and shares surviving entries by reference",
      "dropOldestPairs drops oldest text-only pairs until the deficit is covered",
      "dropOldestPairs drops the first pair when protectFirstUser is false",
      "dropOldestPairs keeps dropping until the deficit is fully covered, then stops",
      "dropOldestPairs leaves a leading assistant message in place",
      "dropOldestPairs never drops or splits a pair containing tool_use",
      "dropOldestPairs pass non-array input through unchanged",
      "dropOldestPairs protectFirstUser holds the first user message and its pair",
      "dropOldestPairs protects the recent tail of keepRecentTurns entries",
      "dropOldestPairs reports savedChars as the cumulative serialized chars removed",
      "dropOldestPairs returns the input reference for an empty array",
      "dropOldestPairs returns the input reference when deficitChars is 0",
      "dropOldestPairs returns the input reference when deficitChars is missing",
      "dropOldestPairs returns the input reference when deficitChars is negative",
      "dropOldestPairs treats nested tool_result content as non-droppable"
    ],
    "tests/unit/param-support.test.js": [
      "stripUnsupportedParams clamps VolcEngine Ark GLM max token fields to the model output ceiling",
      "stripUnsupportedParams flattens Cloudflare AI OpenAI content-part arrays",
      "stripUnsupportedParams keeps VolcEngine Ark GLM max tokens when already under the ceiling",
      "stripUnsupportedParams still drops unsupported GitHub model params",
      "stripUnsupportedParams strips reasoning_content from Mistral message history"
    ],
    "tests/unit/passthrough-dedupe-normalize-3611.test.js": [
      "passthrough dedupe/normalization (PR 3611) drops a duplicated finish chunk",
      "passthrough dedupe/normalization (PR 3611) keeps upstream [DONE] and does not send a second one from flush",
      "passthrough dedupe/normalization (PR 3611) newline-terminates a trailing buffer frame glued before [DONE]",
      "passthrough dedupe/normalization (PR 3611) normalizes delta.reasoning into delta.reasoning_content"
    ],
    "tests/unit/pending-request-timeouts.test.js": [
      "pending request timeout ownership does not extend an older request deadline when a new request starts",
      "pending request timeout ownership does not let an unmatched account stop decrement another account",
      "pending request timeout ownership expires a remaining overlap after its sibling stops",
      "pending request timeout ownership expires only the contributing account while preserving another model and account",
      "pending request timeout ownership keeps no-account requests in the model total until their own stop or expiry"
    ],
    "tests/unit/perplexity-web.test.js": [
      "buildPplxRequestBody includes required params",
      "buildPplxRequestBody sets query_str at both top-level AND params (required by upstream API)",
      "buildQuery — degenerate first turn empty history and empty current message still produce a query field",
      "buildQuery first turn: returns JSON with instructions + query",
      "buildQuery follow-up (with backendUuid): returns plain currentMsg, no JSON",
      "buildQuery ignores tools on follow-up turn (uses session)",
      "buildQuery includes history when present on first turn",
      "buildQuery injects tools into instructions on first turn",
      "buildQuery truncates query if JSON exceeds 96000 chars",
      "formatToolsHint handles flat tool schema",
      "formatToolsHint handles OpenAI tool schema (function wrapper)",
      "formatToolsHint returns empty string for no tools",
      "formatToolsHint truncates long descriptions to first line, max 200 chars",
      "parseOpenAIMessages extracts system + history + current msg",
      "parseOpenAIMessages handles multi-part content (array of text blocks)",
      "parseOpenAIMessages skips empty content messages",
      "parseOpenAIMessages treats developer role as system",
      "PerplexityWebExecutor — session continuity across turns a follow-up matching a stored transcript reuses the backend uuid and sends a plain query",
      "PerplexityWebExecutor — session continuity across turns a follow-up with no new user message is rejected as an empty query",
      "PerplexityWebExecutor — session continuity across turns an expired session entry is dropped and the turn restarts as a first turn",
      "PerplexityWebExecutor — SSE wire-format edge cases a final data line without a trailing newline is still flushed",
      "PerplexityWebExecutor — SSE wire-format edge cases CRLF line endings and a [DONE] sentinel terminate cleanly",
      "PerplexityWebExecutor — SSE wire-format edge cases event: end_of_stream terminates without needing [DONE]",
      "PerplexityWebExecutor.execute — non-streaming details a network failure returns 502; an abort is rethrown to the caller",
      "PerplexityWebExecutor.execute — non-streaming details a null upstream body returns a 502 empty-body error",
      "PerplexityWebExecutor.execute — non-streaming details an error event returns a 502 with the upstream message",
      "PerplexityWebExecutor.execute — non-streaming details an unmapped model rides through as a raw copilot preference",
      "PerplexityWebExecutor.execute — non-streaming details collects thinking into reasoning_content and text-fallback events into content",
      "PerplexityWebExecutor.execute — streaming a body stream that throws mid-read yields a stream-error chunk and [DONE], never a hang",
      "PerplexityWebExecutor.execute — streaming emits role chunk, content deltas, reasoning for search steps, finish and [DONE]",
      "PerplexityWebExecutor.execute — streaming surfaces an upstream error event as an inline error chunk, still terminated",
      "PerplexityWebExecutor.execute applies THINKING_MAP when reasoning_effort is set",
      "PerplexityWebExecutor.execute injects body.tools into query_str instructions",
      "PerplexityWebExecutor.execute maps pplx-auto → mode=concise, pref=pplx_pro",
      "PerplexityWebExecutor.execute returns 400 on missing messages",
      "PerplexityWebExecutor.execute sends Bearer header when credentials.accessToken provided",
      "PerplexityWebExecutor.execute sends Cookie header when credentials.apiKey provided",
      "PerplexityWebExecutor.execute surfaces 429 with rate-limit message",
      "PerplexityWebExecutor.execute surfaces upstream 401 with friendly auth message"
    ],
    "tests/unit/ping-reasoning-models-3010.test.js": [
      "pingModelByKind reasoning models (#3010) passes a normal answer with the larger budget",
      "pingModelByKind reasoning models (#3010) still fails when there are no choices and no reasoning",
      "pingModelByKind reasoning models (#3010) treats a reasoning-only (length-limited) response as ok:true",
      "pingModelByKind reasoning models (#3010) uses a 1024-token budget for the chat completions probe"
    ],
    "tests/unit/plan-transfer.test.js": [
      "merges exact names and ordered members without changing aliases, siblings or unexported restrictions",
      "requires explicit capacity exclusion and rejects malformed, ambiguous or credential-bearing files",
      "retains pasted input and blocks replay when a server write may already have committed",
      "stores only a reviewed draft and reads its exact immutable revision before returning to the editor"
    ],
    "tests/unit/pr3445-nonrouting.test.js": [
      "PR 3445 non-routing port convertResponsesStreamToJson preserves status:incomplete + incomplete_details",
      "PR 3445 non-routing port opencode-go muse demotes forced tool_choice to auto (with and without suffix)",
      "PR 3445 non-routing port providerModels resolves formats/targetFormat through a thinking suffix",
      "PR 3445 non-routing port SSE-to-JSON emits finish_reason length on max_output_tokens truncation (chat client)",
      "PR 3445 non-routing port SSE-to-JSON emits stop_reason max_tokens on truncation (claude client)",
      "PR 3445 non-routing port translator maps max_tokens to max_output_tokens with precedence",
      "PR 3445 non-routing port translator normalizes tool_choice to Responses shape",
      "PR 3445 non-routing port translator respects stream:false instead of hardcoding stream:true"
    ],
    "tests/unit/prefetch-images.test.js": [
      "prefetchRemoteImages claude source -> kiro target: source.url -> base64",
      "prefetchRemoteImages gemini source -> gemini target: fileData URL -> inlineData base64",
      "prefetchRemoteImages no-op for targets that accept remote URLs (openai)",
      "prefetchRemoteImages openai source -> ollama target: converts remote URL to base64",
      "prefetchRemoteImages skips data URI (already inline)"
    ],
    "tests/unit/prefix-stability.test.js": [
      "anchorClaudeCache {ttl} seam defaults to the legacy policy: 1h on system, bare 5m on the tail",
      "anchorClaudeCache {ttl} seam ttl \"1h\" lifts a client anchor that carried no ttl of its own",
      "anchorClaudeCache {ttl} seam ttl \"1h\" preserves a client-explicit ttl anchor verbatim",
      "anchorClaudeCache {ttl} seam ttl \"1h\" upgrades only the conversation-tail anchor",
      "anchorClaudeCache {ttl} seam ttl \"5m\" is byte-identical to the default call",
      "chooseCacheTtl drops non-finite and negative samples before counting",
      "chooseCacheTtl returns 1h once 3 samples have a p90 gap over 20 minutes",
      "chooseCacheTtl returns 5m when the p90 gap is at or under 20 minutes",
      "chooseCacheTtl returns 5m with fewer than 3 samples, however large the gaps",
      "handleChatCore prefix telemetry keeps the 5m policy when the measured gaps are short, even with the flag on",
      "handleChatCore prefix telemetry lands epochHitRate and volatileKeys on the context-status entry",
      "handleChatCore prefix telemetry stamps the 1h tail anchor only once 3 measured gaps outlive 20 min, and only with the flag on",
      "recordEpochRate is undefined with no samples and ignores non-finite ones",
      "recordEpochRate keeps a rolling mean over the last EPOCH_RATE_WINDOW samples",
      "volatileFieldReport accepts sketches and never mutates them (no-rewrite guarantee)",
      "volatileFieldReport does not flag a change whose value starts in the last 25% of the body",
      "volatileFieldReport flags a key that only exists in the newer body",
      "volatileFieldReport flags an early key that changed while the rest stayed stable",
      "volatileFieldReport flags nothing when every key changed — no single key is the divergence source",
      "volatileFieldReport skips unparseable entries and counts only the pairs it compared"
    ],
    "tests/unit/preparation-cache-bounds.test.js": [
      "embedding vector cache is bounded evicts least-recently-used entries rather than growing without limit",
      "tool disclosure session cache is bounded keeps a live session disclosures across the eviction of others",
      "tool disclosure session cache is bounded stops growing once past its ceiling, however many sessions arrive"
    ],
    "tests/unit/preparation-media-cancellation.test.js": [
      "bounded remote-image preparation abandons DNS lookup promptly and never starts fetch after late DNS success",
      "bounded remote-image preparation cancels readers and queued images together and does not commit completed siblings",
      "bounded remote-image preparation coalesces repeated URLs, bounds concurrent work and commits in source order",
      "bounded remote-image preparation keeps timeout protection when caller cancellation is supplied",
      "bounded remote-image preparation refuses aggregate expansion from repeated images without partially mutating the request"
    ],
    "tests/unit/preparation-service-cancellation.test.js": [
      "optional preparation cancellation cancels embedding without changing caller input or treating abort as provider failure",
      "optional preparation cancellation cancels headroom without changing caller input or treating abort as provider failure",
      "optional preparation cancellation passes a cancellation signal to a cooperative PXPIPE adapter and ignores late output"
    ],
    "tests/unit/pricing-controls-api.test.js": [
      "persists known model rates and resets only the reviewed scope",
      "rejects malformed pricing and ambiguous reset scopes without mutation"
    ],
    "tests/unit/privacy-redact-2728.test.js": [
      "privacy filter (#2728) emits a chunk that cannot be mid-alias without holding it back",
      "privacy filter (#2728) is a no-op with nothing to redact and never re-aliases its own output",
      "privacy filter (#2728) pseudonymises emails in message content and restores them verbatim",
      "privacy filter (#2728) redacts operator-supplied literal terms, longest match first",
      "privacy filter (#2728) restores an alias split across streaming chunks",
      "privacy filter (#2728) walks Responses `input` and a Claude `system` block",
      "protocol keys survive the content walk (audit finding 1) redacts thinking text but never the Claude signature",
      "protocol keys survive the content walk (audit finding 1) tool_use.id/name and tool_result.tool_call_id stay byte-identical when a term matches the tool name",
      "the round trip a client actually sees (#2728) an email leaves redacted and comes back verbatim, non-streaming",
      "the round trip a client actually sees (#2728) costs nothing and changes nothing when the filter is off",
      "the round trip a client actually sees (#2728) keeps the document valid when the original carries JSON metacharacters",
      "the round trip a client actually sees (#2728) leaves the payload untouched when the filter throws",
      "the round trip a client actually sees (#2728) restores a placeholder split across two stream chunks",
      "the round trip a client actually sees (#2728) restores a term carried inside a tool call's arguments",
      "the wiring that makes it reachable (#2728) does the redaction work only when the gate is on",
      "the wiring that makes it reachable (#2728) hands the mapping to both response paths",
      "the wiring that makes it reachable (#2728) is off by default in settings and passed in from the chat handler"
    ],
    "tests/unit/probe-abort-signal-1450.test.js": [
      "a connection probe is bounded by an abort signal (#1450) gives up on a black-holed upstream instead of hanging",
      "a connection probe is bounded by an abort signal (#1450) passes an abort signal on the plain api-key path",
      "a connection probe is bounded by an abort signal (#1450) routes the Ollama probes through the proxy-aware fetch, with a signal"
    ],
    "tests/unit/probe-max-tokens-1672.test.js": [
      "a credential probe asks for a usable token budget (#1672) a hand-added OpenAI-compatible endpoint gets the same budget",
      "a credential probe asks for a usable token budget (#1672) anthropic asks for the shared floor, not one token",
      "a credential probe asks for a usable token budget (#1672) Azure uses the same floor on its own parameter name",
      "a credential probe asks for a usable token budget (#1672) glm asks for the shared floor, not one token",
      "a credential probe asks for a usable token budget (#1672) keeps the floor at the value ping.js already settled on",
      "a credential probe asks for a usable token budget (#1672) kimi spends nothing at all on an api-key probe",
      "a credential probe asks for a usable token budget (#1672) minimax asks for the shared floor, not one token"
    ],
    "tests/unit/project-admin-api.test.js": [
      "binding and unbinding enforce exact observed identity and revision without leaking the key",
      "bounds body reads independently of content length and rejects unknown fields and query dimensions",
      "creates, persists, validates and rejects stale project policies through real repository handlers",
      "refuses a caller before reading any project mutation body"
    ],
    "tests/unit/project-attribution-migration.test.js": [
      "project attribution migration with better-sqlite3 preserves unknown legacy quantities and reservations, then retains exact new project identities across reopen",
      "project attribution migration with sql.js preserves unknown legacy quantities and reservations, then retains exact new project identities across reopen"
    ],
    "tests/unit/project-budgets.test.js": [
      "exact project attribution and joint budget authority alert-only project policy observes without blocking while key enforcement remains active",
      "exact project attribution and joint budget authority archives only subsequent dispatches and preserves in-flight attribution after explicit unbinding",
      "exact project attribution and joint budget authority bounds the complete project binding snapshot independently of the per-key limit",
      "exact project attribution and joint budget authority paginates the full observed identity population without duplicates or key mixing",
      "exact project attribution and joint budget authority projects delivery preparation status without exposing subscribed endpoint identities or hashes",
      "exact project attribution and joint budget authority refuses omitted, mismatched, and cross-key metadata on an explicitly bound key",
      "exact project attribution and joint budget authority rejects stale policy writes and paginates immutable policy versions and projects",
      "exact project attribution and joint budget authority releases key and project exposure only on confirmed nonacceptance",
      "exact project attribution and joint budget authority requires an explicitly observed binding and never assigns earlier usage retrospectively",
      "exact project attribution and joint budget authority retains authenticated task and project references after request retention expires, including pagination",
      "exact project attribution and joint budget authority retains strict unknown-bound refusal and explicit best-effort single outstanding exposure",
      "exact project attribution and joint budget authority settles and reconciles one shared reservation, hourly counters, and immutable alert receipts exactly once",
      "exact project attribution and joint budget authority shares one project ceiling across concurrent keys without leaking rejected key reservations",
      "recorded project spending projection does not infer an exhaustion horizon from an incomplete lifetime cost balance",
      "recorded project spending projection does not present a reversed observation interval for a newly created project",
      "recorded project spending projection refuses sparse or incomplete cost evidence rather than making missing usage free",
      "recorded project spending projection uses complete-hour ledger coverage with explicit assumptions and no provider charge guarantee"
    ],
    "tests/unit/project-controls-ui.test.js": [
      "identifies the exact client on unbinding without broadening its mutation payload",
      "keeps an acknowledged create identity when readback fails and requires a fresh read before retry",
      "keeps creation disabled until initial state loads",
      "shows exact binding evidence, preserves identity after stale refusal, and passes exact scope to navigation"
    ],
    "tests/unit/project-id-onboard-paths.test.js": [
      "cleanupNow sweeps drops malformed pending entries and logs a sweep error without killing the timer",
      "cleanupNow sweeps evicts a stale cache entry and aborts an over-age pending fetch",
      "cleanupNow sweeps start and stop are idempotent",
      "onboardUser polling accepts the string form of the onboard project",
      "onboardUser polling bails out terminally when done:true carries no usable project",
      "onboardUser polling retries an onboard HTTP error and gives up after the attempt cap",
      "onboardUser polling returns the project id once onboarding reports done",
      "onboardUser polling stops retrying the moment the external signal aborts an attempt",
      "onboardUser polling treats a per-attempt 30s timeout as retryable, not terminal",
      "onboardUser polling waits 2s between not-done polls and succeeds on a later attempt",
      "response body reading survives a non-JSON body from loadCodeAssist"
    ],
    "tests/unit/project-notification-recovery.test.js": [
      "bounds a recovery batch and backs off invalid retained intent without starving other rows",
      "cancels a recovered delivery when its original destination changed",
      "never manufactures authorization for disabled or historically unknown destinations",
      "retains event-time destinations and accounting when delivery creation fails, then recovers exactly once",
      "retains unavailable authorization explicitly instead of retrying against later configuration"
    ],
    "tests/unit/provider-base-url-field-3467.test.js": [
      "buildProviderSpecificData ignores a typed baseUrl for a provider that does not declare the field",
      "buildProviderSpecificData returns nothing for an ordinary provider",
      "buildProviderSpecificData saves a trimmed baseUrl when the provider declares the field",
      "buildProviderSpecificData saves nothing when the box is left empty, so the default still applies",
      "buildProviderSpecificData still builds the Azure shape",
      "buildProviderSpecificData still builds the Cloudflare and region shapes",
      "registry: providers with a per-connection endpoint declare it each placeholder matches the default that provider actually falls back to",
      "registry: providers with a per-connection endpoint declare it ollama-local declares a baseUrlField",
      "registry: providers with a per-connection endpoint declare it providers with a fixed endpoint do not declare one",
      "registry: providers with a per-connection endpoint declare it selfhosted-embedding declares a baseUrlField",
      "registry: providers with a per-connection endpoint declare it selfhosted-stt declares a baseUrlField",
      "registry: providers with a per-connection endpoint declare it selfhosted-tts declares a baseUrlField"
    ],
    "tests/unit/provider-check-404-2032.test.js": [
      "provider Check rejects 404 uniformly (#2032) all twelve call sites were converted, none left behind",
      "provider Check rejects 404 uniformly (#2032) every status guard routes through the one predicate",
      "provider Check rejects 404 uniformly (#2032) the predicate rejects 401, 403 and 404 and nothing else"
    ],
    "tests/unit/provider-cleanup.test.js": [
      "custom model token limits API keeps omitted token limits absent from persisted models",
      "custom model token limits API persists supplied positive integer input and output limits",
      "custom model token limits API rejects supplied limits that are not positive integers without persisting a model",
      "provider deletion alias cleanup preserves the not-found response when the provider node does not exist",
      "provider deletion alias cleanup removes only aliases whose decoded model belongs to the deleted provider",
      "provider deletion alias cleanup rolls back all provider state through the real node:sqlite fallback adapter",
      "provider deletion alias cleanup rolls back all provider state through the real sql.js fallback adapter",
      "provider deletion alias cleanup rolls back connection and alias deletion when provider-node deletion fails",
      "provider deletion alias cleanup uses the real node:sqlite fallback adapter for exact alias cleanup",
      "provider deletion alias cleanup uses the real sql.js fallback adapter for exact alias cleanup"
    ],
    "tests/unit/provider-concurrency-2519.test.js": [
      "per-provider concurrency cap (#2519) allows a request below the cap",
      "per-provider concurrency cap (#2519) counts only the provider being routed to",
      "per-provider concurrency cap (#2519) does not count completed requests from the snapshot",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open on %p",
      "per-provider concurrency cap (#2519) fails open when the active snapshot cannot be read",
      "per-provider concurrency cap (#2519) is off when nothing is configured",
      "per-provider concurrency cap (#2519) refuses once the provider is at the cap, and says the numbers"
    ],
    "tests/unit/provider-custom-models.test.js": [
      "provider custom model rows filters built-in models and typed custom models",
      "provider custom model rows keeps identical model IDs separate per provider",
      "provider custom model rows keeps legacy alias-backed models visible without duplicating custom models"
    ],
    "tests/unit/provider-display-split.test.js": [
      "provider display split (E1) AI_PROVIDERS entries still carry merged display + transport",
      "provider display split (E1) Cloudflare exposes API-key authentication on its free-tier card",
      "provider display split (E1) display fields source from providersDisplay.js",
      "provider display split (E1) helpers still work after split"
    ],
    "tests/unit/provider-error-detail-3424.test.js": [
      "describeProviderError (#3424) collapses newlines so the dashboard line stays one line",
      "describeProviderError (#3424) does not repeat a code the message already carries",
      "describeProviderError (#3424) falls back to the code when there is no message at all",
      "describeProviderError (#3424) keeps a string reason exactly as before, clamped to 2000 chars",
      "describeProviderError (#3424) names the transport failure behind `fetch failed`",
      "describeProviderError (#3424) never serializes the error object wholesale",
      "describeProviderError (#3424) reads the usual provider JSON error shapes",
      "describeProviderError (#3424) still says 'Provider error' when there is nothing to say"
    ],
    "tests/unit/provider-fallback-initials-1831.test.js": [
      "one rule for the provider badge (#1831) a custom embedding provider gets its own badge rather than a generic one",
      "one rule for the provider badge (#1831) a custom provider reads as what it is compatible with, not as its id",
      "one rule for the provider badge (#1831) a regional sibling resolves to the one brand mark, not a second copy",
      "one rule for the provider badge (#1831) a registry provider keeps the badge it declares",
      "one rule for the provider badge (#1831) anything else takes the name a human sees, not the internal id",
      "one rule for the provider badge (#1831) falls back to the id only when there is no display name",
      "one rule for the provider badge (#1831) is stable, so two surfaces cannot disagree",
      "one rule for the provider badge (#1831) keeps custom protocol compatibility separate from provider branding",
      "one rule for the provider badge (#1831) never returns an empty badge",
      "one rule for the provider badge (#1831) resolves registry marks locally without third-party asset requests"
    ],
    "tests/unit/provider-favicon-discovery-1830.test.js": [
      "caching (#1830) does not re-probe a hit",
      "caching (#1830) does not re-probe a miss either, which is the expensive case",
      "candidate order (#1830) does not repeat the origin root when the base URL already is it",
      "candidate order (#1830) never walks up from an IP literal, where a label up means nothing",
      "candidate order (#1830) refuses a non-http scheme and a malformed base outright",
      "candidate order (#1830) tries endpoint-adjacent, then the origin root, then the registrable domain",
      "what it accepts back (#1830) 204s when nothing answers, which is the caller's cue to use initials",
      "what it accepts back (#1830) refuses a body over the size cap, declared or actual",
      "what it accepts back (#1830) refuses a body that is not an image type",
      "what it accepts back (#1830) returns the first candidate that answers, as a data URI",
      "what the route will fetch (#1830) 404s an unknown node instead of guessing an origin",
      "what the route will fetch (#1830) still runs the SSRF guard, since a node base URL is operator input",
      "what the route will fetch (#1830) takes a node id, never a URL, so it is not a fetch proxy"
    ],
    "tests/unit/provider-format-contracts.test.js": [
      "credentialAuthMode normalizes declared authType",
      "credentialAuthMode shape decides when no authType is declared",
      "detectFormat antigravity wrapped gemini",
      "detectFormat bare contents array is gemini",
      "detectFormat claude tool blocks decide when images are absent",
      "detectFormat claude via system field with text-block content",
      "detectFormat claude vs openai image shapes",
      "detectFormat openai-only indicator fields win before claude checks",
      "detectFormat responses input (array or string) without messages",
      "detectFormat string content with system indicator is claude, else openai default",
      "getTargetFormat compatible prefixes resolve by contract, unknown providers default",
      "getTargetFormat matches each registered provider's own configured format",
      "resolveOpenAICompatibleApiType falls back to the id substring for legacy nodes",
      "resolveOpenAICompatibleApiType stored apiType is authoritative over the node id",
      "resolveTransport credential-scoped selection among format matches",
      "resolveTransport falls back to the unscoped entry, then to the first match",
      "resolveTransport no authModes anywhere: undeclared format resolves to null (provider default)",
      "resolveTransport plain first-match when no transport declares authModes",
      "resolveTransport registry-configured transports resolve their own declared format",
      "resolveTransport returns null with no transports",
      "resolveTransport undeclared client format stays on the credential's endpoint (#943)",
      "thinking config normalization hasThinkingConfig",
      "thinking config normalization isLastMessageFromUser reads messages or contents, empty is true",
      "thinking config normalization normalizeThinkingConfig drops thinking only on non-user last turn"
    ],
    "tests/unit/provider-health-stats-1336.test.js": [
      "getProviderHealth — nothing recorded returns no rows and still states the window that produced them",
      "getProviderHealth — success rate and latency per account averages only the requests that measured a latency, and says how many",
      "getProviderHealth — success rate and latency per account carries the model down to the model grain",
      "getProviderHealth — success rate and latency per account falls back to the account grain for an unknown groupBy rather than grouping by nothing",
      "getProviderHealth — success rate and latency per account reports no average rather than an average of zero when nothing was measured",
      "getProviderHealth — success rate and latency per account rolls the accounts back up at the provider grain",
      "getProviderHealth — success rate and latency per account splits one provider into its accounts",
      "getProviderHealth — the window bounds the numbers excludes a request older than the requested period",
      "getProviderHealth — the window bounds the numbers includes it once the period is unbounded",
      "getProviderHealth — the window bounds the numbers lets an explicit date range win over the period and reports itself as a range"
    ],
    "tests/unit/provider-import-json-1329.test.js": [
      "normalization (#1329) accepts api_key as the apikey spelling some exports carry",
      "normalization (#1329) computes expiresAt from expiresIn when only that is present",
      "normalization (#1329) defaults to an active connection, as the login flow leaves one",
      "normalization (#1329) keeps an expiresAt the export already carried",
      "normalization (#1329) keeps an explicit isActive false",
      "reporting (#1329) one failing write does not abandon the rest",
      "reporting (#1329) reports per-item outcomes and never echoes a token",
      "what it refuses to write (#1329) drops a field the connection layer does not own",
      "what it refuses to write (#1329) drops server-owned fields instead of letting the body set them",
      "what it refuses to write (#1329) refuses an item with no credential at all",
      "what it refuses to write (#1329) refuses an unknown authType instead of coercing one",
      "what it refuses to write (#1329) refuses an unknown provider rather than creating a dead row",
      "what it refuses to write (#1329) refuses providerSpecificData that is not an object",
      "what the importer accepts (#1329) refuses a body that carries nothing",
      "what the importer accepts (#1329) takes a database export shaped { providerConnections: [...] }",
      "what the importer accepts (#1329) takes a wrapped { accounts: [...] }",
      "what the importer accepts (#1329) takes an array",
      "what the importer accepts (#1329) takes the single object from the report verbatim"
    ],
    "tests/unit/provider-models-minimax-m3.test.js": [
      "MiniMax-M3 model registration does not regress the existing M2.7 / M2.5 / M2.1 entries",
      "MiniMax-M3 model registration exposes MiniMax-M3 through getModelsByProviderId for both provider IDs",
      "MiniMax-M3 model registration includes MiniMax-M3 in PROVIDER_MODELS.minimax",
      "MiniMax-M3 model registration includes MiniMax-M3 in PROVIDER_MODELS['minimax-cn']"
    ],
    "tests/unit/provider-node-ssrf-3293.test.js": [
      "provider node validation SSRF boundary (#3293) gives Docker host guidance for a trusted local http://127.0.0.1:11434/v1 failure",
      "provider node validation SSRF boundary (#3293) gives Docker host guidance for a trusted local http://localhost:11434/v1 failure",
      "provider node validation SSRF boundary (#3293) keeps a public provider URL working through the guarded dispatcher",
      "provider node validation SSRF boundary (#3293) preserves private self-hosted validation for a trusted local peer",
      "provider node validation SSRF boundary (#3293) returns 400 for the reported IPv4-mapped IPv6 bypass"
    ],
    "tests/unit/provider-pricing-minimax-m3.test.js": [
      "MiniMax-M3 pricing includes MiniMax-M3 in MODEL_PRICING",
      "MiniMax-M3 pricing MiniMax-M3 cached price matches the design spec (0.06)",
      "MiniMax-M3 pricing MiniMax-M3 input price matches the design spec (0.30)",
      "MiniMax-M3 pricing MiniMax-M3 output price matches the design spec (1.20)",
      "MiniMax-M3 pricing MiniMax-M3 pricing has numeric shape (input, output, cached)"
    ],
    "tests/unit/provider-proxy-config.test.js": [
      "persists an account concurrency ceiling, rejects invalid values and clears only that ceiling",
      "provider proxy write ownership derives and clears the complete active pool selection pair",
      "provider proxy write ownership PATCH lazily clears only the exact historical default tuple",
      "provider proxy write ownership PATCH maps __none__ to the persisted direct marker and clears a pool pair",
      "provider proxy write ownership PATCH retains a false strict snapshot when a pooled record has a legacy tuple",
      "provider proxy write ownership POST maps __none__ to the persisted direct marker",
      "provider proxy write ownership POST omits the historical tuple smuggled through provider data",
      "provider proxy write ownership POST records explicit local direct separately from no selection",
      "provider proxy write ownership POST writes only the explicit supported proxy policy",
      "provider proxy write ownership rejects combining a selected pool with legacy connection proxy fields",
      "provider proxy write ownership rejects no-proxy without an explicit enabled supported URL",
      "provider proxy write ownership selected pools override a retained direct policy until the binding is cleared"
    ],
    "tests/unit/provider-registry-index-generator-2955.test.js": [
      "deterministic provider-registry index generator (#2955) checks the tracked registry index and leaves repeated generation byte-stable",
      "deterministic provider-registry index generator (#2955) reports drift without overwriting it, then repairs to a checkable index",
      "deterministic provider-registry index generator (#2955) treats a missing index as drift and can create it",
      "deterministic provider-registry index generator (#2955) writes a lexical static registry and keeps disabled providers inactive"
    ],
    "tests/unit/provider-retry-delay-cap-2895.test.js": [
      "per-provider retry-delay cap (#2895) caps a free-tier pool's blind backoff so the pool rotates inside that window",
      "per-provider retry-delay cap (#2895) caps a free-tier provider's long reset at its real window",
      "per-provider retry-delay cap (#2895) leaves a free-tier credential failure on its full lock",
      "per-provider retry-delay cap (#2895) leaves a paid provider's exponential backoff untouched",
      "per-provider retry-delay cap (#2895) leaves a paid provider's reported reset alone",
      "per-provider retry-delay cap (#2895) still clamps a paid provider at the global ceiling"
    ],
    "tests/unit/provider-selection-queue.test.js": [
      "provider-scoped account-selection queue allows different providers to overlap while either selection is pending",
      "provider-scoped account-selection queue preserves filtering and preferred-account selection",
      "provider-scoped account-selection queue releases the next same-provider selection after rejection",
      "provider-scoped account-selection queue removes every provider queue entry after the selections settle",
      "provider-scoped account-selection queue serializes an alias with its canonical provider ID",
      "provider-scoped account-selection queue serializes two selections for the same provider"
    ],
    "tests/unit/provider-test-models-routing.test.js": [
      "provider test-models route kind routing routes huggingface image models to /api/v1/images/generations"
    ],
    "tests/unit/provider-thinking-config.test.js": [
      "normalizeThinkingConfig keeps openai reasoning_effort on non-user turns"
    ],
    "tests/unit/provider-thinking-injection-2927.test.js": [
      "PR #2927 provider thinking precedence does not inject provider thinking into native Claude passthrough",
      "PR #2927 provider thinking precedence keeps a client thinking budget ahead of the provider default",
      "PR #2927 provider thinking precedence keeps an explicit client level ahead of the provider default",
      "PR #2927 provider thinking precedence keeps disabled client thinking ahead of the provider default",
      "PR #2927 provider thinking precedence replaces an unlevelled adaptive effort marker on a translated route",
      "PR #2927 provider thinking precedence uses a configured level for unlevelled Claude thinking on a translated route"
    ],
    "tests/unit/provider-validation.test.js": [
      "Provider Validation API Anthropic Compatible should normalize URL by removing /messages suffix",
      "Provider Validation API Anthropic Compatible should send correct headers for Anthropic API",
      "Provider Validation API Error Messages - /chat/completions Status Codes should return auth error for 401",
      "Provider Validation API Error Messages - /chat/completions Status Codes should return failed for other codes",
      "Provider Validation API Error Messages - /chat/completions Status Codes should return invalid model for 400",
      "Provider Validation API Error Messages - /chat/completions Status Codes should return not found for 404",
      "Provider Validation API Error Messages - /chat/completions Status Codes should return server error for 503",
      "Provider Validation API Error Messages - /models Status Codes should return auth error for 401",
      "Provider Validation API Error Messages - /models Status Codes should return auth error for 403",
      "Provider Validation API Error Messages - /models Status Codes should return not found for 404",
      "Provider Validation API Error Messages - /models Status Codes should return server error for 500",
      "Provider Validation API Error Messages - /models Status Codes should return server error for 502",
      "Provider Validation API Error Messages - /models Status Codes should return unexpected for other codes",
      "Provider Validation API Error Messages - Network should map CERT_HAS_EXPIRED to user-friendly message",
      "Provider Validation API Error Messages - Network should map ECONNREFUSED to user-friendly message",
      "Provider Validation API Error Messages - Network should map ENOTFOUND to user-friendly message",
      "Provider Validation API Error Messages - Network should map timeout to user-friendly message",
      "Provider Validation API OpenAI Compatible should fallback to chat/completions when /models fails and modelId provided",
      "Provider Validation API OpenAI Compatible should return error when /models fails and no modelId",
      "Provider Validation API OpenAI Compatible should return valid:true when /models succeeds",
      "Provider Validation API Response Format should return correct format for failure with error",
      "Provider Validation API Response Format should return correct format for success via /models",
      "Provider Validation API Response Format should return correct format for success via chat",
      "Provider Validation API URL Validation should validate correct URL format"
    ],
    "tests/unit/providers-client-priority-sort-1323.test.js": [
      "#1323 sort=priority survives the rows the DB can actually produce a throw upstream of the sort is what a 500 on this route means",
      "#1323 sort=priority survives the rows the DB can actually produce caps pageSize so one request cannot ask for the whole table",
      "#1323 sort=priority survives the rows the DB can actually produce clamps a page past the end instead of answering a negative offset",
      "#1323 sort=priority survives the rows the DB can actually produce does not throw on an empty connection list",
      "#1323 sort=priority survives the rows the DB can actually produce does not throw when every priority ties and the provider key decides",
      "#1323 sort=priority survives the rows the DB can actually produce does not throw when priority is absent entirely",
      "#1323 sort=priority survives the rows the DB can actually produce does not throw when priority is null — the nullable column's own value",
      "#1323 sort=priority survives the rows the DB can actually produce sorts when every row carries a priority",
      "#1323 sort=priority survives the rows the DB can actually produce still answers 200 when the request omits sort (priority is the default)"
    ],
    "tests/unit/providers-list-filter-2998.test.js": [
      "provider connection listing filter (#2998) keeps redacting secrets on the filtered path",
      "provider connection listing filter (#2998) resolves a display-name spelling to the stored provider id",
      "provider connection listing filter (#2998) returns only the requested provider's connections",
      "provider connection listing filter (#2998) still lists every provider when no filter is given",
      "provider connection listing filter (#2998) treats a blank filter as unfiltered and an unknown one as empty"
    ],
    "tests/unit/proxy-abort-no-direct-fallback-2211.test.js": [
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=false: AbortError: This operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=false: Error: cancelled",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=false: Error: The operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=false: TimeoutError: The operation timed out",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=true: AbortError: This operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=true: Error: cancelled",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=true: Error: The operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for cloudcode-pa.googleapis.com, strict=true: TimeoutError: The operation timed out",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=false: AbortError: This operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=false: Error: cancelled",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=false: Error: The operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=false: TimeoutError: The operation timed out",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=true: AbortError: This operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=true: Error: cancelled",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=true: Error: The operation was aborted",
      "an aborted request never falls back to direct (#2211) preserves transport cancellation for example.com, strict=true: TimeoutError: The operation timed out",
      "an aborted request never falls back to direct (#2211) rejects before dispatch when the caller cancels during pool creation"
    ],
    "tests/unit/proxy-fetch-dispatcher-eviction.test.js": [
      "proxy dispatcher cache eviction accepts positive safe-integer proxy timeout overrides",
      "proxy dispatcher cache eviction bounds concurrent distinct construction before dispatch",
      "proxy dispatcher cache eviction cancels one waiting caller without dispatching it or canceling another reservation",
      "proxy dispatcher cache eviction coalesces concurrent cold construction for one effective proxy",
      "proxy dispatcher cache eviction coalesces equivalent URL spelling but keeps proxy credentials distinct",
      "proxy dispatcher cache eviction gracefully closes only the evicted dispatcher without aborting active work",
      "proxy dispatcher cache eviction owns an evicted dispatcher's rejected close promise",
      "proxy dispatcher cache eviction promotes a cache hit so overflow closes B and retains the touched A",
      "proxy dispatcher cache eviction reclaims an idle dispatcher after a synchronous close failure before replacing it",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"-1\"",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"\"",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"0\"",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"1.5\"",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"10junk\"",
      "proxy dispatcher cache eviction rejects invalid proxy timeout override \"9007199254740992\"",
      "proxy dispatcher cache eviction reuses the dispatcher for a repeated normalized proxy identity",
      "proxy dispatcher cache eviction revokes local-refusal proof if an earlier GET fallback already invoked transport",
      "proxy dispatcher cache eviction uses a separate dispatcher when effective timeout configuration changes",
      "proxy dispatcher cache eviction uses one Happy Eyeballs Agent for intentional direct requests",
      "proxy dispatcher cache eviction uses streaming-safe defaults for newly created proxy dispatchers"
    ],
    "tests/unit/proxy-fetch-dns-negative-cache-864.test.js": [
      "MITM-bypass DNS failure is cached (#864) asks the external resolver once, not once per request",
      "MITM-bypass DNS failure is cached (#864) bounds the resolver so one unreachable lookup cannot stall for c-ares defaults",
      "MITM-bypass DNS failure is cached (#864) expires the cached failure, so a resolver that comes back is retried",
      "MITM-bypass DNS failure is cached (#864) still serves every one of those requests through native fetch"
    ],
    "tests/unit/proxy-fetch-install.test.js": [
      "proxy fetch installation boundaries explicitly installs global proxy fetch once from the OAuth provider barrel",
      "proxy fetch installation boundaries explicitly installs global proxy fetch once from the open-sse barrel",
      "proxy fetch installation boundaries installs the global proxy transport once only when explicitly requested",
      "proxy fetch installation boundaries keeps global fetch identity when the Ollama handler is imported in a fresh module graph",
      "proxy fetch installation boundaries keeps named proxyAwareFetch functional without installing it globally",
      "proxy fetch installation boundaries keeps the legacy 'Firecrawl' request byte-identical and direct under HTTPS_PROXY",
      "proxy fetch installation boundaries keeps the legacy 'Jina' request byte-identical and direct under HTTPS_PROXY"
    ],
    "tests/unit/proxy-fetch-loopback-pools.test.js": [
      "actual loopback proxy pool lifecycle keeps active response bodies available through cache pressure and reuses a canceled slot",
      "actual loopback proxy pool lifecycle keeps explicit direct dispatchers and loopback bypass untouched",
      "actual loopback proxy pool lifecycle preserves slow-reader backpressure and cancels an unfinished large body promptly",
      "actual loopback proxy pool lifecycle preserves unread but transport-complete response bodies after idle eviction",
      "actual loopback proxy pool lifecycle separates origin authority and proxy authentication while sharing each effective route",
      "actual loopback proxy pool lifecycle stops pending construction at shutdown before upstream dispatch and refuses later direct work"
    ],
    "tests/unit/proxy-fetch-mitm-abort.test.js": [
      "proxyAwareFetch MITM bypass aborts destroys the manual HTTPS request and socket with the deadline reason",
      "proxyAwareFetch MITM bypass aborts rejects a pre-aborted caller before DNS, socket, HTTPS, or fallback work",
      "proxyAwareFetch MITM bypass aborts settles at the response-header deadline while DNS is pending and removes its listener"
    ],
    "tests/unit/proxy-fetch-mitm-proxy-fallback.test.js": [
      "proxyAwareFetch MITM bypass via configured proxy falls back to direct bypass on a non-abort proxy failure (even a falsy rejection)",
      "proxyAwareFetch MITM bypass via configured proxy rejects through waitWithSignal when DNS fails under a live signal",
      "proxyAwareFetch MITM bypass via configured proxy rethrows an abort from the proxy attempt instead of falling back"
    ],
    "tests/unit/proxy-fetch-transport-coverage.test.js": [
      "createProxyDispatcher socks connect hook hands the socks socket to undici on success and the error on failure",
      "installGlobalProxyFetch swaps global fetch for the patched one, idempotently",
      "MITM bypass host with a proxy fails hard under strictProxy when the proxy path fails",
      "MITM bypass host with a proxy uses the proxy dispatcher directly (proxy resolves DNS externally)",
      "MITM DNS-bypass request (no proxy) a socket connect error surfaces as the direct-fetch fallback",
      "MITM DNS-bypass request (no proxy) an abort during DNS resolution propagates instead of being cached as failure",
      "MITM DNS-bypass request (no proxy) an abort while the socket is pending rejects with the abort reason",
      "MITM DNS-bypass request (no proxy) falls through to direct fetch when DNS resolution fails, and caches the failure",
      "MITM DNS-bypass request (no proxy) falls through to direct fetch when the transport errors",
      "MITM DNS-bypass request (no proxy) resolves the real IP and speaks HTTPS to it with the original hostname pinned",
      "proxy failure classification fails hard under strictProxy on a non-abort failure",
      "proxy failure classification falls back to direct on a non-abort failure without strictProxy",
      "proxy failure classification rethrows an abort-shaped proxy failure instead of retrying direct",
      "redactProxyUrlForLog replaces a parseable URL with an empty hostname",
      "relay route rewrites the request onto the relay URL with target/path headers",
      "route resolution edges declares invalid a proxy value that survives normalization but never parses",
      "route resolution edges declares selected-proxy invalid when enablement is claimed without a url",
      "route resolution edges falls back to direct for a garbage URL with no env proxy (unparseable protocol)",
      "route resolution edges treats an unparseable target as not matched by NO_PROXY and keeps the proxy"
    ],
    "tests/unit/proxy-log-redaction-2343.test.js": [
      "logProxySelection (#2343) does not print a relay URL's query secret",
      "logProxySelection (#2343) does not print proxy credentials, and does not echo an unparseable proxy URL",
      "redactProxyUrlForLog (#2343) fails closed rather than echoing an unparseable value",
      "redactProxyUrlForLog (#2343) keeps only scheme, host and port"
    ],
    "tests/unit/proxy-pool-bulk-delete-3400.test.js": [
      "proxy pools can be removed in bulk (#3400) deletes every unbound id given",
      "proxy pools can be removed in bulk (#3400) ignores a non-string id rather than deleting something unexpected",
      "proxy pools can be removed in bulk (#3400) leaves the listing filter alone",
      "proxy pools can be removed in bulk (#3400) reads the connections once, not once per id",
      "proxy pools can be removed in bulk (#3400) refuses a pool a connection still uses, and keeps deleting the rest",
      "proxy pools can be removed in bulk (#3400) rejects a request with no ids",
      "proxy pools can be removed in bulk (#3400) reports a failed delete per id rather than as one failure"
    ],
    "tests/unit/proxy-pool-relay-ssrf.test.js": [
      "POST /api/proxy-pools/[id]/test answers 200 for a public relay pool",
      "POST /api/proxy-pools/[id]/test answers 400 and persists the reason when a relay pool points at loopback",
      "POST /api/proxy-pools/[id]/test does not block an http pool whose proxy is on loopback",
      "testProxyUrl (ProxyAgent path) stays usable for private proxies dials a loopback proxy instead of refusing it as SSRF",
      "testRelayUrl SSRF guard allows a genuinely public relay and sends the relay probe headers",
      "testRelayUrl SSRF guard maps a DNS-rebinding refusal raised at connect time to 400, not 500",
      "testRelayUrl SSRF guard refuses carrier NAT 100.64/10 with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses cloud metadata with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses internal suffix with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses IPv4-mapped IPv6 loopback with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses IPv6 loopback with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses loopback (the reproduced exploit) with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses loopback by name with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses multicast with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses non-http protocol with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses private 10/8 with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses private 172.16/12 with an actionable 400 and never dials",
      "testRelayUrl SSRF guard refuses private 192.168/16 with an actionable 400 and never dials",
      "testRelayUrl SSRF guard rejects an empty relay URL without dialling",
      "testRelayUrl SSRF guard still reports an ordinary upstream failure as 500"
    ],
    "tests/unit/proxy-pool-strict-snapshot.test.js": [
      "proxy-pool strict snapshots does not overwrite a concurrently rebound connection snapshot",
      "proxy-pool strict snapshots does not overwrite a concurrently rebound no-auth strategy snapshot",
      "proxy-pool strict snapshots rolls back pool and snapshots when a fan-out write fails",
      "proxy-pool strict snapshots updates normal and no-auth snapshots in one pool PUT transaction"
    ],
    "tests/unit/proxy-url-shorthand-962.test.js": [
      "a proxy can be pasted in the vendor shorthand (#962) accepts the shorthand forms",
      "a proxy can be pasted in the vendor shorthand (#962) encodes credentials, so a password with @ or : cannot re-parse as a host",
      "a proxy can be pasted in the vendor shorthand (#962) normalises the value that flows downstream, not just the one it checks",
      "a proxy can be pasted in the vendor shorthand (#962) rejects a partial credential pair rather than guessing",
      "a proxy can be pasted in the vendor shorthand (#962) rejects what is not a proxy",
      "a proxy can be pasted in the vendor shorthand (#962) returns an already-valid URL byte-for-byte",
      "a proxy can be pasted in the vendor shorthand (#962) still accepts every full URL form it accepted before"
    ],
    "tests/unit/pxpipe-events-store.test.js": [
      "appendPxpipeEvent creates the dir and appends a timestamped JSONL line",
      "appendPxpipeEvent never throws when the write path is unusable",
      "appendPxpipeEvent rotates the file once it exceeds the size cap",
      "getPxpipeStats buckets events into windows and classifies applied/bypassed/errors",
      "getPxpipeStats zero events yields zeroed finalized totals",
      "readPxpipeEvents applies sinceMs and limit",
      "readPxpipeEvents merges rotated + current files sorted by ts, skipping corrupt lines",
      "readPxpipeEvents returns empty when no files exist"
    ],
    "tests/unit/pxpipe-install.test.js": [
      "findNpm returns null when the lookup produces nothing or throws",
      "findNpm returns the first line of which/where output, trimmed",
      "getInstallInfo fails closed on an unreadable package.json",
      "getInstallInfo reports installed with the package version when both files exist",
      "getInstallInfo reports not installed when the package or its entry is absent",
      "getInstallLogTail returns \"\" with no log and the last N non-empty lines with one",
      "installPxpipe rejects when npm exits 0 but the package never materialized",
      "installPxpipe rejects when npm exits non-zero",
      "installPxpipe rejects when the spawn itself errors",
      "installPxpipe rejects with NPM_NOT_FOUND when npm is absent",
      "installPxpipe serializes concurrent calls, writes the host package.json and log, and resolves install info on exit 0",
      "path shape PXPIPE_DIR sits inside the isolated DATA_DIR and the entry sits inside packageRoot"
    ],
    "tests/unit/pxpipe-loader-coverage.test.js": [
      "getTransform fails open to null when the load throws",
      "getTransform returns null on a cold cache when autoLoad is off, without loading",
      "getTransform returns the cached transform when warm even with autoLoad off",
      "getTransform returns the transform function when loadable",
      "loadPxpipe cache-busts by version so an upgrade reloads without a restart",
      "loadPxpipe coalesces concurrent loads into one in-flight promise",
      "loadPxpipe defaults the cache-bust key when the version is missing",
      "loadPxpipe loads the installed entry, caches it, and reports version and loadedAt",
      "loadPxpipe rejects a package that does not export transformAnthropicMessages",
      "loadPxpipe throws NOT_INSTALLED when no package is installed",
      "selfTest passes a synthetic Claude request through and reports ok with a reason",
      "selfTest propagates NOT_INSTALLED instead of masking it",
      "selfTest throws when the transform returns an unexpected shape",
      "unloadPxpipe invalidates an in-flight load so stop cannot silently restart its worker",
      "unloadPxpipe returns whether a module was loaded and clears the cache"
    ],
    "tests/unit/pxpipe-worker-cancellation.test.js": [
      "PXPIPE owned worker lifecycle bounds distinct queued jobs and rejects excess payload before dispatch",
      "PXPIPE owned worker lifecycle removes cancelled queued work without dispatching it",
      "PXPIPE owned worker lifecycle reuses the worker and leaves caller bytes intact",
      "PXPIPE owned worker lifecycle terminates active CPU work on cancellation then recovers without overlapping workers",
      "PXPIPE owned worker lifecycle terminates timed-out CPU work and refuses post-close admission"
    ],
    "tests/unit/pxpipe.test.js": [
      "compressWithPxpipe gates applies the transform and reports savings",
      "compressWithPxpipe gates bypasses small prompts below minChars",
      "compressWithPxpipe gates does not log skipped requests as savings",
      "compressWithPxpipe gates fails open on timeout",
      "compressWithPxpipe gates fails open when the transform throws",
      "compressWithPxpipe gates passes through when the transform declines (not_profitable)",
      "compressWithPxpipe gates skips non-Claude formats",
      "compressWithPxpipe gates skips when disabled",
      "compressWithPxpipe gates skips when transform is unavailable (not installed)"
    ],
    "tests/unit/pycharm-h2c-models-1326.test.js": [
      "#1326 a JetBrains h2c GET /v1/models is answered, not dropped leaves a non-h2c upgrade to the application's own listener",
      "#1326 a JetBrains h2c GET /v1/models is answered, not dropped replays the bodyless upgrade over HTTP/1.1 instead of writing zero bytes",
      "#1326 a JetBrains h2c GET /v1/models is answered, not dropped still answers the same GET without the upgrade header"
    ],
    "tests/unit/qac-prefix-stability.test.js": [
      "qac prefix stability across turns fixed point: re-running on the stage's own output with the same query is a no-op",
      "qac prefix stability across turns growth: a block shorter than its placeholder is left verbatim, longer ones are compressed",
      "qac prefix stability across turns oscillates: the same historical prefix is compressed at turn N and uncompressed at turn N+1"
    ],
    "tests/unit/qoder-billing.test.js": [
      "isBillingBlock detects code 10605 (queue throttle)",
      "isBillingBlock detects code 112 (quota exhausted)",
      "isBillingBlock detects pricingUrl field",
      "isBillingBlock returns false for empty or non-string input",
      "isBillingBlock returns false for normal errors without billing markers",
      "wrapQoderSSE billing detection passes through normal errors (non-billing) as wrapped SSE",
      "wrapQoderSSE billing detection passes through successful responses unchanged",
      "wrapQoderSSE billing detection returns 403 response when first frame has pricingUrl",
      "wrapQoderSSE billing detection returns 403 response when first frame is billing block (code 10605)",
      "wrapQoderSSE billing detection returns 403 response when first frame is billing block (code 112)"
    ],
    "tests/unit/qoder-connect-timeout.test.js": [
      "QoderExecutor response-header timeout clears its deadline and preserves an unrelated network error",
      "QoderExecutor response-header timeout clears its deadline when ordinary response headers arrive",
      "QoderExecutor response-header timeout restores caller cancellation when transport wraps the rejection",
      "QoderExecutor response-header timeout uses registry before global unless provider overrides",
      "QoderExecutor response-header timeout uses registry before global unless provider overrides"
    ],
    "tests/unit/qoder-model-config-consistency-1565.test.js": [
      "qoder sends one consistent model_config (#1565) key and is_reasoning keep the values they had",
      "qoder sends one consistent model_config (#1565) no top-level model selector was invented",
      "qoder sends one consistent model_config (#1565) the config still comes from the server catalog, keyed by the requested model",
      "qoder sends one consistent model_config (#1565) the documented failure mode is the one being fixed",
      "qoder sends one consistent model_config (#1565) the nested copy carries the full server block, not a stub",
      "qoder sends one consistent model_config (#1565) the top-level model_config still carries the server block unchanged"
    ],
    "tests/unit/qoder-models-pat-exchange.test.js": [
      "getQoderModelConfig returns a defensive copy keyed to the requested model",
      "getQoderModelConfig returns null for an unknown key and when the catalog is unavailable",
      "isQoderPat recognizes only pt-prefixed strings",
      "resolveQoderCredentials PAT exchange exchanges a PAT for a job token and resolves the user id",
      "resolveQoderCredentials PAT exchange falls back to the stored userId when userinfo fails, and honours expires_in",
      "resolveQoderCredentials PAT exchange passes non-PAT credentials through unchanged",
      "resolveQoderCredentials PAT exchange throws on a failed exchange and on a missing token",
      "resolveQoderModels catalog fetch caches per credential and invalidates on demand",
      "resolveQoderModels catalog fetch coalesces concurrent misses into one upstream call",
      "resolveQoderModels catalog fetch forwards a live caller abort onto the fetch signal",
      "resolveQoderModels catalog fetch propagates a pre-aborted caller signal as a rejection",
      "resolveQoderModels catalog fetch returns null and warns when the PAT exchange fails inside resolveQoderModels",
      "resolveQoderModels catalog fetch returns null on HTTP failure, bad body shape, and missing identity",
      "resolveQoderModels catalog fetch routes a jt- token to the alternate chat base and maps model fields",
      "resolveQoderModels catalog fetch routes a non-jt token to the primary model list URL"
    ],
    "tests/unit/qoder-models.test.js": [
      "Qoder cmodel catalog fallback creates a usable Cantus config when the live catalog omits cmodel"
    ],
    "tests/unit/qoder-oauth-service-coverage.test.js": [
      "fetchUserInfo falls back to username when name is missing and empties absent fields",
      "fetchUserInfo returns empty strings on a non-ok response",
      "fetchUserInfo returns empty strings when fetch throws",
      "fetchUserInfo sends the bearer token to the userinfo URL and trims the profile fields",
      "generatePkcePair produces a base64url verifier whose sha256 matches the challenge",
      "initiateDeviceFlow builds the verification URL on the login endpoint with PKCE, nonce and machine id",
      "parseExpiry fallbacks falls back to 30 days when both hints are missing",
      "parseExpiry fallbacks honors expiresInSeconds of zero as already expired",
      "parseExpiry fallbacks parses a numeric ms-epoch string without Date.parse year confusion",
      "parseExpiry fallbacks parses an RFC3339 string",
      "pollDeviceToken aborts a stalled request via the fetch timeout",
      "pollDeviceToken GETs the device-token URL with encoded nonce and verifier",
      "pollDeviceToken maps a successful body including expiry and defaults",
      "pollDeviceToken passes refresh_token and user_id through when present",
      "pollDeviceToken returns pending on HTTP 202",
      "pollDeviceToken returns pending on HTTP 404",
      "pollDeviceToken throws on a 200 with invalid JSON",
      "pollDeviceToken throws on a 200 with no token",
      "pollDeviceToken throws when nonce or verifier is missing",
      "pollDeviceToken throws with the HTTP status when the error body is not JSON",
      "pollDeviceToken throws with the upstream message on a JSON error body"
    ],
    "tests/unit/qoder-quota-112-disable.test.js": [
      "Qoder quota exhaustion (403/code 112) disables the connection so the pool stops retrying a dead account",
      "Qoder quota exhaustion (403/code 112) does not disable non-qoder providers that happen to report code 112",
      "Qoder quota exhaustion (403/code 112) leaves plain qoder 403 errors without code 112 on the normal cooldown path",
      "Qoder quota exhaustion (403/code 112) leaves transient billing blocks (queue throttle code 10605) on the normal cooldown path"
    ],
    "tests/unit/qoder-resolved-model-2813.test.js": [
      "qoder resolved model name (#2813) carries the resolved catalogue name on every chunk",
      "qoder resolved model name (#2813) does not overwrite a resolved name the upstream already supplied",
      "qoder resolved model name (#2813) leaves the stream byte-identical when the catalogue has no display name",
      "qoder resolved model name (#2813) passes a non-JSON inner body through untouched rather than dropping it",
      "qoder resolved model name (#2813) reaches the client through the passthrough stream qoder actually runs in"
    ],
    "tests/unit/qoder-transport-coverage.test.js": [
      "annotateResolvedModel adds x_resolved_model once and leaves non-JSON and arrays alone",
      "buildQoderRequestBody normalization derives stable, distinct record ids from content, tools and budget",
      "buildQoderRequestBody normalization handles non-string non-array content and skips null messages",
      "buildQoderRequestBody normalization hoists system text, flattens text arrays, caps max_tokens by request",
      "buildQoderRequestBody normalization preserves openai image blocks and converts claude image sources",
      "buildUrl routes jt- tokens to the alternate base and others to the encoded URL",
      "execute guard paths passes a non-ok upstream response through unchanged",
      "execute guard paths recovers via forced refresh when the cache is cold",
      "execute guard paths returns 400 when model config resolution fails entirely",
      "execute guard paths returns 401 when accessToken is missing",
      "execute guard paths returns 401 when PAT exchange fails",
      "execute guard paths returns 401 when userId is missing",
      "isBillingBlock classifies quota / throttle / pricingUrl signatures and rejects the rest",
      "needsRefresh / refreshCredentials never refreshes",
      "normalizeMessages edges returns empty shape for a non-array input",
      "wrapQoderSSE converts a non-200 envelope after content into an error chunk + [DONE]",
      "wrapQoderSSE emits [DONE] when the upstream closes without a terminal frame",
      "wrapQoderSSE honors an inner [DONE] body and strips embedded newlines from chunks",
      "wrapQoderSSE returns 403 on a billing-block first frame",
      "wrapQoderSSE returns the response untouched when not ok or bodyless",
      "wrapQoderSSE skips unparseable and non-data lines without dying",
      "wrapQoderSSE unwraps envelopes, annotates resolved model, terminates on [DONE]"
    ],
    "tests/unit/qoder.test.js": [
      "buildCosyHeaders Authorization is a Bearer COSY token with payload+sig",
      "buildCosyHeaders auto-generates a machineId when none is supplied",
      "buildCosyHeaders Cosy-Bodyhash is the MD5 of the request body, Cosy-Bodylength is the length",
      "buildCosyHeaders Cosy-Machineid + Cosy-Machinetoken match the supplied machineId",
      "buildCosyHeaders Cosy-Sigpath also handles the encoded chat URL",
      "buildCosyHeaders Cosy-Sigpath strips the leading /algo prefix",
      "buildCosyHeaders Cosy-User reflects the supplied userId verbatim",
      "buildCosyHeaders empty body produces the canonical empty-MD5 hash",
      "buildCosyHeaders produces all required Cosy-* headers",
      "buildCosyHeaders throws when authToken is missing",
      "buildCosyHeaders throws when userId is missing",
      "buildCosyHeaders two calls with identical inputs differ only in fields that include fresh randomness",
      "generatePkcePair produces base64url-safe verifier and challenge of the right length",
      "generatePkcePair returns codeVerifier (not verifier) on the higher-level helper",
      "generatePkcePair verifier and challenge are different (challenge is sha256 of verifier)",
      "initiateDeviceFlow produces a verification URL pointing at qoder.com/device/selectAccounts",
      "initiateDeviceFlow returns nonce and machineId as UUIDs",
      "normalizeMessages converts claude-style base64 image blocks to image_url data URIs",
      "normalizeMessages drops image blocks with no usable url but keeps the text",
      "normalizeMessages flattens multipart text content into a string",
      "normalizeMessages hoists role:system out of messages into systemText",
      "normalizeMessages joins multiple system messages with a blank line",
      "normalizeMessages preserves base64 data: URI images (no OSS upload needed)",
      "normalizeMessages preserves image_url blocks (http URL) instead of dropping them",
      "normalizeMessages returns empty results for empty input",
      "parseExpiry accepts ms-epoch as a JSON number",
      "parseExpiry accepts ms-epoch as a numeric string",
      "parseExpiry accepts RFC3339 strings",
      "parseExpiry does not interpret short numeric strings as a year",
      "parseExpiry falls back to ~30 days when both inputs are missing",
      "parseExpiry falls back to ~30 days when both inputs are unparseable",
      "parseExpiry falls back to expiresInSeconds when expiresAt is missing",
      "parseExpiry treats expires_in: 0 as already expired (now), not 30-day fallback",
      "QODER_MODEL_MAP allows Qoder's latest model key",
      "QODER_MODEL_MAP exposes Qoder's latest model in the static provider catalog",
      "qoderEncodeBody accepts string and Buffer inputs equivalently",
      "qoderEncodeBody handles empty input without throwing",
      "qoderEncodeBody is deterministic for identical input",
      "qoderEncodeBody only emits characters from the custom alphabet",
      "qoderEncodeBody preserves base64 length (input length divisible by 3)",
      "qoderEncodeBody preserves base64 length (input length not divisible by 3)",
      "qoderEncodeBody produces different output for different input",
      "wrapQoderSSE does not forward chunks after [DONE] has been emitted",
      "wrapQoderSSE drains a trailing partial line without a newline in flush()",
      "wrapQoderSSE forwards an OpenAI envelope chunk and emits [DONE] in flush",
      "wrapQoderSSE non-ok responses are returned unchanged (no transform)",
      "wrapQoderSSE strips embedded newlines from inner body before forwarding",
      "wrapQoderSSE upstream error envelope produces an error chunk + [DONE]"
    ],
    "tests/unit/query-aware-compress.test.js": [
      "audit follow-ups: elide-marker awareness, 0.04 default, emoji-safe previews default threshold is 0.04: keeps a weakly-relevant block that 0.08 destroyed",
      "audit follow-ups: elide-marker awareness, 0.04 default, emoji-safe previews guards the block-content path too: a text block with the marker survives inside array content",
      "audit follow-ups: elide-marker awareness, 0.04 default, emoji-safe previews never placeholder-compresses a historical block carrying the rtk elide marker, even at score 0",
      "audit follow-ups: elide-marker awareness, 0.04 default, emoji-safe previews preview slices on code points: an emoji at the cut never splits into a lone surrogate",
      "compressPrefixByQuery caps notes at 8 and sets notesTruncated",
      "compressPrefixByQuery compresses irrelevant historical blocks and keeps relevant ones verbatim",
      "compressPrefixByQuery does not mutate a deep-frozen input and shares untouched parts by reference",
      "compressPrefixByQuery fails open on a short query and returns the input reference",
      "compressPrefixByQuery fails open on empty messages",
      "compressPrefixByQuery fails open when query has fewer than 3 usable terms",
      "compressPrefixByQuery honors previewChars in the placeholder and notes",
      "compressPrefixByQuery keeps a block at exactly the threshold, compresses below it",
      "compressPrefixByQuery leaves a block verbatim when its placeholder would not be shorter than the text",
      "compressPrefixByQuery leaves tool_result content and system messages alone",
      "compressPrefixByQuery never touches recent turns even when they are irrelevant",
      "compressPrefixByQuery respects a custom keepRecentTurns boundary",
      "compressPrefixByQuery returns input reference when nothing falls below the threshold",
      "compressPrefixByQuery treats string content as one block and replaces it with the placeholder string"
    ],
    "tests/unit/quota-429-cross-account-replay.test.js": [
      "quota 429 with upstream x-should-retry: false does not widen a quota failure on a status other than 429",
      "quota 429 with upstream x-should-retry: false rotates to other accounts instead of stopping at the first",
      "quota 429 with upstream x-should-retry: false still refuses to replay when the outcome is genuinely uncertain"
    ],
    "tests/unit/quota-account-controls-integration.test.js": [
      "cancels queued work after deleteProviderConnection succeeds",
      "cancels queued work after deleteProviderConnectionsByProvider succeeds",
      "commits {\"authType\":\"apikey\"} then invalidates the matching quota claim",
      "commits {\"isActive\":false} then invalidates the matching quota claim",
      "does not rescan scheduling on unrelated edits or refused writes",
      "retains the confirmed account change and revokes active work when inventory cannot be read"
    ],
    "tests/unit/quota-auto-ping.test.js": [
      "quota auto-ping does not ping Codex just because reported usage is zero",
      "quota auto-ping does not ping Codex on the first resetAt observation",
      "quota auto-ping does not ping Codex when monthly quota is exhausted",
      "quota auto-ping does not ping Codex when resetAt is stable",
      "quota auto-ping does not ping Codex when session quota is exhausted",
      "quota auto-ping does not ping Codex when setting is absent",
      "quota auto-ping does not ping Codex when weekly quota is exhausted",
      "quota auto-ping does not ping same Codex reset twice when seconds drift",
      "quota auto-ping does not repeat Codex ping inside the minimum ping interval",
      "quota auto-ping keeps Claude session quota key behavior",
      "quota auto-ping sends Codex ping when session resetAt slides",
      "quota auto-ping sends one tiny gpt-5.5 Codex request through the executor",
      "quota auto-ping skips non-OAuth Codex connections",
      "quota auto-ping starts the scheduler only when an account opts in",
      "quota auto-ping stops the scheduler when the last account opts out"
    ],
    "tests/unit/quota-autoping-sender-branches.test.js": [
      "antigravity sender per-family outcomes counts a 400 family as not warmed and reports a partial warm as success",
      "claude sender walks the whole candidate list records a failure after every candidate model is refused",
      "codex sender stream handling cancels the body and fails the warm on a non-ok response, even when cancel throws",
      "codex sender stream handling drains a text-less ok response through its body reader",
      "generic sender refusals fails when no model is resolvable at all",
      "generic sender refusals fails when the provider has no executor",
      "generic sender refusals treats a 5xx answer as a failed warm",
      "quota bookkeeping around the warm contains a settings-read failure inside the tick",
      "quota bookkeeping around the warm logs, and survives, a failed early-lift lock clear",
      "quota bookkeeping around the warm marks the connection rate-limited when the governing quota is exhausted (string numbers)",
      "quota bookkeeping around the warm records a failure when the credential refresh throws",
      "quota bookkeeping around the warm records a failure when the post-warm state write fails, so the spend is not repeated",
      "quota bookkeeping around the warm reports a started clock and keeps the in-memory mirror when the durable write fails",
      "quota bookkeeping around the warm skips a connection on cooldown without touching the proxy resolver",
      "scheduler lifecycle starts once, ticks on the interval, and stops cleanly"
    ],
    "tests/unit/quota-check-queue.test.js": [
      "durable quota check queue allows only one adapter to claim a due job and hides its ownership secret from listings",
      "durable quota check queue applies scope and status filters before totals and pagination",
      "durable quota check queue cancels a running job for account-inactive and re-enables the same account cleanly",
      "durable quota check queue cancels a running job for auth-unsupported and re-enables the same account cleanly",
      "durable quota check queue cancels a running job for setting-disabled and re-enables the same account cleanly",
      "durable quota check queue cancels every outstanding job without allowing existing owners to finalize them",
      "durable quota check queue does not manufacture measurements when a target has no prior observation",
      "durable quota check queue persists each cancellation transition and refuses cancellation by a stale claimant",
      "durable quota check queue persists the schedule, target links, and ownership expiry across adapter reopen",
      "durable quota check queue preserves the exact next-check horizon when inventory is reconciled again",
      "durable quota check queue refuses a reset timestamp that contradicts its linked prior observation",
      "durable quota check queue refuses a target linked to a mismatched observation {\"connectionId\":\"other-account\"}",
      "durable quota check queue refuses a target linked to a mismatched observation {\"provider\":\"other-provider\"}",
      "durable quota check queue refuses a target linked to a mismatched observation {\"scope\":\"monthly\"}",
      "durable quota check queue rejects completion at lease expiry even before another worker claims the job",
      "durable quota check queue renews a lease, reclaims its expiry, and refuses all writes from the stale owner",
      "durable quota check queue retains an exact prior observation and exposes its authoritative resource and unit"
    ],
    "tests/unit/quota-evidence.test.js": [
      "displays a measured zero and rejects invalid percentage quantities",
      "keeps both window constraints visible and selects the exact scope",
      "keeps unknown units unknown instead of deriving percentage from stored quantity",
      "orders explicitly reported durations, leaves input intact and does not infer monthly duration"
    ],
    "tests/unit/quota-exhausted-ratelimit-1125.test.js": [
      "exhausted quota is recorded, not just detected (#1125) a write failure never breaks the tick",
      "exhausted quota is recorded, not just detected (#1125) does not rewrite a mark it already made",
      "exhausted quota is recorded, not just detected (#1125) leaves an account with quota left untouched",
      "exhausted quota is recorded, not just detected (#1125) marks the account rate limited until the reported reset",
      "exhausted quota is recorded, not just detected (#1125) what it writes is what account fallback filters on"
    ],
    "tests/unit/quota-fleet-api.test.js": [
      "fleet scenario administrative API captures an empty fleet and replays it through the real comparison with no-store responses",
      "fleet scenario administrative API checks authorization before reading a capture and does not return private capture errors",
      "fleet scenario administrative API refuses unknown fields, malformed JSON, oversized bodies and future history ranges"
    ],
    "tests/unit/quota-fleet-scenario.test.js": [
      "observed replenishment evidence censors unknown, future, duplicate-time, inferred and changing-denominator intervals",
      "observed replenishment evidence exposes partially elapsed rolling releases as censored intervals and never confirms a deadline",
      "observed replenishment evidence separates net depletion and replenishment from unidentifiable gross consumption",
      "retained fleet counterfactuals compares scaled demand on identical capture without adding overlapping product windows",
      "retained fleet counterfactuals compares supported preferences for new sessions and preserves existing session continuity",
      "retained fleet counterfactuals excludes unavailable accounts through the gateway and refuses disabled model eligibility",
      "retained fleet counterfactuals keeps stale, incomplete and rolling histories unavailable for point forecasts",
      "retained fleet counterfactuals rejects tampering, mismatched history population, unbounded controls and unknown exclusions"
    ],
    "tests/unit/quota-history-acquisition.test.js": [
      "usage acquisition provenance dashboard and routing reads retain the same sample once, and a later provider response adds one",
      "usage acquisition provenance history failure preserves successful usage and never retries the provider",
      "usage acquisition provenance records the successful auth retry and persists its snapshot instead of the initial rejection",
      "usage acquisition provenance the real dispatcher preserves cache identity and distinguishes fresh unchanged responses"
    ],
    "tests/unit/quota-history-autoping.test.js": [
      "scheduler to persisted quota history distinguishes actual HTTP response and scheduler bookkeeping from later running-clock confirmation",
      "scheduler to persisted quota history does not label a cached pre-warm observation as confirmation",
      "scheduler to persisted quota history records actual reads, a not-before reset check and skips held ticks without manufacturing events",
      "scheduler to persisted quota history records refresh exceptions without persisting provider error text",
      "scheduler to persisted quota history records usage exceptions without persisting provider error text",
      "scheduler to persisted quota history records warm exceptions without persisting provider error text",
      "scheduler to persisted quota history retains a rejected HTTP response without recording an accepted warm",
      "scheduler to persisted quota history retention failure does not add attempts or prevent existing warm bookkeeping"
    ],
    "tests/unit/quota-history.test.js": [
      "history queries and operator boundary advertises available history only after actual observations exist",
      "history queries and operator boundary bounds optional history waiting and cancels abandoned reads",
      "history queries and operator boundary filters before counting and paging, with stable ordering and explicit horizon",
      "history queries and operator boundary joins warming outcomes to the exact observed resource and filters before pagination",
      "history queries and operator boundary keeps current capacity readable when historical analytics are unavailable",
      "history queries and operator boundary reads history off-thread and reports its committed snapshot",
      "history queries and operator boundary rejects ambiguous dimensions and worker SQL or path injection",
      "history queries and operator boundary rejects anonymous and inference credentials before the repository query",
      "history queries and operator boundary rejects invalid query {\"kind\":\"quotaWindows\"}",
      "history queries and operator boundary rejects invalid query {\"limit\":\"2\"}",
      "history queries and operator boundary rejects invalid query {\"page\":\"-1\"}",
      "history queries and operator boundary rejects invalid query {\"page\":\"9007199254740992\"}",
      "history queries and operator boundary rejects invalid query {\"pageSize\":\"0\"}",
      "history queries and operator boundary rejects invalid query {\"pageSize\":\"201\"}",
      "history queries and operator boundary rejects invalid query {\"since\":\"\"}",
      "history queries and operator boundary rejects invalid query {\"since\":\"2026-09-07T00:00:00.000Z\",\"until\":\"2026-09-01T00:00:00.000Z\"}",
      "history queries and operator boundary rejects invalid query {\"start\":\"2026-09-01T00:00:00.000Z\",\"since\":\"2026-09-01T00:00:00.000Z\"}",
      "history queries and operator boundary rejects invalid query {\"timeField\":\"1=1\"}",
      "history queries and operator boundary retains scheduled and actual check events separately without raw errors",
      "history queries and operator boundary returns authenticated persisted data and sanitized failure without refreshing a provider",
      "history queries and operator boundary uses half-open UTC ranges with documented conflict-free time aliases",
      "retained quota observations deduplicates reused snapshots, retains changed payloads at the same time and new unchanged observations",
      "retained quota observations does not turn unknown time or invalid numbers into an epoch or measured zero",
      "retained quota observations keeps cached reader receipts stable and fresh unchanged reader receipts distinct",
      "retained quota observations makes samples and check outcomes visible to a separate persisted reader",
      "retained quota observations persists roundtrip without labeling a synthetic 100 scale as absolute units",
      "retained quota observations preserves known zero balances, explicit units and independent resource scopes",
      "retained quota observations rolls back a failed multi-window insert and leaves existing samples intact",
      "retained quota observations updates current freshness for unchanged values but does not append history during selection"
    ],
    "tests/unit/quota-lifecycle.test.js": [
      "aborts an active warming stream and preserves uncertainty without another request",
      "account mutation notification cancels pending metadata without opening a new check",
      "bounds each tick to eight metadata checks without overlapping upstream work",
      "cancels an opted-out check even when every quota is already running and no warming is planned",
      "cannot warm or complete after a renewal failure",
      "enforces a bounded check deadline while a collector does not answer",
      "holds the metadata permit after cancellation until the shared collector drains",
      "persists account-inactive seen between quota read and dispatch",
      "persists account-missing seen between quota read and dispatch",
      "persists auth-unsupported seen between quota read and dispatch",
      "persists setting-disabled seen between quota read and dispatch",
      "rechecks authority after the durable dispatch guard was saved",
      "reconciles a setting opt-out even when no scheduler interval is installed",
      "records the exact persisted schedule horizon in the history and in the next started check",
      "stops a manual in-progress tick while metadata is pending and never dispatches its late result"
    ],
    "tests/unit/quota-outcome-migration.test.js": [
      "quota outcome migration with better-sqlite3 backs up the original schema, preserves unknown history, and persists exact new outcomes across reopen",
      "quota outcome migration with better-sqlite3 stops before changing quota history when the required backup cannot be persisted",
      "quota outcome migration with sql.js backs up the original schema, preserves unknown history, and persists exact new outcomes across reopen",
      "quota outcome migration with sql.js stops before changing quota history when the required backup cannot be persisted"
    ],
    "tests/unit/quota-pause-expired-evidence.test.js": [
      "expired and missing quota observations cannot pause selection can still pause on an unexpired sibling after the expired window releases",
      "expired and missing quota observations cannot pause selection does not derive a full percentage balance from missing used values",
      "expired and missing quota observations cannot pause selection does not turn -1 into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn \"   \" into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn \"\" into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn {} into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn 101 into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn false into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn null into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn null into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn null into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn true into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn undefined into usable percentage evidence",
      "expired and missing quota observations cannot pause selection does not turn undefined into usable percentage evidence",
      "expired and missing quota observations cannot pause selection releases an expired zero balance without changing the historical evidence",
      "expired and missing quota observations cannot pause selection retains an actual zero and an adapter-compatible numeric string before reset",
      "expired and missing quota observations cannot pause selection the actual stale-while-refresh quota gate does not bench an expired account",
      "expired and missing quota observations cannot pause selection uses the projection's injected observation time for expiry",
      "expired and missing quota observations cannot pause selection uses the same inclusive reset boundary in the routing helper and display"
    ],
    "tests/unit/quota-pause.test.js": [
      "deriveQuotaSnapshot (raw usage → per-window snapshot) captures each window key with its remaining % (min not collapsed)",
      "deriveQuotaSnapshot (raw usage → per-window snapshot) captures per-window resetAt",
      "deriveQuotaSnapshot (raw usage → per-window snapshot) falls back to used/total when remainingPercentage is absent (codex)",
      "deriveQuotaSnapshot (raw usage → per-window snapshot) marks unlimited windows",
      "deriveQuotaSnapshot (raw usage → per-window snapshot) returns null when there is no usable quota data",
      "evaluateQuota (routing engine) does not pause when only an unconfigured window is low",
      "evaluateQuota (routing engine) fail-open: live fetch error never pauses",
      "evaluateQuota (routing engine) fetches quota evidence even when no window thresholds are set, and never pauses",
      "evaluateQuota (routing engine) live-fetches on a cache miss, then pauses and persists the per-window snapshot",
      "evaluateQuota (routing engine) uses a fresh persisted snapshot without a live fetch",
      "evaluateQuota (routing engine) uses the in-memory cache on a subsequent call (no second fetch)",
      "getQuotaPauseInfo reports disabled state",
      "getQuotaPauseInfo reports per-window config + paused state",
      "isQuotaEligible apikey only eligible when provider supports usage",
      "isQuotaEligible cookie is never eligible",
      "isQuotaEligible oauth is always eligible",
      "isQuotaPaused / getPausedWindow (per-window) auto-recovers once the offending window rebounds",
      "isQuotaPaused / getPausedWindow (per-window) disabled when no thresholds set",
      "isQuotaPaused / getPausedWindow (per-window) does not pause for a window with no threshold even if low",
      "isQuotaPaused / getPausedWindow (per-window) getPausedWindow returns the triggering window key",
      "isQuotaPaused / getPausedWindow (per-window) never pauses an unlimited window even if threshold set",
      "isQuotaPaused / getPausedWindow (per-window) never pauses ineligible providers (fail-open)",
      "isQuotaPaused / getPausedWindow (per-window) pauses when a configured window is below its threshold (boundary inclusive)"
    ],
    "tests/unit/quota-product-groups.test.js": [
      "quota product display groups does not infer a product from unfamiliar provider keys or future periods",
      "quota product display groups does not mutate frozen input and keeps order stable as readings and resets change",
      "quota product display groups groups known general account windows for glm",
      "quota product display groups groups known general account windows for glm-cn",
      "quota product display groups groups known general account windows for kimi",
      "quota product display groups groups known general account windows for ollama",
      "quota product display groups groups known general account windows for opencode-go",
      "quota product display groups groups only exact adapter duration suffixes for minimax",
      "quota product display groups groups only exact adapter duration suffixes for minimax-cn",
      "quota product display groups groups the requested synthetic three-period General and Spark example as two products",
      "quota product display groups keeps actual Codex review and Spark families separate, including primary collisions",
      "quota product display groups keeps Claude model scopes apart from the general account quota",
      "quota product display groups preserves unknown, unlimited, zero, raw precision, thresholds and timestamps",
      "quota product display groups renders six adapter-supported windows as two products without dropping collisions"
    ],
    "tests/unit/quota-schedule-api.test.js": [
      "passes exact account and pagination to the durable queue without triggering any execution",
      "protects schedule evidence before database access",
      "refuses ambiguous query page=",
      "refuses ambiguous query page=0",
      "refuses ambiguous query page=1.2",
      "refuses ambiguous query page=1&page=2",
      "refuses ambiguous query page=1e2",
      "refuses ambiguous query pageSize=-1",
      "refuses ambiguous query pageSize=0x10",
      "refuses ambiguous query unexpected=1",
      "reports database failure as unavailable"
    ],
    "tests/unit/quota-stale-while-refresh-pf2.test.js": [
      "quota evidence stale-while-revalidate (P-F2) dedupes a cold-start (no snapshot at all) fetch across concurrent misses",
      "quota evidence stale-while-revalidate (P-F2) dedupes concurrent TTL-expired misses to one in-flight refresh",
      "quota evidence stale-while-revalidate (P-F2) fail-open: a rejecting background refresh never reaches the caller",
      "quota evidence stale-while-revalidate (P-F2) fail-open: a rejecting cold-start fetch still never pauses",
      "quota evidence stale-while-revalidate (P-F2) serves a stale snapshot synchronously and refreshes in the background"
    ],
    "tests/unit/quota-tracker-access-token-1322.test.js": [
      "a pasted-token account is quota-eligible (#1322) access_token behaves exactly like the OAuth grant it is",
      "a pasted-token account is quota-eligible (#1322) the rules that were already right are unchanged",
      "a pasted-token account is quota-eligible (#1322) the shared helper accepts access_token",
      "one owner for the rule (#1322) the listing route reads the shared helper",
      "one owner for the rule (#1322) the usage route reads the shared helper instead of its own copy",
      "the Quota Tracker counts that account (#1322) does not report an empty install when the only account is a pasted token",
      "the Quota Tracker counts that account (#1322) lists a pasted token alongside an OAuth account of the same provider",
      "the Quota Tracker counts that account (#1322) still excludes a provider that reports no usage at all"
    ],
    "tests/unit/quota-trend.test.js": [
      "quota trend evidence breaks at missing measurements, unknown time and a large observation gap",
      "quota trend evidence does not equate percentage points with tokens, money or a synthetic absolute denominator",
      "quota trend evidence does not project past a known reset as evidence of availability or replenishment",
      "quota trend evidence keeps an unrepresentable distant horizon unavailable instead of showing a dated prediction",
      "quota trend evidence keeps zero-rate intervals and represents an unbounded slow horizon honestly",
      "quota trend evidence preserves contradictory observations at one timestamp and refuses a forecast",
      "quota trend evidence rejects inconsistent absolute balances and never converts null to zero",
      "quota trend evidence rejects stale, future and rolling-window extrapolation without inventing a current balance",
      "quota trend evidence requires five distinct samples and 15 minutes of observation",
      "quota trend evidence separates observed replenishment from proof of a reset and starts a fresh segment",
      "quota trend evidence starts a new segment when limit changes",
      "quota trend evidence starts a new segment when resetAt changes",
      "quota trend evidence uses all distinct observed intervals and exposes a dated balance and rate units",
      "quota trend evidence validates scenario controls and cannot infer redistribution onto other accounts"
    ],
    "tests/unit/quota-warm-outcomes.test.js": [
      "truthful quota warming outcomes does not dispatch Antigravity families whose quota clock is already running",
      "truthful quota warming outcomes does not dispatch if the durable pre-dispatch guard cannot be saved",
      "truthful quota warming outcomes does not invent resource identity for an absent window",
      "truthful quota warming outcomes does not mark HTTP 400 as an accepted warming request",
      "truthful quota warming outcomes does not mark HTTP 401 as an accepted warming request",
      "truthful quota warming outcomes does not mark HTTP 403 as an accepted warming request",
      "truthful quota warming outcomes does not mark HTTP 404 as an accepted warming request",
      "truthful quota warming outcomes does not mark HTTP 429 as an accepted warming request",
      "truthful quota warming outcomes keeps accepted targets protected if the final compatibility-state write fails",
      "truthful quota warming outcomes keeps partial Antigravity outcomes on their actual target and stops after an uncertain response",
      "truthful quota warming outcomes persists the dispatch guard before entering the upstream executor and retains interrupted streams",
      "truthful quota warming outcomes retains HTTP 500 uncertainty across scheduler restart without another dispatch",
      "truthful quota warming outcomes retains HTTP 503 uncertainty across scheduler restart without another dispatch",
      "truthful quota warming outcomes retains the exact sample, resource and unit for an accepted target"
    ],
    "tests/unit/quota-warming-durability.test.js": [
      "default quota scheduler persistence commits its guard before HTTP 200 dispatch and cannot replay after database reopen",
      "default quota scheduler persistence commits its guard before HTTP 503 dispatch and cannot replay after database reopen"
    ],
    "tests/unit/quota-warming-settings.test.js": [
      "merges per-account opt-ins atomically without replacing siblings or other settings",
      "reads back exact supported opt-in and disable through the real route without a provider call",
      "refuses invalid warming shapes and unsupported accounts before persisting"
    ],
    "tests/unit/quota-window-bridge-null-evidence.test.js": [
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage \"   \"",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage \"\"",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage {}",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage false",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage null",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage null",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage null",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage null",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage true",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage undefined",
      "quota source absence cannot become measured capacity does not manufacture full measured capacity from usage undefined",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage \"   \"",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage \"\"",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage {}",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage false",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage null",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage null",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage null",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage null",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage true",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage undefined",
      "quota source absence cannot become measured capacity does not manufacture zero from missing percentage undefined",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining \"   \" and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining \"\" and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining {} and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining false and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining null and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining null and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining null and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining null and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining true and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining undefined and uses real usage",
      "quota source absence cannot become measured capacity ignores missing/invalid remaining undefined and uses real usage",
      "quota source absence cannot become measured capacity keeps batch raw-key alignment and caller state intact",
      "quota source absence cannot become measured capacity keeps ranker admission valid for a measured 90 instead of falsely exhausting the account",
      "quota source absence cannot become measured capacity never promotes a boolean or blank total into a measured scale",
      "quota source absence cannot become measured capacity preserves genuine zero measurements and finite numeric strings accepted by usage adapters",
      "quota source absence cannot become measured capacity preserves scope, observed time, reset deadline and the ranker's horizon"
    ],
    "tests/unit/quota-window-lock-headroom.test.js": [
      "429 retry-after naming a window reset caps a 143h weekly-reset retry-after the same way",
      "429 retry-after naming a window reset caps the model lock at a minute when our own windows still show headroom",
      "429 retry-after naming a window reset honours the provider reset when a general window is genuinely at zero",
      "429 retry-after naming a window reset honours the provider reset when no window evidence is readable",
      "429 retry-after naming a window reset honours the provider reset when the window read throws",
      "429 retry-after naming a window reset leaves a non-429 lock alone even with headroom on record",
      "429 retry-after naming a window reset never benches a headroom account past the six-hour provider ceiling",
      "quota and temporary failure classification honors a verified weekly depletion beyond the generic six-hour ceiling",
      "quota and temporary failure classification marks 503 as temporary without guessing that generation is safe to replay",
      "quota and temporary failure classification rotates on a temporary rate limit instead of making the caller wait"
    ],
    "tests/unit/quota-window-parity.test.js": [
      "quota window presentation parity across providers gives every 5h window the same label and the same order",
      "quota window presentation parity across providers gives every 7d window the same label and the same order",
      "quota window presentation parity across providers keeps provider product grouping intact while the windows read alike",
      "quota window presentation parity across providers never invents a duration it cannot source",
      "quota window presentation parity across providers sorts the 5h window before the 7d one for every provider"
    ],
    "tests/unit/quota-windows-skip-write-pf3.test.js": [
      "putWindows compare-and-skip (P-F3) writes once, then skips the write entirely when nothing changed",
      "putWindows compare-and-skip (P-F3) writes when a previously empty table receives windows",
      "putWindows compare-and-skip (P-F3) writes when a value actually changes",
      "putWindows compare-and-skip (P-F3) writes when the window set shape changes (a scope disappears)"
    ],
    "tests/unit/quota-workbench-ui.test.js": [
      "Quota workbench interaction does not construct a forecast when the population is incomplete or unavailable",
      "Quota workbench interaction filters contributing rows with observation zoom while preserving an out-of-zoom selection",
      "Quota workbench interaction links a point to its contributing table page and keeps it selected during a refresh",
      "Quota workbench interaction makes scenario exclusion local and does not perform a mutation or upstream request",
      "Quota workbench interaction restores the exact same-scope historical identity and keeps missing selections unavailable",
      "Quota workbench interaction shows unknown balance/times and excludes them from plotted measurements",
      "Quota workbench interaction uses a shared account/provider/time scope and explicitly discloses unavailable model attribution"
    ],
    "tests/unit/quota-workbench.test.js": [
      "quota workbench query boundary filters before population counting, preserves exclusive time and separates units/resources",
      "quota workbench query boundary keeps all-retained scope explicit and rejects missing, duplicate or unsupported dimensions",
      "quota workbench query boundary keeps the historical scan in the worker and supports committed native and persisted sql.js snapshots",
      "quota workbench query boundary passes the caller signal to the bounded analytics system",
      "quota workbench query boundary reads beyond a first history page before computing the trend",
      "quota workbench query boundary refuses incomplete populations without scanning or forecasting the first 5000",
      "quota workbench query boundary requires operator authorization and returns no-store evidence or sanitized busy failures",
      "quota workbench query boundary uses reported percentage evidence when the named quota unit has no absolute balance"
    ],
    "tests/unit/rate-limit-backoff-config.test.js": [
      "configurable 429 account backoff (#3343) falls back per invalid knob and rejects a contradictory schedule",
      "configurable 429 account backoff (#3343) keeps the historical schedule when no environment configuration is supplied",
      "configurable 429 account backoff (#3343) lets each valid knob override its default without requiring the other knobs",
      "configurable 429 account backoff (#3343) uses a configured schedule for both 429 status and rate-limit text matches"
    ],
    "tests/unit/rate-limit-key-eviction-1245.test.js": [
      "chat admission queue (authenticated keys) drops idle keys, under the same #1245 memory bound as the window",
      "chat admission queue (authenticated keys) evicts a waiter that outlives the wait budget",
      "chat admission queue (authenticated keys) hands a freed slot to the head of the queue, in FIFO order",
      "chat admission queue (authenticated keys) keeps a key that still has a request in flight",
      "chat admission queue (authenticated keys) queues past the concurrency ceiling instead of refusing",
      "chat admission queue (authenticated keys) refuses ONLY past queue capacity, with a hint inside the wait budget",
      "chat rate limiter key eviction (#1245) drops keys whose window has gone quiet",
      "chat rate limiter key eviction (#1245) keeps a key that is still inside its window",
      "chat rate limiter key eviction (#1245) still limits an UNAUTHENTICATED caller over the per-window ceiling"
    ],
    "tests/unit/rate-limit-not-reclassified-2556.test.js": [
      "a 429 is decided by its status, not by its body text (#2556) falls back even when the body reads \"{\"error\":{\"type\":\"invalid_request_\"",
      "a 429 is decided by its status, not by its body text (#2556) falls back even when the body reads \"Bad request: too many requests\"",
      "a 429 is decided by its status, not by its body text (#2556) falls back even when the body reads \"maximum context length reached the\"",
      "a 429 is decided by its status, not by its body text (#2556) falls back even when the body reads \"unknown model quota exhausted, ret\"",
      "a 429 is decided by its status, not by its body text (#2556) falls back on a plain rate limit",
      "a 429 is decided by its status, not by its body text (#2556) leaves a genuine client error on another status alone",
      "a 429 is decided by its status, not by its body text (#2556) raises the backoff level, so a repeat waits longer",
      "a 429 is decided by its status, not by its body text (#2556) still lets a shared-path failure rotate without a lock"
    ],
    "tests/unit/ratelimit-reset-window-3203.test.js": [
      "per-key rate-limit window from reset headers (#3203) benches the account for exactly the window the provider named",
      "per-key rate-limit window from reset headers (#3203) bounds a window the provider reports as absurdly long",
      "per-key rate-limit window from reset headers (#3203) ignores the headers on a status that is not a rate limit",
      "per-key rate-limit window from reset headers (#3203) keeps Retry-After ahead of the reset headers",
      "per-key rate-limit window from reset headers (#3203) prefers the request window over the shared reset header",
      "per-key rate-limit window from reset headers (#3203) reads an absolute RFC3339 reset",
      "per-key rate-limit window from reset headers (#3203) reads the request window from a compound duration",
      "per-key rate-limit window from reset headers (#3203) reads the request window from a generic reset header",
      "per-key rate-limit window from reset headers (#3203) reads the request window from a seconds duration",
      "per-key rate-limit window from reset headers (#3203) reads the request window from a sub-second duration",
      "per-key rate-limit window from reset headers (#3203) rejects a bare epoch seconds reset value",
      "per-key rate-limit window from reset headers (#3203) rejects a empty reset value",
      "per-key rate-limit window from reset headers (#3203) rejects a past timestamp reset value",
      "per-key rate-limit window from reset headers (#3203) rejects a prose reset value",
      "per-key rate-limit window from reset headers (#3203) rejects a spaced duration reset value",
      "per-key rate-limit window from reset headers (#3203) rejects a zero duration reset value"
    ],
    "tests/unit/reasoning-content-injector.test.js": [
      "Xiaomi reasoning-content echo adds a placeholder for a missing assistant reasoning trace through xiaomi-mimo",
      "Xiaomi reasoning-content echo adds a placeholder for a missing assistant reasoning trace through xiaomi-tokenplan",
      "Xiaomi reasoning-content echo does not add OpenAI reasoning_content to Xiaomi's Claude transport",
      "Xiaomi reasoning-content echo preserves Xiaomi reasoning_content returned by the model"
    ],
    "tests/unit/reasoning-disable-2440.test.js": [
      "per-model reasoning disable (#2440) advertises the level only where the model can actually disable",
      "per-model reasoning disable (#2440) clamps to minimal instead of disabling when the model cannot turn it off",
      "per-model reasoning disable (#2440) lets MODEL_CAPABILITY_OVERRIDES restore the disable for one model",
      "per-model reasoning disable (#2440) parses the per-model (none)/(off) suffix into a disable intent",
      "per-model reasoning disable (#2440) turns that suffix into the provider's own disable field",
      "per-model reasoning disable (#2440) wins over a thinking intent the client put in the body"
    ],
    "tests/unit/reasoning-history-pivot-2400.test.js": [
      "assistant reasoning survives the request pivot (#2400) a claude turn that is only thinking is not dropped",
      "assistant reasoning survives the request pivot (#2400) claude thinking block becomes openai reasoning_content",
      "assistant reasoning survives the request pivot (#2400) does not duplicate a thinking block the caller already sent",
      "assistant reasoning survives the request pivot (#2400) gemini thought parts do not leak into visible content",
      "assistant reasoning survives the request pivot (#2400) keeps reasoning on an ollama assistant turn that also calls a tool",
      "assistant reasoning survives the request pivot (#2400) openai reasoning_content becomes a leading claude thinking block",
      "assistant reasoning survives the request pivot (#2400) openai reasoning_content becomes the ollama thinking field"
    ],
    "tests/unit/reasoningContentInjector.test.js": [
      "injectReasoningContent — DeepSeek thinking round-trip applies provider-level rule for provider 'deepseek' (scope all)",
      "injectReasoningContent — DeepSeek thinking round-trip does not touch non-deepseek providers/models",
      "injectReasoningContent — DeepSeek thinking round-trip injects reasoning_content on a deepseek- assistant message that lacks it",
      "injectReasoningContent — DeepSeek thinking round-trip maps deepseek-v4-pro-none alias to disabled thinking and strips reasoning_effort",
      "injectReasoningContent — DeepSeek thinking round-trip matches deepseek model id case-insensitively for custom providers (#1543)",
      "injectReasoningContent — DeepSeek thinking round-trip preserves an existing reasoning_content instead of overwriting it",
      "injectReasoningContent — MiniMax thinking round-trip applies provider-level rule for provider 'minimax-cn' (scope all)",
      "injectReasoningContent — MiniMax thinking round-trip DefaultExecutor transformRequest runs the injector for minimax",
      "injectReasoningContent — MiniMax thinking round-trip injects reasoning_content on a minimax assistant message that lacks it",
      "injectReasoningContent — MiniMax thinking round-trip injects reasoning_content on minimax assistant message with tool_calls but no reasoning_content",
      "injectReasoningContent — MiniMax thinking round-trip preserves an existing reasoning_content on minimax instead of overwriting",
      "OpenCodeExecutor — issue #1543 regression runs the injector so deepseek-v4-flash-free round-trips reasoning_content"
    ],
    "tests/unit/reauth-connection-1851.test.js": [
      "OAuth completion routed at an existing connection carries the same target through the device-code poll",
      "OAuth completion routed at an existing connection still adds an account when no target is named",
      "OAuth completion routed at an existing connection surfaces a refused re-auth as a status rather than a new row",
      "OAuth completion routed at an existing connection updates the named row instead of adding a second account",
      "POST /api/providers/[id]/reauth applies an exported account file to the existing connection",
      "POST /api/providers/[id]/reauth refuses a codex file dropped on another provider's account",
      "POST /api/providers/[id]/reauth refuses a malformed file before it can blank a working credential",
      "reauthorizeProviderConnection clears the failure state recorded against the credential that just died",
      "reauthorizeProviderConnection leaves the fallback order of the whole provider untouched",
      "reauthorizeProviderConnection never lets a sign-in payload rewrite the proxy binding",
      "reauthorizeProviderConnection refuses a credential from a different provider",
      "reauthorizeProviderConnection refuses a payload with no usable credential and keeps the working one",
      "reauthorizeProviderConnection refuses a visibly different account unless forced",
      "reauthorizeProviderConnection replaces the credential and keeps everything else",
      "reauthorizeProviderConnection reports a missing target instead of creating one",
      "reauthorizeProviderConnection still clears failure state on the codex re-login that merges by account id"
    ],
    "tests/unit/recent-requests-model.test.js": [
      "recent requests model display carries resolved model and requested model separately, with no arrow join",
      "recent requests model display dedupes prefixed and bare forms of the same request into one row",
      "recent requests model display drops zero-token rows (pending placeholders) from the listing",
      "recent requests model display parses requestedModel from the DB meta JSON string"
    ],
    "tests/unit/reconciliation/admin-authz.test.js": [
      "optimistic concurrency leaves state unchanged on conflict has nothing to roll back to on a fresh instance, and says so without writing",
      "optimistic concurrency leaves state unchanged on conflict is 404 for a drain on a connection that does not exist, and writes nothing",
      "optimistic concurrency leaves state unchanged on conflict is 404 for an unknown release, and writes nothing",
      "optimistic concurrency leaves state unchanged on conflict rejects a malformed mutation body before reading any state",
      "optimistic concurrency leaves state unchanged on conflict returns 412 on a stale activation ifMatch and writes nothing",
      "optimistic concurrency leaves state unchanged on conflict returns 412 with currentVersion and writes nothing when ifMatch is stale",
      "state is byte-identical after a rejected call changes state only once a loopback operator is accepted",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: anonymous activation",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: anonymous drain off",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: anonymous drain on",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: anonymous recheck",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: anonymous rollback",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: inference-key activation",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: inference-key drain off",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: inference-key drain on",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: inference-key recheck",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: inference-key rollback",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: remote operator drain on",
      "state is byte-identical after a rejected call leaves drain, activation and rollback state untouched: remote operator rollback",
      "state is byte-identical after a rejected call leaves state untouched even when the rejected body would have been malformed",
      "state is byte-identical after a rejected call reads no quota state on a rejected quota call",
      "state is byte-identical after a rejected call spends no generation on a rejected recheck",
      "the handler gate on /api/admin refuses an operator who is not a loopback peer on every mutation",
      "the handler gate on /api/admin refuses each state-changing handler called directly without a credential",
      "the handler gate on /api/admin refuses each state-changing handler holding only an inference key",
      "the handler gate on /api/admin refuses operator-class reads without an operator credential",
      "the handler gate on /api/admin serves the inference-class reads to an inference key",
      "the middleware gate on /api/admin admits a dashboard session for an operator-class read",
      "the middleware gate on /api/admin admits a loopback operator on a mutation",
      "the middleware gate on /api/admin admits an inference key on the two inference-class reads",
      "the middleware gate on /api/admin does not accept requireLogin=false as an operator credential",
      "the middleware gate on /api/admin does not let /api/admin/health/detail ride the health prefix",
      "the middleware gate on /api/admin refuses an anonymous operator-class read with 401",
      "the middleware gate on /api/admin refuses an inference key on a state-changing endpoint with 403",
      "the middleware gate on /api/admin refuses an inference key on an operator-class read with 403",
      "the middleware gate on /api/admin refuses an invalid, revoked-looking key exactly as it refuses no key at all",
      "the middleware gate on /api/admin refuses an operator who is not a loopback peer on a mutation",
      "the middleware gate on /api/admin still refuses an anonymous caller on the inference-class reads",
      "the middleware gate on /api/admin treats a trailing slash as the same path"
    ],
    "tests/unit/reconciliation/admission.test.js": [
      "accountLease: reservation is the slot, and release is idempotent a double release cannot free ANOTHER request's slot",
      "accountLease: reservation is the slot, and release is idempotent admits exactly `capacity` reservations and refuses the next",
      "accountLease: reservation is the slot, and release is idempotent counts per connection, so one full account never blocks another",
      "accountLease: reservation is the slot, and release is idempotent frees a slot on release and re-admits, so a queue can drain",
      "accountLease: reservation is the slot, and release is idempotent ignores a fabricated lease and one issued by a different registry",
      "accountLease: reservation is the slot, and release is idempotent is idempotent: a double release frees nothing the second time",
      "accountLease: reservation is the slot, and release is idempotent re-reads the limit on every acquire, so a live change takes effect",
      "accountLease: reservation is the slot, and release is idempotent refuses to build without an injected capacityOf",
      "accountLease: reservation is the slot, and release is idempotent skips the gate entirely for an explicitly ungated connection",
      "selectAndReserve: every lease is released on all four paths leaves zero leaked leases after all four paths run against one registry",
      "selectAndReserve: every lease is released on all four paths path 1 — success: the finally block releases exactly once",
      "selectAndReserve: every lease is released on all four paths path 2 — thrown error: the slot still comes back",
      "selectAndReserve: every lease is released on all four paths path 3 — abort signal: the abort handler AND the finally both fire, once between them",
      "selectAndReserve: every lease is released on all four paths path 3b — an aborted caller's double release cannot steal a live slot",
      "selectAndReserve: every lease is released on all four paths path 4 — client disconnect: the cleanup callback releases the held slot",
      "selectAndReserve: parallel selection at the final slot admits exactly one of ten racers when one slot remains",
      "selectAndReserve: parallel selection at the final slot falls over to the next ranked account when the first is full",
      "selectAndReserve: parallel selection at the final slot keeps a session on its pinned account rather than round-robining",
      "selectAndReserve: parallel selection at the final slot never exceeds capacity across a burst on three accounts",
      "selectAndReserve: parallel selection at the final slot refuses to run without an injected clock or registry",
      "selectAndReserve: parallel selection at the final slot reports unavailable rather than throwing when nothing is eligible",
      "selectAndReserve: parallel selection at the final slot runs selection AND reservation inside one transaction"
    ],
    "tests/unit/reconciliation/affinity-request-wiring.test.js": [
      "E1.1 G4 / E1.1w W4: the chat handler supplies the session evidence selection hashes gives two different client sessions two DIFFERENT evidence payloads",
      "E1.1 G4 / E1.1w W4: the chat handler supplies the session evidence selection hashes hands headers as a PLAIN object, not a Headers instance",
      "E1.1 G4 / E1.1w W4: the chat handler supplies the session evidence selection hashes never carries a client credential header into the hash input (rule 8)",
      "E1.1 G4 / E1.1w W4: the chat handler supplies the session evidence selection hashes passes the client session header through to the selection junction",
      "E1.1 G4 / E1.1w W4: the chat handler supplies the session evidence selection hashes passes the parsed body so a body-carried session key still resolves"
    ],
    "tests/unit/reconciliation/affinity-state-entry.test.js": [
      "affinity.state.entry: resumed conversation keeps exact model endpoint account and connection loads the exact connection pinned under THIS model, not a sibling model's connection on the same session (mutation: accept changed connection tuple)",
      "affinity.state.entry: resumed conversation keeps exact model endpoint account and connection loads the exact providerNode recorded at pin time, never substituting or dropping it (mutation: accept changed account tuple)",
      "affinity.state.entry: resumed conversation keeps exact model endpoint account and connection never resolves a pin for a malformed (empty-string) session identity, even when a row for it exists on disk (mutation: reuse malformed identity)"
    ],
    "tests/unit/reconciliation/affinity-state-exit.test.js": [
      "affinity.state.exit: fifteen identities remain stable across a second turn and a restart (mutation: hop assigned endpoint) keeps every one of fifteen sessions on its first-pinned account across a ranking flip and a restart",
      "affinity.state.exit: setPin replaces the one row atomically (mutation: skip atomic replacement) a second setPin for the same (session, model) replaces the row in place rather than erroring or duplicating it",
      "affinity.state.exit: the affinity key a real request writes is a digest (mutation: store unhashed identity) persists a sha256 digest for the affinity key, never the raw client-supplied session identity however it is shaped"
    ],
    "tests/unit/reconciliation/affinity.test.js": [
      "affinity never round-robins between healthy accounts a repin replaces the one row and restamps pinnedAt, never creating a second",
      "affinity never round-robins between healthy accounts a switch that does happen leaves a durable receipt naming both sides",
      "affinity never round-robins between healthy accounts drain releases every session on one account in one pass and leaves the others",
      "affinity never round-robins between healthy accounts holds the same connection across a restart in the middle of the sequence",
      "affinity never round-robins between healthy accounts lists receipts newest first",
      "affinity never round-robins between healthy accounts returns the same connection for N sequential reads and records no switch",
      "affinity never round-robins between healthy accounts touchPin moves lastSeenAt and never pinnedAt",
      "durable session affinity survives a process restart carries the quota evidence across the restart too, so ranking is not re-derived blind",
      "durable session affinity survives a process restart drops an expired pin on read, and after a restart, rather than routing to a lapsed lease",
      "durable session affinity survives a process restart keeps pins for different models on the same session independent",
      "durable session affinity survives a process restart leaves a NULL-TTL pin alone when the sweep runs",
      "durable session affinity survives a process restart reads the pin back from the file after the adapter is dropped and re-opened",
      "durable session affinity survives a process restart writes an actual row to disk, not process state"
    ],
    "tests/unit/reconciliation/blind-pin-release.test.js": [
      "a pin is held on evidence of headroom, never on absence of evidence does not move a pin that has its own readable headroom",
      "a pin is held on evidence of headroom, never on absence of evidence holds a blind pin when every alternative is equally blind",
      "a pin is held on evidence of headroom, never on absence of evidence holds a blind pin when the only readable alternative is depleted",
      "a pin is held on evidence of headroom, never on absence of evidence is stable across repeated decisions once the session has moved",
      "a pin is held on evidence of headroom, never on absence of evidence releases a depleted pin to a readable alternative, unchanged",
      "a pin is held on evidence of headroom, never on absence of evidence releases a pin whose account has no quota evidence when a readable one exists",
      "a pin is held on evidence of headroom, never on absence of evidence still holds the pin when no account anywhere carries a deadline",
      "a pin is held on evidence of headroom, never on absence of evidence still holds the pin when the whole pool reads depleted",
      "a pin is held on evidence of headroom, never on absence of evidence the ranker calls a seat with no windows usable, which is what made the pin immortal"
    ],
    "tests/unit/reconciliation/cache-accounting.test.js": [
      "G2 cache aliases resolve at one point anthropic canonical produces the same canonical pair",
      "G2 cache aliases resolve at one point cache_creation_tokens spelling produces the same canonical pair",
      "G2 cache aliases resolve at one point cache_read_tokens spelling produces the same canonical pair",
      "G2 cache aliases resolve at one point cache_write_tokens spelling produces the same canonical pair",
      "G2 cache aliases resolve at one point has no second normalization point in the aggregates",
      "G2 cache aliases resolve at one point keeps an inclusive prompt inclusive rather than double-counting it",
      "G2 cache aliases resolve at one point normalizes exactly once — canonicalizing twice is a fixed point",
      "G2 cache aliases resolve at one point reads a nested cache write that carries no top-level alias",
      "G2 cache aliases resolve at one point resolves a cache write arriving with no cache read",
      "G2 canonical cache values carry through every consumer anthropic canonical reaches record, account, daily and cost identically",
      "G2 canonical cache values carry through every consumer cache_creation_tokens spelling reaches record, account, daily and cost identically",
      "G2 canonical cache values carry through every consumer cache_read_tokens spelling reaches record, account, daily and cost identically",
      "G2 canonical cache values carry through every consumer cache_write_tokens spelling reaches record, account, daily and cost identically",
      "G2 canonical cache values carry through every consumer sums cache reads and writes across every spelling in the daily totals"
    ],
    "tests/unit/reconciliation/cache-write-accounting.test.js": [
      "account switch keeps a cache-read drop visible instead of blending it surfaces both the switch receipt and the post-switch cache miss in the same window",
      "cache-write aliases normalize to the identical canonical pair all three spellings produce byte-identical canonicalizeUsage output",
      "cache-write aliases normalize to the identical canonical pair carries the cache-creation figure into the Responses-API shape for every spelling",
      "cache-write aliases normalize to the identical canonical pair produces identical per-account cache-write totals across all three spellings",
      "cache-write aliases normalize to the identical canonical pair still reads a raw (non-canonicalized) payload through the same shared resolver",
      "malformed cache fields fail toward zero, never NaN or the wrong bucket a non-numeric cache-write value drops to zero without polluting the read count or NaN-ing prompt_tokens",
      "malformed cache fields fail toward zero, never NaN or the wrong bucket a null or malformed cache-write field never reaches the Responses-API output, and cached_tokens is unaffected"
    ],
    "tests/unit/reconciliation/capacity-config.test.js": [
      "accountCapacity: per-connection capacity is configurable documents its default for a connection that configures nothing",
      "accountCapacity: per-connection capacity is configurable fails open to the default on a malformed value rather than throttling to zero",
      "accountCapacity: per-connection capacity is configurable honours a caller-supplied default, keeping the ceiling a live knob",
      "accountCapacity: per-connection capacity is configurable still gates at a limit of 1, which is distinct from ungated",
      "accountCapacity: per-connection capacity is configurable treats an explicit 0 as deliberately ungated, not as a missing value",
      "accountCapacity: per-connection capacity is configurable uses the connection's own value, so two accounts can differ by an order of magnitude",
      "accountCapacity: the provider ceiling is OPTIONAL and additional applies the per-account gate when no provider ceiling exists at all",
      "accountCapacity: the provider ceiling is OPTIONAL and additional does not let a higher provider ceiling raise a deliberately small account",
      "accountCapacity: the provider ceiling is OPTIONAL and additional ignores a malformed provider ceiling instead of throttling on it",
      "accountCapacity: the provider ceiling is OPTIONAL and additional is fully ungated only when the account opts out AND no ceiling is configured",
      "accountCapacity: the provider ceiling is OPTIONAL and additional keeps the provider ceiling in force even when the account opts out of gating",
      "accountCapacity: the provider ceiling is OPTIONAL and additional lets a lower provider ceiling bound the account, as an outer safety limit",
      "accountCapacity: the provider ceiling is OPTIONAL and additional names its source, so a switch receipt can record why a request queued",
      "accountCapacity: the provider ceiling is OPTIONAL and additional reads the provider off the connection when the caller does not name one",
      "accountCapacity: the provider ceiling is OPTIONAL and additional still gates when settings exist but configure no ceiling for this provider"
    ],
    "tests/unit/reconciliation/chat-lease-lifecycle.test.js": [
      "E1.1w: handleSingleModelChat releases its lease on every exit a pinned account the scheduler substituted away releases the substitute's slot",
      "E1.1w: handleSingleModelChat releases its lease on every exit a throw out of the core releases through the finally",
      "E1.1w: handleSingleModelChat releases its lease on every exit all accounts unavailable: the refusal path takes no slot with it",
      "E1.1w: handleSingleModelChat releases its lease on every exit attempt ceiling: the capped return still releases",
      "E1.1w: handleSingleModelChat releases its lease on every exit client abort: releases without waiting for a body that nobody will read",
      "E1.1w: handleSingleModelChat releases its lease on every exit empty accepted stream returns without replay and releases its slot",
      "E1.1w: handleSingleModelChat releases its lease on every exit request replay: the retried attempt does not stack a second slot",
      "E1.1w: handleSingleModelChat releases its lease on every exit terminal success: holds through the stream, releases when the body ends"
    ],
    "tests/unit/reconciliation/client-catalog.test.js": [
      "client.catalog.entry — managed catalog returns exactly four clean names drops a disabled model rather than listing it (mutation: skip managed catalog filter)",
      "client.catalog.entry — managed catalog returns exactly four clean names positive control: the disabled models are genuinely in the fixture pre-filter",
      "client.catalog.entry — managed catalog returns exactly four clean names projects every entry to exactly {model, provider, fullModel, caps}, nothing else (mutation: expose internal route name)",
      "client.catalog.entry — managed catalog returns exactly four clean names returns exactly the four entries the disabled filter leaves standing"
    ],
    "tests/unit/reconciliation/client-completion.test.js": [
      "client.completion.entry — selected lane reaches only its registered model dispatches the exact requested model to the exact selected connection, nothing substituted",
      "client.completion.entry — selected lane reaches only its registered model honors an exact pin match and reaches only that connection",
      "client.completion.entry — selected lane reaches only its registered model refuses a model with no resolvable provider rather than dispatching it (mutation: accept unknown model)",
      "client.completion.entry — selected lane reaches only its registered model refuses to substitute a healthy connection for the one the client pinned (mutation: ignore managed client identity)"
    ],
    "tests/unit/reconciliation/codex-responses-admission.test.js": [
      "codex.responses.entry — Codex Responses managed admission dispatches no model other than the one explicitly requested (mutation: accept another model)",
      "codex.responses.entry — Codex Responses managed admission fails the request rather than retrying on another connection when the pinned one is quota-exhausted (mutation: retry quota failure on another connection)",
      "codex.responses.entry — Codex Responses managed admission reaches exactly the requested codex model on the exact pinned connection (live_gate: explicitly selected connection)",
      "codex.responses.entry — Codex Responses managed admission refuses to substitute a different connection for the exact Codex pin (mutation: drop exact Codex connection pin)"
    ],
    "tests/unit/reconciliation/codex-responses-receipt.test.js": [
      "codex.responses.entry — the persisted receipt names the connection that was explicitly selected is the one redacted receipt: the connection survives, the credential does not",
      "codex.responses.entry — the persisted receipt names the connection that was explicitly selected records the exact pinned connection, not a different one that happened to be configured"
    ],
    "tests/unit/reconciliation/compound-quota-account-ordering.test.js": [
      "compound quota account ordering (issue 01 acceptance test) case 1: soonest usable reset wins when neither account is exhausted",
      "compound quota account ordering (issue 01 acceptance test) case 2: a constraining 30d window suppresses a sooner-resetting 5h window",
      "compound quota account ordering (issue 01 acceptance test) case 3: identical windows differ only by static priority, which is the tie-break only"
    ],
    "tests/unit/reconciliation/credit-state-exit.test.js": [
      "credit.state.exit: a refresh writes only the safe derived snapshot for the account it refreshed drops a reading that cannot produce a finite percentage rather than writing it (mutation: write nonfinite balance)",
      "credit.state.exit: a refresh writes only the safe derived snapshot for the account it refreshed never lets one connection refresh write under a different connection id (mutation: refresh with changed account)",
      "credit.state.exit: a refresh writes only the safe derived snapshot for the account it refreshed writes only {key, remainingPercentage, resetAt, unlimited} per window and only {lastQuotaSnapshot} as the patch, nothing from the raw upstream payload (mutation: expose upstream payload)"
    ],
    "tests/unit/reconciliation/deadline-bucket-spread.test.js": [
      "deadlineBucket: resolution follows the horizon buckets a deadline at 1/24th of its own horizon",
      "deadlineBucket: resolution follows the horizon leaves an unknown horizon at full millisecond resolution",
      "rankAccounts: quota awareness survives the bucketing a depleted seat stays ineligible however light its load",
      "rankAccounts: quota awareness survives the bucketing a materially sooner deadline still wins outright over a loaded-but-later seat",
      "rankAccounts: quota awareness survives the bucketing a seat under the headroom floor is still skipped for a loaded one above it",
      "rankAccounts: quota awareness survives the bucketing an established healthy pin never moves for the spread",
      "rankAccounts: quota awareness survives the bucketing ranking stays a total order under the bucketed key",
      "rankAccounts: staircased resets still spread (the live defect) M consecutive placements distribute across N comparable seats",
      "rankAccounts: staircased resets still spread (the live defect) the deciding key is load-spread, not reset-horizon"
    ],
    "tests/unit/reconciliation/depletion-repin.test.js": [
      "reset-aware repin: the decision point is depletion, and earliest means deadline does not repin to an account that was available all along",
      "reset-aware repin: the decision point is depletion, and earliest means deadline does NOT return to a restocked account whose deadline lands later",
      "reset-aware repin: the decision point is depletion, and earliest means deadline holds one pin across many requests on unchanged evidence",
      "reset-aware repin: the decision point is depletion, and earliest means deadline holds the pin when no account carries any deadline at all",
      "reset-aware repin: the decision point is depletion, and earliest means deadline keeps a healthy pin even when another account has restocked",
      "reset-aware repin: the decision point is depletion, and earliest means deadline never lets another account pull a session off a healthy one",
      "reset-aware repin: the decision point is depletion, and earliest means deadline ranks a just-replenished account by its NEW deadline, not its stale one",
      "reset-aware repin: the decision point is depletion, and earliest means deadline ranks a mixed-shape pool instead of refusing, and still holds the pin",
      "reset-aware repin: the decision point is depletion, and earliest means deadline returns to a restocked account when its deadline lands first",
      "sequential depletion: one, then two, then three holds account one for every request while one has headroom",
      "sequential depletion: one, then two, then three keeps the pin rather than spraying when every account is depleted",
      "sequential depletion: one, then two, then three moves to account three only once one AND two are exhausted",
      "sequential depletion: one, then two, then three moves to account two only once one is exhausted, and names exhaustion",
      "sequential depletion: one, then two, then three pins an unpinned session to the ranker winner and calls it the initial pin",
      "sequential depletion: one, then two, then three stays on three while one and two are still depleted"
    ],
    "tests/unit/reconciliation/disabled-model-policy.test.js": [
      "pure disabled-model policy accepts a verified custom provider prefix without changing the upstream model ID",
      "pure disabled-model policy an own empty array overrides all equivalent provider defaults",
      "pure disabled-model policy an own nonempty list also replaces inheritance",
      "pure disabled-model policy assigns a shadowed alias to its configured node without changing canonical provider ownership",
      "pure disabled-model policy contradictory duplicate account aliases cannot bypass a disable",
      "pure disabled-model policy does not cross providers, accounts or model identifiers",
      "pure disabled-model policy ignores malformed lists without mistaking them for an explicit override",
      "pure disabled-model policy keeps direct canonical policy IDs distinct even when a node uses one as a route prefix",
      "pure disabled-model policy recognizes additional registered provider aliases",
      "pure disabled-model policy resolves canonical and alias keys identically for cc",
      "pure disabled-model policy resolves canonical and alias keys identically for claude"
    ],
    "tests/unit/reconciliation/disabled-model-routing.test.js": [
      "direct requests and combo filtering respect the same policy allows an explicit account override through the direct request preflight",
      "direct requests and combo filtering respect the same policy filters all disabled combo members without resurrecting them, but retains an account override",
      "direct requests and combo filtering respect the same policy refuses disabled resolved model cc/claude-fable-5 before generation",
      "direct requests and combo filtering respect the same policy refuses disabled resolved model claude/claude-fable-5 before generation",
      "direct requests and combo filtering respect the same policy refuses disabled resolved model my-fable-alias before generation",
      "direct requests and combo filtering respect the same policy refuses when every account has its own disable and no provider default exists",
      "disabled model policy reaches real account selection enforces an account disable written under cc for either provider spelling",
      "disabled model policy reaches real account selection enforces an account disable written under claude for either provider spelling",
      "disabled model policy reaches real account selection inherits provider disables and honors an explicit empty account override through the HTTP API",
      "disabled model policy reaches real account selection keeps parallel agents on the only permitted account without changing model",
      "disabled model policy reaches real account selection leaves OAuth enabled when only the no-auth provider switch is set",
      "disabled model policy reaches real account selection maps a configured node prefix to its credential provider ID",
      "disabled model policy reaches real account selection moves off an explicitly disabled pin, and a temporary failure elsewhere does not narrow the pool",
      "disabled model policy reaches real account selection preserves a healthy pin, moves only when its model is explicitly disabled, and stays after recovery",
      "disabled model policy reaches real account selection refuses admission if the configured policy cannot be read",
      "operator edits preserve equivalent persisted scopes cannot repopulate an old cache across a completed concurrent operator write",
      "operator edits preserve equivalent persisted scopes does not apply a configured node prefix disable to the built-in provider it shadows",
      "operator edits preserve equivalent persisted scopes does not treat malformed historical value {\"broken\" as an explicit empty override",
      "operator edits preserve equivalent persisted scopes does not treat malformed historical value null as an explicit empty override",
      "operator edits preserve equivalent persisted scopes does not write a built-in disable into a configured node that shadows its alias",
      "operator edits preserve equivalent persisted scopes keeps an inherited alias disable while adding another model through the canonical API",
      "operator edits preserve equivalent persisted scopes keeps native slash IDs and synchronizes only the edited legacy scope",
      "operator edits preserve equivalent persisted scopes reconciles conflicting legacy rows when enabling one account and leaves others disabled",
      "resolved physical models precede account admission keeps a healthy bare-model pin and physical model when a connection default changes",
      "resolved physical models precede account admission keeps an explicit bare alias on its exact permitted model in chat",
      "resolved physical models precede account admission keeps an explicit bare alias on its exact permitted model in embeddings",
      "resolved physical models precede account admission keeps an explicit bare alias on its exact permitted model in rerank",
      "resolved physical models precede account admission preserves an explicitly prefixed native auto model in chat",
      "resolved physical models precede account admission preserves an explicitly prefixed native auto model in embeddings",
      "resolved physical models precede account admission preserves an explicitly prefixed native auto model in rerank",
      "resolved physical models precede account admission preserves an operator combo named auto before looking for defaults",
      "resolved physical models precede account admission refuses ambiguous bare defaults before account admission",
      "resolved physical models precede account admission resolves a bare auto default before checking disabled accounts and pinning",
      "resolved physical models precede account admission resolves a bare default alias only when all active defaults agree",
      "resolved physical models precede account admission uses a strict account default before admission without switching accounts"
    ],
    "tests/unit/reconciliation/drain-admission.test.js": [
      "drain.state.entry does not delete the pin or kill an in-flight stream a lease and session pin already bound to the connection survive it being marked draining, and complete normally",
      "drain.state.entry excludes a draining connection from NEW selection excludes it from a same-account retry too, not only a first attempt",
      "drain.state.entry excludes a draining connection from NEW selection never selects the draining connection even though it would otherwise rank first",
      "drain.state.entry lifted returns the connection to selection a connection refused while draining is selected again once the drain is lifted",
      "every connection draining refuses new work rather than serving anyway refuses across every candidate when a provider with several connections is entirely draining",
      "every connection draining refuses new work rather than serving anyway returns no credentials once the sole connection is draining, though it served the identical request before"
    ],
    "tests/unit/reconciliation/drain-restart.test.js": [
      "affinity survives restart a session pin set before a restart reads back identically after the adapter is dropped and reopened",
      "drain.state.entry: POST /api/admin/drain/{connectionId} finishes existing streams rather than cutting them off a stream open before the drain keeps counting through it and ends only when the stream itself does",
      "drain.state.exit: DELETE /api/admin/drain/{connectionId} and the new-work refusal it lifts records completion once active work has reached zero, and stops the recheck probe from spending new traffic while draining",
      "rollback restores the prior release rollback with no explicit target restores the release the last activation replaced",
      "rollback restores the prior release the restored release persists across a restart, not merely process memory"
    ],
    "tests/unit/reconciliation/evidence-state-entry.test.js": [
      "evidence.state.entry: only fresh qualified evidence is eligible a stale-confidence account never outranks a fresh one, even with a strictly better raw reset order (mutation: accept stale evidence)",
      "evidence.state.entry: only fresh qualified evidence is eligible clamps a raw reading whose remaining exceeds its own total, never accepting an over-cap value as evidence (mutation: accept over-cap price)",
      "evidence.state.entry: only fresh qualified evidence is eligible rejects a window whose capacity ceiling is missing or nonpositive rather than treating it as usable entitlement (mutation: accept missing or nonpositive throughput)"
    ],
    "tests/unit/reconciliation/evidence-state-exit.test.js": [
      "evidence.state.exit: a refresh that finds no evidence leaves the last good evidence on disk (mutation: replace last good on refresh failure) keeps the previously-written window row when the next refresh comes back with no evidence at all",
      "evidence.state.exit: a write never fabricates or promotes unqualified evidence never promotes a reading with no parseable reset evidence into the written set (mutation: promote failed benchmark)",
      "evidence.state.exit: a write never fabricates or promotes unqualified evidence tags an unmeasured percentage-only reading unknown, never fabricating a measured total (mutation: invent throughput)"
    ],
    "tests/unit/reconciliation/host-provision-exit.test.js": [
      "host.provision.exit: two peers holding the identical provisioned token reach the identical operator verdict grants operator access on an independent read of the same persisted material, twice (mutation: none — baseline determinism the rest of this file depends on)",
      "host.provision.exit: two peers holding the identical provisioned token reach the identical operator verdict never echoes the token or its underlying secret in a granted or refused admin response body (mutation: print private material)",
      "host.provision.exit: two peers holding the identical provisioned token reach the identical operator verdict persists the underlying secret owner-only, never permissive (mutation: write permissive mode)",
      "host.provision.exit: two peers holding the identical provisioned token reach the identical operator verdict rejects a token derived from different, unsynced material (mutation: change sibling affinity material)"
    ],
    "tests/unit/reconciliation/load-spread.test.js": [
      "decideRepin: load never spends a warm cache holds a healthy pin no matter how loaded it is against an idle alternative",
      "decideRepin: load never spends a warm cache holds a healthy pin that is under the headroom floor, which only orders NEW pins",
      "decideRepin: load never spends a warm cache repins only once the pin is INELIGIBLE, and then the load picks where",
      "rankAccounts: headroom floor for a NEW pin a new pin skips an idle account under the floor for a loaded one above it",
      "rankAccounts: headroom floor for a NEW pin an existing pin keeps serving from an account under the floor",
      "rankAccounts: headroom floor for a NEW pin exactly at the floor counts as headroom",
      "rankAccounts: headroom floor for a NEW pin exports a floor of 5% of the limit, never below one unit",
      "rankAccounts: load spread for a NEW pin (a) two fresh accounts, one carrying 3 pins: the new pin goes to the empty one",
      "rankAccounts: load spread for a NEW pin (b) an existing pin stays on its account regardless of load",
      "rankAccounts: load spread for a NEW pin (c, existing pin) unknown never outranks fresh for a pinned session even when the pin is loaded",
      "rankAccounts: load spread for a NEW pin (c) fresh quota evidence outranks an idle account with unknown evidence",
      "rankAccounts: load spread for a NEW pin (d) an idle fresh account beats an idle unknown one",
      "rankAccounts: load spread for a NEW pin (e) the trace names the deciding key and the vocabulary is closed",
      "rankAccounts: load spread for a NEW pin an alt with no load prints no load token, so a nominal line costs no extra bytes",
      "rankAccounts: load spread for a NEW pin without activeLoad the comparator and the alt tokens are unchanged",
      "schedulerRepos.countActivePins: one grouped SELECT over live pins for the model counts expiring-in-future and no-TTL rows for the model, skips expired rows and other models",
      "selectAndReserve: wires live pins and open leases into the spread a healthy pin ignores the load it is ranked with, and does not move",
      "selectAndReserve: wires live pins and open leases into the spread a new pin lands on the account with no live pins, and the count is read for this model",
      "selectAndReserve: wires live pins and open leases into the spread a pin whose account went unusable rotates to the LIGHTEST account, not the first",
      "selectAndReserve: wires live pins and open leases into the spread open leases count as load even when the repos cannot count pins"
    ],
    "tests/unit/reconciliation/managed-upstream-exit.test.js": [
      "managed.upstream.exit: \"drop exact endpoint pin\" / \"permit route fallback\" a different assignment (api-key credential) pins to its OWN endpoint, never the oauth one",
      "managed.upstream.exit: \"drop exact endpoint pin\" / \"permit route fallback\" resolves the SAME assigned endpoint on every call — no drift, no fallback substitution",
      "managed.upstream.exit: \"replay paid or partial rate limit\" is never a same-connection instant retry a paid/exhausted account (402) falls over with a real cooldown, never 0ms",
      "managed.upstream.exit: \"replay paid or partial rate limit\" is never a same-connection instant retry a partial rate limit (429) backs off exponentially — never a flat instant replay",
      "managed.upstream.exit: receipt model, endpoint and attempt equal assignment (live_gate) the persisted receipt records exactly the assigned model and endpoint — never a substituted one",
      "managed.upstream.exit: receipt model, endpoint and attempt equal assignment (live_gate) two different attempts never merge into one receipt — each keeps its own endpoint"
    ],
    "tests/unit/reconciliation/nested-request-fail-closed.test.js": [
      "A disallowed lane or a malformed identity fails closed before capacity is taken a malformed connection identity takes no slot, and a forged lease frees nobody",
      "A disallowed lane or a malformed identity fails closed before capacity is taken a malformed model identity fails closed rather than matching by guess",
      "A disallowed lane or a malformed identity fails closed before capacity is taken an unknown identity with no eligible account is refused, never admitted by default",
      "A disallowed lane or a malformed identity fails closed before capacity is taken refuseDisallowedModel answers 403 when a child asks outside its key allowlist",
      "A disallowed lane or a malformed identity fails closed before capacity is taken the lane check runs BEFORE account selection, so a refused child leaks no capacity",
      "Nested requests traverse normal routing, with no depth-aware branch a nested child is not exempt from host-resource checks: at the ceiling it waits",
      "Nested requests traverse normal routing, with no depth-aware branch depth buys no discount: a child at depth 5 costs exactly one slot, like the root",
      "Nested requests traverse normal routing, with no depth-aware branch every generation of a five-deep chain is admitted by the same path as its root"
    ],
    "tests/unit/reconciliation/opencode-effort-scope.test.js": [
      "G6 a gateway enum outranks the model name applies to an openai-compatible node, which declares no PROVIDERS entry",
      "G6 a gateway enum outranks the model name gives opencode its own ladder for a codex-named model",
      "G6 a gateway enum outranks the model name is wire-visible, not only a picker label",
      "G6 a gateway enum outranks the model name matches what the same route resolves for a non-codex model",
      "G6 a gateway enum outranks the model name meta serves a codex-named model on its own meta enum",
      "G6 a gateway enum outranks the model name nube serves a codex-named model on its own openai enum",
      "G6 a gateway enum outranks the model name ollama serves a codex-named model on its own ollama enum",
      "G6 a gateway enum outranks the model name openrouter serves a codex-named model on its own openai enum",
      "G6 a gateway enum outranks the model name siliconflow serves a codex-named model on its own openai enum",
      "G6 a gateway enum outranks the model name tokenrouter serves a codex-named model on its own tokenrouter enum",
      "G6 a gateway enum outranks the model name venice serves a codex-named model on its own openai enum",
      "G6 a gateway enum outranks the model name vercel-ai-gateway serves a codex-named model on its own openai enum",
      "G6 the narrowing stops exactly where it should keeps a PROVIDER-SCOPED pattern authoritative over the route format",
      "G6 the narrowing stops exactly where it should keeps codex's own per-model patterns intact",
      "G6 the narrowing stops exactly where it should keeps ollama's gpt-oss pattern narrowing its own gateway enum",
      "G6 the narrowing stops exactly where it should leaves codex, which declares no gateway format, on the codex ladder",
      "G6 the narrowing stops exactly where it should leaves kiro, which declares no gateway format, on the codex ladder",
      "G6 the narrowing stops exactly where it should leaves openai, which declares no gateway format, on the codex ladder",
      "G6 the narrowing stops exactly where it should returns null for a model with no reasoning capability at all"
    ],
    "tests/unit/reconciliation/operator-control.test.js": [
      "operator.control.entry — a genuine operator is admitted (the gate is real, not closed) serves drain status and lands a drain-on to a real operator",
      "operator.control.entry — drain rejects a normal client (mutation: accept normal client) refuses an anonymous drain-on call and leaves drain state untouched",
      "operator.control.entry — drain rejects a normal client (mutation: accept normal client) refuses an inference-keyed drain-off call and leaves drain state untouched",
      "operator.control.entry — identity content is verified, not merely its presence (mutation: skip operator identity check) refuses a loopback caller presenting the WRONG cli token on drain status",
      "operator.control.entry — identity content is verified, not merely its presence (mutation: skip operator identity check) refuses a loopback caller presenting the WRONG cli token on drain, and writes nothing",
      "operator.control.entry — operator status rejects a normal client (mutation: accept normal client) refuses an anonymous read of drain status",
      "operator.control.entry — operator status rejects a normal client (mutation: accept normal client) refuses an inference-keyed read of drain status"
    ],
    "tests/unit/reconciliation/operator-pin-affinity.test.js": [
      "operator-pin branch: selection semantics that must not move still overrides a LIVE durable pin held by another account",
      "operator-pin branch: selection semantics that must not move still overrides the ranker",
      "operator-pin branch: selection semantics that must not move still refuses rather than substituting when a STRICT pin is unavailable",
      "operator-pin branch: selection semantics that must not move still reports a WAIT when the pinned account is at capacity",
      "operator-pin branch: the selection is DURABLE (the defect) hands the durable pin to the SCHEDULER path on the next unpinned request",
      "operator-pin branch: the selection is DURABLE (the defect) persists a HASH, never the raw session id, a key or the prompt body",
      "operator-pin branch: the selection is DURABLE (the defect) records a switch receipt when the operator moves the session off its pin",
      "operator-pin branch: the selection is DURABLE (the defect) TOUCHES the pin on a reused operator-pinned selection without moving pinnedAt",
      "operator-pin branch: the selection is DURABLE (the defect) writes NOTHING when the pinned account is at capacity",
      "operator-pin branch: the selection is DURABLE (the defect) writes the pin on a first operator-pinned selection"
    ],
    "tests/unit/reconciliation/patch-parity.test.js": [
      "G7.1 max effort survives instead of being rewritten to xhigh drops a level outside the ladder rather than forwarding it upstream",
      "G7.1 max effort survives instead of being rewritten to xhigh keeps max for a model whose declared ladder contains max",
      "G7.1 max effort survives instead of being rewritten to xhigh passes low, medium, high and xhigh through untouched",
      "G7.1 max effort survives instead of being rewritten to xhigh still clamps max down for a model whose ladder stops at xhigh",
      "G7.10 no bundled sample logs ship in the package packs no file under any logs directory",
      "G7.2 cache controls survive request normalization carries the markers through the whole request translation for that provider",
      "G7.2 cache controls survive request normalization declares the quirk on the provider that honours the markers",
      "G7.2 cache controls survive request normalization keeps cache_control when the caller asks normalization to preserve it",
      "G7.2 cache controls survive request normalization strips cache_control by default, which is what most upstreams require",
      "G7.3 cache and cost truth survive every conversion accepts the Anthropic spelling of the cache read as well",
      "G7.3 cache and cost truth survive every conversion carries the upstream's own price, including a genuine zero",
      "G7.3 cache and cost truth survive every conversion projects the cache split and the cost onto the Claude terminal event",
      "G7.3 cache and cost truth survive every conversion reads a top-level cached count that no nested details block carried",
      "G7.3 cache and cost truth survive every conversion reports the cached count on the non-streaming Responses conversion",
      "G7.3 cache and cost truth survive every conversion reports the same cache and cost on the non-streaming Claude conversion",
      "G7.4 repeated tool-argument fragments normalize to one payload appends an ordinary delta",
      "G7.4 repeated tool-argument fragments normalize to one payload collapses an exact terminal replay of a complete object",
      "G7.4 repeated tool-argument fragments normalize to one payload emits each tool closure once even when finish_reason repeats",
      "G7.4 repeated tool-argument fragments normalize to one payload emits one input_json_delta per tool call through the Claude converter",
      "G7.4 repeated tool-argument fragments normalize to one payload leaves a doubled string alone when the halves are not valid JSON",
      "G7.4 repeated tool-argument fragments normalize to one payload replaces rather than appends when the fragment restates the buffer",
      "G7.5 a usage-only event terminates the stream holds the terminal back when the finish chunk promises usage is coming",
      "G7.5 a usage-only event terminates the stream reaches the converter instead of being dropped for carrying no choice",
      "G7.5 a usage-only event terminates the stream releases a held terminal at flush so a truncated stream still closes",
      "G7.5 a usage-only event terminates the stream terminates immediately when the provider makes no such promise",
      "G7.6 an exact account pin is honored or refused, never substituted caps the attempt budget only for a positive safe integer",
      "G7.6 an exact account pin is honored or refused, never substituted forwards the fields that identify the route and the upstream prompt cache",
      "G7.6 an exact account pin is honored or refused, never substituted invents none of those fields when the caller sent none",
      "G7.6 an exact account pin is honored or refused, never substituted proceeds when selection honoured the pin",
      "G7.6 an exact account pin is honored or refused, never substituted refuses rather than serving a substituted connection",
      "G7.6 an exact account pin is honored or refused, never substituted rotates past the first failure when no ceiling was given",
      "G7.6 an exact account pin is honored or refused, never substituted stops at the caller's attempt ceiling instead of spending the whole pool",
      "G7.7 a deterministic 4xx never quarantines a healthy model classifies every unmatched 4xx as deterministic, with no fallback and no cooldown",
      "G7.7 a deterministic 4xx never quarantines a healthy model exempts the statuses that describe timing rather than the request",
      "G7.7 a deterministic 4xx never quarantines a healthy model keeps a credential fact rotating, because that is not a caller error",
      "G7.7 a deterministic 4xx never quarantines a healthy model keeps the two 4xx classifiers independent, and disagreeing only where intended",
      "G7.7 a deterministic 4xx never quarantines a healthy model leaves 5xx and a non-numeric status to the transient path",
      "G7.8 provider metadata is opted into and allowlisted asks for authoritative usage on every request",
      "G7.8 provider metadata is opted into and allowlisted asks for the trailing usage chunk only when streaming",
      "G7.8 provider metadata is opted into and allowlisted forwards the metadata header only for the exact opt-in value",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired adds nothing when the upstream sent no id at all",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired drops a value carrying a header separator, which is the injection vector",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired drops an empty or oversize id",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired forwards an id made only of allowlisted characters",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired names one response header for both the streaming and JSON paths",
      "G7.9 an upstream generation id is forwarded or dropped, never repaired returns a new object rather than mutating the caller's header set"
    ],
    "tests/unit/reconciliation/provider-stream-entry.test.js": [
      "provider.stream.entry — a managed stream emits prompt, first content, one valid tool call a chunk with no recognizable delta shape produces no output — never a raw echo of a malformed frame",
      "provider.stream.entry — a managed stream emits prompt, first content, one valid tool call a provider that RESTATES the accumulated argument string each chunk still yields one valid call, not a doubled payload",
      "provider.stream.entry — a managed stream emits prompt, first content, one valid tool call assembles tool-call fragments split across many chunks into exactly one valid call — never duplicated",
      "provider.stream.entry — a managed stream emits prompt, first content, one valid tool call emits the first real content chunk on its own call — never withheld until the stream buffers whole",
      "provider.stream.entry — a managed stream emits prompt, first content, one valid tool call sends message_start (the prompt frame) before any content or tool block"
    ],
    "tests/unit/reconciliation/provider-terminal-exit.test.js": [
      "provider.terminal.exit: \"accept missing terminal\" — the flush synthesizes exactly one terminal when the upstream never sends finish_reason a tool call left open by a dropped connection still closes with one terminal at flush",
      "provider.terminal.exit: \"accept missing terminal\" — the flush synthesizes exactly one terminal when the upstream never sends finish_reason calling the flush a second time after a tool-call terminal is a no-op, not a second terminal",
      "provider.terminal.exit: \"lose usage-only frame\" — a deferred terminal is released by the usage-only frame, with the real counts the terminal waits for stream_options.include_usage, then fires on the usage-only frame carrying the real tokens",
      "provider.terminal.exit: \"settle twice\" — a repeated upstream terminal never re-fires a second finish_reason chunk after the first produces no further output at all"
    ],
    "tests/unit/reconciliation/qualification.test.js": [
      "GET /api/admin/qualification discloses no credential across the whole list",
      "GET /api/admin/qualification lists every connection in the frozen Connection shape",
      "GET /api/admin/qualification reports a draining connection as drained",
      "GET /api/admin/qualification/{connectionId} discloses no credential from the decrypted connection record",
      "GET /api/admin/qualification/{connectionId} does not promote an old inferred model or configuration time to generation evidence",
      "GET /api/admin/qualification/{connectionId} is 404 for a connection that does not exist, and says nothing else",
      "GET /api/admin/qualification/{connectionId} returns a qualification receipt in the frozen shape",
      "POST /api/admin/qualification/{connectionId}/recheck admits one probe at a time per connection",
      "POST /api/admin/qualification/{connectionId}/recheck keeps a failed probe a 200 and redacts the upstream's error text",
      "POST /api/admin/qualification/{connectionId}/recheck keeps malformed validation results unknown instead of treating truthy values as a pass",
      "POST /api/admin/qualification/{connectionId}/recheck probes anyway when force is set",
      "POST /api/admin/qualification/{connectionId}/recheck records a provider validation without inventing a generated response or model",
      "POST /api/admin/qualification/{connectionId}/recheck records the probe rather than the generated content",
      "POST /api/admin/qualification/{connectionId}/recheck refuses to probe a draining connection",
      "POST /api/admin/qualification/{connectionId}/recheck rejects a malformed body before it contacts the upstream",
      "POST /api/admin/qualification/{connectionId}/recheck rejects an unrecognized field before it contacts the upstream",
      "POST /api/admin/qualification/{connectionId}/recheck reuses a fresh provider validation instead of repeating the check",
      "POST /api/admin/qualification/{connectionId}/recheck still records a finding when the probe itself throws"
    ],
    "tests/unit/reconciliation/quota-ranking.test.js": [
      "quotaRanking: compound window shapes (Acceptance: Compound windows) 5h + 7d — the account whose LONGEST window resets soonest wins",
      "quotaRanking: compound window shapes (Acceptance: Compound windows) 5h + 7d + 30d — the 30d window is compared first and binds the result",
      "quotaRanking: compound window shapes (Acceptance: Compound windows) 7d only — a single-shape cohort still ranks by that window's reset",
      "quotaRanking: compound window shapes (Acceptance: Compound windows) falls through to the next-longest window when the longest ties",
      "quotaRanking: eligibility requires headroom in EVERY known hard window a depleted window whose resetAt has already elapsed counts as replenished",
      "quotaRanking: eligibility requires headroom in EVERY known hard window an account with headroom in one scope and none in another is ineligible",
      "quotaRanking: eligibility requires headroom in EVERY known hard window reports every account depleted without calling it a refusal to rank",
      "quotaRanking: mixed window shapes and clock injection accepts a Date as well as an epoch number, with identical results",
      "quotaRanking: mixed window shapes and clock injection degrades ONLY when no account anywhere carries a deadline",
      "quotaRanking: mixed window shapes and clock injection is a total order — the same inputs always produce the same ranking",
      "quotaRanking: mixed window shapes and clock injection ranks a mixed-shape pool on each account own binding window",
      "quotaRanking: mixed window shapes and clock injection refuses to run without an injected clock",
      "quotaRanking: mixed window shapes and clock injection returns no winner for an empty cohort instead of throwing",
      "quotaRanking: mixed window shapes and clock injection short-circuits a single-member cohort with no ranking work",
      "quotaRanking: null resetAt is not usable entitlement (overlay-spec §1, strict) falls an account with a null resetAt out of ranking rather than sorting it first",
      "quotaRanking: null resetAt is not usable entitlement (overlay-spec §1, strict) ranks a member with a null resetAt LAST instead of taking the pool down with it",
      "quotaRanking: null resetAt is not usable entitlement (overlay-spec §1, strict) rejects an unparseable resetAt on the same strict grounds",
      "quotaRanking: tie-break path a missing priority is unbounded, so a configured one outranks it",
      "quotaRanking: tie-break path never lets priority override a sooner-resetting longest window",
      "quotaRanking: tie-break path orders unknown-confidence evidence behind fresh evidence, without dropping it",
      "quotaRanking: tie-break path prefers the previous pin over priority on an exact reset tie (no round-robin)",
      "quotaRanking: tie-break path uses configured priority ONLY when every reset timestamp is identical",
      "quotaRanking: window classification and horizons a per-model weekly at zero does not deplete the whole account",
      "quotaRanking: window classification and horizons classifies a qualified period name as a sub-quota, not entitlement",
      "quotaRanking: window classification and horizons drops scoped windows but keeps the account rankable on its general ones",
      "quotaRanking: window classification and horizons falls back on the bare window name when no duration is given",
      "quotaRanking: window classification and horizons reads a parenthetical duration in preference to the bare name",
      "quotaRanking: window classification and horizons reads a period name across an underscore boundary",
      "quotaRanking: window classification and horizons treats a per-model sub-quota as scoped and ignores it for ranking"
    ],
    "tests/unit/reconciliation/receipt-state-exit.test.js": [
      "receipt.state.exit — one redacted receipt exists for every non-cache generation a real generation (nonzero tokens) gets exactly one persisted receipt",
      "receipt.state.exit — one redacted receipt exists for every non-cache generation a zero-token event (no generation happened) gets no receipt at all",
      "receipt.state.exit — one redacted receipt exists for every non-cache generation mutation \"omit provider attempt\": the provider, model and endpoint that served this generation survive exactly",
      "receipt.state.exit — one redacted receipt exists for every non-cache generation mutation \"persist raw metadata\": the raw API key and the translated request body never survive to the row",
      "receipt.state.exit — one redacted receipt exists for every non-cache generation mutation \"trust estimated cost\": the recorded count is the same count the client was handed"
    ],
    "tests/unit/reconciliation/receipts-paging.test.js": [
      "G-RECEIPTS F3 - a paginated audit log returns every row exactly once agrees with a full read, in the same order, at every page size",
      "G-RECEIPTS F3 - a paginated audit log returns every row exactly once issues an opaque cursor, not a bare timestamp a caller could hand-craft",
      "G-RECEIPTS F3 - a paginated audit log returns every row exactly once keeps a tie stable rather than leaving it to arbitrary repo order",
      "G-RECEIPTS F3 - a paginated audit log returns every row exactly once loses no row to a tied timestamp when paging one at a time",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped accepts a valid since and filters on it",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped clamps an oversized limit rather than erroring, and says so",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped rejects a malformed cursor instead of restarting from the top",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped rejects a non-numeric, zero, negative or fractional limit",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped rejects an unparseable since",
      "G-RECEIPTS F4 - a typed parameter is validated, never silently dropped still accepts an absent parameter and the documented defaults",
      "Shared routing scope across the complete retained population filters before pagination and reaches records beyond the old thousand-row scan",
      "Shared routing scope across the complete retained population preserves all predicates as conjunctions including either account side",
      "Shared routing scope across the complete retained population rejects malformed scope before state reads 0",
      "Shared routing scope across the complete retained population rejects malformed scope before state reads 1",
      "Shared routing scope across the complete retained population rejects malformed scope before state reads 2",
      "Shared routing scope across the complete retained population rejects malformed scope before state reads 3"
    ],
    "tests/unit/reconciliation/redaction.test.js": [
      "G5 — the disk log never holds the secret writes no canary to disk from any stage, header, body or stream frame",
      "G5 — the persisted DB row never holds the secret keeps the secret out of every API projection built on the row",
      "G5 — the persisted DB row never holds the secret scrubs BEFORE truncating, so the preview cannot carry what the body could not",
      "G5 — the persisted DB row never holds the secret still stores the metadata the row exists for",
      "G5 — the persisted DB row never holds the secret stores none of the five planted canaries, in any field",
      "G5 — the redactor removes secrets and nothing else drops secret-shaped KEYS whatever their value looks like",
      "G5 — the redactor removes secrets and nothing else drops secret-shaped VALUES wherever they sit, including free text",
      "G5 — the redactor removes secrets and nothing else fails CLOSED on a body it cannot walk",
      "G5 — the redactor removes secrets and nothing else keeps the accounting fields, which a substring rule on \"token\" would blank",
      "G5 — the redactor removes secrets and nothing else reconciles the two header lists that had already drifted apart"
    ],
    "tests/unit/reconciliation/retry-metadata.test.js": [
      "G4 — a local admission refusal tells the truth about itself answers a client rate-limit with the window's own reset, not a constant",
      "G4 — a local admission refusal tells the truth about itself marks a local concurrency refusal as admission, and does not blame the provider",
      "G4 — a local admission refusal tells the truth about itself marks an exhausted-account refusal as a PROVIDER failure, with the §4 floor",
      "G4 — a local admission refusal tells the truth about itself prefers a real lock expiry over the floor when one exists",
      "G4 — auth, payment and policy are NEVER retried classifies the terminal statuses as non-retryable, and the transient ones as retryable",
      "G4 — auth, payment and policy are NEVER retried does not rotate an account on a policy rejection — the request is what was refused",
      "G4 — auth, payment and policy are NEVER retried never re-dispatches the SAME account after a terminal upstream status",
      "G4 — auth, payment and policy are NEVER retried returns an uncertain transient failure without replaying the generation",
      "G4 — auth, payment and policy are NEVER retried strips Retry-After from a terminal status even when a real reset instant is supplied",
      "G4 — Retry-After comes from real state, never a constant derives the wait from a quota window's own reset instant",
      "G4 — Retry-After comes from real state, never a constant emits NO header when there is no real state to derive one from",
      "G4 — Retry-After comes from real state, never a constant floors at 1 second and never reports 0, even for a window already past",
      "G4 — Retry-After comes from real state, never a constant rounds UP, so a client waking on the hint finds the window actually open"
    ],
    "tests/unit/reconciliation/routing-cache-money-safety.test.js": [
      "cache and money routing invariants applies scoped subquota exhaustion only to the requested model family",
      "cache and money routing invariants does not coerce missing quota values into explicit zero entitlement",
      "cache and money routing invariants does not hold a pin whose explicit entitlement is zero",
      "cache and money routing invariants holds the pin when the entire pool has uncertain depleted snapshots",
      "cache and money routing invariants honors account model eligibility when the previous pin is no longer entitled",
      "cache and money routing invariants orders every tied quota horizon before any shorter one",
      "cache and money routing invariants prioritizes earlier monthly reset despite lower load on a later account",
      "cache and money routing invariants releases a reservation if recordSwitch fails",
      "cache and money routing invariants releases a reservation if setPin fails",
      "cache and money routing invariants releases a reservation if transaction commit fails",
      "cache and money routing invariants spreads concurrent new agents with tied horizons and retains their pins",
      "cache and money routing invariants waits for a healthy pin at capacity without writing a new pin",
      "cache and money routing invariants waits until every exhausted window on at least one account has reset"
    ],
    "tests/unit/reconciliation/routing-identity-stability.test.js": [
      "agent routing identity ignores per-request ids and moving cache breakpoints",
      "agent routing identity keeps an anonymous conversation stable when assistant history appears",
      "agent routing identity reports the provenance of the affinity key for {\"x-client-request-id\":\"request-only\"}",
      "agent routing identity reports the provenance of the affinity key for {\"x-session-id\":\"client-agent\"}",
      "agent routing identity reports the provenance of the affinity key for {}",
      "agent routing identity separates anonymous initial tasks and authenticated clients",
      "agent routing identity separates inherited sessions when agent cache prefixes differ",
      "agent routing identity uses explicit session identity when history is compacted"
    ],
    "tests/unit/reconciliation/scheduler-wiring.test.js": [
      "cancels a provider-selection waiter without unlocking its live predecessor",
      "E1.1w: a mixed-shape pool still ranks, and never serves a depleted account answers 429 with the earliest real reset once nothing in the pool has headroom",
      "E1.1w: a mixed-shape pool still ranks, and never serves a depleted account holds a session on its own depleted account rather than refusing on a stale reading",
      "E1.1w: a mixed-shape pool still ranks, and never serves a depleted account skips a depleted account that sits first in the list and takes the soonest deadline",
      "E1.1w: getProviderCredentials selects through the scheduler advances lastSeenAt on a sticky re-selection without moving pinnedAt",
      "E1.1w: getProviderCredentials selects through the scheduler CLEARS a stale window once a live read reports nothing rankable (W3)",
      "E1.1w: getProviderCredentials selects through the scheduler fails OPEN on a quota lookup that throws",
      "E1.1w: getProviderCredentials selects through the scheduler honours an operator pin and still takes a lease for it",
      "E1.1w: getProviderCredentials selects through the scheduler keeps a second request on the SAME account while that account stays eligible",
      "E1.1w: getProviderCredentials selects through the scheduler KEEPS a stored window when the read produced no snapshot at all (W3)",
      "E1.1w: getProviderCredentials selects through the scheduler persists derived quota windows so the ranker reads a live table (W3)",
      "E1.1w: getProviderCredentials selects through the scheduler picks the account the RANKER prefers, not the one round-robin would",
      "E1.1w: getProviderCredentials selects through the scheduler repins and writes a switch receipt when the pinned account is gone",
      "E1.1w: getProviderCredentials selects through the scheduler reports a WAIT rather than a silent round-robin when every account is at capacity",
      "E1.1w: lease lifetime (W2) a double release frees nothing extra",
      "E1.1w: lease lifetime (W2) a fabricated lease frees nothing",
      "E1.1w: lease lifetime (W2) a retry that switches accounts leaves nothing behind on the first",
      "E1.1w: lease lifetime (W2) hands a streaming response its lease and releases when the body ends",
      "E1.1w: lease lifetime (W2) releases a body-less response immediately",
      "E1.1w: lease lifetime (W2) releases the lease when proxy resolution makes the account unusable",
      "E1.1w: lease lifetime (W2) releases when the client cancels a stream mid-flight",
      "E1.1w: the persisted session identity is a HASH (W4) derives a STABLE hash for one session across requests",
      "E1.1w: the persisted session identity is a HASH (W4) gives two different client sessions two different pins",
      "E1.1w: the persisted session identity is a HASH (W4) treats a request with NO client evidence as one stable anonymous session",
      "E1.1w: the persisted session identity is a HASH (W4) writes no raw session id, credential or prompt body to either table",
      "model entitlement and temporary pin recovery keeps serving the pinned account through a temporary 429 failure",
      "model entitlement and temporary pin recovery keeps serving the pinned account through a temporary 503 failure",
      "model entitlement and temporary pin recovery marks capacity waits as terminal across model fallback boundaries",
      "model entitlement and temporary pin recovery releases a reserved lease when proxy resolution throws",
      "model entitlement and temporary pin recovery skips an account whose configured model list excludes the requested model"
    ],
    "tests/unit/reconciliation/service-tier.test.js": [
      "G1 Codex service tier fidelity emits no tier at all when the request carries none",
      "G1 Codex service tier fidelity forwards first-class tier default byte-for-byte",
      "G1 Codex service tier fidelity forwards first-class tier priority byte-for-byte",
      "G1 Codex service tier fidelity forwards first-class tier ultrafast byte-for-byte",
      "G1 Codex service tier fidelity keeps `fast` an alias rather than a fourth first-class tier",
      "G1 Codex service tier fidelity normalizes exactly once — re-running is a fixed point",
      "G1 Codex service tier fidelity omits non-string tier [] (fails by omission, never null)",
      "G1 Codex service tier fidelity omits non-string tier {} (fails by omission, never null)",
      "G1 Codex service tier fidelity omits non-string tier 123 (fails by omission, never null)",
      "G1 Codex service tier fidelity omits non-string tier null (fails by omission, never null)",
      "G1 Codex service tier fidelity omits non-string tier true (fails by omission, never null)",
      "G1 Codex service tier fidelity omits non-string tier undefined (fails by omission, never null)",
      "G1 Codex service tier fidelity omits unrecognized tier \"\" instead of remapping it",
      "G1 Codex service tier fidelity omits unrecognized tier \"flex\" instead of remapping it",
      "G1 Codex service tier fidelity omits unrecognized tier \"Priority\" instead of remapping it",
      "G1 Codex service tier fidelity omits unrecognized tier \"PRIORITY\" instead of remapping it",
      "G1 Codex service tier fidelity omits unrecognized tier \"scale\" instead of remapping it",
      "G1 Codex service tier fidelity omits unrecognized tier \"ultra\" instead of remapping it",
      "G1 Codex service tier fidelity resolves the `fast` alias to priority and never emits `fast`",
      "G1 Codex service tier fidelity survives a second transformRequest pass over its own output",
      "G1 Codex service tier fidelity tolerates surrounding whitespace on a real tier"
    ],
    "tests/unit/reconciliation/sse-liveness.test.js": [
      "G3 — downstream heartbeats during every silent interval a real downstream byte DEFERS the next beat by a full interval",
      "G3 — downstream heartbeats during every silent interval beats through PRE-TTFT silence, and through MID-STREAM silence after real data",
      "G3 — downstream heartbeats during every silent interval keepaliveMs = 0 disables heartbeats entirely (spec §5: 0 disables)",
      "G3 — downstream heartbeats during every silent interval NEVER enters the translator — pre-TTFT and mid-stream",
      "G3 — downstream heartbeats during every silent interval NEVER resets the upstream stall clock — a hung provider still trips it on time",
      "G3 — downstream heartbeats during every silent interval stops when the stream ENDS — upstream close",
      "G3 — downstream heartbeats during every silent interval stops when the stream ENDS — upstream error and downstream cancel"
    ],
    "tests/unit/reconciliation/stream-done-sentinel.test.js": [
      "G-SSE - the [DONE] sentinel follows the client protocol on the translate path emits the sentinel exactly once, never twice",
      "G-SSE - the [DONE] sentinel follows the client protocol on the translate path still withholds the sentinel from a client that speaks Claude",
      "G-SSE - the [DONE] sentinel follows the client protocol on the translate path terminates a claude-upstream stream for an OpenAI chat-completions client"
    ],
    "tests/unit/reconciliation/stream-uncommon-paths.test.js": [
      "G-SSE uncommon paths does not append a sentinel after the upstream ERRORS mid-stream",
      "G-SSE uncommon paths does not double-terminate when the upstream sent its own sentinel",
      "G-SSE uncommon paths emits exactly one sentinel on a very long stream",
      "G-SSE uncommon paths records usage and emits nothing further when the CLIENT hangs up",
      "G-SSE uncommon paths terminates a stream whose upstream closed WITHOUT a terminal event",
      "G-SSE uncommon paths terminates an EMPTY completion, so a client does not hang on a zero-content reply"
    ],
    "tests/unit/reconciliation/subscription-upstream-exit.test.js": [
      "subscription.upstream.exit: \"accept cross-model response\" never lets the provider pick the billed model extractUsageFromResponse never returns a model field — nothing for a spoofed response to override",
      "subscription.upstream.exit: \"accept cross-model response\" never lets the provider pick the billed model the receipt bills the ASSIGNED model even when the response usage carries a different one",
      "subscription.upstream.exit: \"route subscription lane through metered provider\" an API-key credential is scoped to the metered platform host, never the subscription host",
      "subscription.upstream.exit: \"route subscription lane through metered provider\" an OAuth subscription credential is scoped to the subscription host, never the metered platform host",
      "subscription.upstream.exit: receipt has configured connection and null metered cost (live_gate) a subscription connection with no configured per-token price retains unknown cost"
    ],
    "tests/unit/reconciliation/switch-receipt.test.js": [
      "buildSwitchReceipt: no secret and no prompt body survives cannot be widened by a hostile extra input key",
      "buildSwitchReceipt: no secret and no prompt body survives leaks none of the synthetic secrets planted across every input",
      "buildSwitchReceipt: no secret and no prompt body survives stores the session HASH and never the raw session identity",
      "buildSwitchReceipt: the exact field set rule 8 requires carries every required field and NOTHING else",
      "buildSwitchReceipt: the exact field set rule 8 requires normalizes the timestamp to ISO-8601 UTC from a number or a Date",
      "buildSwitchReceipt: the exact field set rule 8 requires pins each window to the normalized vocabulary and nothing more",
      "buildSwitchReceipt: the exact field set rule 8 requires records a first pin as a switch from nothing rather than dropping it",
      "buildSwitchReceipt: the exact field set rule 8 requires refuses to run without an injected clock",
      "buildSwitchReceipt: the exact field set rule 8 requires stores an empty window list for unrankable evidence, never raw passthrough",
      "buildSwitchReceipt: the exact field set rule 8 requires takes only the id from a full connection record handed in as from/to",
      "selectAndReserve: a receipt is persisted for every switch records the first pin and then the repin, each with the full field set",
      "selectAndReserve: a receipt is persisted for every switch records the normalized windows of the account it switched TO",
      "selectAndReserve: a receipt is persisted for every switch writes no receipt when the session stays on its pinned account"
    ],
    "tests/unit/reconciliation/terminal-retry-advertisement.test.js": [
      "G-RETRY - a permanent fault never advertises a recovery timer classifies 404 as terminal: a model that does not exist is not created by waiting",
      "G-RETRY - a permanent fault never advertises a recovery timer keeps the transient statuses retryable",
      "G-RETRY - a permanent fault never advertises a recovery timer keeps the two 4xx classifiers consistent for 404",
      "G-RETRY - a permanent fault never advertises a recovery timer omits the human reset text from a terminal body, not just the header",
      "G-RETRY - a permanent fault never advertises a recovery timer still tells a genuinely rate-limited caller when to come back",
      "G-RETRY - a permanent fault never advertises a recovery timer strips Retry-After from a 404 even when a real lock expiry is supplied"
    ],
    "tests/unit/reconciliation/update-activation-exit.test.js": [
      "update.activation.exit update.activation.exit: a release that predates this activation survives it (discard data directory)",
      "update.activation.exit update.activation.exit: activation never lifts a drain it did not request (activate before drain)",
      "update.activation.exit update.activation.exit: health, status, catalog and qualified pools all pass while the connection is still drained",
      "update.activation.exit update.activation.exit: status detail reports error rather than a false ok when state is not yet seeded (start proxy before state seed)"
    ],
    "tests/unit/reconciliation/update-package-entry.test.js": [
      "update.package.entry update.package.entry: activates a staged release on file with a passing status",
      "update.package.entry update.package.entry: refuses a release staged with status failed, and writes nothing",
      "update.package.entry update.package.entry: refuses a releaseId staging never registered, rather than activating it, and writes nothing"
    ],
    "tests/unit/reconciliation/update-rollback-exit.test.js": [
      "update.rollback.exit update.rollback.exit: refuses a rollback target that was never staged, rather than reporting success (delete last good state)",
      "update.rollback.exit update.rollback.exit: refuses to report success when nothing verified-good is on file to restore (report success after failed health)",
      "update.rollback.exit update.rollback.exit: restores the exact prior release after a failed activation, byte for byte"
    ],
    "tests/unit/reconciliation/workflow-contract-capacity.test.js": [
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent a saturated host makes the next child WAIT with a retry hint; it never evicts a live session",
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent a workflow declaring 180 agents and running a batch of 8 holds exactly 8 leases",
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent another session acquires capacity while the future waves remain unstarted",
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent five waves of eight: peak concurrency stays at the batch while 40 requests are served",
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent no account is ever admitted past its own configured ceiling",
      "Workflow Contract, gateway side: one slot per admitted request, none per declared agent the declared lifetime total is not an input: declaring 180 and declaring 20 are identical"
    ],
    "tests/unit/refresh-body-stall-1450.test.js": [
      "a token refresh is bounded when the upstream body stalls (#1450) gives up on a silent body instead of hanging, and fails the way an errored refresh already does",
      "a token refresh is bounded when the upstream body stalls (#1450) passes a deadline on every refresh path, not just the generic one",
      "a token refresh is bounded when the upstream body stalls (#1450) still throws out of the Kiro path, which has no catch of its own today"
    ],
    "tests/unit/refresh-preserves-proxy-pool-884.test.js": [
      "a refresh never drops the stored providerSpecificData (#884) chat.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) chat.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) embeddings.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) embeddings.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) fetch.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) fetch.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) finds the handlers that persist a refresh",
      "a refresh never drops the stored providerSpecificData (#884) imageGeneration.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) imageGeneration.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) rerank.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) rerank.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) search.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) search.js passes the existing map at EVERY refresh site",
      "a refresh never drops the stored providerSpecificData (#884) videoGeneration.js never replaces the map with only the refreshed fields",
      "a refresh never drops the stored providerSpecificData (#884) videoGeneration.js passes the existing map at EVERY refresh site",
      "the merge itself keeps the base (#884) spreads existing under refreshed, so an untouched key survives"
    ],
    "tests/unit/refresh-proxy-affinity-2703.test.js": [
      "token refresh honours the connection proxy (#2703) forwards the connection's proxy config on a real refresh call",
      "token refresh honours the connection proxy (#2703) leaves no raw fetch( call in any refresh path",
      "token refresh honours the connection proxy (#2703) passes null options when the connection carries no provider data",
      "token refresh honours the connection proxy (#2703) returns null instead of throwing when the transport fails",
      "token refresh honours the connection proxy (#2703) still routes a refresh with no connection config through proxyAwareFetch"
    ],
    "tests/unit/refresh-runtime-persistence.test.js": [
      "refresh persistence revision and stop boundary does not persist a detached project lookup after the owner stops",
      "refresh persistence revision and stop boundary does not persist a refresh completing after cancellation",
      "refresh persistence revision and stop boundary drains background project enrichment before releasing its scheduler permit",
      "refresh persistence revision and stop boundary keeps background ownership until a cancelled redemption settles and never writes afterward",
      "refresh persistence revision and stop boundary preserves the selected command transport only while its account snapshot remains current",
      "refresh persistence revision and stop boundary returns the winning operator revision on conflict without continuing with stale tokens",
      "refresh persistence revision and stop boundary skips removed or no-longer-eligible background rows 0",
      "refresh persistence revision and stop boundary skips removed or no-longer-eligible background rows 1",
      "refresh persistence revision and stop boundary skips removed or no-longer-eligible background rows 2",
      "refresh persistence revision and stop boundary supplies the initiating persisted revision to the atomic writer"
    ],
    "tests/unit/refresh-runtime-provider-context.test.js": [
      "carries manager transport into specialized provider refresh and does not merge incompatible revisions",
      "coalesces direct calls on one transport and refuses a conflicting one",
      "does not reuse Kiro credentials under different client registration metadata",
      "keeps xAI discovery and redemption on the bounded refresh transport",
      "refuses a generic direct refresh under a changed proxy without a second redemption",
      "shares a physical token chain across compatible accounts while rejecting a repeated owner at a newer revision"
    ],
    "tests/unit/refresh-runtime-races.test.js": [
      "background stop and restart aborts active waiters and stops queued jobs, including a concurrent skipped tick",
      "background stop and restart cancels scheduled callbacks and explicit restart installs only one timer pair",
      "background stop and restart canonical credential revision excludes names but includes proxy settings and deactivation",
      "background stop and restart stops work when shutdown happens while the database read is pending",
      "refresh runtime concurrency and exclusivity bounds active token entries without evicting an in-flight one-use owner",
      "refresh runtime concurrency and exclusivity coalesces compatible token identities but refuses different revisions and proxy contexts",
      "refresh runtime concurrency and exclusivity does not coalesce the same connection after a credential revision changes",
      "refresh runtime concurrency and exclusivity keeps a one-use redemption owned after one waiter cancels",
      "refresh runtime concurrency and exclusivity runs only four background jobs at once and continues after failure"
    ],
    "tests/unit/refresh-runtime-repo.test.js": [
      "atomic credential revision guard allows unrelated name edits and preserves provider fields while applying a valid rotation",
      "atomic credential revision guard checks cancellation inside the transaction and leaves the row unchanged",
      "atomic credential revision guard rejects a late credential write after state changes 0",
      "atomic credential revision guard rejects a late credential write after state changes 1",
      "atomic credential revision guard rejects a late credential write after state changes 2",
      "atomic credential revision guard rejects a late credential write after state changes 3",
      "invalidates pool transport and strictness revisions, preserving revisions on unrelated pool edits",
      "invalidates revisions through reauthorization and same-account creation upserts",
      "never imports a trusted credential revision even when restoring identical bytes",
      "refuses token A to B to A and never trusts an injected revision ID"
    ],
    "tests/unit/regression-gate-contract.test.js": [
      "canonical regression gate accepts a valid report with an exact assertion inventory",
      "canonical regression gate classifies invalid input empty-report",
      "canonical regression gate classifies invalid input malformed-report",
      "canonical regression gate classifies invalid input missing-report",
      "canonical regression gate compares every declared assertion-status count with the inventory",
      "canonical regression gate does not let zero failed assertions override a failed suite",
      "canonical regression gate rejects assertion statuses outside the Vitest report contract",
      "canonical regression gate rejects an unexplained nonzero runner exit even when the report is green",
      "canonical regression gate rejects expired-exemption independently",
      "canonical regression gate rejects formerly-fixed-recurrence independently",
      "canonical regression gate rejects import-failure independently",
      "canonical regression gate rejects obsolete-snapshot independently",
      "canonical regression gate rejects removed-assertion independently",
      "canonical regression gate rejects removed-file independently",
      "canonical regression gate rejects setup-failure independently",
      "canonical regression gate rejects unclassified-skip independently",
      "canonical regression gate rejects unknown-failed-assertion independently",
      "canonical regression gate requires the raw runner exit instead of reconstructing it from the report"
    ],
    "tests/unit/replay-rejection-boundary.test.js": [
      "bounded final diagnostic inspection cancels the source and keeps unknown content unknown",
      "caller cancellation interrupts a stalled diagnostic body",
      "cancels a rejected response before an abortable retry delay and makes no second dispatch",
      "cancels oversized discarded diagnostics before a permitted retry",
      "does not replay an ambiguous HTTP408 against another URL",
      "does not replay an ambiguous HTTP409 against another URL",
      "does not replay an ambiguous HTTP500 against another URL",
      "does not replay an ambiguous HTTP502 against another URL",
      "does not replay an ambiguous HTTP503 against another URL",
      "does not replay an ambiguous HTTP504 against another URL",
      "does not replay an ambiguous HTTP507 against another URL",
      "honors x-should-retry denial before a configured retry or fallback",
      "honors x-tokenproxy-replay-safe denial before a configured retry or fallback",
      "requires an error rejection and never lets explicit true override a veto"
    ],
    "tests/unit/request-details-buffer-bound-1245.test.js": [
      "#1245 the observability write buffer is bounded does not buffer at all while observability is off",
      "#1245 the observability write buffer is bounded keeps the NEWEST entries, matching the retention sweep's own order",
      "#1245 the observability write buffer is bounded stops growing once the flush stalls, instead of tracking request count",
      "#1245 the observability write buffer is bounded the ceiling never falls below maxRecords, so no kept row is evicted early"
    ],
    "tests/unit/request-details-redaction.test.js": [
      "request-details redaction handles empty details",
      "request-details redaction keeps non-sensitive fields untouched",
      "request-details redaction removes conversation payloads but keeps metadata"
    ],
    "tests/unit/request-details-shutdown.test.js": [
      "bounds shutdown of a stuck adapter and never writes after the adapter-close boundary",
      "flushes every module instance before adapter close without multiplying process listeners",
      "joins an in-flight flush and subsequent buffered work before allowing adapter close"
    ],
    "tests/unit/request-details-tab.test.js": [
      "API route contract — validation boundary invalid startDate → route catches, returns 500 (not thrown)",
      "API route contract — validation boundary page=-5 → 400",
      "API route contract — validation boundary page=0 → 400 (guard now reachable after NaN-check fix)",
      "API route contract — validation boundary pageSize=101 → 400",
      "API route contract — validation boundary pageSize=abc (NaN) → defaults to 20, returns 200",
      "API route contract — validation boundary valid request → 200 with details + pagination shape",
      "backupDbLite — retains the complete database backup file includes requestDetails rows and all other tables",
      "getDistinctProviders — providers route (no full-row parse) returns unique provider list without parsing data blobs",
      "getDistinctProviders — providers route (no full-row parse) skips null providers, returns sorted",
      "request details — tab crash-risk cases corrupt data column → parseJson fallback {}, no throw",
      "request details — tab crash-risk cases invalid startDate → Invalid Date ISO throws inside getRequestDetails is caught upstream",
      "request details — tab crash-risk cases large pageSize (providers route uses 9999) → returns all, no crash",
      "request details — tab crash-risk cases missing tokens/timestamp on row → getInputTokens-style access safe",
      "request details — tab crash-risk cases oversized field → stored truncated + reparseable (no circular)",
      "request details — tab crash-risk cases pagination beyond last page → empty details, valid meta",
      "request details — tab crash-risk cases valid date filter range → no throw",
      "token helpers — render-time crash safety cached via cache_read_input_tokens alias",
      "token helpers — render-time crash safety empty object → 0 across all helpers",
      "token helpers — render-time crash safety legacy Claude row (prompt < cache) → returns cache",
      "token helpers — render-time crash safety prompt_tokens preferred, falls back to input_tokens",
      "token helpers — render-time crash safety toLocaleString on helper result never throws",
      "token helpers — render-time crash safety undefined/null tokens → 0, no throw"
    ],
    "tests/unit/request-error-visible-2221.test.js": [
      "a failed request keeps a readable reason (#2221) caps error text so a provider echoing the request back cannot dump it",
      "a failed request keeps a readable reason (#2221) handles a non-string error object and a missing status",
      "a failed request keeps a readable reason (#2221) redacts a successful response completely — no leak through the new branch",
      "a failed request keeps a readable reason (#2221) serves the upstream error text and status for a failed request",
      "a failed request keeps a readable reason (#2221) still redacts request bodies on that same failed row",
      "a failed request keeps a readable reason (#2221) treats a null error as no error at all"
    ],
    "tests/unit/request-lifetime.test.js": [
      "already aborted caller cannot dispatch an adapter retry",
      "cancels polling sleeps immediately and forbids a subsequent dispatch",
      "isolates concurrent caller contexts and composes timeout with caller abort"
    ],
    "tests/unit/request-logger-masking.test.js": [
      "request log header masking masks short credential values too",
      "request log header masking never writes a bearer token verbatim"
    ],
    "tests/unit/request-stats-repo.test.js": [
      "aggregation over the saved rows a time-window filter excludes rows outside it",
      "aggregation over the saved rows getStatsFilters returns the distinct dimensions with cascading linkage maps",
      "aggregation over the saved rows getStatsItems paginates newest-first with an honest page envelope",
      "aggregation over the saved rows getStatsSeries returns [] with no matching rows and conserves totals when bucketing",
      "aggregation over the saved rows getStatsSummary sums tokens and splits input from cache reads/writes",
      "aggregation over the saved rows getTrafficWindow counts errors and measured latencies inside the window only",
      "buildStatsWhere a scalar and an array both become IN clauses, empties are skipped",
      "buildStatsWhere no filter means no WHERE clause and no params",
      "buildStatsWhere start/end dates become ISO-normalized timestamp bounds",
      "saveRequestStats — the upsert is the streaming start/complete contract a second save with the same id updates the row in place (stream complete over stream start)",
      "saveRequestStats — the upsert is the streaming start/complete contract ignores a detail without an id and never throws on garbage",
      "saveRequestStats — the upsert is the streaming start/complete contract inserts a row with canonicalized token fields and defaults",
      "usageHistory backfill (fresh table path) backfills once from usageHistory when requestStats is empty, extracting cache splits from the tokens JSON"
    ],
    "tests/unit/request-stats-series-bounds.test.js": [
      "seriesBounds handles a large real-history timestamp set without spreading it into a call"
    ],
    "tests/unit/request-workbench-ui.test.js": [
      "opens with no network request, reaches every operation from the board, and leaves sending disabled without a key",
      "prepares a video poll with the exact returned ID and account, without polling automatically",
      "retains the input and stops observation without replay after cancellation",
      "validates without inference and only explicit sending consumes the transient password field"
    ],
    "tests/unit/request-workbench.test.js": [
      "native gateway operation contracts chat composes its implemented same-origin gateway path",
      "native gateway operation contracts embedding composes its implemented same-origin gateway path",
      "native gateway operation contracts fetch composes its implemented same-origin gateway path",
      "native gateway operation contracts gemini composes its implemented same-origin gateway path",
      "native gateway operation contracts image-edit composes its implemented same-origin gateway path",
      "native gateway operation contracts image-generate composes its implemented same-origin gateway path",
      "native gateway operation contracts messages composes its implemented same-origin gateway path",
      "native gateway operation contracts moderation composes its implemented same-origin gateway path",
      "native gateway operation contracts ocr composes its implemented same-origin gateway path",
      "native gateway operation contracts ollama composes its implemented same-origin gateway path",
      "native gateway operation contracts preserves advanced native options and sets streaming at the Gemini URL boundary",
      "native gateway operation contracts refuses nonexistent Gemini edit semantics, unsupported video adapters and image URL/mask shortcuts",
      "native gateway operation contracts rejects invalid JSON shapes, credential fields, image bounds and malformed rerank inputs before fetch",
      "native gateway operation contracts repeats STT granularities and leaves multipart Content-Type to the browser",
      "native gateway operation contracts rerank composes its implemented same-origin gateway path",
      "native gateway operation contracts responses composes its implemented same-origin gateway path",
      "native gateway operation contracts retains video account binding, idempotency and opaque poll identity",
      "native gateway operation contracts search composes its implemented same-origin gateway path",
      "native gateway operation contracts speech composes its implemented same-origin gateway path",
      "native gateway operation contracts transcription composes its implemented same-origin gateway path",
      "native gateway operation contracts video-edit composes its implemented same-origin gateway path",
      "native gateway operation contracts video-extend composes its implemented same-origin gateway path",
      "native gateway operation contracts video-generate composes its implemented same-origin gateway path",
      "native gateway operation contracts video-poll composes its implemented same-origin gateway path",
      "native gateway operation contracts voices composes its implemented same-origin gateway path",
      "one explicit cancellable request with bounded evidence keeps large JSON response data within the byte bound while bounding only its visible text",
      "one explicit cancellable request with bounded evidence never retries HTTP refusals, aborts or uncertain transport failures",
      "one explicit cancellable request with bounded evidence rejects credentials misplaced into request content and non-gateway targets without a call",
      "one explicit cancellable request with bounded evidence sends one bearer request, refuses redirects, and redacts body/header/escaped echoes before export",
      "one explicit cancellable request with bounded evidence stops oversized response reading and never offers truncated binary as a complete download"
    ],
    "tests/unit/require-api-key-env-2834.test.js": [
      "REQUIRE_API_KEY actually enforces the gate (#2834) can only tighten, never loosen",
      "REQUIRE_API_KEY actually enforces the gate (#2834) is read where the gate is decided",
      "REQUIRE_API_KEY actually enforces the gate (#2834) SECURITY.md no longer says it enforces nothing",
      "REQUIRE_API_KEY actually enforces the gate (#2834) still rejects a missing and an invalid key once required"
    ],
    "tests/unit/required-unavailable-callers.test.js": [
      "required proxy unavailable caller boundaries auto ping forwards selected strict options without permitting fallback",
      "required proxy unavailable caller boundaries auto ping skips unavailable selection before refresh or usage",
      "required proxy unavailable caller boundaries Codex reset forwards selected strict options without permitting fallback",
      "required proxy unavailable caller boundaries Codex reset GET returns 503 before refresh or reset-credit egress",
      "required proxy unavailable caller boundaries Codex reset POST returns 503 before refresh or reset-credit egress",
      "required proxy unavailable caller boundaries Cursor v1 catalog keeps an available strict route instead of falling back",
      "required proxy unavailable caller boundaries hot reload forwards selected strict options before its first upstream operation",
      "required proxy unavailable caller boundaries hot reload returns 503 before credential refresh or poke",
      "required proxy unavailable caller boundaries model context serves local inventory without touching an unavailable Cursor proxy",
      "required proxy unavailable caller boundaries new-model discovery rejects an unavailable Cursor selection before returning its cached fallback",
      "required proxy unavailable caller boundaries new-model discovery retains a valid cache when connection lookup is unavailable",
      "required proxy unavailable caller boundaries non-Cursor v1 models retain their normal static result",
      "required proxy unavailable caller boundaries provider Cursor models returns 503 before catalog or static fallback",
      "required proxy unavailable caller boundaries provider test forwards selected strict options without direct fallback",
      "required proxy unavailable caller boundaries provider test returns its typed failed-test shape before proxy testing",
      "required proxy unavailable caller boundaries quota guard preserves a usable strict proxy selection",
      "required proxy unavailable caller boundaries quota guard returns its no-live typed result before usage, even with a fresh snapshot",
      "required proxy unavailable caller boundaries usage route forwards selected strict options without permitting fallback",
      "required proxy unavailable caller boundaries usage route returns 503 before credential refresh or usage",
      "required proxy unavailable caller boundaries v1 combo-only models returns 503 before a static Cursor fallback",
      "required proxy unavailable caller boundaries v1 Cursor models returns 503 before catalog or static fallback",
      "required proxy unavailable caller boundaries v1 non-LLM models returns 503 before a static Cursor fallback"
    ],
    "tests/unit/rerank-endpoint-936.test.js": [
      "a malformed upstream answer degrades rather than throws a body with no ranked list yields an empty result set, not a crash",
      "a malformed upstream answer degrades rather than throws an upstream error is surfaced with its status, not swallowed as success",
      "bad input is refused before any upstream call a document that is neither a string nor { text }",
      "bad input is refused before any upstream call a provider with no rerank API is a 400 naming it, not a call to a guessed URL",
      "bad input is refused before any upstream call blank query",
      "bad input is refused before any upstream call empty documents",
      "bad input is refused before any upstream call missing documents",
      "bad input is refused before any upstream call missing query",
      "the contract is Cohere's, not one invented here (#936) accepts a bare string or Cohere v1's { text }, and names the index that is neither",
      "the contract is Cohere's, not one invented here (#936) every provider in the table is one this repo already has a registry row for",
      "the contract is Cohere's, not one invented here (#936) routes the vendors that publish a rerank API and refuses the ones that do not",
      "the endpoint is actually wired to /v1 the route and its handler resolve, so a new file is not silently unreachable",
      "usage is recorded only when the vendor states it exactly reads each vendor's own field",
      "usage is recorded only when the vendor states it exactly refuses to invent a count, so nothing is billed on a guess",
      "what the client sends reaches the vendor in the vendor's own spelling return_documents means the same thing on a vendor that never echoes them",
      "what the client sends reaches the vendor in the vendor's own spelling sends Cohere top_n, and does not send return_documents Cohere v2 rejects",
      "what the client sends reaches the vendor in the vendor's own spelling translates the same request to Voyage's top_k and reads back its data array"
    ],
    "tests/unit/rerank-handler-auth-fallback.test.js": [
      "dispatch, fallback and lease hygiene a bare combo name expands through handleComboChat and answers from the first working member",
      "dispatch, fallback and lease hygiene a non-fallback failure returns the core's own response with its status",
      "dispatch, fallback and lease hygiene a resolved bare alias retains its physical model despite a connection default",
      "dispatch, fallback and lease hygiene allRateLimited returns the unavailable envelope with retry info and no core call",
      "dispatch, fallback and lease hygiene happy path: core called with provider/model split, usage persisted, lease released",
      "dispatch, fallback and lease hygiene lease released even when the core throws",
      "dispatch, fallback and lease hygiene no credentials at all is a 400 naming the provider",
      "dispatch, fallback and lease hygiene no usage in the core result → nothing persisted (no phantom rows)",
      "dispatch, fallback and lease hygiene rotates to the next account when markAccountUnavailable says fallback, excluding the failed one",
      "dispatch, fallback and lease hygiene when accounts run out, the LAST upstream error and status are reported, not a generic 500",
      "trust-boundary validation 400 on an unresolvable model name",
      "trust-boundary validation 400 on invalid JSON body",
      "trust-boundary validation 400 on missing model, query, documents — each named",
      "trust-boundary validation 401 when requireApiKey is on and the key does not validate",
      "trust-boundary validation 403 when the key's model allowlist excludes the model (quota protection #1154)"
    ],
    "tests/unit/resolved-ssrf-guard.test.js": [
      "resolved SSRF guard allows a public DNS result 2606:2800:220:1:248:1893:25c8:1946",
      "resolved SSRF guard allows a public DNS result 93.184.216.34",
      "resolved SSRF guard blocks a redirected IP literal at the socket connector boundary",
      "resolved SSRF guard blocks the real fetch socket before it reaches a DNS-rebound private service",
      "resolved SSRF guard rejects a hostname when any DNS answer is private",
      "resolved SSRF guard rejects a private DNS result ::1",
      "resolved SSRF guard rejects a private DNS result ::ffff:7f00:1",
      "resolved SSRF guard rejects a private DNS result 10.0.0.1",
      "resolved SSRF guard rejects a private DNS result 127.0.0.1",
      "resolved SSRF guard rejects a private DNS result 169.254.169.254",
      "resolved SSRF guard rejects a private DNS result fc00::1",
      "resolved SSRF guard rejects a private DNS result fe80::1",
      "resolved SSRF guard rejects IPv4-compatible or mapped private address http://[::7f00:1]",
      "resolved SSRF guard rejects IPv4-compatible or mapped private address http://[::ffff:7f00:1]:19998",
      "resolved SSRF guard rejects IPv4-compatible or mapped private address http://[::ffff:a9fe:a9fe]/latest/meta-data",
      "resolved SSRF guard rejects IPv4-compatible or mapped private address http://[0:0:0:0:0:ffff:7f00:1]",
      "resolved SSRF guard returns public DNS answers to the socket lookup"
    ],
    "tests/unit/resource-admission.test.js": [
      "measured bounded adaptation long stream duration and missing evidence never create congestion or recovery",
      "measured bounded adaptation minimum samples, smoothing, cooldown, hysteresis and gradual recovery resist oscillation",
      "measured bounded adaptation operator override is bounded and existing streams survive contraction",
      "measured bounded adaptation provider reservation is atomic, bounded and releases independently",
      "quota and provider evidence qualification ignores stale quota, retains only fresh pause evidence and expires it",
      "quota and provider evidence qualification throttle recovery advances one provider permit per cooldown without oscillation",
      "resource admission lifecycle and fairness body failures, client cancellation and bodyless responses finalize once",
      "resource admission lifecycle and fairness body pull preserves backpressure and EOF releases exactly once",
      "resource admission lifecycle and fairness caller abort cancels an unread upstream and frees its permit",
      "resource admission lifecycle and fairness cancels queued clients immediately and honors absolute deadlines",
      "resource admission lifecycle and fairness client FIFO and bounded queues prevent monopolization",
      "resource admission lifecycle and fairness holds stream capacity beyond handler return; release is idempotent"
    ],
    "tests/unit/response-header-timeout.test.js": [
      "response header timeout aborts with a typed error and clears idempotently",
      "response header timeout forwards a later caller abort and removes its timer",
      "response header timeout forwards an already-aborted caller without timeout reclassification",
      "response header timeout keeps caller cancellation connected after response headers clear the timer",
      "response header timeout rejects invalid timeout 120001",
      "response header timeout rejects invalid timeout 15000",
      "response header timeout rejects invalid timeout 15000.5",
      "response header timeout rejects invalid timeout 999",
      "response header timeout rejects invalid timeout Infinity",
      "response header timeout rejects invalid timeout NaN",
      "response header timeout resolves executor precedence before creating the timer",
      "response header timeout restores the caller reason when transport wraps the abort"
    ],
    "tests/unit/responses-abort-terminal.test.js": [
      "Responses abort terminal synthesis does not synthesize terminal for non-Responses streams (callback null)",
      "Responses abort terminal synthesis emits response.failed + [DONE] when upstream errors (abort/stall)"
    ],
    "tests/unit/responses-api-format-coverage.test.js": [
      "convertResponsesApiFormat converts instructions, messages, function calls and outputs into chat shape",
      "convertResponsesApiFormat flushes a trailing assistant tool-call turn and trailing tool results",
      "convertResponsesApiFormat flushes pending tool results before a following message and passes unknown content parts through",
      "convertResponsesApiFormat returns the body untouched without input, or when input is invalid",
      "hoistAdditionalTools bounds namespace recursion depth so a hostile body cannot loop",
      "hoistAdditionalTools hoists tools, unwraps namespaces, drops the item, and merges into existing tools[]",
      "hoistAdditionalTools no-ops without an input array or without additional_tools items",
      "normalizePassthroughToolSchemas ignores a body without a tools array",
      "normalizePassthroughToolSchemas leaves non-function and parameterless tools untouched by identity",
      "normalizeResponsesInput injects a placeholder for an empty array and passes a populated array through",
      "normalizeResponsesInput returns null for anything else",
      "normalizeResponsesInput wraps a string as a user message and replaces an empty string with a placeholder",
      "normalizeToolParameters returns a fresh object when a strip is needed, leaving the caller's schema intact",
      "normalizeToolParameters returns an empty object schema for falsy input",
      "normalizeToolParameters returns the same reference when nothing needs stripping",
      "stripUnsupportedSchemaKeywords removes lookaround patterns and encrypted flags recursively, including arrays",
      "stripUnsupportedSchemaKeywords tolerates null and primitive nodes",
      "typeResponsesInputItems is idempotent on already-typed items and no-ops without input",
      "typeResponsesInputItems types an untyped role item and converts a string content to a typed part array"
    ],
    "tests/unit/responses-early-stream-2666.test.js": [
      "Responses early streaming bridge (#2666) cancels an explicit Responses request while routing without scheduling account fallback",
      "Responses early streaming bridge (#2666) converts a delayed non-SSE failure into a safe Responses terminal without provider text",
      "Responses early streaming bridge (#2666) does not fall back when a cancelled bridge receives a late empty success",
      "Responses early streaming bridge (#2666) does not fall back when cancellation arrives during the empty-stream peek",
      "Responses early streaming bridge (#2666) keeps a supplied bridge signal through nested combo callbacks",
      "Responses early streaming bridge (#2666) keeps explicit Responses streaming live while passing the parsed body and bridge signal to chat",
      "Responses early streaming bridge (#2666) leaves non-explicit stream=\"true\" on the normal response path",
      "Responses early streaming bridge (#2666) leaves non-explicit stream=false on the normal response path",
      "Responses early streaming bridge (#2666) leaves non-explicit stream=undefined on the normal response path",
      "Responses early streaming bridge (#2666) opens a real SSE response before routing settles and forwards successful bytes unchanged"
    ],
    "tests/unit/responses-effort-nesting-3154.test.js": [
      "reasoning_effort is nested by wire format, not by provider (#3154) /v1/responses really does resolve to the format the guard names",
      "reasoning_effort is nested by wire format, not by provider (#3154) codex is still covered by the widened guard",
      "reasoning_effort is nested by wire format, not by provider (#3154) it preserves a reasoning object the client already sent",
      "reasoning_effort is nested by wire format, not by provider (#3154) it still deletes the flat field after nesting",
      "reasoning_effort is nested by wire format, not by provider (#3154) openai is a separate provider, which is why the old guard missed it",
      "reasoning_effort is nested by wire format, not by provider (#3154) the guard keys on the format constant"
    ],
    "tests/unit/responses-encrypted-schema-1758.test.js": [
      "a tool schema's `encrypted: true` does not 400 the request (#1758) does not mutate the caller's schema, which a combo reuses across providers",
      "a tool schema's `encrypted: true` does not 400 the request (#1758) keeps the rest of the schema and the other tool declarations intact",
      "a tool schema's `encrypted: true` does not 400 the request (#1758) leaves a schema with no encrypted keyword completely untouched",
      "a tool schema's `encrypted: true` does not 400 the request (#1758) strips encrypted on the same-format Responses passthrough",
      "a tool schema's `encrypted: true` does not 400 the request (#1758) strips encrypted when pivoting Responses -> Chat Completions"
    ],
    "tests/unit/responses-input-item-type-3390.test.js": [
      "every Responses input item is typed before it leaves (#3390) gives a typed message the content-part array the item union requires",
      "every Responses input item is typed before it leaves (#3390) is a no-op on a chat-completions body with no input[]",
      "every Responses input item is typed before it leaves (#3390) leaves an already-typed item and a non-message item untouched",
      "every Responses input item is typed before it leaves (#3390) types an untyped item on the same-format Responses passthrough",
      "every Responses input item is typed before it leaves (#3390) types an untyped item when a chat-shaped caller already carries input[]"
    ],
    "tests/unit/responses-lookaround-schema-1556.test.js": [
      "a lookaround pattern does not 400 the request (#1556) does not mutate the caller's schema, which a combo reuses",
      "a lookaround pattern does not 400 the request (#1556) drops a positive lookahead",
      "a lookaround pattern does not 400 the request (#1556) drops the other lookaround forms",
      "a lookaround pattern does not 400 the request (#1556) keeps a pattern the validator accepts",
      "a lookaround pattern does not 400 the request (#1556) keeps the rest of the schema intact",
      "a lookaround pattern does not 400 the request (#1556) leaves a schema with no lookaround completely untouched"
    ],
    "tests/unit/responses-only-fields-strip-2318.test.js": [
      "Responses-API-only fields never reach a Chat Completions upstream (#2318) keeps the converted messages and unrelated sampling fields",
      "Responses-API-only fields never reach a Chat Completions upstream (#2318) still strips the fields that were already handled",
      "Responses-API-only fields never reach a Chat Completions upstream (#2318) strips background",
      "Responses-API-only fields never reach a Chat Completions upstream (#2318) strips client_metadata",
      "Responses-API-only fields never reach a Chat Completions upstream (#2318) strips truncation"
    ],
    "tests/unit/responses-output-index-2611.test.js": [
      "Responses output_index identifies the item, not the choice (#2611) gives reasoning, message and tool call distinct output indices",
      "Responses output_index identifies the item, not the choice (#2611) keeps an item's own events on one index",
      "Responses output_index identifies the item, not the choice (#2611) keeps every produced item in the completed output array"
    ],
    "tests/unit/responses-prompt-cache-key-3216.test.js": [
      "#3216 prompt_cache_key across the chat/responses translation does not invent a key when the client sent none",
      "#3216 prompt_cache_key across the chat/responses translation preserves an explicit key when converting chat → responses",
      "#3216 prompt_cache_key across the chat/responses translation still drops the key on the responses → chat direction"
    ],
    "tests/unit/responses-reasoning-summary-3571.test.js": [
      "rebuilt reasoning items always carry summary (#3571) emits summary: [] for a reasoning turn that only has encrypted_content",
      "rebuilt reasoning items always carry summary (#3571) leaves every rebuilt reasoning item summary-bearing across a full agentic turn",
      "rebuilt reasoning items always carry summary (#3571) still carries the text when the turn had a reasoning summary"
    ],
    "tests/unit/responses-shape-nonstream-2885.test.js": [
      "a Responses client gets output[], whatever the provider speaks (#2885) converts a claude reply for a Responses client",
      "a Responses client gets output[], whatever the provider speaks (#2885) converts a ollama reply for a Responses client",
      "a Responses client gets output[], whatever the provider speaks (#2885) matches what the gemini branch already did",
      "a Responses client gets output[], whatever the provider speaks (#2885) still returns choices[] for an claude reply to an OpenAI client",
      "a Responses client gets output[], whatever the provider speaks (#2885) still returns choices[] for an ollama reply to an OpenAI client"
    ],
    "tests/unit/responses-stream-to-json-usage.test.js": [
      "convertResponsesStreamToJson usage passthrough folds a flat cached_tokens field into input_tokens_details",
      "convertResponsesStreamToJson usage passthrough folds a flat reasoning_tokens field into output_tokens_details and recomputes a missing total",
      "convertResponsesStreamToJson usage passthrough keeps exact provider cost for internal persistence without serializing it",
      "convertResponsesStreamToJson usage passthrough leaves a supplied reader release to its outer deadline owner",
      "convertResponsesStreamToJson usage passthrough maps Anthropic-style cache_read_input_tokens to cached_tokens",
      "convertResponsesStreamToJson usage passthrough preserves OpenAI Responses input/output token details (codex shape)"
    ],
    "tests/unit/responses-tool-carry-1534.test.js": [
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) carries the request-scoped tool metadata onto the Claude body",
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) declares the namespaced MCP tool by its own name, not an empty placeholder",
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) leaves a plain function tool exactly as it was — the half that never broke",
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) resolves nothing when the request declared no tools",
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) returns apply_patch as a custom_tool_call the Codex CLI can run (#1707)",
      "Responses tool identity survives the pivot to a Claude upstream (#1534, #1707) returns the MCP call under its declared name and namespace (#1534)"
    ],
    "tests/unit/responses-transform-stream.test.js": [
      "Responses API transform delivers an event that arrived without its blank-line terminator",
      "Responses API transform emits one completed event, not one per flush path",
      "Responses API transform is unchanged for a well-formed stream",
      "Responses API transform survives a multi-byte character split across two network chunks"
    ],
    "tests/unit/responses-transformer-flush-and-buffer.test.js": [
      "a \"[DONE]\" data line inside the stream is skipped, not parsed as JSON",
      "a message left in the buffer with no trailing blank-line terminator is still emitted at flush",
      "a whitespace-only message segment between two real ones is skipped without emitting anything for it",
      "an all-blank buffer at flush time does not replay a bogus empty message",
      "logger.logInput receives the trimmed raw chunk text when a logger is supplied",
      "reasoning left open with no finish_reason is still closed at flush"
    ],
    "tests/unit/responses-transformer-idempotent-close.test.js": [
      "finish_reason closes everything once; flush() must not re-close a stream with reasoning + text + tool_calls, closed via finish_reason, then flushed, emits each done event exactly once",
      "finish_reason closes everything once; flush() must not re-close two finish_reason chunks in a row do not double-emit any done event",
      "SSE data-line regex: exact accepted/rejected shapes a message with usage but no choices updates usage and is still reflected in the completed event",
      "SSE data-line regex: exact accepted/rejected shapes does not match a line where \"data:\" is not at the start (leading garbage)",
      "SSE data-line regex: exact accepted/rejected shapes matches \"data:   \" with multiple leading spaces before the payload",
      "SSE data-line regex: exact accepted/rejected shapes matches \"data:\" with no space before the payload"
    ],
    "tests/unit/responses-transformer-logger.test.js": [
      "createResponsesLogger creates the log directory recursively under the given logsDir with a responses_<model>_ prefix",
      "createResponsesLogger flush swallows a writeFileSync failure rather than throwing",
      "createResponsesLogger logInput/logOutput buffer events and flush writes both files newline-joined",
      "createResponsesLogger returns null in a worker environment where fs.mkdirSync is not a function",
      "createResponsesLogger returns null without throwing when mkdirSync throws"
    ],
    "tests/unit/responses-transformer-payload-shape.test.js": [
      "full lifecycle: reasoning, then message, then tool call, then finish emits the exact payload shape at every step",
      "regex and control-flow edge cases not covered by the shape assertions above a chunk with an empty choices array carries no length and is skipped without throwing",
      "regex and control-flow edge cases not covered by the shape assertions above a data: line with no trailing content after the colon is skipped, not treated as empty payload",
      "regex and control-flow edge cases not covered by the shape assertions above a message with only whitespace between blank lines produces no events",
      "regex and control-flow edge cases not covered by the shape assertions above a tool_calls entry index defaults to 0 when omitted",
      "regex and control-flow edge cases not covered by the shape assertions above choice.index defaults to 0, not left undefined, when the upstream omits it"
    ],
    "tests/unit/responses-transformer-reasoning-and-misc.test.js": [
      "buffer split regex: message boundary is exactly a blank line (\"\\n\\n\") a single blank-line-separated pair of messages both process",
      "closeMessage/closeToolCall field fallbacks closeMessage falls back to an empty string fullText when nothing was ever buffered for that idx",
      "closeMessage/closeToolCall field fallbacks closeToolCall falls back to \"{}\" arguments and empty name when never set",
      "createResponsesLogger directory naming the log dir timestamp keeps only digits (colons and dots stripped), 15 chars",
      "createResponsesLogger directory naming two loggers created back to back get different uniqueId suffixes",
      "emitReasoningDelta empty-text guard an empty-string reasoning_content chunk after startReasoning emits no delta event",
      "flush() trailing-buffer replay a message left in the buffer with no closing blank line is still processed at stream end",
      "flush() trailing-buffer replay an empty trailing buffer (nothing after the last blank line) triggers no extra transform call",
      "startReasoning is idempotent: a second call does not re-open or move the index two reasoning_content chunks emit exactly one output_item.added for reasoning",
      "think-tag splitting produces text after the closing tag \"<think>reason</think>answer\" in one delta closes reasoning and emits \"answer\" as message text",
      "think-tag splitting produces text after the closing tag a </think> split across content containing another literal \"</think>\" keeps the remainder joined, not truncated at the first occurrence",
      "toResponsesUsage null-object guard a non-object usage value (a string) produces no usage on response.completed"
    ],
    "tests/unit/responses-transformer-stream.test.js": [
      "lifecycle and text path a final message left in the buffer without its terminator is still processed on flush",
      "lifecycle and text path a multi-byte UTF-8 character split across chunks survives intact",
      "lifecycle and text path an SSE message split across network chunks is buffered, not dropped",
      "lifecycle and text path emits created + in_progress once, deltas, and completes on flush without finish_reason",
      "lifecycle and text path invalid data lines and [DONE] from upstream are skipped silently",
      "lifecycle and text path sequence numbers increase monotonically across every event",
      "reasoning <think> tags split reasoning from text even across separate deltas",
      "reasoning reasoning_content deltas open a reasoning item and close with the full text",
      "tool calls a stream ending mid-tool-call still emits the done events on flush (no dangling call)",
      "tool calls a text message followed by a tool_call closes the message before the call opens",
      "tool calls buffers streamed arguments per tool index and closes with the concatenated JSON",
      "usage mapping (cost and context accounting) maps prompt/completion tokens with cached and reasoning details into response.completed",
      "usage mapping (cost and context accounting) missing total_tokens is derived from the sum; absent details are omitted",
      "usage mapping (cost and context accounting) no usage from upstream → completed carries no usage key (nothing fabricated)"
    ],
    "tests/unit/responses-transformer-tool-call-edges.test.js": [
      "a repeated tool_calls delta for the same index does not re-open the item or double the output index two chunks for tcIdx 0, first with id, second with only arguments, share one output_item.added",
      "nextOutputIndex only ever increases (no decrement on any path) reasoning then a tool call get strictly increasing, never-reused output indices",
      "sequence_number strictly increments by 1, starting at 1 every emitted event carries a monotonically increasing sequence_number",
      "tool_calls arguments delta guarded by tc.function?.arguments truthiness a tool_calls chunk with no function.arguments emits no arguments.delta event",
      "tool_calls.index uses ?? 0, not && 0 (an explicit index:0 must not be dropped) a tool_calls entry with index explicitly 0 opens output_index 0, not undefined"
    ],
    "tests/unit/responses-transformer-toolcall-init.test.js": [
      "arguments.delta guard: no id ever seen for the index an arguments-only chunk with no prior id and no id on this chunk emits no arguments.delta",
      "closeReasoning fires even when the closing tag leaves an empty thinkPart \"<think></think>x\" still emits reasoning_summary_text.done",
      "function_call open-time name fallback a tool_calls entry with no function.name opens with name \"\""
    ],
    "tests/unit/responses-transformer-usage.test.js": [
      "Responses API transformer usage includes final Chat Completions usage in response.completed",
      "Responses API transformer usage preserves transformed usage in a non-streaming Responses body"
    ],
    "tests/unit/retry-config-env-1524.test.js": [
      "DEFAULT_RETRY_CONFIG env overrides (#1524) accepts an explicit 0 as 'stop retrying this status'",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a above the ceiling value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a empty value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a fractional value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a Infinity value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a NaN value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a negative value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a not-a-number value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default attempts on a whitespace value",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default delay on -100",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default delay on 1e999",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default delay on 600001",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) falls back to the default delay on abc",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) keeps the shipped defaults when nothing is set",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) lets an operator switch 429 retries on, which ship off by default",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) overrides attempts and delay per status code, leaving the others alone",
      "DEFAULT_RETRY_CONFIG env overrides (#1524) stays the config the retry loop actually reads"
    ],
    "tests/unit/routing-controls-ui.test.js": [
      "guards a mixed bulk set/remove using reviewed keys and accepts unrelated readback siblings",
      "invalidates an inline bulk review when its visible values change",
      "rebases a retained automatic draft after conflict and requires renewed review before saving",
      "reports post-commit persistence uncertainty even when the current process reads changed override values",
      "retains automatic rules and blocks an interrupted submission",
      "reviews all three automatic rules and reads the exact local receipt after saving"
    ],
    "tests/unit/routing-plan-simulator.test.js": [
      "applies captured agent role groups without reordering their intersection",
      "offline route, draft and session plans applies alias target revisions and exact configuration diffs to session consequences",
      "offline route, draft and session plans matches real controlled fallback dispatcher order and never reports a served model",
      "offline route, draft and session plans plans a fusion panel explicitly without fabricating a completed judge selection",
      "offline route, draft and session plans preserves captured round-robin and resets draft activation cursor without mutating runtime cursor",
      "offline route, draft and session plans refuses stale revisions, mutated captures, unsupported session policies and cycles",
      "offline route, draft and session plans shares capability/context ordering and excludes disabled members with ordered skip evidence",
      "offline route, draft and session plans supports clear/expiry at the next request boundary without implying a forced account change",
      "offline route, draft and session plans uses the runtime automatic selection contract against a captured catalog",
      "shows both cascade stages when raw request/session facts are unknown and applies explicit hypothetical facts"
    ],
    "tests/unit/routing-simulator-api.test.js": [
      "bounds declared and actual bodies, query fields, IDs, unsupported routes and malformed nested state",
      "captures and simulates versioned routes and session effects without repository writes or transport",
      "captures real repository state and exact existing pin, validates and simulates without writes or leases",
      "evaluates automatic rules on the local catalog without invoking provider discovery",
      "refuses an expired sealed capture and changed active configuration",
      "refuses captured configuration and stored draft drift before replay",
      "requires operator class and loopback before reading capture bodies",
      "resolves configured prefix shadows and enforces that node account policy"
    ],
    "tests/unit/routing-simulator-local-catalog.test.js": [
      "imports and builds the persisted catalog without importing live provider discovery"
    ],
    "tests/unit/routing-simulator-parity.test.js": [
      "real credential selector parity with frozen quota transport covers bounded randomized eligibility, quota horizons, pins, capacity and policy fixtures",
      "real credential selector parity with frozen quota transport keeps a pinned account with a temporary failure, and respects operator exclusions and model allowlists"
    ],
    "tests/unit/routing-simulator.test.js": [
      "immutable offline account policy applies model scoped quota, disable inheritance and explicit account overrides",
      "immutable offline account policy does not invent hard account gates for context, reasoning or modalities",
      "immutable offline account policy does not make mismatched lock metadata authoritative by normalizing timestamp spelling",
      "immutable offline account policy keeps a pinned account carrying a temporary failure, and still honours an operator disable",
      "immutable offline account policy orders longest horizons before short resets and load, with healthy pins stable",
      "immutable offline account policy preserves absence and unknown primitive quota values instead of manufacturing a model lock",
      "immutable offline account policy preserves legacy quota holding nuance and rotates scoped depletion to an eligible alternative",
      "immutable offline account policy rejects tampering, unsupported versions, unrelated models and smuggled fields",
      "immutable offline account policy shares versioned draft validation without activating or claiming combo simulation",
      "immutable offline account policy strips credentials, names, proxy endpoints and raw errors before sealing",
      "immutable offline account policy uses shorter horizons to break long horizon ties, then load, with capacity spill only for a new pin",
      "immutable offline account policy waits on a healthy pin at capacity and does not reserve or change captured state"
    ],
    "tests/unit/rtk-double-compression-3566.test.js": [
      "RTK compresses the caller's messages in place (#3566) is why a second attempt on the same body compresses twice"
    ],
    "tests/unit/rtk-error-shapes.test.js": [
      "RTK preserves failed tool results in every shape compresses an ordinary OpenAI tool message, array content",
      "RTK preserves failed tool results in every shape compresses an ordinary OpenAI tool message, string content",
      "RTK preserves failed tool results in every shape compresses an ordinary Responses function_call_output",
      "RTK preserves failed tool results in every shape honours the Kiro isError spelling on a tool_result block",
      "RTK preserves failed tool results in every shape leaves a function_call_output marked is_error alone",
      "RTK preserves failed tool results in every shape leaves a function_call_output marked status:error alone",
      "RTK preserves failed tool results in every shape leaves an array-content tool message marked status:error alone",
      "RTK preserves failed tool results in every shape leaves an OpenAI tool message marked is_error alone",
      "RTK preserves failed tool results in every shape leaves an OpenAI tool message marked status:error alone",
      "RTK preserves failed tool results in every shape still honours is_error on a Claude tool_result block"
    ],
    "tests/unit/rtk.e2e.test.js": [
      "RTK end-to-end compresses git diff tool_result and writes [RTK] savings to log",
      "RTK end-to-end compresses grep-style tool_result",
      "RTK end-to-end rtkEnabled flag is true (user must enable via dashboard)",
      "RTK end-to-end server is reachable"
    ],
    "tests/unit/rtk.multi-provider.e2e.test.js": [
      "RTK multi-provider E2E compresses git diff for antigravity (ag/* → openai→antigravity)",
      "RTK multi-provider E2E compresses git diff for claude (cc/* → openai→claude)",
      "RTK multi-provider E2E compresses git diff for codex (cx/* → openai→openai-responses)",
      "RTK multi-provider E2E compresses git diff for cursor (cu/* → openai→cursor)",
      "RTK multi-provider E2E compresses git diff for deepseek (deepseek/* → openai, passthrough)",
      "RTK multi-provider E2E compresses git diff for gemini (gemini/* → openai→gemini)",
      "RTK multi-provider E2E compresses git diff for kiro (kr/* → openai→kiro)",
      "RTK multi-provider E2E compresses git diff for ollama (ollama/* → openai→ollama)",
      "RTK multi-provider E2E server reachable and rtkEnabled=true"
    ],
    "tests/unit/rtk.test.js": [
      "autoDetectFilter (extras) detects Cursor search list",
      "autoDetectFilter (extras) detects ls via total + perms rows",
      "autoDetectFilter (extras) detects tree via box-drawing glyphs",
      "autoDetectFilter detects find",
      "autoDetectFilter detects git diff",
      "autoDetectFilter detects git log via commit header",
      "autoDetectFilter detects git status",
      "autoDetectFilter detects grep",
      "autoDetectFilter falls back to dedupLog for generic text",
      "compressMessages (disabled) returns null when disabled",
      "compressMessages (enabled) compresses Claude array-form tool_result text parts",
      "compressMessages (enabled) compresses Claude string-form tool_result",
      "compressMessages (enabled) compresses OpenAI tool message (string content)",
      "compressMessages (enabled) handles mix of messages without crashing",
      "compressMessages (enabled) never produces empty content (R14 guard)",
      "compressMessages (enabled) skips below MIN_COMPRESS_SIZE (<500 bytes)",
      "compressMessages (enabled) skips is_error tool_result",
      "compressMessages (enabled) skips when body has no messages",
      "formatRtkLog formats savings line with percentage",
      "formatRtkLog returns null when no hits",
      "gitLog filter compresses git log --oneline without losing commit subjects",
      "gitLog filter drops merge commit line ('Merge: abc1234 def5678')",
      "gitLog filter handles git log --graph without --oneline (graph-prefixed commit headers)",
      "gitLog filter keeps commit header + subject in default git log, drops body detail",
      "gitLog filter keeps stat-summary lines verbatim",
      "gitLog filter preserves input when compressed output inflates",
      "gitLog filter replaces embedded diff markers with '... diff body omitted'",
      "gitLog filter returns empty string for empty input",
      "gitLog filter returns empty string for null/undefined input",
      "gitLog filter strips graph-only decoration but keeps commit subjects",
      "gitLog filter truncates beyond maxLines and reports skipped count",
      "RTK filters (extras) ls: compact_ls strips perms/owner, keeps name + size",
      "RTK filters (extras) ls: filters noise dirs",
      "RTK filters (extras) readNumbered: compacts very long line-numbered dump",
      "RTK filters (extras) searchList: groups Cursor Glob output by parent dir",
      "RTK filters (extras) smartTruncate: keeps head+tail, drops middle",
      "RTK filters (extras) smartTruncate: passes through small input",
      "RTK filters (extras) tree: removes summary, keeps structure",
      "RTK filters dedupLog collapses consecutive duplicates",
      "RTK filters find groups paths by parent dir, shows basenames (Rust format)",
      "RTK filters gitDiff truncates hunks beyond 100 lines and preserves file header",
      "RTK filters gitStatus groups by kind and produces compact output (Rust format)",
      "RTK filters grep groups matches by file and caps per-file lines (Rust format)",
      "safeApply returns input if filter returns non-string",
      "safeApply returns input if filter throws"
    ],
    "tests/unit/rtkFindWindows.test.js": [
      "Windows find-path detection detects Windows drive-letter paths as `find`",
      "Windows find-path detection still detects Unix paths as `find` (no regression)",
      "Windows find-path detection still routes a Windows file:line dump to a compacting filter",
      "Windows find-path grouping compresses the dump (output shorter than input)",
      "Windows find-path grouping groups Windows backslash paths and normalizes to forward slashes"
    ],
    "tests/unit/rtkKiro.test.js": [
      "Kiro format RTK support compresses tool results in Kiro conversationState.currentMessage",
      "Kiro format RTK support compresses tool results in Kiro conversationState.history",
      "Kiro format RTK support handles Kiro body with no tool results gracefully",
      "Kiro format RTK support handles malformed Kiro body without crashing",
      "Kiro format RTK support handles multiple tool results across history and currentMessage",
      "Kiro format RTK support preserves error tool results without compression",
      "Kiro format RTK support returns null when RTK is disabled"
    ],
    "tests/unit/same-account-retry-cooldown-1641.test.js": [
      "same-account retry permission and cooldown (#1641) does not immediately retry or rotate a healthy pin during a 120000 ms wait",
      "same-account retry permission and cooldown (#1641) does not immediately retry or rotate a healthy pin during a 500 ms wait",
      "same-account retry permission and cooldown (#1641) does not immediately retry or rotate a healthy pin during a 5000 ms wait",
      "same-account retry permission and cooldown (#1641) does not replay uncertain generation despite an explicit same-account retry hint",
      "same-account retry permission and cooldown (#1641) retries the same account only with explicit permission, no generation, and zero cooldown",
      "same-account retry permission and cooldown (#1641) rotates after verified depletion without retrying a credential before its reset"
    ],
    "tests/unit/same-provider-key-fallback-2429.test.js": [
      "same-provider key fallback semantics (#2429) 401 auth error rotates to the next key and cools the failed one",
      "same-provider key fallback semantics (#2429) 403 quota exhausted rotates to the next key and cools the failed one",
      "same-provider key fallback semantics (#2429) 429 rate limit rotates to the next key and cools the failed one",
      "same-provider key fallback semantics (#2429) a 429 marks the key unavailable, and success clears it",
      "same-provider key fallback semantics (#2429) a client-side error does NOT burn a key",
      "same-provider key fallback semantics (#2429) a provider-wide or network failure rotates without burning the key",
      "same-provider key fallback semantics (#2429) quota exceeded body rotates to the next key and cools the failed one",
      "same-provider key fallback semantics (#2429) repeat rate limits back the same key off further each time",
      "same-provider key fallback semantics (#2429) selects a live key while a sibling is cooling, and reports empty only when all are out"
    ],
    "tests/unit/saml-flow-contract.test.js": [
      "buildSamlAuthorizeUrl extracts the request ID from the AuthnRequest and returns the redirect URL",
      "buildSamlAuthorizeUrl returns an empty request ID when the XML carries none",
      "claim pickers pickSamlDisplayName follows custom attribute, common claims, given+surname, then email",
      "claim pickers pickSamlEmail falls back through common claims and the attributes object",
      "claim pickers pickSamlEmail honors the configured custom attribute first, arrays included",
      "createSamlInstance configures signature enforcement and PEM-formatted IdP cert",
      "generateSamlMetadata emits SP metadata bound to the resolved origin",
      "getSamlBaseUrl falls back to request URL origin, then to the loopback default",
      "getSamlBaseUrl prefers settings.baseUrl over env and headers, trimming slashes",
      "getSamlBaseUrl uses forwarded headers when nothing is configured",
      "getSamlRuntimeConfig reports configured state from stored settings",
      "validateSamlResponse accepts a raw string body and skips replay check when no request ID was stored",
      "validateSamlResponse propagates signature validation failures from the SAML engine",
      "validateSamlResponse rejects a response carrying no InResponseTo when one is expected",
      "validateSamlResponse rejects a response whose InResponseTo does not match the stored request ID (replay)",
      "validateSamlResponse rejects when SAMLResponse is missing from the POST body",
      "validateSamlResponse rejects when the IdP certificate is not configured",
      "validateSamlResponse returns the profile on a valid, matching response"
    ],
    "tests/unit/saml.test.js": [
      "SAML 2.0 Auth Engine Utilities Claims Extraction pickSamlDisplayName extracts custom attribute, common names, or falls back to email",
      "SAML 2.0 Auth Engine Utilities Claims Extraction pickSamlEmail extracts custom attribute or common claims",
      "SAML 2.0 Auth Engine Utilities formatX509Certificate cleans existing PEM header/footer and extra whitespace",
      "SAML 2.0 Auth Engine Utilities formatX509Certificate formats raw Base64 string into standard 64-column PEM block",
      "SAML 2.0 Auth Engine Utilities formatX509Certificate returns empty string for null, undefined, or invalid inputs",
      "SAML 2.0 Auth Engine Utilities generateSamlMetadata generates valid SP XML metadata with Entity ID and ACS binding",
      "SAML 2.0 Auth Engine Utilities InResponseTo Replay Validation throws error if samlCert is not configured",
      "SAML 2.0 Auth Engine Utilities InResponseTo Replay Validation throws error when expectedRequestId is supplied but InResponseTo does not match",
      "SAML 2.0 Auth Engine Utilities InResponseTo Replay Validation throws error when expectedRequestId is supplied but InResponseTo is missing",
      "SAML 2.0 Auth Engine Utilities isSamlConfigured returns false if entryPoint or cert is missing",
      "SAML 2.0 Auth Engine Utilities isSamlConfigured returns true when entryPoint and cert are non-empty",
      "SAML 2.0 Auth Engine Utilities Settings Repository Defaults mergeWithDefaults safely populates SAML defaults for existing installations"
    ],
    "tests/unit/schema-distiller.test.js": [
      "__proto__ property names survive distillation (audit finding 12) a property literally named __proto__ stays an own property, with no prototype swap",
      "distillToolSchemas engages at exactly the 8KB boundary",
      "distillToolSchemas handles empty and non-array input without engaging",
      "distillToolSchemas never mutates the caller's array",
      "distillToolSchemas preserves non-object entries and passthrough schema fields",
      "distillToolSchemas preserves whitespace inside all descriptions",
      "distillToolSchemas returns the input unchanged below the 8KB floor",
      "distillToolSchemas second pass is a fixed point (idempotent)",
      "distillToolSchemas strips four annotation keywords and keeps dialect and structural keywords",
      "distillToolSchemas supports OpenAI parameters and Responses-schema shapes too",
      "property names colliding with STRIP_KEYS keeps properties named title/default/example/examples, strips keywords inside their values",
      "property names colliding with STRIP_KEYS name-map protection nests through arrays and $defs"
    ],
    "tests/unit/scope-options.test.js": [
      "scope options lists only models of providers with an account, matching alias and id",
      "scope options puts popular providers first and keeps every provider in the full list",
      "scope options resolves a model row alias back to the provider id"
    ],
    "tests/unit/search-callers-request-builders.test.js": [
      "credential requirements throw before any request is built google-pse with a token but no cx still throws",
      "credential requirements throw before any request is built google-pse without a token throws",
      "credential requirements throw before any request is built linkup without a token throws",
      "credential requirements throw before any request is built searchapi without a token throws",
      "credential requirements throw before any request is built xquik rejects an invalid queryType",
      "credential requirements throw before any request is built xquik without a token throws",
      "credential requirements throw before any request is built youcom without a token throws",
      "helpers getProviderSetting prefers providerOptions, falls back to providerSpecificData, ignores non-strings and blanks",
      "helpers parseDomainFilter splits includes and dash-prefixed excludes",
      "helpers toPageNumber converts offset to a 1-indexed page and rejects nonsense",
      "per-provider request shape brave: GET with count and X-Subscription-Token; news switches the path",
      "per-provider request shape ddgs: strips an endpoint-shaped base, region needs BOTH halves, year timelimit excluded on news",
      "per-provider request shape exa: domain filter split into includeDomains/excludeDomains, news category",
      "per-provider request shape glm: JSON-RPC tools/call envelope with search_query and count",
      "per-provider request shape google-pse: num capped at 10, dateRestrict mapped, start capped at 91, key+cx in query",
      "per-provider request shape linkup: depth whitelisted with standard fallback; timeRange becomes fromDate/toDate",
      "per-provider request shape searxng: /search appended once, page number from offset, json format",
      "per-provider request shape serper: news endpoint, gl lowercased, hl, POST with X-API-Key",
      "per-provider request shape unknown provider falls back to generic bearer POST with the unified body",
      "per-provider request shape youcom: count capped at 100, offset converted to page-offset capped at 9, livecrawl from contentOptions",
      "resolveBaseUrl and the SSRF boundary a malformed or non-http override throws instead of silently falling back",
      "resolveBaseUrl and the SSRF boundary client providerOptions.baseUrl IS guarded; admin providerSpecificData.baseUrl is NOT (#1231)",
      "resolveBaseUrl and the SSRF boundary no override returns the config baseUrl with trailing slashes stripped, no guard call"
    ],
    "tests/unit/search-credential-fallback.test.js": [
      "credentialFallback survives the registry → UI projection MEDIA_ENTRY_KEYS carries the field onto AI_PROVIDERS",
      "GLM hosts its own MCP search — no standalone zai-search provider declares webSearch and the web_search_prime searchConfig",
      "GLM hosts its own MCP search — no standalone zai-search provider keeps the fork's chat-based search as the failover route (#2425)",
      "GLM hosts its own MCP search — no standalone zai-search provider never introduces a zai-search provider",
      "glm MCP request builder + normalizer survives a non-JSON envelope body without throwing",
      "glm MCP request builder + normalizer unwraps the MCP text envelope before mapping rows",
      "glm MCP request builder + normalizer wraps the query in a tools/call JSON-RPC envelope",
      "ollama-search registry entry is a webSearch provider that borrows the ollama chat key",
      "ollama-search registry entry is exported from the generated registry index",
      "ollama-search registry entry points at the Ollama Cloud web_search endpoint",
      "ollama-search registry entry reuses the ollama brand mark rather than shipping a second copy",
      "ollama-search request builder + normalizer maps {title,url,content} rows and keeps content as full text",
      "ollama-search request builder + normalizer omits the auth header when no token is supplied",
      "ollama-search request builder + normalizer posts {query, max_results} with a bearer token",
      "ollama-search request builder + normalizer returns an empty set rather than throwing on a malformed payload",
      "search failure locks are scoped to websearch:<provider> attributes the lock to the provider that owns the borrowed connection",
      "search failure locks are scoped to websearch:<provider> reads and writes the lock under the same websearch-scoped key"
    ],
    "tests/unit/search-normalizers-malformed.test.js": [
      "malformed payloads never throw and always return the unified shape brave-search: empty object",
      "malformed payloads never throw and always return the unified shape brave-search: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape brave-search: results as object",
      "malformed payloads never throw and always return the unified shape brave-search: results as string",
      "malformed payloads never throw and always return the unified shape ddgs: empty object",
      "malformed payloads never throw and always return the unified shape ddgs: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape ddgs: results as object",
      "malformed payloads never throw and always return the unified shape ddgs: results as string",
      "malformed payloads never throw and always return the unified shape exa: empty object",
      "malformed payloads never throw and always return the unified shape exa: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape exa: results as object",
      "malformed payloads never throw and always return the unified shape exa: results as string",
      "malformed payloads never throw and always return the unified shape glm: empty object",
      "malformed payloads never throw and always return the unified shape glm: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape glm: results as object",
      "malformed payloads never throw and always return the unified shape glm: results as string",
      "malformed payloads never throw and always return the unified shape google-pse: empty object",
      "malformed payloads never throw and always return the unified shape google-pse: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape google-pse: results as object",
      "malformed payloads never throw and always return the unified shape google-pse: results as string",
      "malformed payloads never throw and always return the unified shape linkup: empty object",
      "malformed payloads never throw and always return the unified shape linkup: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape linkup: results as object",
      "malformed payloads never throw and always return the unified shape linkup: results as string",
      "malformed payloads never throw and always return the unified shape ollama-search: empty object",
      "malformed payloads never throw and always return the unified shape ollama-search: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape ollama-search: results as object",
      "malformed payloads never throw and always return the unified shape ollama-search: results as string",
      "malformed payloads never throw and always return the unified shape perplexity: empty object",
      "malformed payloads never throw and always return the unified shape perplexity: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape perplexity: results as object",
      "malformed payloads never throw and always return the unified shape perplexity: results as string",
      "malformed payloads never throw and always return the unified shape searchapi: empty object",
      "malformed payloads never throw and always return the unified shape searchapi: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape searchapi: results as object",
      "malformed payloads never throw and always return the unified shape searchapi: results as string",
      "malformed payloads never throw and always return the unified shape searxng: empty object",
      "malformed payloads never throw and always return the unified shape searxng: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape searxng: results as object",
      "malformed payloads never throw and always return the unified shape searxng: results as string",
      "malformed payloads never throw and always return the unified shape serper: empty object",
      "malformed payloads never throw and always return the unified shape serper: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape serper: results as object",
      "malformed payloads never throw and always return the unified shape serper: results as string",
      "malformed payloads never throw and always return the unified shape tavily: empty object",
      "malformed payloads never throw and always return the unified shape tavily: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape tavily: results as object",
      "malformed payloads never throw and always return the unified shape tavily: results as string",
      "malformed payloads never throw and always return the unified shape unknown provider id returns the empty shape",
      "malformed payloads never throw and always return the unified shape xquik: empty object",
      "malformed payloads never throw and always return the unified shape xquik: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape xquik: results as object",
      "malformed payloads never throw and always return the unified shape xquik: results as string",
      "malformed payloads never throw and always return the unified shape youcom: empty object",
      "malformed payloads never throw and always return the unified shape youcom: null-ish nested fields",
      "malformed payloads never throw and always return the unified shape youcom: results as object",
      "malformed payloads never throw and always return the unified shape youcom: results as string",
      "unified result shape invariants an item missing its url still yields a result with url '' and no display_url (stream survives)",
      "unified result shape invariants brave: web vs news containers, favicon from meta_url",
      "unified result shape invariants ddgs maps href→url and body→snippet",
      "unified result shape invariants exa full text lands in content with its length; snippet prefers highlights",
      "unified result shape invariants glm unwraps the MCP text envelope; unparseable envelope text yields empty results, not a throw",
      "unified result shape invariants google-pse parses the string totalResults; garbage becomes null, not NaN",
      "unified result shape invariants ollama-search accepts both { results: [] } and a bare array",
      "unified result shape invariants score is clamped to [0,1] and null when absent (exa)",
      "unified result shape invariants serper news type reads the news array, not organic",
      "unified result shape invariants serper web: maps link→url, 1-based positions, strips scheme for display_url",
      "unified result shape invariants xquik builds x.com URLs, keeps pagination, and tolerates missing author/media",
      "unified result shape invariants youcom: snippet from first string in snippets[], livecrawl markdown lands as content"
    ],
    "tests/unit/search-ssrf-guard.test.js": [
      "resolveBaseUrl SSRF guard allows public http override",
      "resolveBaseUrl SSRF guard allows public https override",
      "resolveBaseUrl SSRF guard rejects cloud metadata override",
      "resolveBaseUrl SSRF guard rejects localhost hostname override",
      "resolveBaseUrl SSRF guard rejects loopback override",
      "resolveBaseUrl SSRF guard rejects non-http protocols",
      "resolveBaseUrl SSRF guard rejects private IP override",
      "resolveBaseUrl SSRF guard uses provider default when no override"
    ],
    "tests/unit/searxng-config-1231.test.js": [
      "SearXNG endpoint configuration (#1231) a client-supplied override is still refused for an internal host",
      "SearXNG endpoint configuration (#1231) appends /search when the configured value is an origin only",
      "SearXNG endpoint configuration (#1231) falls back to the loopback default when nothing is configured",
      "SearXNG endpoint configuration (#1231) sends the search to the host SEARXNG_URL names, not to the default"
    ],
    "tests/unit/searxng-private-endpoint-1231.test.js": [
      "an operator-stored search endpoint is trusted like the registry one (#1231) a caller override still wins over the stored one, and is still guarded",
      "an operator-stored search endpoint is trusted like the registry one (#1231) a public caller override is still honoured",
      "an operator-stored search endpoint is trusted like the registry one (#1231) a stored value that is not an http(s) URL is still refused",
      "an operator-stored search endpoint is trusted like the registry one (#1231) accepts a private address stored on the connection",
      "an operator-stored search endpoint is trusted like the registry one (#1231) falls back to the registry endpoint when nothing overrides it",
      "an operator-stored search endpoint is trusted like the registry one (#1231) still rejects the same address supplied by the caller"
    ],
    "tests/unit/searxng-provider-config.test.js": [
      "SearXNG provider configuration preserves the loopback default when SEARXNG_URL is unset",
      "SearXNG provider configuration uses SEARXNG_URL when the deployment config supplies one"
    ],
    "tests/unit/secret-storage-999.test.js": [
      "connection secrets do not leave over the dashboard API (#999) a single connection redacts the same set",
      "connection secrets do not leave over the dashboard API (#999) an edit does not echo the credentials back in its response",
      "connection secrets do not leave over the dashboard API (#999) creating a connection does not echo the key that created it",
      "connection secrets do not leave over the dashboard API (#999) redaction is at the boundary — the router still has the credentials it needs",
      "connection secrets do not leave over the dashboard API (#999) the connection list redacts every credential, top level and provider-specific",
      "connection secrets do not leave over the dashboard API (#999) the fields the dashboard renders survive redaction"
    ],
    "tests/unit/secretCol-key-cache-pf1.test.js": [
      "secretCol key cache (P-F1) derives the machine-id key once across many encrypts and decrypts",
      "secretCol key cache (P-F1) round-trips through the cached key"
    ],
    "tests/unit/secretCol.test.js": [
      "secretCol reads legacy plaintext JSON without the enc1: prefix",
      "secretCol returns the fallback for null/invalid input",
      "secretCol round-trips a JSON value through encryption"
    ],
    "tests/unit/security-audit.test.js": [
      "AUDIT-001: Synchronous restart guard mitmIsRestarting should be reset on max-restarts early return",
      "AUDIT-001: Synchronous restart guard mitmIsRestarting should be set before first await expression",
      "AUDIT-002: API key masking byApiKey object keys should use an opaque identity, never a raw key",
      "AUDIT-002: API key masking getUsageHistory should use apiKeyMasked instead of apiKey",
      "AUDIT-002: API key masking getUsageStats should expose masked display fields with opaque identities",
      "AUDIT-002: API key masking source should contain maskApiKey function",
      "AUDIT-003: Proxy URL validation should accept valid http proxy URLs",
      "AUDIT-003: Proxy URL validation should accept valid https proxy URLs",
      "AUDIT-003: Proxy URL validation should accept valid socks5 proxy URLs",
      "AUDIT-003: Proxy URL validation should reject non-allowed schemes (file://)",
      "AUDIT-003: Proxy URL validation should reject non-allowed schemes (javascript:)",
      "AUDIT-003: Proxy URL validation should reject URLs with shell metacharacters (backtick)",
      "AUDIT-003: Proxy URL validation should reject URLs with shell metacharacters (dollar)",
      "AUDIT-003: Proxy URL validation should reject URLs with shell metacharacters (newline)",
      "AUDIT-003: Proxy URL validation source should contain validateProxyUrl function",
      "AUDIT-004: Atomic lock file for MITM startup manager.js should define LOCK_FILE constant",
      "AUDIT-004: Atomic lock file for MITM startup should clean up lock file on all exit paths",
      "AUDIT-004: Atomic lock file for MITM startup should use O_EXCL flag (wx) for atomic creation",
      "AUDIT-018: XSS escaping in OAuth callback should escape ampersand, angle brackets, and quotes",
      "AUDIT-018: XSS escaping in OAuth callback should use safeMessage in rendered HTML, not raw message",
      "AUDIT-018: XSS escaping in OAuth callback source should contain escapeHtml function"
    ],
    "tests/unit/seen-models-repo.test.js": [
      "acknowledgement acknowledges everything when called with no items",
      "acknowledgement acknowledges named models only",
      "empty table reconcile with nothing observed neither seeds nor reports",
      "empty table reports zero models everywhere",
      "first scan seeds acknowledged inserts every observed model as acknowledged and reports none as new",
      "later scans classify new vs unseen a model appearing after the seed is genuinely new (acknowledged=0)",
      "later scans classify new vs unseen a still-unacknowledged model re-observed lands in unseen, not new",
      "seedSeenModels is idempotent on existing ids and inserts new ids as acknowledged"
    ],
    "tests/unit/selection-dock-sizing.test.js": [
      "keeps closed comparison content-sized in a labelled local scroll region",
      "keeps the same stacked flow on narrow screens",
      "keeps wide comparison and detail mounted across selections and returns focus on close",
      "retains comparison state, unsaved details and the focused caret across both responsive transitions",
      "stacks the detail under the comparison below the wide width, never in a dialog",
      "uses available container space even when the viewport is wide"
    ],
    "tests/unit/sensenova-provider.test.js": [
      "SenseNova provider filters image-only rows from model discovery while preserving chat metadata",
      "SenseNova provider lists models through the authenticated official endpoint",
      "SenseNova provider publishes both rolling five-hour and weekly allowances for both free pools",
      "SenseNova provider registers only the current official chat-capable catalog",
      "SenseNova provider resolves sn without creating the misspelled sensnova alias",
      "SenseNova provider tests saved keys with a bounded GET models probe",
      "SenseNova provider uses the default OpenAI transport with Bearer API-key authentication"
    ],
    "tests/unit/server-crash-visibility-1814.test.js": [
      "a crash in the server process says so (#1814) a client that walked away does not take the server down",
      "a crash in the server process says so (#1814) a non-Error rejection is described rather than dropped",
      "a crash in the server process says so (#1814) both crash events are handled",
      "a crash in the server process says so (#1814) each one still exits, because installing a listener suppresses Node's default",
      "a crash in the server process says so (#1814) the message identifies the process and the port",
      "a crash in the server process says so (#1814) the port is read at crash time, not closed over"
    ],
    "tests/unit/server-shutdown-hooks.test.js": [
      "the server arms its graceful-shutdown signals both signals resolve to the same handler, so neither path is unhandled",
      "the server arms its graceful-shutdown signals registers both termination signals",
      "the server arms its graceful-shutdown signals uses once, so a second signal cannot re-enter a shutdown already running"
    ],
    "tests/unit/server-tool-block-index-2868.test.js": [
      "a stale server tool block index does not swallow a real call (#2868) forwards the arguments of a tool_use landing on the previously parked index",
      "a stale server tool block index does not swallow a real call (#2868) releases the parked index when a new message starts",
      "a stale server tool block index does not swallow a real call (#2868) still drops the server tool block's own deltas within its message"
    ],
    "tests/unit/session-claude-code-header-2fd99eae.test.js": [
      "Claude Code session id via x-claude-code-session-id header body metadata.user_id still wins when both are present",
      "Claude Code session id via x-claude-code-session-id header no header and no metadata falls through to the existing derivation",
      "Claude Code session id via x-claude-code-session-id header the header pins one session across connections and bodies that would otherwise diverge",
      "Claude Code session id via x-claude-code-session-id header uses the header when metadata.user_id is absent (Responses translation dropped it)"
    ],
    "tests/unit/session-manager.test.js": [
      "resolveContinuationId does not evict a recently used continuation id when the store exceeds its cap",
      "resolveContinuationId does not let ephemeral Kiro continuations evict explicit session continuations",
      "resolveContinuationId evicts old continuation ids when the store exceeds its cap",
      "resolveContinuationId keeps continuation id stable for the same Kiro session",
      "resolveContinuationId uses a different continuation id for a different Kiro session",
      "resolveSessionId assistant anchor wins once assistant text reaches cap",
      "resolveSessionId client override: x-session-id header wins, skips later steps",
      "resolveSessionId different connectionId -> different id",
      "resolveSessionId different scope -> different id",
      "resolveSessionId does not switch Kiro headerless requests to assistant-text session ids mid-conversation",
      "resolveSessionId does not treat raw metadata.user_id as a Kiro conversation session",
      "resolveSessionId does not treat request-scoped previous_response_id as a Kiro session override",
      "resolveSessionId does not treat request-scoped x-client-request-id as a session override",
      "resolveSessionId explicit TokenProxy session header preserves routing identity",
      "resolveSessionId fallback: empty body+no header+no workspaceId -> deriveSessionId(connectionId)",
      "resolveSessionId first user message anchor when assistant text below cap",
      "resolveSessionId keeps Claude Code session_id metadata as a Kiro conversation session",
      "resolveSessionId keeps raw metadata.user_id as a non-Kiro session fallback",
      "resolveSessionId keeps x-client-request-id as a session override outside Kiro scope",
      "resolveSessionId marks generated headerless Kiro sessions as ephemeral",
      "resolveSessionId stickiness: same body+connectionId+scope -> same id",
      "resolveSessionId uses fresh Kiro sessions for unrelated headerless requests on the same connection",
      "resolveSessionId workspaceId path: empty body + workspaceId set -> normalized workspaceId"
    ],
    "tests/unit/session-memo-bounds.test.js": [
      "sessionMemo bounds and scope expires an entry after the TTL rather than replaying a stale decision",
      "sessionMemo bounds and scope keys memo content on the text, distinguishing length and content",
      "sessionMemo bounds and scope refuses to store or serve an absent session key",
      "sessionMemo bounds and scope separates namespaces and sessions so one memo never answers for another",
      "sessionMemo bounds and scope stays under its cap as sessions accumulate, keeping the newest"
    ],
    "tests/unit/session-pin-controls.test.js": [
      "a command created during quota reads waits for a fresh boundary instead of bypassing its preflight",
      "a distant operator deadline never extends the existing idle expiry on apply",
      "a legacy no-TTL pin gains an expiry only from the explicitly applied operator deadline",
      "a replaced binding invalidates an old queued owner without moving the newer pin",
      "absolute expiry survives a pin touch and expires at the exact boundary",
      "an explicit identity under a different client namespace does not inherit a queued command",
      "API preview, apply, receipt and next real selection preserve exact model and existing leases",
      "auth precedes body parsing and persistence false/false/true",
      "auth precedes body parsing and persistence false/true/true",
      "auth precedes body parsing and persistence true/false/false",
      "bounds and sanitizes malformed mutation bodies",
      "bounds list query ?before=bad",
      "bounds list query ?connectionId=",
      "bounds list query ?lastSeenFrom=2030-01-01T00%3A00%3A00.000Z&lastSeenFrom=2030-01-01T00%3A00%3A00.000Z",
      "bounds list query ?lastSeenFrom=yesterday",
      "bounds list query ?lastSeenTo=2026-09-06",
      "bounds list query ?limit=0",
      "bounds list query ?limit=1&limit=2",
      "bounds list query ?limit=51",
      "bounds list query ?model=",
      "bounds list query ?model=",
      "bounds list query ?provider=",
      "bounds list query ?provider=claude&provider=claude",
      "bounds list query ?unknown=1",
      "clear cancels queued reassignment, and later selection can choose the same account",
      "command consumption rollback releases reservation and survives a fresh scheduler facade",
      "disabled target and explicit conflicting account never evade the queue or change model",
      "idle expiry advancing makes a preview stale and records the conflict without changing the binding",
      "list joins only an exact stored routing identity and preserves unknown historical requested/served data",
      "pending target at capacity waits, retains the old pin and consumes exactly once after release",
      "pending target cannot consume when proxy admission is unavailable",
      "pending target cannot consume when quota admission is unavailable",
      "preview expiry and pin expiry cannot be bypassed by a later apply",
      "preview reports a depleted target as a conflict rather than applying healthy-pin leniency",
      "read-only listing and key pagination do not mutate affinity or audit state",
      "rejects a 36-char hex-dash id that is not a canonical UUID",
      "repeated preview and apply are idempotent only for the original payload",
      "scoped filters narrow the list before pagination and keep the cursor stable"
    ],
    "tests/unit/session-pin-migration.test.js": [
      "migrates actual version10 pins without changing binding, deadlines or inventing control history"
    ],
    "tests/unit/session-pin-timeline-ui.test.js": [
      "builds request URLs and unknown wording as pure functions",
      "expands into a paged timeline and collapses back without losing the pin selection",
      "keeps the bounded recent view as the default and reads no timeline until expanded",
      "names an unavailable source and surfaces a read failure instead of an empty list",
      "opens a receipt from a timeline entry without leaving the timeline",
      "pages beyond page one and restarts paging when a filter changes",
      "renders merge order, exact identifiers and every unknown as a word"
    ],
    "tests/unit/session-pin-timeline.test.js": [
      "a kind filter narrows before the cursor and never leaks another source in",
      "keeps a filtered set stable across the cursor rather than filtering after the cut",
      "merges the three sources into one descending time order with exact ids and a time basis",
      "never claims a served model for a request that did not record success",
      "pages beyond page one without repeating or dropping an entry",
      "refuses a duplicate, unknown or malformed parameter and a forged cursor",
      "renders an unjoinable source as unavailable rather than as an empty history",
      "round-trips a cursor and refuses one that re-encodes differently",
      "serves the timeline over the admin route and refuses a non-operator",
      "validates the worker-side operation the same way the route parses it"
    ],
    "tests/unit/session-pins-ui.test.js": [
      "a failed preview never enables mutation or substitutes guessed request evidence",
      "a selected pin missing from the refreshed page keeps its selection and says so",
      "edits invalidate a preview and cannot submit a different target under an old receipt",
      "keeps an applied clear receipt visible after the pin is absent",
      "labels the recent history as bounded, never as complete pagination",
      "pagination returns to the exact previous cursor",
      "reflows the inventory instead of hiding columns behind a horizontal scroller",
      "renders every receipt status as its own distinct state",
      "requests the scoped list when the workspace carries filters",
      "requires preview then explicit apply, retains receipt and refreshes the pin list",
      "retains the exact uncertain action id and reads its receipt without replaying apply",
      "returns focus to the selected inventory control when the direct editor closes",
      "selection survives a refresh and live reorder without moving off the selected pin",
      "shows stale-state refusal, removes apply and requires a new preview",
      "wires shared workspace scope into the backend list filters"
    ],
    "tests/unit/session-store-lru.test.js": [
      "assistant-anchored session eviction keeps the conversation that is still being used when the cap is reached",
      "deriveSessionId eviction evicts the least-recently-used connection, not the first inserted",
      "deriveSessionId eviction keeps an untouched connection stable while under the cap",
      "deriveSessionId eviction keeps the connection that is still being used when the cap is reached",
      "resolveContinuationId eviction (already LRU — guards the reference behaviour) keeps the continuation that is still being used"
    ],
    "tests/unit/settings-connect-timeout.test.js": [
      "connect timeout settings repository atomically patches siblings and deletes only null fields",
      "connect timeout settings repository deletes both proxy snapshot fields when a bulk strategy clear carries nulls",
      "connect timeout settings repository drops an invalid imported provider override in memory while preserving its siblings and raw data",
      "connect timeout settings repository merges 15000 into an old row without writing it",
      "connect timeout settings repository serializes concurrent provider patches without losing fields",
      "connect timeout settings repository uses 15000 in memory for an invalid imported global without rewriting raw data",
      "connect timeout settings route accepts a boolean Codex Fast value in the legacy strategy map",
      "connect timeout settings route accepts a valid legacy provider strategy map",
      "connect timeout settings route atomically persists and deletes Codex Fast without losing siblings",
      "connect timeout settings route GET exposes the in-memory default without migrating or leaking secrets",
      "connect timeout settings route persists a valid global timeout",
      "connect timeout settings route persists context controls as exact booleans and refuses non-boolean literals",
      "connect timeout settings route persists one provider field without losing siblings",
      "connect timeout settings route rejects dangerous provider id __proto__",
      "connect timeout settings route rejects dangerous provider id constructor",
      "connect timeout settings route rejects dangerous provider id prototype",
      "connect timeout settings route rejects dangerous provider ids in the legacy map",
      "connect timeout settings route rejects dangerous provider value key __proto__",
      "connect timeout settings route rejects dangerous provider value key constructor",
      "connect timeout settings route rejects dangerous provider value key prototype",
      "connect timeout settings route rejects invalid atomic Codex Fast literal 0 without a write",
      "connect timeout settings route rejects invalid atomic Codex Fast literal 1 without a write",
      "connect timeout settings route rejects invalid atomic Codex Fast literal 2 without a write",
      "connect timeout settings route rejects invalid atomic Codex Fast literal 3 without a write",
      "connect timeout settings route rejects invalid atomic Codex Fast literal 4 without a write",
      "connect timeout settings route rejects invalid global literal 120001",
      "connect timeout settings route rejects invalid global literal 15000",
      "connect timeout settings route rejects invalid global literal 15000.5",
      "connect timeout settings route rejects invalid global literal 999",
      "connect timeout settings route rejects invalid global literal null",
      "connect timeout settings route rejects invalid global literal true",
      "connect timeout settings route rejects invalid legacy Codex Fast literal 0 without a write",
      "connect timeout settings route rejects invalid legacy Codex Fast literal 1 without a write",
      "connect timeout settings route rejects invalid legacy Codex Fast literal 2 without a write",
      "connect timeout settings route rejects invalid legacy Codex Fast literal 3 without a write",
      "connect timeout settings route rejects invalid legacy Codex Fast literal 4 without a write",
      "connect timeout settings route rejects invalid nested timeout 120001",
      "connect timeout settings route rejects invalid nested timeout 15000.5",
      "connect timeout settings route rejects invalid nested timeout 8000",
      "connect timeout settings route rejects invalid nested timeout 999",
      "connect timeout settings route rejects invalid nested timeout true",
      "connect timeout settings route rejects malformed provider command 0",
      "connect timeout settings route rejects malformed provider command 1",
      "connect timeout settings route rejects malformed provider command 2",
      "connect timeout settings route rejects malformed provider command 3",
      "connect timeout settings route rejects mixed provider command and ordinary settings",
      "connect timeout settings route rejects nested numeric overflow",
      "connect timeout settings route rejects parsed numeric overflow rather than JSON-stringifying it to null",
      "connect timeout settings route rejects unknown siblings beside a provider command",
      "connect timeout settings route rejects unsafe legacy provider strategy map 0",
      "connect timeout settings route rejects unsafe legacy provider strategy map 1",
      "connect timeout settings route rejects unsafe legacy provider strategy map 2",
      "connect timeout settings route rejects unsafe legacy provider strategy map 3",
      "connect timeout settings route rejects unsafe legacy provider strategy map 4",
      "provider strategy proxy-pool snapshots atomically clears an existing no-auth pool and snapshot with literal 0",
      "provider strategy proxy-pool snapshots atomically clears an existing no-auth pool and snapshot with literal 1",
      "provider strategy proxy-pool snapshots atomically clears an existing no-auth pool and snapshot with literal 2",
      "provider strategy proxy-pool snapshots atomically clears an existing no-auth pool and snapshot with literal 3",
      "provider strategy proxy-pool snapshots bulk clearing a no-auth pool removes its snapshot and retains direct credentials",
      "provider strategy proxy-pool snapshots derives a fixed no-auth strict snapshot from an active pool",
      "provider strategy proxy-pool snapshots migrates a pairless fixed no-auth selection before returning credentials",
      "provider strategy proxy-pool snapshots reads back each virtual-account rotation mode without losing its fixed pool",
      "provider strategy proxy-pool snapshots rejects inactive and client-strict no-auth pool selections without writing",
      "settings compatibility does not publish a retired workbench flag from the startup environment",
      "settings compatibility omits retired controls from settings reads without deleting saved data"
    ],
    "tests/unit/settings-context-overrides-atomic.test.js": [
      "context window override mutations checks only reviewed keys atomically and preserves a concurrently changed sibling",
      "context window override mutations keeps the existing whole-map settings replacement contract",
      "context window override mutations serializes concurrent per-key writes and preserves unrelated overrides",
      "context window override mutations stores reserved keys literally and counts only own deletions"
    ],
    "tests/unit/settings-database-auth.test.js": [
      "settings/database auth gate accepts valid JWT plus correct password",
      "settings/database auth gate rejects junk CLI token with no password (presence-only bypass)",
      "settings/database auth gate rejects valid identity with wrong password"
    ],
    "tests/unit/settings-history-retention.test.js": [
      "history retention settings defaults old settings to preservation without migrating or changing legacy keys",
      "history retention settings persists an explicit 1-day window through PATCH and fresh GET, then preservation",
      "history retention settings persists an explicit 365-day window through PATCH and fresh GET, then preservation",
      "history retention settings persists an explicit 45-day window through PATCH and fresh GET, then preservation",
      "history retention settings rejects invalid days even when changing to preservation",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":-1}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":\"45\"}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":{}}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":0}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":1.5}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":366}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":null}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionDays\":true}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionMode\":\"delete\"}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionMode\":[]}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionMode\":null}",
      "history retention settings rejects invalid policy atomically with unrelated settings {\"statsRetentionMode\":true}"
    ],
    "tests/unit/settings-json-column-bytes.test.js": [
      "parseJson byte columns falls back rather than throwing when the bytes are not JSON",
      "parseJson byte columns parses a Buffer column to the same object",
      "parseJson byte columns parses a byte column holding an ARRAY, so an allowlist is not silently dropped",
      "parseJson byte columns parses a text column to the same object",
      "parseJson byte columns parses a Uint8Array column to the same object",
      "parseJson byte columns returns the fallback for null and undefined",
      "parseJson byte columns spreading a parsed Buffer column yields no per-byte keys",
      "parseJson byte columns spreading a parsed text column yields no per-byte keys",
      "parseJson byte columns spreading a parsed Uint8Array column yields no per-byte keys",
      "parseJson byte columns still passes an already-parsed object through untouched"
    ],
    "tests/unit/shaping-control-inventory-ui.test.js": [
      "preserves headroomLossless compatibility state and explains the missing runtime",
      "renders evidence inline under its row, with no second layer or editor"
    ],
    "tests/unit/shaping-control-inventory.test.js": [
      "optimization control evidence distinguishes a measured zero from an event with no byte measurement",
      "optimization control evidence does not infer coverage from a legacy nonzero aggregate or missing stage",
      "optimization control evidence keeps historical execution independent of present configuration",
      "optimization control evidence keeps signed growth and the measured-record denominator",
      "optimization control evidence retains the default-off permission when its setting has never been persisted",
      "optimization settings boundary exposes persisted profile controls with existing source owners",
      "optimization settings boundary keeps adaptive lifetime global and each new context stage default-off",
      "optimization settings boundary keeps an inherited service timeout distinct from numeric zero",
      "optimization settings boundary requires usable integer values before submitting a threshold patch",
      "optimization settings boundary validates direct response levels and exact lists against the saved controls contract"
    ],
    "tests/unit/shaping-evaluation-api.test.js": [
      "retains cancellation and refuses to promote incomplete evidence",
      "retains selected corpus versions with consent, pagination and exact experiment attribution"
    ],
    "tests/unit/shaping-evaluation-sets.test.js": [
      "evaluates valid simple messages without manufacturing thinking or task outcome evidence",
      "refuses empty, duplicate, oversized and unsupported-format evaluation inputs",
      "validates an explicitly selected corpus and hashes exact inputs deterministically"
    ],
    "tests/unit/shaping-evaluation-ui.test.js": [
      "cancels the active comparison without offering promotion from incomplete evidence",
      "imports only explicitly acknowledged inputs and preserves selected versions outside the visible page",
      "links retained handoff source attempts without manufacturing absent session identities",
      "requires handoff review, preserves sessions across pages and reports uncertain persistence"
    ],
    "tests/unit/shaping-gateway-parity.test.js": [
      "matches actual gateway stage output before final anchoring with lossy consent false",
      "matches actual gateway stage output before final anchoring with lossy consent true"
    ],
    "tests/unit/shaping-handoffs.test.js": [
      "connects approved packets without changing routing and refuses excess context (false)",
      "connects approved packets without changing routing and refuses excess context (true)",
      "expiry, revocation and project rebinding stop subsequent injection without replay",
      "preserves native payload structures and avoids duplicate insertion",
      "preserves native payload structures and avoids duplicate insertion",
      "preserves native payload structures and avoids duplicate insertion",
      "preserves native payload structures and avoids duplicate insertion",
      "refuses conflicting packet markers and preserves required leading tool results",
      "requires consent and exact same-project identities, with one active target packet",
      "retains handoff execution origin across retries and keeps request retention valid",
      "validates, persists and revokes through the real API with paginated redacted receipts"
    ],
    "tests/unit/shaping-ordered-coverage.test.js": [
      "covers all stage families alone, all ordered pairs, every group size and the full ordered pipeline",
      "keeps diet content changes disabled without the fixture opt-in",
      "keeps epochAuto content changes disabled without the fixture opt-in",
      "keeps epochMicro content changes disabled without the fixture opt-in",
      "keeps handoff content changes disabled without the fixture opt-in",
      "keeps lingua content changes disabled without the fixture opt-in",
      "keeps pxpipe content changes disabled without the fixture opt-in"
    ],
    "tests/unit/shaping-plan-overrides-ui.test.js": [
      "blocks replay when the transport outcome is unknown",
      "freezes the reviewed hashes, requires consent and verifies the exact retained receipt",
      "reads an earlier retained receipt after a new component mount",
      "retains a stale draft with a conflict notice and never automatically replays it"
    ],
    "tests/unit/shaping-provenance-gateway.test.js": [
      "gateway optional stage fault isolation records diet failure and dispatches exactly once",
      "gateway optional stage fault isolation records epochAuto failure and dispatches exactly once",
      "gateway optional stage fault isolation records epochMicro failure and dispatches exactly once",
      "gateway optional stage fault isolation records handoff failure and dispatches exactly once",
      "gateway optional stage fault isolation records headroom failure and dispatches exactly once",
      "gateway optional stage fault isolation records inject failure and dispatches exactly once",
      "gateway optional stage fault isolation records lingua failure and dispatches exactly once",
      "gateway optional stage fault isolation records mem failure and dispatches exactly once",
      "gateway optional stage fault isolation records midinject failure and dispatches exactly once",
      "gateway optional stage fault isolation records pairs failure and dispatches exactly once",
      "gateway optional stage fault isolation records privacy failure and dispatches exactly once",
      "gateway optional stage fault isolation records pxpipe failure and dispatches exactly once",
      "gateway optional stage fault isolation records qac failure and dispatches exactly once",
      "gateway optional stage fault isolation records reorder failure and dispatches exactly once",
      "gateway optional stage fault isolation records rtk failure and dispatches exactly once",
      "gateway optional stage fault isolation records schema failure and dispatches exactly once",
      "gateway optional stage fault isolation records thinking failure and dispatches exactly once",
      "gateway optional stage fault isolation records tools failure and dispatches exactly once",
      "gateway optional stage fault isolation retains mixed failures as distinct ordered stages on one attempt"
    ],
    "tests/unit/shaping-runtime-settings-ui.test.js": [
      "preserves a refused nonsecret draft and blocks an uncertain replay",
      "saves and verifies local configuration without contacting a sidecar or loading a service"
    ],
    "tests/unit/shaping-service-provenance.test.js": [
      "optional services report observed failures an epoch summarizer rejection is explicit and preserves all messages",
      "optional services report observed failures embedding HTTP failure reports one attempt without reordered messages",
      "optional services report observed failures Headroom http is one failed call with its body retained",
      "optional services report observed failures Headroom malformed is one failed call with its body retained",
      "optional services report observed failures Headroom network is one failed call with its body retained",
      "optional services report observed failures Lingua distinguishes a service failure from its local skip gates",
      "optional services report observed failures memory preparation refuses an unisolatable collection before any mutation",
      "optional services report observed failures PXPIPE failure retains the request and redacts arbitrary error text",
      "optional services report observed failures RTK swallowed filter exceptions still reach the stage diagnostic",
      "optional services report observed failures RTK traversal failure emits a bounded diagnostic instead of an ambiguous null"
    ],
    "tests/unit/shaping-stage-provenance.test.js": [
      "explicit shaping provenance keeps historical measurement provenance unknown and rejects forged outcome codes",
      "explicit shaping provenance keeps the source execution identity across dispatch retries and separates new preparation",
      "explicit shaping provenance persists equal-byte application, failure and cancellation without guessing from bytes",
      "explicit shaping provenance rolls back a partially mutating failure, reports a bounded code, and continues once"
    ],
    "tests/unit/shaping-workbench.test.js": [
      "changes only a named plan with validated inheritance, real runtime resolution and retained receipts",
      "checks defaults after a concurrent explicit setting is deleted before promotion",
      "keeps canonical gateway order and deterministic body hashes with honest signed bytes",
      "persists control versions, plan overrides and runtime receipts across a complete disk reopen",
      "persists versions and experiment evidence across actual disk reopen",
      "preserves unavailable saved headroomLossless without offering an execution change",
      "projects actual defaults without credentials, routing overrides or endpoint settings",
      "refuses a plan write if the reviewed global permission state changed",
      "refuses an anonymous read and mutation before state access",
      "reports post-commit persistence uncertainty without claiming that activation failed",
      "reports unsupported stages and refuses unreviewed promotion",
      "requires explicit content-change consent and refuses out-of-scope settings",
      "retains immutable revisions, rejects stale revisions and does not change live settings",
      "rolls back plans settings when its retained receipt fails",
      "rolls back runtime settings when its retained receipt fails",
      "rolls settings back if writing its receipt fails inside the transaction",
      "runs an actual bounded worker, persists results and atomically promotes then rolls back",
      "runs and promotes a legacy profile without rewriting its stored version or hash",
      "validates control changes, records an immutable version and refuses stale replay",
      "validates runtime settings and redacts legacy URL credentials without replacing untouched values",
      "versions every context control with strict booleans and requires consent for content changes"
    ],
    "tests/unit/shared-analytics-projections.test.js": [
      "explicit invalidation during computation cannot repopulate old cache, and TTL preserves bounded reuse",
      "only fresh actual admission pressure reduces refresh; stale/missing evidence cannot fabricate overload",
      "policy changes that reset admission samples retain conservative analytics refresh until measurements return",
      "shared usage projections abort during pending work removes listeners and discards the abandoned projection",
      "shared usage projections failed refresh preserves old computation time and marks stale; oversize delivery closes cleanly",
      "shared usage projections refresh pressure slows projections and recovers without serializing unrelated controls",
      "shared usage projections separates period and authorized scope, bounds subscribers and fairly refreshes other periods",
      "shared usage projections shares one computation across identical scopes and keeps one latest frame for a slow subscriber",
      "worker cache respects byte/entry budgets and no version disables reuse",
      "worker queue serves a second scope ahead of a saturated scope and bounds each share",
      "worker reuse includes canonical filters, authorization, exact page and changing data version"
    ],
    "tests/unit/shared-api-cancellation.test.js": [
      "propagates caller cancellation and distinguishes it from network failure"
    ],
    "tests/unit/shared-path-failure-2951.test.js": [
      "#2951 shared-path failures do not drain a multi-key pool classifies a non-string error body",
      "#2951 shared-path failures do not drain a multi-key pool connect refused rotates without locking the key",
      "#2951 shared-path failures do not drain a multi-key pool connect timeout rotates without locking the key",
      "#2951 shared-path failures do not drain a multi-key pool dns failure rotates without locking the key",
      "#2951 shared-path failures do not drain a multi-key pool does not escalate an already-raised backoff level",
      "#2951 shared-path failures do not drain a multi-key pool leaves genuine per-key failures locking as before",
      "#2951 shared-path failures do not drain a multi-key pool nvidia model capacity rotates without locking the key",
      "#2951 shared-path failures do not drain a multi-key pool strictProxy refusal rotates without locking the key"
    ],
    "tests/unit/shutdown-flushers.test.js": [
      "shutdown flushers flushes before an application-requested exit",
      "shutdown flushers runs application flushers before the SQLite adapter closes",
      "shutdown flushers waits for priority groups and isolates failed flushers"
    ],
    "tests/unit/socks-proxy-dispatcher-2053.test.js": [
      "a socks5 proxy actually carries the request (#2053) an http proxy URL is untouched by the socks path",
      "a socks5 proxy actually carries the request (#2053) proxyAwareFetch tunnels through the socks proxy",
      "a socks5 proxy actually carries the request (#2053) the settings-screen proxy test reports a working socks proxy as working"
    ],
    "tests/unit/specialized-direct-dispatch.test.js": [
      "aborts a MiMo retry when the next reservation is refused",
      "brackets the exact paid wire request for github-messages",
      "brackets the exact paid wire request for github-responses",
      "brackets the exact paid wire request for ollama",
      "brackets the exact paid wire request for vertex",
      "does not dispatch after a rejected reservation for github-messages",
      "does not dispatch after a rejected reservation for github-responses",
      "does not dispatch after a rejected reservation for vertex",
      "does not infer nonacceptance from an oversized GitHub diagnostic body",
      "does not repeat a MiMo response marked unsafe to replay",
      "forwards both hooks through the local Ollama override without inventing a dispatch",
      "keeps replay-unsafe GitHub failures intact without trying another endpoint",
      "marks only the recognized GitHub endpoint rejection as proven nonacceptance",
      "never calls a paid endpoint when the caller was already cancelled",
      "records MiMo auth replay as two paid dispatches and excludes bootstrap",
      "retains two distinct physical callbacks across actual GitHub endpoint fallback"
    ],
    "tests/unit/specialized-dispatch-hooks.test.js": [
      "devin admits the exact prepared wire once and observes actual headers before translation",
      "devin cancels an unused response and preserves an accounting exception",
      "devin does not replay an uncertain transport failure or fabricate response headers",
      "devin does not send generation if cancelled while admission is pending",
      "devin propagates admission refusal unchanged with no paid send",
      "grok-web admits the exact prepared wire once and observes actual headers before translation",
      "grok-web cancels an unused response and preserves an accounting exception",
      "grok-web does not replay an uncertain transport failure or fabricate response headers",
      "grok-web does not send generation if cancelled while admission is pending",
      "grok-web normalization retains replay denial and cooldown without forwarding private cookies",
      "grok-web propagates admission refusal unchanged with no paid send",
      "perplexity admits the exact prepared wire once and observes actual headers before translation",
      "perplexity cancels an unused response and preserves an accounting exception",
      "perplexity does not replay an uncertain transport failure or fabricate response headers",
      "perplexity does not send generation if cancelled while admission is pending",
      "perplexity normalization retains replay denial and cooldown without forwarding private cookies",
      "perplexity propagates admission refusal unchanged with no paid send",
      "qoder admits the exact prepared wire once and observes actual headers before translation",
      "qoder cancels an unused response and preserves an accounting exception",
      "qoder does not replay an uncertain transport failure or fabricate response headers",
      "qoder does not send generation if cancelled while admission is pending",
      "qoder propagates admission refusal unchanged with no paid send",
      "windsurf admits the exact prepared wire once and observes actual headers before translation",
      "windsurf cancels an unused response and preserves an accounting exception",
      "windsurf does not replay an uncertain transport failure or fabricate response headers",
      "windsurf does not send generation if cancelled while admission is pending",
      "windsurf propagates admission refusal unchanged with no paid send",
      "zenmux-free admits the exact prepared wire once and observes actual headers before translation",
      "zenmux-free cancels an unused response and preserves an accounting exception",
      "zenmux-free does not replay an uncertain transport failure or fabricate response headers",
      "zenmux-free does not send generation if cancelled while admission is pending",
      "zenmux-free normalization retains replay denial and cooldown without forwarding private cookies",
      "zenmux-free propagates admission refusal unchanged with no paid send"
    ],
    "tests/unit/sqlite-driver-reasons-987.test.js": [
      "#987 SQLite driver chain names a reason for every link, not just that all of them failed",
      "#987 SQLite driver chain says the packaged build is missing sql.js's WASM binary when that is what broke",
      "#987 SQLite driver chain still returns an adapter when a later link works"
    ],
    "tests/unit/sqlite-runtime-abi-2493.test.js": [
      "better-sqlite3 ABI stamp (#2493) does not rewrite the ABI stamp when npm exits zero but the installed binary cannot load",
      "better-sqlite3 ABI stamp (#2493) rejects a binary stamped with a different Node ABI",
      "better-sqlite3 ABI stamp (#2493) rejects a matching stamp when an actual child load fails",
      "better-sqlite3 ABI stamp (#2493) runs postinstall against explicit test state without touching a fake production home",
      "better-sqlite3 ABI stamp (#2493) stamps an unstamped binary only after the child load succeeds",
      "runtime installs are saved, not extraneous (#1605) never passes --no-save, which would let the next install prune the engine",
      "the native install is skipped on an unsupported Node (#1657) does not attempt an install that the pin cannot satisfy",
      "the native install is skipped on an unsupported Node (#1657) still attempts the install on a supported Node major"
    ],
    "tests/unit/sqlite-runtime-startup-3312.test.js": [
      "SQLite runtime startup (#3312) does not invoke npm for missing better-sqlite3 during normal startup",
      "SQLite runtime startup (#3312) limits the postinstall native warm-up to 30 seconds on a Node 26-compatible version"
    ],
    "tests/unit/sqljs-atomic-persist.test.js": [
      "sqljs atomic persist keeps the previous file intact when the persist write fails",
      "sqljs atomic persist leaves no .tmp file behind after a successful persist",
      "sqljs atomic persist removes a stale .tmp left by a crashed prior run",
      "sqljs atomic persist writes the database file on close after writes"
    ],
    "tests/unit/sqljs-export-integrity.test.js": [
      "keeps foreign-key enforcement after a background snapshot save",
      "keeps foreign-key enforcement after an explicit durable snapshot flush",
      "restores runtime constraints even when snapshot publication fails"
    ],
    "tests/unit/sse-framing-adoption.test.js": [
      "collects ordered identity/signature/cache fields for \"\\n\", multiline=false",
      "collects ordered identity/signature/cache fields for \"\\n\", multiline=true",
      "collects ordered identity/signature/cache fields for \"\\r\", multiline=false",
      "collects ordered identity/signature/cache fields for \"\\r\", multiline=true",
      "collects ordered identity/signature/cache fields for \"\\r\\n\", multiline=false",
      "collects ordered identity/signature/cache fields for \"\\r\\n\", multiline=true",
      "framing boundaries and lifetime detects multiline real progress and replays the original bytes including metadata",
      "framing boundaries and lifetime does not promote truncated JSON to a terminal or accept a comment/id/retry as payload",
      "framing boundaries and lifetime keeps outer abort ownership during an incomplete event and releases the reader once",
      "framing boundaries and lifetime preserves an otherwise complete final JSON event without an SSE blank delimiter",
      "framing boundaries and lifetime reads progressively under a slow consumer and cancels the upstream reader once",
      "framing boundaries and lifetime releases a replay reader at normal EOF",
      "framing boundaries and lifetime retains peek byte-threshold fallback and raw bytes for oversized incomplete frames"
    ],
    "tests/unit/sse-keepalive.test.js": [
      "SSE keepalive (post-transform, every silent interval) emits pings post-transform during silence, before AND after the first chunk; translator input never sees a ping",
      "SSE keepalive (post-transform, every silent interval) keepalive timer is cleared on every terminal path (complete / error / disconnect)",
      "SSE keepalive (post-transform, every silent interval) keepaliveMs = 0 disables pings entirely",
      "SSE keepalive (post-transform, every silent interval) TTFT fire clears the keepalive"
    ],
    "tests/unit/sse-null-events.test.js": [
      "SSE null-event suppression does not format nullish payloads as SSE records",
      "SSE null-event suppression does not surface a null same-format flush chunk",
      "SSE null-event suppression preserves normal same-format chunks and the done sentinel",
      "SSE null-event suppression still emits state-derived cross-format terminal events during flush"
    ],
    "tests/unit/sse-terminal-observer.test.js": [
      "bounded typed SSE terminal observer discards records beyond either hard limit and still accepts a later typed terminal",
      "bounded typed SSE terminal observer does not treat terminal-looking ordinary content as a typed terminal",
      "bounded typed SSE terminal observer recognizes complete OpenAI, Claude, and Responses terminal records across byte boundaries",
      "bounded typed SSE terminal observer releases partial parser state without turning a fragment into a terminal",
      "bounded typed SSE terminal observer requires an explicit SSE event and JSON type to agree while accepting data-only terminals",
      "bounded typed SSE terminal observer returns null for emitted formats without an exact terminal predicate"
    ],
    "tests/unit/stage-measurement-reuse.test.js": [
      "flags every body mutation between the two tools measurements",
      "retains disabled byte stages and final size without serializing those stages"
    ],
    "tests/unit/stall-rotation-3136.test.js": [
      "stall timeout takes the account out of rotation (#3136) does not lock the account on a generic stream error",
      "stall timeout takes the account out of rotation (#3136) does not lock the account when the client disconnects",
      "stall timeout takes the account out of rotation (#3136) does not lock when the provider already reported output tokens",
      "stall timeout takes the account out of rotation (#3136) does not lock when the stall interrupts a healthy final response",
      "stall timeout takes the account out of rotation (#3136) locks at most once when abandon fires twice",
      "stall timeout takes the account out of rotation (#3136) locks the account when the stall happens mid-reasoning",
      "stall timeout takes the account out of rotation (#3136) locks the account when the stream stalls before producing an answer",
      "stall timeout takes the account out of rotation (#3136) still does not lock on a completed stream that produced content",
      "stall timeout takes the account out of rotation (#3136) still locks on an empty completed stream (unchanged path)",
      "stall timeout takes the account out of rotation (#3136) survives an onEmptyStream that rejects"
    ],
    "tests/unit/standalone-assets.test.js": [
      "standalone build assets copies static and public assets into the default standalone layout",
      "standalone build assets copies the request-sanitizing server wrapper into the standalone output",
      "standalone build assets does not modify workspace-traced CLI builds",
      "standalone build assets uses a custom Next dist directory"
    ],
    "tests/unit/start-port-env-2602.test.js": [
      "PORT governs the production start (#2602) custom-server forwards argv, which is why the flag won",
      "PORT governs the production start (#2602) defaults to 20128, the documented port, not the old 20127",
      "PORT governs the production start (#2602) defaults to the documented port only when PORT is unset",
      "PORT governs the production start (#2602) leaves the dev scripts on their own documented port",
      "PORT governs the production start (#2602) the start script no longer pins a port"
    ],
    "tests/unit/stats-summary-honesty.test.js": [
      "getStatsItems — per-row absences keep their own reason gives a token-free row no hit rate and no latency",
      "getStatsItems — per-row absences keep their own reason keeps a measured zero hit rate on a row that did send tokens",
      "getStatsSummary — an average states its own denominator averages over the rows that measured it and says how many those were",
      "getStatsSummary — an average states its own denominator still reports no hit rate when nothing was cacheable",
      "getStatsSummary — an empty selection measured nothing reports no average latency rather than an average of zero",
      "getStatsSummary — an empty selection measured nothing reports no cache hit rate rather than a rate of zero"
    ],
    "tests/unit/stdout-backpressure-2447.test.js": [
      "a stalled stdout reader cannot wedge the server (#2447) drops writes past the threshold and keeps them below it",
      "a stalled stdout reader cannot wedge the server (#2447) guards the console chokepoint rather than individual call sites",
      "a stalled stdout reader cannot wedge the server (#2447) leaves a TTY alone, since only a pipe can wedge",
      "a stalled stdout reader cannot wedge the server (#2447) reports what it dropped once the reader catches up",
      "a stalled stdout reader cannot wedge the server (#2447) the threshold is overridable without editing the file"
    ],
    "tests/unit/step-router.test.js": [
      "cascade flow through handleChat (executor mocked) does not escalate a 400: the cheap error is the answer",
      "cascade flow through handleChat (executor mocked) does not pin a different session on the same model",
      "cascade flow through handleChat (executor mocked) escalates a retryable cheap failure (429) to the strong model and records both attempts",
      "cascade flow through handleChat (executor mocked) escalates a retryable cheap failure (500) to the strong model and records both attempts",
      "cascade flow through handleChat (executor mocked) escalates a retryable cheap failure (503) to the strong model and records both attempts",
      "cascade flow through handleChat (executor mocked) is inert without config: one dispatch, original model, no tag",
      "cascade flow through handleChat (executor mocked) leaves a strong-class step on the strong model untagged",
      "cascade flow through handleChat (executor mocked) never mutates the caller's request body across the escalation",
      "cascade flow through handleChat (executor mocked) pins an escalated session: the next step goes straight to strong",
      "cascade flow through handleChat (executor mocked) routes an exploration-class step to the cheap model tagged cascade-cheap",
      "chat.js cascade wiring defaults cascadePairs to an empty array so the feature ships inert",
      "chat.js cascade wiring escalates retryable cheap failures to the strong model and pins the session",
      "chat.js cascade wiring plans only solo requests and dispatches through a cascade-aware wrapper",
      "chat.js cascade wiring re-dispatches the SAME body object, never a mutated copy",
      "chat.js cascade wiring resolves the session id from client headers/body before account selection",
      "chat.js cascade wiring tags usage rows via routeKindOverride so both attempts record their kind",
      "classifyExploration classifies a plain Q&A body as exploration",
      "classifyExploration ignores a tool_error older than the last 3 turns",
      "classifyExploration ignores edit/write calls in an assistant turn that is not the last",
      "classifyExploration is false at or above the 64K-token prompt ceiling",
      "classifyExploration is false when a tool_result with is_error is in the last 3 turns (Claude shape)",
      "classifyExploration is false when an OpenAI tool message carries is_error",
      "classifyExploration is false when the last assistant turn made a Write or str_replace call (Claude shape)",
      "classifyExploration is false when the last assistant turn made an Edit tool call (OpenAI shape)",
      "classifyExploration is false when the last assistant turn ran Bash with a heredoc, true without one",
      "classifyExploration reads the Bash command from a Claude tool_use input object",
      "classifyExploration skips system messages when counting turns",
      "isRetryableCascadeStatus treats 408/429/5xx as retryable",
      "isRetryableCascadeStatus treats 4xx (except 408/429) and 499 as non-retryable",
      "normalizeCascadePairs / normalizeModelRef accepts provider/model and provider:model spellings",
      "normalizeCascadePairs / normalizeModelRef drops malformed entries and keeps valid ones",
      "normalizeCascadePairs / normalizeModelRef returns an empty map for non-array config",
      "planCascade does not pin sessions for models outside the cascade config",
      "planCascade forgets the pin after the 30-minute TTL",
      "planCascade handles a null sid without pinning state",
      "planCascade is inert for a strong-class body (edit/write in last assistant turn)",
      "planCascade is inert when the request model is not a configured strong model",
      "planCascade is inert without config",
      "planCascade matches a colon-spelled config entry against a slash-spelled request model",
      "planCascade routes exploration-class steps to the cheap model with the cascade-cheap tag",
      "planCascade sends a pinned session straight to strong with the cascade-strong tag"
    ],
    "tests/unit/stream-branch-coverage.test.js": [
      "passthrough normalization cancel() estimates usage from accumulated content and reports aborted",
      "passthrough normalization drops a duplicate finish chunk and a duplicate [DONE]",
      "passthrough normalization flushes a trailing malformed frame exactly as received",
      "passthrough normalization normalizes a data:-without-space line and passes non-data lines through",
      "passthrough normalization normalizes delta.reasoning to delta.reasoning_content",
      "passthrough normalization re-uses provider usage on a finish chunk that already carries valid usage",
      "passthrough normalization removes an empty tool_calls array from a streaming delta",
      "passthrough normalization skips a non-JSON data line instead of forwarding garbage",
      "passthrough normalization strips Azure prompt_filter_results and content_filter_results",
      "remaining edge branches accumulates claude thinking deltas in translate mode",
      "remaining edge branches counts ollama message.tool_calls toward output length",
      "remaining edge branches drops a trailing data: null frame and normalizes a trailing no-space frame",
      "remaining edge branches skips a passthrough chunk with no valuable content",
      "remaining edge branches translates a tail chunk left in the buffer at flush time",
      "translate-mode accumulation branches accumulates ollama NDJSON message content and thinking",
      "translate-mode accumulation branches accumulates openai delta.reasoning when reasoning_content is absent",
      "translate-mode accumulation branches counts claude tool_use name and input_json fragments toward output length",
      "translate-mode accumulation branches logs the OpenAI intermediate for a cross-format pivot stream",
      "translate-mode accumulation branches splits gemini parts into content and thinking on part.thought"
    ],
    "tests/unit/stream-content-boundary.test.js": [
      "non-combo streaming content boundary (#3463) cancels a timed-out first read before replaying a delayed content frame",
      "non-combo streaming content boundary (#3463) rejects a role-only SSE stream before marking the request healthy",
      "non-combo streaming content boundary (#3463) replays only buffered keepalives and closes a timed-out endless stream"
    ],
    "tests/unit/stream-content-reader-lifetime.test.js": [
      "peek reader lifetime awaits upstream cancellation, forwards its reason, and releases once cleanup settles",
      "peek reader lifetime releases the original reader after replay reaches EOF"
    ],
    "tests/unit/stream-disconnect-finalizes-3090.test.js": [
      "a client disconnect finalizes the request detail row (#3090) fires once, not once per route",
      "a client disconnect finalizes the request detail row (#3090) fires onDisconnect when only the caller signal aborts",
      "a client disconnect finalizes the request detail row (#3090) fires onDisconnect when the stream is cancelled",
      "a client disconnect finalizes the request detail row (#3090) the disconnect handler is wired to the row finalizer"
    ],
    "tests/unit/stream-disconnect-reason-2843.test.js": [
      "disconnect reason reporting (#2843) bounds the reason so an attached payload cannot flood the log",
      "disconnect reason reporting (#2843) keeps the default reason when nothing is supplied",
      "disconnect reason reporting (#2843) names an Error reason instead of stringifying the object",
      "disconnect reason reporting (#2843) normalises a DOMException-style reason to name and message",
      "disconnect reason reporting (#2843) passes the raw reason through to onDisconnect unchanged",
      "disconnect reason reporting (#2843) renders progress detail as counters only",
      "disconnect reason reporting (#2843) reports upstream progress and terminal state when the runtime cancels the response"
    ],
    "tests/unit/stream-failure-detail-2221.test.js": [
      "#2221 a stream that fails before it starts is still recorded leaves the returned error envelope byte-identical to what callers already expect",
      "#2221 a stream that fails before it starts is still recorded records a JSON error payload served as 200 OK, with the upstream's own status",
      "#2221 a stream that fails before it starts is still recorded records a missing response body",
      "#2221 a stream that fails before it starts is still recorded records an empty 200 stream",
      "#2221 a stream that fails before it starts is still recorded records the upstream HTML error page a non-SSE content type carries"
    ],
    "tests/unit/stream-first-valid-event-gate.test.js": [
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 1: Empty stream (0 bytes) returns success=false and does NOT call onRequestSuccess",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 2: JSON error disguised in 200 stream returns success=false and does NOT call onRequestSuccess",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 3: Non-SSE HTML response returns success=false and does NOT call onRequestSuccess",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 4: Valid stream with data returns success=true and calls onRequestSuccess",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 5: Null body returns success=false and 502",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 6: Raw string error in JSON returns success=false and 502",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 7: FastAPI detail error payload returns success=false and 502",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) Case 8: Assistant output containing the word 'error' in normal payload is NOT treated as an error",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) keeps an ordinary first reader failure as a 502 account error",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) returns typed 499 without an account error when the caller aborts the first reader read",
      "Streaming first-valid-event gate (Issue 2951 Finding 3) returns typed 499 without an account error when the caller aborts the non-SSE body drain"
    ],
    "tests/unit/stream-keeps-generated-images.test.js": [
      "hasValuableContent — OpenAI format drops an empty images array rather than treating it as content",
      "hasValuableContent — OpenAI format keeps a chunk carrying only a generated image",
      "hasValuableContent — OpenAI format keeps the shapes it already kept",
      "hasValuableContent — OpenAI format still drops a chunk whose delta is genuinely empty"
    ],
    "tests/unit/stream-mode-default-3492.test.js": [
      "clientRequestedStreaming does not treat a truthy non-boolean as a request to stream",
      "clientRequestedStreaming keeps the Gemini and Antigravity surfaces on SSE regardless of the body",
      "clientRequestedStreaming reads a body with no stream key as non-streaming",
      "clientRequestedStreaming reads an explicit stream:false as non-streaming",
      "clientRequestedStreaming reads an explicit stream:true as streaming",
      "clientRequestedStreaming survives a missing body",
      "clientRequestedStreaming treats the Claude surface like OpenAI"
    ],
    "tests/unit/stream-model-echo.test.js": [
      "stream model echo keeps the exact prefixed form the client sent",
      "stream model echo leaves non-catalog providers untouched",
      "stream model echo re-injects the listing prefix when the client sent a bare name"
    ],
    "tests/unit/stream-newline-scanner.test.js": [
      "stream newline scanner drops an unterminated null data frame before the sentinel",
      "stream newline scanner flushes a final unterminated data line before the sentinel",
      "stream newline scanner keeps partial cancellation state synchronized",
      "stream newline scanner keeps same-format Responses event framing",
      "stream newline scanner preserves a UTF-8 code point split across byte chunks",
      "stream newline scanner preserves CRLF passthrough framing byte-for-byte",
      "stream newline scanner preserves delimiters split across chunks",
      "stream newline scanner preserves LF passthrough framing byte-for-byte",
      "stream newline scanner uses an index scanner instead of allocating a line array per chunk"
    ],
    "tests/unit/stream-reasoning-delta.test.js": [
      "hasValuableContent (OpenAI format) drops empty chunks",
      "hasValuableContent (OpenAI format) keeps chunks carrying delta.reasoning (ollama.com/v1 style)",
      "hasValuableContent (OpenAI format) keeps chunks carrying delta.reasoning_content (OpenAI style)"
    ],
    "tests/unit/stream-terminal-contract.test.js": [
      "post-transform SSE terminal contract does not turn literal Claude terminal prose into a normal completion",
      "post-transform SSE terminal contract emits a typed terminal before the TTFT watchdog abandons a supported stream",
      "post-transform SSE terminal contract keeps a transform-flush OpenAI terminal healthy without synthesis or delay",
      "post-transform SSE terminal contract keeps downstream cancellation as a disconnect without a synthetic terminal",
      "post-transform SSE terminal contract passes a caller abort through the streaming handler without an incomplete terminal",
      "post-transform SSE terminal contract routes emitted Claude EOF without message_stop through error instead of success",
      "post-transform SSE terminal contract synthesizes once after overflow reaches EOF and never completes",
      "post-transform SSE terminal contract synthesizes once when a supported stream ends via network reset",
      "post-transform SSE terminal contract synthesizes once when a supported stream ends via transform error",
      "post-transform SSE terminal contract treats an in-flight caller abort as a disconnect without a synthetic terminal",
      "post-transform SSE terminal contract turns an incomplete supported EOF into one typed terminal and abandonment",
      "post-transform SSE terminal contract uses the emitted Claude bytes, not source prose, to choose normal completion"
    ],
    "tests/unit/stream-to-json-converter.test.js": [
      "convertResponsesStreamToJson consumes an externally owned reader without releasing its lock",
      "convertResponsesStreamToJson parses data-only SSE (no event: line, type read from JSON payload) — SLG/singularityapi-style",
      "convertResponsesStreamToJson parses standard SSE with explicit event: lines"
    ],
    "tests/unit/stream-usage-estimate-3017.test.js": [
      "a completed stream without upstream usage is estimated (#3017) it needs text to estimate from, and does not invent numbers otherwise",
      "a completed stream without upstream usage is estimated (#3017) it only fills in when the upstream gave nothing usable",
      "a completed stream without upstream usage is estimated (#3017) the interrupted path still estimates too",
      "a completed stream without upstream usage is estimated (#3017) the success path estimates, not just the interrupted path",
      "the usage fallback behaves as the fix assumes a missing or all-zero usage is not valid",
      "the usage fallback behaves as the fix assumes a real usage block is left alone",
      "the usage fallback behaves as the fix assumes an estimate produces non-zero counts from content"
    ],
    "tests/unit/streaming-handler-branches.test.js": [
      "buildOnStreamComplete guards a rejecting verification callback is also swallowed and warned",
      "buildOnStreamComplete guards a synchronously-throwing onEmptyStream is caught, and completion is one-shot",
      "buildOnStreamComplete guards antigravity terminal verification: non-function callback ignored, throwing callback warned",
      "buildOnStreamComplete guards onStreamAbandoned after completion is a no-op; stall with no content locks",
      "handleStreamingResponse abort and fallback-logging branches caller abort observed right after the first reader read returns 499",
      "handleStreamingResponse abort and fallback-logging branches caller abort observed right after the non-SSE body read returns 499",
      "handleStreamingResponse abort and fallback-logging branches caller already aborted at entry returns the abort result immediately",
      "handleStreamingResponse abort and fallback-logging branches missing response body fails with no-body",
      "handleStreamingResponse abort and fallback-logging branches non-SSE HTML page without log.errorLine falls back to console.warn and uses the upstream status",
      "handleStreamingResponse abort and fallback-logging branches peek read error fails the stream with the reader error message",
      "handleStreamingResponse abort and fallback-logging branches upstream error framed as content fails with its status and warns without errorLine",
      "handleStreamingResponse antigravity JSON-RPC branches a bounded-read overflow (>64KB JSON) fails with the safe antigravity message",
      "handleStreamingResponse antigravity JSON-RPC branches a reader that throws mid-JSON fails with the safe antigravity message",
      "handleStreamingResponse success-path branches antigravity buffered JSON is replayed through the pipe and success notified on content",
      "handleStreamingResponse success-path branches cancelling the returned stream propagates to the upstream reader",
      "handleStreamingResponse success-path branches codex source-format fallback: unknown client format on a Responses provider still streams",
      "handleStreamingResponse success-path branches responses passthrough synthesizes a failed terminal when upstream ends without one"
    ],
    "tests/unit/streaming-interrupted-detail.test.js": [
      "interrupted streaming request detail abandons only once even if both disconnect and error fire",
      "interrupted streaming request detail does not double-finalize when a late completion races after abandon",
      "interrupted streaming request detail does not overwrite after normal completion",
      "interrupted streaming request detail keeps normal completion behavior intact (success row + usage save)",
      "interrupted streaming request detail populates streamState from live SSE chunks (passthrough)",
      "interrupted streaming request detail recovers partial provider usage from streamState when abandon fires after chunks",
      "interrupted streaming request detail writes a cancelled row with the same streamDetailId when abandoned"
    ],
    "tests/unit/streaming-placeholder-status-1625.test.js": [
      "#1625 the in-progress row does not claim an outcome it has not seen marks an interrupted stream cancelled rather than leaving it pending",
      "#1625 the in-progress row does not claim an outcome it has not seen still lets the completing write set success on the same row",
      "#1625 the in-progress row does not claim an outcome it has not seen writes the placeholder row as pending, not success"
    ],
    "tests/unit/strict-proxy-propagation.test.js": [
      "PR A: strictProxy Propagation Credential Propagation (auth.js) propagates strictProxy=false for normal provider connection path",
      "PR A: strictProxy Propagation Credential Propagation (auth.js) propagates strictProxy=true for no-auth virtual connection path",
      "PR A: strictProxy Propagation Credential Propagation (auth.js) propagates strictProxy=true for normal provider connection path",
      "PR A: strictProxy Propagation Full Propagation & Fetch Behavior (chatCore -> proxyAwareFetch) Case A: strictProxy=true + proxy failure -> direct fetch invocation count = 0",
      "PR A: strictProxy Propagation Full Propagation & Fetch Behavior (chatCore -> proxyAwareFetch) preserves the exact caller abort reason through strict proxy wrapping",
      "PR A: strictProxy Propagation Full Propagation & Fetch Behavior (chatCore -> proxyAwareFetch) strictProxy=false preserves replay safety for GET after proxy failure",
      "PR A: strictProxy Propagation Full Propagation & Fetch Behavior (chatCore -> proxyAwareFetch) strictProxy=false preserves replay safety for POST after proxy failure",
      "PR A: strictProxy Propagation Full Propagation & Fetch Behavior (chatCore -> proxyAwareFetch) uses a direct Agent without treating it as a proxy egress",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed for ambiguous disabled legacy without resolving a route",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed for direct conflict without resolving a route",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed for invalid strict proxy URL without resolving a route",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed for missing enabled legacy without resolving a route",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed on a malformed strictProxy \"false\" in the historical false-empty shape",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed on a malformed strictProxy \"true\" in the historical false-empty shape",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed on a malformed strictProxy {} in the historical false-empty shape",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed on a malformed strictProxy 1 in the historical false-empty shape",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails closed on a malformed strictProxy null in the historical false-empty shape",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are inactive before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are lookup throws before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are malformed before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are missing before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are no owner before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration fails pairless selected pools that are write fails before route resolution",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps legacy proxy non-strict provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps legacy proxy strict provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps new direct marker provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps new proxy non-strict provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps new proxy strict provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps old pool-none marker provenance out of environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration keeps selected proxy fields intact in the safe transport options",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration persists a pairless selected pool before returning usable credentials",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration persists fixed no-auth pool snapshots and carries rotating pairs without overwriting settings",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration preserves a proxy selection pair across an OAuth refresh merge",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration reads the historical false-empty tuple as unselected and permits environment policy",
      "PR A: strictProxy Propagation Pre-egress proxy provenance migration returns no normal credentials when an active connection selection is unavailable"
    ],
    "tests/unit/stt-combo-3600.test.js": [
      "STT combo expansion (#3600) falls back to the next combo member when the first fails",
      "STT combo expansion (#3600) still routes a plain model straight through"
    ],
    "tests/unit/stt-core-dispatch.test.js": [
      "error propagation a thrown fetch becomes a 502, never an unhandled rejection",
      "error propagation assemblyai: an upload failure propagates the upload status without submitting",
      "error propagation assemblyai: upload → submit → poll completes with the transcript; poll error propagates",
      "error propagation upstream 429 with a JSON error body keeps the status and extracts the message",
      "error propagation upstream 500 with a plain-text body keeps the text",
      "provider dispatch and request shape deepgram with explicit language sets language= and not detect_language",
      "provider dispatch and request shape deepgram: raw binary body, model in query, language falls back to detect_language",
      "provider dispatch and request shape gemini: inline_data with base64 audio and language appended to the prompt",
      "provider dispatch and request shape huggingface: rejects a path-traversal model id before any fetch",
      "provider dispatch and request shape openai-compatible default: multipart with model and only the whitelisted optional fields",
      "provider dispatch and request shape per-connection baseUrl override redirects the request and strips the trailing slash",
      "trust boundary 400 when the file field is missing, before any fetch",
      "trust boundary 400 when the provider has no sttConfig",
      "trust boundary 401 when auth is required and no credential is present; authType none needs no token"
    ],
    "tests/unit/stt-timestamp-granularities-1034.test.js": [
      "STT forwards timestamp_granularities (#1034) accepts the bracketless spelling and normalises it",
      "STT forwards timestamp_granularities (#1034) does not duplicate when a client sends both spellings",
      "STT forwards timestamp_granularities (#1034) forwards every value of the repeated bracketed field",
      "STT forwards timestamp_granularities (#1034) sends nothing when the client asks for nothing",
      "STT forwards timestamp_granularities (#1034) the previously forwarded fields are unchanged"
    ],
    "tests/unit/suggested-models-ssrf.test.js": [
      "suggested-models SSRF boundary rejects an unknown filter before fetch",
      "suggested-models SSRF boundary rejects untrusted openrouter-free source http://169.254.169.254/latest/meta-data/ before fetch",
      "suggested-models SSRF boundary rejects untrusted openrouter-free source https://opencode.ai/zen/v1/models before fetch",
      "suggested-models SSRF boundary rejects untrusted openrouter-free source https://openrouter.ai@127.0.0.1/private before fetch",
      "suggested-models SSRF boundary uses the canonical registry source for a valid type"
    ],
    "tests/unit/sumopod-x5lab-provider-routes.test.js": [
      "SumoPod and X5Lab saved connection tests keeps 'sumopod' proxy failures bounded",
      "SumoPod and X5Lab saved connection tests keeps 'x5lab' proxy failures bounded",
      "SumoPod and X5Lab saved connection tests uses the saved proxy for 'sumopod' /models validation",
      "SumoPod and X5Lab saved connection tests uses the saved proxy for 'x5lab' /models validation"
    ],
    "tests/unit/sumopod-x5lab-providers.test.js": [
      "SumoPod and X5Lab API-key providers registers 'sumopod' as a Bearer OpenAI-compatible passthrough",
      "SumoPod and X5Lab API-key providers registers 'x5lab' as a Bearer OpenAI-compatible passthrough",
      "SumoPod and X5Lab API-key providers ships the official 128px 'sumopod' provider mark",
      "SumoPod and X5Lab API-key providers ships the official 128px 'x5lab' provider mark",
      "SumoPod and X5Lab API-key providers uses a Bearer API key for 'sumopod'",
      "SumoPod and X5Lab API-key providers uses a Bearer API key for 'x5lab'"
    ],
    "tests/unit/supporting-page-read-errors.test.js": [
      "makes a failed refresh visible beside retained data, case 0",
      "makes a failed refresh visible beside retained data, case 1",
      "preserves authentication refusals for a partial read",
      "retains the gateway failure notice when the process health read fails",
      "scopes a failed version read while retaining successful process health"
    ],
    "tests/unit/system-cache-control-2069.test.js": [
      "system-prompt cache_control survives to a preserving provider (#2069) collapses back to the original string for a non-preserving provider",
      "system-prompt cache_control survives to a preserving provider (#2069) drops empty blocks rather than emitting empty text entries",
      "system-prompt cache_control survives to a preserving provider (#2069) is byte-identical to the old shape when no marker is present",
      "system-prompt cache_control survives to a preserving provider (#2069) keeps the block shape and the marker when one is present",
      "system-prompt cache_control survives to a preserving provider (#2069) preserves the marker through the filter for a preserving provider",
      "system-prompt cache_control survives to a preserving provider (#2069) still handles a plain string system prompt"
    ],
    "tests/unit/system-import-ui.test.js": [
      "keeps a committed import with failed process refresh as a warning, with route message false",
      "keeps a committed import with failed process refresh as a warning, with route message true",
      "keeps a lost import response uncertain without claiming commit or automatically resubmitting",
      "keeps password refusal distinct from a committed partial import"
    ],
    "tests/unit/system-inject-responses-shape-3390.test.js": [
      "a Responses input array always gets a typed item (#3390) appends into an existing system item as typed content",
      "a Responses input array always gets a typed item (#3390) leaves a chat-completions messages array on the plain string shape",
      "a Responses input array always gets a typed item (#3390) prefers top-level instructions when present, touching neither array",
      "a Responses input array always gets a typed item (#3390) still types it when the label does say responses",
      "a Responses input array always gets a typed item (#3390) types the injected item even when the format label is not responses",
      "injectSystemPrompt never mutates caller collections (audit finding 11) claude: caller system array untouched, body gets a new one",
      "injectSystemPrompt never mutates caller collections (audit finding 11) gemini: caller systemInstruction parts array untouched",
      "injectSystemPrompt never mutates caller collections (audit finding 11) openai: caller messages array and the system message content array untouched"
    ],
    "tests/unit/system-state-queries.test.js": [
      "getSpendWindow against a real SQLite file is served by idx_uh_ts rather than a table scan",
      "getSpendWindow against a real SQLite file sums cost inside the window only, and reports 0 for an empty window",
      "getTrafficWindow against a real SQLite file answers an empty table without inventing a percentile",
      "getTrafficWindow against a real SQLite file counts only rows inside the window and reports the newest event unbounded",
      "getTrafficWindow against a real SQLite file is served by idx_rs_ts rather than a table scan",
      "getTrafficWindow against a real SQLite file returns a null percentile when every row in the window is unmeasured",
      "upstream health counts classifies a connection as degraded only on a persisted error signal",
      "upstream health counts classifies persisted 429 as rate-limited and returns no connection data",
      "upstream health counts keeps persisted failures degraded even when the connection is disabled"
    ],
    "tests/unit/system-state.test.js": [
      "abort propagation refuses before touching any source when the signal is already aborted",
      "abort propagation stops at the next source boundary when the client aborts mid-flight",
      "authentication on the existing global gate is not on the public allow-list or a public prefix",
      "authentication on the existing global gate lets an authenticated dashboard session through the same gate",
      "authentication on the existing global gate refuses an unauthenticated request with 401 — never 200",
      "GET /api/system/state — shape, units and windows carries generatedAt, the rolling window and every documented measure",
      "GET /api/system/state — shape, units and windows clamps windowSeconds to the supported range instead of trusting the caller",
      "GET /api/system/state — shape, units and windows computes throughput per second and error rate as a ratio over the window",
      "GET /api/system/state — shape, units and windows gives every measure an explicit unit matching the exported contract",
      "GET /api/system/state — shape, units and windows names degraded Providers with safe likely causes from the same bounded read",
      "GET /api/system/state — shape, units and windows names the source table and the index each answered measure was read through",
      "GET /api/system/state — shape, units and windows states the time window each measure covers — rolling for traffic, instant for upstreams",
      "partial source failure degrades to null, never to a 500 never exposes a repository exception through unavailable state",
      "partial source failure degrades to null, never to a 500 nulls only the failed source's measures and keeps 200 for the rest",
      "partial source failure degrades to null, never to a 500 survives every source failing at once and still answers 200 with a full shape",
      "the null contract — unanswerable is null, never zero always reports failoverCount as null with a stated reason",
      "the null contract — unanswerable is null, never zero reports freshness as empty with no event, and live/idle against the window",
      "the null contract — unanswerable is null, never zero returns null error rate on an empty database rather than a 0 that reads as healthy"
    ],
    "tests/unit/systemInject-dedup.test.js": [
      "systemInject deduplication Claude format appends prompt to existing system string",
      "systemInject deduplication Claude format does NOT duplicate before cache_control on second call",
      "systemInject deduplication Claude format does NOT duplicate when called twice on system array",
      "systemInject deduplication Claude format does NOT duplicate when called twice on system string",
      "systemInject deduplication Claude format injects into system array without cache_control",
      "systemInject deduplication Claude format injects prompt into empty system string",
      "systemInject deduplication Claude format inserts before cache_control block",
      "systemInject deduplication Gemini format appends to existing systemInstruction",
      "systemInject deduplication Gemini format creates systemInstruction if none exists",
      "systemInject deduplication Gemini format does NOT duplicate when called twice",
      "systemInject deduplication Gemini format handles Antigravity wrapped format",
      "systemInject deduplication Gemini format handles snake_case system_instruction",
      "systemInject deduplication multi-turn conversation simulation does NOT accumulate duplicates across multiple turns (Claude)",
      "systemInject deduplication multi-turn conversation simulation does NOT accumulate duplicates across multiple turns (OpenAI)",
      "systemInject deduplication OpenAI format appends to existing system message",
      "systemInject deduplication OpenAI format creates system message if none exists",
      "systemInject deduplication OpenAI format does NOT duplicate when called twice",
      "systemInject deduplication OpenAI format handles array content in Responses API",
      "systemInject deduplication OpenAI format handles developer role",
      "systemInject deduplication OpenAI format handles instructions field"
    ],
    "tests/unit/systeminject-part-type-3202.test.js": [
      "system injection uses the part type the body actually speaks (#3202) appends a text part on the chat/completions path",
      "system injection uses the part type the body actually speaks (#3202) keeps the Responses part type on the Responses path",
      "system injection uses the part type the body actually speaks (#3202) mirrors an existing input_text array even off the Responses path",
      "system injection uses the part type the body actually speaks (#3202) never emits a Responses part type on the chat/completions path",
      "system injection uses the part type the body actually speaks (#3202) still appends to a string system message"
    ],
    "tests/unit/technical-telemetry.test.js": [
      "does not restore over another owner response wrapper and stops new observations",
      "drops excess technical spans under exporter pressure while counting every response",
      "ends spans and preserves HTTP writes when metric instruments throw",
      "exports real SDK metrics and spans for an actual HTTP response without content or identity",
      "holds the active measure until response completion and finalizes once",
      "is disabled by default and rejects ambiguous collector configuration",
      "records abandonment independently of a provisional successful status",
      "sends bounded OTLP HTTP exports only to the explicit collector",
      "the shipped HTTP wrapper observes actual requests and tolerates observer failure",
      "uses fixed route classes without identifiers or query dimensions",
      "zero trace sampling still records unsampled HTTP metrics"
    ],
    "tests/unit/telemetry-vocabulary.test.js": [
      "fmtDuration — a latency that may never have been measured keeps the magnitude of a sub-second duration instead of collapsing to 0.0s",
      "fmtDuration — a latency that may never have been measured renders a never-recorded latency distinctly from a measured zero",
      "fmtDuration — a latency that may never have been measured renders seconds above a second",
      "fmtPercent — a whole-percent headroom figure renders a measured zero as 0%",
      "fmtPercent — a whole-percent headroom figure renders an unknown total as n/a rather than 0%",
      "fmtPercent — a whole-percent headroom figure rounds a measured percentage",
      "fmtRate — a ratio whose denominator may be zero renders a measured rate",
      "fmtRate — a ratio whose denominator may be zero renders a measured zero rate as 0.0%",
      "fmtRate — a ratio whose denominator may be zero renders an uncomputable rate as n/a, never as 0.0%",
      "measurement vocabulary counts a real zero as measured and nothing else",
      "measurement vocabulary gives the two absent cases distinct words"
    ],
    "tests/unit/thinking-effort-openai-max-clamp.test.js": [
      "applyThinking (openai): clamp max effort to xhigh \"high\" passes through unchanged",
      "applyThinking (openai): clamp max effort to xhigh \"xhigh\" passes through unchanged (highest valid OpenAI level)",
      "applyThinking (openai): clamp max effort to xhigh client output_config.effort:\"max\" → reasoning_effort:\"xhigh\" (not \"max\")",
      "applyThinking (openai): clamp max effort to xhigh direct reasoning_effort:\"max\" clamped to \"xhigh\"",
      "applyThinking (openai): clamp max effort to xhigh max budget (thinking.budget_tokens:128000) → reasoning_effort:\"xhigh\" (budgetToLevel caps at xhigh)"
    ],
    "tests/unit/thinking-level-clamp-3572.test.js": [
      "thinking level never escapes the declared set (#3572) declares a ladder covering every level the level sets use",
      "thinking level never escapes the declared set (#3572) holds the invariant across every shipped openai-format level set",
      "thinking level never escapes the declared set (#3572) no longer returns xhigh without checking the set contains it"
    ],
    "tests/unit/thinking-levels-gpt56-sol.test.js": [
      "getThinkingLevels does not add max for other codex models",
      "getThinkingLevels does not add max for other Codex models",
      "getThinkingLevels does not expose Codex-only GPT-5.6 overrides on Kiro",
      "getThinkingLevels returns Codex levels for gpt-5.6-luna",
      "getThinkingLevels returns Codex levels for gpt-5.6-luna-review",
      "getThinkingLevels returns Codex levels for gpt-5.6-sol",
      "getThinkingLevels returns Codex levels for gpt-5.6-sol-review",
      "getThinkingLevels returns Codex levels for gpt-5.6-terra",
      "getThinkingLevels returns Codex levels for gpt-5.6-terra-review"
    ],
    "tests/unit/thinking-levels-kiro.test.js": [
      "getThinkingLevels for Kiro advertises native levels for supported Kiro models",
      "getThinkingLevels for Kiro does not advertise native intensity for legacy Kiro models"
    ],
    "tests/unit/thinking-signature-preserved-2693.test.js": [
      "a signed thinking block is replayed byte-for-byte (#2693) an anthropic-compatible provider does not overwrite a real signature",
      "a signed thinking block is replayed byte-for-byte (#2693) and replaces one that cannot be a Claude signature at all",
      "a signed thinking block is replayed byte-for-byte (#2693) but still fills one in when the upstream sent none",
      "a signed thinking block is replayed byte-for-byte (#2693) Claude native keeps a valid signature and drops an invalid block, unchanged",
      "a signed thinking block is replayed byte-for-byte (#2693) DeepSeek still passes its blocks through untouched",
      "a signed thinking block is replayed byte-for-byte (#2693) the sanity check: the fixture signature is one the validator accepts"
    ],
    "tests/unit/thinking-strip.test.js": [
      "stripHistoricalThinking caps notes at notesMax and flags truncation",
      "stripHistoricalThinking inserts a placeholder when stripping empties the content array",
      "stripHistoricalThinking keeps thinking when there is only one assistant turn",
      "stripHistoricalThinking leaves string content untouched",
      "stripHistoricalThinking never touches tool_use or tool_result blocks",
      "stripHistoricalThinking passes through empty and non-array input",
      "stripHistoricalThinking passes through garbage entries",
      "stripHistoricalThinking returns the input reference unchanged and never mutates a frozen input",
      "stripHistoricalThinking strips historical thinking but keeps the last assistant turn's thinking",
      "stripHistoricalThinking strips redacted_thinking blocks"
    ],
    "tests/unit/thinking-unified-format-matrix.test.js": [
      "disable paths per capability-declared format claude-adaptive: mode none disables on a model that can disable",
      "disable paths per capability-declared format deepseek: mode none disables on a model that can disable",
      "disable paths per capability-declared format kimi: mode none disables on a model that can disable",
      "disable paths per capability-declared format qwen: mode none disables on a model that can disable",
      "extractThinking shapes gemini thinkingConfig.thinkingLevel lowercases into a level",
      "extractThinking shapes ollama think: truthy number is auto, zero is none",
      "extractThinking shapes ollama think: unknown non-empty string becomes a level",
      "extractThinking shapes qwen enable_thinking with and without a positive budget",
      "format derivation from targetFormat (no capability format) claude target derives claude-budget and auto maps to the fixed auto budget",
      "format derivation from targetFormat (no capability format) claude target with a level uses the shared level-to-budget map",
      "format derivation from targetFormat (no capability format) gemini target: large budget raises maxOutputTokens to the top floor capped by maxOutput",
      "format derivation from targetFormat (no capability format) gemini-cli envelope: a non-object request.generationConfig is replaced",
      "format derivation from targetFormat (no capability format) kiro target is a body no-op (thinking travels via system tag)",
      "format derivation from targetFormat (no capability format) unknown target format falls back to the openai wire",
      "hunyuan and step branches hunyuan: none disables, auto enables without budget, level carries the mapped budget",
      "hunyuan and step branches step: an unusable intent mode sends no field at all",
      "hunyuan and step branches step: none omits the field, levels clamp xhigh/max down to high",
      "level-set clamps kimi format: a level outside the kimi enum sends no effort field",
      "level-set clamps ollama provider format: unknown level defaults think to medium",
      "level-set clamps openai format: level below every supported entry takes the lowest on offer",
      "parseSuffix / stripThinkingSuffix fallthroughs stripThinkingSuffix is a no-op on non-strings and plain names",
      "parseSuffix / stripThinkingSuffix fallthroughs unrecognized suffix value yields a null override"
    ],
    "tests/unit/token-refresh-analytics-3570.test.js": [
      "only a connection with rotation state is reported a missing or non-array input is an empty report, not a crash",
      "only a connection with rotation state is reported an API-key connection is left out rather than listed as unknown forever",
      "only a connection with rotation state is reported an API-key connection that does carry an expiry is still reported",
      "only a connection with rotation state is reported an OAuth connection rotates even before its first refresh",
      "the countdown is the thing that makes the card live an already-dead token reports a negative rather than a clamped zero",
      "the countdown is the thing that makes the card live an epoch-milliseconds expiry is read as milliseconds",
      "the countdown is the thing that makes the card live an epoch-seconds expiry is read as seconds, not as 1970",
      "the countdown is the thing that makes the card live an unparseable stamp is null, not NaN",
      "the countdown is the thing that makes the card live time to expiry and time since refresh are relative to now",
      "the endpoint the card polls is never cached, so polling it returns moving numbers",
      "the endpoint the card polls makes no provider call, so a poll costs one local read",
      "the endpoint the card polls reads persisted connections and returns the summary",
      "the report carries no credential nothing from the connection's token material reaches the row",
      "the report carries no credential the row is a fixed whitelist, not the connection with fields deleted",
      "the status matches what the router would actually do a provider with no entry of its own falls back to the shared buffer",
      "the status matches what the router would actually do inside the provider's own refresh lead is due",
      "the status matches what the router would actually do no expiry at all is unknown rather than assumed healthy",
      "the status matches what the router would actually do outside it is fresh",
      "the status matches what the router would actually do past its expiry is expired",
      "the status matches what the router would actually do the threshold is the provider's, not a number invented here",
      "the summary answers what the card shows at a glance counts each status and how many are tracked at all",
      "the summary answers what the card shows at a glance names the next rotation, skipping the ones already past it",
      "the summary answers what the card shows at a glance orders by soonest expiry, with the ones that have none last",
      "the summary answers what the card shows at a glance stamps when it was generated, so a stale poll is visible"
    ],
    "tests/unit/token-refresh-check-and-refresh.test.js": [
      "github copilot branch expiry math copilotTokenExpiresAt (epoch seconds) is converted to ms before the buffer comparison: far-future is not refreshed",
      "github copilot branch expiry math updateProviderCredentials for copilot writes exactly providerSpecificData, no stray fields",
      "options.force must be exactly true, not merely truthy force: \"yes\" (truthy but not === true) does not bypass the shouldRefreshCredentials gate",
      "proactive-refresh log carries the exact expiresIn/lastRefreshAt fields a lastRefreshAt on the credentials is passed through verbatim, not nulled",
      "proactive-refresh log carries the exact expiresIn/lastRefreshAt fields a null expiresAt logs expiresIn: null and lastRefreshAt: null when absent"
    ],
    "tests/unit/token-refresh-codebuddy-trae.test.js": [
      "classifyOAuthRefreshError flags invalid_grant as permanent",
      "classifyOAuthRefreshError flags refresh_token_reused and refresh_token_invalidated as permanent",
      "classifyOAuthRefreshError tolerates non-JSON error text without throwing",
      "classifyOAuthRefreshError treats an unrecognized error as transient (retryable)",
      "refreshCodebuddyIntlToken (codebuddy-intl) failure path does not persist a bad expiresAt",
      "refreshCodebuddyIntlToken (codebuddy-intl) uses the www.codebuddy.ai domain header and its own refresh URL",
      "refreshCodebuddyToken (codebuddy-cn) a thrown transport error is caught and returns null rather than escaping",
      "refreshCodebuddyToken (codebuddy-cn) does not persist a bad expiresAt: a non-zero code fails the refresh with null",
      "refreshCodebuddyToken (codebuddy-cn) fails closed when code is 0 but accessToken is missing",
      "refreshCodebuddyToken (codebuddy-cn) keeps the old refresh token when the response does not rotate one",
      "refreshCodebuddyToken (codebuddy-cn) posts an empty JSON body with the Tencent-specific headers",
      "refreshTraeToken — real request shape via an injected registry entry fails without persisting a token when the response carries no AccessToken",
      "refreshTraeToken — real request shape via an injected registry entry normalizes a string ISO ExpiresAt into a positive expiresIn",
      "refreshTraeToken — real request shape via an injected registry entry posts ClientID/RefreshToken/ClientSecret/UserID and normalizes a numeric ExpiresAt",
      "refreshTraeToken — real request shape via an injected registry entry returns null on a non-ok upstream response"
    ],
    "tests/unit/token-refresh-credential-field-guards.test.js": [
      "expiresAt (no expiresIn) takes the else-if branch and computes expiresIn from the clock",
      "neither expiresIn nor expiresAt present writes no expiresAt/expiresIn keys",
      "no accessToken in the input means no accessToken key in the write",
      "no idToken in the input means no idToken key in the write",
      "no lastRefreshAt in the input means no lastRefreshAt key in the write"
    ],
    "tests/unit/token-refresh-credential-fields.test.js": [
      "checkAndRefreshToken: github copilot missing-token and expiry math a copilot refresh that resolves falsy leaves providerSpecificData untouched",
      "checkAndRefreshToken: github copilot missing-token and expiry math a missing copilotToken forces a refresh with remaining computed as 0 - now (negative), not skipped",
      "checkAndRefreshToken: needsProjectId false branch and creds.id fallback a provider not needing a projectId (claude) never calls getProjectIdForConnection even with a fresh accessToken and no existing projectId",
      "checkAndRefreshToken: needsProjectId false branch and creds.id fallback creds.id is used as connectionId fallback when connectionId is absent",
      "releaseConnection calls removeConnection and logs the exact connectionId field",
      "updateProviderCredentials field writes a raw expiresAt with no existingProviderSpecificData still writes providerSpecificData for a bare copilotToken",
      "updateProviderCredentials field writes a thrown DB error logs the exact connectionId and error.message, then returns false",
      "updateProviderCredentials field writes logs success:true with the exact connectionId on a truthy DB write",
      "updateProviderCredentials field writes writes idToken and lastRefreshAt when present"
    ],
    "tests/unit/token-refresh-dedup-contracts.test.js": [
      "chainPeers caps members per chain, evicting the oldest",
      "chainPeers expires members past the chain TTL",
      "chainPeers lists the other connections seen with the token, excluding the asker",
      "connsLabel caps at 3 with a +N tail",
      "dedupRefresh coalescing a failed refresh is evicted so the next call retries",
      "dedupRefresh coalescing a settled result is reused within its TTL, then expires",
      "dedupRefresh coalescing concurrent calls on the same provider:token share one in-flight promise",
      "dedupRefresh coalescing different tokens do not coalesce",
      "dedupRefresh coalescing no token: every call runs its own fn",
      "tokenFingerprint is the first 8 hex of sha256 and never the token",
      "tokenFingerprint null for empty input"
    ],
    "tests/unit/token-refresh-dispatch.test.js": [
      "tokenRefresh dispatch getAccessToken default: unsupported provider → null",
      "tokenRefresh dispatch getAccessToken returns null for missing/invalid refreshToken",
      "tokenRefresh dispatch refreshTokenByProvider returns null without refreshToken"
    ],
    "tests/unit/token-refresh-expiry-math.test.js": [
      "getEffectiveRefreshLeadMs — lead never swallows the whole lifetime clamps the codex 5-day lead against a 1-hour-lived token",
      "getEffectiveRefreshLeadMs — lead never swallows the whole lifetime falls back to the raw lead when lastRefreshAt is absent (no lifetime known)",
      "getEffectiveRefreshLeadMs — lead never swallows the whole lifetime halves the lead when the configured lead exceeds the token lifetime",
      "getEffectiveRefreshLeadMs — lead never swallows the whole lifetime keeps the configured lead when lifetime is comfortably longer",
      "getRefreshLeadMs — per-connection override bounds rejects an override above the 30-day cap and keeps the provider default",
      "getRefreshLeadMs — per-connection override bounds rejects zero, negative, NaN and string overrides",
      "shouldRefreshCredentials — no early, no late codex staleness treats a missing lastRefreshAt as stale",
      "shouldRefreshCredentials — no early, no late codex: fresh lastRefreshAt and far expiry means no refresh",
      "shouldRefreshCredentials — no early, no late codex: proactively refreshes on staleness even when expiry is far out",
      "shouldRefreshCredentials — no early, no late does not refresh a token with 10 minutes left on the default 5-minute lead",
      "shouldRefreshCredentials — no early, no late reads a seconds-epoch expiresAt as seconds, not milliseconds",
      "shouldRefreshCredentials — no early, no late refreshes a token inside the default 5-minute lead",
      "shouldRefreshCredentials — no early, no late refreshes an already-expired token",
      "shouldRefreshCredentials — no early, no late returns false for null credentials"
    ],
    "tests/unit/token-refresh-generic.test.js": [
      "refreshAccessToken — config-driven profiles claude: JSON body, client_id only (no client_secret)",
      "refreshAccessToken — config-driven profiles dedupes concurrent calls with same refresh token (same dedupKey)",
      "refreshAccessToken — config-driven profiles github: omits client_secret when config has none",
      "refreshAccessToken — config-driven profiles iflow: Basic Auth header from clientId:clientSecret, form body keeps client_secret",
      "refreshAccessToken — config-driven profiles kimi: merges X-Msh-* headers from credentials.providerSpecificData.deviceId",
      "refreshAccessToken — config-driven profiles returns null on non-ok response",
      "refreshAccessToken — config-driven profiles returns null when refreshToken missing",
      "refreshAccessToken — legacy generic path (no profile) still works for an unprofiled provider via config.refreshUrl/clientId/clientSecret"
    ],
    "tests/unit/token-refresh-handler-map-coverage.test.js": [
      "formatProviderCredentials gemini shape keeps apiKey, accessToken and projectId for gemini",
      "getAllAccessTokens collects tokens only for active connections with a provider",
      "getAllAccessTokens returns an empty object when connections is absent or not an array",
      "REFRESH_HANDLERS dispatch antigravity routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch claude routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch codebuddy-cn routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch codebuddy-intl routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch codex routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch gcli routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch gemini routes through the google refresh path",
      "REFRESH_HANDLERS dispatch gemini-cli routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch github routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch grok-cli routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch iflow routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch kimi routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch kimi-coding routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch kiro routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch trae routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch vertex returns null when apiKey is not service-account JSON",
      "REFRESH_HANDLERS dispatch vertex-partner returns null when apiKey is not service-account JSON",
      "REFRESH_HANDLERS dispatch windsurf routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch xai routes to a mocked provider refresh and returns its result",
      "REFRESH_HANDLERS dispatch zed routes to a mocked provider refresh and returns its result",
      "refreshVertexToken network-error path returns null when the token mint fetch throws (catch path)"
    ],
    "tests/unit/token-refresh-lead.test.js": [
      "per-connection refresh lead overrides does not repeatedly refresh a newly issued short-lived token",
      "per-connection refresh lead overrides rejects invalid override values and keeps the provider default",
      "per-connection refresh lead overrides uses a finite positive connection override for on-request refresh",
      "per-connection refresh lead overrides uses the connection override in the background refresh selector"
    ],
    "tests/unit/token-refresh-local-persist.test.js": [
      "checkAndRefreshToken a failed refresh returns the ORIGINAL bag — a still-valid token is never wiped",
      "checkAndRefreshToken an error-object refresh (invalid_grant) also leaves the stored bag untouched",
      "checkAndRefreshToken does nothing when the token is not near expiry",
      "checkAndRefreshToken falls back to creds.id as connectionId so persistence targets the right row",
      "checkAndRefreshToken github: keeps the dying copilot token when the exchange fails",
      "checkAndRefreshToken github: leaves a copilot token with 30 minutes left alone",
      "checkAndRefreshToken github: refreshes a copilot token inside the 5-minute buffer",
      "checkAndRefreshToken github: refreshes the copilot token when it is missing even if the OAuth token is fresh",
      "checkAndRefreshToken persists and returns the refreshed bag with a recomputed expiresAt",
      "refreshGitHubAndCopilotTokens chains the fresh GitHub access token into the copilot exchange",
      "refreshGitHubAndCopilotTokens propagates a failed GitHub refresh without calling the copilot exchange",
      "refreshGitHubAndCopilotTokens returns the GitHub bag alone when the copilot exchange fails",
      "releaseConnection evicts the projectId cache entry for the closed connection",
      "releaseConnection is a no-op for a falsy id",
      "updateProviderCredentials — expiry normalization derives expiresIn from a bare ISO expiresAt",
      "updateProviderCredentials — expiry normalization drops an unparsable expiresAt instead of storing garbage",
      "updateProviderCredentials — expiry normalization expiresIn wins over expiresAt and produces both fields",
      "updateProviderCredentials — expiry normalization merges providerSpecificData over the existing bag rather than replacing it",
      "updateProviderCredentials — expiry normalization returns false and swallows a localDb write failure",
      "updateProviderCredentials — issue record on rotation keeps the original issue timestamp when the token did not rotate",
      "updateProviderCredentials — issue record on rotation seeds the issue record when none was persisted yet",
      "updateProviderCredentials — issue record on rotation stamps refreshTokenIssuedAt and Fp on a genuine rotation"
    ],
    "tests/unit/token-refresh-log-and-merge.test.js": [
      "a credentials object with a real expiresAt logs a numeric expiresIn, not null",
      "copilot log reports expiresIn: \"missing\" (string) when copilotToken is absent, not a number",
      "github with no providerSpecificData at all does not throw and still forces a copilot refresh",
      "mergedCreds passed to updateProviderCredentials carries existingProviderSpecificData from the prior creds"
    ],
    "tests/unit/token-refresh-merge-and-project.test.js": [
      "_refreshProjectId: resolved projectId is persisted calls updateProviderCredentials with the resolved projectId once getProjectIdForConnection resolves",
      "_refreshProjectId: resolved projectId is persisted does not persist when getProjectIdForConnection resolves falsy",
      "checkAndRefreshToken: does not refresh when _shouldRefreshCredentials is false and force is not set skips the refresh call entirely",
      "releaseConnection is a no-op for a falsy connectionId (does not call removeConnection)",
      "updateProviderCredentials: field-by-field merge a copilotToken-only update writes just that field into providerSpecificData, not copilotTokenExpiresAt",
      "updateProviderCredentials: field-by-field merge an invalid expiresAt with no expiresIn writes neither field",
      "updateProviderCredentials: field-by-field merge keeps the original issued-at when the refresh token is unchanged",
      "updateProviderCredentials: field-by-field merge merges providerSpecificData onto existingProviderSpecificData rather than replacing it",
      "updateProviderCredentials: field-by-field merge normalizes a raw expiresAt into expiresIn when expiresIn is absent",
      "updateProviderCredentials: field-by-field merge prefers expiresIn over a raw expiresAt when both are present",
      "updateProviderCredentials: field-by-field merge restarts refreshTokenIssuedAt/Fp when the refresh token actually rotated",
      "updateProviderCredentials: field-by-field merge returns false and logs, without throwing, when updateProviderConnection rejects",
      "updateProviderCredentials: field-by-field merge returns true when updateProviderConnection resolves truthy, false when it resolves falsy",
      "updateProviderCredentials: field-by-field merge writes accessToken only when present, not when absent"
    ],
    "tests/unit/token-refresh-merge-failure.test.js": [
      "mergeRefreshedCredentials computes expiresAt from expiresIn against the given clock and prefers it over a raw expiresAt",
      "mergeRefreshedCredentials keeps the old refresh_token when the provider rotates only the access token",
      "mergeRefreshedCredentials merges providerSpecificData shallowly instead of replacing it",
      "mergeRefreshedCredentials passes an unrecoverable error object through unmodified (retry loop must stop)",
      "mergeRefreshedCredentials returns null on a failed refresh so a still-valid stored token survives",
      "mergeRefreshedCredentials stamps lastRefreshAt whenever a token landed",
      "mergeRefreshedCredentials stores the rotated refresh_token when the provider returns a new one",
      "refreshProviderCredentials merges the dispatched refresh against the current bag",
      "refreshProviderCredentials propagates a null refresh as null (caller keeps the old bag)",
      "refreshProviderCredentials returns null for null input without dispatching a refresh",
      "withCredentialRefreshLock — one refresh per connection coalesces concurrent refreshes of the same connection into one call",
      "withCredentialRefreshLock — one refresh per connection does not coalesce different connections of the same provider",
      "withCredentialRefreshLock — one refresh per connection releases the lock after completion so a later refresh runs again",
      "withCredentialRefreshLock — one refresh per connection releases the lock even when the refresh throws"
    ],
    "tests/unit/token-refresh-private-helpers.test.js": [
      "_refreshProjectId verification-hooks branch: only antigravity gets real hooks gemini-cli (needs a projectId but is not antigravity) calls getProjectIdForConnection with an empty hooks object",
      "needsProjectId allowlist (via checkAndRefreshToken, provider not in the list) a grok-cli refresh never calls createAntigravityVerificationHooks or getProjectIdForConnection",
      "normalizeExpiresAt falsy guard (via updateProviderCredentials else-if) an empty-string expiresAt with no expiresIn writes neither field (falsy short-circuits before Date parsing)"
    ],
    "tests/unit/token-refresh-provider-bodies.test.js": [
      "null-refresh providers zed and windsurf refreshers return null (long-lived credential, re-login on expiry)",
      "refreshCodexToken malformed JSON on 200 fails the refresh with null, not stored garbage",
      "refreshCodexToken sends a JSON body with the codex client_id and grant_type=refresh_token",
      "refreshCopilotToken GETs the copilot_internal token endpoint with token auth and returns {token, expiresAt}",
      "refreshCopilotToken returns null without a call when the GitHub access token is missing",
      "refreshGoogleToken returns null and never throws when the success body is not JSON",
      "refreshGoogleToken returns null on a non-ok response instead of a half-empty token bag",
      "refreshGoogleToken returns null without a network call when refreshToken is missing",
      "refreshGoogleToken sends grant_type=refresh_token with the given client_id/client_secret as a form body",
      "refreshGoogleToken stores a rotated refresh_token when Google returns one",
      "refreshKiroToken (social path) IDC path posts clientId/clientSecret/grantType and keeps the old token when none returned",
      "refreshKiroToken (social path) posts only the refreshToken and maps the camelCase reply",
      "refreshTraeToken currently returns null without a network call (trae absent from registry)",
      "refreshWithRetry retries a throwing refresh and returns the first truthy result",
      "refreshWithRetry returns null after exhausting retries"
    ],
    "tests/unit/token-refresh-providers-error-paths.test.js": [
      "codebuddy cn/intl failure exits refreshCodebuddyIntlToken returns null on HTTP failure and on a code!=0 payload",
      "codebuddy cn/intl failure exits refreshCodebuddyToken returns null on HTTP failure and on a code!=0 payload",
      "issueRecord keeps the earliest persisted issue time a persisted refreshTokenIssuedAt earlier than firstSeen backdates the record",
      "refreshCodexToken transient failure returns null on a non-permanent HTTP failure",
      "refreshCopilotToken failure exits returns null on HTTP failure, thrown fetch, and missing input token",
      "refreshKiroToken AWS and social failure exits IDC/AWS path returns null on an HTTP failure",
      "refreshKiroToken AWS and social failure exits social path returns null on an HTTP failure",
      "refreshKiroToken AWS and social failure exits social path success returns the token pair without a profileArn refetch when one is stored",
      "refreshKiroToken external_idp returns null on an HTTP failure and on invalid refresh config",
      "refreshKiroToken external_idp returns tokens plus the normalized providerSpecificData on success",
      "refreshTraeToken catch exit returns null when the fetch throws",
      "refreshXaiToken failure classification maps an invalid_grant service error to the closed error shape",
      "refreshXaiToken failure classification returns null for a non-grant service error and for a missing token",
      "thin delegate wrappers reach the generic refresh refreshClaudeOAuthToken returns the refreshed token pair",
      "thin delegate wrappers reach the generic refresh refreshGitHubToken returns the refreshed token pair",
      "thin delegate wrappers reach the generic refresh refreshIflowToken returns the refreshed token pair",
      "thin delegate wrappers reach the generic refresh refreshKimiToken returns the refreshed token pair"
    ],
    "tests/unit/token-refresh-providers-gap-coverage.test.js": [
      "codebuddy intl guards cn variant returns null with no token",
      "codebuddy intl guards returns null with no token and on a thrown transport error",
      "exported classifier helpers classifyOAuthRefreshError handles empty, plain-text and nested-code bodies",
      "exported classifier helpers formatIssueAge renders minutes, hours and days",
      "exported classifier helpers refreshFailureWhy returns the closed enum, never the payload",
      "generic refreshAccessToken edges evicts the oldest issue record past the registry bound",
      "generic refreshAccessToken edges fires chain-diverged once when the issuer rejects the exact held token",
      "generic refreshAccessToken edges resolves the URL through the registry fallback for an unprofiled provider",
      "generic refreshAccessToken edges returns null and logs on a thrown transport error",
      "generic refreshAccessToken edges returns null for a provider with no config or refresh URL",
      "generic refreshAccessToken edges sends the profile extraHeaders on the wire",
      "refreshCodexToken outcomes keeps the input refresh token on a success without rotation",
      "refreshCodexToken outcomes returns null on a thrown transport error",
      "refreshCodexToken outcomes returns null with no token",
      "refreshCodexToken outcomes returns the unrecoverable shape on a permanent failure",
      "refreshKiroToken profileArn resolution and region guard AWS-credential success keeps the input refresh token",
      "refreshKiroToken profileArn resolution and region guard refuses an IDC refresh with an invalid stored region",
      "refreshKiroToken profileArn resolution and region guard returns null with no token",
      "refreshKiroToken profileArn resolution and region guard social success adopts the refreshed profileArn from the response",
      "refreshKiroToken profileArn resolution and region guard social success fetches the profileArn when the response omits it",
      "refreshKiroToken profileArn resolution and region guard social success keeps an existing profileArn without patching",
      "refreshXaiToken success path keeps the input refresh token when the service returns none",
      "refreshXaiToken success path returns the mapped token pair and reuses the service singleton",
      "trae payload fallbacks trae accepts an unwrapped lowercase payload with a future ISO expiry",
      "trae payload fallbacks trae leaves expiresIn undefined for a past ISO expiry and nulls a missing AccessToken",
      "trae payload fallbacks trae returns null with no token and with no configured exchange URL"
    ],
    "tests/unit/token-refresh-service.test.js": [
      "formatProviderCredentials never leaks the refresh token for API-key-shaped providers, keeps it for OAuth-shaped ones",
      "formatProviderCredentials returns null for an unknown provider",
      "getAccessToken and refreshTokenByProvider routing mergeRefreshedProviderSpecificData survives one side missing",
      "getAccessToken and refreshTokenByProvider routing merges refreshed providerSpecificData OVER the existing data, keeping untouched keys",
      "getAccessToken and refreshTokenByProvider routing refreshTokenByProvider falls back to the generic refresh for unmapped providers",
      "getAccessToken and refreshTokenByProvider routing returns null for an unmapped provider via getAccessToken",
      "getAccessToken and refreshTokenByProvider routing returns null without calling any handler when the refresh token is absent or non-string",
      "getAccessToken and refreshTokenByProvider routing routes a mapped provider to its handler with the connection credentials",
      "getEffectiveRefreshLeadMs falls back to the plain lead when either timestamp is missing or unparseable",
      "getEffectiveRefreshLeadMs halves the token lifetime when the lead would consume it entirely",
      "getEffectiveRefreshLeadMs keeps the configured lead when the lifetime comfortably exceeds it",
      "getEffectiveRefreshLeadMs treats epoch-seconds and ISO strings as the same instant",
      "getRefreshLeadMs a valid per-connection override wins over everything",
      "getRefreshLeadMs defaults to TOKEN_EXPIRY_BUFFER_MS for unknown providers",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs rejects invalid override %p and falls back",
      "getRefreshLeadMs routes the legacy kimi-coding id to the kimi lead",
      "getRefreshLeadMs uses the registry-derived lead for a configured provider",
      "isUnrecoverableRefreshError flags invalid_grant as unrecoverable",
      "isUnrecoverableRefreshError flags invalid_request as unrecoverable",
      "isUnrecoverableRefreshError flags refresh_token_reused as unrecoverable",
      "isUnrecoverableRefreshError flags unrecoverable_refresh_error as unrecoverable",
      "isUnrecoverableRefreshError is falsy for transient errors, non-objects, and null",
      "refreshWithRetry retries through throws and falsy results, then succeeds",
      "refreshWithRetry returns null after exhausting every attempt",
      "refreshWithRetry returns the first truthy result without retrying",
      "vertex service-account parsing and key validation accepts an RSA-2048 PKCS8 key, including with escaped newlines",
      "vertex service-account parsing and key validation names the failure for missing, malformed, non-RSA, and undersized keys",
      "vertex service-account parsing and key validation parses only a complete service_account JSON",
      "vertex service-account parsing and key validation refreshVertexToken mints via faked fetch, then serves the cache without a second fetch",
      "vertex service-account parsing and key validation returns null (not throw) on an invalid key and on an upstream non-ok"
    ],
    "tests/unit/token-refresh-wrapper-exports.test.js": [
      "_refreshProjectId gating (via checkAndRefreshToken) does not fetch a projectId for a provider that does not need one",
      "_refreshProjectId gating (via checkAndRefreshToken) does not fetch a projectId when the connection already has one",
      "_refreshProjectId gating (via checkAndRefreshToken) fetches a projectId for antigravity/gemini-cli when the connection has none",
      "checkAndRefreshToken: github copilot expiry boundary (<=, not <) does NOT refresh exactly at the buffer boundary (remaining === buffer, strict <)",
      "checkAndRefreshToken: github copilot expiry boundary (<=, not <) refreshes one second inside the buffer",
      "local wrapper exports forward args and return value formatProviderCredentials",
      "local wrapper exports forward args and return value getAccessToken",
      "local wrapper exports forward args and return value getAllAccessTokens",
      "local wrapper exports forward args and return value refreshAccessToken",
      "local wrapper exports forward args and return value refreshClaudeOAuthToken",
      "local wrapper exports forward args and return value refreshCodexToken",
      "local wrapper exports forward args and return value refreshCopilotToken",
      "local wrapper exports forward args and return value refreshGitHubToken",
      "local wrapper exports forward args and return value refreshGoogleToken",
      "local wrapper exports forward args and return value refreshIflowToken",
      "local wrapper exports forward args and return value refreshKiroToken",
      "local wrapper exports forward args and return value refreshTokenByProvider",
      "local wrapper exports forward args and return value shouldRefreshCredentials forwards to oauthCredentialManager without a log arg"
    ],
    "tests/unit/token-saver-api.test.js": [
      "GET /api/token-saver/stats clamps query params (recentLimit/timelineDays/sinceMs)",
      "GET /api/token-saver/stats getTokenSaverStats throwing fails the request rather than reporting zeros (mocked, self-cleaning)",
      "GET /api/token-saver/stats performs no outbound network at all — getPxpipeStats reused directly for pxpipe source",
      "GET /api/token-saver/stats readable empty store reports headroom idle, not unavailable",
      "GET /api/token-saver/stats returns 200 with stable schema and truthful unit labels",
      "GET /api/token-saver/stats store failure still yields stable empty schema with HTTP 200",
      "GET /api/token-saver/stats zero-savings headroom row counts as activity: state ok with headroomRequests 1"
    ],
    "tests/unit/token-saver-cache-prefix-stability.test.js": [
      "invariant 1: identical input ⇒ byte-identical transformed body each stage alone is deterministic (a flaky stage is a cache-buster on its own)",
      "invariant 1: identical input ⇒ byte-identical transformed body rtk actually engaged on the fixture (a silent no-op would vacuously pass)",
      "invariant 1: identical input ⇒ byte-identical transformed body two consecutive requests with the same body transform to the same bytes",
      "invariant 2: nothing before an existing cache anchor moves anchorClaudeCache on a valid client plan leaves every pre-anchor byte alone",
      "invariant 2: nothing before an existing cache anchor moves growing the conversation keeps the transformed history prefix byte-identical",
      "invariant 2: nothing before an existing cache anchor moves injection lands INSIDE the cached region, before the last system anchor, at a stable index",
      "invariant 2: nothing before an existing cache anchor moves system injection replayed on the next turn is a no-op (dedup guard holds the prefix)",
      "invariant 3: cache-keep classification never regresses a full 4-anchor plan survives with exactly 4 (no fifth breakpoint minted)",
      "invariant 3: cache-keep classification never regresses a valid 2-anchor client plan keeps at least its anchors through anchoring",
      "invariant 3: cache-keep classification never regresses an anchorless body takes the legacy path and still ends fully anchored"
    ],
    "tests/unit/token-saver-compactor-template.test.js": [
      "token-saver compactor: structured checklist summary template extracts file paths and error text from the compacted history",
      "token-saver compactor: structured checklist summary template is deterministic: same input produces identical summary text",
      "token-saver compactor: structured checklist summary template recent turns are preserved verbatim after compaction",
      "token-saver compactor: structured checklist summary template summary block contains all six labeled section markers",
      "token-saver compactor: structured checklist summary template summary marker appears exactly once"
    ],
    "tests/unit/token-saver-composed-invariants.test.js": [
      "composed saver pipeline invariants same pipeline without compaction: pruned turns keep structure and notice",
      "composed saver pipeline invariants savers run in order, recent window survives byte-identical, save= is honest",
      "composed saver pipeline invariants token-saver opt-out header: dispatched body equals entry, no save= fields"
    ],
    "tests/unit/token-saver-elide-key-persistence.test.js": [
      "elide integrity key persistence (audit finding 4) reuses an existing key file across loads instead of regenerating",
      "elide integrity key persistence (audit finding 4) two fresh module loads produce the same marker for the same input",
      "elide integrity key persistence (audit finding 4) writes the key file mode 600"
    ],
    "tests/unit/token-saver-events.test.js": [
      "audit follow-ups: mem sub-action fields and turns allowlist accepts the tools saver",
      "audit follow-ups: mem sub-action fields and turns allowlist drops a turns field that is not an array of numbers",
      "audit follow-ups: mem sub-action fields and turns allowlist drops malformed mem attribution fields and clamps negatives",
      "audit follow-ups: mem sub-action fields and turns allowlist persists bounded mem attribution fields (toolPrunedChars, mediaPrunedItems, compactedTokens)",
      "audit follow-ups: mem sub-action fields and turns allowlist persists the turns array capped at 8 integers, dropping garbage",
      "tokenSaver event store accepts only saver rtk, headroom, pxpipe",
      "tokenSaver event store append and read fail open on filesystem errors",
      "tokenSaver event store clamps non-finite and negative numerics",
      "tokenSaver event store concurrent appends from one process never lose rows or throw",
      "tokenSaver event store drops non-finite/overflowing bytesSaved, saveTokEst and ce",
      "tokenSaver event store never mutates the input event object",
      "tokenSaver event store persists RTK row as characters with allowlisted fields only",
      "tokenSaver event store persists signed bytesSaved/saveTokEst and ce on saver rows",
      "tokenSaver event store read on missing dir returns empty array",
      "tokenSaver event store rejects unknown savers",
      "tokenSaver event store rotates at 5MB keeping exactly one .1 backup (seam-driven)",
      "tokenSaver event store SEC-5a: chmod-corrects a loose events file to 0600 on open",
      "tokenSaver event store second rotation overwrites old backup (bounded to two files)",
      "tokenSaver event store skips corrupt and truncated lines on read",
      "tokenSaver event store strips unknown fields (privacy: no prompts/tools/format/reasons)",
      "tokenSaver event store T-F4: non-applied rows count as bypassed, never as errors"
    ],
    "tests/unit/token-saver-gating-matrix.test.js": [
      "header master switch matrix combo enabling headroom (global false) → headroom attempted",
      "header master switch matrix combo overriding rtkEnabled=false while global true → RTK skipped, others run",
      "header master switch matrix header 'off' → headroom called disabled, phantom check skipped",
      "header master switch matrix header 'off' → no caveman/ponytail injection, memory pruner no-op",
      "header master switch matrix header 'off' → pxpipe transform never invoked and body untouched",
      "header master switch matrix header 'off' → RTK produces no stats and no saver event fires",
      "header master switch matrix header \"oFF\" is case-insensitive off",
      "header master switch matrix header \"Off\" is case-insensitive off",
      "header master switch matrix header \"OFF\" is case-insensitive off",
      "header master switch matrix header absent → every enabled saver runs",
      "saver pipeline order anchoring runs last on the fully-transformed body",
      "saver pipeline order memory pruner runs AFTER caveman (pruner sees the injected body)",
      "saver pipeline order RTK compresses BEFORE caveman injection (marker survives RTK untouched)",
      "token saver settings defaults (settingsRepo) every saver flag keeps its expected default"
    ],
    "tests/unit/token-saver-headroom-circuit-breaker.test.js": [
      "circuit breaker a success after failures closes the breaker and clears the count",
      "circuit breaker is keyed per endpoint, resettable via resetHeadroomCircuitBreaker",
      "circuit breaker opens after TWO consecutive failures; third request is short-circuited without fetch",
      "fail-open failure modes (body untouched, no throw) fetch timeout/reject → null, body unchanged",
      "fail-open failure modes (body untouched, no throw) malformed JSON from proxy → null via outer catch, body unchanged",
      "fail-open failure modes (body untouched, no throw) proxy HTTP 500 → null, body unchanged",
      "fail-open failure modes (body untouched, no throw) proxy returns object without messages[] → null, body unchanged"
    ],
    "tests/unit/token-saver-headroom-phantom.test.js": [
      "in-band phantom gate: proxy claims savings but body barely shrinks keeps original on conflicting metrics: big token claim, <5% byte shrink",
      "in-band phantom gate: proxy claims savings but body barely shrinks keeps original when claimed token shrink is under 5%",
      "in-band phantom gate: proxy claims savings but body barely shrinks keeps original when proxy reports zero/negative savings",
      "isHeadroomPhantomSavings unit contract after < before*0.95 but tokens_saved=0 → NOT phantom by this function",
      "isHeadroomPhantomSavings unit contract custom minShrinkRatio shifts the byte threshold",
      "isHeadroomPhantomSavings unit contract tokens_saved absent/NaN → NOT phantom",
      "isHeadroomPhantomSavings unit contract tokens_saved>0 AND after < before*0.95 (bytes) → NOT phantom",
      "isHeadroomPhantomSavings unit contract tokens_saved>0 AND after >= before*0.95 (bytes) → phantom",
      "isHeadroomPhantomSavings unit contract zero/missing byte snapshots → NOT phantom"
    ],
    "tests/unit/token-saver-headroom-size-gates.test.js": [
      "claude format: oversized body slices instead of skipping compresses only the oldest messages that fit under the cap, never the last two",
      "MAX_COMPRESS_BODY_BYTES size gate body at LIMIT-1 compresses",
      "MAX_COMPRESS_BODY_BYTES size gate body at LIMIT+1 skips: no fetch, body untouched, diagnostic names the limit",
      "MAX_COMPRESS_BODY_BYTES size gate body exactly at LIMIT compresses (gate is strictly-greater)",
      "MAX_COMPRESS_BODY_BYTES size gate measurement includes the whole body, not just messages (non-claude formats still skip)"
    ],
    "tests/unit/token-saver-headroom-validator.test.js": [
      "claude shape validator commits when count/order/role/content-shape hold; system and tools stay local",
      "claude shape validator rejects a candidate with a malformed content shape",
      "claude shape validator rejects a dropped claude message",
      "claude shape validator rejects a proxy that swaps tool_use/tool_result ids",
      "claude shape validator rejects reordered claude messages",
      "empty messages array round-trips safely: no throw, no mutation, null result",
      "error tool block skips compression entirely claude is_error tool_result: fetch never called",
      "error tool block skips compression entirely openai-responses function_call_output with status error: fetch never called",
      "openai chat.completions validator: tool pairing identity commits when tool_call_id and tool_calls ids are preserved",
      "openai chat.completions validator: tool pairing identity rejects a response that drops a message",
      "openai chat.completions validator: tool pairing identity rejects a response that reorders messages",
      "openai chat.completions validator: tool pairing identity rejects a response that rewrites a tool_call_id",
      "openai chat.completions validator: tool pairing identity rejects a response that rewrites tool_calls ids",
      "openai-responses pivot guard (3571) function_call / function_call_output items: refused before any fetch",
      "openai-responses pivot guard (3571) keeps an assistant message item with empty content[]",
      "openai-responses pivot guard (3571) message-only input proceeds to the proxy",
      "openai-responses pivot guard (3571) preserves message-item fields with no chat projection slot (name, cache_control)",
      "openai-responses pivot guard (3571) reasoning items: refused before any fetch",
      "openai-responses: instruction items kept (fix #2132 shape) developer/system items in input[] survive compression in place"
    ],
    "tests/unit/token-saver-inject-determinism.test.js": [
      "caveman injector determinism injecting twice at the same level is a byte-identical no-op",
      "caveman injector determinism level 'off' injects nothing and does not throw",
      "caveman injector determinism multi-turn: injected text byte-identical across turns (stable prefix for caching)",
      "caveman injector determinism re-inject after a new user turn is a no-op (dedup guard fires)",
      "caveman injector determinism re-inject at a different level: actual behavior, asserted explicitly",
      "caveman injector determinism unknown level behaves safely: no throw, no injection",
      "DEFECT TP-INJ-3 (medium): ponytail level escalation swallowed by dedup ponytail lite then full should land the full instruction",
      "ponytail injector determinism injecting twice at the same level is a byte-identical no-op",
      "ponytail injector determinism level 'off' injects nothing and does not throw",
      "ponytail injector determinism multi-turn: injected text byte-identical across turns (stable prefix for caching)",
      "ponytail injector determinism re-inject after a new user turn is a no-op (dedup guard fires)",
      "ponytail injector determinism re-inject at a different level: actual behavior, asserted explicitly",
      "ponytail injector determinism unknown level behaves safely: no throw, no injection"
    ],
    "tests/unit/token-saver-inject-formats.test.js": [
      "format coverage Antigravity wraps Gemini shape in body.request",
      "format coverage Claude array-of-blocks: exactly one text block, inserted before the last cache_control block",
      "format coverage Claude string system: prompt appended once, then deduped",
      "format coverage DEFECT TP-INJ-1 (high): Gemini systemInstruction as plain string is clobbered",
      "format coverage Gemini snake_case system_instruction is respected",
      "format coverage Gemini systemInstruction parts: appends exactly one part, deduped",
      "format coverage OpenAI system-as-message (array content): mirrors existing part type, appends one part",
      "format coverage OpenAI system-as-message (string content): appended with separator",
      "format coverage OpenAI with no system message: unshifts one system message at index 0",
      "format coverage Responses instructions (string): appended, deduped on re-inject",
      "format coverage Responses without instructions: unshifts typed message item carrying input_text part",
      "non-tamper Claude body: thinking blocks + signatures, cache_control, tools, model all untouched",
      "non-tamper ponytail injector non-tamper on the same shapes",
      "non-tamper tools array, model, max_tokens, temperature untouched (openai)"
    ],
    "tests/unit/token-saver-inject-level-tables.test.js": [
      "caveman boundary blocks every level carries the shared safety boundaries",
      "caveman level table every declared level has a non-empty prompt",
      "caveman level table every prompt key is a declared level (no orphan keys)",
      "caveman level table level 'off' injects nothing into any format",
      "caveman level table levels are strictly ordered by intensity (lite is a strict prefix-subset of full)",
      "caveman level table no two levels are identical (hash compare)",
      "ponytail boundary blocks DEFECT TP-INJ-2 (medium): ponytail levels lack the shared boundary blocks",
      "ponytail level table every declared level has a non-empty prompt",
      "ponytail level table every prompt key is a declared level (no orphan keys)",
      "ponytail level table level 'off' injects nothing into any format",
      "ponytail level table levels are strictly ordered by intensity (lite is a strict prefix-subset of full)",
      "ponytail level table no two levels are identical (hash compare)"
    ],
    "tests/unit/token-saver-memory-defaults-stacked.test.js": [
      "token-saver defaults sync (settingsRepo.mergeWithDefaults) memory saver defaults match the shipped contract",
      "token-saver stacked order: RTK compress -> applyMemoryEnhancements (compounding-loss check) body already through lossless structural compression still respects pruner bounds and keeps pairs",
      "token-saver stacked order: RTK compress -> applyMemoryEnhancements (compounding-loss check) trailing user media is never touched even after RTK compression"
    ],
    "tests/unit/token-saver-memory-error-policy.test.js": [
      "memory tool pruner: error-flagged results exempt Claude tool_result with is_error survives byte-identical; neighbor prunes",
      "memory tool pruner: error-flagged results exempt item-level isError inside a Claude content array exempts only that item",
      "memory tool pruner: error-flagged results exempt OpenAI array content: item-level status:'error' exempts only that part",
      "memory tool pruner: error-flagged results exempt OpenAI tool role: msg-level is_error exempt; string neighbor prunes",
      "memory tool pruner: error-flagged results exempt Responses function_call_output with status:'error' survives",
      "R-F2: error:true and status:'failed' are exempt from pruning item-level error:true inside a Claude content array exempts only that item",
      "R-F2: error:true and status:'failed' are exempt from pruning OpenAI tool msg with error:true survives byte-identical",
      "R-F2: error:true and status:'failed' are exempt from pruning Responses function_call_output with status:'failed' survives byte-identical"
    ],
    "tests/unit/token-saver-memory-isolation.test.js": [
      "memory stage isolation (audit finding 3) a second run on the same body object cannot double-prune the caller's history",
      "memory stage isolation (audit finding 3) gemini contents arrays are isolated too",
      "memory stage isolation (audit finding 3) the caller's messages content is untouched after a run, even while still over budget"
    ],
    "tests/unit/token-saver-memory-media-compactor.test.js": [
      "token-saver compactor: compactContextWindow called directly (default off in settings) conversational items <= recentTurnsToKeep -> untouched even over threshold",
      "token-saver compactor: compactContextWindow called directly (default off in settings) enabled:false -> untouched",
      "token-saver compactor: compactContextWindow called directly (default off in settings) input over threshold -> old turns replaced, exactly one marker-bearing block, recent turns verbatim",
      "token-saver compactor: compactContextWindow called directly (default off in settings) summary keeps only text highlights; thinking blocks from old turns are dropped",
      "token-saver compactor: compactContextWindow called directly (default off in settings) threshold under -> untouched",
      "token-saver media pruner: contract per mediaPruner.js audio / input_audio blocks in historical turns replaced with their type in the note",
      "token-saver media pruner: contract per mediaPruner.js data URI in historical string content replaced with base64 note",
      "token-saver media pruner: contract per mediaPruner.js enabled:false -> untouched",
      "token-saver media pruner: contract per mediaPruner.js historical image block replaced by text note; mixed text survives; trailing user media untouched",
      "token-saver media pruner: contract per mediaPruner.js inlineData / fileData Gemini parts replaced; text part kept",
      "token-saver media pruner: contract per mediaPruner.js message-level images array emptied and note appended",
      "token-saver media pruner: contract per mediaPruner.js no assistant/model message -> nothing pruned (trailing-run detection finds no cutoff)",
      "token-saver media pruner: contract per mediaPruner.js non-media blocks (pdf via type=file, plain text) are NOT media per isMediaBlock and survive"
    ],
    "tests/unit/token-saver-memory-toolpruner.test.js": [
      "R-F5: rtk-elided historical results keep their integrity marker Claude array-form part carrying a sha-era elide marker is kept verbatim",
      "R-F5: rtk-elided historical results keep their integrity marker OpenAI string content carrying an hmac elide marker is kept verbatim; the healthy neighbor prunes",
      "R-F5: rtk-elided historical results keep their integrity marker Responses function_call_output carrying an elide marker is kept verbatim",
      "token-saver tool pruner: evidence and cache metadata cache_control preserved on surviving blocks",
      "token-saver tool pruner: evidence and cache metadata is_error Claude tool_result blocks are exempt from pruning (evidence contract)",
      "token-saver tool pruner: keepRecentTurns boundaries ALL turns are tool turns -> oldest pruned, keepRecentTurns intact",
      "token-saver tool pruner: keepRecentTurns boundaries exactly keepRecentTurns tool turns -> nothing pruned",
      "token-saver tool pruner: keepRecentTurns boundaries history with NO tool turns -> untouched",
      "token-saver tool pruner: keepRecentTurns boundaries keepRecentTurns - 1 tool turns -> nothing pruned",
      "token-saver tool pruner: keepRecentTurns boundaries keepRecentTurns + 1 tool turns -> exactly the oldest pruned, recent 2 byte-identical",
      "token-saver tool pruner: keepRecentTurns boundaries keepRecentTurns + 5 tool turns -> oldest 5 pruned, last 2 byte-identical",
      "token-saver tool pruner: pairing invariant Claude interleaved multi-tool turns -> no orphaned tool_use/tool_result after pruning",
      "token-saver tool pruner: pairing invariant OpenAI Responses function_call/function_call_output pairs intact",
      "token-saver tool pruner: pairing invariant OpenAI role=tool history -> tool_call_id/tool_calls pairs intact",
      "token-saver tool pruner: truncation contract NON-tool content in a pruned Claude turn survives verbatim",
      "token-saver tool pruner: truncation contract pruned text carries exact truncation note and stays within max + notice budget",
      "token-saver tool pruner: truncation contract short historical tool output is NOT touched (no note appended)"
    ],
    "tests/unit/token-saver-prefix-stages-pipeline.test.js": [
      "all prefix flags off save= is absent entirely and the ledger is silent",
      "embedding reorder stage (chatCore pipeline) embed endpoint 500: stage silent, request still succeeds",
      "embedding reorder stage (chatCore pipeline) flag on with embeddings answering: relevant turn moves next to the tail",
      "mid-prefix note placement after pairs (audit finding 6) qac+pairs+midinject: the note lands on a real user turn at or before the live boundary, never on a compressed placeholder",
      "mid-prefix note stage (chatCore pipeline) flag on with no earlier prefix stage: nothing to summarize, note absent",
      "mid-prefix note stage (chatCore pipeline) flag on with qac on: boundary note injected, guard stays silent",
      "pair dropping stage (chatCore pipeline) flag on under a deficit: oldest droppable pair is gone",
      "pair dropping stage (chatCore pipeline) flag on with no deficit: prefix untouched",
      "prefix stages and the target format openai target: every prefix stage stays off the wire and the ledger",
      "query-aware compression stage (chatCore pipeline) flag on, inside the window: history is left verbatim (prefix stays cacheable)",
      "query-aware compression stage (chatCore pipeline) flag on: low-relevance historical turn collapses to a placeholder",
      "query-aware compression stage (chatCore pipeline) memo replay: a tool_result turn (no query) reproduces the previous turn's compression",
      "thinking strip stage (chatCore pipeline) flag on, Anthropic-native upstream: nothing stripped (the provider does not bill historical thinking)",
      "thinking strip stage (chatCore pipeline) flag on: historical thinking blocks stripped, live turn keeps its chain",
      "tools normalization stage (audit finding 7) duplicate built-ins stripped by dedupe: save= carries a tools stage, path carries tool-strip, event row lands",
      "tools normalization stage (audit finding 7) no duplicates: no tools stage entry and no tool-strip code"
    ],
    "tests/unit/token-saver-pxpipe-saver-field.test.js": [
      "pxpipe event payload carries saver:'pxpipe' applied path: emitted event has saver:'pxpipe'",
      "pxpipe event payload carries saver:'pxpipe' fail-open path (transform throws): emitted event still has saver:'pxpipe'",
      "pxpipe joins the main token-saver sink applied path: main-sink row with rid, bytesSaved, imageCount, and the path code"
    ],
    "tests/unit/token-saver-pxpipe.test.js": [
      "chatCore pxpipe wiring header absent + pxpipeEnabled → transformed body dispatched, event fired",
      "chatCore pxpipe wiring header off → proxy never invoked, dispatched body byte-identical",
      "chatCore pxpipe wiring pxpipeEnabled false (default) → proxy never invoked even with header absent",
      "compressWithPxpipe estimated-token accounting falls back to 4761 tokens/image when the proxy reports no pixel count",
      "compressWithPxpipe estimated-token accounting tokensBeforeEst = chars/4; after = remaining text + image pixels/750",
      "compressWithPxpipe fail-open integrity proxy error → body null, input request byte-identical, reason recorded",
      "compressWithPxpipe fail-open integrity timeout → body null, input request byte-identical, reason 'timeout'",
      "compressWithPxpipe threshold boundary exactly minChars chars → proxy invoked",
      "compressWithPxpipe threshold boundary one char below minChars → proxy never called, request untouched",
      "tokenSaver events allowlist (no prompt content leaks) non-vetted saver label is rejected (saver field is a closed set)",
      "tokenSaver events allowlist (no prompt content leaks) pxpipe event persists only allowlisted fields; poisoned extras dropped"
    ],
    "tests/unit/token-saver-response-headers.test.js": [
      "x-tp saver response headers forced SSE→JSON path fires x-tp-compact-hint only when ce collapses >50%",
      "x-tp saver response headers gateway-built error responses carry the saver headers too",
      "x-tp saver response headers non-streaming JSON path carries ctx-tokens and save-bytes from the saver ledger",
      "x-tp saver response headers savers off and no sid: no x-tp saver headers are invented",
      "x-tp saver response headers streaming SSE path sets the headers on the initial response headers"
    ],
    "tests/unit/token-saver-rtk-boundaries.test.js": [
      "disabled path enabled=false returns null and leaves the body untouched",
      "disabled path enabled=true without a messages/input array returns null",
      "MIN_COMPRESS_SIZE boundary exactly 499B is skipped byte-identical, no hits",
      "MIN_COMPRESS_SIZE boundary exactly 500B is compressed",
      "RAW_CAP boundary 10485761B (10 MiB + 1) is skipped byte-identical, no hits",
      "RAW_CAP boundary exactly 10485760B (10 MiB) is still attempted and compresses",
      "zero compressible blocks body with only user/system text: untouched, empty stats, null log line",
      "zero compressible blocks body with tool results under MIN_COMPRESS_SIZE: untouched, no hits"
    ],
    "tests/unit/token-saver-rtk-elide.test.js": [
      "elide never splits a surrogate pair at the tail boundary tail starting on a low surrogate advances past the whole pair",
      "elide: composed invariants cache_control blocks byte-identical",
      "elide: composed invariants is_error tool_result stays byte-identical inside the composed body",
      "elide: composed invariants message count, order, and roles unchanged",
      "elide: composed invariants the huge blob was elided (hit recorded, marker present)",
      "elide: composed invariants thinking blocks and signatures untouched",
      "elide: composed invariants tool_use ids unchanged and still paired with exactly one tool_result",
      "elide: direct filter contract determinism: same input yields byte-identical output",
      "elide: direct filter contract never grows: for sampled sizes/shapes output is null or strictly smaller",
      "elide: direct filter contract returns null at or below ELIDE_MIN_CHARS, elides above",
      "elide: via compressMessages boundary: exactly 4000 chars is untouched, 4001 is elided",
      "elide: via compressMessages content that autodetect handles still routes to its filter, not elide",
      "elide: via compressMessages elides a 20KB multi-line blob: head/tail verbatim, marker exact, N and sha correct",
      "elide: via compressMessages elides a 20KB single-line blob: head/tail verbatim, marker exact, N and sha correct",
      "elide: via compressMessages is_error block is exempt: 20KB error blob stays byte-identical",
      "elide: via compressMessages newline-boundary preference: head breaks at the nearest newline within 100 chars of the cut",
      "elide: via compressMessages newline-boundary: exact cut when no newline is near",
      "elide: via compressMessages no-grow through the pipeline: an inflating rewrite is discarded",
      "elide: via compressMessages stats: hit carries filter 'elide' and saved = before - after UTF-8 bytes",
      "SEC-1: HMAC integrity marker integrity holds within one process: same input, same marker",
      "SEC-1: HMAC integrity marker integrity is content-bound: a one-char middle change under identical head/tail changes the hmac",
      "SEC-1: HMAC integrity marker marker carries hmac, never sha, and never equals the bare sha256 prefix"
    ],
    "tests/unit/token-saver-rtk-error-shapes.test.js": [
      "DEFECT D-err-4: error flag nested inside a content-array item Claude: tool_result content item carrying is_error must be skipped",
      "DEFECT D-err-4: error flag nested inside a content-array item OpenAI: tool content item carrying is_error must be skipped",
      "DEFECT D-err-4: error flag nested inside a content-array item Responses: function_call_output item carrying is_error must be skipped",
      "DEFECT D-err-5: Kiro toolResults ignore is_error / isError flags Kiro toolResult with is_error:true must be skipped",
      "DEFECT D-err-5: Kiro toolResults ignore is_error / isError flags Kiro toolResult with isError:true must be skipped",
      "error shapes are skipped: all three flags, all four formats array-content variants: flags on the enclosing block are honoured (Claude + OpenAI + Responses)",
      "error shapes are skipped: all three flags, all four formats claude: error-flagged results stay byte-identical, healthy result compresses",
      "error shapes are skipped: all three flags, all four formats kiro: error-flagged results stay byte-identical, healthy result compresses",
      "error shapes are skipped: all three flags, all four formats openai: error-flagged results stay byte-identical, healthy result compresses",
      "error shapes are skipped: all three flags, all four formats responses: error-flagged results stay byte-identical, healthy result compresses",
      "R-F2: error:true and status:'failed' join the error vocabulary Claude tool_result block with status:'failed' stays byte-identical",
      "R-F2: error:true and status:'failed' join the error vocabulary item-level error:true inside a content array is honoured, healthy sibling still compresses",
      "R-F2: error:true and status:'failed' join the error vocabulary Kiro toolResult with status:'failed' stays byte-identical",
      "R-F2: error:true and status:'failed' join the error vocabulary OpenAI tool msg with error:true stays byte-identical",
      "R-F2: error:true and status:'failed' join the error vocabulary OpenAI tool msg with status:'failed' stays byte-identical",
      "R-F2: error:true and status:'failed' join the error vocabulary Responses function_call_output with error:true stays byte-identical"
    ],
    "tests/unit/token-saver-rtk-filter-fidelity.test.js": [
      "dedup-log filter: 3000-line input vs DEDUP_LINE_MAX=2000 no-grow guard: an inflating dedup rewrite is discarded, content unchanged",
      "dedup-log filter: 3000-line input vs DEDUP_LINE_MAX=2000 truncates at the line cap with an explicit marker",
      "DEFECT D-min-1: quoted (non-ASCII) git-diff path becomes 'unknown' quoted unicode filename is preserved in the compacted header",
      "git-diff filter: unicode filenames + CRLF line endings detects git-diff, compresses, and keeps unicode names and CRLF intact",
      "git-diff filter: unicode filenames + CRLF line endings per-hunk cap and +/- counts survive the CRLF rewrite",
      "grep filter: ANSI colour codes detects grep, compresses, and preserves ANSI escapes verbatim",
      "prose containing the word 'commit' is not git-log stays byte-identical and records no git-log hit",
      "R-F4: quoted diff --git pairs keeps octal-escaped (core.quotePath) names verbatim, including the escapes",
      "R-F4: quoted diff --git pairs unescapes \\\" and \\\\ inside a quoted pair and keeps an embedded ' b/' in the name",
      "R-F4: quoted diff --git pairs unquoted common case is unchanged",
      "smart-truncate: single-line 1 MiB blob matches no structured filter, so the size-based elide catch-all fires: head+tail kept with an integrity marker"
    ],
    "tests/unit/token-saver-rtk-roundtrip-invariants.test.js": [
      "round-trip invariants: Claude format invariant a: message count, order, and roles unchanged",
      "round-trip invariants: Claude format invariant b: every tool_use id pairs with exactly one tool_result, ids unchanged",
      "round-trip invariants: Claude format invariant c: all three block-level error flags keep content byte-identical",
      "round-trip invariants: Claude format invariant d: every cache_control block byte-identical (type + ttl)",
      "round-trip invariants: Claude format invariant e: thinking and redacted_thinking blocks untouched, signatures unchanged",
      "round-trip invariants: Claude format invariant f: no tool_result text becomes empty unless it started empty",
      "round-trip invariants: Claude format invariant g: long user and assistant text blocks survive verbatim",
      "round-trip invariants: Claude format reports savings (body actually carried compressible blocks)",
      "round-trip invariants: OpenAI format invariant a: message count, order, and roles unchanged",
      "round-trip invariants: OpenAI format invariant b: tool_call ids unchanged, each call id pairs with exactly one tool message",
      "round-trip invariants: OpenAI format invariant c: all three error flag shapes keep content byte-identical",
      "round-trip invariants: OpenAI format invariant f: no tool text becomes empty",
      "round-trip invariants: OpenAI format invariant g: long user and assistant text survive verbatim",
      "round-trip invariants: OpenAI format reports savings",
      "round-trip invariants: OpenAI Responses format invariant a: item count, order, and item types unchanged",
      "round-trip invariants: OpenAI Responses format invariant b: function_call ids and call_ids unchanged, outputs still paired",
      "round-trip invariants: OpenAI Responses format invariant c: all three error flag shapes keep output byte-identical",
      "round-trip invariants: OpenAI Responses format invariant f: no output text becomes empty",
      "round-trip invariants: OpenAI Responses format invariant g: long user text survives verbatim",
      "round-trip invariants: OpenAI Responses format reports savings"
    ],
    "tests/unit/token-saver-rtk-stats-honesty.test.js": [
      "stats honesty appliedCount (hits.length) equals the number of text blocks actually rewritten",
      "stats honesty body with no targeted fields: zero ledger, zero stats",
      "stats honesty bytesBefore/bytesAfter equal the independent per-block UTF-8 byte ledger",
      "stats honesty bytesSaved (bytesBefore - bytesAfter) equals the sum of per-hit savings",
      "stats honesty multibyte content: ledger is counted in UTF-8 bytes, not UTF-16 units",
      "stats honesty per-hit saved equals the actual before/after delta of its block"
    ],
    "tests/unit/token-saver-safe-semantics.test.js": [
      "atomic fail-open and error evidence a later frozen target leaves earlier targets and their references untouched",
      "atomic fail-open and error evidence does not split an emoji at the elision head boundary",
      "atomic fail-open and error evidence preserves nested Kiro error flag {\"error\":true}",
      "atomic fail-open and error evidence preserves nested Kiro error flag {\"is_error\":true}",
      "atomic fail-open and error evidence preserves nested Kiro error flag {\"isError\":true}",
      "atomic fail-open and error evidence preserves nested Kiro error flag {\"status\":\"error\"}",
      "atomic fail-open and error evidence preserves nested Kiro error flag {\"status\":\"failed\"}",
      "Headroom contract with a deterministic offline upstream accepts only semantic-preserving tool JSON in safe mode",
      "Headroom contract with a deterministic offline upstream honors historical user-compression setting false locally",
      "Headroom contract with a deterministic offline upstream honors historical user-compression setting true locally",
      "Headroom contract with a deterministic offline upstream preserves the latest request under lossy consent with user compression false",
      "Headroom contract with a deterministic offline upstream preserves the latest request under lossy consent with user compression true",
      "Headroom contract with a deterministic offline upstream protects every part of a projected Gemini current user message",
      "Headroom contract with a deterministic offline upstream protects tool calls, signatures and cache anchors even with lossy consent",
      "Headroom contract with a deterministic offline upstream refuses changed instruction and retains body references",
      "Headroom contract with a deterministic offline upstream refuses changed last-user and retains body references",
      "Headroom contract with a deterministic offline upstream refuses changed number and retains body references",
      "Headroom contract with a deterministic offline upstream refuses changed tool-identity and retains body references",
      "Headroom contract with a deterministic offline upstream refuses nested tool errors before any upstream call {\"error\":true}",
      "Headroom contract with a deterministic offline upstream refuses nested tool errors before any upstream call {\"is_error\":true}",
      "Headroom contract with a deterministic offline upstream refuses nested tool errors before any upstream call {\"isError\":true}",
      "Headroom contract with a deterministic offline upstream refuses nested tool errors before any upstream call {\"status\":\"error\"}",
      "Headroom contract with a deterministic offline upstream refuses nested tool errors before any upstream call {\"status\":\"failed\"}",
      "PXPIPE explicit visual-loss policy clears its timeout after completion and labels estimates as lossy",
      "PXPIPE explicit visual-loss policy rejects invalid visual result inflated-cost and retains input",
      "PXPIPE explicit visual-loss policy rejects invalid visual result instructions and retains input",
      "PXPIPE explicit visual-loss policy rejects invalid visual result roles and retains input",
      "PXPIPE explicit visual-loss policy rejects invalid visual result unchanged and retains input",
      "PXPIPE explicit visual-loss policy rejects visual loss of current-user with exact fail-open",
      "PXPIPE explicit visual-loss policy rejects visual loss of redacted-thinking with exact fail-open",
      "PXPIPE explicit visual-loss policy rejects visual loss of signature with exact fail-open",
      "PXPIPE explicit visual-loss policy rejects visual loss of thinking with exact fail-open",
      "PXPIPE explicit visual-loss policy requires lossy consent before invoking the visual encoder",
      "schema literal preservation lossy annotation opt-in never traverses enum/const data or collapses descriptions",
      "schema literal preservation safe defaults retain all annotations and return the original array",
      "semantic-preserving JSON compaction legacy elision requires an explicit opt-in and reports semantic loss",
      "semantic-preserving JSON compaction passes through malformed JSON/code [ 1 2 ]",
      "semantic-preserving JSON compaction passes through malformed JSON/code { \"x\": 1, }",
      "semantic-preserving JSON compaction passes through malformed JSON/code {\"x\": NaN}",
      "semantic-preserving JSON compaction passes through malformed JSON/code {\"x\":\"unterminated}",
      "semantic-preserving JSON compaction passes through malformed JSON/code const x = { value: 2 };",
      "semantic-preserving JSON compaction preserves every string, numeric lexeme, duplicate key and token position",
      "semantic-preserving JSON compaction records actual UTF-8 text deltas and remains a fixed point under legacy opt-in",
      "semantic-preserving JSON compaction safe RTK preserves opaque text, indentation and all semantic anchors"
    ],
    "tests/unit/token-saver-schema-distill-pipeline.test.js": [
      "schema distillation saver stage (chatCore pipeline) emits a schema token-saver event row with signed bytesSaved",
      "schema distillation saver stage (chatCore pipeline) flag off: schemas dispatch verbatim and the ledger has no schema stage",
      "schema distillation saver stage (chatCore pipeline) flag on: dispatched tools are distilled, ledger records the schema stage",
      "schema distillation saver stage (chatCore pipeline) safe mode preserves schema values when enabled without lossy consent",
      "schema distillation saver stage (chatCore pipeline) sid-keyed context_status snapshot is written with the final telemetry",
      "schema distillation saver stage (chatCore pipeline) token-saver header off suppresses the stage even when the flag is on"
    ],
    "tests/unit/token-saver-stage-stats.test.js": [
      "rid allowlist and (rid, saver) dedupe keeps valid 8-hex rid normalized lowercase, drops invalid",
      "rid allowlist and (rid, saver) dedupe mixed rid/no-rid rows aggregate independently",
      "rid allowlist and (rid, saver) dedupe two rows with same rid+saver (fallback attempts) count once; different rids count twice",
      "tokenSaver per-stage savings aggregates the ledger-backed savers (inject, mem, schema, privacy)",
      "tokenSaver per-stage savings applies sinceMs filtering to stages exactly like the window totals",
      "tokenSaver per-stage savings distinguishes a measured zero from an absent measurement and retains signed growth",
      "tokenSaver per-stage savings stages ride along through the /api/token-saver/stats route response",
      "tokenSaver per-stage savings sums signed bytesSaved per saver per window"
    ],
    "tests/unit/token-saver-stats-failure.test.js": [
      "GET /api/token-saver/stats when the read model throws does not leak the underlying failure text to the client",
      "GET /api/token-saver/stats when the read model throws fails loudly instead of replying 200 with zeroed windows"
    ],
    "tests/unit/token-saver-stats.test.js": [
      "tokenSaver stats read model grouping is O(rows + days), not O(days * rows) — single-pass bucket assignment",
      "tokenSaver stats read model never sums unlike units — exposes per-unit fields only",
      "tokenSaver stats read model non-headroom savers never bump headroomRequests",
      "tokenSaver stats read model query clamp: sinceMs non-finite ignored, timelineDays and recentLimit clamped",
      "tokenSaver stats read model recent cap 500 and newest-first while file on disk stays chronological",
      "tokenSaver stats read model timeline rows use UTC (toISOString), not local getDate",
      "tokenSaver stats read model UTC daily timeline buckets with correct length and sums",
      "tokenSaver stats read model windows all/today/yesterday/last7d/last30d count correctly",
      "tokenSaver stats read model zero-savings headroom request counts in headroomRequests without inventing savings"
    ],
    "tests/unit/token-saver-telemetry.test.js": [
      "token-saver REQ telemetry ce= grows on an appended turn and drops to near zero when history is rewritten",
      "token-saver REQ telemetry inject stage emits an event row reporting its growth honestly",
      "token-saver REQ telemetry save= arithmetic matches independently recomputed stage byte deltas (headroom enabled)",
      "token-saver REQ telemetry save= omits stages that did not run (headroom disabled, memory still prunes)",
      "token-saver REQ telemetry saver event rows carry signed bytesSaved, saveTokEst and ce",
      "token-saver REQ telemetry saver-guard fires once when a stage grows the body by more than 5% of entry",
      "token-saver REQ telemetry T-F1: ce= reports the exact shared-prefix length, not a 64KB floor",
      "token-saver REQ telemetry T-F2: the final stage closes the ledger — stage deltas sum to dispatched minus ledger entry bytes",
      "token-saver REQ telemetry T-F3: privacy bytes are measured as their own stage, not attributed to headroom"
    ],
    "tests/unit/token-savings-overview-ui.test.js": [
      "commits a processing level from its own select",
      "commits a threshold on blur and refuses a value outside its limits",
      "distinguishes measured zero, growth, reduction and absent coverage without summing stages",
      "filters by state bucket and by category without hiding anything behind a reveal",
      "keeps unknown and unavailable controls ineligible for a mutation",
      "lays Advanced out as one row per control with every field in place",
      "models one bucket per control and one state per group",
      "offers one Everyday card per control group with its routine switch and no editor",
      "reports a failed measurement read without presenting an empty sample as zero savings",
      "reports partial saved state as not reported in Everyday",
      "routes a toggle to the caller without showing an unsaved setting as enabled"
    ],
    "tests/unit/token-savings-page-ui.test.js": [
      "keeps a task selection separate from the level and opens no dialog",
      "keeps the refusal and the saved off state at the control after a settings conflict",
      "reads nothing but settings on open and keeps the level as the only switch",
      "renders every threshold and level in place once the level is Advanced",
      "reviews turning a control on in place and refuses to save without consent",
      "saves a threshold, a level and a list from the field itself",
      "saves through the retained hash and consent boundary, then verifies the readback",
      "sends nothing for a value outside a field limit and keeps the runtime default blank",
      "turns a control off directly, because removing a transformation grants nothing"
    ],
    "tests/unit/tokenrouter-thinking-levels-2478.test.js": [
      "TokenRouter thinking levels (#2478) does not offer a level TokenRouter rejects",
      "TokenRouter thinking levels (#2478) is provider-scoped: the same model id elsewhere keeps its own ladder",
      "TokenRouter thinking levels (#2478) offers TokenRouter's own reasoning_effort enum",
      "TokenRouter thinking levels (#2478) omits the field for none/auto rather than sending a rejected value",
      "TokenRouter thinking levels (#2478) passes the top of that ladder through unclamped"
    ],
    "tests/unit/tool-args-before-id-3416.test.js": [
      "a fragment arriving after the stream terminal is reported (#3416) stays quiet when every fragment arrives before the terminal",
      "a fragment arriving after the stream terminal is reported (#3416) warns, naming the index and the size of what was lost",
      "tool argument fragments arriving before the tool id (#3416) keeps a fragment that arrives before the id chunk",
      "tool argument fragments arriving before the tool id (#3416) keeps two concurrent tool calls in their own buffers",
      "tool argument fragments arriving before the tool id (#3416) preserves escaped quotes and shell metacharacters across fragments",
      "tool argument fragments arriving before the tool id (#3416) still buffers normally when the id leads, the common case"
    ],
    "tests/unit/tool-args-flush-drain-3416.test.js": [
      "tool arguments survive a stream that ends without finish_reason (#3416) does not re-emit when the stream already reached its terminal",
      "tool arguments survive a stream that ends without finish_reason (#3416) emits the whole buffered input and closes the block on flush",
      "tool arguments survive a stream that ends without finish_reason (#3416) stays null on a flush with no tool call open"
    ],
    "tests/unit/tool-args-reassembly-2868.test.js": [
      "a call whose arguments did not reassemble is reported (#2868) does not warn for a text block that happens to share the stop path",
      "a call whose arguments did not reassemble is reported (#2868) stays quiet on a call that reassembled correctly",
      "a call whose arguments did not reassemble is reported (#2868) stays quiet on a tool call that carried no arguments at all",
      "a call whose arguments did not reassemble is reported (#2868) warns, naming the tool, when the accumulated arguments are not JSON",
      "streamed tool-call arguments stay separated (#2868) a second message does not reuse the first message's indices while its calls are still mapped",
      "streamed tool-call arguments stay separated (#2868) fragments accumulate into the exact original argument string",
      "streamed tool-call arguments stay separated (#2868) gives each call its own index within one message"
    ],
    "tests/unit/tool-call-arguments.test.js": [
      "ensureToolCallIds function arguments does not fabricate a missing function object",
      "ensureToolCallIds function arguments normalizes empty arguments to an empty JSON object",
      "ensureToolCallIds function arguments normalizes missing arguments to an empty JSON object",
      "ensureToolCallIds function arguments normalizes null arguments to an empty JSON object",
      "ensureToolCallIds function arguments normalizes undefined arguments to an empty JSON object",
      "ensureToolCallIds function arguments preserves a non-empty argument string",
      "ensureToolCallIds function arguments serializes object arguments"
    ],
    "tests/unit/tool-call-output-tokens-1382.test.js": [
      "#1382 a tool-call-only turn records the output tokens it spent counts them the same way as visible text of the same size",
      "#1382 a tool-call-only turn records the output tokens it spent counts tool-call bytes in passthrough mode",
      "#1382 a tool-call-only turn records the output tokens it spent counts tool-call bytes in translate mode",
      "#1382 a tool-call-only turn records the output tokens it spent leaves the visible transcript empty — this is accounting, not content",
      "#1382 a tool-call-only turn records the output tokens it spent still reports zero for a stream that produced nothing at all"
    ],
    "tests/unit/tool-call-reasoning-content-1480.test.js": [
      "an assistant tool-call turn always carries reasoning_content (#1480) does not add the field to a plain assistant turn with no tool calls",
      "an assistant tool-call turn always carries reasoning_content (#1480) emits an empty string when the assistant produced no thinking",
      "an assistant tool-call turn always carries reasoning_content (#1480) keeps text content beside the tool calls",
      "an assistant tool-call turn always carries reasoning_content (#1480) keeps the tool_calls themselves intact",
      "an assistant tool-call turn always carries reasoning_content (#1480) still carries real thinking when there was some"
    ],
    "tests/unit/tool-deduper.test.js": [
      "toolDeduper — DeepSeek same-name dedup (new) claude client + deepseek + MCP trigger → both rules apply (union stripped)",
      "toolDeduper — DeepSeek same-name dedup (new) deepseek + 3 same-name tools → keeps first, drops both duplicates",
      "toolDeduper — DeepSeek same-name dedup (new) deepseek + distinct names → nothing stripped",
      "toolDeduper — DeepSeek same-name dedup (new) deepseek + OpenAI function-shape tools → dedups by function.name",
      "toolDeduper — DeepSeek same-name dedup (new) deepseek model + (max) thinking suffix → still dedups (suffix stripped before match)",
      "toolDeduper — DeepSeek same-name dedup (new) deepseek model + duplicate tool names → keeps first definition",
      "toolDeduper — DeepSeek same-name dedup (new) no model declared → same-name dedup does NOT run (safe default)",
      "toolDeduper — DeepSeek same-name dedup (new) non-DeepSeek model + duplicate tool names → untouched (GLM/MiniMax/Kimi accept them)",
      "toolDeduper — MCP-equivalent built-in rules (existing behavior) claude client + Exa MCP → drops built-in WebSearch/WebFetch",
      "toolDeduper — MCP-equivalent built-in rules (existing behavior) legacy call without opts still applies MCP rules for claude callers (back-compat shape)",
      "toolDeduper — MCP-equivalent built-in rules (existing behavior) non-claude client → MCP built-in rules do NOT run (behavior preserved)"
    ],
    "tests/unit/tool-disclosure-e2e.test.js": [
      "tool disclosure e2e comparison combined: filter then BM25 stacks reductions",
      "tool disclosure e2e comparison file-read query: BM25 keeps filesystem tools, deprioritises email/stripe",
      "tool disclosure e2e comparison no-op below maxTools threshold",
      "tool disclosure e2e comparison PR query: BM25 keeps github tools, deprioritises filesystem/stripe",
      "tool disclosure e2e comparison session-sticky: a later query never reshapes the disclosed list",
      "tool disclosure e2e comparison static filter removes excluded server before BM25",
      "tool disclosure e2e comparison stats ring buffer records entries after disclosure"
    ],
    "tests/unit/tool-disclosure-prefix-stability.test.js": [
      "tool disclosure prefix stability keeps turn t's tool order as a prefix of turn t+1's"
    ],
    "tests/unit/tool-disclosure.test.js": [
      "BM25 index + scoring scores all zero for an empty query",
      "BM25 index + scoring scores filesystem tools higher for a file-reading query",
      "BM25 index + scoring scores GitHub tools higher for a pull request query",
      "disclosureTools always keeps ToolSearch regardless of query",
      "disclosureTools handles empty tools array",
      "disclosureTools keeps tools used in prior conversation",
      "disclosureTools ranks filesystem tools near top for a file query",
      "disclosureTools ranks gmail tools near top for an email query",
      "disclosureTools rebuilds index when tool set changes",
      "disclosureTools reduces tool count to maxTools",
      "disclosureTools respects alwaysInclude config",
      "disclosureTools returns tools unchanged when count <= maxTools",
      "disclosureTools reuses cached index on second call with same tool set",
      "disclosureTools works without connectionId (no caching path)",
      "extractLastUserMessage handles array content blocks",
      "extractLastUserMessage returns empty string when no user messages",
      "extractLastUserMessage returns last user message string",
      "extractPinnedNames always includes ToolSearch",
      "extractPinnedNames includes alwaysInclude config list",
      "extractPinnedNames includes Claude format tool_use blocks from history",
      "extractPinnedNames includes forced tool_choice",
      "extractPinnedNames includes OpenAI format tool_calls from history",
      "tokenize handles empty / null",
      "tokenize lowercases and splits on non-alphanumeric",
      "tokenize preserves underscore-joined tokens",
      "tokenize removes stop words"
    ],
    "tests/unit/tool-filter.test.js": [
      "fail-open on malformed operator config (audit finding 15) a garbage regex config no-ops instead of throwing into the request",
      "fail-open on malformed operator config (audit finding 15) a non-array whitelist shape no-ops instead of throwing",
      "toolFilter excludes by description pattern",
      "toolFilter excludes tools by exact name",
      "toolFilter excludes tools by MCP server (bare name)",
      "toolFilter excludes tools by MCP server (glob prefix)",
      "toolFilter filters to includeOnlyServers when specified",
      "toolFilter handles empty tools array",
      "toolFilter handles OpenAI function-wrapping shape",
      "toolFilter includeOnlyDescriptionPattern keeps only matching",
      "toolFilter includeOnlyServers ORs with includeOnlyTools",
      "toolFilter keeps alwaysInclude tools even when excluded by server rule",
      "toolFilter returns empty array when all excluded",
      "toolFilter returns tools unchanged when config has no rules",
      "toolFilter returns tools unchanged when config is null"
    ],
    "tests/unit/tool-name-split-2077.test.js": [
      "tool name arriving after the tool id (#2077) does not repeat the block when the name is echoed on later chunks",
      "tool name arriving after the tool id (#2077) keeps two calls distinct when their names arrive out of order",
      "tool name arriving after the tool id (#2077) still emits a call whose name never arrives rather than dropping it",
      "tool name arriving after the tool id (#2077) still opens once when id and name arrive together",
      "tool name arriving after the tool id (#2077) uses the real name when the id leads and the name follows"
    ],
    "tests/unit/tool-pruner-prefix-stability.test.js": [
      "tool pruner prefix stability prunes as a monotone, idempotent prefix rewrite"
    ],
    "tests/unit/tool-result-missing-id-3362.test.js": [
      "tool_result id repair (#3362) does not hand the same id to two results",
      "tool_result id repair (#3362) does not reuse ids across assistant turns",
      "tool_result id repair (#3362) leaves a valid id alone",
      "tool_result id repair (#3362) never leaves the id empty when there is no open call",
      "tool_result id repair (#3362) pairs a result that arrived without an id with the open tool call",
      "tool_result id repair (#3362) pairs parallel results in order",
      "tool_result id repair (#3362) reaches Anthropic with a tool_use_id, end to end",
      "tool_result id repair (#3362) repairs the Claude shape too (tool_result block in user content)",
      "tool_result id repair (#3362) still sanitizes an id that breaks Anthropic's pattern",
      "tool_result id repair (#3362) stops a duplicate empty result being inserted after it"
    ],
    "tests/unit/tool-result-pairing-2933.test.js": [
      "tool result pairing repair (#2933, #2338) answers every call when only one of several was answered",
      "tool result pairing repair (#2933, #2338) does not duplicate answers that already exist across a run",
      "tool result pairing repair (#2933, #2338) keeps a Claude turn's results in one user message",
      "tool result pairing repair (#2933, #2338) leaves a fully answered turn untouched",
      "tool result pairing repair (#2933, #2338) leaves a message with no tool calls alone",
      "tool result pairing repair (#2933, #2338) opens a user message when a Claude turn has no results at all",
      "tool result pairing repair (#2933, #2338) repairs a Claude turn with tool_result blocks, not an OpenAI tool message"
    ],
    "tests/unit/tool-schema-numeric-constraints-422.test.js": [
      "string numeric schema constraints are coerced to numbers (#422) coerces a top-level constraint",
      "string numeric schema constraints are coerced to numbers (#422) coerces constraints on a nested property",
      "string numeric schema constraints are coerced to numbers (#422) coerces inside items and composition branches",
      "string numeric schema constraints are coerced to numbers (#422) does not mutate the caller schema",
      "string numeric schema constraints are coerced to numbers (#422) guarantees a string description",
      "string numeric schema constraints are coerced to numbers (#422) leaves a non-numeric string alone rather than producing NaN",
      "string numeric schema constraints are coerced to numbers (#422) leaves a value that is already a number, and a draft-4 boolean, alone"
    ],
    "tests/unit/tool-schema-zero-arg-349.test.js": [
      "a zero-argument object schema is strict-compatible (#349) does not mutate a caller schema it did have to fix",
      "a zero-argument object schema is strict-compatible (#349) fills properties inside items and composition branches",
      "a zero-argument object schema is strict-compatible (#349) fills properties on a nested object schema",
      "a zero-argument object schema is strict-compatible (#349) fills properties on an OpenAI-format tool that already reached us as one",
      "a zero-argument object schema is strict-compatible (#349) leaves a non-object schema without properties",
      "a zero-argument object schema is strict-compatible (#349) leaves a well-formed schema untouched and does not mutate the caller",
      "a zero-argument object schema is strict-compatible (#349) reaches the Responses API target, which is where Codex validates"
    ],
    "tests/unit/tools-status-api.test.js": [
      "rejects anonymous access before reading process state",
      "reports unavailable without disclosing process errors",
      "returns a no-store snapshot to an authenticated operator"
    ],
    "tests/unit/tools-status.test.js": [
      "read-only MCP bridge status projects only live status and client counts while leaving handles untouched",
      "read-only MCP bridge status reports declared presets without creating a registry or spawning"
    ],
    "tests/unit/trae-connect-timeout.test.js": [
      "Trae response-header deadlines clears both deadlines after a successful non-stream turn",
      "Trae response-header deadlines events headers clear the connect timer and leave only the stream timer",
      "Trae response-header deadlines non-streaming waits for events headers and returns a fallback-eligible 502",
      "Trae response-header deadlines preserves caller abort during session creation",
      "Trae response-header deadlines preserves caller abort while non-stream events wait for headers",
      "Trae response-header deadlines streaming returns 200 before an events-header timeout",
      "Trae response-header deadlines times out session creation before returning a success wrapper"
    ],
    "tests/unit/trae-oauth-provider.test.js": [
      "buildAuthUrl builds a verification URL carrying client_id and the login trace id",
      "buildAuthUrl falls back to state as the trace id when loginTraceID is absent",
      "exchangeToken — callback (oauth) mode, ExchangeToken over fetch falls through to the next allowlisted origin when the first 404s",
      "exchangeToken — callback (oauth) mode, ExchangeToken over fetch parses the callback, then posts ClientID/RefreshToken/ClientSecret to the first API origin",
      "exchangeToken — callback (oauth) mode, ExchangeToken over fetch rejects a callback with no refreshToken before ever calling fetch",
      "exchangeToken — callback (oauth) mode, ExchangeToken over fetch throws when every origin fails rather than returning a half-empty token",
      "exchangeToken — paste-token (imported) mode, no network accepts a raw pasted token and strips a Cloud-IDE-JWT prefix",
      "exchangeToken — paste-token (imported) mode, no network strips a Bearer prefix",
      "mapTokens — expiry normalization and region scope derives expiresIn from a relative expiresAt (absolute epoch seconds)",
      "mapTokens — expiry normalization and region scope email/displayName are undefined rather than null when GetUserInfo has none",
      "mapTokens — expiry normalization and region scope falls back to TRAE_CONFIG.tokenLifetimeDays when neither is present",
      "mapTokens — expiry normalization and region scope floors a past/near expiresAt at 60s rather than going negative",
      "mapTokens — expiry normalization and region scope maps aiRegion China to scope marscode-cn",
      "mapTokens — expiry normalization and region scope maps aiRegion CN to scope marscode-cn",
      "mapTokens — expiry normalization and region scope maps aiRegion SG to scope marscode-sg",
      "mapTokens — expiry normalization and region scope maps aiRegion Singapore to scope marscode-sg",
      "mapTokens — expiry normalization and region scope maps aiRegion undefined to scope marscode-us",
      "mapTokens — expiry normalization and region scope maps aiRegion US-East to scope marscode-us",
      "mapTokens — expiry normalization and region scope uses an explicit expiresIn verbatim",
      "postExchange — GetUserInfo falls back to {email:null,name:null} rather than throwing when every origin fails",
      "postExchange — GetUserInfo returns identity fields from the first successful origin",
      "prepareConfig — GetLoginGuidance returns loginHost and a generated loginTraceID from the first successful url",
      "prepareConfig — GetLoginGuidance throws when every guidance url fails"
    ],
    "tests/unit/translation-client-error-integrity.test.js": [
      "translation failures are local, terminal, and payload-safe audio fails without changing the source",
      "translation failures are local, terminal, and payload-safe audio returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe Cursor image fails without changing the source",
      "translation failures are local, terminal, and payload-safe Cursor image returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe file reference fails without changing the source",
      "translation failures are local, terminal, and payload-safe file reference returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe keeps CommandCode's current Chat Completions argument string intact",
      "translation failures are local, terminal, and payload-safe Kiro Claude URL fails without changing the source",
      "translation failures are local, terminal, and payload-safe Kiro Claude URL returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe Kiro receives client max_completion_tokens before generation starts",
      "translation failures are local, terminal, and payload-safe Kiro receives client max_output_tokens before generation starts",
      "translation failures are local, terminal, and payload-safe Kiro receives client max_tokens before generation starts",
      "translation failures are local, terminal, and payload-safe Kiro Responses URL fails without changing the source",
      "translation failures are local, terminal, and payload-safe Kiro Responses URL returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe Kiro URL fails without changing the source",
      "translation failures are local, terminal, and payload-safe Kiro URL returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe malformed JSON fails without changing the source",
      "translation failures are local, terminal, and payload-safe nameless call fails without changing the source",
      "translation failures are local, terminal, and payload-safe nameless call returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe opaque thinking fails without changing the source",
      "translation failures are local, terminal, and payload-safe opaque thinking returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe passes the opaque and tool media blocks unchanged on a native Claude route",
      "translation failures are local, terminal, and payload-safe tool error fails without changing the source",
      "translation failures are local, terminal, and payload-safe tool error returns400 before fetch or executor dispatch",
      "translation failures are local, terminal, and payload-safe tool image fails without changing the source",
      "translation failures are local, terminal, and payload-safe tool image returns400 before fetch or executor dispatch"
    ],
    "tests/unit/translator-custom-prefix.test.js": [
      "Translator custom provider prefix resolution should handle model ID without prefix",
      "Translator custom provider prefix resolution should resolve Anthropic-compatible provider prefix via getModelInfo",
      "Translator custom provider prefix resolution should resolve OpenAI-compatible provider prefix via getModelInfo"
    ],
    "tests/unit/translator-helpers-edge.test.js": [
      "normalizeClaudePassthrough — haiku adaptive thinking (docs 11 §1) downgrades adaptive thinking to enabled+budget for haiku models",
      "normalizeClaudePassthrough — haiku adaptive thinking (docs 11 §1) folds mid-conversation system messages into user turns in place",
      "normalizeClaudePassthrough — haiku adaptive thinking (docs 11 §1) keeps adaptive thinking for sonnet/opus",
      "parseDataUri / encodeDataUri (docs 11 §4) encode/parse roundtrip",
      "parseDataUri / encodeDataUri (docs 11 §4) parses a base64 data uri",
      "parseDataUri / encodeDataUri (docs 11 §4) returns null for http urls and non-strings",
      "parseDataUri / encodeDataUri (docs 11 §4) tolerates newlines inside base64 payload"
    ],
    "tests/unit/translator-request-normalization.test.js": [
      "request normalization claudeToOpenAIRequest flattens text-only content arrays into string",
      "request normalization claudeToOpenAIRequest preserves multimodal arrays",
      "request normalization filterToOpenAIFormat flattens a text-only SYSTEM array to string",
      "request normalization filterToOpenAIFormat keeps a SYSTEM array when a cache_control survives",
      "request normalization filterToOpenAIFormat preserves a text-only USER array as blocks",
      "request normalization parseSSELine still supports SSE data lines",
      "request normalization parseSSELine supports provider raw NDJSON stream lines",
      "request normalization translateRequest keeps /v1/messages Claude->OpenAI text payloads string-safe",
      "request normalization translateRequest preserves output_config for Anthropic Claude",
      "request normalization translateRequest strips unsupported Anthropic output_config for MiniMax Claude-compatible endpoints"
    ],
    "tests/unit/translator-same-format-decloak.test.js": [
      "translateResponse same-format passthrough (OAuth tool cloak) is a no-op without a cloak map, and on a non-Claude same-format stream",
      "translateResponse same-format passthrough (OAuth tool cloak) leaves an uncloaked chunk untouched",
      "translateResponse same-format passthrough (OAuth tool cloak) restores the original tool name on tool_use content_block_start",
      "translateResponse same-format passthrough (OAuth tool cloak) still emits nothing for the null flush chunk"
    ],
    "tests/unit/tray-orphan-reaping-1571.test.js": [
      "an orphaned tray binary is reaped before a new one spawns (#1571) a survivor is reaped before the new instance is created",
      "an orphaned tray binary is reaped before a new one spawns (#1571) it checks the binary name, per platform",
      "an orphaned tray binary is reaped before a new one spawns (#1571) it never signals its own pid",
      "an orphaned tray binary is reaped before a new one spawns (#1571) SIGTERM, not SIGKILL",
      "an orphaned tray binary is reaped before a new one spawns (#1571) the pid file is removed once the tray really exits",
      "an orphaned tray binary is reaped before a new one spawns (#1571) the pid is recorded when the tray starts",
      "an orphaned tray binary is reaped before a new one spawns (#1571) the process identity is verified before any signal is sent"
    ],
    "tests/unit/ttft-watchdog.test.js": [
      "pipeWithDisconnect TTFT watchdog aborts with a ttft-timeout error when the upstream emits nothing within the window",
      "pipeWithDisconnect TTFT watchdog does not fire when the first byte arrives just inside the window"
    ],
    "tests/unit/tts-provider-options-2036.test.js": [
      "a provider's own speech settings survive the normalized body (#2036) a request cannot change the modality that makes this a speech call",
      "a provider's own speech settings survive the normalized body (#2036) a request cannot change the voice the model id resolved",
      "a provider's own speech settings survive the normalized body (#2036) behaves as before when nothing extra is supplied",
      "a provider's own speech settings survive the normalized body (#2036) keeps a sibling speech setting the caller did send",
      "a provider's own speech settings survive the normalized body (#2036) keeps the settings a caller does not send",
      "a provider's own speech settings survive the normalized body (#2036) passes a caller's generation settings through",
      "the extras are collected from the request and carried down the fields the router owns are not treated as provider options",
      "the extras are collected from the request and carried down they reach the adapter"
    ],
    "tests/unit/update-optout-1563.test.js": [
      "a pinned install can decline the npm latest it rolled back from (#1563) /api/version reports no update and never reaches the registry",
      "a pinned install can decline the npm latest it rolled back from (#1563) /api/version/update refuses to install rather than trusting the hidden banner",
      "a pinned install can decline the npm latest it rolled back from (#1563) isUpdateDisabled reads the flag, and an off-ish value is not opting out",
      "a pinned install can decline the npm latest it rolled back from (#1563) the launcher's own check honours the same flag",
      "a pinned install can decline the npm latest it rolled back from (#1563) without the flag the same handler still looks the version up"
    ],
    "tests/unit/updater-app-port-2575.test.js": [
      "#2575 the updater waits on the port this server is actually serving falls back to the configured default when PORT is unset",
      "#2575 the updater waits on the port this server is actually serving hands the live PORT down to the detached updater",
      "#2575 the updater waits on the port this server is actually serving leaves the updater's own status port alone"
    ],
    "tests/unit/updater-relaunch-path-2186.test.js": [
      "the update relaunches the install it just updated (#2186) only uses that path when it exists, so a stale value cannot strand the relaunch",
      "the update relaunches the install it just updated (#2186) prefers the launcher's own path over resolving by name",
      "the update relaunches the install it just updated (#2186) still passes the flags that preserve tray or foreground mode",
      "the update relaunches the install it just updated (#2186) the fallback refuses to fetch a version nobody installed",
      "the update relaunches the install it just updated (#2186) the launcher hands its path down"
    ],
    "tests/unit/upstream-error-content-3228.test.js": [
      "a 200-wrapped rate-limit blurb is detected as an upstream error (#3228) catches a plain 'rate limit exceeded' blurb",
      "a 200-wrapped rate-limit blurb is detected as an upstream error (#3228) catches a plain 'too many requests' blurb",
      "a 200-wrapped rate-limit blurb is detected as an upstream error (#3228) does not fire on a real answer that merely discusses rate limits at length",
      "a 200-wrapped rate-limit blurb is detected as an upstream error (#3228) does not fire on ordinary short answers"
    ],
    "tests/unit/upstream-error-content-3232.test.js": [
      "a streamed upstream error is caught before anything reaches the client (#3636) extracts frame text across the shapes the error arrives in",
      "a streamed upstream error is caught before anything reaches the client (#3636) leaves a genuine first token alone",
      "a streamed upstream error is caught before anything reaches the client (#3636) reports no content so the caller falls over",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) assumes retryable when the payload does not say",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) catches the Codex overload blurb (#3232)",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) catches the NaraRouter upstream-error lines (#3242)",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) catches the qoder marker, whose status sits before the colon (#1996)",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) catches the structured CommandCode marker and reads its own retry flag (#3468)",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) catches the truncated-stream marker (#3636)",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) does not fire on a non-string",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) does not fire on a real answer that merely mentions a signature",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) does not fire on ordinary short answers",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) does not treat ordinary prose containing 'error' as a marker",
      "an upstream error carried as 200 content is detected (#3232 #3242 #3468 #3636) is wired into the non-streaming handler ahead of the empty-content check"
    ],
    "tests/unit/upstream-error-reset-879.test.js": [
      "parseUpstreamError reset metadata (#879) accepts only a future RFC3339 error.retryAfter timestamp",
      "parseUpstreamError reset metadata (#879) caps a representable but enormous relative delay before downstream date serialization",
      "parseUpstreamError reset metadata (#879) derives a reset from positive finite retry_after_ms",
      "parseUpstreamError reset metadata (#879) derives a reset from positive finite retryAfterMs",
      "parseUpstreamError reset metadata (#879) does not promote a top-level retryAfter field into account state",
      "parseUpstreamError reset metadata (#879) keeps the existing Retry-After header ahead of body reset metadata",
      "parseUpstreamError reset metadata (#879) keeps the existing text retry fallback when body metadata is invalid",
      "parseUpstreamError reset metadata (#879) propagates body reset metadata through DefaultExecutor without an executor override",
      "parseUpstreamError reset metadata (#879) rejects a calendar-invalid RFC3339 timestamp before Date.parse can normalize it",
      "parseUpstreamError reset metadata (#879) rejects malformed absolute timestamp",
      "parseUpstreamError reset metadata (#879) rejects past absolute timestamp",
      "parseUpstreamError reset metadata (#879) rejects string relative delay",
      "parseUpstreamError reset metadata (#879) rejects zero camel-case relative delay",
      "parseUpstreamError reset metadata (#879) rejects zero snake-case relative delay"
    ],
    "tests/unit/upstream-reset-truncates-as-200-1513.test.js": [
      "#1513 an upstream reset must not read as a finished answer does the same for the other socket codes a proxy produces",
      "#1513 an upstream reset must not read as a finished answer errors the client stream when the upstream socket dies mid-answer",
      "#1513 an upstream reset must not read as a finished answer leaves the observed formats to the terminal observer, which already errors them",
      "#1513 an upstream reset must not read as a finished answer still closes cleanly when the caller is the one who walked away"
    ],
    "tests/unit/upstream-route-alignment.test.js": [
      "upstream route: body format and transport agree finds the model-pinned formats it is meant to guard",
      "upstream route: body format and transport agree minimax-cn/MiniMax-M3: claude client → claude body on the claude transport",
      "upstream route: body format and transport agree minimax-cn/MiniMax-M3: openai client → claude body on the claude transport",
      "upstream route: body format and transport agree minimax/MiniMax-M3: claude client → claude body on the claude transport",
      "upstream route: body format and transport agree minimax/MiniMax-M3: openai client → claude body on the claude transport",
      "upstream route: body format and transport agree xiaomi-tokenplan/mimo-v2.5-pro-claude: claude client → claude body on the claude transport",
      "upstream route: body format and transport agree xiaomi-tokenplan/mimo-v2.5-pro-claude: openai client → claude body on the claude transport",
      "upstream route: the per-model transport guard still applies does not hand a claude client the claude transport for a chat-only model",
      "upstream route: the per-model transport guard still applies keeps the claude transport for a model that declares it",
      "upstream route: unpinned models keep the sourceFormat-matched transport minimax/MiniMax-M2.7 on a claude client stays claude",
      "upstream route: unpinned models keep the sourceFormat-matched transport minimax/MiniMax-M2.7 on a openai client stays openai"
    ],
    "tests/unit/usage-api-key-identity.test.js": [
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for 24h",
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for 30d",
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for 60d",
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for 7d",
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for all",
      "usage API-key identities (#2919) keeps every key identity and model/provider pair distinct for today"
    ],
    "tests/unit/usage-attribution-media.test.js": [
      "actual media requests preserve exact dispatch identity embeddings links successful usage after an authentication retry",
      "actual media requests preserve exact dispatch identity embeddings validation does not create a dispatch or billable row",
      "actual media requests preserve exact dispatch identity rerank links successful usage after an authentication retry",
      "actual media requests preserve exact dispatch identity rerank validation does not create a dispatch or billable row"
    ],
    "tests/unit/usage-attribution-pipeline.test.js": [
      "mock dispatch through exact accounting and actual analytics worker gives concurrent logical requests distinct IDs, with shared counters only inside the same request",
      "mock dispatch through exact accounting and actual analytics worker links actual SSE completion and forced SSE-to-JSON completion to their physical attempts",
      "mock dispatch through exact accounting and actual analytics worker records every BaseExecutor wire retry while charging only its successful usage",
      "mock dispatch through exact accounting and actual analytics worker records failed dispatch rate coverage without inventing billed usage",
      "mock dispatch through exact accounting and actual analytics worker separates a rejected authentication attempt from its successful retry without a second charge",
      "mock dispatch through exact accounting and actual analytics worker uses one exact UUID from pending dispatch to completion and paged query after a rate change"
    ],
    "tests/unit/usage-attribution-schema.test.js": [
      "usage attribution additive migration preserves legacy rows without inventing links and enforces one completion per exact attempt"
    ],
    "tests/unit/usage-attribution.test.js": [
      "exact usage attribution and rate provenance deduplicates only the exact attempt and counts two actual attempts as one logical request",
      "exact usage attribution and rate provenance does not convert an undocumented tick scale into provider-reported dollars",
      "exact usage attribution and rate provenance does not invent a currency, a price, an explicit session or historical identities",
      "exact usage attribution and rate provenance keeps explicit free cache/reasoning rates and merges a partial operator override with defaults",
      "exact usage attribution and rate provenance keeps provider-reported USD separate from its estimate, including a real zero",
      "exact usage attribution and rate provenance preserves a reported amount when the response omits token quantities",
      "exact usage attribution and rate provenance rejects malformed pages, limits and attribution dimensions",
      "exact usage attribution and rate provenance retains the pre-dispatch rate after an operator update and links one exact attempt"
    ],
    "tests/unit/usage-chart-all-period-2415.test.js": [
      "all-time usage charts (#2415) bounds long histories while preserving every bucket's totals",
      "all-time usage charts (#2415) returns every lifetime day chronologically with a year-bearing label",
      "all-time usage charts (#2415) serves all-time chart data from the chart API"
    ],
    "tests/unit/usage-chart-bucket-start-3163.test.js": [
      "hourly usage chart carries a canonical bucket start (#3163) ignores a client-supplied timezone parameter",
      "hourly usage chart carries a canonical bucket start (#3163) keeps the label and totals alongside the instant, and serves both over the API",
      "hourly usage chart carries a canonical bucket start (#3163) stamps every 24h bucket with its own hour boundary",
      "hourly usage chart carries a canonical bucket start (#3163) stamps every today bucket with its own hour boundary"
    ],
    "tests/unit/usage-concern.test.js": [
      "toOpenAIUsage claude: folds cache read+create into prompt, exposes details",
      "toOpenAIUsage claude: no cache -> no prompt_tokens_details",
      "toOpenAIUsage commandcode: keeps totalTokens fallback",
      "toOpenAIUsage gemini fallback: candidates=0 -> derive from total - prompt - thoughts",
      "toOpenAIUsage gemini: full fields, completion = candidates + thoughts",
      "toOpenAIUsage kiro: input/output straight",
      "toOpenAIUsage ollama: prompt_eval_count/eval_count",
      "toOpenAIUsage unknown kind / null raw -> null",
      "toResponsesUsage maps Chat Completions usage and token details",
      "toResponsesUsage returns null when no token counts are available"
    ],
    "tests/unit/usage-connection-status-1901.test.js": [
      "live model locks are selected by expiry drops one that has",
      "live model locks are selected by expiry ignores every non-lock field on the connection",
      "live model locks are selected by expiry ignores unparseable and empty values instead of throwing",
      "live model locks are selected by expiry keeps a lock that has not expired",
      "live model locks are selected by expiry keeps a model id that itself contains an underscore",
      "per-connection usage carries the DB status (#1901) an expired lock is not reported as a lock",
      "per-connection usage carries the DB status (#1901) it reports live model locks",
      "per-connection usage carries the DB status (#1901) it reports whether the connection is enabled",
      "per-connection usage carries the DB status (#1901) it reuses the snapshot routing reads rather than deriving its own",
      "per-connection usage carries the DB status (#1901) the response is additive: usage keeps its own shape",
      "per-connection usage carries the DB status (#1901) the snapshot is still persisted for routing"
    ],
    "tests/unit/usage-date-range-3442.test.js": [
      "the chart follows the same range a day with no traffic is plotted as zero rather than dropped",
      "the chart follows the same range a long range is grouped rather than returning a point per day forever",
      "the chart follows the same range no range leaves the fixed periods untouched",
      "the chart follows the same range one bucket per day across the range, and only across the range",
      "the range is parsed before it is trusted a backwards range is refused rather than silently swapped",
      "the range is parsed before it is trusted a missing endDate means the single start day, not from-then-to-now",
      "the range is parsed before it is trusted an unparseable date is not a range",
      "the range is parsed before it is trusted both ends are inclusive",
      "the range is parsed before it is trusted no startDate means no range, so the period keeps deciding",
      "the range is parsed before it is trusted one day is a range covering that whole local day",
      "the range reaches the endpoints the page calls a request without startDate still reaches the old code path",
      "the range reaches the endpoints the page calls the chart endpoint does the same",
      "the range reaches the endpoints the page calls the live SSE stream is deliberately left on periods only",
      "the range reaches the endpoints the page calls the stats endpoint reads startDate/endDate and passes them through",
      "yesterday alone can be selected (#3442) a range with no traffic in it reports zero rather than falling back",
      "yesterday alone can be selected (#3442) a two-day range sums both days and still excludes today",
      "yesterday alone can be selected (#3442) no range at all leaves every period exactly as it was",
      "yesterday alone can be selected (#3442) recentRequests is scoped to the range too, not left showing today",
      "yesterday alone can be selected (#3442) the day before yesterday is separately selectable",
      "yesterday alone can be selected (#3442) the range wins over the period, including over today and 24h",
      "yesterday alone can be selected (#3442) the totals are yesterday's only, excluding today and the day before"
    ],
    "tests/unit/usage-dispatch.test.js": [
      "usage dispatch every supported provider routes to its handler (no fallback message)",
      "usage dispatch unsupported provider → not-implemented message"
    ],
    "tests/unit/usage-feedback-loop.test.js": [
      "usage reaches the client on every terminal path carries the observed usage on a Claude incomplete terminal",
      "usage reaches the client on every terminal path carries the observed usage on an OpenAI incomplete terminal",
      "usage reaches the client on every terminal path falls back to an estimate when the stream produced content but no usage frame",
      "usage reaches the client on every terminal path never loses the terminal when the usage getter throws",
      "usage reaches the client on every terminal path prefers the reported usage over an estimate",
      "usage reaches the client on every terminal path reports nothing when the stream produced nothing",
      "usage reaches the client on every terminal path reports the tokens a failed Responses stream already spent",
      "usage reaches the client on every terminal path still emits a terminal when no usage was observed"
    ],
    "tests/unit/usage-legacy-import.test.js": [
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for 24h",
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for 30d",
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for 60d",
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for 7d",
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for all",
      "imported history with unavailable key identity reconciles totals without claiming anonymous access for today"
    ],
    "tests/unit/usage-logs-pagination-1345.test.js": [
      "#1345 /api/usage/logs pages answers an empty page rather than 500 when the page is past the end",
      "#1345 /api/usage/logs pages caps pageSize so one request cannot name the whole table",
      "#1345 /api/usage/logs pages caps the scanned prefix, so a far page cannot ask the DB for millions of rows",
      "#1345 /api/usage/logs pages keeps the bare array for a request with no paging params",
      "#1345 /api/usage/logs pages never reads past the scan ceiling on a page inside it",
      "#1345 /api/usage/logs pages rejects a junk page or pageSize by falling back, not by throwing",
      "#1345 /api/usage/logs pages reports hasNext false on the last window",
      "#1345 /api/usage/logs pages returns the first window plus its pagination when asked",
      "#1345 /api/usage/logs pages returns the second window, not the first again"
    ],
    "tests/unit/usage-misc-coverage.test.js": [
      "getGlmUsage requires a key and maps 401 and other errors",
      "getGlmUsage returns the error message when the fetch throws",
      "getGlmUsage skips non-token limits, defaults plan to Unknown, and null resetAt for zero times",
      "getIflowUsage returns a static connected message",
      "getOllamaUsage converts 0..1 ratios to percentage quotas and titles the plan from /api/me",
      "getOllamaUsage falls back to the default plan when /api/me fails, and handles no limits",
      "getOllamaUsage maps 401/403 to invalid-key and other non-ok to an error message",
      "getOllamaUsage reports non-JSON usage bodies",
      "getOllamaUsage requires a key",
      "getOllamaUsage returns the error message when the fetch throws",
      "getOpencodeGoUsage builds all three windows, clamping percent into 0..100",
      "getOpencodeGoUsage requires a key and maps auth/other failures",
      "getOpencodeGoUsage returns an empty quota set when usage is absent, and the error on throw",
      "getQoderUsage reports non-JSON bodies",
      "getQoderUsage requires a token and reports non-ok status",
      "getQoderUsage returns the error message when the fetch throws",
      "getQoderUsage shapes user and org quotas with a shared ISO resetAt and scalar metadata",
      "getQoderUsage treats a missing or non-positive expiresAt as null resetAt",
      "getVercelAiGatewayUsage builds Used and Remaining quotas from decimal strings",
      "getVercelAiGatewayUsage includes trimmed error text on other failures",
      "getVercelAiGatewayUsage reports an unfunded account when both numbers are zero",
      "getVercelAiGatewayUsage requires a key and maps auth failures",
      "getVercelAiGatewayUsage returns the error message when the fetch throws"
    ],
    "tests/unit/usage-period-echo-3198.test.js": [
      "a usage snapshot names the selection it answers (#3198) echoes the period it was asked for",
      "a usage snapshot names the selection it answers (#3198) keeps the echo consistent with the rows the snapshot counted",
      "a usage snapshot names the selection it answers (#3198) reports an explicit day range as a range, with its bounds",
      "recent rows are scoped to the period they are shown under (#3198) drops rows older than the selected window",
      "recent rows are scoped to the period they are shown under (#3198) scopes nothing for 'all', which is the pre-existing behaviour",
      "recent rows are scoped to the period they are shown under (#3198) survives an empty, absent or unusable ring"
    ],
    "tests/unit/usage-probe-gate-3061.test.js": [
      "live quota probes run under a ceiling (#3061) a failed probe releases its slot instead of wedging the gate shut",
      "live quota probes run under a ceiling (#3061) a forced refresh is not served by an unforced probe already in flight",
      "live quota probes run under a ceiling (#3061) a key is reusable once its probe has settled",
      "live quota probes run under a ceiling (#3061) aborting a QUEUED probe dequeues it without touching running probes",
      "live quota probes run under a ceiling (#3061) an aborted queued key is reusable immediately",
      "live quota probes run under a ceiling (#3061) callers coalescing onto a still-queued probe share one upstream call",
      "live quota probes run under a ceiling (#3061) collapses concurrent callers that want the same thing onto one call",
      "live quota probes run under a ceiling (#3061) drains the queue, so a bounded ceiling is not a dropped request",
      "live quota probes run under a ceiling (#3061) never runs more than the limit at once, however many are asked for",
      "live quota probes run under a ceiling (#3061) the wait queue is bounded and overflow rejects with PROBE_QUEUE_FULL"
    ],
    "tests/unit/usage-reasoning-effort-2483.test.js": [
      "the label a request is grouped by (#2483) buckets a token budget by thousands, so a budget is a group and not a row",
      "the label a request is grouped by (#2483) calls a disabled thinking block off, in every shape",
      "the label a request is grouped by (#2483) calls an enabled block with no budget auto",
      "the label a request is grouped by (#2483) is undefined when the request asked for no reasoning at all",
      "the label a request is grouped by (#2483) reads Claude's explicit output_config effort",
      "the label a request is grouped by (#2483) reads Ollama's own spelling",
      "the label a request is grouped by (#2483) reads the OpenAI flat effort",
      "the label a request is grouped by (#2483) reads the Responses nested effort",
      "the rollup dimension (#2483) counts into its own dimension, leaving byModel's key alone",
      "the rollup dimension (#2483) persists the label in meta, beside requestedModel",
      "the rollup dimension (#2483) reads it back on both history paths",
      "the rollup dimension (#2483) skips a request that asked for no reasoning rather than bucketing it",
      "what is grouped (#2483) labels the body that went upstream, so an injected override counts"
    ],
    "tests/unit/usage-recent-period.test.js": [
      "getUsageStats recent panel honours the selected period keeps every row when no period is selected",
      "getUsageStats recent panel honours the selected period never lists a row the period totals did not count",
      "getUsageStats recent panel honours the selected period reaches back a week for 7d and a month for 30d",
      "getUsageStats recent panel honours the selected period shows only the last day's rows for 24h",
      "getUsageStats recent panel honours the selected period shows only today's rows for today"
    ],
    "tests/unit/usage-refresh-revision.test.js": [
      "refuses late usage-refresh persistence after a concurrent credential or account transition {\"accessToken\":\"fixture-reauth-access\",\"refreshToken\":\"fixture-reauth-refresh\"}",
      "refuses late usage-refresh persistence after a concurrent credential or account transition {\"isActive\":false}",
      "retains a redeemed credential pair through the real repository when its original revision remains current"
    ],
    "tests/unit/usage-repo-live-state.test.js": [
      "buildRecentRequestRow masks keys and reads both row shapes masks short, long and absent keys without leaking material",
      "buildRecentRequestRow masks keys and reads both row shapes parses string tokens/meta from a DB row and masks the api key",
      "getUsageHistory filters by provider/model/dates and masks the key on the way out",
      "logs, spend, health appendRequestLog is a no-op and getRecentLogs derives pipe-delimited lines from usageHistory",
      "logs, spend, health getProviderHealth falls back to the account grain on an unknown groupBy and never claims unmeasured latency",
      "logs, spend, health getSpendWindow reports the sample count with the sum",
      "pending requests and active sessions an active session is stamped by saveRequestUsage and an error stop marks it",
      "pending requests and active sessions debounced stats events actually fire",
      "pending requests and active sessions keeps absent account identity null instead of resolving by a display label",
      "pending requests and active sessions tracks start/stop per model and account, and exposes them via getActiveRequests",
      "per-connection daily usage returns zeros without a connectionId and sums only that connection today",
      "resolveDayRange parses bare local days, defaults end to start, rejects backwards and garbage",
      "saveRequestUsage endpoint backfill a second save carrying the endpoint completes the endpoint-less row instead of inserting",
      "stats and chart conservation over live rows an explicit range snapshot equals the period snapshot when the range covers today",
      "stats and chart conservation over live rows chart buckets conserve tokens for 24h, today, all and an explicit range",
      "stats and chart conservation over live rows period snapshots agree: today/24h live-history totals match the all-time rollup for fresh rows"
    ],
    "tests/unit/usage-row-dedupe.test.js": [
      "usageHistory row identity does not fold a later request into an unrelated endpoint-less row",
      "usageHistory row identity keeps every request that shares a millisecond with another",
      "usageHistory row identity keeps them when they carry no endpoint either",
      "usageHistory row identity still completes a row that was written without its endpoint"
    ],
    "tests/unit/usage-stats-masked-key.test.js": [
      "usage stats byApiKey masking emits no raw API key in byApiKey keys for period 30d",
      "usage stats byApiKey masking emits no raw API key in byApiKey keys for period 60d",
      "usage stats byApiKey masking emits no raw API key in byApiKey keys for period 7d",
      "usage stats byApiKey masking emits no raw API key in byApiKey keys for period all"
    ],
    "tests/unit/usage-stream-coalesce-3061.test.js": [
      "the usage stream never stacks stats recalculations (#3061, #3029) coalesces the lightweight pending handler on the same terms",
      "the usage stream never stacks stats recalculations (#3061, #3029) does not recalculate at all once the stream is closed",
      "the usage stream never stacks stats recalculations (#3061, #3029) keeps at most one getUsageStats in flight under a burst of updates",
      "the usage stream never stacks stats recalculations (#3061, #3029) still runs once more for whatever arrived while it was busy",
      "the usage stream never stacks stats recalculations (#3061, #3029) unsubscribes both handlers when the client disconnects"
    ],
    "tests/unit/usage-stream-listener-leak.test.js": [
      "/api/usage/stream cleanup does not accumulate listeners across disconnected clients",
      "/api/usage/stream cleanup does not subscribe at all when the client leaves during the first (awaited) stats read",
      "/api/usage/stream cleanup still cleans up when the consumer cancels the stream instead",
      "/api/usage/stream cleanup unsubscribes from statsEmitter when the client aborts"
    ],
    "tests/unit/v1-mcp-route.test.js": [
      "POST /api/v1/mcp JSON-RPC a notifications/* method WITH an id is not a notification: method-not-found",
      "POST /api/v1/mcp JSON-RPC anonymous caller (no session headers) gets isError, never another caller's anonymous entry",
      "POST /api/v1/mcp JSON-RPC initialize answers identically for text/event-stream Accept",
      "POST /api/v1/mcp JSON-RPC initialize handshake shape",
      "POST /api/v1/mcp JSON-RPC keeps the same null and signed measurement contract as the REST projection",
      "POST /api/v1/mcp JSON-RPC no bearer at all → 401, every method gated",
      "POST /api/v1/mcp JSON-RPC notifications/cancelled (id null) is a 202 no-op, no JSON-RPC reply",
      "POST /api/v1/mcp JSON-RPC notifications/initialized is a 202 no-op",
      "POST /api/v1/mcp JSON-RPC parse failure → -32700 with null id",
      "POST /api/v1/mcp JSON-RPC rejects an unrecognized bearer credential",
      "POST /api/v1/mcp JSON-RPC tools/call for an unknown session → isError with a clear message",
      "POST /api/v1/mcp JSON-RPC tools/call picks the freshest entry across provider candidates",
      "POST /api/v1/mcp JSON-RPC tools/call returns the caller's own telemetry (default sid derivation)",
      "POST /api/v1/mcp JSON-RPC tools/call with an explicit valid sid reads that sid directly",
      "POST /api/v1/mcp JSON-RPC tools/call with an invalid sid arg → isError, store untouched",
      "POST /api/v1/mcp JSON-RPC tools/list exposes exactly context_status with the contract schema",
      "POST /api/v1/mcp JSON-RPC unknown method → JSON-RPC -32601",
      "POST /api/v1/mcp JSON-RPC unknown tool name → isError"
    ],
    "tests/unit/vendor-model-rows-glm53-flash-grok46.test.js": [
      "grok-4.6 capability window leaves grok-4.5 and the generic grok-4 glob alone",
      "grok-4.6 capability window reports the 500k context x.ai documents, not the *grok-4* 256k default",
      "vendor model rows resolve on their providers glm default model is unchanged by the inserts",
      "vendor model rows resolve on their providers glm lists glm-5.3-flash",
      "vendor model rows resolve on their providers glm-cn default model is unchanged by the inserts",
      "vendor model rows resolve on their providers glm-cn lists glm-4.6v",
      "vendor model rows resolve on their providers glm-cn lists glm-5.3-flash",
      "vendor model rows resolve on their providers opencode-go default model is unchanged by the inserts",
      "vendor model rows resolve on their providers opencode-go keeps its chat-only transport pin on the new GLM row",
      "vendor model rows resolve on their providers opencode-go lists deepseek-v4-flash-vision-exp",
      "vendor model rows resolve on their providers opencode-go lists glm-5.3-flash"
    ],
    "tests/unit/venice-provider.test.js": [
      "Venice AI provider builds into the runtime PROVIDERS map with the openai format default",
      "Venice AI provider enables dynamic model discovery and passthrough",
      "Venice AI provider exposes its seed models (incl. the signature uncensored model)",
      "Venice AI provider is registered as an OpenAI-compatible apikey provider",
      "Venice AI provider keeps every registry id unique after adding venice"
    ],
    "tests/unit/version-compare-like-for-like-1012.test.js": [
      "the update check compares the same package on both sides (#1012) a server started without the launcher still reports something",
      "the update check compares the same package on both sides (#1012) the comparison direction is unchanged",
      "the update check compares the same package on both sides (#1012) the launcher passes its own version to the server",
      "the update check compares the same package on both sides (#1012) the route prefers the launcher's version",
      "the update check compares the same package on both sides (#1012) the two packages really are distinct and independently versioned"
    ],
    "tests/unit/version-route-build-sha.test.js": [
      "version route build sha carries buildSha from TP_BUILD_SHA in the registry-lookup branch",
      "version route build sha carries buildSha in the opt-out branch (TOKENPROXY_NO_UPDATE)",
      "version route build sha reports null buildSha when the env value is empty"
    ],
    "tests/unit/version-route-tray.test.js": [
      "version route tray mode reports isTrayMode=false for TRAY_MODE=true",
      "version route tray mode reports isTrayMode=true for TRAY_MODE=1"
    ],
    "tests/unit/vertex-sa-key.test.js": [
      "refreshVertexToken — invalid SA key returns null without minting when private key is below 2048 bits",
      "validateVertexSaKey accepts RSA-2048 private keys (jose RS256 requirement)",
      "validateVertexSaKey rejects garbage and missing private_key",
      "validateVertexSaKey rejects non-RSA keys",
      "validateVertexSaKey rejects RSA keys below 2048 bits with a user-facing message"
    ],
    "tests/unit/vertex-tool-schema-1081.test.js": [
      "Vertex tool schema survives translation without a malformed property (#1081) accepts a Claude-shaped tool (name/input_schema), the other shape Claude Code sends",
      "Vertex tool schema survives translation without a malformed property (#1081) repairs a bare-string property value across many tools, matching the reported tool at index 37"
    ],
    "tests/unit/voices-selfcall-auth-1551.test.js": [
      "the voices self-call authenticates itself (#1551) keeps the loopback origin, which is the SSRF defence",
      "the voices self-call authenticates itself (#1551) presents the internal CLI token",
      "the voices self-call authenticates itself (#1551) the target really is behind the deny-by-default, which is why this mattered",
      "the voices self-call authenticates itself (#1551) uses the same salt as the other internal self-calls"
    ],
    "tests/unit/volcengine-ark-custom-node-3253.test.js": [
      "a custom node already expresses the Ark plan endpoints (#3253) a base pasted with the canonical path already on it is not doubled",
      "a custom node already expresses the Ark plan endpoints (#3253) the Anthropic-compatible base is used verbatim too",
      "a custom node already expresses the Ark plan endpoints (#3253) the OpenAI-compatible base is used verbatim, plan path and all",
      "the reported 404 is model-name ownership, not the endpoint (#3253) a bare Ark model id resolves to that built-in entry, producing the error in the report",
      "the reported 404 is model-name ownership, not the endpoint (#3253) prefixing the model with the node's own prefix is what avoids the collision",
      "the reported 404 is model-name ownership, not the endpoint (#3253) the built-in entry ships only the Coding Plan, which is the gap actually reported"
    ],
    "tests/unit/web-cookie-validation.test.js": [
      "grok-web validation should accept raw token without sso= prefix",
      "grok-web validation should POST to /rest/app-chat/conversations/new",
      "grok-web validation should return valid:false with error when response is 401",
      "grok-web validation should return valid:false with error when response is 403",
      "grok-web validation should return valid:true when response is 200",
      "grok-web validation should return valid:true when response is 400 (auth accepted but bad body)",
      "grok-web validation should return valid:true when response is 429 (rate limited but auth ok)",
      "grok-web validation should send Cloudflare-bypass headers",
      "grok-web validation should strip sso= prefix from apiKey",
      "perplexity-web validation should accept raw token without prefix",
      "perplexity-web validation should POST to /rest/sse/perplexity_ask",
      "perplexity-web validation should return valid:false when response is 401",
      "perplexity-web validation should return valid:false when response is 403",
      "perplexity-web validation should return valid:true when response is 200",
      "perplexity-web validation should strip __Secure-next-auth.session-token= prefix"
    ],
    "tests/unit/web-handlers-error-paths.test.js": [
      "fetch-specific validation and routing 400 on an unparseable url",
      "fetch-specific validation and routing 400 on missing url",
      "fetch-specific validation and routing 400 when the provider has no credentials at all",
      "fetch-specific validation and routing 400 when the SSRF guard rejects the target",
      "fetch-specific validation and routing expands a combo through handleComboChat",
      "fetch-specific validation and routing maps a non-fallback core failure to errorResponse",
      "fetch-specific validation and routing noAuth provider: failure maps to errorResponse with the core's status",
      "fetch-specific validation and routing noAuth provider: success wraps result.data as JSON",
      "fetch-specific validation and routing passes the connection's proxy fields and the refresh callback preserves them",
      "fetch-specific validation and routing returns unavailableResponse when all accounts are rate limited",
      "fetch-specific validation and routing rotates on shouldFallback and reports the last error when exhausted",
      "search-specific validation and routing 400 on missing/blank query",
      "search-specific validation and routing 400 when the provider has no credentials at all",
      "search-specific validation and routing expands a combo through handleComboChat with the settings strategy",
      "search-specific validation and routing falls back to the linked chat provider's credentials under the search lock key",
      "search-specific validation and routing honours clientErrorStatus on the all-rate-limited path",
      "search-specific validation and routing noAuth provider skips credential lookup entirely",
      "search-specific validation and routing refresh callback preserves providerSpecificData; success callback clears the error",
      "search-specific validation and routing resolves an alias to its provider id before routing",
      "search-specific validation and routing returns the core's failure response when fallback is refused",
      "search-specific validation and routing returns unavailableResponse when all accounts are rate limited",
      "search-specific validation and routing rotates on shouldFallback and reports the last error when exhausted",
      "shared request validation (search + fetch) fetch: 400 on invalid JSON body",
      "shared request validation (search + fetch) fetch: 400 on missing provider/model",
      "shared request validation (search + fetch) fetch: 400 on unknown provider",
      "shared request validation (search + fetch) fetch: 400 when provider lacks the capability",
      "shared request validation (search + fetch) fetch: 401 when requireApiKey and key is invalid",
      "shared request validation (search + fetch) fetch: 401 when requireApiKey and no key presented",
      "shared request validation (search + fetch) fetch: allowlist refusal short-circuits too",
      "shared request validation (search + fetch) records the device on a valid key and enforces the model allowlist",
      "shared request validation (search + fetch) search: 400 on invalid JSON body",
      "shared request validation (search + fetch) search: 400 on missing provider/model",
      "shared request validation (search + fetch) search: 400 on unknown provider",
      "shared request validation (search + fetch) search: 400 when provider lacks the capability",
      "shared request validation (search + fetch) search: 401 when requireApiKey and key is invalid",
      "shared request validation (search + fetch) search: 401 when requireApiKey and no key presented"
    ],
    "tests/unit/webhook-notifications-3141.test.js": [
      "deliver cancels unused response bodies and retains a stable receiver deduplication ID",
      "deliver does no network work for an already cancelled delivery",
      "deliver honours retries: 0 for the manual test button",
      "deliver never retries an SSRF-blocked target and never throws",
      "deliver omits the signature header when no secret is configured",
      "deliver retries a 429 but never a plain 4xx",
      "deliver retries a 5xx three times with the 1s/5s/30s ladder, then gives up",
      "deliver signs the exact body it sends with HMAC-SHA256",
      "deliver stops a retry delay promptly on caller cancellation without another send",
      "dispatch emit() returns without throwing when every endpoint fails",
      "dispatch only posts to endpoints subscribed to the event and still active",
      "dispatch skips everything while notifications are disabled",
      "normalizeNotificationsConfig drops urlless entries, unknown events and out-of-range thresholds",
      "normalizeNotificationsConfig treats an empty subscription as all events, not none",
      "watcher.evaluate fires high.error.rate once per crossing and never below minSamples",
      "watcher.evaluate fires provider.unhealthy and provider.recovered on transitions only",
      "watcher.evaluate seeds the first sighting silently so a restart does not replay old incidents",
      "watcher.evaluate swallows a source failure instead of propagating it",
      "watcher.evaluate treats a removed connection as gone, not as a recovery"
    ],
    "tests/unit/windsurf-executor.test.js": [
      "buildGetChatMessageRequest appends one field-4 message per chat message",
      "buildGetChatMessageRequest embeds the apiKey inside the metadata sub-message",
      "buildGetChatMessageRequest emits metadata (field 1), cascade_id (2), model (3), messages (4+)",
      "decodeCompletionChunk decodes a ContentChunk (field 1 → text)",
      "decodeCompletionChunk decodes a DoneChunk (field 3 → UsageStats with prompt/completion tokens)",
      "decodeCompletionChunk decodes an ErrorChunk (field 4 → message)",
      "decodeCompletionChunk returns { kind: 'unknown' } for empty buffer",
      "grpcWebFrame encodes empty payload as a 5-byte frame",
      "grpcWebFrame prepends a 5-byte header: 0x00 flag + big-endian length",
      "windsurf MODEL_ALIAS_MAP applies default effort level for bare gpt-5.x ids",
      "windsurf MODEL_ALIAS_MAP maps Claude 4.5 to MODEL_PRIVATE_* aliases",
      "windsurf MODEL_ALIAS_MAP maps SWE models to snake-case wire names",
      "windsurf MODEL_ALIAS_MAP passes through unknown ids as-is",
      "WindsurfExecutor class buildHeaders emits grpc-web+proto + Bearer token",
      "WindsurfExecutor class buildHeaders omits Authorization when no token",
      "WindsurfExecutor class buildUrl returns the GetChatMessage endpoint",
      "WindsurfExecutor class constructor wires config from PROVIDERS.windsurf",
      "WindsurfExecutor class registry transport baseUrl matches the executor chat endpoint"
    ],
    "tests/unit/windsurf-oauth-provider.test.js": [
      "buildAuthUrl builds a sign-in URL on the configured auth base with state and redirect",
      "exchangeToken — callback mode accepts a fragment-style callback",
      "exchangeToken — callback mode falls back to the default api server when RegisterUser omits it, accepts snake_case",
      "exchangeToken — callback mode parses the callback, registers, and returns the apiKey as accessToken",
      "exchangeToken — callback mode rejects a callback missing access_token",
      "exchangeToken — callback mode rejects a state mismatch",
      "exchangeToken — callback mode rejects an error callback with the description included",
      "exchangeToken — callback mode rejects an error callback without description",
      "exchangeToken — callback mode rejects when RegisterUser omits the apiKey",
      "exchangeToken — callback mode rejects when RegisterUser returns invalid JSON",
      "exchangeToken — callback mode rejects when RegisterUser returns non-2xx with status in the message",
      "exchangeToken — paste-token mode accepts a pasted sk-ws- apiKey without any network call",
      "exchangeToken — paste-token mode registers a pasted firebase JWT",
      "mapTokens maps tokens and provider-specific data, defaulting authMethod to oauth",
      "mapTokens omits email/displayName when user info is absent",
      "postExchange fetches email/name via GetOneTimeAuthToken → GetCurrentUser",
      "postExchange reads a flat GetCurrentUser response (no user wrapper)",
      "postExchange returns null user info when no firebaseIdToken is present",
      "postExchange returns nulls when no authToken comes back",
      "postExchange swallows network failures into null user info"
    ],
    "tests/unit/windsurf-transport-coverage.test.js": [
      "decoder edge branches done chunk skips foreign fields around and inside usage",
      "decoder edge branches done chunk without usage decodes to zero tokens",
      "decoder edge branches error chunk without a message field falls back to a default message",
      "decoder edge branches multi-byte varint lengths round-trip through the request builder",
      "decoder edge branches skips non-target fields inside a ContentChunk before finding text",
      "decoder edge branches skips varint/fixed64/fixed32 fields and stops on an unknown wire type",
      "WindsurfExecutor.execute (mocked transport) a body-less upstream still emits a stop chunk without usage",
      "WindsurfExecutor.execute (mocked transport) a reader that throws mid-stream emits a windsurf_error event",
      "WindsurfExecutor.execute (mocked transport) a zero-status trailer and unknown frame flags are not errors",
      "WindsurfExecutor.execute (mocked transport) reassembles a frame split across reads and empty message list gets one user turn",
      "WindsurfExecutor.execute (mocked transport) returns the upstream untouched on non-ok status",
      "WindsurfExecutor.execute (mocked transport) streams content + done frames into OpenAI SSE with usage",
      "WindsurfExecutor.execute (mocked transport) surfaces a non-zero gRPC trailer status, decoding grpc-message",
      "WindsurfExecutor.execute (mocked transport) surfaces an ErrorChunk as an SSE error event then [DONE]",
      "WindsurfExecutor.execute (mocked transport) transformRequest is null and refreshCredentials resolves null (out-of-band)"
    ],
    "tests/unit/workspace-inline-surfaces.test.js": [
      "disables Live for a snapshot, applies Paused at once and states when reads paused",
      "edits a custom UTC range inline and keeps its validation",
      "offers update behavior as one inline radiogroup whose Live choice returns a fixed range to current evidence",
      "opens saved investigations under the strip, confirms deletion inline and holds the section while it writes",
      "shows retained evidence and the export as one section at a time, and clearing closes the details"
    ],
    "tests/unit/workspace-investigation-state.test.js": [
      "captures and restores fixed bounds, selected record, lens controls and comparison IDs",
      "clears scope params back to defaults and survives a malformed shared link",
      "ignores a hand-edited selection param rather than trusting it",
      "preserves exact application project scope through URL, save and analytics queries",
      "recovers exact comparison identities across lens remounts, saves and legacy restores",
      "rejects invalid comparison identities in a shared link",
      "rejects invalid comparison identities in a shared link",
      "rejects invalid comparison identities in a shared link",
      "restores a named filter set without replacing retained selected evidence",
      "restores history scope and exact selection on back/forward without overwriting the location",
      "restores saved advanced filters while retaining selected evidence for filter sets",
      "restores the selected record from the URL, so the retained banner survives a reload",
      "retains the exact historical quota series through a saved definition and reload",
      "retains the same typed identity across lens remounts and incompatible scope changes",
      "serializes the shared scope into the URL and restores it on a hard reload"
    ],
    "tests/unit/workspace-observation-policy.test.js": [
      "a reduced or stale shared projection never labels its old totals live",
      "explicit selection while paused reports pending evidence until its read completes",
      "live refreshes, pause closes streams and cancels timers, and late events cannot update retained evidence",
      "partial frames retain the original freshness deadline rather than extending it on delivery",
      "pausing a pending automatic retry after failure settles both readers without replacement",
      "pausing unfinished live reads aborts them without dispatching replacements",
      "reopening a conditional inspector reads fresh evidence instead of reusing its last body",
      "retains a failed refresh and the last successful observation through retry and pause until replacement succeeds",
      "server refresh advice slows background analytics while explicit refresh and pause stay immediate",
      "setHistorical prevents background work and preserves manual inspection",
      "setSnapshot prevents background work and preserves manual inspection",
      "summary reads visible evidence once, survives StrictMode, and does not start background work",
      "switching unfinished live evidence to summary leaves one active read per resource"
    ],
    "tests/unit/workspace-resource.test.js": [
      "Shared workspace resource ownership aborts the previous scope and ignores its late response",
      "Shared workspace resource ownership does not label previous data as a failed new scope",
      "Shared workspace resource ownership maps absolute scope and bounded page options without unsupported array filters",
      "Shared workspace resource ownership refreshes the current scope and cancels a read on unmount",
      "Shared workspace resource ownership stops automatic polling after a snapshot header while allowing manual refresh",
      "Shared workspace resource ownership takes snapshot identity only from guard response headers"
    ],
    "tests/unit/workspace-snapshot-status.test.js": [
      "labels a historical control snapshot, stops recurring reads, and preserves manual refresh"
    ],
    "tests/unit/xai-image-edit-adapter-1608.test.js": [
      "an edit reaches the edit endpoint with its image (#1608) a generation is unchanged",
      "an edit reaches the edit endpoint with its image (#1608) a model the edit endpoint does not list stays on generation",
      "an edit reaches the edit endpoint with its image (#1608) a provider with no edit endpoint is untouched",
      "an edit reaches the edit endpoint with its image (#1608) an edit goes to the edit endpoint",
      "an edit reaches the edit endpoint with its image (#1608) takes the first of several images and carries the rest",
      "an edit reaches the edit endpoint with its image (#1608) the edit entry's own field whitelist decides the rest",
      "an edit reaches the edit endpoint with its image (#1608) the source image travels, which is the whole point of the request"
    ],
    "tests/unit/xai-image-edits-1608.test.js": [
      "#1608 xai image-edits registry contract declares imageEditConfig for the xAI JSON edits endpoint",
      "#1608 xai image-edits registry contract every declared edit model is a real image model in xai's own catalog",
      "#1608 xai image-edits registry contract scopes edit support to grok-imagine-image-2.0 only, per xAI's own docs"
    ],
    "tests/unit/xai-oauth-executor-coverage.test.js": [
      "connect rejects on a state mismatch before any token exchange",
      "connect rejects with the provider error_description when the callback carries an error",
      "connect runs the full PKCE round-trip: challenge matches verifier, tokens and email returned",
      "decodeIdTokenEmail prefers email, then preferred_username, then sub",
      "decodeIdTokenEmail returns undefined for non-JWT input",
      "discoverEndpoints falls back to the static config endpoints on a non-ok discovery response, and caches",
      "discoverEndpoints falls back to the static config endpoints when discovery throws",
      "exchangeXaiCode posts a form-encoded PKCE exchange without a client secret and returns the token JSON",
      "exchangeXaiCode propagates the upstream error body on non-ok exchange",
      "refreshAccessToken refreshes against the (fallback) token endpoint with a refresh_token grant",
      "refreshAccessToken throws with the upstream body on refresh failure",
      "validateOAuthEndpoint rejects empty and unparseable values with the field name in the error"
    ],
    "tests/unit/xai-oauth-service.test.js": [
      "xai/oauth service builds authorize URLs with CLIProxyAPI query extras",
      "xai/oauth service discovers endpoints without custom user-agent headers",
      "xai/oauth service exchanges dashboard codes against the discovered xAI token endpoint",
      "xai/oauth service generates dashboard auth data with CLIProxyAPI PKCE size and discovered endpoints",
      "xai/oauth service loads dashboard OAuth providers without starting Kimchi GitHub release discovery",
      "xai/oauth service validates discovered endpoints are https x.ai URLs"
    ],
    "tests/unit/xai-tokenRefresh.test.js": [
      "xai/token-refresh wrapper formatProviderCredentials returns Bearer-shape for xai",
      "xai/token-refresh wrapper refreshTokenByProvider returns expiresIn for refreshed xai tokens",
      "xai/token-refresh wrapper refreshTokenByProvider returns null when refreshToken missing",
      "xai/token-refresh wrapper refreshXaiToken module loads without throwing"
    ],
    "tests/unit/xai-video-core.test.js": [
      "handleVideoProxyCore 401 → refreshes once and retries once with the new token",
      "handleVideoProxyCore 401 twice → still only one refresh and one retry (no loop)",
      "handleVideoProxyCore API-key accounts (no refreshToken) never attempt refresh on 401",
      "handleVideoProxyCore failed refresh → 401 propagates with a single upstream call (account flagged for re-auth upstream)",
      "handleVideoProxyCore forwards a creation POST byte-for-byte and passes request_id through",
      "handleVideoProxyCore forwards multipart bodies untouched with the original boundary header",
      "handleVideoProxyCore maps client aborts to 408 without retrying",
      "handleVideoProxyCore never re-sends a creation POST after a network error",
      "handleVideoProxyCore passes a failed job (HTTP 200, status failed) through without translating",
      "handleVideoProxyCore passes done polling payload through verbatim",
      "handleVideoProxyCore passes pending polling payload through verbatim",
      "handleVideoProxyCore passes processing polling payload through verbatim",
      "handleVideoProxyCore rejects providers without videoConfig",
      "handleVideoProxyCore sanitizes bearer tokens and credential values out of upstream errors",
      "handleVideoProxyCore url-encodes the request id when polling",
      "registry wiring exposes videoConfig for xai",
      "registry wiring registers grok-imagine-video with kind video (kept out of LLM lists)",
      "registry wiring supports exactly the three creation actions",
      "sanitizeSecrets leaves normal text untouched",
      "sanitizeSecrets redacts bearer tokens",
      "sanitizeSecrets redacts explicit credential values"
    ],
    "tests/unit/xai-video-handler.test.js": [
      "handleVideoCreate does NOT rotate accounts on a 500 creation error (job may exist upstream)",
      "handleVideoCreate forwards multipart bodies byte-exact with default xai provider",
      "handleVideoCreate forwards the original raw JSON bytes when no rewrite is needed",
      "handleVideoCreate honors preferred x-connection-id when selecting the account",
      "handleVideoCreate rejects providers without video support",
      "handleVideoCreate returns 400 on invalid JSON",
      "handleVideoCreate returns 400 when no credentials are connected",
      "handleVideoCreate returns the serving connection id in x-tokenproxy-connection-id",
      "handleVideoCreate rotates to the next account on 401 (auth errors cannot have created a job)",
      "handleVideoCreate round-trips the exposed create pin header through a GET poll",
      "handleVideoCreate strips the xai/ prefix from model before forwarding",
      "handleVideoGet polls a Veo job with the provider saved on its x-tokenproxy connection pin",
      "handleVideoGet polls upstream pinned to the x-connection-id account and passes status through",
      "handleVideoGet records the failure when polling hits a terminal auth error",
      "handleVideoGet rejects a Veo request id pinned to an xAI connection before fetching",
      "handleVideoGet rejects an xAI request id pinned to a Gemini connection before fetching",
      "handleVideoGet returns generic unavailable for a legacy all-locked video account",
      "handleVideoGet returns only the selected all-locked pair's projected status, reason, and retry"
    ],
    "tests/unit/xiaomi-mimo-tts.test.js": [
      "Xiaomi MiMo TTS exposes MiMo TTS models + preset voices in the TTS catalog",
      "Xiaomi MiMo TTS posts to chat completions with text as assistant message and voice in audio.voice",
      "Xiaomi MiMo TTS threads the language hint into a role:user instruction",
      "Xiaomi MiMo TTS threads the style field into the role:user message",
      "Xiaomi MiMo TTS uses mimo_default when no voice is provided",
      "Xiaomi MiMo TTS wires the provider into media-providers TTS (serviceKind, adapter, UI config)"
    ],
    "tests/unit/xiaomi-tokenplan-capabilities-3304.test.js": [
      "xiaomi-tokenplan TTS models never claim vision (#3304) gives the TTS models audio output instead",
      "xiaomi-tokenplan TTS models never claim vision (#3304) leaves the genuinely multimodal omni model alone",
      "xiaomi-tokenplan TTS models never claim vision (#3304) no xiaomi-tokenplan TTS model advertises vision or video",
      "xiaomi-tokenplan TTS models never claim vision (#3304) no xmtp TTS model advertises vision or video",
      "xiaomi-tokenplan TTS models never claim vision (#3304) ships TTS models to check"
    ],
    "tests/unit/xiaomi-tokenplan-capabilities.test.js": [
      "Xiaomi Token Plan capabilities adds the configured vision fallback before a Token Plan text model",
      "Xiaomi Token Plan capabilities does not change the standard MiMo capability pattern",
      "Xiaomi Token Plan capabilities keeps Token Plan omni models multimodal",
      "Xiaomi Token Plan capabilities marks V2.5 chat models as text-only through xiaomi-tokenplan",
      "Xiaomi Token Plan capabilities marks V2.5 chat models as text-only through xmtp"
    ],
    "tests/unit/xiaomi-tokenplan-connection-test.test.js": [
      "Xiaomi Token Plan connection test accepts a non-401 response because valid keys may not list models",
      "Xiaomi Token Plan connection test falls back to the default cluster for region undefined",
      "Xiaomi Token Plan connection test falls back to the default cluster for region unknown",
      "Xiaomi Token Plan connection test rejects a 401 response",
      "Xiaomi Token Plan connection test tests the ams cluster selected by the connection",
      "Xiaomi Token Plan connection test tests the cn cluster selected by the connection",
      "Xiaomi Token Plan connection test tests the sgp cluster selected by the connection"
    ],
    "tests/unit/xmtp-claude-route-1860.test.js": [
      "a model-level targetFormat moves the transport with it (#1860) a Claude client on the plain model gets the claude transport",
      "a model-level targetFormat moves the transport with it (#1860) an OpenAI client on the -claude model routes to the claude transport",
      "a model-level targetFormat moves the transport with it (#1860) that transport carries Anthropic auth, not bearer",
      "a model-level targetFormat moves the transport with it (#1860) the alias used for model lookup is the one chatCore derives",
      "a model-level targetFormat moves the transport with it (#1860) the plain model still follows the client's own format"
    ],
    "tests/unit/xquik-search-provider-f0a6d358.test.js": [
      "POST /api/providers/validate probes the no-charge endpoint prefers validateUrl over the charged search endpoint",
      "POST /api/providers/validate probes the no-charge endpoint still falls back to baseUrl for a provider that declares no validateUrl",
      "the search result carries credits and pagination omits the credits field for a provider that does not bill per result",
      "the search result carries credits and pagination reports provider_credits_used per result and forwards pagination",
      "xquik normalizer reports cursor pagination",
      "xquik normalizer turns the tweets envelope into results with a permalink and author",
      "xquik registry entry bills per returned post rather than per query",
      "xquik registry entry declares a no-charge validateUrl distinct from the charged search URL",
      "xquik registry entry is an apikey webSearch provider searching X",
      "xquik registry entry is exported from the generated registry index and reaches AI_PROVIDERS",
      "xquik request builder passes the cursor and queryType through provider_options",
      "xquik request builder rejects a queryType Xquik does not accept, and a missing key",
      "xquik request builder sends a GET with the key in x-api-key"
    ],
    "tests/unit/xunfei-provider.test.js": [
      "Xunfei Spark provider builds into the runtime PROVIDERS map with the openai format default",
      "Xunfei Spark provider exposes its seed models",
      "Xunfei Spark provider is registered as an OpenAI-compatible apikey provider",
      "Xunfei Spark provider keeps every registry id unique after adding xunfei"
    ],
    "tests/unit/zai-glm-web-search-2425.test.js": [
      "z.ai / GLM web search (#2425) appears in the web search provider list without losing its llm kind",
      "z.ai / GLM web search (#2425) asks for the web_search tool with results attached",
      "z.ai / GLM web search (#2425) is registered as a chat-based search provider",
      "z.ai / GLM web search (#2425) reads sources from web_search[].link, which is not named url",
      "z.ai / GLM web search (#2425) searches on the open-platform endpoint, not the coding one",
      "z.ai / GLM web search (#2425) sends the key as a bearer token",
      "z.ai / GLM web search (#2425) survives a reply with no sources"
    ],
    "tests/unit/zed-auth-token-lifecycle.test.js": [
      "buildZedUserAuthHeader formats userId + accessToken and throws when either is missing",
      "fetchZedLlmToken — cache discipline a rotated access token gets its own cache slot (no stale token for new login)",
      "fetchZedLlmToken — cache discipline forceRefresh bypasses a live cache entry",
      "fetchZedLlmToken — cache discipline mints once and serves the cached token on the second call (no double round trip)",
      "fetchZedLlmToken — cache discipline surfaces the server error message on a non-ok mint",
      "fetchZedLlmToken — cache discipline throws instead of caching when the mint reply carries no token",
      "fetchZedLlmToken — cache discipline throws when no organization can be resolved anywhere",
      "keypair auth roundtrip decrypts an OAEP-encrypted access token with the generated verifier",
      "keypair auth roundtrip falls back to PKCS1 padding when OAEP fails",
      "keypair auth roundtrip rejects a verifier without the zed-rsa-pkcs1 prefix instead of decrypting with garbage",
      "parseZedCallbackPayload parses a callback URL query",
      "parseZedCallbackPayload parses a raw JSON paste and a bare query string",
      "parseZedCallbackPayload rejects a payload missing user_id or access_token",
      "resolveZedOrganizationId falls back to the personal organization from user info",
      "resolveZedOrganizationId prefers the explicit connection org over user info",
      "resolveZedOrganizationId unwraps tuple-shaped ids",
      "shouldRefreshZedLlmToken fires on 401 and on either expiry header, not on a clean 200"
    ],
    "tests/unit/zed-executor.test.js": [
      "ZedExecutor credential lifecycle never claims a refresh path (long-lived token, must fail closed instead of looping refresh)",
      "ZedExecutor.execute — payload envelope falls back to credentials._clientSessionId for thread_id",
      "ZedExecutor.execute — payload envelope wraps the provider request in the Zed thread envelope and posts to /completions",
      "ZedExecutor.execute — stream translation a failed status frame becomes a visible error chunk, then the stream finishes",
      "ZedExecutor.execute — stream translation a non-2xx upstream response is returned as-is: status and body reach the caller",
      "ZedExecutor.execute — stream translation translates xAI (OpenAI-shaped) event lines into an SSE stream ending in [DONE]",
      "ZedExecutor.parseError other codes are prefixed Zed <code>, plain bodies pass through, status preserved",
      "ZedExecutor.parseError trial_blocked gets the explanatory message with the upstream text embedded",
      "ZedExecutor.resolveModel — provider selection catalog failure degrades to name inference (grok→XAi, gemini→Google, else OpenAi)",
      "ZedExecutor.resolveModel — provider selection re-resolves with forceRefresh when the model is missing, then infers from the name",
      "ZedExecutor.resolveModel — provider selection uses the catalog provider when present"
    ],
    "tests/unit/zed-generation-dispatch.test.js": [
      "accounting failure at rejected headers cancels the body and prevents hidden retry",
      "cancellation during admission prevents the generation transport",
      "cancels explicit rejected 401 then admits every fresh wire attempt in order",
      "cancels explicit rejected 403 then admits every fresh wire attempt in order",
      "never replays accepted200 carrying x-zed-expired-token",
      "never replays accepted200 carrying x-zed-outdated-token",
      "preserves exposure when 200 explicitly forbids replay",
      "preserves exposure when 401 explicitly forbids replay",
      "preserves exposure when 403 explicitly forbids replay",
      "preserves exposure when 429 explicitly forbids replay",
      "preserves exposure when 503 explicitly forbids replay",
      "preserves GET catalog refresh without generation hooks",
      "real executor wires its prepared provider envelope through the actual token/retry helper",
      "refuses a second admission after auth rejection before its paid transport"
    ],
    "tests/unit/zed-quota-3406.test.js": [
      "formatZedPlanLabel labels every known plan_v3 id",
      "formatZedPlanLabel still renders an unknown id, since plan_v3 is KnownOrUnknown",
      "getZedUsage is reachable through getUsageForProvider('zed')",
      "getZedUsage refuses without a token or a user id instead of calling out",
      "parseZedUsageLimit reads both serde forms of UsageLimit",
      "parseZedUsageLimit tolerates the bare-number and missing forms",
      "parseZedUserUsage drops a limited-0 bucket rather than rendering it as unlimited",
      "parseZedUserUsage falls back to a message when a blocked account has no rows at all",
      "parseZedUserUsage keeps the quota rows when invoices are overdue, and says so on the plan label",
      "parseZedUserUsage maps a limited edit-prediction bucket to a quota row with the billing-cycle reset",
      "parseZedUserUsage marks an unlimited bucket as unlimited instead of a zero cap",
      "parseZedUserUsage produces rows the routing quota-pause snapshot can consume",
      "parseZedUserUsage reads model_requests when an older deployment returns it"
    ],
    "tests/unit/zenmux-free-body-and-branches.test.js": [
      "a message_delta chunk in stream mode carries stop_reason through as finish_reason",
      "a message_delta chunk with no stop_reason defaults finish_reason to \"stop\"",
      "a system message is prefixed onto the user question joined by a blank line",
      "a thrown error with no statusCode maps to 502 with upstream_error type",
      "addRound returning non-ok leaves roundId null, so updateRound never fires",
      "buildSSEStream emits an [Error: ...] content chunk plus [DONE] when the upstream body throws mid-read",
      "the addRound request body carries the exact literal field set",
      "throws \"ctoken not found in cookies\" and never calls fetch when cookies lack ctoken",
      "updateRound fires a third request with the exact success/finishReason fields when text is non-empty",
      "updateRound is skipped (no third fetch) when the collected text is empty",
      "validateCookies session-expired branch a thrown network error is caught and returned as the error field",
      "validateCookies session-expired branch an ok response with success:false returns \"session expired\", not a thrown error"
    ],
    "tests/unit/zenmux-free-deep-equality.test.js": [
      "_collectText buffers a data: line split across two read chunks (holds the trailing partial in buf)",
      "a line that is not data-prefixed is skipped entirely, not parsed",
      "chat request headers carry the exact literal set, nothing more or less",
      "non-stream response envelope matches the exact shape, id prefixed chatcmpl-zmf-",
      "the opening SSE chunk in stream mode sets role assistant with a null finish_reason",
      "upstream error status mapping 401 maps to statusCode 401 with the cookies-expired message",
      "upstream error status mapping 402 maps to statusCode 402 with the quota-exhausted message",
      "upstream error status mapping a generic 500 maps to statusCode 500 with an HTTP-500 message",
      "validateCookies non-ok HTTP response a non-ok status short-circuits with the exact HTTP-<status> message, not parsing a body",
      "validateCookies non-ok HTTP response no ctoken in the cookie string short-circuits before any fetch, with the \"no ctoken\" error",
      "validateCookies non-ok HTTP response valid credentials return the displayName and email from data"
    ],
    "tests/unit/zenmux-free-executor.test.js": [
      "validateCookies HTTP error and expired session both report invalid; success carries user identity",
      "validateCookies no ctoken → invalid without a network call",
      "ZenmuxFreeExecutor.execute — request construction fails closed without a ctoken: 502 error envelope, no network call",
      "ZenmuxFreeExecutor.execute — request construction sends the cookie and ctoken to zenmux.ai only, with the pinned upstream model",
      "ZenmuxFreeExecutor.execute — request construction system message is prepended to the question",
      "ZenmuxFreeExecutor.execute — stream vs non-stream branches stream:false collects text into a chat.completion JSON with a usage block",
      "ZenmuxFreeExecutor.execute — stream vs non-stream branches stream:true re-encodes Claude deltas as OpenAI chunks ending in [DONE]",
      "ZenmuxFreeExecutor.execute — upstream error mapping 401/403 → 401 cookies-expired envelope (triggers credential invalidation, not retry-forever)",
      "ZenmuxFreeExecutor.execute — upstream error mapping 402 → quota exhausted with status preserved",
      "ZenmuxFreeExecutor.execute — upstream error mapping other non-2xx → same status propagated, never a silent 200"
    ],
    "tests/unit/zenmux-free-literals.test.js": [
      "_collectText resolves to empty string when the response has no body at all",
      "a falsy apiKey (no credentials) fails closed with ctoken-not-found, never an empty-string cookie header",
      "a missing body.messages defaults to an empty list rather than throwing",
      "addRound targets the frontend/chat/addRound path exactly",
      "buildSSEStream error path emits an [Error: ...] chunk and still terminates with [DONE]",
      "chat request targets the exact anthropic messages path on zenmux.ai",
      "claudeBody carries the pinned deepseek model id and default max_tokens 4096",
      "does not send a temperature field when the request omits one",
      "User-Agent header is a real Chrome UA string, not empty",
      "validateCookies error handling a thrown network error surfaces as valid:false with the error message",
      "ZenmuxFreeExecutor.execute error envelope defaults an error with no statusCode defaults to 502, not 200 or undefined",
      "ZenmuxFreeExecutor.execute error envelope defaults passes the original request body through as transformedBody on error"
    ],
    "tests/unit/zenmux-free-mutation.test.js": [
      "addRound and chat requests carry the exact zenmux headers (Referer, Origin, UA, Content-Type)",
      "defaults the question to \"Hello\" when there is no user message",
      "ignores a JSON-malformed data line and keeps decoding the rest",
      "never appends text from an event whose type is not content_block_delta, even if delta.text is set",
      "stream mode does not enqueue an empty-content chunk for a delta with no text or thinking",
      "stream mode never treats a non-message_delta event carrying delta as the finish event",
      "updateRound is not sent when addRound never produced a roundId",
      "updateRound is not sent when the collected text is empty, even with a roundId",
      "usage estimate divides length by 4, not multiplies",
      "validateCookies: success and data are both required (AND, not OR) success:false with a data payload still reports session expired",
      "validateCookies: success and data are both required (AND, not OR) success:true with null data still reports session expired"
    ],
    "tests/unit/zenmux-free-provider.test.js": [
      "zenmux-free provider builds into the runtime PROVIDERS map",
      "zenmux-free provider dispatches to its own executor by provider id",
      "zenmux-free provider is registered as a cookie-auth free provider",
      "zenmux-free provider keeps every registry id unique after adding zenmux-free",
      "zenmux-free provider resolves its aliases through the model service, including uiAlias"
    ],
    "tests/unit/zenmux-free-remaining.test.js": [
      "_collectText picks up the thinking field when delta.text is absent",
      "a final read with no trailing newline still yields the last data line (buf held from lines.pop())",
      "addRound POST body is sent with method POST to the frontend base",
      "buildSSEStream: a message_delta event with a falsy delta emits no finish chunk",
      "chatRequestId sent to the anthropic endpoint has no dashes (uuid stripped)",
      "cid embeds a 12-char randomUUID slice and created is whole seconds, not ms",
      "claudeBody sent to the chat endpoint has exact user/text message shape",
      "non-stream usage totals prompt+completion tokens computed as ceil(length/4)",
      "r1 not ok leaves roundId null, so step 3 updateRound never fires",
      "roundId is read from the addRound response body and reused by updateRound",
      "updateRound body carries the pinned modelInfo.slug field exactly",
      "validateCookies request shape a network throw is caught and reported as the thrown error message",
      "validateCookies request shape no ctoken in cookies short-circuits before any fetch call",
      "validateCookies request shape non-ok HTTP status reports the exact status code, not \"session expired\"",
      "validateCookies request shape sends only Cookie and User-Agent headers, no Origin",
      "validateCookies request shape success:true but data missing reports session expired, not valid"
    ]
  }
}
